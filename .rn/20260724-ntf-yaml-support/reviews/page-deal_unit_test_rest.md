# レビュー記録 — 取引単体テスト（RESTfulウェブサービス）

**成果物**: `ja/development_tools/testing_framework/implementation/deal_unit_test/rest.rst`（60行。ラベル `deal_unit_test_rest`）
**キュー番号**: `#27-07`（`ntf-doc-weekend-queue.md` §3）
**個別指示**: `ntf-doc-27-small-3rd.md` §1・§2・§6

## 1. 参照リポジトリ

| リポジトリ | コミット | 用途 |
|---|---|---|
| `nablarch-document` | 作業ツリー（`df7bff7` の続き） | 成果物・規約・マッピング |
| 削除済みの旧解説書 | `2e501ad` | 出典。`git show 2e501ad:<path>` で読む |
| `nablarch-testing-rest` | `9ada31e` | コード例に現れるAPIの実在確認。`src/` は当該コミットで clean（`git status --porcelain -- src/` が0件） |
| `nablarch-example-web` | 作業ツリー | `ProjectUpdateForm` の実在確認 |

**`nablarch-testing-rest` は作業指示 §4-2 の固定リストに無い。** 固定されているのは `nablarch-testing`（`e21bf67`）と `nablarch-testing-yaml`（`190cc9a`）の2本である。本ページのコード例が使うAPIはいずれも `nablarch-testing-rest` にしか無く（`nablarch-testing@e21bf67` に `SimpleRestTestSupport` も `RestMockHttpRequest` も存在しないことを `git ls-tree -r --name-only e21bf67 | grep -i rest` で確認）、作業指示 §1-9 が許す `/home/tie303177/work/nablarch/` 配下の参照リポジトリであるため、`9ada31e` を自分で固定して使った。

## 2. 出典行の消化（全2行）

`mapping.csv` を `csv.DictReader` で読み、`dest_page == '取引単体テスト（RESTfulウェブサービス）'` を抽出した結果は2行。`DROP`・`REFERENCE` は無い。

| `mapping_id` | 出典 | disposition | `dest_section` | 反映先 |
|---|---|---|---|---|
| `current-0148` | `05_UnitTestGuide/03_DealUnitTest/rest.rst:4-8` | MERGE | 機能概要 | `:15`（出典 `:5`）・`:17`（出典 `:7`） |
| `current-0149` | 同 `:11-37` | MERGE | 使用方法 | `:28`（出典 `:12`）・`:30-52`（出典 `:14-36`） |

**落とした行は0件。** 出典 `:4-8`・`:11-37` の非空行はすべて上表で消化している。

**同じ出典ファイルの `:40-95`（`current-0150`〜`0152`）は本ページの範囲外である。** 3行とも `dest_part = 第2部 導入と設定` / `dest_page = 取引単体テストの設定（RESTfulウェブサービス）` であり、作成済みの `setup/deal_unit_test/rest.rst` が持つ。本ページはそこへ `:ref:` を張るだけにした（G12 の二重掲載を作らない）。

## 3. 実装で確認した事実

コード例に現れる識別子を1件ずつ `nablarch-testing-rest@9ada31e` で照合した。

| 識別子 | 実装 | 判定 |
|---|---|---|
| `get(String)` → `RestMockHttpRequest` | `src/main/java/nablarch/test/core/http/SimpleRestTestSupport.java:136` | 一致 |
| `put(String)` → `RestMockHttpRequest` | 同 `:156` | 一致 |
| `sendRequest(HttpRequest)` → `HttpResponse` | 同 `:186` | 一致 |
| `assertStatusCode(String, HttpResponse.Status, HttpResponse)` | 同 `:312` | 一致 |
| `RestMockHttpRequest#setBody(Object)` → `RestMockHttpRequest` | `src/main/java/nablarch/fw/web/RestMockHttpRequest.java:61` | 一致（メソッドチェーンが成立する） |
| `parseProject` / `assertProjectEquals` | `/home/tie303177/work/nablarch/` 配下の全 `.java` に0件（`grep -rn --include=*.java`、`.claude/` 配下を除く） | **フレームワークのAPIではない** |
| `ProjectUpdateForm` | `nablarch-example-web/src/main/java/com/nablarch/example/app/web/form/ProjectUpdateForm.java:17` に同名クラスが実在。ただし `Project` を引数に取るコンストラクタは無い | アプリケーション側のクラス |

**`sendRequest(HttpRequest)` は `defaultProcessor` を適用する**（`SimpleRestTestSupport.java:186-188` が `sendRequest(request, defaultProcessor)` を呼ぶ）。`defaultProcessor` はシステムリポジトリの `defaultProcessor` から取得し、未登録なら何もしない実装を使う（同 `:96-105`）。この事実が、本ページ末尾の tip から第2部へ送る根拠である。

### 確認したが本文に書かなかったこと

- **`SimpleRestTestSupport` は JUnit 4 ベースである**（`:20-21` が `org.junit.Before` / `org.junit.Rule` を import）。出典のコード例は import を示していないため、本ページでも示していない。テストクラスの作り方は `#27-12` の領分である
- **`setBody` は Content-Type 未設定なら `RestMockHttpRequestBuilder` の `defaultContentType`（`RestMockHttpRequestBuilder.java:17` = `application/json`）を立て、`JacksonBodyConverter` でJSONに変換する。** リクエストの組み立て方は `#27-12`、テストデータの記法は `テストデータの書き方` の領分であり、本ページには書いていない
- **`getHttpRequestBuilder()` は `restMockHttpRequestBuilder` が未登録なら例外を投げる**（`SimpleRestTestSupport.java:111-117`）。前提となるコンポーネント登録は第2部の領分である

## 4. 実測

| 項目 | 値 | 取得方法 |
|---|---|---|
| 行数 | 60行 | `wc -l` |
| 見出し | L1×1・L2×2・L3×1 | 下線行の機械抽出 |
| 下線 | L1 50（表示幅39）・L2 50（8）・L3 49（24） | `unicodedata.east_asian_width` で表示幅を計算 |
| `:ref:` | 3件（`request_unit_test_rest`・`testdata_notation`・`deal_unit_test_setting_rest`） | 正規表現抽出 |
| `.. code-block:: java` | 1件。ディレクティブ0字下げ・本文2字下げ | インデント計測 |
| `tip` / `important` | 1件 / 0件 | `grep` |
| `.. image::` | 0件 | `grep` |

## 5. 出典から変えた点

### D-1. アウトラインの適用 — `使用方法` 配下のL3は1つだけにした（**この項は `#27-10`・`#27-11`・`#27-15` からも参照する**）

`design.md` の第3部「ページのアウトライン」節は `使用方法` 配下に「テストクラスを作成する／テストメソッドを作成する／テストデータを作成する／テストを実行する／テスト結果を確認する」の5つを挙げる。本ページの出典は32行で、そのうち手順にあたるのはコード例1本だけであり、5つを立てると出典に無いことを書くことになる。

**`style.md` S-02 が第2部について定める「出典が無い場合は見出し自体を置かない」を、第3部にも適用した。** 第3部には同じ明文が無いため、この適用自体を `decide` に上げる（§7 の `decide-1`）。指示は `ntf-doc-27-small-3rd.md:22-28`（§1-1）にあり、**`#27-10`・`#27-11`・`#27-15` の3ページも同じ適用を行うが、記録はこの項1回で足りる**（同 `:28`）。

### D-2. 出典 `:7` の「リクエスト毎のテストを連続実行する」を「1つのテストメソッドの中でリクエストを順に送る」に改めた

出典の言い回しは、テストメソッドを複数並べて順に流す読み方を許す。しかし出典自身のコード例（`:16-36`）は1つのテストメソッドの中で3リクエストを送っており、実装上も `defaultProcessor.reset()` が各テストメソッドの開始時に呼ばれる（`SimpleRestTestSupport.java:96,103`。`@Before setUp` から）。**同じページの中で読者に2つの実装像を与えないため、コード例と実装に合わせた。** `design.md` §8 の「出典と実装が食い違えば実装を優先する」の適用である。

### D-3. 出典 `:5` に理由を足さなかった

初版は「リクエスト単体テストで取引全体を検証できるため」という理由を書いていたが、**出典 `:5` は理由を述べておらず、実装で裏の取れる種類の主張でもない。** 是正で削除した。出典どおり「1リクエストが1取引に対応する場合は、取引単体テストを実施する必要はない」までにとどめている。

### D-4. コード例の補助メソッドに注記を足した

`parseProject`・`assertProjectEquals` はフレームワークのAPIではない（§3 の実測）。出典はこの区別に触れていないため、読者はすべてが提供されていると誤解する。**`:54` に「テストクラスに用意した補助メソッドである」の1文を足した。** `design.md` §8 の「出典が書いていない前提の追記」に当たる。

### D-5. 出典 `:12` の導入文に3本目のリクエストを足した

出典の導入文は「更新対象を取得し、取得した情報から更新用のフォームを作成して更新実行、想定通りに更新されていることを検証している」で、コード例が持つ3本目のGET（再取得して照合）に触れていない。**導入文と直後のコード例を一致させるため、「更新後のプロジェクトを再度取得して」を足した。**

### D-6. コードブロックのインデントを4字から2字に直した

出典 `:16-36` は4字下げである。`style.md` S-05 は2字下げと定めており、NTF解説書の新規ページ123件のコードブロックのうち122件が2字下げである（機械集計）。**出典のインデントをそのまま持ち込まず、規約に合わせた。**

### D-7. `テストデータの書き方` への `:ref:` を張った

`design.md` の第3部アウトラインは `テストデータを作成する` を「テストデータの書き方」への `:ref:` と定めている。本ページはL3を立てないため（D-1）、`使用方法` 直下の1文（`:22`）に `:ref:` を置いた。**`#27-12` への委譲だけにすると、`テストデータの書き方` へ到達する経路がページから消える。**

### D-8. 末尾の tip は、参照先の手順を書き写さず導線だけにした

初版の tip は「テストメソッドの中で受け渡すのではなく、コンポーネント設定ファイルに引き継ぎ用の実装クラスを登録する」と書いていた。2点直した。

- **排他性を落とした。** テストメソッド単位でプロセッサを渡す `sendRequest(HttpRequest, RequestResponseProcessor)` が公開されており（`SimpleRestTestSupport.java:197`）、クッキーは `RestMockHttpRequest#setCookie`（`RestMockHttpRequest.java:131`）でも積める。出典 `:42` も「以下の方法で実現できる」であって唯一の方法とは書いていない
- **手段の説明を落とした。** 「実装クラスをコンポーネント設定ファイルに `defaultProcessor` という名前で登録する」は承認済みの `setup/deal_unit_test/rest.rst:10,19` が持つ事実である。`ntf-doc-27-small-3rd.md:36`（§1-3）に従い、要約して書き写さず `:ref:` で送った

## 6. 4観点レビュー

QA／設計／クラフト／検証の4観点をそれぞれ別のサブエージェントで実施した。指摘は QA 10件（高2・中5・低3）、設計 12件（高2・中4・低6）、クラフト 18件（高2・中8・低8）、検証は8項目すべて PASS。**是正は1ラウンドで畳み、成果物の `.rst` に含めている。**

### 是正した指摘（重い順）

1. **`使用方法` の実行単位が機能概要と食い違う**（QA高・クラフト高・設計低）→ D-2
2. **コードブロックが4字下げで `style.md` S-05 違反**（設計中・QA中）→ D-6。NTF新規ページ123件中で唯一の例外だった
3. **tip が参照先の手順を書き写し、かつ実装に無い排他性を作っている**（設計中・QA中・クラフト高）→ D-8
4. **`テストデータの書き方` への導線が無い**（設計中）→ D-7
5. **コード例の識別子がフレームワークAPIかアプリ側かを区別できない**（QA中・設計低・クラフト中）→ D-4
6. **出典に無い理由付けを書いている**（設計低・QA低）→ D-3
7. **導入文がコード例の3本目のリクエストに触れていない**（QA低・クラフト低）→ D-5
8. **委譲の1文がL3見出しの配下にあり、見出しと中身が合っていない**（設計中）→ 委譲文を `使用方法` 直下に移した
9. 文言（クラフト中・低）→ 「上の例では」→「この例では」（解説書内で「上の例」は本ページのみだった）、「次の例は」→「次の例では」、「リクエストの送信結果」→「レスポンス」、「を参照すること。」→「を参照。」（解説書内で「を参照。」64件・「を参照すること。」3件）、初版の tip の109字1文を2文に分割

### 採らなかった指摘

- **L3見出しを「1つのテストメソッドで複数のリクエストを送る」に改めよ**（クラフト中）— `ntf-doc-27-small-3rd.md:66` が `テストメソッドを作成する` と確定させており、同 `:194`（S2）が「§2〜§5 のセクション構成と一致すること」をゲートにしている。**個別指示が正である**（作業指示 §0 の「食い違ったら個別指示が正」）
- **`#27-12` を先に書くまで本ページの公開順を後ろに回せ**（クラフト中・設計高・QA高）— キューの順序は作業指示 §3 が決めている。`ntf-doc-27-small-3rd.md:71-72`（§2）が `#27-12` へ `:ref:` を張るよう明示している。§7 の `decide-2` に上げた
- **第2部 `setup/deal_unit_test/rest.rst` から本ページへの `:ref:` を足せ**（設計低）— 承認済みページの変更に当たる。§7 の `decide-3` に上げた
- **`setBody` のContent-Type自動設定とJSON変換を書け**（QA中）— リクエストの組み立て方は `#27-12` の領分である。§3「確認したが本文に書かなかったこと」に記録した
- **依存モジュール・コンポーネント登録への導線を足せ**（QA低）— `style.md` S-02 は処理方式固有の依存を「当該ページの機能概要または使用方法配下で個別に触れる」としているが、RESTfulウェブサービスの依存は第2部 `setup/request_unit_test/rest.rst:24-37` が持つ。第3部の本ページから重ねて書かない
- **`テスト結果を確認する` に相当する記述が無い**（設計低）— 出典に無い。コード例の `assertStatusCode` が実例になっている
- **依頼文の「57行」が実測56行と合わない**（3観点が指摘）— レビュー依頼文の誤りである。成果物側の欠陥ではない。是正後の実測は60行

## 7. 判断待ち（`decide`）

### decide-1. 第3部に `style.md` S-02 の「出典が無い場合は見出し自体を置かない」を適用したこと

**この判定は `#27-10`・`#27-11`・`#27-15` にも及ぶ。** S-02 は第2部についてのみ明文を持ち、第3部については `design.md` の第3部「ページのアウトライン」節が5つのL3を挙げるだけである。`ntf-doc-27-small-3rd.md:22-28` が本作業での扱いを決めているが、**規約本体（`style.md` S-02）に第3部の但し書きを足すかどうかは未決である。** 作業指示 §1-3 により規約は書き換えていない。

### decide-2. `:24` の `:ref:` の飛び先が現時点で4行のスタブである

`implementation/request_unit_test/rest.rst` は `#27-00` で作ったラベル＋タイトルのみの4行である（`wc -l` で実測）。ラベルは実在するのでビルドは警告を出さないが、**「テストクラスの作成方法とテストの実行方法は…と同じである」を追った読者は、`#27-12` を書き終えるまで空ページに着く。** `ntf-doc-27-small-3rd.md:44`（§1-5）は「キューはこの4ページを、それぞれの参照先より後に置いてある」と述べるが、**`#27-07`（8番目）→ `#27-12`（13番目）はこの前提を満たしていない**（`steering.md:590,595` で実測）。`#27-12` の完了時に本ページを読み直す必要はないが、週明けの一括検証では順序を確認されたい。

### decide-3. 第2部 `setup/deal_unit_test/rest.rst` から本ページへの `:ref:` が無い

本ページは末尾の tip から第2部へ送っているが、逆向きの導線が無い（`grep -rn 'deal_unit_test_rest' ja/ --include=*.rst` の結果は定義行1件のみ）。**第2部のページは承認済みであり、本タスクでは触れていない。** 足すかどうかは user 判断に回す。
