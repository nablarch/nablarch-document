# レビュー記録: テストデータの記載例（`implementation/testdata_examples.rst`）

対象タスク: `#10`
成果物: `ja/development_tools/testing_framework/implementation/testdata_examples.rst`（L2 9 / L3 28 / L4 56、1,883行）

## ラウンド1: 4観点レビュー（2026-08-07）

`steering.md` Rules に従い、A（網羅性）/ B（トンマナ）/ C（用語）/ D（整合性）を**それぞれ別のサブエージェント**で実施した。各観点には成果物・目的・完了条件・チェックリストのみを渡し、self-check ファイルや他観点の判定は渡していない。全プロンプトに Rules の3点（実測で裏付ける / 付属の検証スクリプトを使わず独立に組む / 敵対的にレビューする）を含めた。

### 観点A（網羅性）

| # | 指摘 | 対応 |
|---|---|---|
| A-must-1 | 複数レコードレイアウトの例がレコード長不一致（HEADER 6バイト / DATA 40バイト）。写すと実行時エラー | 対応。HEADER に `FILLER`（34バイト）を追加して40バイトに揃え、レコード長一致の `important` を追加 |
| A-must-2 | リクエストパラメータの例の ID が `searchParams` で、予約 ID `requestParams` でないため読み込まれない | 対応。`requestParams` に変更し、予約 ID である旨を導入文に明記 |
| A-should-3 | 姉妹ページとの記述重複7箇所 | 対応。重複していた `important`/`tip` 5件をポインタ（`:ref:`）化、地の文2件を圧縮 |
| A-should-4 | 同期応答系の Excel/YAML でレコード種別が実際には食い違うのに断りがない | 対応。L2「メッセージングのデータを記述する」導入文に形式差の明示とポインタを追加 |
| A-should-5 | 「複数件処理」と説明しているのにデータが1件 | 対応。`case2` に2件目（`20002 / 40 / evening`）を追加 |
| A-should-6 | コメント行の例に肝心の `//` 行が入っていない | 対応。表に `// この行もスキップされます` の行を戻し、行内コメントの説明を追加（判断1参照） |
| A-should-7 | `testdata_notation.rst:1241` の YAML 例に旧識別子 `requestMessages` が残存 | 対応（姉妹ページ側の修正。判断2参照） |
| A-note-8〜12 | 出典との差異はすべて姉妹ページで裏付けあり／Excel↔YAML 値突合は機械検査で不一致0件／`searchParams` 抜粋・空ファイル表・`context002` 未定義 | `context002` を追加、抜粋にコメント付与。空ファイルの Excel 例は他節と同じくディレクティブを地の文に置く方針を維持（判断3参照） |

65行すべての反映先が特定され、単純な落丁0件・創作0件であることを確認した。

### 観点B（トンマナ）

| # | 指摘 | 対応 |
|---|---|---|
| B-must-1 | 同じ失敗モードを同一ページ内で `tip` と `important` に書き分けている（S-06） | 対応。グループID交互記述の `tip` を `important` に変更し、検出されないままテストが成功する旨を追記 |
| B-should-1 | 形式非依存の注意事項6件が Excel/YAML 専用 L4 の内側にある（S-10 規約1・3） | 対応。6件すべてを当該 L3 の共通部（最初の L4 より前）へ移動。移動後の機械走査で L4 配下に残る admonition は1件のみで、これは Excel 固有の内容（シート内の記述順）であり正しい配置 |
| B-should-2 | L3 導入文28件中27件が S-11 の「配下の L4 を予告する」要件を満たしていない | 対応。全 L3 の導入文に形式別 L4 の予告を追加。機械走査で未充足0件を確認 |
| B-should-3 | S-07 のページ単位例外の発動条件（コードリテラル混在表が大半）を本ページは満たさない | 対応。`style.md` S-07 に「Excelのセル内容を格子状に示す表を多数持つページ」を発動条件として追記（判断4参照） |
| B-should-4 | ファイル節だけが Excel↔YAML 対応表を持つ（S-10 規約1） | 未対応。粒度・行構造の対応は S-10 規約1 の「両形式の対応関係そのものを理解しないと概念が掴めないもの」に該当すると判断し維持した（判断5参照） |
| B-note-1 | L2 導入文の定型句が4種類に揺れ、「シナリオ」は `ja/` 配下で本ファイルにしか出現しない | 対応。「用途ごとに示す」に統一（testShots のみ「処理方式ごとに示す」）。「シナリオ」は0件になった |
| B-note-5 | S-03 例外1 の「全L3に置くものではない」と実態（28/28）のずれ | 対応。`style.md` S-03 例外1 に記載例ページの但し書きを追記（判断4参照） |
| B-note-2/3/4 | サンプルデータの `description` 文体混在／`:ref:` 前後の書式／アンダーライン長の1文字差 | 未対応（記録のみ）。いずれも規約対象外で表示にも影響しない |

RST記法・見出し階層・ラベル・目次・文体・段落内改行はすべて適合（違反0件）。

### 観点C（用語）

| # | 指摘 | 対応 |
|---|---|---|
| C-must-1 | `HTTP同期応答` が正表記 `HTTPメッセージ送信` に揃っていない | 対応 |
| C-should-1 | 用語集 §5.10 の `レコード種別行` が使われず独自複合語（`レコード種別＋フィールド名称行`）になっている | 対応。正表記 `レコード種別行` に統一し、L2 導入文に「先頭要素がレコード種別、以降がフィールド名称」を1文追加。`glossary.md` §10 未解決事項3（本ページでの再確認）はこれで解消 |
| C-should-2 | ラベル列の位置を指す語が `先頭列`/`先頭セル`/`先頭要素` に分散 | 対応。姉妹ページで定義を持つ `先頭要素` に統一 |
| C-should-3 | 同一 L3 内で `応答電文本文` と `メッセージボディ` が同じ対象を指す | 対応 |
| C-should-4 | 素の `フレームワーク` を主語に使っている | 対応。`テスティングフレームワーク` に変更 |
| C-should-5 | `処理方式` に `エンティティバリデーション` を含めている（用語集 §5.2 は7名称に限定、§5.8 は別軸と明記） | 部分対応。本ページの「他の処理方式と別体系」を「他のカラム体系とは別の体系」に変更。ただし `処理方式ごとに示す` という枠組み自体は姉妹ページと共通の用法であり、片側だけ直すと不整合になるため維持した（申し送り、判断6参照） |
| C-note-1〜7 | 識別子行の書式は本ページが規約準拠で姉妹ページ側が逸脱／`NULL`・`null` の大小混在／細かい言い回しの差 | 記録のみ |

`glossary.md` §5（正表記103・揺れ155）・§8（91対応行）の全件走査で、上記以外の揺れは0件だった。

### 観点D（整合性）

実装（`nablarch-testing` / `nablarch-testing-yaml`）と JSON Schema に照らした検証。YAML 28ブロックはパース・スキーマ検証とも違反0件。

| # | 指摘 | 対応 |
|---|---|---|
| D-must-1 | 複数レコードレイアウトのレコード長不一致（A-must-1 と同一。`FixedLengthFile#getRecordLength` が `IllegalStateException`） | 対応（A-must-1 と同じ） |
| D-must-2 | `MESSAGE=sendSyncTestData/{リクエストID}/message` という識別子は実装に存在しない。`sendSyncTestData` は `FilePathSetting` のベースパスキーで、読み込み単位は `{リクエストID}/message`、識別子は `RESPONSE_*_MESSAGES=リクエストID` 等 | 対応。当該 L3 を「同期応答メッセージ送信の応答電文を配置する」として全面的に書き直した。姉妹ページ `testdata_notation.rst:1113` の同じ誤りも是正（判断2参照） |
| D-must-3 | `EXPECTED_COMPLETE_TABLE` の「値だけ空欄＝デフォルト値補完」は誤り。補完対象はカラム名自体を書かなかったカラム（`TableData#fillDefaultValues` が `allColumns - columnNames` を対象にする） | 対応。`UPDATE_DATE` をカラム名の行から外した例に差し替え、「書かない」の意味を導入文で明示 |
| D-must-4 | `quoting-delimiter` の YAML 例が Excel 例と等価にならない。YAML 経路には `QuotationTrimmer` が入らないため、`"\"hello, world\""` は引用符付きのまま残る | 対応。`"hello, world"` に修正し、形式差を説明する文に書き換え |
| D-should-1 | `messages:` に「連番が `rows:` の記述順から自動付与される」は成立しない（連番付与は `response_*`/`expected_request_*` 経路のみ） | 対応（D-must-2 の書き直しに含む） |
| D-should-2 | `errorMode:` の tip が誤ったデータブロックの節にある。効くのは `RESPONSE_*` のみで、ヘッダ・本文の両方に記載が必要 | 対応。`important` として応答電文の節の共通部へ移動し、両方に記述する旨を追記 |
| D-should-3 | コメント行の説明と表が一致していない（A-should-6 と同一） | 対応（A-should-6 と同じ） |
| D-should-4 | `HTTP_METHOD` は `context` の `LIST_MAP` から取得される（`TestCaseInfo#getHttpMethod`）。姉妹ページは testShots のカラムとして列挙しており実装と食い違う | 対応。本ページは修正不要。姉妹ページ側を是正（判断2参照） |
| D-note-1/2 | JSON Schema の `field-separator` 説明と `messages` の `id` 説明が実装と食い違う（本ページの記述が正しい） | 未対応。`nablarch-testing-yaml` 側の課題として申し送り |
| D-note-4 | Excel/YAML の実装差（`MockMessages` 生成・連番・`errorMode` 行の切り捨て）は本ページ単独では解消できない | 記録のみ。「同じ内容を YAML 形式で記述すると」という書き方をこの領域に広げない方針とした |

## 判断・申し送り

1. **コメント行を表内に残した**。`style.md` S-10 規約2 は「1セルしか値がない行を表の中に残さない」と定めるが、これは識別子行を対象とした規定である。コメント行は Excel シートの格子そのものを示す例の一部であり、位置（カラム名の行の前・データ行の間）が説明対象であるため表内に置いた。**user review で判断を仰ぐ**。
2. **承認済みの `#9`（`testdata_notation.rst`）に3件の是正を入れた**。いずれも実装で裏付けた事実誤りであり、放置すると本ページと矛盾する。(a) `sendSyncTestData` をデータブロックの識別子と説明していた記述（`SendSyncSupport.java:46,49,346-348`、`DataType.MESSAGE` はこの経路に渡らない）、(b) YAML 例の識別子が旧名 `requestMessages` のまま（同ページ本文は「固定である」と記述）、(c) `HTTP_METHOD` を testShots のカラムとして列挙（`TestCaseInfo.java:28,40,306-308`）。**user review で判断を仰ぐ**。
3. 空ファイルの Excel 例は表を置かず地の文に留めた。ディレクティブ行は本ページ全体で地の文に書く方針で統一しているため。
4. `style.md` に2箇所追記した。S-07 のページ単位例外に「Excelのセル内容を格子状に示す表を多数持つページ」を発動条件として追加、S-03 例外1 に「記載例ページでは全L3に形式別見出し対を置く」を追加。いずれも本ページの構成を規約側で追認するもの。
5. ファイル節の Excel↔YAML 対応表は維持した。S-10 規約1 の第1類型（両形式の対応関係そのものを理解しないと概念が掴めないもの）に該当すると判断した。他の8つの L2 が同種の表を持たないのは、行構造の対応が問題になるのがファイルデータだけであるため。
6. `処理方式` に `エンティティバリデーション` を含める用法は、姉妹ページと共通の逸脱として残した（用語集 §5.2 と §5.8 のどちらを改めるかは用語集オーナーの判断事項）。

## `#11` 以降への申し送り

1. `testdata_notation.rst` の識別子行11箇所がコード書式（`` `` ``）で、`style.md` S-10 規約4（普通の文字＋「」）に反している。同ページの後続レビュー時に是正する
2. `nablarch-testing-yaml` の `ntf-testdata-yaml-schema.json` の説明文2箇所が実装と食い違う（`field-separator` のタブ記法、`messages` の `id`）。フレームワーク側の課題として起票が必要
3. Excel 経路と YAML 経路には実装レベルの非対称がある（`MockMessages` の生成有無、電文連番の付与、`errorMode` 行の他フィールド切り捨て、`QuotationTrimmer` の有無）。メッセージング系の後続ページで「同じ結果になる」と書かない
4. `glossary.md` §10 未解決事項3（`レコード種別行` の根拠）は本ページで実体化したため解消済み。用語集側の記載更新は未実施
5. `NULL` / `null` の大文字小文字の使い分けが `style.md` に規定されていない（`glossary.md` §6 の申し送りと同種）

## `#10` 差し戻し対応（2026-08-07）— セル格子への識別子行追加

user review で `must` 1件（Excel のセル格子を表す表から識別子行が抜けており、表をそのまま
写しても動くテストデータにならない）を受け、`style.md` S-10 規約2 を差し替えたうえで
本ページ47件・`testdata_notation.rst` 2件を是正した。判断を仰いだ2件（上記「判断・申し送り」
1・2）はいずれも**承認**された。作業指示は `.rn/20260724-ntf-yaml-support/ntf-doc-10-cellgrid.md`、
検証記録は `checks/task-10-cellgrid.md`。

### `#11` 以降への申し送り（追加）

6. **Excel のセル格子を表す表を作る場合は、識別子行・ディレクティブ行などシート上に実在する行を
   すべて表に含め、`:header-rows: 0` とする**（`style.md` S-10 規約2）。2列目以降は空セルにする。
   セル結合は使わない。表の直前の地の文には識別子の「意味」だけを書き、「どの行に続けて記述するか」
   という並び順の説明は書かない（表が示すため）
7. **特に「リクエスト単体テスト」系のページと「エンティティ単体テスト」は `testShots` のセル格子を
   載せる可能性が高い。** カラム数が多く前半・後半に分割する場合は、**分割したすべての表に識別子行を
   付ける**（分割はキー列を繰り返す形になるため、各表の1列目がシート上の識別子の位置に当たる）
8. 上記6に伴い、申し送り1（`testdata_notation.rst` の識別子行11箇所がコード書式）の対象範囲が
   変わった。同ページで是正した2件（L437・L977 付近）は表のセルに移したため、規約4 に従い囲みなしの
   普通の文字にしてある。残りは地の文に残る識別子であり、従来どおり「」で囲む形が正である
9. `testdata_notation.rst` に、新しい規約2 の定義ではセル格子に当たるが今回の作業指示の対象外と
   された表が3件ある（L530・L1453・L1484）。いずれも識別子行を持たない断片であり、是正するかは
   未決。詳細は `checks/task-10-cellgrid.md` §6-2
10. ダブルクォートを含むセルは、Sphinx の smartquotes が既定で有効なため HTML 上で曲がった
    クォートとして描画される（`checks/task-10-cellgrid.md` §6-7）。「そのまま写せる」ことを
    目的とする表で直線のダブルクォートが必要な場合、ページ全体の方針判断が要る
    → **決着済み。** 下記「差し戻し対応（2回目）」のとおり `must` として是正した。方針は
    申し送り11 に置き換わる

## `#10` 差し戻し対応（2回目、2026-08-07）— セル値の描画忠実性と規約のスコープ

1回目（識別子行の追加）に対する user review で、判断を仰いだ3件に結論が示された。作業指示は
`.rn/20260724-ntf-yaml-support/ntf-doc-10-quotes.md`、検証記録は `checks/task-10-quotes.md`。

| 仰いだ事項 | 結論 | 対応 |
|---|---|---|
| `#9` の対象外セル格子3件（L530・L1453・L1484） | 現状維持で承認 | 変更せず。`style.md` S-10 規約2 にスコープ条件を明記（STEP 3） |
| 規約4 の参照整合 | 承認 | 取り消していない |
| smartquotes によるダブルクォートの描画 | **対象外ではなく `must`** | `testdata_examples.rst` の4セルをリテラル記法で囲んだ（STEP 1） |

対応の要点は3点である。

1. **STEP 1** — 素のダブルクォートを含む4セル（`quoting-delimiter` の設定値、`"hello, world"`、
   `" "`、`"""`）をリテラル記法で囲んだ。値そのものは変えていない。あわせて両ページの全
   `list-table` のセルを機械走査し、他に該当が無いことを確認した（走査スクリプトの妥当性は
   是正前の版で4件を検出することで裏付けた）
2. **STEP 2** — 識別子行の追加で行番号がずれていないかを、位置を指す表現30件について全件判定した。
   是正1件（`testdata_notation.rst` L1451「2行目でテーブルの論理名を」→「ヘッダ行の下の行で」）。
   残り29件は現状維持が正しい。`ヘッダ行（2行目）`・`データ行（3行目以降）`・`1行目に LIST_MAP=`
   は、シート上では識別子行が1行目であるため正しい
3. **STEP 3** — `style.md` S-10 規約2 に、複数のデータタイプに共通する規則を示す断片は識別子行を
   持たなくてよい旨を追記した。観点は11個で不変

**smartquotes の実証** — 「設定が無いから既定で有効なはず」で済ませず、同じビルド成果物で
`tag.rst:81` の素の `"` が `&#8221;` に変換されていることを確認した。是正後の4セルは `&quot;`
（直線）で出力されており、ページ全体の丸クォート出現数は0である。

### `#11` 以降への申し送り（追加）

11. **セル格子に素のダブルクォート・シングルクォートを含む値を書く場合は、リテラル記法
    （`` `` ``）で囲む。** `ja/conf.py` に `smartquotes` の設定が無く、docutils の
    smart_quotes 機構が既定で働くため、囲まないと丸クォート（`”`）として描画され、
    「表をそのまま写せば動く」という約束が破れる。申し送り10 はこれに置き換わる
12. **複数のデータタイプに共通する規則を示すための断片は、識別子行を持たなくてよい**
    （`style.md` S-10 規約2）。識別子を1つ選ぶと、その規則が特定のデータタイプ専用である
    かのように読めてしまうためである。特定のデータブロックの記述例を示す表には識別子行を含める。
    これにより申し送り9（`testdata_notation.rst` の3件）は決着済みとなった
13. **セル格子に識別子行を足したページでは、行番号を指す地の文を全数見直す。** 判定の観点は
    「その表現が指しているのは Excel シートの行か、印刷された表の行か」である。シートの行を
    指すなら識別子行が1行目であり、表の行を指すなら1つずれる。紛らわしい箇所は行番号を
    使わない表現に改める（`checks/task-10-quotes.md` §2 に判定表の型がある）
