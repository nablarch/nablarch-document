# レビュー記録: `about/index.rst`（第1部「テスティングフレームワークとは」）

対象コミット: `aa36feb`（初版）→ `31b1f1d`（fix round 1）→ `ef44dfc`（fix round 2〜3、`#8`初回レビュー完了時点）
→ `#8`差し戻し対応（`ntf-doc-08-rework.md`、本ラウンド）。

4観点（A:網羅性 / B:トンマナ / C:用語 / D:整合性）を、それぞれ独立サブエージェントで実施。各ラウンドとも
Rules「レビューを依頼するサブエージェント…」の3点（実測コマンドで裏付け／検証スクリプトを正解として使わず独立に組む／
敵対的にレビュー）をプロンプトに含めた。

**区分列について**: `#8`差し戻し対応（`design.md`§11.7改訂）で導入した重大度区分（must/decide/note）を、
初回レビュー（ラウンド1〜3）分についても遡って付し直した（`ntf-doc-08-rework.md` B-4）。区分は指摘の性質に
基づくものであり、当時「不要」と判定された指摘も、性質上は must 相当であれば must と表記している
（対応要否列で実際の要否を区別する）。

## ラウンド1（対象: `aa36feb`）

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R1-A1 | 1 | A | must | `current-0165`の構成図（`abstract_structure.png`）が欠落。表のみ残り、構成物間の関係（使用する/読み取る/テストする）を示す情報が失われている | 要 | | `31b1f1d`で`.. image::`を追加し原資産を参照 |
| R1-A2 | 1 | A | must | `input-0116`のデータブロック定義（種別＋識別子の値、14種）が`input-0002`のデータタイプ/グループIDの説明に置き換わっており、原文の定義が失われている | 要 | | `31b1f1d`で両方の事実を区別して記載するよう修正 |
| R1-A3 | 1 | A | must | `current-0180`の末尾要約文「以上の設定により…」が欠落 | 不要 | 稼動環境冒頭に同旨の記述が既にあり、情報の欠落ではない | — |
| R1-B1 | 1 | B | must | 出典文面の逐語的流用が4箇所（テストデータ外部化箇条書き／テストの独立性／マスタデータ投入tip／マルチスレッドimportant） | 要 | | `31b1f1d`で書き直し（ただしR3-B1〜B3で再指摘、後述） |
| R1-B2 | 1 | B (S-07) | must | アーキテクチャ表がlist-tableだが、短文・`:ref:`なしのセルでありsimple table基準に該当 | 要 | | `31b1f1d`でsimple tableに変換 |
| R1-B3 | 1 | B/D (S-08) | must | 「稼動環境を参照」が`:ref:`化されておらず、対象ラベルも未定義 | 要 | | `31b1f1d`でラベル追加・`:ref:`化 |
| R1-B4 | 1 | B (S-06) | note | 取引単体テストの`important`がテーブル直上の内容と重複し、必須警告の基準に弱い | 不要 | design.md L49が取引単体テストの非自動性を明示するよう要求しており、importantによる強調は妥当と判断 | — |
| R1-C1 | 1 | C | must | 処理方式ごとの正式名称テーブルが旧細分類名（メッセージ受信/同期応答メッセージ送信/HTTPメッセージ送信）を使用し、design.md§6の正式名称（HTTPメッセージング/MOMによるメッセージング/テーブルをキューとして使ったメッセージング）に未対応。後者2件が欠落 | 要 | | `31b1f1d`で名称をdesign.md§6に統一（内容欄はR2-A3・R3-C1で継続修正） |
| R1-D1 | 1 | D | must | R1-B3と同一事象（`:ref:`化） | 要 | | R1-B3で対応済み |
| R1-D2 | 1 | D | decide | マスタデータ投入ツール/マスタデータ復旧機能への言及がリンクなしの地の文 | 不要（現時点） | 参照先ページ（`setup/master_data_restore.rst`、`tools/master_data_tool.rst`）が未作成のため`:ref:`化不可。該当ページ作成時に`:ref:`化する申し送り事項として記録 | 該当ページ作成タスクへの申し送り（本ファイル末尾参照） |

## ラウンド2（対象: `31b1f1d`）

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R2-A1 | 2 | A | must | `current-0175`のメリット3件（プロジェクト全体再利用/メンテナンス容易化/実行速度向上）とタイミング文（1回実行 or 準備済み前提）が範囲内なのに欠落 | 要 | | `ef44dfc`で復元（内容面はR3で解消、文体面はR3-B1で再指摘） |
| R2-A2 | 2 | A | must | `input-0002`の「コメント」が特殊記法・マーカーカラムの列挙から欠落 | 要 | | `ef44dfc`で復元 |
| R2-A3 | 2 | A | must | `input-0028`由来テーブルの「同期応答メッセージ送信処理」の事実が消失。かつMOM/テーブルをキュー行の新規内容欄がどの資料にも追跡できない | 要 | | `ef44dfc`でMOM行に両事実を統合、テーブルをキュー行はdesign.md§6の一次記述に根拠付け（用語面はR3-C1で再指摘。このテーブルをキュー行自体が`#8`差し戻し対応A-3で「マッピングにない内容」と判定され削除された。後述） |
| R2-B1 | 2 | B | must | ラウンド1の書き直しが不十分（複数箇所が同義語置換に留まる） | 要 | | `ef44dfc`で追加書き直し（一部箇所はR3-B1〜B3で再指摘） |
| R2-B2 | 2 | B (S-07) | must | データブロックの考え方の用途表もsimple table基準に該当（アーキテクチャ表と同条件） | 要 | | `ef44dfc`でsimple tableに変換 |

## ラウンド3（対象: `ef44dfc`、`#8`初回レビュー最終ラウンド）

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R3-A / R3-D | 3 | A, D | — | 指摘なし（両観点ともPASS） | — | — | — |
| R3-B1 | 3 | B | note | `about/index.rst:133-135`（テストの独立性マスタデータ段落）が`current-0175:597-608`のほぼ逐語的な言い換えに留まる（活用形の違いのみ） | **取り下げ** | `#8`差し戻し対応（ユーザー実物比較）で過剰指摘と判定。B-1新基準の再レビューでも「指摘不要」と再確認済み（後述ラウンドB-R1参照） | — |
| R3-B2 | 3 | B | note | `about/index.rst:120-121`の「テストソースコードの重複を大幅に削減できる」が`current-0176:621`と一致（節の順序のみ入替） | **取り下げ** | `#8`差し戻し対応（ユーザー実物比較）で「一致するのは短い技術的言明1句のみ、言い換えは意味を損なう」として過剰指摘と判定。**禁止事項によりこの箇所は本ラウンドで修正しない**（後述「B-1基準の再適用と実測結果の齟齬」参照） | — |
| R3-B3 | 3 | B | note | `about/index.rst:157-158`（グループID文）が`input-0002:31-32`の骨格をほぼそのまま踏襲 | **取り下げ** | `#8`差し戻し対応（ユーザー実物比較）で「列挙順序は出典のモデルそのもの」として過剰指摘と判定。**禁止事項によりこの箇所は本ラウンドで修正しない** | — |
| R3-B4 | 3 | B (S-07) | must | 処理方式ごとの正式名称テーブル（list-table、8行2列、`:ref:`なし・複数行なし）が、同一ページで既にsimple table化した他2表と基準上一貫しない | 要 | | `#8`差し戻し対応A-4でsimple table化（後述） |
| R3-B5 | 3 | B (S-06) | note | R1-B4と同一事象の再指摘（低確信度） | 不要 | R1-B4で判断済み、再オープンしない | — |
| R3-C1 | 3 | C | must | `about/index.rst:101`のMOMによるメッセージング行内容「同期応答電文」が未定義語であり、`glossary.md:156`が揺れ表記と定める旧行名「同期応答メッセージ送信処理」の言い回しが残存している。`glossary.md`§5.11の要求電文/応答電文、§5.4の`同期応答メッセージ送信`を使うべき | 要 | | `#8`差し戻し対応A-2で`同期応答メッセージ送信`に修正（後述） |

**旧「未解決指摘のまとめ」（5件、ユーザー判断待ちとして記録されていたもの）は、`#8`差し戻し対応（下記）で全件処理済み。**
内訳: A-2/A-4に該当する2件（R3-B4, R3-C1）は修正。R3-B1/B2/B3の3件はユーザーが実物比較のうえ過剰指摘と判定し
取り下げ（`ntf-doc-08-rework.md` A-5）。

---

# `#8` 差し戻し対応（`ntf-doc-08-rework.md`、対象コミット: `ef44dfc` → 本ラウンド）

## 対応の全体像

1. `design.md`§11のレビュー観点そのものを是正（B-1〜B-4、コミット`5f8c97b`）。詳細は`design.md`§11.6/§11.7参照
2. A-1（見出し・導入文の食い違い）・A-2（`同期応答電文`の用語違反）を修正
3. A-3（`テーブルをキューとして使ったメッセージング`行の要否）を判断
4. A-4（2列表のsimple table化）をA-1に合わせて実施
5. A-5（R3-B1/B2/B3）は**修正しない**（ユーザー判定を尊重、禁止事項）
6. 是正後の観点で`about/index.rst`を再レビュー（**ラウンドを1から数え直す**。観点自体を全面的に改訂したため、
   旧ラウンド1〜3の指摘履歴の消化状況は上限カウントに算入しない）

## A-1: 見出し・導入文の修正

`処理方式ごとの正式名称`（見出し）+「リクエスト単体テストは、対象とする処理方式によって、以下のとおり呼び分ける。」
（導入文）が、表の中身（クラス単体テスト・取引単体テストを含む、処理方式でもリクエスト単体テストでもない2行を含む）
と食い違っていた。旧4観点レビュー（A〜D、3ラウンド）はこれを検出できなかった（観点DにチェックA-1相当の項目が
無かったため。`design.md`§11.6の観点Dチェック項目に追加済み）。

見出しを`テスト種別の正式名称`に変更（出典`input-0028`の見出し「テスト種別の正式名称」に整合。`design.md`§6
「処理方式の名称」とは別物であることが名前から分かる）。導入文を「テスティングフレームワークが提供する各テスト種別の
正式名称は、以下のとおりである。このうちリクエスト単体テストは、対象とする主な処理方式によって呼び分ける。」に変更
（「主な」を付し、非網羅であることを明示。理由はA-3参照）。

## A-2: `同期応答電文`の用語修正

`about/index.rst:101`（旧行）の「MOMによる要求電文の受信、または同期応答電文の送信を1リクエスト単位で行うテスト」の
うち`同期応答電文`（`glossary.md`に定義がない造語）を、`glossary.md:156`の正表記`同期応答メッセージ送信`に置換。
「MOMによる要求電文の受信、または同期応答メッセージ送信を1リクエスト単位で行うテスト」に修正。
`grep -c "同期応答電文" about/index.rst` → `0`（ゲート達成）。

## A-3: `テーブルをキューとして使ったメッセージング`行の判断 — **削る**

**判断: 削除する。**

根拠:
- `mapping.csv`の`input-0028`行の`note`は出典表の内容を「クラス単体テスト・リクエスト単体テスト（ウェブアプリケーション/
  RESTfulウェブサービス/バッチ処理/メッセージ受信処理/同期応答メッセージ送信処理/HTTP同期応答メッセージ送信処理の
  **6区分**）・取引単体テスト」と明示しており、テーブルをキューとして使ったメッセージングはこの6区分に含まれない
- 出典`input-0028`（`ntf-doc-terms.md:415-426`）を実測しても8行中にこの処理方式への言及はない
- 旧`ef44dfc`版でこの行を追加した根拠は`design.md`§6（章立て方針を定める設計文書）であり、`mapping.csv`の当該行
  そのものではない。`design.md`11.3「マッピングにない記述を追加しない」に照らすと、この行はマッピングに出典を
  持たない追加内容だった
- 削除後、観点A（網羅性）の独立サブエージェントに「削除によってmapping.csvの記述が失われたか」を再検証させ、
  「網羅性の問題ではない。削除は正当」の判定を得た（本ラウンドの再レビュー記録、後述）

**このため導入文に「主な」を付し、処理方式一覧が非網羅であることを明示した（A-1参照）。**

なお、この行を「対象範囲」節（`design.md`§2が「対応する処理方式の一覧」を要求）へ移して復元することも再レビュー中に
検討したが、この節にも同内容をカバーする`mapping.csv`行が存在しない（`current-0377`の`note`は「対応しないもの」
2件のみを対象と明記）ため、A-3と同じ理由で見送った。この論点は「観点Dの新規発見」として下記に別途記録し、
`decide`としてユーザー判断に上げる。

### A-3 追記（`ntf-doc-08-decide.md`によるユーザー判断で判断を覆した）

**上記の削除判断は誤りだった。ユーザーの`/rn:gm`（`ntf-doc-08-decide.md`）により覆し、当該行を復元した。**

誤りの原因は、この表の役割を「`mapping.csv`の出典の転記」と捉えたことにある。実際には**本文書（`about/index.rst`
テスト種別の正式名称節）が使うテスト種別の正式名称の索引**であり、`mapping/vocabulary.md:74`に
`リクエスト単体テスト（テーブルをキューとして使ったメッセージング）`が第3部の生きたページ名として存在する以上、
索引にその名前が無いのは不整合である。出典`input-0028`（`ntf-doc-terms.md:415-426`）が6区分時代の資料で
この処理方式を含まないことは、`#6`で確定した7処理方式（`design.md`§6）より古い資料であることを示すに過ぎず、
索引から行を落とす根拠にはならない。

この教訓を踏まえ、`design.md`§8に「出典と確定設計が食い違う場合は確定設計を優先する」旨の規約を追加し、
§11.7の`must`定義にも`vocabulary.md`のページ名・`design.md`の確定内容との不一致を追加した（`#9`以降の
同種の誤判断を防ぐため。詳細は`design.md`参照）。

対応: `about/index.rst`の「テスト種別の正式名称」表に、`リクエスト単体テスト（MOMによるメッセージング）`行と
`取引単体テスト`行の間へ`リクエスト単体テスト（テーブルをキューとして使ったメッセージング）`行を復元した
（`a583f69`で削除する前の文面をそのまま復元。内容は`design.md`§6の一次記述に基づく）。あわせて、この表への
`:ref:`ラベル（`testing_framework_about-test_type_names`）を新設し、「対象範囲」節から導線1文で参照させることで、
`design.md`§2が求めていた「対応する処理方式の一覧」の責務をこの表1箇所に統合した（`design.md`§2も
対象範囲・テストの種類の記載内容を改訂し、この責務分担を明文化）。

## A-4: 2列表のsimple table化

A-1の見出し変更に合わせ、`テスト種別の正式名称`表をlist-tableからsimple tableに変換（`style.md` S-07、
2〜3列の短文表はsimple table）。`docutils.utils.column_width()`で列幅を計算し罫線を生成（`len()`は使わず、
東アジア文字を幅2として計算）。`python3`でdocutilsパーサにより直接パースし malformed table エラーが
無いことを確認済み（後述のビルド確認も参照）。

## A-5: 修正しない（禁止事項の遵守）

R3-B1/B2/B3の3件は着手しない。詳細は下記「観点B再レビュー」の記録を参照。

## 是正後の観点での再レビュー（ラウンドを1から数え直す）

`design.md`§11.6/§11.7改訂後の基準で、A-1〜A-4適用後の`about/index.rst`を再度4観点（独立サブエージェント）で
レビューした。

### ラウンド1

| 指摘ID | 観点 | 区分 | 指摘内容 | 対応要否 | 対応内容 |
|---|---|---|---|---|---|
| A-R1-N1 | A | note | `input-0116`の用途表内容が抽象化され、処理方式別の具体例（ウェブ/バッチのフィールド例）が反映されていない | 不要 | 第1部は概要ページであり、design.md#2のテストデータ節定義は処理方式別詳細を求めない。他ページで扱うべき内容と判断 |
| B-R1-1 | B | must | 「テストデータファイルへの外部化」冒頭2文が出典（`01_Abstract.rst`）とほぼ完全一致（新規発見、旧3ラウンドで未指摘） | 要 | 文構造を全面変更して修正 |
| B-R1-2 | B | note→**取り下げ** | 「テストソースコードの重複を大幅に削減できる」= R3-B2相当。新基準を機械適用すると21字で閾値超過とされmust判定されたが、**A-5禁止事項によりこの箇所は修正しない** | 不要 | `design.md`§11.6に「ユーザーの個別判断は本基準より優先する」を追記し、字数閾値はあくまで目安である旨を明記（後述） |
| B-R1-3 | B | must | 「対象範囲」節の2つの`important`が出典（`testing_framework/index.rst`旧版）とほぼ完全一致（新規発見） | 要 | 両方とも文構造・語順を変更して修正 |
| B-R1-4 | B | note→一部must | 「データブロックの考え方」の2文。(a)「データタイプは…グループID…」文＝R3-B3相当（**A-5禁止事項により修正しない**）、(b)「個々のデータブロックを区別する単位は…組み合わせ」文＝新規発見（input-0116由来、旧3ラウンド未指摘）は独立にmust | (a)不要 (b)要 | (a)不変更 (b)文構造を変更して修正 |
| B-R1-5 | B | note | アーキテクチャ節の構成物表がほぼ丸ごと出典のgrid tableと一致（表単位のため三基準のいずれにも厳密には該当しないと判断） | 不要 | 記録のみ |
| C-R1-1 | C | note | 「期待するテスト結果」（外部化節）と「期待値」（データブロック表、glossary正表記）がページ内で表記不統一 | 不要 | note区分のため本ラウンドでは修正しない。記録のみ |
| D-R1-1 | D | must | 「テスト種別の正式名称」の導入文が処理方式を網羅するように読めるが、表は5/6処理方式のみ（テーブルをキュー欠落）で矛盾 | 要 | 導入文に「主な」を追加して非網羅を明示（A-1参照） |
| D-R1-2 | D | decide | 「全体像」節の`@Test`コード例・tipがdesign.mdの節定義（位置づけ・概要）の粒度を超える可能性 | 不要 | `current-0166`が当該内容込みでmapping.csvに割り当て済みのため対応不要と判断。ユーザーへの参考情報として記録 |

### ラウンド2（B-R1-1/3/4(b)、D-R1-1の修正後の再検証）

| 指摘ID | 観点 | 区分 | 指摘内容 | 対応要否 | 対応内容 |
|---|---|---|---|---|---|
| B-R2-1 | B | note→**取り下げ** | 「テストデータファイルへの外部化」冒頭文（B-R1-1修正後）がまだ出典と一部一致 | 要（B-R1-1の再修正として） | 文構造を再度全面変更 |
| B-R2-2 | B | note→**取り下げ** | 箇条書き末尾（＝R3-B2相当）が依然一致。※この時点ではまだA-5禁止事項の適用漏れに気づいておらず一時的に修正したが、最終的に禁止事項に従い**原文へ差し戻した**（後述） | — | 原文へ差し戻し |
| D-R2-1 | D | must | 「対象範囲」節が`design.md`§2の定義（対応する処理方式の一覧＋対応しないもの）のうち前半を欠く。`current-0377`の note にも「対応する処理方式の一覧」に対応する出典が無く、当初コミット`aa36feb`から一貫して未実装のまま3ラウンドの旧レビューをすり抜けていた | decide | `design.md`§6の名称一覧を転記して一時追加したが、mapping.csvに出典を持たない追加であるため`#8`差し戻し対応の禁止事項（出典にない内容を判断を仰がずに追加しない）に抵触すると判断し、**追加を取り消して元に戻した**。ユーザー判断待ちの新規`decide`案件として記録（後述） |
| D-R2-2 | D | note | D-R2-1と合わせ、ページ内のどこにもNTF対応処理方式6件の全体像が無い | — | D-R2-1の判断に従属。ユーザー判断待ち |

### ラウンド3（最終）

| 指摘ID | 観点 | 区分 | 指摘内容 | 対応要否 | 対応内容 |
|---|---|---|---|---|---|
| — | B | — | B-R2-1/2の再修正箇所を再検証し、両方とも「解消」と判定（このB-R2-2判定は、後述の理由により後日A-5禁止事項に従い差し戻した） | — | — |
| D-R3-1 | D | must | 対象範囲へ処理方式一覧を追加した結果、「テスト種別の正式名称」表（5処理方式のみ）との間で新たな矛盾が生じた | — | D-R2-1と同じ理由で対象範囲への追加自体を取り消したため、この矛盾も解消（追加していないので矛盾も生じない） |

## B-1基準の再適用と実測結果の齟齬（重要な教訓）

新基準（B-1）で再レビューしたところ、B-R1-2/B-R2-2（＝R3-B2、「テストソースコードの重複を大幅に削減できる」）を、
独立サブエージェントが**3回とも機械的にはmust**と判定した（該当句が21字で「20字程度」の目安をわずかに超えるため）。
一方、ユーザーは`ntf-doc-08-rework.md` A-5で、この箇所を実物比較のうえ明示的に「対応不要」「言い換えは意味を損なう」
と判定し、**禁止事項として「修正しない」を指定していた**。

対応: 一度誤って2回にわたり書き換えてしまったが、禁止事項の適用漏れに気づいた時点で**原文（`ef44dfc`時点の文面）へ
差し戻した**。あわせて`design.md`§11.6に「字数は目安であり、厳密な閾値ではない」「ユーザーの個別判断は本基準より
優先する」を追記し、同種の機械的な蒸し返しが#9以降で起きないようにした。

同様に、R3-B3（グループID文、「データタイプは、テストデータを解析する際の起点となる識別子であり…」）も、
入れ子になっていた別文（「個々のデータブロックを区別する単位は…組み合わせ」、input-0116由来）と混同して一度
書き換えてしまったが、こちらもA-5対象部分（グループID文そのもの）のみ原文へ差し戻し、A-5の対象外だった
「組み合わせ」文の書き直しは維持した（この文は旧3ラウンドのいずれの指摘記録にも現れておらず、ユーザーが
明示的に判断した対象ではないため）。

## 新規 decide 案件 — `ntf-doc-08-decide.md`によるユーザー判断で解決済み

**D-R2-1 / D-R2-2（「対象範囲」節の処理方式一覧欠落）— 解決**

ユーザーは選択肢2（`design.md`§2の「対象範囲」節定義を「対応しないものの明示のみ」に修正し、「対応する処理方式の
一覧」は「テスト種別の正式名称」節に委ねる）を採用した。一覧を2箇所に持たせると処理方式の増減時に片方が古くなる
ため、責務を「テスト種別の正式名称」表1箇所に寄せる判断。あわせてA-3の削除判断も覆し、`vocabulary.md:74`に
存在する`リクエスト単体テスト（テーブルをキューとして使ったメッセージング）`行を索引へ復元した（詳細は上記
「A-3追記」参照）。

`design.md`§2の「対象範囲」「テストの種類」の記載内容を改訂し、`about/index.rst`の「対象範囲」節には
「テスト種別の正式名称」表への`:ref:`導線1文のみを置き、処理方式一覧は再掲していない。

以下は判断前の記録（経緯を残すため元の記述を維持）。

**D-R2-1 / D-R2-2（「対象範囲」節の処理方式一覧欠落）**

`design.md`§2は「対象範囲」節の記載内容を「対応する処理方式の一覧。対応しないもの（Jakarta Batch、マルチスレッド
機能）」と定めているが、`about/index.rst`の「対象範囲」節には「対応しないもの」の2つの`important`しかなく、
「対応する処理方式の一覧」が存在しない。この欠落は初版（`aa36feb`）から一貫して存在し、旧3ラウンドの4観点レビュー
でも検出されなかった（観点Dの新チェック項目で今回はじめて検出）。

この内容をカバーする`mapping.csv`行が存在しない（`current-0377`の`note`は「対応しないもの」のみを対象と明記）。
`design.md`§6（処理方式の名称一覧）を根拠に処理方式名を列挙する案を試したが、これは`mapping.csv`に出典を持たない
追加であり、`#8`差し戻し対応の禁止事項「出典にない内容を、判断を仰がずに追加しない」に抵触すると判断し、
**追加を取り消して元（`important`2件のみ）に戻した。**

選択肢:
1. `design.md`§6の6処理方式名を転記する形で「対象範囲」に一覧を追加する（`decide`として今回ユーザーに承認を得れば
   実施可能。実施した場合、「テスト種別の正式名称」表からA-3で削除したテーブルをキュー行の扱いとの整合も再検討要）
2. `design.md`§2の「対象範囲」節定義を「対応しないものの明示のみ」に修正し、「対応する処理方式の一覧」は
   「テスト種別の正式名称」節（実質的に処理方式一覧を兼ねる）に委ねる、と定義を変更する
3. 現状（一覧なし）を追認し、対応不要と判断する

コーディネーターとしては選択肢1を推奨する（design.mdの構成要求を満たし、A-3で一覧から外したテーブルをキューの
存在も明示できるため）が、`mapping.csv`の枠外での追加である以上、ユーザーの承認を得てから実施する。

## `#8` decide案件回答（`ntf-doc-08-decide.md`）後の再レビュー（ラウンドを1から数え直す）

`design.md`§8「出典と確定設計が食い違う場合」・§11.7 `must`定義改訂後の基準で、STEP3適用後の`about/index.rst`を
独立サブエージェント4体（観点A/B/C/D）で再レビューした。ラウンド数は規約変更によりリセットする。

### ラウンド1

| 指摘ID | 観点 | 区分 | 指摘内容 | 対応要否 | 対応内容 |
|---|---|---|---|---|---|
| — | A | — | 指摘0件。追加復元した`テーブルをキューとして使ったメッセージング`行は`vocabulary.md`・`design.md`§6と完全一致し、`mapping.csv`13行の網羅性にも問題なし | 不要 | — |
| — | B | — | 指摘0件。`:ref:`ラベル命名、simple table罫線、導線文の文体、逐語的流用のいずれも問題なし | 不要 | — |
| — | C | — | 指摘0件。`glossary.md`正表記、`design.md`§6との名称一致、`vocabulary.md:74`との文字列完全一致を確認 | 不要 | — |
| D-DR1-1 | D | must | 「テスト種別の正式名称」の導入文「対象とする**主な**処理方式によって呼び分ける」の「主な」が、表が5/6処理方式のみだった`#8`A-3時点（非網羅性を明示する目的でA-1により付加）の名残であり、行復元により表がNTF対象6処理方式を過不足なく網羅する現在は、読者に「一部のみ掲載」と誤解させる | 要 | 「主な」を削除し「対象とする処理方式によって呼び分ける。」に修正 |

### ラウンド2（D-DR1-1修正後の再検証）

修正箇所は文言中の1語削除のみで、新たな内容追加を伴わないため、観点Dの担当項目（見出し・導入文と中身の一致）のみ
実測で再確認した。`grep -n "主な" ja/development_tools/testing_framework/about/index.rst`で該当箇所が該当行から
消え、他に「主な」を含む行が無いことを確認（誤爆なし）。表の8行（クラス単体1・リクエスト単体6・取引単体1）と
導入文「対象とする処理方式によって呼び分ける」が過不足なく整合することを目視確認。指摘0件、`must`解消。

**このラウンドで`must`は残っていない。** `decide`区分の新規案件も無し（D-R2-1/D-R2-2は本STEPで解決済み、上記
「新規decide案件」節参照）。

## `/rn:gm`フィードバック対応（2026-08-05、4点）

ユーザーからの直接フィードバック4点に対応。

1. **「全体像」冒頭の段落分割**: ユーザー自身が3文をそれぞれ独立した段落（空行区切り）に分割済み（`about/index.rst:10-14`）。段落内改行ではなく段落そのものの分割であり、Rules「日本語の地の文は段落内で改行しない」には抵触しない。そのまま維持した。
2. **「テストコードは定型かつ少量で済む」に外部化・大規模データの必然性を追記**: 「テストデータを、テストコードとは別のファイルに宣言的に記述する」という外部化の事実を明記し、「Nablarchが対象とする基幹システムはテーブル数や項目数が多く、テストデータのパターンも多数に上りやすいため...欠かせない」という理由づけを追加（`ja/index.rst`「基幹システム構築経験」・`about_nablarch/concept.rst`「基幹システムをはじめとする企業情報システム」の既存の位置づけに基づく）。旧「テストデータ」節にあった`current-0163`/`0176`（外部化の事実・利点）を`dest_section`変更で本特徴点に統合した（design.md §2改訂）。
3. **「アーキテクチャ」はアーキテクト限定にすべきでは、との指摘 — 検討の上、維持**: design.md §1の読者定義（第1部=両読者の共通前提）と、直前の「テストの種類」（本番同等の経路等）を裏づける仕組みという位置づけから、アプリ開発者にも必要な共通前提と判断。内容も図＋導入文のみに既に最小化済み（前ラウンドで表を削除済み）のため変更しない。
4. **「テストデータ」節を廃止**: 外部化（`current-0163`/`0176`）は上記2で「特徴」に統合。独立性（`current-0175`、マスタデータ投入ツール・マスタデータ復旧機能のtip含む）は「これは何か」ではなく「どう書くか」の実装注意事項であるため、`input-0002`/`input-0116`と同じ理由で第3部「テストデータの書き方」（未作成、`#9`以降で作成）へ`dest_part`/`dest_page`/`dest_section`ごと再割当（design.md §2改訂、`mapping.csv`・`volume.md`・`vocabulary.md`・`verify_mapping.py`更新、`verify_mapping.py`エラー0件を確認）。マスタデータ投入ツールの非推奨懸念については、現行解説書・input資料のいずれにも非推奨の記録がなく、`mapping.csv`上も第4部に独立ページ（`マスタデータ投入ツール`）としての割当が確定済みであることを確認し、非推奨ではないと判断した。

対応後、ページ全体を通し読みし、「テストデータ」節削除に伴う浮いた参照（`:ref:`・見出し）が残っていないことを確認した。Dockerビルド（`nablarch-document-build-sandboxed`イメージ）で`build succeeded`、警告1件（想定内の`undefined label: testdata_converter`のみ）を確認。

## ユーザーフィードバック対応（2026-08-05、直接指摘3点）

`/rn:gm`を介さない直接フィードバック3点（アーキテクチャの導入文・稼動環境のモジュール一覧・JUnit Vintageの説明）に対応。

1. **「アーキテクチャ」の導入文「それで？となる」**: 図（`abstract_structure.png`）を実際に確認し、テストクラスがテスト対象クラスを直接呼び出す、テスト対象クラスは本番と同じくNablarch Application Frameworkを介して動作する、テストデータはExcelファイルからテスティングフレームワーク経由で供給される、という関係を導入文に明記。「本番同等の基盤上でテストできる」という結論（特徴1点目の裏付け）まで書き、「何を伝えたいか」に答えた。削除済みの構成物一覧表（構成物・説明・作成者）の復元ではなく、図の意味を1段落で述べるにとどめた。
2. **「モジュール一覧」＞全体像と関係あるか、JUnit5の話では**: 「モジュール一覧」という単独見出しを廃止し、「JUnit 5用拡張機能を使う場合の依存関係」「JUnit Vintageを使う場合の依存関係」という条件付き見出しに分割。JUnit4のみで使う場合はこの節を読む必要が無いことが見出しから分かるようにした。
3. **JUnit Vintageの説明が「選択肢があるのか、選択基準は」**: 「稼動環境」冒頭に、2つの方法（JUnit5用拡張機能／JUnit Vintage）が存在する理由と選択基準（新規テストをJUnit5で書きたいか、既存のJUnit4テストをそのまま動かしたいだけか。併用も可）を要約し、詳細は`:ref:`\`JUnit 5用拡張機能 <junit5_extension>`\`（第2部、未作成）へ導線した。選択基準の出典（`current-0178`「JUnit VintageはJUnit4のテストをJUnit4のまま動かすに過ぎない」・`current-0265`「本拡張機能は新規テストをJUnit5で書くためのもの」）は第2部`機能概要`に割当済みのため、マッピング行は変更せず要約＋`:ref:`の形にとどめた（「全体像」特徴3点目のExcel/YAMLと同じパターン）。

Dockerビルド（`nablarch-document-build-sandboxed`）で`build succeeded`、警告2件（想定内の`undefined label: testdata_converter`・`undefined label: junit5_extension`のみ）を確認。詳細はdesign.md §2「「アーキテクチャ」は図のみとし」節末尾・「モジュール一覧の集約」節末尾を参照。

## ユーザーフィードバック対応（2026-08-05、直接指摘2件・「稼動環境」全廃）

前回ラウンドで追加した「稼動環境」の2見出し（JUnit5用拡張機能/JUnit Vintageそれぞれの依存関係）に対し、
「詳細はXX参照なので、下記は全体像に不要では？リンク先に書くべきでは？」との指摘。続けて、選択基準の要約文
（1〜2文＋`:ref:`）に縮小した後も「これも不要では？JUnit5で使いたい場合はXXXを参照、かと。なんでJUnit5の説明を
全体像に書こうとするの？このページは後続に共通する前提を書くんだよね」との追加指摘を受けた。

第1部は「両読者の共通前提」（design.md §1）を書くページであり、「JUnit4がベースだがJUnit5も使える」という
対応バージョンの事実はpremiseだが、「2つの方法のどちらを選ぶか」という選択基準・依存関係の生データは
premiseではなく、JUnit5を使う読者だけが必要とする使用方法レベルの情報と判断。`#6`当時の「依存関係は第1部に
集約する」という方針自体を撤回し、`current-0180`/`current-0267`を第2部「JUnit 5用拡張機能」使用方法へ
差し戻した。「稼動環境」は「テスティングフレームワークは、JUnit 4をベースに動作する。JUnit 5で使用する場合は、
`:ref:`\`JUnit 5用拡張機能 <junit5_extension>`\`を参照。」の1文のみに縮小した。

`mapping.csv`・`design.md`§2・`volume.md`・`verify_mapping.py`（`EXPECTED_ZERO_SECTIONS`に第1部「稼動環境」を
追加）を更新し、`verify_mapping.py`エラー0件を確認。Dockerビルドで`build succeeded`、警告2件（想定内の
`undefined label: testdata_converter`・`undefined label: junit5_extension`のみ）を確認。

## 申し送り事項

- **R1-D2（更新: `/rn:gm`フィードバック対応で`about/index.rst`から移動済み）**: 「マスタデータ投入ツール」
  「マスタデータ復旧機能」への言及を含む`current-0175`のtipは、`about/index.rst`の「テストデータ」節廃止に伴い
  第3部「テストデータの書き方」（`mapping.csv`の`dest_page`変更、未作成）へ再割当した。対象ページ
  （`tools/master_data_tool.rst`、`setup/master_data_restore.rst`）が未作成のため現状は地の文のまま引き継ぐ。
  「テストデータの書き方」ページを作成するタスクで、このtipを`:ref:`化することを忘れないこと。
- **D-R1-2**: 「全体像」節のコード例・tipがdesign.mdの節定義の粒度を超える可能性。対応不要と判断したが、
  #9以降で同様の指摘が出た場合の参考情報として記録する。
- **新規（2026-08-05）**: 「稼動環境」冒頭の`:ref:`\`JUnit 5用拡張機能 <junit5_extension>`\`により
  `undefined label: junit5_extension`警告が追加された。`setup/junit5_extension.rst`（第2部）を作成するタスクで
  ページ先頭ラベル`.. _junit5_extension:`を定義すれば解消する。`#last`ゲートで最終確認するまでの想定内の警告として扱う。

## `#10a`（用語「テストショット」への統一、2026-08-07）

user review ではなく、`ntf-doc-terminology.md` と `ntf-doc-terminology-addendum.md` による指示作業である。既存のレビュー記録は書き換えていない（本節の追記のみ）。

**変更は L24 の1行のみ**である（`checks/task-terminology.md` ゲート14 で `git diff` により実測確認）。

| 箇所 | 変更前 | 変更後 | 判定理由 |
|---|---|---|---|
| L24 | テストケースを追加しても実装量が増えにくい | テストを追加しても実装量が増えにくい | 第1部の抽象度に合わせ `テスト` を選んだ。第1部には `テストショット`・`testShots`・`LIST_MAP` の記述が一切なく（実測: `grep -rn 'テストショット\|testShots\|LIST_MAP' about/` が0件）、当該節（`テストコードは定型かつ少量で済む`）には第3部への `:ref:` も無いため、`テストショット` を使うと定義なしの初出になる。文意は「テストを増やしても Java の実装量は増えない」であり、`テスト` で成立する |
| L24 | 大量のテストケースを効率的に用意・保守する | 大量のテストを効率的に用意・保守する | 同上。直前が「テストデータのパターンも多数に上りやすいため」であり、ここでの主語は個々のテストショット行ではなくテスト全体の規模である。同一段落内で語を混在させないため、1件目と同じ `テスト` に揃えた |

ページ内の `テストケース` は0件になった（ゲート3）。用語集の根拠は `mapping/glossary.md` §5.7。

## `#24`（取引単体テストの実行方法の是正、2026-08-14）

`#22` の user review で回答された判断待ち2件のうち、本ページに関わる1件への対応である。既存のレビュー記録は書き換えていない（本節の追記のみ）。ラウンド1（`5e87f6e`）で「ウェブアプリケーションは手動操作、それ以外の処理方式はJUnitで自動実行」と書いたが、出典を持たない2処理方式まで断定で覆っていたため4観点レビューで `must` 差し戻しとなり、ラウンド2で下表の形に改めた。本節はラウンド2の最終形を記録する。

### 是正した内容（何を、なぜ）

本ページは「取引単体テストは手動操作である」と断定していたが、これは削除前の現行解説書 `05_UnitTestGuide/03_DealUnitTest/` の8ファイルのうち `index.rst` 1ページだけの記述を取引単体テスト全体に広げたものであり、残る出典と矛盾していた。取引単体テストの実行方法は処理方式によって異なる。一方で、出典が実行方法を述べているのは4処理方式だけであるため、**出典のある4処理方式だけを名指しし、残る2処理方式については何も述べない**。「それ以外」「すべての処理方式」のような閉じた表現は使わない（使うと、旧版と同型の過度一般化を向きだけ変えて繰り返すことになる）。名指ししない処理方式について、読者は当該処理方式の第3部ページで答えを得る。

| 箇所 | 変更前（`3c7d6ac` 時点） | 変更後（ラウンド2の最終形） |
|---|---|---|
| L58（`:widths:`） | `20,20,30,30` | `9,26,16,49`（新しいセル文言の表示幅の実測比。算出根拠は `checks/task-24.md` ラウンド2 S-4） |
| L73（対比表「実行方法」セル） | 手動操作 | 処理方式によって異なる（手動操作またはJUnitで自動実行） |
| L75（対比表「備考」セル） | APサーバへのデプロイが必要。エビデンスは画面ハードコピー・DBダンプ | 手動操作の場合は、アプリケーションサーバへのデプロイが必要で、エビデンスは画面ハードコピー・DBダンプとなる |
| L79（`important`） | 取引単体テストは、自動実行ではなく手動操作によって行う。自動実行の対象となるのは、クラス単体テストとリクエスト単体テストである。 | 取引単体テストの実行方法は、対象とする処理方式によって異なる。ウェブアプリケーションでは、JUnitでの自動実行はできず、テスト対象のアプリケーションをアプリケーションサーバにデプロイして手動で操作する。RESTfulウェブサービス、Nablarchバッチアプリケーション、MOMによるメッセージングでは、リクエスト単体テストを連続実行することにより、JUnitで自動実行する。 |

備考セルは1文にまとめた。第1文だけに「手動操作の場合は」を付けると、第2文（エビデンス）が取引単体テスト全体の話に読めてしまうためである。出典側のエビデンスの記述（`index.rst:39-40`）はウェブアプリケーションを前提とした総論ページのものであり、手動操作の場合に限られる。

語は `実行方法` に統一した。対比表の列見出し（L61）とリード文（L54）が `実行方法` であるためである。出典側の `実施方法` は、出典を引用するときにのみ使う。

`important` は削除しなかった。対比表のセルは「処理方式によって異なる」という結論だけを担い、どの処理方式がどちらか、自動実行がリクエスト単体テストの連続実行によって成り立つことは `important` が担うため、両者は言い換えの関係にない。取引単体テストは第2部に3ページ・第3部に6ページあり（`design.md`「12. 未確定事項の確定」表#1・#2）、そのすべてがこの区別を前提にする。

対比表の直後にある内訳表（現 L83-94、リクエスト単体テストの6処理方式）は変更していない。`design.md`「「テストの種類」に置く対比」節の「内訳の表は、リクエスト単体テストの6処理方式のみを載せる」という確定事項と両立させるため、取引単体テストの処理方式ごとの一覧表は新設していない。

### 出典8ファイルの実測結果（`git show 2e501ad:<path>`、パスは `ja/development_tools/testing_framework/guide/development_guide/05_UnitTestGuide/03_DealUnitTest/`）

ディレクトリの `.rst` は8ファイルで全件である（`git ls-tree -r --name-only 2e501ad` で確認。他は `_images/` のみ）。8ファイルを `grep -n "手動\|自動\|連続実行"` に掛けた全件は次のとおり。

| 出典 | 該当行（実測） | 実行方法として読めるか |
|---|---|---|
| `index.rst:7-8` | 取引単体テストでは、テスト対象のアプリケーションをアプリケーションサーバにデプロイし、手動でアプリケーションを操作しテストを行う。 | **手動** |
| `batch.rst:5-6` | バッチ処理の取引単体テストは、自動テストフレームワークを使用してテストを行う。リクエスト単体テストを連続実行することにより、取引単位でのテストを行う。 | **自動** |
| `rest.rst:7` | ただし、複数のリクエストにより取引が成立する場合は、リクエスト毎のテストを連続実行することにより取引単体テストが実施可能である。 | **自動** |
| `real.rst:8-10` | ただし、複数のメッセージにより取引が成立する場合は、バッチ処理における取引単体テストと同様に、リクエスト毎のテストを連続実行することにより取引単体テストが実施可能である。 | **自動** |
| `delayed_receive.rst:5-6` | 取引単体テストの実施方法は、同期応答メッセージ受信処理と同じである。実施方法の詳細は `./real` を参照すること。 | **自動**（`real.rst` を指す） |
| `delayed_send.rst` | 該当0件 | — |
| `send_sync.rst:151`・`:176`・`:207` | サイズを自動計算する／Excelファイルを手動で編集して／表の列見出し「自動テストフレームワークの動作」 | **いずれも実行方法ではない** |
| `http_send_sync.rst` | 該当0件 | — |

以上から、公開本文で名指しできる処理方式（`glossary.md` §5.2 の正表記）は次の4つに限られる。

| 処理方式 | 実行方法 | 出典 |
|---|---|---|
| ウェブアプリケーション | 手動 | `index.rst:7-8` |
| RESTfulウェブサービス | 自動 | `rest.rst:7` |
| Nablarchバッチアプリケーション | 自動 | `batch.rst:5-6` |
| MOMによるメッセージング | 自動 | `real.rst:8-10`・`delayed_receive.rst:5-6` |

残る2処理方式は公開本文で述べない。`HTTPメッセージング` は出典 `http_send_sync.rst` に実行方法の記述が0件であり、`テーブルをキューとして使ったメッセージング` は取引単体テストの出典自体が0件である。

あわせて次を実測した。

- `index.rst` はウェブアプリケーションを前提にした総論ページである。テスト準備が「データベース準備（データ投入）・アプリケーションのデプロイ・アプリケーションサーバ起動」（`:19-21`）、テスト結果エビデンスが「画面ハードコピー・DBダンプ(テスト実行前および実行後)」（`:39-40`）である。
- `send_sync.rst:7`・`http_send_sync.rst:7` の「（HTTP）同期応答メッセージ送信処理を伴うウェブアプリケーションで、取引単体テストを行う場合は、Nablarchが提供するモックアップクラスを使用する。」は、テスト対象アプリケーションの種類を述べた文であって実行方法の記述ではない。
- `delayed_send.rst:5-6` は、本文（「同期応答メッセージ送信処理と同じ」＝ `send_sync.rst`）とリンク先（`./real`。`real.rst:2` のページ題は「取引単体テストの実施方法（同期応答メッセージ受信処理)」）が食い違う出典である。MOM を自動とする根拠には使っていない。

### `input-0028` の出典に実行方法列・備考列が無いという実測結果

`dest_section=テストの種類` を持つマッピング行は `input-0028` の1件だけである（`mapping.csv:198`。`grep -n 'テストの種類' mapping.csv` の結果も同行のみ）。その出典 `input/ntf-doc-terms.md:415-426` は「正式名称」「内容」の2列の表であり（`:416-417` がヘッダ行と区切り行）、実行方法列も備考列も持たない。

したがって本ページ L73・L75 は `design.md`「「テストの種類」に置く対比」節の対比表で執筆側が作った記述であり、出典側に是正すべき対応物は無い。**`mapping.csv` は変更していない。**
