# page-deal_unit_test_web — 取引単体テスト（ウェブアプリケーション）

対象ファイル: `ja/development_tools/testing_framework/implementation/deal_unit_test/web.rst`
キュー番号: `#27-11`
個別指示: `ntf-doc-27-small-3rd.md` §1・§4・§6

`ntf-doc-27-small-3rd.md` §1-1 の「出典に無いL3は立てない」を第3部に適用する件の `decide` は、`page-deal_unit_test_rest.md` §5 D-1／§7 decide-1 に1回だけ記録してある（同記録が `#27-10`・`#27-11`・`#27-15` からも参照する旨を明記済み）。本ページでは重複して立てない（S7）。

## 1. 参照リポジトリ

無し。本ページは手動テストの手順を説明するページであり、テスティングフレームワークのクラスを説明していない。実装コードを根拠にした記述は1件も無い。

## 2. 出典行の消化

`mapping.csv` を `csv.DictReader` で読み、`dest_page` が「取引単体テスト（ウェブアプリケーション）」である行を抽出した。5件である。

| mapping_id | src_file | src 行 | 行数 | disposition | dest_section |
| --- | --- | --- | --- | --- | --- |
| current-0142 | `03_DealUnitTest/index.rst` | 6-13 | 8 | MOVE | 機能概要 |
| current-0143 | `03_DealUnitTest/index.rst` | 16-23 | 8 | MOVE | 使用方法 |
| current-0144 | `03_DealUnitTest/index.rst` | 26-33 | 8 | MOVE | 使用方法 |
| current-0145 | `03_DealUnitTest/index.rst` | 36-46 | 11 | MOVE | 使用方法 |
| current-0059 | `02_RequestUnitTest/double_transmission.rst` | 27-39 | 13 | MOVE | 使用方法 |

出典の非空行と反映先の対応は次のとおり。出典は削除済みのため `git show 2e501ad:<path>` で読んだ。A = `03_DealUnitTest/index.rst`、B = `02_RequestUnitTest/double_transmission.rst`。

| 出典行 | 内容 | 反映先 |
| --- | --- | --- |
| A:7-8 | デプロイして手動で操作しテストを行う | `:10` |
| A:10 | 同期応答メッセージ送信処理を伴う場合はモックアップクラス、`:ref:`dealUnitTest_send_sync`` | `:15` |
| A:12 | テストケース毎に以下の手順でテストを実施する | `:20` |
| A:17 | テスト準備として以下を行う | `:24` |
| A:19-21 | データベース準備（データ投入）／アプリケーションのデプロイ／アプリケーションサーバ起動 | `:26` `:27` `:28` |
| A:27 | テストケースに従ってテストを実施する | `:32` |
| A:29-31 | tip: エビデンスのうちDBダンプはテスト実行前のものが必要 | `:34-35` |
| A:37 | 以下のエビデンスを取得する | `:39` |
| A:39-40 | 画面ハードコピー／DBダンプ(テスト実行前および実行後) | `:41` `:42` |
| A:42-43 | tip: テスト補助ツールについては現在検討中 | `:44-45` |
| B:28 | 取引単体テストではクライアントサイドにてテストを実施する | `:49` |
| B:31 | 手順は下記のとおりである | `:51` |
| B:33 | デバッグモードでアプリケーションを起動 | `:53` |
| B:35 | ブレークポイントを設定 | `:54` |
| B:37 | 打鍵にてボタンを選択 | `:55` |
| B:39 | 停止状態で再度選択しリクエストが送信されないことを確認 | `:56` |

**未消化の行は0行である。** 出典の見出し行（A:14-15・A:24-25・A:34-35）は本文ではないため、どの `mapping_id` の範囲にも含まれていない。見出しそのものは L3 として立て直している（D-7）。

A:47-56 は A:34-43 と同一内容の節である（原文を実測して確認した。見出し・本文・tip まで一致）。`mapping.csv` は本体 A:49-58 を `current-0146`（`dest_page` 空・`disposition=DROP`）として、旧解説書側のコピー＆ペースト重複と判定している。本ページに1回だけ書いた扱いは、この割当どおりである（D-1）。

## 3. 実装で確認した事実

無し。§1 のとおり。

## 4. 実測

4観点の是正を全件畳んだ後の本文で測った。

| 項目 | 実測値 |
| --- | --- |
| 行数 | 56行。行末空白0件 |
| 下線 | L1 `:4` 50（表示幅40）／L2 `:13` `:18` 50（8）×2／L3 `:23` `:31` `:38` `:48` 49（16・16・32・32）×4／L4 0件。`unicodedata.east_asian_width` で W/F/A を2として測った。実測則（L1 `max(50,表示幅)`／L2 50固定／L3 `max(49,表示幅)`）からの逸脱0件。L2 下線の直後は空行あり、L3 下線の直後は空行なしで、承認済み `mom.rst`・`batch.rst`・`rest.rst`・`http_messaging.rst` と同型 |
| `:ref:` | 3件 |
| `.. tip::` | 2件（`:34` `:44`）。`.. note::`／`.. warning::`／`.. important::` は0件 |
| コードブロック・画像・表 | 0件 |
| `verify_mapping.py` | exit 0（`OK: no errors`）。`mapping.csv` は未変更 |
| Docker フルビルド（`sphinx-build -E`） | `build succeeded, 1 warning.`。警告は既知の `db_double_submit.rst:108: undefined label: how_to_set_token_in_request_unit_test` 1件のみで新規0件 |

`:ref:` 3件の解決結果。各ラベルの定義行と、その直後の見出しを実際に開いて照合した。

| リンク文字列 | ラベル | 定義位置 | 飛び先の見出し | 一致 |
| --- | --- | --- | --- | --- |
| 取引単体テスト（MOMによるメッセージング） | `deal_unit_test_mom` | `implementation/deal_unit_test/mom.rst:1` | `:3` 取引単体テスト（MOMによるメッセージング） | 一致（本文129行） |
| 取引単体テスト（HTTPメッセージング） | `deal_unit_test_http_messaging` | `implementation/deal_unit_test/http_messaging.rst:1` | `:3` 取引単体テスト（HTTPメッセージング） | 一致（本文30行） |
| リクエスト単体テスト（ウェブアプリケーション） | `request_unit_test_web` | `implementation/request_unit_test/web.rst:1` | `:3` リクエスト単体テスト（ウェブアプリケーション） | 一致。**ただし飛び先は4行のスタブである**（decide-3） |

## 5. 出典から変えた点

**D-1 A:47-56 の重複節を反映しなかった。** `current-0146` が理由付きで DROP としており、A:36-46（`current-0145`）と同一内容だからである。

**D-2 「テストケース」2件を置き換えた。** A:12 → `:20`「実施するテストごとに、次の手順を繰り返す。」、A:27 → `:32`「あらかじめ定めたテストの内容に従ってアプリケーションを操作する。」。`glossary.md:556` の対応表は、何を検証するかを述べる文脈では普通名詞の「テスト」に置き換えると定めており、各ページのゲートは0件を求めている。**A:12 の「毎に」が持つ反復の含意は `:20` の「ごとに、〜繰り返す」で残した。** なお `テストショット` は使えない。`glossary.md:201` の定義は「1回の実行に対応する、1組の入力と期待値」であり、テストデータを持たない手動テストの手順を指せないためである（decide-6）。

**D-3 「打鍵」を落とした。** B:37 の「打鍵にて」。`glossary.md` に裁定行が無く、現行の解説書側にも0件である（`grep -rn "打鍵" ja/` で実測）。`:55` は「画面で、テスト対象のリクエストを送信するボタンを選択する。」とした。

**D-4 HTTPメッセージ送信の導線を追加した（出典に無い）。** 出典 A:10 は同期応答メッセージ送信の1本しか参照していない。追加の根拠は `03_DealUnitTest/http_send_sync.rst:7`（`2e501ad`。「HTTP同期応答メッセージ送信処理を伴うウェブアプリケーションで、取引単体テストを行う場合は、Nablarchが提供するモックアップクラスを使用する。」）と、作成済みの `implementation/deal_unit_test/http_messaging.rst:10,15`。用語は `glossary.md:158` の正表記「HTTPメッセージ送信」に合わせた。

**D-5 使用方法の導入文に二重サブミット防止機能への橋渡しを追加した（出典に無い）。** `:20` の第2文。`current-0142` の note が「導入文と本体の接続をページ内で再構成すること」を求めており、`current-0059` を4本目の L3 として抱えたことでこの1文が必要になった。

**D-6 `current-0059` の note が指定した `:ref:` の設置位置から外した。** note は「本ページでは再掲せず当該箇所への `:ref:` 参照を**使用方法冒頭**に置く」としているが、L3「二重サブミット防止機能を確認する」の冒頭（`:49`）に置いた。使用方法冒頭に置くと、二重サブミット防止機能を使っていない読者にも無関係な参照を先に読ませることになるためである。再掲していないという note の趣旨（`current-0057` は `リクエスト単体テスト（ウェブアプリケーション）` へ MERGE 済み）は満たしている。

**D-7 出典の見出しを「〜する」形に改め、L3 を4本立てた。** 出典の「テスト準備」「テスト実施」「テスト結果エビデンスの収集」は体言止めであり、`style.md` S-03 の形式規約に合わない。`ntf-doc-27-small-3rd.md:129-132` が確定させた4本の見出し（テストを準備する／テストを実施する／テスト結果のエビデンスを取得する／二重サブミット防止機能を確認する）をそのまま採った。

**D-8 A:19「データベース準備（データ投入）」を `:26`「データベースにデータを投入する。」に畳んだ。** 体言止めを「〜する」に開くと「データベースを準備する（データを投入する）」となり、括弧の外と中が同義になるためである。

**D-9 `:32` に「あらかじめ定めた」を補った。** D-2 で「テストケースに従って」を「テストの内容に従って」にすると、何に従うのかが本文から読めなくなるためである。「テスト仕様書」という出典に無い語は使わなかった。

**D-10 tip1 を1文に戻した。** 出典 A:30-31 は「〜必要であるため、〜取得しておくこと。」という因果1文である。初版は2文に割って「テスト実行前」「テストを実施する前」を重ねていた。

**D-11 デバッグモード起動と `:28` の起動の衝突を解消した。** 出典では A:19-21 と B:33 が別ファイルにあったため衝突していなかったが、1ページに畳んだことで「アプリケーションサーバを起動する」（`:28`）と「デバッグモードでアプリケーションを起動する」（B:33）が同じ実行に見える。`:51` に「テストの実施とは別に」を、`:53` に「アプリケーションサーバを」を補った。

**D-12 「両者」の指す先を明示した。** `:49` の初版は「両者で確認内容が分かれる理由」と書いており、直前の「クライアントサイド」「サーバサイド」を指すように読めた。出典 B:7-9（`current-0057`）が言っているのは「リクエスト単体テストと取引単体テストではテスト方法が異なる」であるため、その2つを名指しした。

**D-13 リード文から「テスティングフレームワークを使った自動実行は行わない。」を落とした。** `design.md:117`「『JUnitでの自動実行はできず』のような不可能性の記述は書かない。走査2 のとおり、出典にその記述が0件だからである。」に反する。対比材料の `03_DealUnitTest/batch.rst:5` はバッチが自動であることを述べるだけで、ウェブが自動でないことは述べていない。

**D-14 機能概要から出典 A:7-8 の再掲を落とした。** 初版の機能概要1文目「テスト対象のアプリケーションをアプリケーションサーバにデプロイし、画面を操作してテストを実施する。」は、リード文 `:10` と `:27-28` の両方と重なり、承認済み `about/index.rst:77` を含めると同じ事実が4か所になっていた。デプロイの記述はリード文に統合し、機能概要はモックアップクラスへの導線だけとした。`design.md:678` の②「重複がない」に従った。

**D-15 エビデンスの表記を「画面ハードコピー」「DBダンプ」に揃えた。** 初版は本文を「画面のハードコピー」「データベースのダンプ」と書き換えていたが、`glossary.md` §8 の対応表にその置き換えを求める行は無く（`grep` で確認。ダンプ系は `:584` の `HTML ダンプ`→`HTMLダンプ` のみ）、承認済み `about/index.rst:75` が「画面ハードコピー・DBダンプ」を使っている。同一ページ内で tip2（出典どおり残す指示）の「DBダンプ取得ツール」とも割れていた。あわせて tip2 の `\ DB\ ` エスケープを外し、A:43 と1バイト違わない形にした。

## 6. 4観点レビュー

QA／設計／クラフト／検証の4観点をそれぞれ別のサブエージェントで実施した。指摘は合計25件（QA 7・設計 7・クラフト 9・検証 2）。1ラウンドで是正し、本文への是正は D-8〜D-15 の8件（`:10` `:15` `:20` `:26` `:32` `:34-35` `:41-42` `:44-45` `:49` `:51` `:53` `:55` に反映）。是正は成果物の `.rst` に畳んであり、別コミットに割っていない。

最も重いのは3件。

① **リード文の「テスティングフレームワークを使った自動実行は行わない。」が規約違反だった**（QA）。`design.md:117` が不可能性の記述を明文で禁じており、`design.md:96` の走査2 が「『JUnitでの自動実行はできない』に類する記述は8ファイルに0件」と実測している。設計観点は同じ箇所について「`about/index.rst:73` に合わせて『JUnitによる自動実行は行わない』とせよ」と提案したが、これも不可能性の記述であるため採らず、**QA の「第2文を削除」を採った**（D-13）。承認済み `about/index.rst:77` も肯定形のみである。

② **機能概要がリード文と使用方法の再掲だけになっていた**（設計・クラフトが独立に指摘）。両者とも「`:15` を削り、機能概要はモックアップの段落から始める」を第1案として挙げており、そのとおりにした（D-14）。設計観点はあわせて「手動である理由」「エビデンスが成果物であること」を機能概要に足す案も挙げたが、前者は D-13 と同じ不可能性の記述になり、後者は L3「テスト結果のエビデンスを取得する」と重複するため採らなかった。

③ **エビデンスの表記が承認済みページと不一致で、同一ページ内でも割れていた**（QA・設計・クラフトの3観点が独立に指摘）。3観点とも「本文を `画面ハードコピー`・`DBダンプ` に寄せる」で一致したため、そのとおりにした（D-15）。

不採用のうち重いのは3件。

**（1）tip1 を `.. important::` にせよ**（設計・クラフト）。「テスト実行前のDBダンプを取り損ねると手動テストをやり直すことになるので `tip` の定義『読まなくても機能は正しく使える補足情報』に当たらない」という指摘である。**採らなかった。** `style.md:232-235` の `important` は「無視すると**不具合・非推奨機能の誤用・データ不整合**につながる、読者が必ず守るべき注意事項」と結果を3つに限っており、エビデンスの取り直しはそのいずれでもない。`design.md:121` も、S-06 を同じ狭さで読んで `important` を採らない判断をしている。出典 A:29 も `tip` である。判断を decide-5 に上げる。

**（2）L3「テストを準備する」「テストを実施する」は S-03 の内容条件に反する**（設計）。`style.md:155-156` は「`準備する`／`設定する` のように、ページタイトルを足しても情報が増えない語は不可」としており、指摘は規約に照らして正しい。**採らなかった。** `ntf-doc-27-small-3rd.md:129-132` がこの4見出しを確定させており、変更は S2 違反になる。`#27-09` の同種の指摘（L3「テストを実行する」の改名）も同じ理由で不採用にしている。decide-4 に上げる。

**（3）`mom.rst:62` を書き換えよ**（QA・設計）。`mom.rst:62` が「テストクラスの作り方はウェブアプリケーションの取引単体テストと同じである」と本ページへ委譲しているが、本ページはテストクラスに触れられない（S3 が0件を求める）。**採らなかった。** 承認済みページの改稿は本タスクの範囲外である。decide-2 に上げる。

このほか、クラフト観点の `:32`「テストの内容」の明確化について、提示された2案のうち「テスト仕様書に定めた内容に従って」（出典に無い語を持ち込む案）は採らず、「あらかじめ定めたテストの内容に従って」を採った（D-9）。検証観点の2件の FAIL（S7・G10）は、いずれも本記録と `checks/task-27.md` の `#27-11` 節が未作成であることが理由であり、本記録の作成で解消する。本文の誤りではない。

## 7. 判断待ち

**decide-1 tip2「画面ハードコピー取得ツール、DBダンプ取得ツール等のテスト補助ツールについては現在検討中。」（`:44-45`）を残したこと。** `ntf-doc-27-small-3rd.md:139` が「出典どおり残したうえで `decide` に上げる。自分で削除しないこと」と定めているため、A:43 と1バイト違わない形で残した。**この一文は、いつの時点の「現在」なのかが読者に分からない。** 作業指示 §5 の G6 が禁じる「将来」には当たらないが、同じ性質の記述である。削除・書き換え・残置のいずれにするか判定してほしい。

**decide-2 `mom.rst:62` が本ページへ委譲している「テストクラスの作り方」を、本ページが受けられないこと。** `mom.rst:62`「同期応答メッセージ送信を伴うウェブアプリケーションを対象とする場合、テストクラスの作り方はウェブアプリケーションの取引単体テストと同じである。」に対し、本ページはテストクラスに一切触れていない（`ntf-doc-27-small-3rd.md:110` が「書けない。書かない」、S3 が0件を求める）。**読者は飛び先で答えを得られない。** `mom.rst:62` は後半の「モックアップクラスはコンポーネント設定ファイルで登録するため、テストクラスに記述することはない」だけで文意が通るため、前半を落とす案を提案する。承認済みページの改稿になるため自分では直していない。`#27-09` の判断待ち④の続きである。

**decide-3 `:49` の `:ref:` の飛び先 `implementation/request_unit_test/web.rst` が4行のスタブであること。** 約束している「サーバサイドの動作の確認方法」と「リクエスト単体テストと取引単体テストで確認内容が分かれる理由」の実体は `current-0057`（MERGE）・`current-0058`（MOVE）として同ページに割り当てられており、執筆は `#27-20` である。ラベルはスタブが定義しているためビルド警告は出ず、`#27-20` が書き落としてもどのゲートにも掛からない。**`#27-20` への申し送り: `current-0057` を必ず本文化すること。本ページ `:49` がそこを指している。** `#27-07` decide-2・`#27-08` decide-6・`#27-09` decide-4・`#27-10` decide-4 と同型。

**decide-4 L3「テストを準備する」「テストを実施する」が `style.md:155-156`（S-03 の内容条件）に反すること。** §6 の不採用（2）。`ntf-doc-27-small-3rd.md:129-132` の指定を優先した。

**decide-5 tip1（`:34-35`）を `.. important::` にしなかったこと。** §6 の不採用（1）。`style.md:232-235` の `important` の3条件（不具合・非推奨機能の誤用・データ不整合）に当たらないと判断した。第3部の他ページに前例が無く、ここで基準を作ることになる。

**decide-6 `glossary.md:201`／`:556` の断定に反例があること。** 両行は「業務上のテストケースを指す用法は NTF解説書の本文には現れない」と断定し、そのうえで本文の `テストケース` は常に置換対象としている。しかし本ページの出典 A:12・A:27 は手動テストの手順であり、`testShots` のエントリ（1組の入力と期待値）を指し得ない。**用語集の断定の反例である。** 規約ファイルは書き換えない（作業指示 §1-3）ため上申のみ。本ページは D-2 のとおり普通名詞の「テスト」に置き換えて処理した。
