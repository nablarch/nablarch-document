# レビュー記録: `テストデータの書き方`（`implementation/testdata_notation.rst`）

対象 `mapping_id`: `dest_page=テストデータの書き方` の140行（使用方法139・機能概要1、`DROP`なし）。

4観点（A網羅性 / Bトンマナ / C用語 / D整合性）を、それぞれ別のサブエージェントで実施した。プロンプトには
「実測コマンドで裏付けよ」「検証スクリプトを正解として使わず独立に組め」「敵対的にレビューせよ」の3点を
含めた。ラウンド1のみで、`must` は全件解消・`decide` はユーザーレビューに引き継ぐ。

## ラウンド1

| 指摘ID | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|
| A-1 | A | must | `input-0160` 由来。可変長ファイルの有効ディレクティブキーを「8個」と記載していたが、直後の一覧表は9個（`file-type`/`text-encoding`/`record-separator`/`field-separator`/`quoting-delimiter`/`ignore-blank-lines`/`requires-title`/`max-record-length`/`title-record-type-name`）だった | 要 | — | 「8個」→「9個」に修正 |
| A-2 | A | must | `input-0141` 由来。`errorMode:` による障害系テストが機能する範囲（応答電文・モックアップクラス経由に限る）と、`RequestTestingSendSyncSupport`（`GroupMessageParser`）経路では機能しないという制約が欠落していた | 要 | — | `errorMode` 一覧表の直後に `important` を追加し、適用範囲と非対応経路を明記 |
| A-3 | A | must→decide | `input-0101`/`input-0102`（ウェブアプリのtestShots必須カラム）は `isValidToken`/`forwardUri`/`context` を必須、`requestParams` を任意としているが、旧current側出典（`current-0081`、`index.rst` の表）は `isValidToken`/`forwardUri` を必須マーク無し（任意相当）、`requestParams` は同表に無く別表（別途「必須記載」と地の文で案内）としている。成果物は旧current側の区分に従った。2つの出典が「必須」の割当で食い違っており、ドキュメント同士の突合では正誤を決定できない | 要判断 | 出典間の対立でありdesign.md 11.8「機械検証できないもの」に該当しない新種の対立（実装未確認）。決定には`HttpRequestTestSupport`の実装確認が要る | ユーザーレビューに引き継ぐ（後述「decide」参照） |
| B-F01 | B | decide | L3見出し配下の細分に太字文（「〜する。」形式）を7箇所使用。`style.md` S-04はL1/L2/L3の3階層のみ定義、FW解説書コーパスにもこの用法の前例なし | 要判断 | style.mdに規約が無いための設計判断 | ユーザーレビューに引き継ぐ |
| B-F02〜F10 | B | must | 出典9箇所（`input-0126`カラム省略・`06_TestFWGuide/01_Abstract.rst`複数箇所等）の一文がほぼそのまま（語順・接続・修飾関係一致）残っていた。詳細は各指摘参照 | 要 | — | 9箇所すべて常体で言い換え。主キー省略・FK省略・DELETE挙動・EXPECTED_TABLE省略時挙動・セル書式の準備・空行の`""`記法・DATE型タイムゾーン依存・forwardUri・コメント機能導入文 |
| B(他8観点) | B | — | S-01〜S-03, S-05〜S-08は違反なし | 不要 | 確認済み・問題なし | — |
| T-01 | C | must | L658「メッセージ同期送信・HTTPメッセージ同期送信」の語順が`glossary.md`の正表記（「同期応答メッセージ送信」「HTTPメッセージ送信」）と不一致。同じページの他箇所（L604・L607）は正しい語順だった | 要 | — | 正しい語順に修正 |
| T-02 | C | must | 「セクション」が3箇所（旧L319・L701・L704）でデータブロックの意味に誤用。`glossary.md` §8は「テストデータの単位を指す場合は`データブロック`に置換」と規定 | 要 | — | 3箇所とも「データブロック」に置換 |
| T-03 | C | decide | L1068「電文ヘッダ」「電文ボディ」が、同ページ他所（5箇所以上）で使う正表記「フレームワーク制御ヘッダ」「メッセージボディ」と不統一。`glossary.md`未収録語のため機械的な正表記違反ではない | 要 | ページ内一貫性のための編集判断として妥当性が高く、対応するリスクが低いと判断し著者判断で修正 | 「電文ヘッダ」「電文ボディ」の呼称を削除し「フレームワーク制御ヘッダ」「メッセージボディ側」に統一 |
| D-1 | D | must | L435「``SETUP_TABLES``」（複数形）が、ページ内の14種のデータタイプ一覧（``SETUP_TABLE``単数形のみ）と矛盾。出典`current-0122`（`rest.rst:116`）を確認したところ、現行解説書の全域中でも1箇所のみの表記であり、他に同名の別データタイプとしての言及が無いことを`grep`で確認した | 要 | — | `SETUP_TABLES`→`SETUP_TABLE`に統一（現行解説書側の表記揺れと判断） |
| D-2 | D | decide | 見出し「テストデータファイルの構造を理解する」配下に、テスト独立性・マスタデータ再利用という異質な内容（旧L126-132）が混在し、見出しの範囲を超えていた | 要判断 | 内容の追加・削除を伴わない見出し分割の範囲内であり、著者判断で対応可能と判断 | 「テストの独立性を保つ」というL3見出しを新設し、該当2段落をそちらへ分離 |
| D-3 | D | note | ウェブアプリ用カラム表に`expectedStatusCode`が共通カラム表と重複掲載されていた | 不要 | 実害が小さく、他の処理方式の表には同種の重複が無い局所的な冗長のため記録のみに留める | 未対応（記録のみ） |
| D-4 | D | decide | `about/index.rst`（第1部・特徴3点目）の`:ref:`は現状 `テストデータ変換ツール` のみを指しており、design.md 74行目が「本ページ（テストデータの書き方）作成後に参照を見直すことを検討する」としていた宿題が未着手 | 要判断 | 既に承認済みの第1部ページへの変更を伴うため、著者判断でなくユーザー判断が必要 | ユーザーレビューに引き継ぐ |
| D-5 | D | note | 「外部化」の事実が`about/index.rst`（特徴2点目）と本ページ冒頭の両方で異なる理由づけとともに説明されている | 不要 | design.mdが定める役割分担（外部化の事実は第1部、独立性は第3部）の範囲内であり、致命的な重複ではないため記録のみに留める | 未対応（記録のみ） |

## ラウンド1終了時点のまとめ

- `must`: A-1・A-2・B-F02〜F10（9件）・T-01・T-02・D-1、全て解消。Dockerフルビルド（`-a`）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ）を確認
- `decide`（3件、著者判断で解消したT-03・D-2を除く）: A-3（必須/任意区分の出典対立）・B-F01（L4相当の太字疑似見出しパターン）・D-4（`about/index.rst`特徴3点目の参照見直し）。ユーザーレビューで判断を仰ぐ
- `note`（2件）: D-3・D-5。対応不要、記録のみ

## ラウンド2（差し戻し是正）

`#9`はuser reviewで差し戻し。是正指示は `ntf-doc-09-fix.md`。ラウンド1の`decide`3件のうちA-3・B-F01は実物確認で決着し、
D-4はユーザー判断で`#9`是正に同梱した。加えて観点Aが検出できていなかった網羅性の欠落2件（`requestParams`/`responseResult`、
`searchResult`）を新規`must`として記録する。以下は参照したコミット `e21bf67`（`nablarch/nablarch-testing`の`main`、
`nablarch-testing` 2.2.0、`release-6u2`マージ済み）に基づく。**同リポジトリに`6u3`のブランチ・タグは存在せず
（`git ls-remote origin`で確認済み）、`6u3`との差分は未確認である。**

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| A-3（確定） | 2 | A | must | ラウンド1で出典対立のため`decide`に回した`isValidToken`/`forwardUri`の必須区分は、実装確認により決着した。`TestCaseInfo#isValidToken()`（`TestCaseInfo.java:482-484`）は`getValue`経由で読み、呼び出し元`AbstractHttpRequestTestTemplate#executeTestCase:257`は条件分岐の外にあり全テストケースで必ず評価される。`TestCaseInfo#getExpectedForwardUri()`（`TestCaseInfo.java:237-239`）も同じく`getValue`経由で読み、`assertForwardUri`（`AbstractHttpRequestTestTemplate.java:554-557`）は`assertAll:464`から無条件に呼ばれる。全処理方式で「必須」とはカラム自体の存在を指し値は空でよいこと（ウェブアプリケーション: `TestCaseInfo.java:443-448`の`containsKey`判定、バッチ・メッセージング: `TestShot.java:77-78`の`assertContainsRequiredKeys`、エンティティバリデーション: `EntityTestSupport.java:269-276`の`containsAll`判定）も確認した | 要 | — | `isValidToken`・`forwardUri`の必須列を「必須」に修正し、「必須」の意味を1文で定義（STEP 1(a)(b)） |
| B-F01（確定） | 2 | B | must | ラウンド1で規約不在のため`decide`に回したL4相当の太字疑似見出し7箇所は、`ja/`配下全体の実測により決着した。`ja/application_framework/adaptors/lettuce_adaptor/redisstore_lettuce_adaptor.rst:4,20,41,48`・`ja/biz_samples/12/index.rst:4,8,59,87`に、本ページと同じ`=`→`-`→`~`→`^`の4階層の前例がある。一方、行頭太字の疑似見出しは`ja/application_framework/application_framework/libraries/`配下に0件だった | 要 | — | 7箇所を`^`のL4見出しに格上げし、`style.md`S-04にL4（`^`）を追加（根拠2件）（STEP 3） |
| D-4 | 2 | D | decide→対応 | `about/index.rst`（第1部・特徴3点目）の`:ref:`が`テストデータ変換ツール`のみで、`design.md`L74「本ページ作成後に見直すことを検討する」が未着手だった件 | 要 | — | ユーザー判断（2026-08-06）により`#9`是正に同梱。`about/index.rst`に`:ref:`テストデータの書き方\ <testdata_notation>`\`を追加（既存の`テストデータ変換ツール`参照は維持）し、`design.md`L74を実施済みの結論に書き換えた（STEP 4） |
| A-4 | 2 | A | must | `requestParams`・`responseResult`をtestShotsのカラムとして表に掲載していたが誤りだった。`AbstractHttpRequestTestTemplate.java:74,77`で定数として宣言され、`:336-339`で`getCachedListMap(sheetName, REQUEST_PARAMS_LIST_MAP)`のようにリテラルのままLIST_MAPのIDとして使われており、`getValue(testCaseParams, ...)`を経由するtestShotsカラムではない（対照として`context`は`:334-335`で`getValue`経由であり正真正銘のカラム）。誤りの由来は`TestCaseInfo.java:333-338`のJavadocの記載が実コードと一致していないためとみられる。ラウンド1の観点Aは出典間の突合のみを行い実装を確認していなかったため検出できなかった | 要 | — | `requestParams`・`responseResult`の行をカラム表から削除し、両者が読み込み単位内に置く予約IDを持つ`LIST_MAP`である旨を地の文に明記（STEP 1(c)） |
| A-5 | 2 | A | must | `expectedSearch`の説明が、検索結果をリクエストスコープから取得する際の既定キー`searchResult`を欠落していた。出典`current-0081`（削除前の`05_UnitTestGuide/02_RequestUnitTest/index.rst:99-193`）に記載があり、実装`TestCaseInfo.java:72`の`DEFAULT_SEARCH_RESULT_KEY = "searchResult"`、`setSearchResultKey`（`TestCaseInfo.java:123-125`）で変更可能なことも確認した | 要 | — | `expectedSearch`行の説明に既定キー`searchResult`である旨（`setSearchResultKey`で変更可能な旨を含む）を追記（STEP 2） |

### ラウンド2終了時点のまとめ

- `must`: A-3（確定）・B-F01（確定）・A-4・A-5、全て解消
- `decide`: D-4、ユーザー判断（2026-08-06）により`#9`是正に同梱、対応済み
- 本ラウンドで新規に検出した`note`はない。ラウンド1の`note`（D-3・D-5）は本書の対象外であり蒸し返さない
- Dockerフルビルド（`-a`）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ、本タスクによる新規警告は0件）を確認

## ラウンド3（`/rn:gm`による通し読み改善指示への対応）

`/rn:gm`「ここまでのFBを踏まえてページ全体を見直し、意味のある改善をする」を受け、著者自身によるページ全体の通し読みを実施。個別指摘への対症療法ではなく、独立した再読で見つけた欠陥を対象とする。

| 指摘ID | 区分 | 指摘内容 | 対応内容 |
|---|---|---|---|
| R3-1 | must | 「準備データ（SETUP_TABLE）を記述する」「期待値（EXPECTED_TABLE等）を記述する」「カラムを省略する」の3節にまたがり、「主キーカラムは省略できない」「省略カラムはデフォルト値扱い」の同じ事実が3回反復されていた | 「カラムを省略する」節の該当段落を、既出事実の反復から前2節への`前述のとおり`参照＋デフォルト値表への`次の表`参照に書き換え、反復を解消 |
| R3-2 | must | 「カラムを省略する」節冒頭のimportantが「データベースの検索結果の期待値」「登録系テスト」の両方に「カラムを省略できない」という同一の強さの表現を使っていたが、実装（`nablarch-testing`の`Assertion#assertMapEquals`、`actualAsString.equals(expected)`によるMap完全一致）を確認したところ、前者（`expectedSearch`等の`LIST_MAP`比較）のみが機械的な制約であり、後者（登録系テストの全カラム確認）は`EXPECTED_TABLE`が技術的には省略を許すベストプラクティス上の推奨であることが判明。同じ強さの表現では技術的制約と推奨が区別できず、読者が実際には存在しないバリデーションを想定しうる | `LIST_MAP`を名指しし:ref:リンクを追加のうえ機械的制約である理由（Map完全一致）を明記。登録系テストの記述は「推奨する。〜のためである」という本セッションの既定の言い回し（意図ベース）に書き換え、両者の性質の違いを明示 |
| R3-3 | must（ラウンド1 D-3の未対応分） | ラウンド1のD-3で記録のみとされていた「ウェブアプリのカラム表に`expectedStatusCode`が共通カラム表と重複掲載」を、本ラウンドで解消（他の処理方式の表に同種の重複がなく、通し読みで改めて冗長と判断） | ウェブアプリケーション用カラム表から`expectedStatusCode`の行を削除（共通カラム表の記載のみ残す） |

### ラウンド3終了時点のまとめ

- `must`: R3-1・R3-2・R3-3、全て解消
- `decide`・`note`の新規検出なし
- Dockerフルビルド（クリーン、`rm -rf _build`後再実行）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ、新規警告0件）を確認

## ラウンド4（ページの再構成）

作業指示は `ntf-doc-09-restructure.md`。**根拠はユーザー判断（2026-08-06）で、(a) 器の説明（構造）をL2として独立させる、
(b) 各L3を「共通 → 形式別」の順に統一する、の2点。** これに伴い未解消の`must`2件・`note`1件も同時に解消した。
本ラウンドは配置替え・順序変更が主であり、新しい主題内容は書き加えていない。
メタデータ側（`mapping.csv`・`vocabulary.md`・`design.md`・`style.md`）はコミット`db95c8b`で別途対応済み。

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R4-A | 4 | — | 構成変更 | 冒頭3セクション（配置・データブロック・グループID）は「どう書くか」ではなく「テストデータとは何でできているか」であり、第3部の共通アウトライン（機能概要＋使用方法）に引きずられて`使用方法`の中に埋もれていた | 要 | — | L2`テストデータの構造`を新設し3セクションを移設。`style.md` S-03例外2に従い体言止めへ改題（`テストクラスとテストデータの対応`・`データブロックとデータタイプ`・`グループIDによる使い分け`）。既存ラベル（`-file_structure`/`-data_block_types`/`-group_id`/`-usage`）は維持。ページ内リンク1箇所の文言のみ追随、`testdata_examples.rst`を指す4箇所は`#10`の担当範囲のため無変更 |
| R4-B | 4 | B | 構成変更 | `style.md` S-10規約3（形式別L4対はL3の末尾2つ）に対し、`テーブルのデータを記述する`だけが形式別L4を先頭に置き、その後ろに形式非依存の`準備データ`・`期待値`・`カラムを省略する`が並んでいた | 要 | — | L4を`準備データ`→`共通の準備データをまとめる`→`期待値`→`カラムを省略する`→`Excel形式の場合`→`YAML形式の場合`の順に並べ替え。ラウンド3のR3-1で整理した「前述のとおり」参照が壊れていないことを実読で確認（参照先の2節は新順でも前方にある） |
| R4-C | 4 | B | 構成変更（S-11） | Nabledgeの知識JSONはセクションをフラットに切りL3の`content`に配下L4の本文を含まないため、L3導入文が薄いとL3単独では情報がほぼ得られない。`テストケース一覧（testShots）を記述する`は導入文が1文も無く、見出しの直後がいきなりL4だった | 要 | — | `testShots`に導入文を新規追加（`testShots`の定義と、本体＋4処理方式＋コマンドライン引数というL4構成の要約）。他7つのL3にも配下構成を示す1文を追加。いずれも既存セクション内の記述の要約にとどめた。`値を特殊記法で記述する`と`コメント・マーカーカラム・空エントリを扱う`は追加不要と判断 |
| R4-1 | 4 | D | must | `テストデータを配置する`の`YAML形式の場合`末尾に、形式非依存の`important`（テストの実行順序）と`tip`（マスタデータの共通化）が置かれていた。Excel利用者は`Excel形式の場合`を読んで次へ進むため、このページで最も重い注意に到達しない（S-10規約1違反） | 要 | — | 接続文＋`important`＋`tip`を、当該L3の最初のL4より前へ移動。接続文の「ここまでのファイル構成を踏まえたうえで」は位置と整合しなくなるため「形式によらず」に変更。`important`・`tip`の本文は無変更 |
| R4-2 | 4 | D | must（R4-1と同型・新規検出） | R4-1と同じ構造の欠陥が他に5件あった。`YAML形式の場合`のL4見出しの後ろに形式非依存の本文がそのまま続いており、RSTの節構造上その本文は`YAML形式の場合`の一部である。作業前の実測で`LIST_MAP`33行・ファイル134行・メッセージング88行・特殊記法64行がこの状態にあり、Excel利用者は到達できなかった。ラウンド1〜3では「形式別L4が末尾2つ」という見出しの並びだけを確認しており本文の所属を見ていなかったため検出できていなかった | 要 | — | 5件すべてを最初のL4より前へ移動（`LIST_MAP`のID解決規則／ファイルの記法制約・エラーケース・ディレクティブ・データ型／メッセージングの同期応答送信4データタイプ・`errorMode:`／特殊記法のインタープリタ一覧・文字種・日付形式）。加えて`テーブルのデータを記述する`のデータ型制約（`SqlPStatement`）も同型として共通部へ移動（実装確認、下記R4-3の根拠と同じ）。移動に伴う指示語の反転2箇所（「これらの記法を実現するインタープリタは」→「特殊記法を実現する〜」、「主な特殊記法は、以下のとおりである」→「〜の書き方は、形式ごとに以下のとおりである」）を是正 |
| R4-3 | 4 | A | must（R4-2の付随判断） | `テーブルのデータを記述する`の`Excel形式の場合`にある「Excelファイルに記述できるカラムのデータ型には制約がある」は、出典（`current-0197`、削除前の`06_TestFWGuide/02_DbAccessTest.rst:551-554`）がExcel前提の文面だが、制約の実体はDB層にあり形式に依らない可能性があった | 要 | — | 実装で確認して決着。`nablarch-testing-yaml`の`YamlTestDataParser#getSetupTableData`はExcel側と同じ`nablarch.test.core.db.TableData`を返し（`YamlTableDataBuilder.java:5,74,95,119`）、INSERTを行う`TableData#insertData`（`TableData.java:137,141`）が`SqlPStatement`を使う。両形式共通の制約と確認できたため、文頭を「テストデータに記述できる」に改めL3共通部へ移動 |
| R4-4 | 4 | D | must | `テーブルのデータを記述する`の中でカラム名の行の番号が食い違っていた。L3導入文は「1行目に`データタイプ=値`、2行目にカラム名」、`Excel形式の場合`冒頭は「1行目にカラム名、2行目以降にデータ」。同じ小見出しの中でカラム名の行を1行目とも2行目とも書いていた。加えて「1行目／2行目／3行目」はExcelの行を前提とした表現であり、YAMLに行という概念がないにもかかわらず形式非依存の位置に置かれていた | 要 | — | データタイプ行を1行目とする数え方に統一。L3導入文からは行番号自体を除き「データタイプと識別子の値・カラム名・データ行という共通の構成」に改め、行の数え方の説明は`Excel形式の場合`の中へ移した（「1行目に「SETUP_TABLE=テーブル名」、2行目にカラム名、3行目以降にデータを記載する。」）。同節内の「ヘッダ行（2行目）」「データ行（3行目以降）」および`LIST_MAP`側の数え方はこの統一と一致するため無変更 |
| R4-5 | 4 | A | note | `共通の準備データをまとめる`が「`setUpDb`という名前の読み込み単位（Excel形式では`setUpDb`という名前のシート）」とだけ書いており、YAML形式での対応に触れていなかった。両形式の対比が本ページの前提である以上、YAMLの読者が判断できない | 要 | — | 実装で確認できたため追記した。`DbAccessTestSupport#setUpDb(String sheetName)`（`DbAccessTestSupport.java:165,175,182`）が`sheetName`をそのまま`getSetupTableData`へ渡し、`YamlTestDataParser`（`:108-112`）経由で`YamlLoader#buildFilePath`（`YamlLoader.java:42,72-83`）が`basePath + "/" + resourceName + ".yaml"`を組み立てる。よってYAMLでは「テストクラスに対応するディレクトリ配下の`setUpDb.yaml`というファイル」であると追記 |
| R4-6 | 4 | D | note | `メッセージングのデータを記述する`のL3導入文にある「フレームワーク制御ヘッダ以降のメッセージボディは、1行目がフィールド名称…4行目以降がデータ」も、R4-4と同じくExcelの行を前提とした数え方である | 不要 | 本ラウンドの是正指示は`テーブルのデータを記述する`に限定されており、かつメッセージボディの数え方は「フレームワーク制御ヘッダ以降」を起点とする別体系で、テーブルデータの数え方（データタイプ行=1行目）とは矛盾していない。指示範囲外の内容変更を避けた | 未対応（記録のみ。次ラウンド以降の検討対象） |
| R4-7 | 4 | D | note | `ファイルのデータを記述する`の「0バイトの空ファイル」のコードブロックが`SETUP_VARIABLE=work/members.csv`というExcel/テキスト形式の記法で書かれている。R4-2で`YAML形式の場合`から共通部へ移したため、共通部にExcel記法の例が置かれる形になった | 不要 | 作業前は`YAML形式の場合`の中にあり、より不整合だった（悪化はしていない）。YAML形式での空ファイル表現は出典・実装のいずれでも確認できず、書き足すと推測になるため文面を変更しなかった | 未対応（記録のみ） |

### ラウンド4終了時点のまとめ

- `must`: R4-1・R4-2・R4-3・R4-4、全て解消
- `note`: R4-5は実装確認のうえ追記して解消。R4-6・R4-7は記録のみ（対応不要と判断）
- 構成変更: R4-A（L2`テストデータの構造`の新設）・R4-B（`テーブルのデータを記述する`のL4並べ替え）・R4-C（L3導入文の充実）
- `decide`の新規検出なし
- ゲート5〜11・13の実行結果は `checks/task-09-restructure.md` に記録。Dockerフルビルド（`-a`）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ、新規警告0件）を確認

## ラウンド5（再構成に対する4観点レビューと指摘への対応）

ラウンド4の再構成（コミット`51f8020`）に対し、4観点（A網羅性 / Bトンマナ / C用語 / D整合性）のレビューを実施した。
RST側の`must`はコミット`ea4b5d3`で、規約・設計文書側（`design.md`・`style.md`）の`must`は本コミットで対応した。
再構成そのものを差し戻す指摘はなく、いずれも再構成に伴って生じた不整合、または再構成が露出させた既存の不備である。

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R5-1 | 5 | A | must | `メッセージングのデータを記述する`のL3導入文にある`no`の説明が実装と矛盾していた。実際には先頭列は読み込み対象外のラベル列であり、要求電文と応答電文の突合は`no`の値ではなく記述順で行われる | 要 | — | 実装を確認のうえ、先頭列がラベル列であり突合は記述順で行われる旨に書き換えた。あわせて`expectedMessages`の適用範囲を同期応答メッセージ受信に限定した（`ea4b5d3` F-1） |
| R5-2 | 5 | B | must | 共通部（形式別L4より前）に形式依存の記述が10箇所残っていた。`シート`・`セル`といったExcel前提の語や、YAMLのキー名を共通部で名指しする記述が該当する。ラウンド4のSTEP C（形式非依存の内容を共通部へ引き上げる）の際に、移した文の語彙を形式非依存に直していなかったことが原因 | 要 | — | 10箇所すべてを、当該形式のL4への移動または形式非依存語への言い換えで解消（`ea4b5d3` F-2） |
| R5-3 | 5 | B/D | must | (a)`メッセージングのデータを記述する`の`Excel形式の場合`冒頭で主語が欠落しYAML側と非対称だった。(b)`テストケース一覧（testShots）を記述する`の記載例リンクが最後のL4の中に閉じ込められており、他のL4を読む読者が到達できなかった。(c)L3導入文2件で配下L4の列挙に漏れがあり、列挙順が実配置と一致していなかった | 要 | — | (a)主語を補いYAML側と対称化、(b)記載例リンクをL3共通部へ移動、(c)漏れていたL4を追加し列挙順を実配置に一致させた（`ea4b5d3` F-3〜F-5） |
| R5-4 | 5 | C | must | `glossary.md` §8の正表記違反2件（`メッセージ同期送信`・`メッセージング処理`）が残っていた | 要 | — | `同期応答メッセージ送信`ほか正表記へ置換（`ea4b5d3` F-6） |
| R5-5 | 5 | D | must | `design.md` §4「テストデータの2ページ」の見出し文言一致規約が再構成に追随していなかった。`testdata_examples.rst`のL2見出し9件のうち2件（`データブロック（テストケース、準備データ、期待値）を記述する`・`グループIDでデータブロックを分ける`）は、再構成で`使用方法`から`テストデータの構造`へ移り体言止めへ改題されたため、規約が要求する「`使用方法`配下と同名」を満たさず規約の適用範囲からも外れた。規約が言及する`テストデータを配置する`という見出し名も改題により存在しない | 要 | — | `design.md` §4の当該規約を、(1)`使用方法`配下の各L3と同名の見出しを並べる、(2)`テストデータの構造`配下のうち記述例を持つ2節は改題後の名前で見出しを置く、(3)`テストクラスとテストデータの対応`は実例を持たないため見出しを置かない、の3点が読み取れる形に改めた。ページ間の`:ref:`はラベルで解決しており未定義参照0件を確認済みのため、RST側の変更は不要（`#10`で見出しを改題する際にリンク文言を合わせる） |
| R5-6 | 5 | D | must | `style.md` S-02が「第3部のページは機能概要 → 使用方法の順に並べる」と無条件に書いており、本ページの3セクション構成を例外として知らなかった。同じ`style.md`のS-03例外2は「`design.md` §4で定めた第3部の唯一の例外セクション」を前提にしており、S-02本体だけが例外を知らない片肺状態だった | 要 | — | S-02の第3部の項に、`design.md` §4と同内容の例外を1項追記した |
| R5-7 | 5 | B | must | `style.md` S-03例外1のスコープが不足していた。形式別L4対を「同一ページ内でセクションタイトルを重複させない」の対象外とするとだけ書いており、S-03本体のもう1つの規約「『使用方法』『拡張例』配下の小見出しは動詞終止形の『〜する』で終える」に`Excel形式の場合`・`YAML形式の場合`が違反したままだった（`使用方法`配下に10件）。このままでは残り33ページのレビューで指摘され続ける | 要 | — | 例外1に「〜する」形式の規約についても対象外である旨を明記した（形式名を示す固定の見出し文言であるため体言止めのままとする） |
| R5-8 | 5 | D | must | `style.md` S-03例外1がS-10規約3を「各L3に置くよう定める」と要約していたが、S-10規約3は「規約1に該当しない記述方法の説明がある場合にL4対を立てる」という条件付き規定であり、実態も10個のL3のうち8個にしか置かれていない | 要 | — | 例外1の文言を、条件付きの規定であること（および実態が10個中8個であること）が分かる形に直した |
| R5-9 | 5 | D | note | S-03例外1の根拠（`knowledge/index.md`にlevel 4が現れないため一意性の問題は生じない）は、S-11が述べる「ページ選定後のフェーズではページJSON全体が読まれるためL4も候補になる」という範囲をカバーしていない。厳密な矛盾ではないが、後段フェーズでは同名L4が16個並ぶ | 要 | — | 例外1の根拠に、後段フェーズの読者はページ本文に到達しており、ページ内の`.. contents::`目次では各L4が親L3の下にネストされるため位置で判別できる旨を追記した。ネストは`_build/html/development_tools/testing_framework/implementation/testdata_notation.html`の目次で実測して確認 |
| R5-10 | 5 | B/D | note | 新設L2`テストデータの構造`に導入文が無く見出しの直後がいきなりL3だった。タクソノミ図の直後に各項目をどのセクションで扱うかの案内が無く図とページ構成の対応が読み取れなかった。「以下では」で始まる定型が5箇所連続していた。ほか収集方法の重複記述 | 要 | — | 新設L2に導入文を追加、タクソノミ図の直後に扱いセクションを示す1文（境界注記）を追加、定型5箇所を既存解説書の調子に合わせて変化させ、重複記述を参照に置き換えた（`ea4b5d3` F-7〜F-9） |
| R5-X1 | 5 | D | 却下 | `checks/task-09-restructure.md`が未コミットである | 不要 | このファイルはコーディネーターがタスクのチェックオフ時にコミットする運用であり、実装担当の作業ツリーに未追跡で残っているのは正常な状態である | 未対応（本ラウンドでも未コミットのまま） |
| R5-X2 | 5 | A | 却下 | 空シート・0バイトファイルの扱いがRSTに無く、`input-0118`の網羅に漏れがある | 不要 | 前ラウンドで「低価値な自明情報」として削除する判断が済んでおり記録済み（`steering.md` `#9`の2026-08-06の記録(2)(3)） | 未対応 |
| R5-X3 | 5 | D | 却下 | `テストデータの構造`セクションに運用寄りの`important`/`tip`（テストの実行順序・マスタデータの共通化）が置かれている | 不要 | 作業指示`ntf-doc-09-restructure.md` STEP Cが「当該L3の最初のL4より前へ移すこと」と明示的に指示した配置であり、形式別L4に閉じ込められてExcel利用者が到達できない状態を解消するための意図的な配置である | 未対応 |
| R5-X4 | 5 | C | 却下 | `glossary.md`に`テストデータの構造`を追加すべき | 不要 | 本作業指示の禁止事項が`glossary.md`の変更を禁じている | 未対応（`#10`への申し送りとする。後述） |

### ラウンド5終了時点のまとめ

- `must`（RST側、コミット`ea4b5d3`）: R5-1・R5-2・R5-3・R5-4、全て解消
- `must`（規約・設計文書側、本コミット）: R5-5・R5-6・R5-7・R5-8、全て解消
- `note`: R5-9・R5-10、いずれも対応して解消
- 却下: R5-X1〜R5-X4（理由は上表）。R5-X4のみ`#10`への申し送りとする
- `style.md`の観点は11個のまま（S-01〜S-11）。本ラウンドの修正はすべて既存観点への追記である
- Dockerフルビルド（`-a`）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ、新規警告0件）、`:ref:`の未定義参照0件を確認済み

### `#10`（テストデータの記載例）への申し送り

1. **`testdata_examples.rst`のL2見出し2件を改題する。** `データブロック（テストケース、準備データ、期待値）を記述する` → `データブロックとデータタイプ`、`グループIDでデータブロックを分ける` → `グループIDによる使い分け`。`design.md` §4の見出し文言一致規約（R5-5で改訂）に合わせるため。あわせて`testdata_notation.rst`側のリンク文言も合わせる（`:ref:`のラベル参照自体は現状で正しく解決しており、変更が要るのはリンクの表示文言のみ）
2. **`glossary.md` §5.13に`テストデータの構造`を掲載するかを判断する。** §3の掲載基準2「`design.md`が章・セクション名として使う用語」に該当するようになったため（R5-X4）
3. **`style.md` S-10規約3の見出しレベル表現を見直す。** 規約3は形式別の対を「L4見出し」と明記しているが、`テストデータの記載例`は「使用方法」ラッパーを持たず見出しをL2として並べる構成であるため、形式別の分割は**L3**に落ちる。`#10`着手前に、規約3を見出しレベルの相対表現（「1段下の見出し」等）に改めるか、記載例ページの例外を明記するかを決める必要がある
