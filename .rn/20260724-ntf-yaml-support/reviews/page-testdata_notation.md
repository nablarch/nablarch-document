# レビュー記録: `テストデータの書き方`（`implementation/testdata_notation.rst`）

対象 `mapping_id`: `dest_page=テストデータの書き方` の140行（使用方法139・機能概要1、`DROP`なし）。

4観点（A網羅性 / Bトンマナ / C用語 / D整合性）を、それぞれ別のサブエージェントで実施した。プロンプトには
「実測コマンドで裏付けよ」「検証スクリプトを正解として使わず独立に組め」「敵対的にレビューせよ」の3点を
含めた。ラウンド1のみで、`must` は全件解消・`decide` はユーザーレビューに引き継ぐ。

## ラウンド1

| 指摘ID | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|
| A-1 | A | must | `input-0160` 由来。可変長ファイルの有効ディレクティブキーを「8個」と記載していたが、直後の一覧表は9個（`file-type`/`text-encoding`/`record-separator`/`field-separator`/`quoting-delimiter`/`ignore-blank-lines`/`requires-title`/`max-record-length`/`title-record-type-name`）だった | 要 | — | 「8個」→「9個」に修正 |
| A-2 | A | must | `input-0141` 由来。`errorMode:` による障害系テストが機能する範囲（応答電文・モックアップクラス経由に限る）と、`RequestTestingSendSyncSupport`（`GroupMessageParser`）経路では機能しないという制約が欠落していた | 要 | — | `errorMode` 一覧表の直後に `important` を追加し、適用範囲と非対応経路を明記 |
| A-3 | A | must→decide | `input-0101`/`input-0102`（ウェブアプリのtestShots必須カラム）は `isValidToken`/`forwardUri`/`context` を必須、`requestParams` を任意としているが、旧current側出典（`current-0081`、`index.rst` の表）は `isValidToken`/`forwardUri` を必須マーク無し（任意相当）、`requestParams` は同表に無く別表（別途「必須記載」と地の文で案内）としている。成果物は旧current側の区分に従った。2つの出典が「必須」の割当で食い違っており、ドキュメント同士の突合では正誤を決定できない | 要判断 | 出典間の対立でありdesign.md 11.8「機械検証できないもの」に該当しない新種の対立（実装未確認）。決定には`HttpRequestTestSupport`の実装確認が要る | ユーザーレビューに引き継ぐ（後述「decide」参照） |
| B-F01 | B | decide | L3見出し配下の細分に太字文（「〜する。」形式）を7箇所使用。`style.md` S-04はL1/L2/L3の3階層のみ定義、FW解説書コーパスにもこの用法の前例なし | 要判断 | style.mdに規約が無いための設計判断 | ユーザーレビューに引き継ぐ |
| B-F02〜F10 | B | must | 出典9箇所（`input-0126`カラム省略・`06_TestFWGuide/01_Abstract.rst`複数箇所等）の一文がほぼそのまま（語順・接続・修飾関係一致）残っていた。詳細は各指摘参照 | 要 | — | 9箇所すべて常体で言い換え。主キー省略・FK省略・DELETE挙動・EXPECTED_TABLE省略時挙動・セル書式の準備・空行の`""`記法・DATE型タイムゾーン依存・forwardUri・コメント機能導入文 |
| B(他8観点) | B | — | S-01〜S-03, S-05〜S-08は違反なし | 不要 | 確認済み・問題なし | — |
| T-01 | C | must | L658「メッセージ同期送信・HTTPメッセージ同期送信」の語順が`glossary.md`の正表記（「同期応答メッセージ送信」「HTTPメッセージ送信」）と不一致。同じページの他箇所（L604・L607）は正しい語順だった | 要 | — | 正しい語順に修正 |
| T-02 | C | must | 「セクション」が3箇所（旧L319・L701・L704）でデータブロックの意味に誤用。`glossary.md` §8は「テストデータの単位を指す場合は`データブロック`に置換」と規定 | 要 | — | 3箇所とも「データブロック」に置換 |
| T-03 | C | decide | L1068「電文ヘッダ」「電文ボディ」が、同ページ他所（5箇所以上）で使う正表記「フレームワーク制御ヘッダ」「メッセージボディ」と不統一。`glossary.md`未収録語のため機械的な正表記違反ではない | 要 | ページ内一貫性のための編集判断として妥当性が高く、対応するリスクが低いと判断し著者判断で修正 | 「電文ヘッダ」「電文ボディ」の呼称を削除し「フレームワーク制御ヘッダ」「メッセージボディ側」に統一 |
| D-1 | D | must | L435「``SETUP_TABLES``」（複数形）が、ページ内の14種のデータタイプ一覧（``SETUP_TABLE``単数形のみ）と矛盾。出典`current-0122`（`rest.rst:116`）を確認したところ、現行解説書の全域中でも1箇所のみの表記であり、他に同名の別データタイプとしての言及が無いことを`grep`で確認した | 要 | — | `SETUP_TABLES`→`SETUP_TABLE`に統一（現行解説書側の表記揺れと判断） |
| D-2 | D | decide | 見出し「テストデータファイルの構造を理解する」配下に、テスト独立性・マスタデータ再利用という異質な内容（旧L126-132）が混在し、見出しの範囲を超えていた | 要判断 | 内容の追加・削除を伴わない見出し分割の範囲内であり、著者判断で対応可能と判断 | 「テストの独立性を保つ」というL3見出しを新設し、該当2段落をそちらへ分離 |
| D-3 | D | note | ウェブアプリ用カラム表に`expectedStatusCode`が共通カラム表と重複掲載されていた | 不要 | 実害が小さく、他の処理方式の表には同種の重複が無い局所的な冗長のため記録のみに留める | 未対応（記録のみ） |
| D-4 | D | decide | `about/index.rst`（第1部・特徴3点目）の`:ref:`は現状 `テストデータ変換ツール` のみを指しており、design.md 74行目が「本ページ（テストデータの書き方）作成後に参照を見直すことを検討する」としていた宿題が未着手 | 要判断 | 既に承認済みの第1部ページへの変更を伴うため、著者判断でなくユーザー判断が必要 | ユーザーレビューに引き継ぐ |
| D-5 | D | note | 「外部化」の事実が`about/index.rst`（特徴2点目）と本ページ冒頭の両方で異なる理由づけとともに説明されている | 不要 | design.mdが定める役割分担（外部化の事実は第1部、独立性は第3部）の範囲内であり、致命的な重複ではないため記録のみに留める | 未対応（記録のみ） |

## ラウンド1終了時点のまとめ

- `must`: A-1・A-2・B-F02〜F10（9件）・T-01・T-02・D-1、全て解消。Dockerフルビルド（`-a`）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ）を確認
- `decide`（3件、著者判断で解消したT-03・D-2を除く）: A-3（必須/任意区分の出典対立）・B-F01（L4相当の太字疑似見出しパターン）・D-4（`about/index.rst`特徴3点目の参照見直し）。ユーザーレビューで判断を仰ぐ
- `note`（2件）: D-3・D-5。対応不要、記録のみ

## ラウンド2（差し戻し是正）

`#9`はuser reviewで差し戻し。是正指示は `ntf-doc-09-fix.md`。ラウンド1の`decide`3件のうちA-3・B-F01は実物確認で決着し、
D-4はユーザー判断で`#9`是正に同梱した。加えて観点Aが検出できていなかった網羅性の欠落2件（`requestParams`/`responseResult`、
`searchResult`）を新規`must`として記録する。以下は参照したコミット `e21bf67`（`nablarch/nablarch-testing`の`main`、
`nablarch-testing` 2.2.0、`release-6u2`マージ済み）に基づく。**同リポジトリに`6u3`のブランチ・タグは存在せず
（`git ls-remote origin`で確認済み）、`6u3`との差分は未確認である。**

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| A-3（確定） | 2 | A | must | ラウンド1で出典対立のため`decide`に回した`isValidToken`/`forwardUri`の必須区分は、実装確認により決着した。`TestCaseInfo#isValidToken()`（`TestCaseInfo.java:482-484`）は`getValue`経由で読み、呼び出し元`AbstractHttpRequestTestTemplate#executeTestCase:257`は条件分岐の外にあり全テストケースで必ず評価される。`TestCaseInfo#getExpectedForwardUri()`（`TestCaseInfo.java:237-239`）も同じく`getValue`経由で読み、`assertForwardUri`（`AbstractHttpRequestTestTemplate.java:554-557`）は`assertAll:464`から無条件に呼ばれる。全処理方式で「必須」とはカラム自体の存在を指し値は空でよいこと（ウェブアプリケーション: `TestCaseInfo.java:443-448`の`containsKey`判定、バッチ・メッセージング: `TestShot.java:77-78`の`assertContainsRequiredKeys`、エンティティバリデーション: `EntityTestSupport.java:269-276`の`containsAll`判定）も確認した | 要 | — | `isValidToken`・`forwardUri`の必須列を「必須」に修正し、「必須」の意味を1文で定義（STEP 1(a)(b)） |
| B-F01（確定） | 2 | B | must | ラウンド1で規約不在のため`decide`に回したL4相当の太字疑似見出し7箇所は、`ja/`配下全体の実測により決着した。`ja/application_framework/adaptors/lettuce_adaptor/redisstore_lettuce_adaptor.rst:4,20,41,48`・`ja/biz_samples/12/index.rst:4,8,59,87`に、本ページと同じ`=`→`-`→`~`→`^`の4階層の前例がある。一方、行頭太字の疑似見出しは`ja/application_framework/application_framework/libraries/`配下に0件だった | 要 | — | 7箇所を`^`のL4見出しに格上げし、`style.md`S-04にL4（`^`）を追加（根拠2件）（STEP 3） |
| D-4 | 2 | D | decide→対応 | `about/index.rst`（第1部・特徴3点目）の`:ref:`が`テストデータ変換ツール`のみで、`design.md`L74「本ページ作成後に見直すことを検討する」が未着手だった件 | 要 | — | ユーザー判断（2026-08-06）により`#9`是正に同梱。`about/index.rst`に`:ref:`テストデータの書き方\ <testdata_notation>`\`を追加（既存の`テストデータ変換ツール`参照は維持）し、`design.md`L74を実施済みの結論に書き換えた（STEP 4） |
| A-4 | 2 | A | must | `requestParams`・`responseResult`をtestShotsのカラムとして表に掲載していたが誤りだった。`AbstractHttpRequestTestTemplate.java:74,77`で定数として宣言され、`:336-339`で`getCachedListMap(sheetName, REQUEST_PARAMS_LIST_MAP)`のようにリテラルのままLIST_MAPのIDとして使われており、`getValue(testCaseParams, ...)`を経由するtestShotsカラムではない（対照として`context`は`:334-335`で`getValue`経由であり正真正銘のカラム）。誤りの由来は`TestCaseInfo.java:333-338`のJavadocの記載が実コードと一致していないためとみられる。ラウンド1の観点Aは出典間の突合のみを行い実装を確認していなかったため検出できなかった | 要 | — | `requestParams`・`responseResult`の行をカラム表から削除し、両者が読み込み単位内に置く予約IDを持つ`LIST_MAP`である旨を地の文に明記（STEP 1(c)） |
| A-5 | 2 | A | must | `expectedSearch`の説明が、検索結果をリクエストスコープから取得する際の既定キー`searchResult`を欠落していた。出典`current-0081`（削除前の`05_UnitTestGuide/02_RequestUnitTest/index.rst:99-193`）に記載があり、実装`TestCaseInfo.java:72`の`DEFAULT_SEARCH_RESULT_KEY = "searchResult"`、`setSearchResultKey`（`TestCaseInfo.java:123-125`）で変更可能なことも確認した | 要 | — | `expectedSearch`行の説明に既定キー`searchResult`である旨（`setSearchResultKey`で変更可能な旨を含む）を追記（STEP 2） |

### ラウンド2終了時点のまとめ

- `must`: A-3（確定）・B-F01（確定）・A-4・A-5、全て解消
- `decide`: D-4、ユーザー判断（2026-08-06）により`#9`是正に同梱、対応済み
- 本ラウンドで新規に検出した`note`はない。ラウンド1の`note`（D-3・D-5）は本書の対象外であり蒸し返さない
- Dockerフルビルド（`-a`）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ、本タスクによる新規警告は0件）を確認

## ラウンド3（`/rn:gm`による通し読み改善指示への対応）

`/rn:gm`「ここまでのFBを踏まえてページ全体を見直し、意味のある改善をする」を受け、著者自身によるページ全体の通し読みを実施。個別指摘への対症療法ではなく、独立した再読で見つけた欠陥を対象とする。

| 指摘ID | 区分 | 指摘内容 | 対応内容 |
|---|---|---|---|
| R3-1 | must | 「準備データ（SETUP_TABLE）を記述する」「期待値（EXPECTED_TABLE等）を記述する」「カラムを省略する」の3節にまたがり、「主キーカラムは省略できない」「省略カラムはデフォルト値扱い」の同じ事実が3回反復されていた | 「カラムを省略する」節の該当段落を、既出事実の反復から前2節への`前述のとおり`参照＋デフォルト値表への`次の表`参照に書き換え、反復を解消 |
| R3-2 | must | 「カラムを省略する」節冒頭のimportantが「データベースの検索結果の期待値」「登録系テスト」の両方に「カラムを省略できない」という同一の強さの表現を使っていたが、実装（`nablarch-testing`の`Assertion#assertMapEquals`、`actualAsString.equals(expected)`によるMap完全一致）を確認したところ、前者（`expectedSearch`等の`LIST_MAP`比較）のみが機械的な制約であり、後者（登録系テストの全カラム確認）は`EXPECTED_TABLE`が技術的には省略を許すベストプラクティス上の推奨であることが判明。同じ強さの表現では技術的制約と推奨が区別できず、読者が実際には存在しないバリデーションを想定しうる | `LIST_MAP`を名指しし:ref:リンクを追加のうえ機械的制約である理由（Map完全一致）を明記。登録系テストの記述は「推奨する。〜のためである」という本セッションの既定の言い回し（意図ベース）に書き換え、両者の性質の違いを明示 |
| R3-3 | must（ラウンド1 D-3の未対応分） | ラウンド1のD-3で記録のみとされていた「ウェブアプリのカラム表に`expectedStatusCode`が共通カラム表と重複掲載」を、本ラウンドで解消（他の処理方式の表に同種の重複がなく、通し読みで改めて冗長と判断） | ウェブアプリケーション用カラム表から`expectedStatusCode`の行を削除（共通カラム表の記載のみ残す） |

### ラウンド3終了時点のまとめ

- `must`: R3-1・R3-2・R3-3、全て解消
- `decide`・`note`の新規検出なし
- Dockerフルビルド（クリーン、`rm -rf _build`後再実行）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ、新規警告0件）を確認

## ラウンド4（ページの再構成）

作業指示は `ntf-doc-09-restructure.md`。**根拠はユーザー判断（2026-08-06）で、(a) 器の説明（構造）をL2として独立させる、
(b) 各L3を「共通 → 形式別」の順に統一する、の2点。** これに伴い未解消の`must`2件・`note`1件も同時に解消した。
本ラウンドは配置替え・順序変更が主であり、新しい主題内容は書き加えていない。
メタデータ側（`mapping.csv`・`vocabulary.md`・`design.md`・`style.md`）はコミット`db95c8b`で別途対応済み。

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R4-A | 4 | — | 構成変更 | 冒頭3セクション（配置・データブロック・グループID）は「どう書くか」ではなく「テストデータとは何でできているか」であり、第3部の共通アウトライン（機能概要＋使用方法）に引きずられて`使用方法`の中に埋もれていた | 要 | — | L2`テストデータの構造`を新設し3セクションを移設。`style.md` S-03例外2に従い体言止めへ改題（`テストクラスとテストデータの対応`・`データブロックとデータタイプ`・`グループIDによる使い分け`）。既存ラベル（`-file_structure`/`-data_block_types`/`-group_id`/`-usage`）は維持。ページ内リンク1箇所の文言のみ追随、`testdata_examples.rst`を指す4箇所は`#10`の担当範囲のため無変更 |
| R4-B | 4 | B | 構成変更 | `style.md` S-10規約3（形式別L4対はL3の末尾2つ）に対し、`テーブルのデータを記述する`だけが形式別L4を先頭に置き、その後ろに形式非依存の`準備データ`・`期待値`・`カラムを省略する`が並んでいた | 要 | — | L4を`準備データ`→`共通の準備データをまとめる`→`期待値`→`カラムを省略する`→`Excel形式の場合`→`YAML形式の場合`の順に並べ替え。ラウンド3のR3-1で整理した「前述のとおり」参照が壊れていないことを実読で確認（参照先の2節は新順でも前方にある） |
| R4-C | 4 | B | 構成変更（S-11） | Nabledgeの知識JSONはセクションをフラットに切りL3の`content`に配下L4の本文を含まないため、L3導入文が薄いとL3単独では情報がほぼ得られない。`テストケース一覧（testShots）を記述する`は導入文が1文も無く、見出しの直後がいきなりL4だった | 要 | — | `testShots`に導入文を新規追加（`testShots`の定義と、本体＋4処理方式＋コマンドライン引数というL4構成の要約）。他7つのL3にも配下構成を示す1文を追加。いずれも既存セクション内の記述の要約にとどめた。`値を特殊記法で記述する`と`コメント・マーカーカラム・空エントリを扱う`は追加不要と判断 |
| R4-1 | 4 | D | must | `テストデータを配置する`の`YAML形式の場合`末尾に、形式非依存の`important`（テストの実行順序）と`tip`（マスタデータの共通化）が置かれていた。Excel利用者は`Excel形式の場合`を読んで次へ進むため、このページで最も重い注意に到達しない（S-10規約1違反） | 要 | — | 接続文＋`important`＋`tip`を、当該L3の最初のL4より前へ移動。接続文の「ここまでのファイル構成を踏まえたうえで」は位置と整合しなくなるため「形式によらず」に変更。`important`・`tip`の本文は無変更 |
| R4-2 | 4 | D | must（R4-1と同型・新規検出） | R4-1と同じ構造の欠陥が他に5件あった。`YAML形式の場合`のL4見出しの後ろに形式非依存の本文がそのまま続いており、RSTの節構造上その本文は`YAML形式の場合`の一部である。作業前の実測で`LIST_MAP`33行・ファイル134行・メッセージング88行・特殊記法64行がこの状態にあり、Excel利用者は到達できなかった。ラウンド1〜3では「形式別L4が末尾2つ」という見出しの並びだけを確認しており本文の所属を見ていなかったため検出できていなかった | 要 | — | 5件すべてを最初のL4より前へ移動（`LIST_MAP`のID解決規則／ファイルの記法制約・エラーケース・ディレクティブ・データ型／メッセージングの同期応答送信4データタイプ・`errorMode:`／特殊記法のインタープリタ一覧・文字種・日付形式）。加えて`テーブルのデータを記述する`のデータ型制約（`SqlPStatement`）も同型として共通部へ移動（実装確認、下記R4-3の根拠と同じ）。移動に伴う指示語の反転2箇所（「これらの記法を実現するインタープリタは」→「特殊記法を実現する〜」、「主な特殊記法は、以下のとおりである」→「〜の書き方は、形式ごとに以下のとおりである」）を是正 |
| R4-3 | 4 | A | must（R4-2の付随判断） | `テーブルのデータを記述する`の`Excel形式の場合`にある「Excelファイルに記述できるカラムのデータ型には制約がある」は、出典（`current-0197`、削除前の`06_TestFWGuide/02_DbAccessTest.rst:551-554`）がExcel前提の文面だが、制約の実体はDB層にあり形式に依らない可能性があった | 要 | — | 実装で確認して決着。`nablarch-testing-yaml`の`YamlTestDataParser#getSetupTableData`はExcel側と同じ`nablarch.test.core.db.TableData`を返し（`YamlTableDataBuilder.java:5,74,95,119`）、INSERTを行う`TableData#insertData`（`TableData.java:137,141`）が`SqlPStatement`を使う。両形式共通の制約と確認できたため、文頭を「テストデータに記述できる」に改めL3共通部へ移動 |
| R4-4 | 4 | D | must | `テーブルのデータを記述する`の中でカラム名の行の番号が食い違っていた。L3導入文は「1行目に`データタイプ=値`、2行目にカラム名」、`Excel形式の場合`冒頭は「1行目にカラム名、2行目以降にデータ」。同じ小見出しの中でカラム名の行を1行目とも2行目とも書いていた。加えて「1行目／2行目／3行目」はExcelの行を前提とした表現であり、YAMLに行という概念がないにもかかわらず形式非依存の位置に置かれていた | 要 | — | データタイプ行を1行目とする数え方に統一。L3導入文からは行番号自体を除き「データタイプと識別子の値・カラム名・データ行という共通の構成」に改め、行の数え方の説明は`Excel形式の場合`の中へ移した（「1行目に「SETUP_TABLE=テーブル名」、2行目にカラム名、3行目以降にデータを記載する。」）。同節内の「ヘッダ行（2行目）」「データ行（3行目以降）」および`LIST_MAP`側の数え方はこの統一と一致するため無変更 |
| R4-5 | 4 | A | note | `共通の準備データをまとめる`が「`setUpDb`という名前の読み込み単位（Excel形式では`setUpDb`という名前のシート）」とだけ書いており、YAML形式での対応に触れていなかった。両形式の対比が本ページの前提である以上、YAMLの読者が判断できない | 要 | — | 実装で確認できたため追記した。`DbAccessTestSupport#setUpDb(String sheetName)`（`DbAccessTestSupport.java:165,175,182`）が`sheetName`をそのまま`getSetupTableData`へ渡し、`YamlTestDataParser`（`:108-112`）経由で`YamlLoader#buildFilePath`（`YamlLoader.java:42,72-83`）が`basePath + "/" + resourceName + ".yaml"`を組み立てる。よってYAMLでは「テストクラスに対応するディレクトリ配下の`setUpDb.yaml`というファイル」であると追記 |
| R4-6 | 4 | D | note | `メッセージングのデータを記述する`のL3導入文にある「フレームワーク制御ヘッダ以降のメッセージボディは、1行目がフィールド名称…4行目以降がデータ」も、R4-4と同じくExcelの行を前提とした数え方である | 不要 | 本ラウンドの是正指示は`テーブルのデータを記述する`に限定されており、かつメッセージボディの数え方は「フレームワーク制御ヘッダ以降」を起点とする別体系で、テーブルデータの数え方（データタイプ行=1行目）とは矛盾していない。指示範囲外の内容変更を避けた | 未対応（記録のみ。次ラウンド以降の検討対象） |
| R4-7 | 4 | D | note | `ファイルのデータを記述する`の「0バイトの空ファイル」のコードブロックが`SETUP_VARIABLE=work/members.csv`というExcel/テキスト形式の記法で書かれている。R4-2で`YAML形式の場合`から共通部へ移したため、共通部にExcel記法の例が置かれる形になった | 不要 | 作業前は`YAML形式の場合`の中にあり、より不整合だった（悪化はしていない）。YAML形式での空ファイル表現は出典・実装のいずれでも確認できず、書き足すと推測になるため文面を変更しなかった | 未対応（記録のみ） |

### ラウンド4終了時点のまとめ

- `must`: R4-1・R4-2・R4-3・R4-4、全て解消
- `note`: R4-5は実装確認のうえ追記して解消。R4-6・R4-7は記録のみ（対応不要と判断）
- 構成変更: R4-A（L2`テストデータの構造`の新設）・R4-B（`テーブルのデータを記述する`のL4並べ替え）・R4-C（L3導入文の充実）
- `decide`の新規検出なし
- ゲート5〜11・13の実行結果は `checks/task-09-restructure.md` に記録。Dockerフルビルド（`-a`）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ、新規警告0件）を確認

## ラウンド5（再構成に対する4観点レビューと指摘への対応）

ラウンド4の再構成（コミット`51f8020`）に対し、4観点（A網羅性 / Bトンマナ / C用語 / D整合性）のレビューを実施した。
RST側の`must`はコミット`ea4b5d3`で、規約・設計文書側（`design.md`・`style.md`）の`must`は本コミットで対応した。
再構成そのものを差し戻す指摘はなく、いずれも再構成に伴って生じた不整合、または再構成が露出させた既存の不備である。

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R5-1 | 5 | A | must | `メッセージングのデータを記述する`のL3導入文にある`no`の説明が実装と矛盾していた。実際には先頭列は読み込み対象外のラベル列であり、要求電文と応答電文の突合は`no`の値ではなく記述順で行われる | 要 | — | 実装を確認のうえ、先頭列がラベル列であり突合は記述順で行われる旨に書き換えた。あわせて`expectedMessages`の適用範囲を同期応答メッセージ受信に限定した（`ea4b5d3` F-1） |
| R5-2 | 5 | B | must | 共通部（形式別L4より前）に形式依存の記述が10箇所残っていた。`シート`・`セル`といったExcel前提の語や、YAMLのキー名を共通部で名指しする記述が該当する。ラウンド4のSTEP C（形式非依存の内容を共通部へ引き上げる）の際に、移した文の語彙を形式非依存に直していなかったことが原因 | 要 | — | **【ラウンド6で訂正】** `ea4b5d3` F-2 では、レビューが名指しした10箇所を移動または形式非依存語への言い換えで対応した。しかし対応範囲がレビューの名指し分に限られており、**同型の未解消箇所が複数残っていた**（ラウンド6の R6-1〜R6-3 として再検出）。「10箇所すべて解消」は正しいが「共通部の形式依存記述がすべて解消した」は誤りであり、機械的な全件走査による是正は `d8089aa`（ラウンド6）で行った |
| R5-3 | 5 | B/D | must | (a)`メッセージングのデータを記述する`の`Excel形式の場合`冒頭で主語が欠落しYAML側と非対称だった。(b)`テストケース一覧（testShots）を記述する`の記載例リンクが最後のL4の中に閉じ込められており、他のL4を読む読者が到達できなかった。(c)L3導入文2件で配下L4の列挙に漏れがあり、列挙順が実配置と一致していなかった | 要 | — | (a)主語を補いYAML側と対称化、(b)記載例リンクをL3共通部へ移動、(c)漏れていたL4を追加し列挙順を実配置に一致させた（`ea4b5d3` F-3〜F-5） |
| R5-4 | 5 | C | must | `glossary.md` §8の正表記違反2件（`メッセージ同期送信`・`メッセージング処理`）が残っていた | 要 | — | **【ラウンド6で訂正】** §8違反として解消したのは**1件（`メッセージ同期送信` → `同期応答メッセージ送信`）**である。`メッセージング処理` の方は、§8の適用条件（MOMを指すことが文脈から確定できる場合に限り `MOMによるメッセージング` と書く）が成立しない箇所であり、正表記へは置換していない。実際には第3の表現 `メッセージングのテスト` を新造した（現行 RST の「同期応答メッセージ受信・応答不要メッセージ受信などのメッセージングのテストでは」）。結果は妥当（禁止語を使っておらず、MOMと誤って断定してもいない）だが、記録上「§8違反2件を正表記へ置換」としたのは誤りである |
| R5-5 | 5 | D | must | `design.md` §4「テストデータの2ページ」の見出し文言一致規約が再構成に追随していなかった。`testdata_examples.rst`のL2見出し9件のうち2件（`データブロック（テストケース、準備データ、期待値）を記述する`・`グループIDでデータブロックを分ける`）は、再構成で`使用方法`から`テストデータの構造`へ移り体言止めへ改題されたため、規約が要求する「`使用方法`配下と同名」を満たさず規約の適用範囲からも外れた。規約が言及する`テストデータを配置する`という見出し名も改題により存在しない | 要 | — | `design.md` §4の当該規約を、(1)`使用方法`配下の各L3と同名の見出しを並べる、(2)`テストデータの構造`配下のうち記述例を持つ2節は改題後の名前で見出しを置く、(3)`テストクラスとテストデータの対応`は実例を持たないため見出しを置かない、の3点が読み取れる形に改めた。ページ間の`:ref:`はラベルで解決しており未定義参照0件を確認済みのため、RST側の変更は不要（`#10`で見出しを改題する際にリンク文言を合わせる）。**【ラウンド6で追記】この対応は、作業指示`ntf-doc-09-restructure.md` STEP A-4（L94）の「`design.md` §4「テストデータの2ページ」にある`テストデータの記載例`の見出し文言一致の規約は変更しない」という明示的禁止を上書きしたものである。** 上書きした理由は、同じ作業指示の STEP A-2 が3見出しの改題を指示しており、その結果 L94 が前提としていた「記載例側の見出しは`使用方法`配下と一致していればよい」という状態が崩れたためである。改題後は9見出しのうち2件が`使用方法`配下に対応先を持たなくなり、規約が名指しする`テストデータを配置する`という見出し名も消滅した。禁止を守ると`design.md`が実態と矛盾したまま残るため、指示内の他STEPとの整合を優先して上書きした |
| R5-6 | 5 | D | must | `style.md` S-02が「第3部のページは機能概要 → 使用方法の順に並べる」と無条件に書いており、本ページの3セクション構成を例外として知らなかった。同じ`style.md`のS-03例外2は「`design.md` §4で定めた第3部の唯一の例外セクション」を前提にしており、S-02本体だけが例外を知らない片肺状態だった | 要 | — | S-02の第3部の項に、`design.md` §4と同内容の例外を1項追記した |
| R5-7 | 5 | B | must | `style.md` S-03例外1のスコープが不足していた。形式別L4対を「同一ページ内でセクションタイトルを重複させない」の対象外とするとだけ書いており、S-03本体のもう1つの規約「『使用方法』『拡張例』配下の小見出しは動詞終止形の『〜する』で終える」に`Excel形式の場合`・`YAML形式の場合`が違反したままだった（`使用方法`配下に10件）。このままでは残り33ページのレビューで指摘され続ける | 要 | — | 例外1に「〜する」形式の規約についても対象外である旨を明記した（形式名を示す固定の見出し文言であるため体言止めのままとする） |
| R5-8 | 5 | D | must | `style.md` S-03例外1がS-10規約3を「各L3に置くよう定める」と要約していたが、S-10規約3は「規約1に該当しない記述方法の説明がある場合にL4対を立てる」という条件付き規定であり、実態も10個のL3のうち8個にしか置かれていない | 要 | — | 例外1の文言を、条件付きの規定であること（および実態が10個中8個であること）が分かる形に直した |
| R5-9 | 5 | D | note | S-03例外1の根拠（`knowledge/index.md`にlevel 4が現れないため一意性の問題は生じない）は、S-11が述べる「ページ選定後のフェーズではページJSON全体が読まれるためL4も候補になる」という範囲をカバーしていない。厳密な矛盾ではないが、後段フェーズでは同名L4が16個並ぶ | 要 | — | 例外1の根拠に、後段フェーズの読者はページ本文に到達しており、ページ内の`.. contents::`目次では各L4が親L3の下にネストされるため位置で判別できる旨を追記した。ネストは`_build/html/development_tools/testing_framework/implementation/testdata_notation.html`の目次で実測して確認 |
| R5-10 | 5 | B/D | note | 新設L2`テストデータの構造`に導入文が無く見出しの直後がいきなりL3だった。タクソノミ図の直後に各項目をどのセクションで扱うかの案内が無く図とページ構成の対応が読み取れなかった。「以下では」で始まる定型が5箇所連続していた。ほか収集方法の重複記述 | 要 | — | 新設L2に導入文を追加、タクソノミ図の直後に扱いセクションを示す1文（境界注記）を追加、定型5箇所を既存解説書の調子に合わせて変化させ、重複記述を参照に置き換えた（`ea4b5d3` F-7〜F-9） |
| R5-X1 | 5 | D | 却下 | `checks/task-09-restructure.md`が未コミットである | 不要 | このファイルはコーディネーターがタスクのチェックオフ時にコミットする運用であり、実装担当の作業ツリーに未追跡で残っているのは正常な状態である | 未対応（本ラウンドでも未コミットのまま） |
| R5-X2 | 5 | A | 却下 | 空シート・0バイトファイルの扱いがRSTに無く、`input-0118`の網羅に漏れがある | 不要 | 前ラウンドで「低価値な自明情報」として削除する判断が済んでおり記録済み（`steering.md` `#9`の2026-08-06の記録(2)(3)） | 未対応 |
| R5-X3 | 5 | D | 却下 | `テストデータの構造`セクションに運用寄りの`important`/`tip`（テストの実行順序・マスタデータの共通化）が置かれている | 不要 | 作業指示`ntf-doc-09-restructure.md` STEP Cが「当該L3の最初のL4より前へ移すこと」と明示的に指示した配置であり、形式別L4に閉じ込められてExcel利用者が到達できない状態を解消するための意図的な配置である | 未対応 |
| R5-X4 | 5 | C | 却下 | `glossary.md`に`テストデータの構造`を追加すべき | 不要 | 本作業指示の禁止事項が`glossary.md`の変更を禁じている | 未対応（`#10`への申し送りとする。後述） |

### ラウンド5終了時点のまとめ

- `must`（RST側、コミット`ea4b5d3`）: R5-1・R5-2・R5-3・R5-4、全て解消
- `must`（規約・設計文書側、本コミット）: R5-5・R5-6・R5-7・R5-8、全て解消
- `note`: R5-9・R5-10、いずれも対応して解消
- 却下: R5-X1〜R5-X4（理由は上表）。R5-X4のみ`#10`への申し送りとする
- `style.md`の観点は11個のまま（S-01〜S-11）。本ラウンドの修正はすべて既存観点への追記である
- Dockerフルビルド（`-a`）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ、新規警告0件）、`:ref:`の未定義参照0件を確認済み

## ラウンド6（再レビュー4観点と、規約・記録の是正）

ラウンド5の対応（`ea4b5d3` / `65fc852`）に対し、4観点（A網羅性 / Bトンマナ / C用語 / D整合性）で再レビューを実施した。
RST本文側の`must`はコミット`d8089aa`で、規約文書側の`must`とレビュー記録の訂正は本コミットで対応した。
本ラウンドの特徴は、`must`の大半が**ラウンド4のSTEP Cで共通部へ引き上げた記述の後始末**（形式非依存に直しきれていない）であり、
同じ型の欠陥が3ラウンド連続で再発していた点である。そのため`d8089aa`では個別対応をやめ、機械的な全件走査に切り替えた。

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R6-1 | 6 | A/C | must（事実誤り） | `メッセージングのデータを記述する`の共通部にあった「フィールド名称行の先頭要素は**レコード種別として扱われ**（慣行として ``no``\ と記載する）」が実装と一致していなかった。実装では書かれた値は破棄され、レコード種別は無条件に ``"default"``\ 固定である（`MessageParser.java:60-67`、YAMLも`YamlFileBuilder.java:182-184`で同様）。同じ誤りが整形・補完の表の「レコード種別が**空欄の行に**、既定のレコード種別を**補う**」にも現れていた（実際は空欄かどうかに関わらず無条件に置換される） | 要 | — | 誤った記述を除去し、「フィールド名称行の先頭要素に記載した値がレコード種別として使われることはない」に是正。整形・補完の表も「記述されたレコード種別を破棄し、常に既定のレコード種別（``"default"``\ ）に置き換える」へ修正（`d8089aa`） |
| R6-2 | 6 | B/D | must（回帰） | 新設L2`テストデータの構造`の導入文（ラウンド5の R5-10 で追加したもの）が「テストクラスに対応する**テストデータファイル**・読み込み単位・データブロック」という3層で書かれていた。直下の階層図と`YAML形式の場合`の記述は「テストクラス（Excel: 1ファイル / YAML: 1ディレクトリ）→ 読み込み単位 → データブロック」であり、中間層「テストデータファイル」はExcel（1ファイル＝1テストクラス）でしか成立しない。導入文が階層図と矛盾していた | 要 | — | 導入文から中間層を落とし、「テストクラス・読み込み単位・データブロックという階層」に改めて直下の階層図および`YAML形式の場合`の記述と一致させた（`d8089aa`） |
| R6-3 | 6 | B | must（回帰） | 取引単体テストのモックアップ段落が、Excel前提の文面のまま**丸ごと**共通部（形式別L4より前）へ移されていた。ラウンド4の`checks/task-09-restructure.md`はこれを「形式非依存の内容を形式別L4に残した箇所」として「文面を変えずに位置だけを共通部へ移した」と記録していたが、実際には形式依存の文（「シート名 ``message``\ 固定の1つのExcelファイル」）ごと共通部に置かれており、S-10規約1に反していた | 要 | — | 段落を分割した。形式非依存の3点（識別子の書式がグループIDを持たない点・データは応答電文の場合のみ記載する点・``errorMode:``\ で障害系テストも行える点）を共通部に残し、Excel固有の「シート名 ``message``\ 固定の1つのExcelファイル」は`Excel形式の場合`へ移した（`d8089aa`）。ラウンド4の記録も本コミットで訂正した |
| R6-4 | 6 | B | must（回帰） | 共通部にExcel固有の構文・語彙が残存していた。(a) 0バイトの空ファイルのコードブロックが ``SETUP_VARIABLE=work/members.csv``\ というExcel/テキスト形式の記法、(b) グループID付き識別子の書式例 ``EXPECTED_REQUEST_BODY_MESSAGES[グループID]=リクエストID``\ 、(c) ``errorMode:``\ の記載位置の説明が「``no``\ を除く最初のフィールド」というExcelのラベル列前提、(d) 「行末の空セルを取り除く」が両形式共通のように書かれていた（実際はExcel形式のみ）、(e) 「1**シート**に1テストケース」「1**セル**に記述すると」「エントリ数（``rows``\ 合計）」などの片形式語 | 要 | — | (a)(b)は`Excel形式の場合`へ移動、(c)はラベル列前提を外す、(d)は「（\ Excel\ 形式のみ。\ YAML\ 形式では ``rows:``\ の各要素をそのまま読み込む）」と両形式対比に、(e)は形式非依存語（読み込み単位・フィールド・データ行）へ言い換え（`d8089aa`）。(a)については移動だけではYAML読者に記法が残らないため、YAML側に ``records:``\ の空配列記法を1文補った（申し送り6参照） |
| R6-5 | 6 | D | must（規約、G-7） | `style.md` S-02 と `design.md` §4 が「第3部の例外は『テストデータの書き方』の**1ページのみ**」と書いていたが誤り。同じ第3部の`テストデータの記載例`も、`design.md` §4 自身が別の箇所（「テストデータの2ページ」）で定めているとおり`機能概要`も`使用方法`も持たず、L2に個別の見出しを並べる構成である（`testdata_examples.rst`で実測: L2見出し9件、`機能概要`・`使用方法`いずれも0件）。`design.md` §4 は同一節の内部で矛盾していた | 要 | — | S-02 と `design.md` §4 の双方を、第3部の例外が**2ページ**であることが分かる形に改めた。§4は例外を箇条書き2項に分け、「テストデータの2ページ」節の冒頭文も「上記2つの例外のうち」と接続して同一節内の矛盾を解消した |
| R6-6 | 6 | D | must（規約、G-8） | `style.md` S-10 規約3 が自己矛盾していた。`#9`で「形式別のL4対は、そのL3の末尾2つに置く」を追加した一方、太字ラベルの例外条件2 は従来どおり「規約3のL4見出し対を**すでに1組使った後に生じる**、もう1箇所だけの短い記述方法の違い」と書かれていた。**対を末尾に固定した以上「対の後に生じる」状況は原理的に発生しない。** 実態も追随しておらず、`testdata_notation.rst`の太字ラベル使用箇所4件（L586・L867・L1433・L1462）はいずれも例外条件2の文言を満たさない。規約3が例に挙げた「ディレクティブの書式」「マーカーカラムの対象データタイプ」そのものが規約違反になっていた。あわせて「同じセクション内でExcel/YAMLの話題が複数回出てくる場合も、**最初の**見出し対の下に集約する」も、対が1組・末尾固定になった今は死文だった | 要 | — | 実ファイルで太字ラベルの使用箇所を全件洗い出したうえで（下記）、例外条件2を実態に合う2条件へ書き直した。(i) 形式別L4対を**持つ**L3の共通部で短い記述方法の違いに触れる場合（対は末尾2つに固定されるため共通部の記述は必ず対より前に位置する）、(ii) 形式別L4対を**持たない**L3で短い記述方法の違いに触れる場合。「最初の見出し対の下に集約する」は「2組目の見出し対を作らず、その1組の下にまとめる」に改めた |
| R6-7 | 6 | D | note（G-9） | `#10`申し送り3 の射程が不足していた。S-10規約3の「L4見出し」という文言との衝突だけを書いていたが、`style.md` S-03 例外1 の**根拠自体**（`knowledge/index.md`にlevel 4は現れないため一意性の問題が生じない）が見出しレベルに依存しており、記載例ページで形式別分割がlevel 3に落ちると同じ根拠が逆に「一意性が損なわれる」ことを示す | 要 | — | 申し送り3を拡張し、根拠が記載例ページに適用できないこと、および`#10`着手前に(a)見出し名を変える/(b)形式別に分割しない/(c)根拠を張り替える のいずれを採るか決める必要があることを明記した |
| R6-8 | 6 | D | note（G-10） | `mapping.csv`の`dest_page=テストデータの記載例`の**65行すべてが`dest_section=使用方法`**だが、`design.md` §4 は同ページに「使用方法」見出しを置かないと定めており、`testdata_examples.rst`にも存在しない。`vocabulary.md`の例外3も「機能概要を持たない」しか書いておらず使用方法に触れていない。4者が不整合 | 要（記録のみ） | — | 4者の状態を実測（`csv.DictReader`で65行/全件`使用方法`、RSTでL2見出し9件・`使用方法`0件）したうえで申し送り4として記録した。**本ラウンドでは修正しない。** 作業指示の禁止事項が「他の33ページの`dest_section`を変更しない」と定めており、`テストデータの記載例`は本作業の対象ページではないためである |
| R6-9 | 6 | D | note | 生成HTMLで、重複する形式別L4のアンカーが`#excel`/`#yaml`/`#id7`/`#id8`…と自動採番になっており、見出しの増減で深いリンクが壊れる。S-03例外1の根拠（目次でのネスト表示）はアンカーの安定性には答えていない | 要（記録のみ） | — | ビルド済みHTMLで16個のアンカーを実測し、申し送り5として記録した |
| R6-10 | 6 | A | note | `d8089aa`で内容を2文追加した（YAMLの空ファイル記法 ``records: []``\ 、連番の自動付与）。作業指示の「内容の追加・削除を行わない」に対する例外にあたる | 要（記録のみ） | — | 追加した2文・根拠（JSON Schema `ntf-testdata-yaml-schema.json` の `file_data.records`）・例外とした理由（形式別L4へ移すだけではYAML読者に該当記法が一切残らなくなる）を申し送り6として記録した |
| R6-11 | 6 | A | note | Excelの同期応答送信経路（`SendSyncMessageParser`）は`onReadingNames`を上書きしないため、`DataFileParser`によりフィールド名称行の先頭要素がレコード種別として使われる可能性がある。現行RSTの「Excel・YAMLとも（レコード種別は装飾的）」はR6-1で是正した`MESSAGE`系については真だが、この経路については未検証 | 要（記録のみ） | — | 未検証の可能性がある箇所として申し送り7に記録した。次にメッセージングの節に触れる際に実装を読んで確定させる |

### 太字ラベルの使用箇所（R6-6 の根拠。`testdata_notation.rst` を全件走査）

| 行 | 所属L3 | そのL3の形式別L4対 | 位置 | 該当する例外条件（改訂後） |
|---|---|---|---|---|
| L586 | `テストケース一覧（testShots）を記述する` | なし（処理方式別L4が6つ） | L4 `testShots本体を記述する` の中 | 条件1（既にL4の下でこれ以上の見出しレベルを追加できない） |
| L867 | `ファイルのデータを記述する` | あり（L967/L1074） | L3共通部（対の**前**） | 条件2-(i)（ディレクティブの書式） |
| L1433 | `コメント・マーカーカラム・空エントリを扱う` | なし（L4を1つも持たない） | L3本文 | 条件2-(ii)（コメント記号） |
| L1462 | 同上 | なし | L3本文 | 条件2-(ii)（マーカーカラムの対象データタイプ） |

改訂前の例外条件2（「L4見出し対をすでに1組使った後に生じる」）を満たす箇所は**0件**であった。

### ラウンド6終了時点のまとめ

- `must`（RST側、`d8089aa`）: R6-1・R6-2・R6-3・R6-4、全て解消
- `must`（規約側、本コミット）: R6-5（G-7）・R6-6（G-8）、全て解消
- `note`: R6-7（G-9）・R6-8（G-10）・R6-9・R6-10・R6-11。いずれも申し送り3〜7として記録し、本ラウンドでの修正は行わない
- ラウンド5の記録の訂正（本コミット）: R5-2（「共通部の形式依存記述10箇所すべて解消」が過大であった旨）・R5-4（`glossary.md` §8違反として解消したのは2件ではなく1件である旨）・R5-5（作業指示 STEP A-4 の明示的禁止を上書きした事実と理由）
- `style.md`の観点は11個のまま（S-01〜S-11）。本ラウンドの修正はすべて既存観点（S-02・S-10）への改訂である
- Dockerフルビルド（`-a`）で`build succeeded, 1 warning`（既知の`db_double_submit.rst`のみ、新規警告0件）を再確認。ゲート5〜13の再実行値は`checks/task-09-restructure.md`に記録

## ラウンド7（最終ラウンド。事実誤りの確定と規約2件の実測整合）

ラウンド6の対応（`d8089aa` / `c94a870`）に対する最終の修正ラウンド。指摘は移動を伴わない局所的な語句修正が中心である。
本ラウンドでは**事実に関わる修正をすべて実装ソース（`/home/tie303177/work/nablarch/`）または JSON Schema を自分で読んで裏付けた**。
過去3ラウンドで繰り返した「本文を移動して語彙を直し忘れる」型の回帰を避けるため、不要な移動は行っていない。

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R7-1 (F-1) | 7 | A | must（事実誤り） | レコード種別の記述が over-generalization。3箇所（`Excel形式の場合` L1218「先頭要素に記載した値がレコード種別として使われることはない」・`YAML形式の場合` L1245「Excel・YAML とも装飾的」・整形/補完の表 L1495「常に既定のレコード種別に置き換える」）が、`MESSAGE` 以外のメッセージ系にも当てはまるかのように書かれていた | 要 | — | 実装を自分で読んで確定させた。**`MESSAGE`（`setUpMessages`/`expectedMessages`）だけが破棄され `"default"` に固定される**（`MessageParser.java:58-67` の匿名 `FixedLengthFileParser` が `onReadingNames` を上書きし `temp.remove(0); temp.add(0, "default")`）。**Excelの同期応答メッセージ送信系4データタイプとモックアップ経路は、書いた値がそのままレコード種別になる**（`SendSyncMessageParser.java:110-143` は `createFixedLengthFileParser` を別の匿名クラスで置き換えるが上書きするのは `onReadingValues` と `createNewFile` のみで `onReadingNames` は上書きしない → 既定の `DataFileParser.java:259-263` `createNewFragment` が `currentFragment.setRecordType(fieldNamesLine.get(0))` を実行する）。到達経路も `BasicTestDataParser.java:99-103`（`getMessageWithoutCache` = モックアップ）・`:113-117`（`getSendSyncMessage` → `GroupMessageParser.java:43` → `SendSyncMessageParser`）で確認した。**YAML は全メッセージ系（`messages` と送信同期4キー）が `"default"` 固定**（`YamlFileBuilder.java:182-184` の `skipFwHeader ? DEFAULT_RECORD_TYPE : ...`。`YamlMessageBuilder.java:189,207` がいずれも `skipFwHeader=true` の builder を呼ぶ。JSON Schema `$defs/record_fragment.record_type` の description も同旨）。3箇所すべてを適用範囲を限定する形に書き換え、Excel側には「慣行に従って `no` と記載した場合はレコード種別も `no` になる」という判断材料を1文添えた |
| R7-2 (F-2) | 7 | A/D | must（自己矛盾・出典誤り） | `errorMode:` の `important`（L1196）が「機能するのは応答電文（RESPONSE系）およびモックアップ経由に限られる」と述べた直後に「`RequestTestingSendSyncSupport`（`GroupMessageParser`）を使う経路では反映されない」と書いており、前段と後段が矛盾していた | 要 | — | 実装で決着。RESPONSE系こそが `RequestTestingSendSyncSupport` 経路である（`RequestTestingMessagingProvider.java:190-206` が `new RequestTestingSendSyncSupport(testClass)` から `RESPONSE_HEADER_MESSAGES`/`RESPONSE_BODY_MESSAGES` を取得 → `RequestTestingSendSyncSupport.java:157` → `BasicTestDataParser.java:113-117` → `GroupMessageParser`）。その経路が呼ぶ `RequestTestingMessagePool.java:78-84` が `ErrorMode.TIMEOUT`/`MSG_EXCEPTION` を判定して `null` 返却／`MessagingException` 送出を行う。モックアップ経路も同様に判定する（`SendSyncSupport.java:288-294`）。**`errorMode:` を反映するコードは RESPONSE系を読む2箇所だけであり、要求電文の期待値（`EXPECTED_REQUEST_*`）の読み出し・照合経路（`RequestTestingMessagePool.java:109` の `getRequestTestingReceivedMessage`）には判定が無い**。この2点が読み取れる文へ書き換えた。**出典（`input/ntf-testdata-doc.md:492`）は実装と食い違っており、誤りである。**実装を優先して出典を訂正した（ラウンド4のR4-3・ラウンド5のR5-1と同じ扱い） |
| R7-3 (F-3) | 7 | B/D | must（回帰） | `ファイルのデータを記述する` のL3共通部の用語表（`レコード種別行`/`フィールド名称行`/`データ型行`/`フィールド長行`/`データ行`。定義はいずれも「〜を示す**行**」）がExcelの行前提で、YAMLでは成立しない（YAMLは `records:` 配下の `record_type` / `fields:`（各要素が `name`・`type`・`length` を持つマップ）/ `rows:` という構造。`YamlFileBuilder.java` の `buildFragmentsInternal` で確認）。さらに `d8089aa` がメッセージングのL3共通部を脱「行」化した結果、その文が「**前述のファイルデータと同じ構成を持つ**」と参照する当の用語表だけが「〜行」のまま残り、参照元と参照先で用語が食い違っていた | 要 | — | 用語表を形式非依存にした（`レコード種別`/`フィールド名称`/`データ型`/`フィールド長`/`データ`。定義からも「行」を外し役割だけを残した）。あわせてL3導入文の「各**行**の役割」「各**行**の名称と役割」を「各**要素**の」に改めた。Excel固有の行としての書き方は `Excel形式の場合` の名称表（`レコード種別`・`フィールド名称`・`データ型`・`フィールド長`・`データ`と `ディレクティブ行`）に既にあるため移動は不要。code-block（`ディレクティブ → レコード種別 + フィールド名称 → データ型 → フィールド長 → データ`）は元から「行」を含まず両形式の記述順序として成立するため無変更。これによりメッセージング側の「前述のファイルデータと同じ構成」という参照が成立する |
| R7-4 (F-4) | 7 | A | must（削除の回帰） | `d8089aa` がモックアップ段落を分割した際、旧文の「（`EXPECTED_REQUEST_HEADER_MESSAGES=リクエストID` など、グループIDを持たない点が同期応答メッセージ送信のテストケースと異なる）」から**括弧内の具体例が落ちていた**（`grep -c "EXPECTED_REQUEST_HEADER_MESSAGES=" `: `d545d95` 1件 → `c94a870` 0件）。作業指示の禁止事項「内容の追加・削除を行わない」に対する違反 | 要 | — | 具体例を復活させた。ただし**旧位置（L3共通部）ではなく `Excel形式の場合` に置いた**。`データタイプ=識別子の値` はExcel固有の書式であり、共通部へ戻すと `d8089aa` のR6-4(b)で解消した「共通部にExcel固有の構文が残る」欠陥を再発させるためである。現行文は `Excel形式の場合` のモックアップ段落末尾に「識別子はグループIDを持たず、``EXPECTED_REQUEST_HEADER_MESSAGES=リクエストID`` のように記載する。」として置いた。共通部側の「識別子の書式がグループIDを持たない点が同期応答メッセージ送信のテストケースと異なり」は無変更 |
| R7-5 (F-5a) | 7 | A | note（事実の精度） | 「0バイトの空ファイルを表現するには `records:` に空配列 `[]` を記載する」の直前が、必須キーを `path` のみとしていた | 要 | — | JSON Schema（`nablarch-testing-yaml/src/main/resources/nablarch/test/ntf-testdata-yaml-schema.json` の `$defs/file_data`）を自分で読み、`required` が `["path","type","records"]` の3キーであることを確認して「`path`・`type`・`records` の3キーが必須」に是正した。`records:` キー自体を省略できない旨は必須キーの記述に含まれたため重複表現を落とした |
| R7-6 (F-5b) | 7 | A | note（事実の精度） | 収集方法の表の「単一」行が「データタイプとIDが**完全一致**する最初の1ブロック」としていたが、データタイプ側は前方一致である | 要 | — | 実装で確認（`TestDataParsingTemplate.java:321-333` の `getDataType` が `dataTypeCell.startsWith(type.getName())`、`SingleDataParsingTemplate.java:33-40` が `getTargetType() == dataType && typeValue.equals(id)`）。完全一致なのはIDのみ。「データタイプが合致する（Excel形式ではデータタイプ名の前方一致）データブロックのうち、IDが完全一致する最初の1件を取得」に是正した |
| R7-7 (F-5c) | 7 | D | note（参照のキーずれ） | `データブロックとデータタイプ` の `Excel形式の場合` で、「同じ**データタイプ**のデータブロックを複数記述」した場合の帰結を、データタイプ一覧表の「同一**ID**のデータブロックが複数ある場合」列へ参照させており、問い（キー）がずれていた | 要 | — | 参照の主語を表の列見出しに揃え、「データタイプが同じで識別子の値（ID）も同じデータブロックが複数ある場合にどれが有効になるかは、…のとおりである」に改めた |
| R7-8 (S-1) | 7 | B | must（規約） | `style.md` S-10 規約3 の太字例外条件(iii) の根拠「そのL3で記述方法の違いに触れるのは1〜2箇所だけであり、L4対を立てるだけの分量がない」が実態と不一致。該当L3（`コメント・マーカーカラム・空エントリを扱う`）で形式差に触れているのは**5箇所**であり、`checks/task-09-restructure.md` の自記録とも矛盾していた | 要 | — | 実測に合う表現へ書き直した。**本ラウンド終了時点の実測は6箇所**である（太字ラベル2件・コメント記号の対比・空エントリの判定・整形/補完の表のセル**2件**）。ラウンド6時点の5箇所から1件増えたのは、R7-1 で整形/補完の表の「メッセージ」行に形式別の適用範囲を書き加えたためである。根拠を「箇所が少ないこと」から「形式差がいずれも1文以内に収まり、かつ規則（コメント／マーカーカラム／空エントリ）ごとに分散していて、形式別L4対に分けると規則単位の説明が分断されること」へ差し替えた。**5〜6箇所を形式別L4対に分割するかどうかは別判断であり、本ラウンドでは分割しない**（申し送り9へ） |
| R7-9 (S-2) | 7 | B | must（規約） | `style.md` S-07（表の記法）が本ページの実態を裁定しておらず、`#8` と `#9` の運用が正反対だった。実測: `testdata_notation.rst` は `list-table` 34件 / simple table 0件、承認済みの `about/index.rst`（`#8`）は `list-table` 1件 / simple table 3件。`#8` のレビューでは同じ S-07 が3ラウンド連続で `must` として指摘され `list-table` → simple table への変換まで実施されている | 要 | — | S-07 に例外を明文化して本ページの形を追認した。根拠は `steering.md` Rules L43 の実測記録（`=` のみで罫線を引く simple table のセル文字列は列位置を「表示幅」（全角2・半角1）で揃える必要があり、文字数基準で詰めると `sphinx-build` が `Malformed table` エラーを出す。2026-08-03に `about/index.rst` で実際に発生）。コードリテラル（半角）と日本語（全角）が混在するセルが多数を占める表が大半のページでは幅合わせの保守コストが表の数だけ積み上がるため、短い2列表でも `list-table` に揃えてよいこととし、適用はページ単位で判断すると定めた。**34表の変換は行わない。**あわせて `list-table` 34件中 `:ref:` を含むセルが0件であること（規約本体の基準だけでは `list-table` を選ぶ理由にならないこと）も明記した |

### ラウンド1の記録の訂正（ラウンド1の表は書き換えない）

ラウンド1の観点Bの行（`B(他8観点)`）は「S-01〜S-03, S-05〜S-08は違反なし」と記録しているが、**このうち S-07 についての記録は基準の取り違えである。**
当時の本ページは表がすべて `list-table` であり（本ラウンドの実測でも34件すべてが `list-table`、simple table 0件）、
S-07 の規約本体（「2〜3列程度の短い説明表は simple table を使う」）に照らせば、短い2列表（処理方式/対応クラス、値の指定/記述方法、用語/定義など）が複数該当していた。
同じ S-07 は `#8`（`about/index.rst`）のレビューでは3ラウンド連続で `must` として指摘され、`list-table` → simple table への変換まで実施されている。
`#9` ラウンド1が「違反なし」と判定したのは、規約本体ではなく本ページの既存の形を基準に据えてしまったためである。
本ラウンドの R7-9 で S-07 側に例外を明文化し、本ページの形を正として追認した。

### 形式差に触れる箇所（R7-8 の根拠。`コメント・マーカーカラム・空エントリを扱う` を全件走査）

| 行 | 種類 | 内容 |
|---|---|---|
| L1435 | 太字ラベル | `**Excel\ 形式**\ では、セルの内容を ``//``\ で始めると…` |
| L1462 | 地の文 | コメント記号の対比（Excelの `//` と YAMLの `#`、行全体スキップと行末コメント） |
| L1464 | 太字ラベル | マーカーカラムの対象データタイプ（`**Excel\ 形式**` の3データタイプ / `**YAML\ 形式**` の3キー） |
| L1484 | 地の文 | 空エントリの判定（Excelは行の全セルが空、YAMLは空マッピング `{}` または全値が空文字） |
| L1495 | 表のセル | 行末の空セルの除去（Excel形式のみ。YAML形式では `rows:` の各要素をそのまま読み込む） |
| L1497 | 表のセル | レコード種別の `"default"` 置換の適用範囲（**R7-1 で追加。ラウンド6時点では形式差に触れていなかった**） |

### ラウンド7終了時点のまとめ

- `must`（RST側）: R7-1（F-1）・R7-2（F-2）・R7-3（F-3）・R7-4（F-4）、全て解消
- `must`（規約側）: R7-8（S-1）・R7-9（S-2）、全て解消
- `note`: R7-5（F-5a）・R7-6（F-5b）・R7-7（F-5c）、いずれも実装／JSON Schema を確認のうえ解消
- 記録の訂正: ラウンド1の「S-07 違反なし」が基準の取り違えであった旨（上記）
- **申し送り7（`SendSyncMessageParser` 経路が未検証）は R7-1 で実測により確定したためクローズした**
- `style.md` の観点は11個のまま（S-01〜S-11）。本ラウンドの修正はすべて既存観点（S-07・S-10）への追記・改訂である
- Dockerフルビルド（`-a`）で `build succeeded, 1 warning`（既知の `db_double_submit.rst:108` のみ、新規警告0件）。ゲートの再実行値は `checks/task-09-restructure.md` に記録

## `#10`（テストデータの記載例）への申し送り

1. **`testdata_examples.rst`のL2見出し2件を改題する。** `データブロック（テストケース、準備データ、期待値）を記述する` → `データブロックとデータタイプ`、`グループIDでデータブロックを分ける` → `グループIDによる使い分け`。`design.md` §4の見出し文言一致規約（R5-5で改訂）に合わせるため。あわせて`testdata_notation.rst`側のリンク文言も合わせる（`:ref:`のラベル参照自体は現状で正しく解決しており、変更が要るのはリンクの表示文言のみ）
2. **`glossary.md` §5.13に`テストデータの構造`を掲載するかを判断する。** §3の掲載基準2「`design.md`が章・セクション名として使う用語」に該当するようになったため（R5-X4）
3. **`style.md` S-10規約3の見出しレベル表現を見直す。** 規約3は形式別の対を「L4見出し」と明記しているが、`テストデータの記載例`は「使用方法」ラッパーを持たず見出しをL2として並べる構成であるため、形式別の分割は**L3**に落ちる。`#10`着手前に、規約3を見出しレベルの相対表現（「1段下の見出し」等）に改めるか、記載例ページの例外を明記するかを決める必要がある

   **【ラウンド6で射程を拡張】** 影響は規約3の文言だけにとどまらない。`style.md` S-03 例外1 は、形式別見出しの**重複を許す根拠**として「Nabledge の検索の入口 `knowledge/index.md` に現れるのは level 2 と level 3 だけで level 4 は現れないため、この重複によって検索面の一意性が損なわれることはない」を挙げている。記載例ページで形式別分割が **level 3** に落ちると、この根拠は逆に「検索面に重複した見出しが現れ、一意性が損なわれる」ことを示すことになる。つまり例外1 は、根拠自体が見出しレベルに依存しており、記載例ページには適用できない。`#10`着手前に、次の(a)〜(c)のいずれを採るかを決める必要がある。

   - (a) 記載例側で形式別見出しの名前を変える（`テーブルのデータを記述する（Excel形式）` のようにページ内で一意にする）
   - (b) 記載例側は形式別に分割しない（1つの見出しの中でExcel例とYAML例を並べる）
   - (c) S-03 例外1 の根拠を、見出しレベルに依存しない別の根拠へ張り替える

4. **`mapping.csv` の `dest_page=テストデータの記載例` 65行の `dest_section` を見直す。** 実測（`csv.DictReader`）のとおり、65行**すべて**が `dest_section=使用方法` である。しかし `design.md` §4 は同ページに「使用方法」見出しを置かないと定めており（ラウンド6で例外2ページとして明文化）、`testdata_examples.rst` にも `使用方法` の見出しは存在しない（L2見出し9件はいずれも記述例の見出し）。`vocabulary.md` の例外3 も「機能概要を持たない」ことしか書いておらず、使用方法を持たないことに触れていない。**`mapping.csv`・`design.md`・`vocabulary.md`・RST の4者が食い違っている。**

   本ラウンドでは修正しない。作業指示 `ntf-doc-09-restructure.md` の禁止事項が「他の33ページの `dest_section` を変更しない」と定めており、`テストデータの記載例` は本作業の対象ページ（`テストデータの書き方`）ではないためである。`#10` 着手時に、記載例ページの `dest_section` 語彙を（新設するか、`(L2直下)` 相当の扱いにするか含めて）決めたうえで65行を再割当し、`vocabulary.md` の例外3 も「使用方法も持たない」旨へ拡張する。

5. **生成HTMLにおける形式別L4のアンカーが不安定である。** 重複する `Excel形式の場合` / `YAML形式の場合` は、Sphinx により `#excel` / `#yaml` / `#id7` / `#id8` / `#id9` / `#id10` / `#id15` / `#id16` / `#id17` / `#id18` / `#id25` / `#id26` / `#id28` / `#id29` / `#id31` / `#id32` と自動採番される（`_build/html/.../testdata_notation.html` を実測）。最初の1組だけが意味のあるIDを得て、残り7組は連番IDになる。**見出しを増減させると採番がずれ、外部から張られた深いリンクが壊れる。** S-03 例外1 の根拠（ページ内の `.. contents::` 目次で親L3の下にネスト表示されるため位置で判別できる）は、目次上の可読性には答えているが、アンカーの安定性には答えていない。安定した深いリンクが必要なら、形式別L4に明示ラベル（`.. _testdata_notation-table_data-excel:` など）を付ける必要がある

6. **`d8089aa` で内容を2文だけ追加した（作業指示の「内容の追加・削除を行わない」に対する例外）。** 追加したのは次の2文で、いずれもYAML形式の場合の中である。
   - 「0バイトの空ファイルを表現するには、``records:``\ に空配列 ``[]``\ を記載する（``records:``\ キー自体は省略できない）。」
   - 「同期応答メッセージ送信で使う電文の連番は、``rows:``\ の記述順（1始まり）から自動的に付与されるため、テストデータに記載しない。」

   根拠はJSON Schema（`nablarch-testing-yaml` の jar に同梱される `nablarch/test/ntf-testdata-yaml-schema.json`、`file_data.records`。`input/ntf-testdata-doc.md:70` に所在の記載あり）と実装である。**例外とした理由**: これらの記法はもともとExcel固有の書き方（`SETUP_VARIABLE=...` のコードブロック、先頭ラベル列の連番）でしか書かれておらず、それを `Excel形式の場合` へ移すだけでは、YAML形式の読者に該当記法が**一切残らなくなる**。移動だけでは情報の欠落が生じるため、対応するYAML側の記法を1文ずつ補った

7. **【ラウンド7でクローズ】未検証の可能性がある箇所（次に触れる際に確認すること）。** 現行RSTの `メッセージングのデータを記述する` の `YAML形式の場合` には「``record_type``\ の値は、フレームワーク内部で常に ``"default"``\ に置き換えられる。\ Excel\ ・\ YAML\ とも任意の値を装飾的に記述できるが、実行時の挙動には影響しない。」とあり、`Excel形式の場合` にも「フィールド名称行の先頭要素に記載した値がレコード種別として使われることはない。」とある。この記述は `MESSAGE` 系（`MessageParser`）については真だが、**Excelの同期応答送信経路（`SendSyncMessageParser`）は `onReadingNames` を上書きしないため、`DataFileParser` によりフィールド名称行の先頭要素がレコード種別として使われる**可能性がある。この経路については実装で確認していない。厳密には「Excel・YAMLとも装飾的」が成り立たない可能性があるため、次にメッセージングの節に触れる際に `SendSyncMessageParser` / `DataFileParser` / `MessageParser` の3者を読んで確定させること

   **【ラウンド7でクローズ】** 3者に加えて `BasicTestDataParser` / `GroupMessageParser` / `YamlFileBuilder` / `YamlMessageBuilder` / JSON Schema を読んで確定させた。結論は次のとおりで、RST本文はR7-1で是正済みである。本項目は以後参照不要。
   - Excel `MESSAGE`（`setUpMessages`/`expectedMessages`）: 書いた値は破棄され `"default"` 固定（`MessageParser.java:58-67`）
   - Excel の同期応答メッセージ送信系4データタイプ・取引単体テストのモックアップ経路: 書いた値がそのままレコード種別（`SendSyncMessageParser.java:110-143` は `onReadingNames` を上書きしないため `DataFileParser.java:259-263` が効く）
   - YAML のメッセージ系（`messages` と送信同期4キー）: すべて `"default"` 固定（`YamlFileBuilder.java:182-184`、`YamlMessageBuilder.java:189,207`）

8. **記載例へのリンクの重複。** 「実際の記述例は…を参照。」が7つのL3で各3回（L3共通部＋`Excel形式の場合`＋`YAML形式の場合`）出現し、21本中14本が同一アンカーへの反復である。誤りではないが、`style.md` に「記載例リンクの置き場所」の規約を1つ立ててから、全ページ横断で整えるのが筋である（本ページだけを先に整えると規約のないまま実態が動く）

9. **`コメント・マーカーカラム・空エントリを扱う` の形式別分割の是非。** 形式差に触れる箇所が6箇所ある（内訳はラウンド7の表）。太字ラベルでの対応が妥当か、形式別L4対を立てるべきかを判断する必要がある。`style.md` S-10 規約3 の例外条件(iii) は現在、分割しない根拠を「形式差がいずれも1文以内に収まり、規則ごとに分散しているため分割すると規則単位の説明が分断される」としている（ラウンド7で「1〜2箇所だけ」という誤った根拠から差し替えた）

10. **同一L3共通部の `important` の重複。** 「フィールド名称に重複した名称は許容されない」が `メッセージングのデータを記述する` のL3共通部だけで2回述べられている（現状 L1113-1115 の `important` と、現状 L1155-1157 の `important` 中の一文）。`ファイルのデータを記述する` の `Excel形式の場合`（現状 L1023）にも同じ趣旨の `important` があり、ページ全体では3箇所になる。**内容の削除を伴うためラウンド7では対応しない**

11. **`値を特殊記法で記述する` の `Excel形式の場合`。** 16個の形式別L4のうち唯一、導入文なしでいきなり表から始まる（対のYAML側には導入文がある）。S-10 規約3 は導入文の有無を定めていないが、対の非対称は読者に段差として見える

12. **`グループIDによる使い分け` の `default` の扱い（現状 L254）。** 「テストケース一覧のカラムでデフォルトグループを明示的に使いたい場合は ``default`` と記載する」と「Nablarchバッチアプリケーションでは、グループIDに文字列として ``"default"`` を指定した場合もグループIDなしと同等に扱われる（ウェブアプリケーション・メッセージングのテストには、この挙動は適用されない）」が互いを打ち消し合って読める。実装を確認したうえで、それぞれの主語（どの処理方式のどのカラムの話か）を限定する必要がある

13. **YAML のメッセージ系における `record_type: FW_HEADER` の扱い（ラウンド7の通し読みで検出）。** 現行RST（現状 L1253 の `important`）は「``record_type`` に特別な予約値はない」と述べており、これは JSON Schema の記述（`$defs/record_fragment.record_type` の description「FW_HEADER のような予約値はない」、`$defs/message_data.records` の「旧形式の record_type: FW_HEADER は廃止」）と一致する。一方、実装は `YamlFileBuilder.java:177`（`if (skipFwHeader && FW_HEADER_RECORD_TYPE.equals(recordType)) continue;`、定数は `YamlSection.java:78` の `"FW_HEADER"`）でメッセージ系に限り `record_type: FW_HEADER` のレコードを**読み飛ばす**。旧形式の残骸を無視するための後方互換処理と読めるが、「予約値はない」という本文とは厳密には食い違う。**ラウンド7では修正していない**（スキーマの記述と本文は一致しており、修正は仕様意図の確認を要するため）

## ラウンド8（user review 差し戻し・`ntf-doc-09-recordtype.md`）

user review で差し戻し。指摘は `must` 1件（レコード種別の形式差が共通部に明記されていない）。

| No | ラウンド | 観点 | 種別 | 指摘 | 対応 | 対応内容 |
|---|---|---|---|---|---|---|
| R8-1 | 8 | D | must | `メッセージングのデータを記述する` の `Excel形式の場合`（現状 L1220）と `YAML形式の場合`（現状 L1247）が、レコード種別について異なる挙動を述べているが、両者が異なるという事実がどこにも書かれていない。片方だけ読む読者は差に気づかず、両方読む読者は矛盾と受け取る | 要 | 同L3の共通部（最初の `^` 見出しより前、既存の `important`「フィールド名称に重複した名称は許容されない」の直後）に独立した `important` を追加した。両形式の現在の挙動を並べ、\ Excel\ →\ YAML\ 変換でレコード種別の扱いが変わることを述べるにとどめ、どちらが仕様かは書いていない。形式別L4（L1220・L1247）は**変更していない** |
| R8-2 | 8 | D | must（横並び） | 手順3の横並び確認（形式別L4対を持つ8つのL3の突合）で、同型の未明記の形式差を1件検出。`テーブルのデータを記述する` の `Excel形式の場合` は「データ行のセル数がヘッダ行より少ない場合、記述しなかった分のカラムには**空文字**が設定されたものとして扱われる」と述べ、`YAML形式の場合` は「後続の行が先頭行のキーの一部を持たない場合、そのカラムは **`null`** を明示的に指定したのと同じ扱いになる」と述べている。同じ事象（行にカラムの値を書かなかった場合）について異なる結果（空文字 / `null`）を示しているが、両者が異なるという事実は共通部にも他のどこにも書かれていない | 要 | 同L3の共通部（最初の `^` 見出しより前）に、R8-1 と同じ形の `important` を追加した。両形式の現在の挙動を並べるにとどめ、どちらが正しいかは書いていない。形式別L4の既存記述は**変更していない** |

**手順3（他の形式差の有無）の確認結果**は `checks/task-09-recordtype.md` に対象8セクション分を記録した。

**判定**: `must` 2件（うち1件は横並び確認で自発検出）に対応。`decide`・`note` の新規発生なし。ページ内の既存記述の削除・書き換えは行っていない（追加のみ）。

## `#10`（テストデータの記載例）への申し送り（追加）

14. **レコード種別の形式差が、仕様として意図されたものか、YAML 経路の挙動乖離かは未確定である。** YAML 経路は `YamlFileBuilder` が本体パイプラインとは別に `record_type` を解決しており（`YamlFileBuilder.java:135-137`・`:150` 前後がいずれも `skipFwHeader=true` を渡し、`:176-184` がそのとき `record_type` を `YamlSection.java:84` の `DEFAULT_RECORD_TYPE`＝`"default"` に固定する）、`nablarch/nablarch-testing` PR #75 で扱っている「converter/YAML 経路が本体パイプラインを再実装しているための挙動乖離」と同じ型に該当する可能性がある。Excel 経路側の根拠は `DataFileParser.java:152`（`onReadingNames` が `createNewFragment(line)` を呼ぶ）・`MessageParser.java:60-66`（`MESSAGE` のみ先頭要素を `"default"` に差し替える）・`SendSyncMessageParser.java:110-142`（`onReadingNames` を上書きしないため基底の挙動が働く）である。

    解説書側では現在の挙動を並記するにとどめ、仕様確定は PR #75 側の判断に委ねる。仕様が確定した場合、本ページの当該 `important`（`メッセージングのデータを記述する` の共通部）と両形式別L4の記述を見直す。

15. **テーブルデータで行のカラム値を書かなかった場合の形式差（R8-2）も、同じ型の未確定事項である。** Excel 経路は空文字を補完し、YAML 経路は `null` になる。`ファイルのデータを記述する` では両形式とも空文字補完で揃っている（現状の共通部に明記済み）ため、テーブルデータだけが揃っていない。仕様として意図されたものかは未確認であり、申し送り14 と同様に PR #75 側の判断に委ねる。仕様が確定した場合、`テーブルのデータを記述する` の共通部に追加した `important` と両形式別L4の記述を見直す。

## `#10a`（用語「テストショット」への統一と使用方法の並び替え、2026-08-07）

user review ではなく、`ntf-doc-terminology.md` による指示作業である。既存のレビュー記録は書き換えていない（本節の追記のみ）。

| 項目 | 内容 |
|---|---|
| 用語 | `テストケース` 42件を全件個別に判定し、`テストショット`／`テストショット一覧`／`テストショット番号`／`テスト` に置き換えた。ページ内の `テストケース` は0件。判定表は `checks/task-terminology.md` §2 |
| 見出し | `テストケース一覧（testShots）を記述する` → `テストショット一覧（testShots）を記述する`、`値を特殊記法で記述する` → `null・空文字・改行など特殊な値を記述する`。`:ref:` ラベル（`testdata_notation-test_shots`・`testdata_notation-special_notation`）は変更していない |
| 並び順 | `使用方法` 配下を「作業の入口から詳細へ」（読者が最初に書き始めるものを先に、その中で使う個別の記法を後に置く。申し送り17 と同じ言い方。作業指示 `ntf-doc-terminology.md:102`・`:162` の字句「全体から個別へ」からの逸脱については `checks/task-terminology.md` §13 R4-7 を参照）に改めた。`テストショット一覧` → `LIST_MAP` → `テーブル` → `ファイル` → `メッセージング` → `null・空文字・改行など特殊な値` → `コメント・マーカーカラム・空エントリ`。`テストデータの構造` 配下3節は変更していない |
| 前方参照 | `テストショット一覧` の節の冒頭に、これが `LIST_MAP` の利用例の1つであり `LIST_MAP` 自体の記法は後述である旨の1文を追加し、`:ref:` でリンクした（1文の追加のみ。節の中身は変更していない） |
| 前後関係の是正 | 移動で崩れた参照2件を是正した。`使用方法` 導入文の対象列挙順（「示したうえで」）と、`LIST_MAPのデータを記述する` の「後続の」→「前述の」。全17件の判定表は `checks/task-terminology.md` §3 |
| 見出し数 | `~` 10件・`^` 26件で不変（ゲート5） |

### `#10a` 差し戻し対応ラウンド1（2026-08-07）

レビュー指摘（A-1〜A-5・B-1〜B-4・C-1・D-1〜D-4）のうち、本ページに関わるもの。全体の記録は `checks/task-terminology.md` §10。

| 項目 | 内容 |
|---|---|
| 循環定義の解消（A-1、最重要） | `データブロックとデータタイプ` の表で、`用途`＝`テストショット` の行の `内容` セルが `1エントリ1テストショットの実行条件（…）` となっており、**テストショットをテストショットで説明する循環**だった（置換前の `1エントリ1ケースの実行条件` は循環していなかったため、`#10a` が持ち込んだ退行）。同じ表の `準備データ` 行と同じ水準の定義に改めた: `1エントリが1回の実行（リクエスト送信・バッチ起動・バリデーション実行など）に対応する、1組の入力と期待値（期待するステータスコードなど）`。**この表の `内容` 列が、解説書中で `テストショット` の意味を読者に与える唯一の場所である。** セル内で完結させ、段落は追加していない |
| 語の近接重複（A-4） | `…というテストで、テスト対象の機能に無関係なカラム…` を `…を確認するために、テスト対象の機能に無関係なカラム…` に改めた。`テスト対象` は用語であり動かせないため、重複する側を落とした |

**`#11` 以降への含意**: 用語を置き換える際は、**その語を定義している箇所を置き換え対象に含めないこと**。定義文の中で被定義語を使うと循環になる。特に「用途／内容」形式の表では、`用途` 列を用語に置き換えたら `内容` 列が定義として成立しているかを確認する。

### `#10a` 差し戻し対応ラウンド2（2026-08-07）

ラウンド1の是正が「局所的には正しいが、その場所の日本語が悪くなった」という指摘を3人のレビュアーから独立に受けた。**意味を変えず、置かれた文脈で自然に読めるようにする**ことを主眼にした是正である。全体の記録は `checks/task-terminology.md` §11。

| 項目 | 内容 |
|---|---|
| 定義セルの書き直し（A-1、最優先） | ラウンド1の `1エントリが1回の実行（…）に対応する、1組の入力と期待値（期待するステータスコードなど）` には3つの問題があった。(1) **兄弟行を包含していた**。同じ表は `用途` を3つ並べ、`準備データ`＝入力、`期待値`＝実行後に確認する状態としているのに、この行が「1組の**入力**と**期待値**」と言うため3つが排他にならない（導入文も「3つの用途」と言う）。(2) `1エントリが…に対応する、1組の…` が**係り受けとして「1エントリが…に対応するところの1組」とも読め**、意図（1エントリ＝1組）と一致しない。(3) 他2行が主語節を持たない25〜33字の名詞句であるのに対し、埋め込み主語＋括弧2個＋約60字で**水準が揃っていない**。現行文言は `1回の実行（リクエスト送信・バッチ起動・バリデーション実行など）に対応する、1エントリ分の実行条件（期待するステータスコードなど）`。頭の名詞を `実行条件` にすることで `準備データ`（データ）・`期待値`（状態）と**排他**になり、`1回の実行に対応する` が `1エントリ分` に掛かることで**1エントリ＝1回の実行**が読み取れる（`テストショット` が実行の単位であることは維持。`glossary.md` §5.7 の定義とも矛盾しない）。循環定義でもない |
| `テスト` 4連発の解消（A-2） | `:40` の1文に `テスト` が4回出ていた（`テストで発生した`／`テスト終了時に`／`伴うテストであっても`／`他のテストに`。置換前は `テスト`×2＋`テストケース`×2 で語が分かれていた）。`実行中に生じたマスタデータの変更をテスト終了時に自動的に元の状態へ戻せるため、マスタデータを変更する場合でも、他のテストに影響を与えずに実行できる。` に改め、**意味を変えずに** `テスト` を2回に減らした |

**`#11` 以降への含意**: 用語の統一は1件ずつ正しくても、**同じ文・同じ表の中で語が重なると日本語として悪化する**。置換後に、(a) 同一文内の語の連発、(b) 表の兄弟セルとの排他性、(c) 列見出しと値の反復、を必ず読み直す。

### `#10a` 差し戻し対応ラウンド3（2026-08-07）

4観点の再レビューで設計・検証が FAIL を出した。本ページに関わる是正は次の4件である。全体の記録は `checks/task-terminology.md` §12。**項番は `R3-` 接頭辞であり、上のラウンド1・ラウンド2 とは別系列。**

| 項目 | 内容 |
|---|---|
| 定義セルの例示の是正（R3-A-1、最優先） | ラウンド2の `1回の実行（リクエスト送信・バッチ起動・バリデーション実行など）に対応する、1エントリ分の実行条件（期待するステータスコードなど）` は、頭の名詞が `実行条件` なのに唯一の例示が `期待するステータスコード`＝**実行後に確認する値**であり、兄弟行 `期待値`（`テスト実行後に確認する状態`）との区別が読者に付かなかった。さらに1セル内に `実行` が3回出て、兄弟行（25字・38字）に対し65字と分量が突出していた。共通必須カラム（`:381`〜`:394` の `no`・`description`・`expectedStatusCode`）を読み直したうえで、**実行の識別・パラメータ側**の例に替えて `1回の実行に対応する、1エントリ分の実行条件（番号・説明・リクエストIDなど）` とした（39字、`実行`×2）。**例示に `テストショット番号` を使うと循環定義に戻るため `番号` とした。** 用語集 `glossary.md` §5.7 の定義（`1組の入力と期待値`）とはレイヤが違うだけで同じものを指す旨を、用語集側に注記した（R3-B-1） |
| 主語の復帰（R3-A-2） | `:40` は「マスタデータ復旧機能を使うと、**実行中に**生じた…」で直前の名詞が「復旧機能」となり「復旧機能の実行中」と読め、後半も「マスタデータを変更する**場合**でも、…**実行できる**」で「実行できる」の主語が消えていた（旧文の主語は「テストケース」）。`テスト` 系の語を2回に保ったまま `テスト実行中に生じた…／マスタデータを変更するテストであっても、他に影響を与えずに実行できる。` に改め、主語を戻した |
| `利用例` → `用途` に統一（R3-A-3） | ラウンド1・2 が持ち込んだ `利用例` は `ja/` 配下の既存解説書に先例が0件（`使用例` は27件）。`:354` と `:586` を `用途` に寄せた。`:586` は「〜が持つカラムの処理方式ごとの説明は」という「の」の連結も同時に解消し、`用途の1つであるテストショット一覧（``testShots``\ ）のカラムについては、処理方式ごとに前述の :ref:`…`\ で説明している。` とした |
| 造語的表現の解消（R3-A-7） | `:1220` の「（テストの内容上、長さを揃えられない場合は手動でテストを行う）」は `#10a` STEP 2（N-31）が作った表現である。「〜の内容上」は硬い造語的表現であるため「（テストの内容によって長さを揃えられない場合は手動で行う）」に改めた |

**`#11` 以降への含意**: 用途を排他に分ける表の定義セルでは、**頭の名詞と例示の向きを一致させる**こと（`実行条件` を名乗るなら例示も実行前の条件側にする）。頭の名詞が正しくても例示が兄弟行の領分を指すと、読者にとっては区別が付かない。**ただしこの含意の適用そのものがラウンド4で差し戻された。次のラウンド4の節を必ず併せて読むこと。**

### `#10a` 差し戻し対応ラウンド4（2026-08-07）

4観点の再レビューでクラフト・設計が FAIL を出した。**原因はラウンド3の是正が本文に持ち込んだ退行であり、本ラウンドはその退行の是正だけを行っている。** 本ページに関わる是正は次の4件である。全体の記録は `checks/task-terminology.md` §13。**項番は `R4-` 接頭辞であり、上のラウンド1〜3 とは別系列。**

| 項目 | 内容 |
|---|---|
| 定義セルの事実誤りの是正（R4-1、最優先） | ラウンド3の `1回の実行に対応する、1エントリ分の実行条件（番号・説明・リクエストIDなど）` には3つの退行があった。(1) **`リクエストID` は `testShots` のカラムではない**（`:408` のとおり `context` は「リクエストID・ユーザ情報・HTTPメソッドを記載した ``LIST_MAP`` **名**」であり、リクエストIDはその `LIST_MAP` の `REQUEST_ID` カラムにある。バッチ・メッセージングの `testShots` カラムは `:476`・`:529` の `requestPath`）。**公開本文の事実誤りだった。** (2) `1回の実行` の語釈（`リクエスト送信・バッチ起動など`）が両ページから消え、新語 `テストショット` の意味を本文だけで読み取れなくなっていた。(3) 全処理方式共通の必須カラム3つ（`:389`〜`:394` の `no`・`description`・`expectedStatusCode`）のうち `expectedStatusCode` が落ち、3用途の表が「期待値は `EXPECTED_*`／`RESPONSE_*` にしかない」と読めるようになっていた。**頭の名詞を `実行条件` に固定するのをやめ**、テストショットの弁別的な特徴である「1回の実行を表すエントリそのもの」を軸に書き直した: `リクエスト送信・バッチ起動などの1回の実行を表すエントリ（番号・説明・期待するステータスコード）`（48字、`実行`×1。挙げた3カラムはすべて `:389`〜`:394` で実在を確認済み） |
| 意味の反転の是正（R4-2） | `:1220` はラウンド3が「手動で**テストを**行う」から目的語を落としたため、直前が「長さを揃える」であることと相まって**「長さ揃えを手動でやる」**と読めるようになっていた。原文の趣旨は「そのテスト自体を手動で実施する」であるため `（長さを揃えられない内容のテストは、手動で実施する）` に戻した |
| 落ちた限定の復帰（R4-3） | `:40` はラウンド3が主語を1つ戻す代わりに `テスト終了時`→`終了時`、`他のテストに`→`他に` と限定を2つ落としていた（「他に」は単独で名詞として立たない）。両方を戻した |
| `用途` の衝突の解消（R4-4） | ラウンド3が `利用例`→`用途` に寄せた結果、`:126` の「データタイプは、データブロックの**用途**を示す予約語」という定義と、`:354`「1つのデータタイプが複数の用途を持つ」が正面から衝突していた（`:104` の列見出し `用途`〈3値〉・`:147` の列見出し `用途`〈14データタイプ〉と合わせて同一ページで3粒度）。**`利用例` に戻すのも不可**（`ja/` 配下に先例0件）。`用途`・`利用例` のどちらも使わず、ページが既に確立している語彙で `:354`「これは ``LIST_MAP`` というデータタイプを**使うデータブロック**の1つであり」・`:586`「**このデータタイプを使うデータブロックのうち**、テストショット一覧（``testShots``\ ）のカラムについては、前述の :ref:`…` で処理方式ごとに説明している」と言い換えた（`:586` は「処理方式ごとに前述の」が一続きに読める問題も同時に解消）。`:104`・`:126`・`:147` の列見出しと定義文には触れていない |

**`#11` 以降への含意**: 上のラウンド3の含意（頭の名詞と例示の向きを揃える）は正しいが、**それを適用した書き換えが事実誤りと語釈の消失を招いた。** 推敲のための書き換えでも、挙げる語がページ内に実在するかと、新語の語釈が本文に残るかを毎回確認すること（申し送り24）。

### `#11` 以降への申し送り（追加）

16. **`テストケース` は使わない。** `テストショット`（数えられるもの、`no`／`title`／`description` などのカラムを持つもの、表の行を指すもの）／`テストメソッド`（`@Test` を付けた Java のメソッドそのものを名指しするもの）／`テスト`（何を検証するかを述べているもの。普通名詞）のいずれかを、この基準で1件ずつ選ぶ。一括置換をしない。用語集の根拠は `mapping/glossary.md` §5.7・§5.8
17. **セクションの並びは「作業の入口から詳細へ」を原則とする。** 読者が最初に書き始めるもの（作業の入口）を先に置き、その中で使う個別の記法（詳細）を後に置く。**「AはBの一種である」「AはBの用途の1つである」といった包含関係を優先して B を先に置くと、作業順と一致しない。** 包含関係は、入口側の節の冒頭に1文と `:ref:` を置いて手当てする。`#10a` の実例では、`テストショット一覧` は `LIST_MAP` の**用途の1つ**（構成要素ではない）だが、読者が最初に書くものであるため `LIST_MAP` より前に置いた。**並び順の判断はこのとおりで正しい。**「全体→構成要素」という言い方は本件の関係を取り違えるため使わない
18. **`"null"`（クォートあり）の扱いについて、出典が実装・確定設計と食い違っている。** 本ページの `null（データベースに null を格納）` 行（YAML形式の表）と `文字列の null`＝`該当なし` は正しい。誤っている出典と正しい出典、機構の根拠は `reviews/page-testdata_examples.md` の「出典と本文の食い違い（C-4）」に file:line で記録した。`NullInterpreter`・特殊記法・YAML のクォートルールを扱うページでは同じ食い違いが再燃するため、出典をそのまま写さないこと

**`#10a` 差し戻し対応ラウンド3（2026-08-07）で「本タスクでは対応しない」と判断した指摘を、以下 19〜23 に申し送る。** いずれも本タスクの範囲外と判断したものであって、指摘が誤っているという意味ではない。全体の記録は `checks/task-terminology.md` §12。

19. **STEP 4 の改題（`値を特殊記法で記述する` → `null・空文字・改行など特殊な値を記述する`）が、本ページの導線文に伝播していない。** `:126`（「特殊記法・コメント・マーカーカラムはセル値やエントリ値の解釈規則として…」）・`:138`（「値の解釈規則である特殊記法・コメント・マーカーカラム」）・`:348`（「値の解釈規則（特殊記法、コメント・マーカーカラム・空エントリ）」）は、いずれも改題前と同じく `特殊記法` という語で節を指しており、改題後の見出し文言と一致しない。`#10a` は見出しの改題のみを指示範囲としており、本文編集は禁止事項の例外として最小限に限る運用のため本ラウンドでは変更しなかった。**本ページに次に触れる際、導線文の語を見出しに合わせるか、見出しを指す語として `特殊記法` を使うことを規約として明文化するかを決めること。**（`#10a` 再レビュー 設計#4）
20. **`:126`・`:138` のデータタイプの列挙が `テーブル`・`LIST_MAP` を落としている。** 両行は「グループID・テストショット一覧・ファイルデータ・メッセージング」の4つしか挙げないが、`使用方法` 配下は `テーブル`・`LIST_MAP` を含む5対象＋解釈規則で構成されている（`:348` の列挙とも一致しない）。**この穴は基準コミット `6ba0d2a` 以前から存在し、`#10a` 由来ではない**（`git show 6ba0d2a:….rst` の `:126`・`:138` も同じ4つで、語が `テストケース一覧` だった点だけが異なる）。本ページに次に触れる際に、`:126`・`:138`・`:348` の3つの列挙を揃えること。（`#10a` 再レビュー 設計#8）
21. **ゲート文の「各1件」のような件数表現は、何を数えた件数かを指示書側に明記すること。** `#10a` のゲート7・8 は「新見出しが各1件」と書いているが、`:ref:` のリンクテキストも同じ見出し文言に揃えるよう同じ指示が求めているため、文字列の出現数では成立しない（実測は notation 4／examples 3）。記録側では「見出し行としての件数」と明示して整合させたが、**指示書の字句自体は本タスクの変更対象外のため直していない。** 次に同種のゲートを書く際は「見出し行として各1件」と書くこと。（`#10a` 再レビュー QA S-1）
22. **ゲートの設計として、`mapping.csv` の数値不変チェック（`#10a` ゲート1）は差分空チェック（ゲート2）から自動的に従う。** 冗長であって誤りではないため本ラウンドでは変更していない。次にゲート表を設計する際は、独立に壊れうる性質だけをゲートにすること。（`#10a` 再レビュー QA S-2）
23. **`ja/development_tools/testing_framework/guide/` 配下に、どの `.rst` からも参照されない遺留アセットが残っている。** 実測で **125ファイル・4.9MB**、`.rst` は **0件**。`テストケース` は `_download/UserRegistrationFormTest.java`（4件）・`_download/SystemAccountEntityTest.java`（2件）の**計6箇所**と、**ファイル名** `_download/ユーザ登録_UserComponent_クラス単体テストケース.xlsx`（`grep -rn 'ユーザ登録_UserComponent' ja/ en/` が0件＝どこからも参照されない孤立資産）に残る。公開物に出ず `#10a` のゲート3（`.rst` 対象）の範囲外であるため本ラウンドでは触れていない。**32ページ完了時（`#last`）の棚卸し対象とすること。**（`#10a` 再レビュー QA S-3／検証 R3-9）

    **追記（差し戻し対応ラウンド4 R4-6 h）**: 上の棚卸し対象は `guide/` 配下の `.java` 6箇所とファイル名1件しか挙げていなかったが、実測すると `.rst` にも `テストケース` が残る。`grep -rn 'テストケース' --include=*.rst ja/ en/` の全ヒットは **3件・2ファイル**である。(1) `en/development_tools/testing_framework/guide/development_guide/06_TestFWGuide/01_Abstract.rst:66` — 行頭が `..` のコメント行であり出力されない。`en/` 配下は `#10a` のゲート3（`ja/development_tools/testing_framework/` 配下が対象）の射程外。(2) `ja/biz_samples/11/index.rst:64`・`:68` — 業務サンプル集のページであり NTF解説書32ページに含まれない。**いずれも `#10a` の射程外であるため本ラウンドでも触れていないが、`#last` の棚卸しでは `.java`・`.xlsx` と合わせてこの3件も対象にすること。**

**`#10a` 差し戻し対応ラウンド4（2026-08-07）で追加した申し送りを、以下 24〜27 に記す。** 全体の記録は `checks/task-terminology.md` §13。

24. **定義文・定義セルを書き換えるときは、そこで挙げるカラム名・語がそのページに実在するかを `grep` で必ず確認すること。** `#10a` のラウンド3 は、本ページ `:108` の定義セルを「頭の名詞と例示の向きを揃える」目的で書き換えた際に、`testShots` のカラムではない `リクエストID` を例に挙げてしまった（実際は `context` が指す別の `LIST_MAP` の `REQUEST_ID` カラムであり、バッチ・メッセージングの `testShots` カラムは `requestPath`）。**推敲のための書き換えが公開本文に事実誤りを持ち込んだ。** あわせて、その書き換えで `1回の実行` の語釈（`リクエスト送信・バッチ起動など`）が両ページから消え、本タスクが新たに導入した `テストショット` の意味を本文だけで読み取れなくなっていた。**定義を短くするときは、(a) 挙げる語の実在、(b) 新語の語釈が本文のどこかに残ること、の2点を書き換えのたびに確認する。**（`#10a` 再レビュー ラウンド4 の主指摘。是正は `checks/task-terminology.md` §13 R4-1）
25. **`:98` の「3つの用途」と `testdata_examples.rst:18` の「4つのデータブロック」が食い違っている。** 本ページの3値（テストショット／準備データ／期待値）に対し、記載例ページは期待ログを加えた4つを挙げており、`LIST_MAP=expectedLog` が3値のどれに属するかを本ページの表が示していない。**この食い違いは基準コミット `6ba0d2a` 以前から同文であり `#10a` 由来ではない**（`git show 6ba0d2a:….rst` の `notation:98`・`examples:18` も、語が `テストケース` だった点しか違わない）。本ページに次に触れる際、期待ログを3値のどこに位置づけるかを決めること。（`#10a` 再レビュー ラウンド4 設計 提案7）
26. **新しい語を導入するページでは、「その語の初出に本文だけで読める語釈があること」をゲートに含めるとよい。** `#10a` は `テストショット` を新たに導入したが、語釈は用語集（`mapping/glossary.md`）と本ページ `:108` の定義セルにしかなく、ラウンド3 で定義セルから語釈が落ちた時点で本文から語釈が消えた（ゲート1〜17 のどれもこれを検出できなかった）。**読者は用語集を読まない。**（`#10a` 再レビュー ラウンド4 QA 提案1。本タスクのゲートはユーザーの作業指示が定めたものであるため `#10a` では追加していない）
27. **用語の残存チェック（`#10a` ゲート3）は `.rst` だけを対象にしており、公開物としてはビルド後の HTML・`searchindex.js`・`_download/` の配布物が残る。** `#last` の棚卸しでは、`.rst` ではなく**生成された公開物**を対象に数えること。（`#10a` 再レビュー ラウンド4 QA 提案2。申し送り23 と同じ棚卸しで扱う）

## `#10b`（`#10a` 承認後の仕上げ3件、2026-08-07）

user review ではなく、`ntf-doc-10a-followup.md` による指示作業である。既存のレビュー記録は書き換えていない（本節の追記のみ）。本ページに関わるのは STEP 4 の1件である。

| 項目 | 内容 |
|---|---|
| 定義セルの括弧書き削除（STEP 4） | `データブロックとデータタイプ` の表（`:104`〜`:115`）で、`用途`＝`テストショット` 行の `内容` セルから括弧書きを削除した。`リクエスト送信・バッチ起動などの1回の実行を表すエントリ（番号・説明・期待するステータスコード）` → `リクエスト送信・バッチ起動などの1回の実行を表すエントリ`。**言い換えて残していない。主文は1文字も変えていない** |
| 削除の根拠 | 括弧書きが**エンティティバリデーション体系に当てはまらない**。作業指示が示す根拠は `nablarch-testing` の `EntityTestSupport.java:69-74` の `REQUIRED_COLUMNS_FOR_RELATIONAL_VALIDATION`（`title`・`expectedMessageId1`・`propertyName1` であり `no`・`description`・`expectedStatusCode` を含まない）である（**`nablarch-testing` は本リポジトリ外のため未照合**）。本ページ自身も `使用方法` 側で「エンティティバリデーションを除く各処理方式に共通する必須カラム」と断っており、`mapping/glossary.md` §5.7 の定義（「バリデーション実行など」を含む）とも整合しない。カラムの内訳は `使用方法` 側で処理方式ごとに正確に説明されているため、`テストデータの構造` の概観表で繰り返す必要がない |
| 語釈の維持 | 主文に `リクエスト送信・バッチ起動などの1回の実行` が残るため、申し送り24 が求める「新語 `テストショット` の語釈が本文に残ること」は満たしている |
| 経緯 | **このセルはラウンド2〜4で3回連続して是正対象になっていた**（R2「1エントリ1テストショットの実行条件」の循環定義 → R3「実行条件（番号・説明・リクエストIDなど）」の事実誤り → R4 で現行文言）。`#10b` はそのうち括弧書きを落として主文だけを残す最終形である |
| 見出し・並び順 | 変更していない（`~` 10件・`^` 26件、文言・並び順とも `699dfd0` と完全一致） |
| 記録 | `checks/task-10a-followup.md` §4・ゲート6 |

### `#11` 以降への申し送り（追加）

**`#10b`（`ntf-doc-10a-followup.md` STEP 5-3）で追加した申し送りを、以下 28〜29 に記す。** 全体の記録は `checks/task-10a-followup.md`。

28. **完了条件が「全件表」を求めている項目は、ゲートの実行順の先頭に置く。** `#10a` では同じ表（`テストケース` の置換判定表・値の種類の突合表など）の作成漏れが**3ラウンド連続**で発生した。ゲートを番号順に流すと、全件表の作成は「記録の整備」として最後に回りやすく、本文編集が終わった時点で作業を終えたと錯覚する。**表を先に作れば、表を埋める過程で本文側の欠落も見つかる。** `#10b` ではゲート7（全件表2つ）を実行順の先頭に置いた（`checks/task-10a-followup.md` §6）
29. **概観表・定義セルに具体的なカラム名を列挙しない。** 処理方式ごとにカラム体系が異なるため（エンティティバリデーションは `title`・`expectedMessageId1`・`propertyName1` で、`no`・`description`・`expectedStatusCode` を持たない）、概観表で1つの体系のカラムを挙げると他の体系に当てはまらない。**カラムは `使用方法` 側で処理方式ごとに説明する。** 本ページ `:108` の括弧書き（`（番号・説明・期待するステータスコード）`）が `#10b` STEP 4 で削除されたのはこの理由による。申し送り24（挙げる語の実在を `grep` で確認する）と対になる原則である

**`#10b` 是正ラウンド1（2026-08-07）で追加した申し送りを、以下 30 に記す。**

30. **`mapping/glossary.md` §5.7（`:201`）が定めた「3つの用途を対比する表では `実行条件` 側の言い方を使う」という標準と、本ページ `:108` の定義セルの現行文言が食い違っている。** 用語集は次のように述べている（実物を読んで引用を照合済み。**用語集は変更禁止のため本タスクでは触れていない**）。

    > 一方 `testdata_notation.rst` の `データブロックとデータタイプ` の表は、1つの読み込み単位に共存する3つの用途（テストショット・準備データ・期待値）を排他に分けることを目的とする別の表であり、そこでは兄弟行の `準備データ`（入力）・`期待値`（実行後の状態）と重ならないよう、同じ概念を `実行条件` の側から言い表している（`1回の実行に対応する、1エントリ分の実行条件`）。**両者は同じものを指す。** 残り32ページでも、用語そのものを説明する場面では本項の定義を、3つの用途を対比する表では `実行条件` 側の言い方を使う

    しかし現行のセル（`:108`）は `リクエスト送信・バッチ起動などの1回の実行を表すエントリ` であり、**`実行条件` 側の言い方ではない。** 兄弟行は `:111` が `テスト実行前にデータベース・ファイルへ投入するデータ`、`:114` が `テスト実行後に確認する状態（データベース・出力ファイル・電文・検索結果など）` である。**この食い違いは `#10a` ラウンド4 でセルを現行文言に書き換えた時点で既に生じており、`#10b` STEP 4 の括弧書き削除（`（番号・説明・期待するステータスコード）` の除去）でさらに広がった**（括弧書きが残っていた間は、括弧内が実行条件の内訳を示す形になっていた）。

    **残り32ページに波及する標準であるため、`#11` 以降で次のどちらかを判断すること。** (a) 用語集 §5.7 の当該記述を現行のセルに合わせて更新する（`実行条件` 側の言い方を標準としない）、(b) セルを `実行条件` 側の言い方に戻す（ただし申し送り24 が求める「新語 `テストショット` の語釈が本文に残ること」を壊さないこと。`リクエスト送信・バッチ起動などの1回の実行` の語釈は本ページではこのセルにしかない）。**`#10b` の作業指示は `glossary.md` を変更禁止とし、STEP 4 で「言い換えて残さない。削除する」と明示しているため、本タスクではどちらも実施していない。**

**`#10b` 是正ラウンド3（2026-08-07）で追加した申し送りを、以下 31 に記す。**

31. **本ページの `null・空文字・改行など特殊な値を記述する` 配下にある記載例ページへの前方参照3箇所が、無条件のまま残っている。** 実測すると、`:1342`（L3 `null・空文字・改行など特殊な値を記述する` の導入部）・`:1393`（L4 `Excel形式の場合` の末尾）・`:1443`（L4 `YAML形式の場合` の末尾）の**3箇所**が、いずれも1行まるごと次の同一文である（`grep -n "testdata_examples-special_notation"` のヒットもこの3件だけである）。

    ```
    実際の記述例は :ref:`null・空文字・改行など特殊な値を記述する <testdata_examples-special_notation>` を参照。
    ```

    一方、その参照先である `testdata_examples.rst` の当該節（`:1847`〜`:1993`）の導入文（`:1851`）は、`#10b` の是正を経て**現在**次のようになっている。

    > 特殊記法を使う値の記述例を示す。記法の仕様は\ :ref:\`null・空文字・改行など特殊な値を記述する <testdata_notation-special_notation>\`\ を参照。なお、空文字・改行の記述例は、この節では示していない。

    本ページの記法表は12種の値（`通常の文字列`／`null`／文字列の `null`／空文字／先頭ゼロ付き数値／`true`・`false`／半角スペース1文字／ダブルクォート1文字／`${systemTime}` 系／`${binaryFile:パス}`／`${文字種,文字数}`／改行文字）を扱うが、記載例ページに専用の例があるのはそのうち6種である（突合の全件は `checks/task-10a-followup.md` §3-2）。**空文字・改行の記法を読んだ読者が `:1393`・`:1443` の無条件な「実際の記述例は…を参照」に従って記載例ページへ飛ぶと、「この節では示していない」に突き当たり、書き方 → 記載例 → 書き方 の往復で行き止まりになる。**

    **`#10b` の作業指示は本ページへの是正を STEP 4（`:108` の定義セルの括弧書き削除）のみに限っているため、この3箇所は変更していない。** `#11` 以降で (a) 記載例ページ側の見出しを実態に合わせる、(b) 本ページの前方参照3箇所に条件を付ける（例が無い値の種類には送らない）、(c) 記載例ページに不足する例を追加する（`mapping.csv` の割当見直しが前提）のいずれかを選ぶこと。

    **本項は `page-testdata_examples.md` の申し送り22 と同一の問題を、直す対象が置かれている本ページ側にも記録するものである**（申し送り22 は記載例ページの導入文の側からこの問題を記述しており、本ページの申し送りには対応する記録が無かった。設計レビュー2巡目の `should`）。本ページ側で直す場合の対象行は上記の `:1342`・`:1393`・`:1443` である。

**`#10b` 締め（2026-08-07、作業指示 `ntf-doc-10b-close.md`）で追加した申し送りを、以下 32 に記す。本ページの本文は締めでも変更していない**（締めの本文是正は `testdata_examples.rst:401` の1行のみ）。

32. **完了条件が「全件表」を求める項目は、母集合をホワイトリストで切り出さない。** 対象を全走査したうえで、非該当と判定したものも判定理由を添えて表に載せる。`#10b` STEP 1 の数詞表はホワイトリスト方式（助数詞のリストに載っている語だけを母集合に入れる方式）を採ったため、**3巡のレビューすべてで「リストに無い語」の指摘を受け、そのたびに表へ追加する対応を繰り返した**（1巡目3件で25行→29行、2巡目6件で29行→35行、3巡目は3観点が独立に計9出現を指摘）。**追加された語はいずれも数を数えておらず、本文の是正は一度も発生していない。** 方式が網羅性を証明できない限り、同じ指摘は何巡でも再発する。`#10a` の申し送りと合わせて `#11` の作業指示に反映すること。**同一の申し送りを `page-testdata_examples.md` の 23 にも記録している。**

    なお `#10b` では、レビュー役がホワイトリストを使わない全走査を独立に組んで検証し、**既存の35行の表が取りこぼしている真の数詞は0件**であることを確認した（`checks/task-10a-followup.md` §1-2 冒頭・§8-2）。未達だったのは「表が全件でないこと」ではなく「**同じ表が全件であることを、その抽出方式では証明できないこと**」であり、表の作り直しは行わなかった。申し送りは方式そのものに対するものである。

**`#18`（2026-08-13、作業指示 `ntf-doc-18-default-value-basis.md`）のゲート2 で本ページを全走査した結果を、以下 33 に記す。本ページの本文は `#18` では変更していない**（`#18` の `ja/` 側の変更は `setup/request_unit_test/web.rst` と `setup/common.rst` の2ファイルのみ）。

33. **「デフォルト設定」という語が、`design.md` §8 が定めた意味と衝突しうる。** `#18` で
    `design.md` §8 に「設定項目表の『デフォルト値』の基準」を規定し、**「デフォルト設定」を
    `com.nablarch.configuration:nablarch-testing-default-configuration`（テスティングフレームワークが
    提供するコンポーネント定義一式）を指す語**として使うことにした。本ページの `:1294`・`:1389`・`:1441`
    は、これより前から「デフォルト設定」を**`LineSeparatorInterpreter` の未設定時の挙動**という別の意味で
    使っている（例: `:1389`「デフォルト設定では CR のみが変換対象」）。

    **値の食い違いは無い。** デフォルト設定の `nablarch/test/test-data-interpreter.xml` は
    `lineSeparatorInterpreter` を**プロパティ無しで登録**しており（同ファイルで確認）、フィールド初期値が
    そのまま実効値になるため、本ページの記述は実効値と一致している。`#18` のゲート2 では
    **非該当（一致）**と判定し、作業指示が範囲を `web.rst`・`common.rst` の2ファイルに限定していることから
    是正していない。

    `#19` 以降で本ページに手を入れる機会があれば、`:1294`・`:1389`・`:1441` の「デフォルト設定」を
    「既定の設定」「初期状態」など別語に言い換えるか、`nablarch-testing-default-configuration` を指す
    用法と区別が付く形にすることを検討すること。語の衝突は残り27ページの設定項目表で
    「デフォルト設定」を頻用することにより顕在化する。

## `#23`（テーブルデータの0件の扱い、2026-08-14）

作業指示は `.rn/20260724-ntf-yaml-support/ntf-doc-23-table-zero-rows.md`。self-check は `checks/task-23.md`。

### 追記した内容

| 箇所 | 内容 |
|---|---|
| `:650`（既存行の書き換え1文） | L3「テーブルのデータを記述する」の予告の列挙に「0件のデータの扱い」を加えた。**本タスクで書き換えた既存行はこの1行だけ** |
| `:729-738`（新L4「0件のデータを記述する」） | 意味の説明のみ。データ行を1行も記述しないことで0件にできること／準備データを0件にすると前述の全件 DELETE だけが行われテーブルが空になること／期待値を0件にするとレコードが1件も無いことの検証になること。末尾に記載例ページの新L3へ `:ref:` を張った |
| `:787-801`（既存L4「Excel形式の場合」への追記） | セル格子を1つ追加（識別子行＋カラム名の行のみ・データ行なし）。あわせて「データ行を書かない場合でもカラム名の行は省略できない。識別子行の次の行がカラム名の行として読み込まれるため、書かないと次に現れた行がカラム名の行になる」を追記 |
| `:834-841`（既存L4「YAML形式の場合」への追記） | `rows: []` の `code-block:: yaml` を1つ追加 |

配置は `style.md` S-10 規約3 に従い、新L4を形式別L4対（`Excel形式の場合`・`YAML形式の場合`）より前に置いた。**本タスクで形式別L4対は新設していない**（`checks/task-23.md` ゲート7）。

### `design.md` §8「出典が書いていない適用範囲・副作用の追記」に当たると判断した理由

テストデータのテーブルを0件で書く方法は、34ページのどこにも書かれていない。判定基準は「書かなければ読者が誤るか」であり、準備データを0件にしてテーブルを空にすること・期待値を0件にしてレコードが1件も無いことを検証することは、いずれも読者が普通に必要とする書き方であるため、書き方が無ければ読者は書けない（誤る）。`design.md` §8 の既存の類型（`#21` 確定）で足りるため、**新しい類型は追加せず `design.md` は変更していない**。出典・マッピングにこの主題の行は無いため `mapping.csv` も変更していない。

### `design.md` §8 の実装優先の原則を本件に適用しないというユーザー判断

`design.md` §8 は「出典と実装が食い違う場合は実装を優先する」と定めているが、**本タスクではこの原則を適用しないというユーザー判断が 2026-08-13 に下された**。`expected_tables:` に `rows: []` を書いたときの期待値0件の検証は、`190cc9a` 時点の `nablarch-testing-yaml` では実際には行われない（下表の事実18・14・15 → 事実1・2・9）。それでも解説書は、テスティングフレームワーク本体が追随する前提で仕様どおりに書く。したがって本文には、形式・データタイプによる場合分けも、実装の制約を注記する記述も置いていない。この判断の `design.md` への規定化は本タスクでは行わない（同種の判断が再度出た時点で判断する）。同じ内容を `checks/task-23.md` にも記録している。

### 実装で確認した `file:line`（参照コミット `e21bf67` / `190cc9a`）

`nablarch-testing` は `e21bf67`（`main` / タグ `2.2.0`）、`nablarch-testing-yaml` は `190cc9a`（`feature/ntf-yaml` の最新のソースコミット）。すべて `git show <コミット>:<パス>` で開いて確認した。

| # | file:line | 事実 |
|---|---|---|
| 1 | `nablarch-testing` `src/main/java/nablarch/test/core/db/TableData.java:337-346` | `loadData()` は列名が0個のとき、空のリストを入れて `return` する |
| 2 | 同 `:348` | SELECT 文の組み立てはガードの後にある。列名が0個なら SQL は発行されず常に0行が返る |
| 3 | 同 `:489-493` | `setColumnNames` は長さ0の配列をそのまま代入する（`null` にはならない） |
| 4 | 同 `:501-505` | 列名がデータベースの全カラムに落ちるのは、列名が `null` のときだけ |
| 5 | 同 `:706-722`（特に `:721`） | `fillDefaultValues()` は最後に `setColumnNames(allColumns)` を実行する |
| 6 | 同 `:127-130` | `deleteData` は `DELETE FROM <テーブル名>` であり、列名を使わない |
| 7 | 同 `:137-178`・`:325-334` | `insertData` は `getNonComputedColumns()` を使う。テストデータに書いた列名は使わない。**指示書は範囲を `:137-217` としていたが、実体は `:137-178` である**（`:180-188` は次のメソッド `convert` の javadoc）。事実の内容は一致 |
| 8 | `nablarch-testing` `src/main/java/nablarch/test/Assertion.java:79-83` | `assertTableEquals` は期待値を clone して `loadData()` した結果を実際の値にする |
| 9 | 同 `:259`・`:263`・`306-313` | 比較はループ2つだけで、期待値0行・実際の値0行ではどちらも1回も回らない |
| 10 | `nablarch-testing` `src/main/java/nablarch/test/core/db/DbAccessTestSupport.java:363-369` | `failIfNoDataFound` が捕まえるのは期待値のデータブロックが1つも無い場合だけ |
| 11 | `nablarch-testing` `src/main/java/nablarch/test/core/reader/TableDataParser.java:89-97`（`:94`・`:96`） | 識別子行の次の行をカラム名の行として読み、そこから列名を決める |
| 12 | `nablarch-testing` `src/main/java/nablarch/test/core/reader/TestDataParsingTemplate.java:176-178` | 全セルが空の行は読み飛ばされる |
| 13 | `nablarch-testing` `src/main/java/nablarch/test/core/reader/BasicTestDataParser.java:171-181`（`:177`） | Excel形式では `EXPECTED_COMPLETE_TABLE` に対して `fillDefaultValues()` が実行される |
| 14 | `nablarch-testing-yaml` `src/main/java/nablarch/test/core/reader/yaml/YamlSection.java:156-161` | `resolveColumns` は `rows` が空なら空のリストを返す |
| 15 | `nablarch-testing-yaml` `src/main/java/nablarch/test/core/reader/yaml/YamlTableDataBuilder.java:110-115` | 列名0個のまま `TableData` を生成する。`:110-114` に偽陰性の `FIXME` がある |
| 16 | 同 `:127` | `fillDefaults` が真なら `fillDefaultValues()` が実行される |
| 17 | `nablarch-testing-yaml` `src/main/java/nablarch/test/core/reader/YamlTestDataParser.java:114` | `setup_tables` は `fillDefaults=false` |
| 18 | 同 `:123` | `expected_tables` は `fillDefaults=false`。**記録のみ・本文には反映していない** |
| 19 | 同 `:125` | `expected_complete_tables` は `fillDefaults=true` |
| 補-1 | `nablarch-testing` `src/main/java/nablarch/test/core/db/DbAccessTestSupport.java:182-201` | 準備データの投入は `List<TableData>` が空のときだけ早期 `return` する（`:184-186`）。行数0の `TableData` は要素として存在するため早期 `return` に掛からず、`:195` の `deleteData` が実行される。**「準備データを0件にすると DELETE だけが行われ、テーブルが空になる」という本文の記述の裏付け**（本タスクで独自に確認） |

### 申し送り

34. **`expected_tables:` に `rows: []` を書いたときの期待値0件の検証は、`190cc9a` 時点の
    `nablarch-testing-yaml` では行われない。解説書は、本体が追随する前提で仕様どおりに書いている**
    （2026-08-13 ユーザー判断）。本体の対応が入るまで、この記述は実装に先行している。本体の対応方針が
    決まった時点で、`testdata_notation.rst` の「0件のデータを記述する」と「YAML形式の場合」、
    `testdata_examples.rst` の「0件のテーブルデータを記述する」の3箇所を見直す。

### 4観点レビュー ラウンド1（2026-08-14）

4観点をそれぞれ別のサブエージェントで実施した（A:網羅性 / B:トンマナ / C:用語 / D:整合性）。依頼プロンプトには Rules の3点（実測で裏付ける／付属の検証スクリプトを正解にしない／敵対的にレビューする）と、指示書 §7 の申し送り段落をそのまま入れた。

| 観点 | 判定 | 重複除去前の件数 |
|---|---|---|
| A 網羅性 | PASS | `should` 2 / `note` 5 |
| B トンマナ | PASS | `should` 2 / `note` 4 |
| C 用語 | FAIL | `must` 1 / `should` 4 / `note` 3 |
| D 整合性 | FAIL → 再走で PASS | `must` 2（1回目）/ `should` 4 / `note` 4 |

重複を除いた指摘と対応は次のとおり。

| # | 観点 | 指摘 | 対応 |
|---|---|---|---|
| R1-1 | C `must` / A `should` / D `should` | 「空の配列」が既存表記「空配列」と揺れている（既存3件は `testdata_notation.rst:881`・`:1144`、`testdata_examples.rst:1623` ですべて「空配列」） | **是正。** `testdata_notation.rst:834` を「``rows:``\ に空配列 ``[]``\ を記載する。」（`:1144` と同型）、`testdata_examples.rst:989` を「``rows:``\ に空配列を記述する。」（`:1623` と同文）に改めた |
| R1-2 | A `should` / D `should` | `:733` の「前述の全件 DELETE」の先行詞が `:666` にあり、67行・L4 3つを隔てているため届かない | **是正。** 「``SETUP_TABLE``\ による登録で行われる全件\ DELETE」に改め、`:666` の文言（「``SETUP_TABLE``\ による登録は、対象テーブルを一旦全件\ DELETE\ したうえで…」）と接続を明示した。説明自体は書き直していない |
| R1-3 | C `should` / B `should` | `:733` の「行を0件にした準備データ」だけが「行」を「件」で数えており、他5箇所はデータ側に掛かる。語順も「1件もレコードが無い」と「レコードが1件も無い」で割れている | **是正。** 「レコードが1件もない状態からテストを始めたい場合は、0件の準備データを記述する。」に改めた |
| R1-4 | C `should` | `:650` の列挙語「0件のデータの扱い」が節タイトルと対応していない（他項は下位節タイトルの名詞化） | **是正。** 「0件のデータの記述」に改めた |
| R1-5 | C `note` / B `should` | 「無い」の漢字表記が両ページの多数派（かな）と逆。追記で1→4件に増えた | **是正。** 追記4箇所すべて「ない」に改めた（既存の `:1533` は触っていない） |
| R1-6 | D `must`（2回目の走行では取り下げ）/ A `note` | `testdata_examples.rst:955` が、直前のL3と同じ `ORDER_HEADER` を同じ「バッチ処理」で扱いながら「2件が登録される」「1件も無い」と相反して読める | **是正。** 導入文を「処理対象のデータが1件もない場合のバッチ処理を実行し」に改め、直前のL3とは入力が異なる別の場面であることを明示した。テーブルは指示書 §4-3 が「既存のL3が使っている `MEMBER` などと揃える」としているため変更していない |
| R1-7 | D `must`（1回目の走行） | `:800` の「その次に現れた行がカラム名の行になる」が実装と食い違う。次が別のデータタイプの識別子行なら `TestDataParsingTemplate.java:203-217` で読み込みが `break` する | **Invalid。是正しない。** 指摘が引用した `isOtherType` / `foundTargetType` は `e21bf67` の `TestDataParsingTemplate.java` に**存在しない**（`grep` でヒット0件。実物の `parse` は `:193-219` で `nowReading` と `shouldStopOnNextOne()` を使う）。さらに `TableDataParser.java:93` の `onTargetTypeFound` が `readLine()` で次の行を**自ら消費する**ため、その行が識別子行であってもメインループの `break` 判定に届かず、カラム名の行として読まれる。本文の記述が正しい。**同じサブエージェントの2回目の走行では PASS（`must` 0）になっており、この指摘は取り下げられている** |
| R1-8 | D `should` 1・2 | 「0件で書く」と「ブロック自体を書かない」の違い、FK の親テーブルを空にする用途を書くべき | **対応しない。** 指示書 §3 が「次の3点だけを書く。これ以外を書かない」と定めており、いずれも3点の外である。申し送りとする（下記 35） |
| R1-9 | D `should` 3 | `:817` の「各エントリに ``table`` キーが必須」は、スキーマが `table`・`rows` の2キーを required にしているため片手落ち | **対応しない。** `:817` は既存行であり、指示書 §4-2 が「既存の行を書き換えないこと」と定めている。申し送りとする（下記 36） |
| R1-10 | D `should` 4 / C `should` 4 / B `note` | `:420`・`:423`・`:432` の「空の場合は」（カラム値が空欄の意味）と、`:772`・`:785` の「ヘッダ行」（`:741` は「カラム名の行」）が、追記した語と衝突・混在する | **対応しない。** いずれも既存行であり指示書 §4-2 の禁止に当たる。追記側は同節冒頭の `:741` と記載例側 `:901`・`:985` の語（「カラム名の行」）に揃えてある。申し送りとする（下記 37） |
| R1-11 | B `note` / D `note` | 形式別L4の末尾の参照（`:802`・`:842`）が `testdata_examples-table_data` を指すため、形式別の節から0件の記載例に辿り着けない | **対応しない。** 節末の参照は「そのL3に対応する記載例1つ」という既存25件の型（B が実測）に従っており、0件の記載例への導線は共通部の `:737` に集約している。指示書 §4-3 も `:737` からの参照を指定している |
| R1-12 | A `note` 1 / B `note` 4 / C `should` 1 | 記法ページの見出し「0件のデータを記述する」と記載例ページの「0件のテーブルデータを記述する」が非対称。他の9組はタイトル完全一致 | **対応しない（ユーザー判断事項として報告）。** 見出し文言は指示書 §4-1・§4-3 が明示的に指定している。3観点が独立に挙げたため、`checks/task-23.md` に報告として記録した |
| R1-13 | D `note` 2 | `:787` と `:834` の「準備データ・期待値のいずれでも同じである。」が逐語重複 | **対応しない。** `style.md` S-10 の前提（読者は片方の形式しか読まない）により、形式別の節はそれぞれ単独で読まれる。共通部にあるからといって形式別の節から落とすと、片方しか読まない読者に届かない |
| R1-14 | B `note` 5 / D `note` 4 / C `note` 3 | `testdata_examples.rst:951` は同ページで唯一L3に付いたセクションラベル。ラベル名 `empty_table` と本文の「0件」で語が異なる | **対応しない。** 参照が必要である以上この位置以外に選択肢がなく（B）、ラベルは識別子として `glossary.md` §3 の掲載対象外である（C）。指示書 §4-3 がラベル名を指定している |
| R1-15 | D `note` 1 | 識別子行が読み込み単位の最後の行になった場合の挙動（カラム名0個になる）にも `:800` を広げるべき | **対応しない。** 指示書 §5 が「読み込みが別のデータタイプで止まる」挙動を扱わないよう定めており、同種の周辺挙動である。A も「指示書 §3-3 の文言のままでも可」と判定している |

**是正後の再ゲート**: ゲート3〜5・9〜12 を再実行して全件 PASS、Docker フルビルド（`-a`）は `build succeeded, 1 warning.`（既知の `db_double_submit.rst:108` のみ・新規0件）。詳細は `checks/task-23.md`。

### 申し送り（続き）

35. **「0件で書く」と「そのテーブルのブロック自体を書かない」の違いは、どのページにも書かれていない。**
    実装上この2つは非対称に効く。準備データはブロックを書かなければ `DELETE` が起きず、0件で書けば
    `DELETE` される（`DbAccessTestSupport.java:182-201`）。期待値はブロックを書かない場合、
    `failIfNoDataFound` の値によって検証スキップと例外に分かれる（同 `:363-369`）。
    `#23` では指示書 §3「次の3点だけを書く」の範囲外として扱った。
36. **`testdata_notation.rst:817` の「各エントリに ``table`` キーが必須」は、`table`・`rows` の
    2キーが必須である**（`nablarch-testing-yaml` `190cc9a` の `ntf-testdata-yaml-schema.json` の
    `$defs.table_data` が `required: ["table","rows"]`）。同ページのファイルデータ側 `:1144` は
    3キーを列挙しており、テーブルデータ側だけが片手落ちである。`#23` では既存行の書き換えが
    禁止されていたため是正していない。
37. **同一ページ内の語の衝突2件。** (a) `:420`・`:423`・`:432` の「空の場合はスキップされる」は
    カラムの値が空欄という意味で、`#23` で追加した「データ行が0件」と語が衝突する。
    (b) `:772`・`:785`・`:656` の「ヘッダ行」は、`:741` の「カラム名の行」と同じ行を指している
    （既存で notation 4件対1件、examples は「カラム名の行」のみ）。いずれも既存行の書き換えに
    当たるため `#23` では扱っていない。

## `#35`（読み込み時の整形・補完の表に「テーブル・`LIST_MAP`」の行を追加、2026-08-21）

`:1546`-`:1547` に次の1行を追加した（`:1544`-`:1545` の「ファイル・メッセージ」の行は変えていない）。**次の逐語は本ラウンド時点のものであり、現在は残っていない。**是正1 の追補と是正2 §2 が表の4行を差し替えた。現在の逐語は本ページ §「`#35`-是正4（「ディレクティブ行」を加えて `:1545` を確定、2026-08-24）」にある。

```
  * - テーブル・\ ``LIST_MAP``
    - カラム名の行の行末の空セルを取り除く。カラム名が無い位置のセルは読み込まれない（\ Excel\ 形式のみ）
```

### 出典（すべて `nablarch-testing@e21bf67`。`git show e21bf67:<path>` で読んだ）

| 記述 | `file:line` | 逐語 |
|---|---|---|
| カラム名の行の行末の空セルを取り除く | `src/main/java/nablarch/test/core/reader/HeaderLine.java:33` | `List<String> keys = trimTailCopy(headerLine);   // キャッシュを破壊しないようにコピーして編集` |
| 同上（テーブル系の呼び出し元） | `src/main/java/nablarch/test/core/reader/TableDataParser.java:93` | `header = new HeaderLine(readLine());` |
| 同上（`LIST_MAP` の呼び出し元） | `src/main/java/nablarch/test/core/reader/ListMapParser.java:64` | `header = new HeaderLine(firstLine);` |
| カラム名が無い位置のセルは読み込まれない | `src/main/java/nablarch/test/core/reader/HeaderLine.java:77` | `for (int i = 0; i < keys.size(); i++) {` |

`HeaderLine.java:77` の走査はカラム名の行の長さ（`keys.size()`）でループするため、データ行がそれより右に持つセルは戻り値に入らない。テーブル系のデータ行はこの戻り値を使う（`TableDataParser.java:80` `List<String> row = header.excludeMarkerColumns(line);`）。`LIST_MAP` は `HeaderLine.java:59`-`:67` の `getMapExcludingMarkerColumns` が同じ `excludeMarkerColumns` を経由する。

「（\ Excel\ 形式のみ）」の根拠は、この経路が `TestDataParsingTemplate` の行読み込みの上に載っており、`e21bf67` の `src/main/` に存在する `TestDataReader` の実装が `src/main/java/nablarch/test/core/reader/PoiXlsReader.java:30`（`public class PoiXlsReader implements TestDataReader {`）の1件だけであること。`grep` ではなく、`src/main/` 配下の全 `.java` を `git show` して `implements TestDataReader` を探した結果が同ファイル1件である。

### 追加した行の限定について

`HeaderLine` の走査は**カラム名の行**と**データ行のカラム名が無い位置**にしか効かない。カラム名がある位置のデータ行のセルは、行が短ければ `HeaderLine.java:81`（`String val = (i >= line.size()) ? "" : line.get(i);`）で空文字が埋め戻される。そのため行の主語を「カラム名の行」と「カラム名が無い位置のセル」に限定してある。無限定に「行末の空セルを取り除く」とは書いていない。埋め戻しの実測は `reviews/page-testdata_converter.md` §「`#35`（`:71` の段落の書き換え可否を実装から検証、2026-08-21）」に記録した。

## `#35`-是正1（「（`Excel` 形式のみ）」の成否を YAML 側の実装から確認、2026-08-21）

**結論: 「データ行を名前の幅へ揃える」処理は YAML 形式の読み込みにも存在する。したがって「（\ `Excel`\ 形式のみ）」は、この部分については成り立たない。** 作業指示 `ntf-doc-35-fix1.md` §2 の「あるなら『Excel 形式のみ』は誤りなので、直す前に報告すること」に当たるため、**本ラウンドでは `.rst` を1文字も変更しなかった。** その後、是正1 の追補と是正2 §2 が表の4行を差し替えている（本ページ §「`#35`-是正2（表の4行と `:71` の文面を確定、2026-08-24）」）。

### 参照コミット

| リポジトリ | コミット | 読み方 |
|---|---|---|
| `nablarch-testing-converter` | `e977824` | `git show e977824:<path>` |
| `nablarch-testing` | `e21bf67` | `git show e21bf67:<path>` |
| `nablarch-testing-yaml` | `190cc9a` | `git show 190cc9a:<path>` |

`nablarch-testing-yaml` は本ラウンドの作業指示が挙げていないが、YAML 読み込みの実体はこのリポジトリにある。`nablarch-testing-converter@e977824` の `src/main/java/nablarch/test/tool/converter/yaml/YamlFormatReader.java` は自前の YAML 解析を持たず、`src/main/java/nablarch/test/core/reader/YamlTestCoreAdapter.java:14`-`:20` が `nablarch.test.core.reader.yaml.YamlTableDataBuilder`／`YamlFileBuilder`／`YamlMessageBuilder` を `import` している。この3クラスは `nablarch-testing@e21bf67` にも `nablarch-testing-converter@e977824` にも存在せず（両リポジトリで `git ls-tree -r --name-only <commit> | grep -i yaml` を実行し、`reader/yaml/` 配下のビルダが出ないことを確認した）、`nablarch-testing-yaml` の `src/main/java/nablarch/test/core/reader/yaml/` にある。本プロジェクトが他ページで固定している `190cc9a` を使った（`reviews/page-testdata_converter.md` §「参照リポジトリ」）。

### 経路1: テーブル・`LIST_MAP`（YAML 側にも幅を揃える処理がある）

`nablarch-testing-yaml@190cc9a` の `src/main/java/nablarch/test/core/reader/yaml/YamlTableDataBuilder.java`。

| `file:line` | 逐語 | 何をしているか |
|---|---|---|
| `:180` | `     * 各行をカラム名に揃えた未加工値リストへ写す。` | メソッドの Javadoc が「カラム名に揃え」ると明言している |
| `:198` | `            List<String> rowValues = new ArrayList<String>(columnNames.size());` | 行の長さをカラム名の数で確保する |
| `:199` | `            for (String col : columnNames) {` | カラム名の数だけループする（行のキー数ではない） |
| `:200` | `                rowValues.add(objectToString(rowMap.get(col)));` | カラム名に対応するキーが行に無ければ `null` が入る |

カラム名は先頭行のキーから決まる（`src/main/java/nablarch/test/core/reader/yaml/YamlSection.java:156`-`:161` の `resolveColumns`。`:160` `return new ArrayList<String>(castMap(rows.get(0)).keySet());`）。したがって、2行目以降がカラム名に無いキーを持っていても `:199`-`:200` のループはそのキーを読まない。これは「カラム名が無い位置のセルは読み込まれない」と同じ振る舞いである。

呼び出し元はテーブル系が `:89`（`List<List<String>> rawRows = extractRows(rows, columnNames);`）、`LIST_MAP` が `:153`（`return buildListMapRows(columnNames, extractRows(rows, columnNames), interps);`）で、どちらも同じ `extractRows` を経由する。

### 経路2: ファイル・メッセージ（`Excel` と YAML が同一メソッドを共有している）

幅を揃えるのは `nablarch-testing@e21bf67` の `src/main/java/nablarch/test/core/file/DataFileFragment.java` で、`Excel` 経路と YAML 経路の両方がこのメソッドを呼ぶ。

| `file:line` | 逐語 |
|---|---|
| `:102` | `    public void addValue(List<String> line) {` |
| `:105` | `        for (int i = 0; i < names.size(); i++) {` |
| `:107` | `            String value = i < line.size() ? line.get(i) : "";` |
| `:169` | `    public void addValueWithId(List<String> line, String no) {` |
| `:173` | `        for (int i = 0; i < names.size(); i++) {` |
| `:175` | `            String value = i < line.size() ? line.get(i) : "";` |

`:105`／`:173` はフィールド名称の数（`names.size()`）でループするため、行が短ければ `:107`／`:175` が `""` を埋め、行が長ければ名称の数を超えたセルは `map` に入らない。

YAML 経路からの呼び出しは `nablarch-testing-yaml@190cc9a` の `src/main/java/nablarch/test/core/reader/yaml/YamlFileBuilder.java:233`（`fragment.addValueWithId(rowValues, String.valueOf(rowNo));`）と `:235`（`fragment.addValue(rowValues);`）。この `buildFragmentsInternal` は、ファイル系（`:120`-`:123` `buildFragmentsForFile`）・受信メッセージ（`:135`-`:138` `buildFragmentsForMessage`）・送信同期メッセージ（`:154`-`:157` `buildFragmentsForSendSync`）の3経路すべてから使われる。`YamlFileBuilder` 自身は `:227`-`:230` で `rowList.size()` ぶんしか値を作らず、幅を揃えていない。揃えているのは `DataFileFragment` 側である。

`Excel` 経路からの呼び出しは `nablarch-testing@e21bf67` の `src/main/java/nablarch/test/core/reader/DataFileParser.java:186`・`MessageParser.java:75`・`SendSyncMessageParser.java:129`・`:134`（`git grep -n 'addValue(\|addValueWithId(' e21bf67 -- src/main/java` の結果から `DataFileFragment.java` 自身を除いた4件がこれで全件）。

この `""` 埋めは、`ja/development_tools/testing_framework/implementation/testdata_notation.rst:883` が可変長ファイルについて既に書いている「YAML 形式でも `rows:` の各要素がフィールド数より短ければ `""` で補完される」と同じ処理である。`:883` の既存記述は正しく、YAML 側に幅を揃える処理があることを既に示していた。

### 「（`Excel` 形式のみ）」のうち、成り立つ部分と成り立たない部分

| 記述 | 成否 | 根拠 |
|---|---|---|
| 名前の行の行末の空セルを取り除く | `Excel` 形式のみで成り立つ | `nablarch-testing@e21bf67` の `HeaderLine.java:33` `List<String> keys = trimTailCopy(headerLine);   // キャッシュを破壊しないようにコピーして編集`。YAML 側にカラム名／フィールド名称の行末の空セルという概念が無い。テーブル・`LIST_MAP` のカラム名はマッピングのキー（`YamlSection.java:160`）、ファイル・メッセージのフィールド名称は `fields:` の `name`（`YamlFileBuilder.java:193` `names.add(toStr(field.get(FIELD_NAME)));`）で、いずれも位置ではなく名前で書くため空セルが生じない |
| 名前が無い位置のセルは読み込まれない／データ行を名前の幅へ揃える | **`Excel` 形式のみではない** | 上記の経路1（`YamlTableDataBuilder.java:198`-`:200`）と経路2（`DataFileFragment.java:105`・`:107`） |

### ラウンド`#35`（`17b0254`）の記録の訂正

本ページ §「出典（すべて `nablarch-testing@e21bf67`。`git show e21bf67:<path>` で読んだ）」は「（\ `Excel`\ 形式のみ）」の根拠を、`nablarch-testing@e21bf67` の `src/main/` に `TestDataReader` の実装が `PoiXlsReader` の1件しか無いことに置いていた。**この確認は `nablarch-testing` 1本の中では正しいが、YAML 経路の判定には足りない。** YAML 経路は `TestDataParsingTemplate`／`TestDataReader` を通らず、`nablarch-testing-yaml` の `YamlTableDataBuilder`／`YamlFileBuilder` が直接器を組み立てるためである（`YamlTestCoreAdapter.java:27`-`:29` の Javadoc が「本体の YAML 読み込みは `reader.yaml` パッケージのビルダ……が `YamlLoader#load` の返す順序保持 Map を走査して器を組み立てる」と述べているとおり）。同節の走査は `nablarch-testing` の `src/main/` に閉じており、`nablarch-testing-yaml` を見ていなかった。

表の「テーブル・\ ``LIST_MAP``」の行の「（\ `Excel`\ 形式のみ）」も、この訂正の対象に含まれる。停止条件により本ラウンドでは `.rst` を変更しなかったため、本ラウンド終了時点ではこの行は `17b0254` のまま残っていた。差し替え後の逐語と、「（\ `Excel`\ 形式のみ）」をどの句に掛けたかは本ページ §「`#35`-是正2（表の4行と `:71` の文面を確定、2026-08-24）」にある。

### 併せて見つかった不整合（`.rst` は変更していない）

本ラウンド時点（`17b0254`）の `ja/development_tools/testing_framework/implementation/testdata_notation.rst:1545` の括弧書きはこうなっていた。

```
    - 行末の空セルを取り除く（\ Excel\ 形式のみ。\ YAML\ 形式では ``rows:``\ の各要素をそのまま読み込む）
```

**「\ YAML\ 形式では ``rows:``\ の各要素をそのまま読み込む」は実装と食い違っていた。** `YamlFileBuilder.java:235` が渡した行は `DataFileFragment.java:105`-`:107` でフィールド名称の数へ揃えられ、短ければ `""` が埋まり、長ければ名称の数を超えたセルは捨てられる。同じページの `:883`（「``rows:`` の各要素の長さ（YAML形式）がフィールド数より少ない場合、不足したフィールドは\ ``""``\ として補完される」）とも食い違っていた。この括弧書きは、是正1 の追補 §2 による差し替えで消えている（本ページ §「追補（`ntf-doc-35-fix1-addendum.md` §2）に従って表の2行を書き換えた記録」）。

### 追補（`ntf-doc-35-fix1-addendum.md` §2）に従って表の2行を書き換えた記録

`:1544`-`:1547` を追補 §2 の4行へ置き換えた。書き換え後の逐語は次のとおり。**この逐語も現在は残っていない。**是正2 §2 が A-3・A-4 への是正として更に差し替え、その後さらに是正3 §1・是正4 §1 が `:1545` を差し替えた。現在の逐語は本ページ §「`#35`-是正4（「ディレクティブ行」を加えて `:1545` を確定、2026-08-24）」にある。

```
  * - ファイル・メッセージ
    - フィールド名称の行の行末の空セルを取り除く（\ Excel\ 形式のみ）。フィールド名称が宣言されていない値は読み込まない
  * - テーブル・\ ``LIST_MAP``
    - カラム名の行の行末の空セルを取り除く（\ Excel\ 形式のみ）。カラム名が宣言されていない値は読み込まない
```

**旧 `:1545` の「\ YAML\ 形式では ``rows:``\ の各要素をそのまま読み込む」は消えている。** `grep -n 'の各要素をそのまま読み込む' ja/development_tools/testing_framework/implementation/testdata_notation.rst` が0件であることを実行して確認した。これで、同ページ `:883` の「データ行のセル数（Excel形式）または ``rows:`` の各要素の長さ（YAML形式）がフィールド数より少ない場合、不足したフィールドは\ ``""``\ として補完される」との矛盾は解消した。旧 `:1545` は YAML 形式が `rows:` を無加工で読むと述べていたが、実際には `nablarch-testing-yaml@190cc9a` の `YamlFileBuilder.java:233`・`:235` が渡した行を `nablarch-testing@e21bf67` の `DataFileFragment.java:105`・`:107`（`String value = i < line.size() ? line.get(i) : "";`）がフィールド名称の数へ揃えており、`:883` の記述のほうが実装と一致していた。

**機構B（データ行を名前の行の幅へ揃え、足りない分を補完する側）を表に書かなかった理由。** この事実は同じページの `:658`（テーブル・``LIST_MAP`` について「``Excel`` 形式では、データ行のセル数がヘッダ行のカラム数より少ない場合、記述しなかったカラムには空文字が設定されたものとして扱われる。``YAML`` 形式では……``null`` を明示的に指定したのと同じ扱いになる」）と `:883`（可変長ファイルについて上記のとおり）に既にあり、3つ目の言い方を足すと、今回直した `:1545` と `:883` の不整合と同じ状態を作ることになるためである。表には、この2箇所が書いていない「名前が宣言されていない値は読み込まない」だけを残した。

**`:883`・`:658`・`:787` の既存記述と表の2行が食い違っていないことの確認。** 対象は是正3 で確定した現行の `:1545`・`:1547` である（逐語は本ページ §「`#35`-是正3（「ファイル・メッセージ」の行をトリムの効く3行に限定、2026-08-24）」。是正1 の追補で完了条件4c として始めた確認を、確定文面に対して取り直したものである）。実物を開き、次の3点で両立することを確かめた。(1) 2行が述べるトリムの対象は、`:1545` が「フィールド名称行・データ型行・フィールド長行」、`:1547` が「カラム名の行」であり、どちらもデータ行を含まない。一方 `:883`（「データ行のセル数（Excel形式）または ``rows:`` の各要素の長さ（YAML形式）がフィールド数より少ない場合、不足したフィールドは\ ``""``\ として補完される」）・`:658`（「\ Excel\ 形式では、データ行のセル数がヘッダ行のカラム数より少ない場合、記述しなかったカラムには空文字が設定されたものとして扱われる」）・`:787`（「同じしくみにより、データ行（3行目以降）のセル数がヘッダ行より少ない場合も、記述しなかった分のカラムには空文字が設定されたものとして扱われる。」）はいずれもデータ行だけを対象としており、対象が重ならない。(2) 2行の後半（「フィールド名称が宣言されていない値は読み込まない」「カラム名が宣言されていない値は読み込まない」）は、行が名前の数より長いときに捨てられる側を述べており、`:883`・`:658`・`:787` が述べる「行が短いとき」の補完とは対象が重ならない。(3) 「（\ Excel\ 形式のみ）」はトリムの句にだけ掛かり、後半の句には掛けていない。ファイル・メッセージの後半は両形式で起きる（`nablarch-testing@e21bf67` `src/main/java/nablarch/test/core/file/DataFileFragment.java:105` `for (int i = 0; i < names.size(); i++) {` と同 `:107` `String value = i < line.size() ? line.get(i) : "";` はフィールド名称の数だけ回るため、名称の数を超えた値は読まれない。YAML 形式もここを通る＝`nablarch-testing-yaml@190cc9a` `src/main/java/nablarch/test/core/reader/yaml/YamlFileBuilder.java:235` `fragment.addValue(rowValues);`）。`:883` は補完が両形式で起きることを述べているので、形式の限定でも食い違わない。なお、機構B（データ行を名前の行の幅へ揃える側）を表に書かなかった理由は本ページ §「データ行の補完（機構B）を表に書かなかった理由」にあり、ここでは繰り返さない。

## `#35`-是正2（表の4行と `:71` の文面を確定、2026-08-24）

作業指示 `ntf-doc-35-fix2.md` §1・§2 の逐語指定に従い、`ja/development_tools/testing_framework/tools/testdata_converter.rst:71` の段落と、本ページ対象ファイルの `list-table`「読み込み時の整形・補完」の4行を差し替えた。4観点レビューが挙げた A-1〜A-5（user が全件成立と確認済み）への是正である。**是正1 とその追補が定めた逐語は、この差し替えで置き換わっている。**

### 差し替え後の逐語（是正2 時点）

`implementation/testdata_notation.rst:1544`-`:1547`。**次の逐語は本ラウンド時点のものであり、現在は残っていない。**是正3 §1 が `:1545` を「フィールド名称行・データ型行・フィールド長行」へ限定し、さらに是正4 §1 が先頭に「ディレクティブ行」を加えて差し替えた。現在の逐語は本ページ §「`#35`-是正4（「ディレクティブ行」を加えて `:1545` を確定、2026-08-24）」にある。`:1544`・`:1546`・`:1547` と `tools/testdata_converter.rst:71` は是正2 の逐語のままである。

```
  * - ファイル・メッセージ
    - 行末の空セルを取り除く（\ Excel\ 形式のみ）。フィールド名称が宣言されていない値は読み込まない
  * - テーブル・\ ``LIST_MAP``
    - カラム名の行の行末の空セルを取り除く（\ Excel\ 形式のみ。前述）。カラム名が宣言されていない値は読み込まない
```

`tools/testdata_converter.rst:71`（1段落1行）。

```
名前の行（テーブルと\ ``LIST_MAP``\ ではカラム名の行、ファイルとメッセージではフィールド名称行）の行末の空セルは、\ Excel\ 形式から読み込む時点で取り除かれるため、往復すると消える。データ行の空セルの扱いは形式によって異なるため、詳細は\ :ref:`テストデータの書き方 <testdata_notation>`\ を参照。
```

### 変更点と、実物で確認した根拠

| 指摘 | 変更 | 根拠（自分で開いて確認した `file:line` と逐語） |
|---|---|---|
| A-1・A-2 | `:71` から旧第1文（「行末の空セルの扱いは、名前の行とデータ行で異なる。」）・第3文・第4文を落とし、データ行の空セルの扱いを本文へ送った | データ行の空セルの扱いは形式で分かれる。`ja/…/implementation/testdata_notation.rst:658`「\ Excel\ 形式では、データ行のセル数がヘッダ行のカラム数より少ない場合、記述しなかったカラムには空文字が設定されたものとして扱われる。\ YAML\ 形式では、……そのカラムは\ ``null``\ を明示的に指定したのと同じ扱いになる。」。加えて同 `:1534`「全要素が\ null\ または空文字のエントリは読み飛ばされる。」があり、1文では正しく書けない（`ntf-doc-35-fix2.md` §1 の user 判断） |
| A-3 | 「ファイル・メッセージ」の行から「フィールド名称の行の」を落として無限定に戻した | `nablarch-testing@e21bf67` `src/main/java/nablarch/test/core/reader/DataFileParser.java:68` `List<String> line = NablarchTestUtils.trimTailCopy(original); // キャッシュを破壊しないようにコピーして編集` が同 `:69` の `switch (status)` より前にあり、`READING_DIRECTIVES_AND_NAMES`（同 `:70`）・`READING_TYPES`（同 `:73`）・`READING_LENGTHS`（同 `:76`）・`READING_VALUES`（同 `:79`）の4分岐すべてに掛かる。フィールド名称行に限られない |
| A-3（テーブル側は限定のまま） | 「テーブル・\ ``LIST_MAP``」の行は「カラム名の行の」を残し、2行を非対称にした | `nablarch-testing@e21bf67` `src/main/java/nablarch/test/core/reader/HeaderLine.java:33` `List<String> keys = trimTailCopy(headerLine);   // キャッシュを破壊しないようにコピーして編集`。同 `:32` `HeaderLine(List<String> headerLine)` のコンストラクタ内であり、ヘッダ行にしか掛からない。非対称なのは実装どおりである |
| A-4 | 「前述」はテーブル側の行にだけ付けた | 機構Aを実例つきで先に説明しているのはテーブル側だけである。`ja/…/implementation/testdata_notation.rst:774`「ヘッダ行（2行目）は、末尾に空セルが続いても、そこで記述を止めたのと同じ結果になる。」と直後の実例表（同 `:776`-`:785`）。同じ表の `:1551`「マーカーカラムを除外する（前述）」・`:1553`「データベース登録時に、値が省略されたカラムへデフォルト値を補完する（前述）」が既に同じ書き方を採っている |
| A-5 | `:71` の「フィールド名称の行」を「フィールド名称行」に改めた | `mapping/glossary.md` §5.10「ファイルデータの行の名称」の正表記。同節の表の行の逐語は「`フィールド名称行`」／「各フィールドの名称を並べた行」／「揺れなし」／「なし」／「input資料15件、5ファイル（`S:input/ntf-doc-terms.md:176`）」 |

`implementation/testdata_notation.rst` 内に「フィールド名称の行」は1件も残っていない（`grep -c 'フィールド名称の行' ja/development_tools/testing_framework/implementation/testdata_notation.rst` が `0`。是正2 完了条件3）。

### データ行の補完（機構B）を表に書かなかった理由

是正1 の追補のときと同じである。本ページ §「追補（`ntf-doc-35-fix1-addendum.md` §2）に従って表の2行を書き換えた記録」に記録したとおり、`ja/…/implementation/testdata_notation.rst:658`・`:787`・`:883` に既出であり、形式差を1セルで正しく書けないため。是正2 §2 も同じ判断を明記している。

## `#35`-是正3（「ファイル・メッセージ」の行をトリムの効く3行に限定、2026-08-24）

作業指示 `ntf-doc-35-fix3.md` §1 の逐語指定に従い、`ja/development_tools/testing_framework/implementation/testdata_notation.rst:1545` の1行だけを差し替えた。差分限定レビューが挙げた `must`（是正2 の A-3 でこの行を無限定にしたことが、同ページ `:883` と食い違う）への是正である。`:1544`・`:1546`・`:1547` は変更していない。

### 差し替え後の逐語（是正3 時点）

`implementation/testdata_notation.rst:1544`-`:1547`。是正2 から変わったのは `:1545` だけである。**次の逐語も現在は残っていない。**是正4 §1 が `:1545` の先頭に「ディレクティブ行・」を加えて差し替えた。現在の逐語は本ページ §「`#35`-是正4（「ディレクティブ行」を加えて `:1545` を確定、2026-08-24）」にある。

```
  * - ファイル・メッセージ
    - フィールド名称行・データ型行・フィールド長行の行末の空セルを取り除く（\ Excel\ 形式のみ）。フィールド名称が宣言されていない値は読み込まない
  * - テーブル・\ ``LIST_MAP``
    - カラム名の行の行末の空セルを取り除く（\ Excel\ 形式のみ。前述）。カラム名が宣言されていない値は読み込まない
```

用語は `mapping/glossary.md` §5.10「ファイルデータの行の名称」の正表記（`フィールド名称行`・`データ型行`・`フィールド長行`）に従った。

### 是正2 の A-3（無限定化）が成り立たない理由

| 主張 | 根拠（`git show <commit>:<path>` で読んだ `file:line` と逐語） |
|---|---|
| `trimTailCopy` が4分岐すべてに掛かること自体は正しい（A-3 の根拠は誤っていない） | `nablarch-testing@e21bf67` `src/main/java/nablarch/test/core/reader/DataFileParser.java:68` `List<String> line = NablarchTestUtils.trimTailCopy(original); // キャッシュを破壊しないようにコピーして編集` が同 `:69` の `switch (status)` より前にある |
| ただしデータ行では、トリムに観測できる効果がない | データ行の分岐は同 `:79` `case READING_VALUES:   //---------------- データ行` → 同 `:182` `protected void onReadingValues(List<String> line) {` → 同 `:186` `currentFragment.addValue(tail(line));`。その `addValue` が `nablarch-testing@e21bf67` `src/main/java/nablarch/test/core/file/DataFileFragment.java:105` `for (int i = 0; i < names.size(); i++) {`・同 `:107` `String value = i < line.size() ? line.get(i) : "";` でフィールド名称の数まで `""` を埋め戻すため、末尾を削っても結果が変わらない |
| データ型行・フィールド長行では、トリムに効果がある | `nablarch-testing@e21bf67` `DataFileFragment.java:203` `assertSameSizeAsNames(types, "types");`（同 `:202` `public void setTypes(List<String> types) {`）・同 `:287` `assertSameSizeAsNames(lengths, "lengths");`（同 `:286` `public void setLengths(List<String> lengths) {`）。トリムされないとフィールド名称と個数が合わずエラーになる。`implementation/testdata_notation.rst:883` も「固定長ファイルでは、フィールド名称・データ型・フィールド長の3リストが同サイズで必須であり」と述べている |
| メッセージも同じ経路を通るため、「ファイル・メッセージ」の行に3行分をまとめて書ける | `nablarch-testing@e21bf67` `src/main/java/nablarch/test/core/reader/MessageParser.java:27` `private final FixedLengthFileParser delegate;`・同 `:115` `delegate.onReadLine(line);`。委譲先は同 `src/main/java/nablarch/test/core/reader/FixedLengthFileParser.java:15` `public class FixedLengthFileParser extends DataFileParser<FixedLengthFile> {` であり、`DataFileParser.onReadLine` を通る |

`:883` との食い違いが解消したことの確認は、本ページ §「追補（`ntf-doc-35-fix1-addendum.md` §2）に従って表の2行を書き換えた記録」の末尾の段落（`:883`・`:658`・`:787` との突合）に、確定文面に対して取り直した形で記録した。

### 却下した指摘

「（\ Excel\ 形式のみ。前述）」（`:1547`）の括弧の使い方についての指摘は、**user が却下した**（`ntf-doc-35-fix3.md` §2 の3点目「『（\ Excel\ 形式のみ。前述）』の括弧についての指摘は、**却下でよい**。判断済みである。」）。`:1547` は変更していない。

### 申し送り（続き2）

番号は39から続く。**38 は欠番である**（`#35`-是正2 のコミット `9aa06d7` が申し送り38 とその `### 申し送り` 見出しを削除した。`git show 9aa06d7 -- .rn/20260724-ntf-yaml-support/reviews/page-testdata_notation.md` の差分に `-38. **追加行の「無い」は、同ページの多数派表記（かな）と逆である。**` がある）。

39. **`tools/testdata_converter.rst:71` がデータ行の空セルの扱いを送っている先に、メッセージのデータ行についての記述がない。** `:71` は「データ行の空セルの扱いは形式によって異なるため、詳細は\ :ref:`テストデータの書き方 <testdata_notation>`\ を参照。」で終わるが、`implementation/testdata_notation.rst` の「メッセージングのデータを記述する」節（見出しは `:1152`、節の本文は `:1306`「実際の記述例は :ref:`メッセージングのデータを記述する <testdata_examples-messaging_data>` を参照。」まで。次のラベル `.. _testdata_notation-special_notation:` は `:1308`）には、空セル・補完に触れる記述が1件もない。`sed -n '1152,1309p' ja/development_tools/testing_framework/implementation/testdata_notation.rst | grep -c <語>` を4語で実行し、`空セル` 0・`補完` 0・`空文字` 0・`取り除` 0 を確認した。テーブル側は同ページ `:658`・`:787`、可変長ファイル側は `:883` が受け皿になっているが、メッセージだけが受け皿を持たない。**`#35` は作業指示 §1〜§3 の範囲に限られるため、ここでは直していない。**

40. **`tools/testdata_converter.rst:71` が名前の行として「フィールド名称行」だけを挙げており、`implementation/testdata_notation.rst:1545` の4行と食い違う。** `:71` の逐語は「名前の行（テーブルと\ ``LIST_MAP``\ ではカラム名の行、ファイルとメッセージではフィールド名称行）の行末の空セルは、\ Excel\ 形式から読み込む時点で取り除かれるため、往復すると消える。」（現物を開いて確認）。一方、是正4 §1 で確定した `:1545` は「ディレクティブ行・フィールド名称行・データ型行・フィールド長行」の4行を挙げている。トリムは `nablarch-testing@e21bf67` `src/main/java/nablarch/test/core/reader/DataFileParser.java:68` `List<String> line = NablarchTestUtils.trimTailCopy(original); // キャッシュを破壊しないようにコピーして編集` が同 `:69` `switch (status) {` より前で掛けるため、ディレクティブ行・データ型行・フィールド長行の行末の空セルも往復で消える。`:71` はその3行を落としている。**`#35` は作業指示の §1〜§3 の範囲に限られるため、ここでは直していない**（`tools/testdata_converter.rst` は本ラウンドでも未変更）。**`#35` 着地後に、申し送り39 および `:883` の Excel 側の括弧書き（「全フィールドを省略した行（Excel形式では先頭セルが空の行）」が `PoiXlsReader.java:140`-`:147` の `isBlankLine` で読み飛ばされるため成り立たない疑い）と合わせて、1タスクとして扱う。**

41. **ページ内の行の呼称と、`mapping/glossary.md` §5.10 が正式名称として定義する『行』付きの4語がずれている。** `mapping/glossary.md` §5.10「ファイルデータの行の名称」は `レコード種別行`・`フィールド名称行`・`データ型行`・`フィールド長行` の4語を正表記として定義している（現物を開いて確認。`:268`-`:271`）。一方 `implementation/testdata_notation.rst` のファイルデータ節は、構成図 `:854`-`:858`（「ディレクティブ（0件以上）／レコード種別 + フィールド名称／データ型（各フィールドの型）／フィールド長（固定長のみ）／データ（1件以上）」）も、`:860`「各要素の名称と役割は、以下のとおりである。」が導く用語表 `:866`-`:875` も、Excel 書式表 `:1057`-`:1063` も、いずれも『行』を付けずに呼んでいる。『行』付きが現れるのは同ページでは `:1010`・`:1055`（`ディレクティブ行`）、`:1076`（`フィールド長行`）、`:1267`・`:1269`・`:1271`（`フィールド名称行`）、`:1545`（4語すべて）である（`grep -n` を5語それぞれで実行して確認）。`implementation/testdata_examples.rst` も `:1082`・`:1092`・`:1267`（`レコード種別行`）、`:1094`（`データ型行`・`フィールド長行`）、`:1205`（`フィールド長行`）、`:1341`・`:1619`（`ディレクティブ行`）、`:1646`（`フィールド名称行`）で『行』付きを使う。**どちらへ寄せるかはページ全体の語法の決めごとであり、`:1545` の1行では決められない。** **`#35` 着地後に、申し送り39・40 および `:883` の Excel 側の括弧書きと合わせて1タスクとして扱う。**

42. **`mapping/glossary.md` §5.10 の `フィールド長行` の「意味」欄が誤っている。** 現物の逐語は「各フィールドのバイト長を示す行。**固定長ファイルのみで使うもの**」（`mapping/glossary.md` §5.10「ファイルデータの行の名称」の表。`:271`）。しかしフィールド長はメッセージにも存在する——`implementation/testdata_notation.rst:1158`「フレームワーク制御ヘッダ以降のメッセージボディは、フィールド名称・データ型・フィールド長・データという、前述のファイルデータと同じ構成を持つ。」／同 `:1196`「ディレクティブには、\ ``file-type``\ （テスティングフレームワークが固定長のみに対応するため）と ``record-length``\ （フィールド長から自動計算されるため）を記載する必要はない。」（いずれも現物を開いて確認）。無いのは可変長ファイルのみである（同 `:1076`）。本ページ §「是正2・是正3 の指摘件数と観点」に転記した是正3 の `nice` 3件目「『フィールド長行』は固定長ファイルにのみ存在する（`:1076`）」も、同じ誤りを含んでいた。**`mapping/glossary.md` は `#35` の作業指示が変更禁止としているため、ここでは直していない。** なお、同欄が採った input 資料側の出典（`S:input/ntf-doc-terms.md:178`）には**当たっていない（未確認）**。

43. **`mapping/glossary.md` §5.10 の `レコード種別行` は、実装上そういう行が存在しない。** レコード種別はフィールド名称行の先頭セルである（`nablarch-testing@e21bf67` `src/main/java/nablarch/test/core/reader/DataFileParser.java:248`-`:252`。`:250` `currentFragment.setRecordType(fieldNamesLine.get(0));`／`:251` `currentFragment.setNames(tail(fieldNamesLine));`。本ラウンドの実測でも、シート行 `[REC1][A][B][空][空]` から `recordType=REC1`・`names=[A, B]` が1行分として取り出された）。`implementation/testdata_notation.rst:855` の構成図も「    → レコード種別 + フィールド名称」と1行に2要素を書いており、`implementation/testdata_examples.rst:1082` は「Excel\ 形式では、レコード種別行の先頭要素にレコード種別を、以降の要素にフィールド名称を記述する。」と、レコード種別行＝フィールド名称行として使っている。§5.10 自身も採用根拠欄で「**根拠が弱い。** `:175` は図中のラベルで定義箇所ではない」と注記している。**`mapping/glossary.md` は変更禁止のため、ここでは直していない。**

## `#35`-是正4（「ディレクティブ行」を加えて `:1545` を確定、2026-08-24）

作業指示 `#35-是正4`（最終）§1 の逐語指定に従い、`ja/development_tools/testing_framework/implementation/testdata_notation.rst:1545` の1行だけを差し替えた。是正3 の文面がディレクティブ行を落としていた点（差分限定レビューの `must-1`）への是正である。**`:1544`・`:1546`・`:1547` と `tools/testdata_converter.rst:71` は変更していない。** `mapping/glossary.md` も変更していない。

### 確定した逐語（現在の HEAD）

`implementation/testdata_notation.rst:1544`-`:1547`。是正3 から変わったのは `:1545` だけで、変更は先頭への「ディレクティブ行・」の挿入のみである。次のブロックの `:1545` は `sed -n '1545p'` の出力をそのまま貼ったものである。

```
  * - ファイル・メッセージ
    - ディレクティブ行・フィールド名称行・データ型行・フィールド長行の行末の空セルを取り除く（\ Excel\ 形式のみ）。フィールド名称が宣言されていない値は読み込まない
  * - テーブル・\ ``LIST_MAP``
    - カラム名の行の行末の空セルを取り除く（\ Excel\ 形式のみ。前述）。カラム名が宣言されていない値は読み込まない
```

### 用語「ディレクティブ行」を採った根拠

`mapping/glossary.md` は変更していない。同 §5.10「ファイルデータの行の名称」は、その導入文「ファイルデータのレイアウトを表す行の名称である。」のとおりレイアウトを表す行に限った節であり、ディレクティブはそこではなく §5.8「テストデータ」に `ディレクティブ`（「ファイル・電文のフォーマットに関する属性を、キー名と値の2要素で指定するもの」）として載っている（いずれも現物を開いて確認）。「ディレクティブ行」は同ページが既に使う語である——`ja/…/implementation/testdata_notation.rst:1010`「識別子行の後にディレクティブ行を置き、続けて以下のようにレコード種別以降を記載する。」、同 `:1055`「  * - ディレクティブ行」（`grep -n 'ディレクティブ行'` の結果は本ラウンド差し替え前でこの2件のみ）。

メッセージ側のフレームワーク制御ヘッダを括弧書きで足すことはしなかった（作業指示 §1、根拠は下記 §5 の5点目）。

### 逐語の根拠（作業指示 §5 の転記。参照コミット `nablarch-testing@e21bf67`）

作業指示 §5 は5つの箇条書きと参照コミットの指定（`nablarch-testing@e21bf67`）から成る（完了条件6 の「6点」はこの5点＋参照コミットの指定を指すものとして扱った）。**逐語の反例検証はディレクター側が実測で行っており、本ラウンドではレビューを回していない**（下記「本ラウンドのレビュー」）。ただし転記にあたり、5点すべてを自分で `git show e21bf67:<path>` を実行して現物で裏を取った。結果は5点とも一致した。

| # | 作業指示 §5 の主張 | 自分で当たった出典と逐語（`git show e21bf67:<path>` を実行） | 判定 |
|---|---|---|---|
| 1 | `DataFileParser.java:68` の `NablarchTestUtils.trimTailCopy` が `:69` の `switch` の前に掛かる。`trimTail` の実体は `NablarchTestUtils.java:251`-`:263`（末尾から空要素を削除） | `src/main/java/nablarch/test/core/reader/DataFileParser.java:66` `final void onReadLine(List<String> original) {` / `:68` `List<String> line = NablarchTestUtils.trimTailCopy(original); // キャッシュを破壊しないようにコピーして編集` / `:69` `switch (status) {`。`src/main/java/nablarch/test/NablarchTestUtils.java:246`（Javadoc）「リスト末尾の空要素（nullまたは空文字）を取り除く。（破壊的メソッド）」 / `:251` `public static List<String> trimTail(List<String> list) {` / `:256` `for (int i = list.size() - 1; i >= 0; i--) {` / `:257` `if (StringUtil.hasValue(list.get(i))) {` / `:258` `break;  // 空でない要素があれば終了` / `:260` `list.remove(i); // 空要素を削除` / `:263` `}` | 一致 |
| 2 | `switch` の4状態のうち `READING_DIRECTIVES_AND_NAMES` は `:140`-`:145` の `onReadingDirectives` が `:141` `processDirectives` を先に呼び、偽のときだけ `:143` `onReadingNames` へ落とす。よって同状態はディレクティブ行とフィールド名称行の2種を捌く | 同 `DataFileParser.java:70` `case READING_DIRECTIVES_AND_NAMES:  //------------- ディレクティブ、フィールド名称` / `:71` `onReadingDirectives(line);` / `:73` `case READING_TYPES:` / `:76` `case READING_LENGTHS:` / `:79` `case READING_VALUES:   //---------------- データ行`。同 `:140` `protected void onReadingDirectives(List<String> line) {` / `:141` `boolean isDirective = processDirectives(line);` / `:142` `if (!isDirective) {` / `:143` `onReadingNames(line);` / `:145` `}` | 一致 |
| 3 | ディレクティブ行でトリムに観測できる効果がある。値セルが空の行は `PoiXlsReader.java:119`-`:130` で `["キー", ""]` になり（`:140`-`:147` の `isBlankLine` は先頭が非空のため読み飛ばさない）、`:68` のトリムで `["キー"]` に縮み、`:220`-`:224` の `if (line.size() < 2) throw new IllegalStateException` に落ちる。トリムが無ければ `:228` `setDirective("キー", "")` であった。同ページ `:892` がこのエラーを既に挙げている | `src/main/java/nablarch/test/core/reader/PoiXlsReader.java:119` `List<String> line = new ArrayList<String>(64);` / `:121` `for (int i = 0; i < lastCellNum; i++) {` / `:123` `String cellValue = cell == null ? "" : cell.toString();` / `:124` `line.add(cellValue);` / `:130` `return line;`。同 `:140` `private boolean isBlankLine(List<String> line) {` / `:142` `if (!e.isEmpty()) {` / `:143` `return false;`。`DataFileParser.java:219` `protected boolean processDirectives(List<String> line) {` / `:220` `if (line.size() < 2) {` / `:222` `throw new IllegalStateException(` ＋ `:223` `"directive or data names row must have two columns at least. " + line);` / `:228` `currentFile.setDirective(directive, value);` | 一致（`throw` 文の実体は `:222`-`:223`、ブロックの閉じが `:224`）。`ja/…/testdata_notation.rst:892`「- ディレクティブまたはレコード種別・フィールド名称定義の要素数が2未満である」も現物で確認した |
| 4 | 4行で閉じている。トリムが掛かるのは `TestDataParsingTemplate.java:210` の `onReadLine` だけで、識別子行を捌く `:206` `onTargetTypeFound` には生の `line` が渡る | `src/main/java/nablarch/test/core/reader/TestDataParsingTemplate.java:201` `if (isTargetType(line, id)) {` / `:206` `onTargetTypeFound(line);` / `:209` `if (nowReading) {` / `:210` `onReadLine(line);` | 一致 |
| 5 | メッセージにもディレクティブ行は実在する。`MessageParser.java:79`-`:88` が `super.processDirectives` を通し、`FixedLengthFileParser.java:36`-`:37` の `isDirective` が固定長ディレクティブを判定する | `src/main/java/nablarch/test/core/reader/MessageParser.java:79` `protected boolean processDirectives(List<String> line) {` / `:80` `if (super.processDirectives(line)) {` / `:81` `return true;` / `:86` `if (isFrameworkHeader(fieldName)) {` / `:87` `fwHeader.put(fieldName, value);` / `:88` `return true;`。`src/main/java/nablarch/test/core/reader/FixedLengthFileParser.java:36` `protected boolean isDirective(String key) {` / `:37` `return FixedLengthDirective.VALUES.containsKey(key);` | 一致 |

**フレームワーク制御ヘッダを括弧書きで足さなかった理由**（作業指示 §1 の指示と、上表5行目の実物による）。`MessageParser` の `processDirectives` は `:80` でまず `super.processDirectives`（＝`DataFileParser` のディレクティブ判定）を通しており、メッセージでもディレクティブ行はディレクティブ行として実在する。フレームワーク制御ヘッダはそれが偽だったときの `:86` 以降の別分岐であり、「ディレクティブ行」の語の中身を括弧で言い換える対象ではない。

**`PoiXlsReader.java` のパス。** 作業指示はファイル名のみを挙げているが、`e21bf67` での実体は `src/main/java/nablarch/test/core/reader/PoiXlsReader.java` である（`git ls-tree -r --name-only e21bf67 | grep 'PoiXlsReader.java$'` の結果は1件）。

### 本ラウンドのレビュー

**回していない。** `ntf-doc-13-standing-rules.md:20` の常設ルールにより是正ラウンドの上限は3回であり、本ラウンドはその上限に到達した4回目に当たる。作業指示の冒頭が「上限3ラウンドに到達したため、本ラウンドではレビューを回さない。§1 の逐語はディレクター側が実測で反例検証済み」と定めている。したがって本ラウンドは、レビューの代わりに上表のとおり §5 の5点を自分で現物に当たって検算した。

### 是正2・是正3 の指摘件数と観点（Steps 11・16 の積み残しをここへ移す）

是正2 と是正3 は、いずれも4観点（QA / 設計 / クラフト / 検証）を回さず、差分限定の2観点だけを別々のサブエージェントで回した（`ntf-doc-13-standing-rules.md:20` の常設ルール）。

| ラウンド | 観点 | 判定 | 指摘 | 内訳 |
|---|---|---|---|---|
| 是正2 | 是正が §1〜§3 の範囲に収まっているか（範囲統制） | pass | 1件 | `nice` 1（`mapping/glossary.md:269` の行番号参照。採用） |
| 是正2 | 是正が新しい欠陥を生んでいないか（事実検証） | fail | 4件 | `must` 2（§2 の無限定化が `:883` と食い違う／`:667` の検証結果が旧文面前提。いずれも採用）、`nice` 2（`:71` の飛び先にメッセージのデータ行の記述が無い＝採用。申し送り39 になった／「（\ `Excel`\ 形式のみ。前述）」の括弧の使い方＝**却下**） |
| 是正3 | 範囲統制／事実検証の2観点 | fail | 10件 | `must` 2（`:1545` にディレクティブ行が欠けている＝是正4 §1 で処置／`tools/testdata_converter.rst:71` が「フィールド名称行」だけを挙げ `:1545` と食い違う＝申し送り40 として起票）、`nice` 3（採用。是正4 §3 の3件）、`nice` 3（**未処置**。下記）、却下 2（`:667` 段落が `:658`・`:787` にも触れた点＝指示の趣旨の範囲内／「（\ `Excel`\ 形式のみ。前述）」の括弧＝user 判断済み） |

是正2 の内訳の出典は `checks/task-35.md` §「差分限定レビュー（是正2。4観点は回さない）」の表（同ファイルを開いて確認）。**是正3 の内訳の出典は、コミット `1d88729` の `steering.md` の `State`（`git show 1d88729 -- .rn/20260724-ntf-yaml-support/steering.md` で確認）である。同ラウンドの観点別の生出力は保全されておらず、`must` 2件・`nice` 3件（採用）・`nice` 3件（未処置）・却下2件がそれぞれどちらの観点から出たかは、`nice` 3件（未処置）が「事実検証」と明記されている以外は記録に残っていない。**

**是正3 の `nice` 3件（未処置）。** いずれも本ラウンドの作業指示の範囲外のため処置していない。出典は上記 `1d88729` の `State` である。

1. 同期応答メッセージ送信のラベル列（連番）が `SendSyncMessageParser.java:134` ＋ `DataFileFragment.java:172` で読まれ、`:1545` 後半の句「フィールド名称が宣言されていない値は読み込まない」と字面で衝突する（同ページ `:1269` が別途説明している）
2. ページ内の行の呼称が不統一である（`:1055` のみ「行」付き、`:1057`-`:1063`・`:866`-`:875` は無し。「データ型行」はページ内に定義が無い）
3. 「フィールド長行」は固定長ファイルにのみ存在する（`:1076`）

## `#35`-是正5 §1 の逐語検証（着手前。`.rst` は未変更、2026-08-24）

作業指示 `#35-是正5` §1 に従い、次の逐語案に反例が無いかを4点確かめた。**(1) で反例が出たため、§2 以降には進んでいない**（作業指示 §1 の停止条件「§1 で反例が出たら、そこで止めて報告すること」）。`ja/` 配下は1文字も変更していない（`git diff --stat -- ja/` が空）。

検証対象の逐語案（作業指示 §1 からの転記）:

```
    - ディレクティブ・フィールド名称・データ型・フィールド長の各行の行末の空セルを取り除く（\ Excel\ 形式のみ）。フィールド名称の数を超える位置の値は読み込まない
```

### (1) 『行』を落としてページの語法に揃うか → **反例あり。逐語案は採れない**

作業指示 §1 (1) は「`:1055` の「ディレクティブ行」だけが『行』付きである点も確認すること」と述べるが、**これは成り立たない**。`grep -n` を5語それぞれで実行した結果は次のとおりで、`:1545`（差し替え対象そのもの）を除いても `implementation/testdata_notation.rst` に『行』付きが6件ある。

| 語 | `implementation/testdata_notation.rst` | `implementation/testdata_examples.rst` | `tools/testdata_converter.rst` |
|---|---|---|---|
| `レコード種別行` | 0件 | 3件（`:1082`・`:1092`・`:1267`） | 0件 |
| `フィールド名称行` | 4件（`:1267`・`:1269`・`:1271`・**`:1545`**） | 1件（`:1646`） | 1件（`:71`） |
| `データ型行` | 1件（**`:1545`** のみ） | 1件（`:1094`） | 0件 |
| `フィールド長行` | 2件（`:1076`・**`:1545`**） | 2件（`:1094`・`:1205`） | 0件 |
| `ディレクティブ行` | 3件（`:1010`・`:1055`・**`:1545`**） | 2件（`:1341`・`:1619`） | 0件 |

決定的なのは `:1267`-`:1271` である。**同じページのメッセージングの節が「フィールド名称行」を3回使っている**（いずれも現物を開いて確認した逐語）。

- `:1267`「フレームワーク制御ヘッダは、フィールド名称行（先頭セルに ``no``\ と記載する行）より前に …」
- `:1269`「メッセージボディの各行の先頭要素は、フィールドとしては読み込まれないラベル列である。フィールド名称行の先頭要素には慣行として ``no``\ と記載し、データ行の先頭要素には電文の連番を記載する。…」
- `:1271`「フィールド名称行の先頭要素がレコード種別として扱われるかどうかは、データタイプによって異なる。…」

`:1545` は「ファイル・メッセージ」の行であり、`:1267`-`:1271` はまさにそのメッセージ側の説明である。ここから『行』を落とすと、**同じ対象を同じページの中で二通りに呼ぶ**ことになる。`:1076`「可変長ファイルの準備も、固定長とほぼ同様である。固定長との違いは、可変長ファイルの場合はフィールド長行を記載しない点のみである。」と `:1010`「識別子行の後にディレクティブ行を置き、続けて以下のようにレコード種別以降を記載する。」も同様に『行』付きである。

作業指示 §1 (1) が根拠に挙げた構成図と用語表は、**『行』なしであること自体は正しい**。ただし行番号がずれている（実測値を併記する）。

- 構成図は `:854`-`:858`（作業指示は `:851`-`:856`）。`:852` が `.. code-block:: text`、`:853` が空行。逐語は `:854`「  ディレクティブ（0件以上）」／`:855`「    → レコード種別 + フィールド名称」／`:856`「      → データ型（各フィールドの型）」／`:857`「        → フィールド長（固定長のみ）」／`:858`「          → データ（1件以上）」
- 「各要素の名称と役割は、以下のとおりである。」は `:860`（作業指示は `:864`）。`grep -n` で1件のみ
- 用語表は `:866`-`:879`。作業指示が指す `:866`-`:875` は「用語／定義」の見出し行＋4語（レコード種別 `:868`-`:869`／フィールド名称 `:870`-`:871`／データ型 `:872`-`:873`／フィールド長 `:874`-`:875`）にちょうど対応しており、**この範囲指定は正しい**。以降 `:876`-`:877` がデータ、`:878`-`:879` がパディング

**したがってページの語法は一つに揃っていない。** 「構成図・用語表・Excel 書式表は『行』なし、メッセージの節と `:1010`・`:1055`・`:1076` は『行』付き」という二層構造になっている。`:1545` から『行』を落とす変更は、片方（構成図・用語表）に寄せる代わりに、もう片方（メッセージの節）から離れる。**どちらへ寄せるかはページ全体の語法の決めごとであり、`:1545` の1行だけでは決められない。** この不統一は申し送り41 として起票した。

### (2) 「各行の」で4語すべてに「行末の空セル」が掛かると読めるか → 反例なし（ただし (1) が先に効く）

逐語案は「ディレクティブ・フィールド名称・データ型・フィールド長の**各行の**行末の空セルを取り除く」であり、「各行の」が4語の並び全体を受けるため、4語すべてが『行』として扱われ「行末の空セル」が4行すべてに掛かると読める。「各行の」を落とした「…データ型・フィールド長の行末の空セル」では、「の行末」が直前の「フィールド長」だけに掛かる読みが残る。作業指示 (2) の主張は日本語の係り受けとして成り立っている。**ただしこれは (1) が成り立つ場合にのみ意味を持つ改良であり、(1) の反例を打ち消すものではない。**

なお現行の `:1545`（是正4 で確定）は4語すべてに『行』を付けており、係り受けの曖昧さは元から生じない。

### (3) フィールド長の限定が `:874` で担われ、`:1545` に「固定長のみ」を足すべきでないか → 反例なし。**作業指示の判断は正しい**

- `:874`-`:875` の逐語（現物）: 「  * - フィールド長」／「    - 各フィールドのバイト長（固定長ファイルのみ存在）」。作業指示の引用と一致する
- `:1076` の逐語（現物）: 「可変長ファイルの準備も、固定長とほぼ同様である。固定長との違いは、可変長ファイルの場合はフィールド長行を記載しない点のみである。」——**言っているのは「可変長ファイルに無い」ことだけ**である
- メッセージにはフィールド長が有る。`:1158`「フレームワーク制御ヘッダ以降のメッセージボディは、フィールド名称・データ型・フィールド長・データという、前述のファイルデータと同じ構成を持つ。…」／`:1196`「ディレクティブには、\ ``file-type``\ （テスティングフレームワークが固定長のみに対応するため）と ``record-length``\ （フィールド長から自動計算されるため）を記載する必要はない。」（いずれも現物）

`:1545` は「ファイル・メッセージ」の行であり、メッセージにフィールド長が有る以上、ここに「固定長のみ」等の括弧書きを足すのは誤りになる。**作業指示 §1 (3) の指示（括弧書きを足さない）は正しい。**

**あわせて、既存の記録の誤りを2件見つけた**（作業指示 §4 が求める訂正と同じもの、および `mapping/glossary.md` 側の同型の誤り）。

1. 本ファイル §「是正2・是正3 の指摘件数と観点（Steps 11・16 の積み残しをここへ移す）」の「是正3 の `nice` 3件（未処置）」の3件目「**「フィールド長行」は固定長ファイルにのみ存在する（`:1076`）**」は**不正確**である。`:1076` が言うのは可変長ファイルに無いことだけで、メッセージには存在する（`:1158`・`:1196`）。無いのは可変長ファイルのみである
2. `mapping/glossary.md` §5.10 の `フィールド長行` の「意味」欄「各フィールドのバイト長を示す行。**固定長ファイルのみで使うもの**」（現物 `:271`）も同じ誤りを含む。**`mapping/glossary.md` は作業指示 §2 が変更禁止としているため、本ラウンドでは直していない**

### (4) 「フィールド名称の数を超える位置の値は読み込まない」が実装と一致するか → 反例なし。**5箇所すべて一致**

参照コミット `nablarch-testing@e21bf67`（`git log -1` で `e21bf67e26bca1cb3bddcd00cdecfd10943f9333 2024-09-27 Merge remote-tracking branch 'origin/release-6u2'` を確認）。すべて `git show e21bf67:<path>` を実行して読んだ。

| 作業指示 §1 (4) の主張 | 自分で当たった逐語 | 判定 |
|---|---|---|
| `DataFileFragment.java:105`-`:107` | `src/main/java/nablarch/test/core/file/DataFileFragment.java:102` `public void addValue(List<String> line) {` / `:105` `for (int i = 0; i < names.size(); i++) {` / `:106` `String name = names.get(i);` / `:107` `String value = i < line.size() ? line.get(i) : "";` / `:112` `map.put(name, value);` | 一致 |
| `DataFileParser.java:250`-`:251` | `src/main/java/nablarch/test/core/reader/DataFileParser.java:248` `private void createNewFragment(List<String> fieldNamesLine) {` / `:250` `currentFragment.setRecordType(fieldNamesLine.get(0));` / `:251` `currentFragment.setNames(tail(fieldNamesLine));` | 一致 |
| `DataFileParser.java:186` | 同 `:182` `protected void onReadingValues(List<String> line) {` / `:184` `if (isDataRow(line)) {` / `:186` `currentFragment.addValue(tail(line));` | 一致 |
| `SendSyncMessageParser.java:134` → `DataFileFragment.java:169`-`:172` | `src/main/java/nablarch/test/core/reader/SendSyncMessageParser.java:134` `currentFragment.addValueWithId(temp, temp.remove(NO_COLUMN_NUMBER));`（`:99` `private static final int NO_COLUMN_NUMBER = 0;`）→ `DataFileFragment.java:169` `public void addValueWithId(List<String> line, String no) {` / `:171` `Map<String, String> map = new LinkedHashMap<String, String>();` / `:172` `map.put(FIRST_FIELD_NO, no);` / `:173` `for (int i = 0; i < names.size(); i++) {` / `:175` `String value = i < line.size() ? line.get(i) : "";` | 一致 |
| `FIRST_FIELD_NO` の実体 `DataFileFragment.java:67`、`:471` のコメント | 同 `:66` `/** 最初のフィールドの値（連番）が格納されるキー */` / `:67` `public static final String FIRST_FIELD_NO = "DataFileFragment:firstFieldKey";`。同 `:470` `protected Object removePadding(String fieldName, Object value, FixedLengthDataRecordFormatter formatter) {` / `:471` `if (FIRST_FIELD_NO.equals(fieldName)) {  // No列は実際のデータではないのでパディング除去対象外` | 一致 |

**言い直しが過不足ないか。** 両経路とも、先頭要素を外したうえで `names.size()` 回だけループし、`i < line.size()` が偽なら `""` を入れる。**`names.size()` を超える位置の値はどこにも読まれない。** これは新文面「フィールド名称の数を超える位置の値は読み込まない」と一致する。

旧文面「フィールド名称が宣言されていない値は読み込まない」との違いも確かめた。同期応答メッセージ送信では `temp.remove(0)` で外した先頭要素（ラベル列＝連番）が `FIRST_FIELD_NO` キーで `map` に保持される。この値は「フィールド名称が宣言されていない値」でありながら読み込まれているため、旧文面と字面で衝突する。新文面ではラベル列は「`names.size()` を超える位置」ではなく先頭（0番目を除いた後の相対位置の外側）にあたるため、衝突しない。**過不足なし。**

不足も確かめた。`SendSyncMessageParser.java:123`-`:131` の `ErrorMode` 分岐は `currentFragment.addValue(list)` に要素1個のリストを渡すが、これは `names.size()` 回のループで残りが `""` に**補完される**側の話であり、切り捨て（読み込まない）の記述とは別事象である。新文面がこれを取りこぼす記述にはなっていない。

### 結論

**(1) が反例に当たるため、§2 の差し替えは行っていない。** (2)(3)(4) には反例が無く、`:1545` の後半の句を「フィールド名称の数を超える位置の値は読み込まない」へ言い直すこと自体は実装と一致する。前半の『行』の付け外しだけが、ページ全体の語法の決めごと（申し送り41）に依存して決められない状態にある。

## `#35`-是正5 フェーズA（実測と逐語案。`.rst` は1文字も変更していない、2026-08-24）

user の判断により、`:1545` は**前半の『行』付きを据え置き、後半の句だけを差し替える**ことが決まった（是正5 §1 の3択のうち (2)）。これを A-1 とし、A-2〜A-5 の4件について実測と逐語案を作った。**本節の逐語案はいずれも未承認であり、`ja/` 配下は1文字も変更していない**（`git diff --stat -- ja/` が空であることを実行して確認した）。

### 実測の方法（A-2・A-3・A-4 共通）

過去のラウンドは実装の**読解**だけで判定していた。本ラウンドは、参照コミットのソースを実際にビルドして**動かした**結果を根拠にする。手順は次のとおりで、成果物に付属する検証スクリプトは使っていない（自分で組んだ）。

| 項目 | 内容 |
|---|---|
| 実装 | `nablarch-testing@e21bf67` を `git worktree add <tmp> e21bf67` で取り出し、`mvn -o -DskipTests compile` でビルドした（`target/classes`） |
| YAML 実装 | `nablarch-testing-yaml@190cc9a` を同様に `git worktree add` ＋ `mvn -o -DskipTests compile` でビルドした。クラスパスは `nablarch-testing@e21bf67` の `target/classes` を先頭に置き、YAML 側の推移的依存に含まれる `nablarch-testing` の jar より優先させた |
| 入力 | Apache POI で `.xls` をその場で生成した（セルは「作らない」「空文字のセルを作る」「値を入れる」を作り分けた）。YAML は手書きの `.yaml` |
| 呼び出し | `PoiXlsReader`・`FixedLengthFileParser`・`VariableLengthFileParser`・`MessageParser`・`SendSyncMessageParser`（Excel）、`YamlTestDataParser`（YAML）を直接呼んだ |
| 観測 | `DataFile#all`／`DataFileFragment#names`・`#values`・`#recordType` をリフレクションで読み出して出力した。`PoiXlsReader#readLine()` が返す行そのものも別途出力した |

**参照コミット。** `nablarch-testing@e21bf67`（`e21bf67e26bca1cb3bddcd00cdecfd10943f9333` `2024-09-27` `Merge remote-tracking branch 'origin/release-6u2'`）／`nablarch-testing-yaml@190cc9a`（`revert: rows: [] の列名 DbInfo フォールバックを差し戻す`）／`nablarch-testing-converter@e977824`（ソース読解のみ）。

### A-1 `:1545` の後半の句（user 判断。案の作成は不要）

反映する逐語は次の1行である。前半の『行』付き4語は据え置く。

```
    - ディレクティブ行・フィールド名称行・データ型行・フィールド長行の行末の空セルを取り除く（\ Excel\ 形式のみ）。フィールド名称の数を超える位置の値は読み込まない
```

現行 `:1545` との差は末尾の句のみ（「フィールド名称が宣言されていない値は読み込まない」→「フィールド名称の数を超える位置の値は読み込まない」）。実装一致は本ページ §「`#35`-是正5 §1 の逐語検証」の (4) で確認済みであり、本ラウンドで追加の実測はしていない。

**本ラウンドの実測でも裏が取れた。** 固定長ファイルのデータ行 `[空, a2, b2, c2, 空]`（フィールド名称は `A`・`B` の2件）を読ませた結果は `{A=a2, B=b2}` であり、フィールド名称の数を超える位置の `c2` は読み込まれていない（下記 A-2 の実測結果を参照）。

### A-2 申し送り40 — `tools/testdata_converter.rst:71`

**逐語案（1段落＝1行）。**

```
テーブルと\ ``LIST_MAP``\ ではカラム名の行、ファイルとメッセージではディレクティブ行・フィールド名称行・データ型行・フィールド長行の行末の空セルは、\ Excel\ 形式から読み込む時点で取り除かれるため、往復すると消える。データ行の空セルの扱いは形式によって異なるため、詳細は\ :ref:`テストデータの書き方 <testdata_notation>`\ を参照。
```

現行 `:71` からの変更は、先頭の「名前の行（…ファイルとメッセージではフィールド名称行）」を「テーブルと ``LIST_MAP`` ではカラム名の行、ファイルとメッセージでは（4行の列挙）」へ改めた1点だけである。テーブル・`LIST_MAP` 側の「カラム名の行」は変えていない。後段の1文（データ行を本文へ送る文）はそのまま残した。列挙の4語と順序は `:1545`（A-1）と同一であり、食い違わない。

**列挙した各行がトリム対象である根拠。** `nablarch-testing@e21bf67` の `src/main/java/nablarch/test/core/reader/DataFileParser.java:68` `List<String> line = NablarchTestUtils.trimTailCopy(original); // キャッシュを破壊しないようにコピーして編集` が、同 `:69` `switch (status) {` より**前**にあるため、4分岐すべてに掛かる。

| `:71` に列挙した行 | 分岐（`DataFileParser.java`） | その分岐が呼ぶ処理 |
|---|---|---|
| ディレクティブ行 | `:70` `case READING_DIRECTIVES_AND_NAMES:` → `:71` `onReadingDirectives(line);` | `:140`-`:145` `onReadingDirectives` → `:219` `processDirectives` |
| フィールド名称行 | 同上（ディレクティブでなければ名称行として扱う。`:142`-`:144`） | `:152`-`:155` `onReadingNames` → `:248`-`:252` `createNewFragment`（`:250` `currentFragment.setRecordType(fieldNamesLine.get(0));`／`:251` `currentFragment.setNames(tail(fieldNamesLine));`） |
| データ型行 | `:73` `case READING_TYPES:` → `:74` `onReadingTypes(line);` | `:162`-`:165` `onReadingTypes` → `:163` `currentFragment.setTypes(tail(line));` |
| フィールド長行 | `:76` `case READING_LENGTHS:` → `:77` `onReadingLengths(line);` | `:172`-`:175` `onReadingLengths` → `:173` `currentFragment.setLengths(tail(line));` |

**データ行（`:79` `case READING_VALUES:`）を列挙に入れなかった理由。** 是正3 で確定したとおり、データ行もトリムは掛かるが、直後の `src/main/java/nablarch/test/core/file/DataFileFragment.java:105`-`:107`（`for (int i = 0; i < names.size(); i++) {` ／ `String value = i < line.size() ? line.get(i) : "";`）が `""` で埋め戻すため、値として観測できる効果がない。`:71` は後段の1文でデータ行を本文へ送っており、データ行について何も主張していない。

**変換ツール側も同じ経路を通ることの確認。** `nablarch-testing-converter@e977824` の `src/main/java/nablarch/test/core/reader/TestCoreReaderAdapter.java:143`-`:160` `readFiles`（`:148` `parser = new FixedLengthFileParser(reader, EMPTY_INTERPRETERS, type);`／`:152` `parser = new VariableLengthFileParser(reader, EMPTY_INTERPRETERS, type);`）と `:178`-`:186` `readMessage`（`:179` `MessageParser parser = new MessageParser(reader, EMPTY_INTERPRETERS, DataType.MESSAGE);`）が、本体のパーサをそのまま使う。原文を取り出す経路（`:264`-`:269` `readBlockBodyLines`）も `:464` `bodyLines.add(NablarchTestUtils.trimTailCopy(line));` で同じトリムを掛ける。呼び出し元は `src/main/java/nablarch/test/tool/converter/xls/XlsFormatReader.java:206`・`:212`・`:229`・`:240`・`:274`。

**実測（固定長ファイル）。** 生成した `.xls` のシート（`空` は空文字のセル）と、`PoiXlsReader#readLine()` が返した行、および解析結果は次のとおり。

| シートの行 | セル |
|---|---|
| 0 | `SETUP_FIXED=ffile` / `空` / `空` / `空` / `空` |
| 1 | `text-encoding` / `Windows-31J` / `空` / `空` / `空` |
| 2 | `record-separator` / `LF` / `空` / `空` / `空` |
| 3 | `REC1` / `A` / `B` / `空` / `空` |
| 4 | `空` / `半角` / `半角` / `空` / `空` |
| 5 | `空` / `5` / `5` / `空` / `空` |
| 6 | `空` / `a1` / `b1` / `空` / `空` |
| 7 | `空` / `a2` / `b2` / `c2` / `空` |

```
   line[0] = [SETUP_FIXED=ffile, , , , ]
   line[1] = [text-encoding, Windows-31J, , , ]
   line[2] = [record-separator, LF, , , ]
   line[3] = [REC1, A, B, , ]
   line[4] = [, 半角, 半角, , ]
   line[5] = [, 5, 5, , ]
   line[6] = [, a1, b1, , ]
   line[7] = [, a2, b2, c2, ]
   -> readLine() が返した行数 = 8

== E5: SETUP_FIXED の解析: probe/a2file
   DataFile 件数 = 1
   path=ffile
    recordType=REC1
    names=[A, B]
    values.size()=2
      [0] {A=a1, B=b1}
      [1] {A=a2, B=b2}
```

`readLine()` は行末の空セルを付けたまま返している（トリムは `PoiXlsReader` ではなく `DataFileParser.java:68` が行う）。それでも `names` は `[A, B]` の2件になっており、フィールド名称行の行末の空セルが取り除かれたことが観測できる。データ型行・フィールド長行も、`DataFileFragment.java:203`・`:287` の `assertSameSizeAsNames` を通っている以上、2件へ揃えられている（揃わなければ例外になる）。ディレクティブ行も行末に空セルを2つ付けたまま受け付けられている。

### A-3 申し送り39 — メッセージのデータ行の空セル

**実測（Excel、`MESSAGE`）。** シートは次のとおり（`空` は空文字のセル）。フィールド名称は `A`・`B`・`C` の3件。

| シートの行 | セル | ねらい |
|---|---|---|
| 6 | `1` / `a1` / `b1` / `c1` / `空` | ふつうの行 |
| 7 | `2` / `a2` / `空` / `空` | 行末が空（値が不足） |
| 8 | `3` / `a3` / `b3` / `c3` / `zzz` | フィールド名称の数を超える値 |
| 9 | `4` / `空` / `b4` / `c4` | 途中の空セル |
| 10 | `空` / `空` / `空` / `空` / `空` | 全セルが空の行 |
| 11 | `5` / `a5` / `b5` / `c5` / `空` | ふつうの行 |

```
   line[6] = [1, a1, b1, c1, ]
   line[7] = [2, a2, , ]
   line[8] = [3, a3, b3, c3, zzz]
   line[9] = [4, , b4, c4]
   line[10] = [5, a5, b5, c5, ]
   -> readLine() が返した行数 = 11

== E3: MESSAGE の解析（Excel）: probe/a3msg
   FixedLengthFile 件数 = 1
    recordType=default
    names=[A, B, C]
    values.size()=5
      [0] {A=a1, B=b1, C=c1}
      [1] {A=a2, B=, C=}
      [2] {A=a3, B=b3, C=c3}
      [3] {A=, B=b4, C=c4}
      [4] {A=a5, B=b5, C=c5}
```

同期応答メッセージ送信（`RESPONSE_BODY_MESSAGES`、`SendSyncMessageParser`）でも同じ結果になった。

```
    values.size()=4
      [0] {DataFileFragment:firstFieldKey=1, A=a1, B=b1, C=c1}
      [1] {DataFileFragment:firstFieldKey=2, A=a2, B=, C=}
      [2] {DataFileFragment:firstFieldKey=3, A=a3, B=b3, C=c3}
      [3] {DataFileFragment:firstFieldKey=4, A=, B=b4, C=c4}
```

**実測（YAML、`messages`）。** 同じ5行を `rows:` に書いた（`["a1","b1","c1"]` ／ `["a2"]` ／ `["a3","b3","c3","zzz"]` ／ `["","b4","c4"]` ／ `[]`）。

```
== Y1: messages（YAML）
    recordType=default
    names=[A, B, C]
    values.size()=5
      [0] {A=a1, B=b1, C=c1}
      [1] {A=a2, B=, C=}
      [2] {A=a3, B=b3, C=c3}
      [3] {A=, B=b4, C=c4}
      [4] {A=, B=, C=}
```

**両形式で同じ点。** (a) 値の数がフィールド名称の数より少なければ、不足したフィールドは `""` になる。(b) フィールド名称の数を超える位置の値（`zzz`）は読み込まれない。(c) 途中の空セル・空文字はそのまま `""` になる。いずれも `nablarch-testing@e21bf67` の `src/main/java/nablarch/test/core/file/DataFileFragment.java:105`-`:107`（`addValue`）・`:173`-`:175`（`addValueWithId`）という同じメソッドを両形式が通るためである（YAML 側の呼び出しは `nablarch-testing-yaml@190cc9a` の `src/main/java/nablarch/test/core/reader/yaml/YamlFileBuilder.java:233`・`:235`）。

**両形式で異なる点。** Excel 形式の「全セルが空の行」（シート行10）は、そもそもパーサに届かない。`PoiXlsReader#readLine()` の出力に現れていない（`nablarch-testing@e21bf67` の `src/main/java/nablarch/test/core/reader/PoiXlsReader.java:93` `if (isBlankLine(list)) {` ／ `:141`-`:147` の `isBlankLine` が全要素空で真）。YAML 形式の空配列 `[]` は `values` に残り、全フィールドが `""` のレコードになる（上の `[4] {A=, B=, C=}`）。

**逐語案（2文。`:1158` の直後に新しい段落として1行で挿入する）。**

```
メッセージボディのデータ行では、記述した値の数（\ Excel\ 形式ではラベル列を除いたセル数、\ YAML\ 形式では ``rows:``\ の各要素の長さ）がフィールド名称の数より少ない場合、不足したフィールドは\ ``""``\ として補完される。フィールド名称の数を超える位置の値は読み込まない。
```

「ラベル列を除いたセル数」としたのは、メッセージボディの各行の先頭要素がフィールドではないため（`:1269`「メッセージボディの各行の先頭要素は、フィールドとしては読み込まれないラベル列である。」）。第2文は `:1545`（A-1）と同じ言い回しにそろえた。

**Excel の「全セルが空の行は読み飛ばされる」ことは、作業指示の「1〜2文」に収めるため、この案に含めていない。** 実測ではメッセージでも起きる（上の Excel の出力に、全セルが空のシート行10が現れていない）。同ページでこの事実に触れているのは `:1534` だけで、そこはテーブル・`LIST_MAP` に限定した書き方になっている（A-4 参照）。メッセージの節にも書くなら第3文として次を足す形になるが、**入れるかどうかは user の判断を待つ**。

```
なお、\ Excel\ 形式で行の全セルを空にした場合は、その行自体が読み飛ばされる。
```

### A-4 `:883` の Excel 側の括弧書き

**結論: 「先頭セルが空の行」は `isBlankLine` に掛かる。現行の括弧書きは成り立たない。**

**実測。** 可変長ファイル（`SETUP_VARIABLE`、フィールド名称は `name`・`memo`）のシートに、全セルが空の行（シート行6）と、`""` を1つ書いた行（シート行7）を並べた。

| シートの行 | セル |
|---|---|
| 5 | `空` / `yamada` / `memo1` |
| 6 | `空` / `空` / `空` |
| 7 | `空` / `""` / `空` |
| 8 | `空` / `tanaka` / `memo2` |

```
   line[3] = [データ, name, memo]
   line[4] = [, 半角, 半角]
   line[5] = [, yamada, memo1]
   line[6] = [, "", ]
   line[7] = [, tanaka, memo2]
   -> readLine() が返した行数 = 8

== E2: SETUP_VARIABLE の解析: probe/a4var
   DataFile 件数 = 1
   path=vfile
    recordType=データ
    names=[name, memo]
    values.size()=3
      [0] {name=yamada, memo=memo1}
      [1] {name=, memo=}
      [2] {name=tanaka, memo=memo2}
```

シート行6（全セルが空）は `readLine()` の出力に現れず、`values` にも残っていない。セルを1つも作らない行に置き換えた場合も同じで、`values.size()` は2になった。一方、`""` を1つ書いた行（シート行7）は `{name=, memo=}` として残っている。

**判定の根拠（`file:line`）。**

| `file:line`（`nablarch-testing@e21bf67`） | 逐語 | 効き方 |
|---|---|---|
| `src/main/java/nablarch/test/core/reader/PoiXlsReader.java:93` | `            if (isBlankLine(list)) {` | 真なら `:94` `continue;` で次の行へ進み、その行は返さない |
| 同 `:141`-`:147` | `    private boolean isBlankLine(List<String> line) {` … `            if (!e.isEmpty()) {` … `        return true;` | 全要素が空文字なら真 |
| `src/main/java/nablarch/test/core/reader/TestDataParsingTemplate.java:176` | `            if (isBlankLine(line)) {` | 同 `:316`-`:317` `private boolean isBlankLine(List<String> line) { return StringUtil.isNullOrEmpty(line); }`。コメント除去後にもう一度、全要素空の行を落とす |

「全フィールドを省略した行」を Excel 形式で書くと、レコード種別のセル（先頭セル）もデータ行では空であるため（`DataFileParser.java:204`-`:210` `isDataRow` が先頭セルの空を条件にする）、**行全体が空になり `isBlankLine` に掛かる。** したがって現行 `:883` の「Excel形式では先頭セルが空の行」は、そのとおりに書くとレコードが残らない。

**正しい Excel の記法は、同じページに既にある。** `:1363`「可変長ファイルなどで空行を表現したい場合は、特殊記法のダブルクォートを使って ``""``\ のように空文字列を記述する。」／`:1367`「空行を表現する際、行内の全フィールドを ``""``\ にする必要はなく、いずれか1つのフィールドに記述すれば十分である。読みやすさの観点からは、先頭のフィールドに ``""``\ を書く方法を勧める。」。本体のテストデータも同じ書き方をしている（`nablarch-testing@e21bf67` の `src/test/java/nablarch/test/core/reader/VariableLengthFileParserTest.xls` のシート `testEmptyRowSingleItem` 5行目 `[空][""][// 空行が読み込めていること]`、シート `testEmptyRowMultiItems` 6行目 `[空][""][空][…]`。POI で読み出して確認した）。

**逐語案（`:883` の1段落＝1行）。**

```
固定長ファイルと可変長ファイルには、それぞれ固有の記法制約がある。固定長ファイルでは、フィールド名称・データ型・フィールド長の3リストが同サイズで必須であり、1ファイルデータブロック内の全レコード定義は同一レコード長でなければならない（違反時はエラー）。フィールド値がフィールド長を超えた場合もエラーになる。可変長ファイルでは、フィールド名称・データ型の2リストが同サイズで必須であり、フィールド長は不要である。データ行のセル数（Excel形式）または ``rows:`` の各要素の長さ（YAML形式）がフィールド数より少ない場合、不足したフィールドは\ ``""``\ として補完される。これを利用し、全フィールドを省略した行（Excel形式ではいずれか1つのフィールドに ``""``\ と記述した行、YAML形式では ``rows:`` に空配列 ``[]`` を記載した行）を書けば、全フィールドが\ ``""``\ のレコードとして保持される（固定長ファイルの場合はスペースパディングされた定長レコードとして書き出される）。この扱いは、テーブルデータの空行のスキップとは異なる仕組みである。なお、\ Excel\ 形式で行の全セルを空にした場合は、その行自体が読み飛ばされるため、レコードにならない。
```

現行との差は2箇所（括弧書きの Excel 側と、末尾の1文の追加）。括弧書きは「Excel形式では先頭セルが空の行」→「Excel形式ではいずれか1つのフィールドに ``""``\ と記述した行」。YAML 側は実測どおりで変更しない（上の Y1 `[4] {A=, B=, C=}`、および可変長ファイルの `rows: []` でも `{name=, memo=}` が残ることを確認した）。

**全セルを空にした行が読み飛ばされることも書き足した。** 当初は `:1534` との重複を避けて省いたが、`:1534` の逐語を開き直したところ「この空エントリの省略は、…**テーブルデータや\ ``LIST_MAP``\ のエントリ自体を無いものとして扱う点で異なる**」とテーブル系に限定して書いており、ファイルデータについては同ページのどこにも書かれていない（`grep -n '読み飛ば' ja/development_tools/testing_framework/implementation/testdata_notation.rst` の結果は `:1534` の1件のみ）。実測どおり、この読み飛ばしは `PoiXlsReader.java:93` の位置で起きるためデータタイプを問わない。**重複ではなく、書き漏れである。** 末尾に置いたのは、直前の「この扱いは、テーブルデータの空行のスキップとは異なる仕組みである。」の「この扱い」が読み飛ばしの側に掛かって読めないようにするため。

### A-5 申し送り41 — 行の呼称

**結論: 矛盾ではなく別概念である。ページの是正は不要。申し送り41 はクローズしてよい。**

`mapping/glossary.md` §5.10 は**行**の名称を定義し、`implementation/testdata_notation.rst:854`-`:858` の構成図と `:866`-`:879` の用語表は**要素**の名称を挙げている。根拠は次の4点で、いずれも現物を開いて確かめた。

1. **§5.10 の見出しと導入文が「行」に限定している。** 見出しは「5.10 ファイルデータの行の名称」、導入文は「ファイルデータのレイアウトを表す行の名称である。」
2. **`:860` が用語表を「要素」として導いている。** 逐語は「各要素の名称と役割は、以下のとおりである。」。表の中身も要素であり、`:876`-`:877` の `データ`（「実データ（レコード1件分の値の並び）」）と `:878`-`:879` の `パディング`（「フィールド長に対してデータのバイト長が短い場合に、データ型に応じて自動で補完される処理」）が含まれる。**`パディング` は処理であって行ではない。** 行の名称の一覧なら入り得ない
3. **構成図 `:855` が「1行に2要素」を示している。** 逐語は「    → レコード種別 + フィールド名称」。行の名称の一覧なら「レコード種別 + フィールド名称」という項目は書けない。実装でも、レコード種別はフィールド名称行の**先頭セル**である（`nablarch-testing@e21bf67` `src/main/java/nablarch/test/core/reader/DataFileParser.java:248`-`:252`。`:250` `currentFragment.setRecordType(fieldNamesLine.get(0));`／`:251` `currentFragment.setNames(tail(fieldNamesLine));`）。本ラウンドの実測でも、シート行 `[REC1][A][B][空][空]` から `recordType=REC1`・`names=[A, B]` が1行分として取り出されている（A-2 の実測結果）
4. **同じ解説書が両方を使い分けている。** `implementation/testdata_examples.rst:1082`「Excel\ 形式では、レコード種別行の先頭要素にレコード種別を、以降の要素にフィールド名称を記述する。」——**1文の中で「レコード種別行」（行）と「レコード種別」「フィールド名称」（要素）を書き分けている。** 同ファイル `:1092`「  * - レコード種別行」・`:1094`「  * - データ型行・フィールド長行」も行として使っている

したがって、ページ内で『行』付きが使われている7箇所（`implementation/testdata_notation.rst:1010`・`:1055`・`:1076`・`:1267`・`:1269`・`:1271`・`:1545`）は、いずれも「行」を指す文脈であり、構成図・用語表の『行』なしと衝突していない。`implementation/testdata_examples.rst` への波及も無い（`:1082`・`:1092`・`:1094`・`:1205`・`:1267`・`:1341`・`:1619`・`:1646` はすべて行を指す）。

**是正5 §1 (1) の反例（`:1545` から『行』を落とすとメッセージの節から離れる）は、この結論と整合する。** 『行』を落とすと、行を指す語を要素の語で書くことになる。A-1 が『行』付きを据え置くのは、この意味でも正しい。

**残る1件。** `mapping/glossary.md` §5.10 の `レコード種別行` は、実装上「レコード種別だけの行」は存在せず、フィールド名称行の先頭セルである。同節自身が採用根拠欄で「**根拠が弱い。** `:175` は図中のラベルで定義箇所ではない」と注記しており、`implementation/testdata_examples.rst:1082` は「レコード種別行の先頭要素にレコード種別を、以降の要素にフィールド名称を記述する」と、レコード種別行＝フィールド名称行として使っている。**用語集は変更禁止のため本ラウンドでは触れていない。** 申し送り43 として起票した。
