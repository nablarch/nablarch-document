# レビュー記録: `setup/common.rst`（第2部「共通設定」）

対象タスク: `#11`（作業指示 `ntf-doc-11-common.md`）。ベースコミット `f4e4de8`。

対象ページ: `ja/development_tools/testing_framework/setup/common.rst`（新規作成）
出典: 削除済みの `ja/development_tools/testing_framework/guide/development_guide/06_TestFWGuide/03_Tips.rst`（削除前コミット `2e501ad`）
マッピング行: `dest_page=共通設定` の5行（`current-0225` / `current-0226` / `current-0227` / `current-0228` / `current-0246`）。129 lines、すべて `dest_section=使用方法`・`disposition=MERGE`・`audience=user`。

## 作成時の判断（レビュー前に確定したもの）

作業指示 `ntf-doc-11-common.md` が「`reviews/page-common.md` に記録すること」と明示している3件を含め、
本ページ作成時に行った判断を記録する。

### D-1 `FixedSystemTimeProvider` の桁数は実装を優先し「14桁 / 17桁」とした（作業指示 STEP 2-1）

| 項目 | 内容 |
|---|---|
| 出典の記述 | `03_Tips.rst:330-331` 「`yyyyMMddHHmmss` (12桁)」「`yyyyMMddHHmmssSSS` (15桁)」 |
| 実装での確認結果 | `yyyyMMddHHmmss` は14桁、`yyyyMMddHHmmssSSS` は17桁 |
| 実装の `file:line` | `nablarch-testing` の `src/main/java/nablarch/test/FixedSystemTimeProvider.java:23,26`（フォーマット文字列の定義）、`:46,48`（`len == LONGEST_FORMAT.length()` / `len == SHORTEST_FORMAT.length()` による桁数判定）、`:51`（形式外は `IllegalArgumentException`） |
| 参照コミット | `nablarch/nablarch-testing` の `main`、`e21bf67` |
| 判断 | `design.md` §8「出典と実装が食い違う場合は実装を優先する」に従い、**14桁 / 17桁**と記載した。出典の「12桁 / 15桁」は本ページに写していない（ゲート6で0件を確認） |
| 補足 | 実装側の Javadoc（`FixedSystemTimeProvider.java:35-36`）にも同じ「12桁 / 15桁」という誤りがある。判定の根拠は Javadoc ではなくコード（`:46,48`）である。出典自身が挙げる設定例 `20100914123456`（`03_Tips.rst:321`）も14桁であり、出典内部でも整合していない |
| 実装確認の実施者 | `#11` 作業指示 STEP 2 のとおり、レビュー役が `e21bf67` を clone して実コードで確認した結果を根拠として採用した。本タスクでは `nablarch-testing` を再取得していない（起動ディレクトリ外のため） |
| **是正ラウンド1での追記** | 判断内容は不変。是正ラウンド1（R1-3・R1-4）で行数が増えたため、桁数の記述位置は `:45` → **`:49`** に移動した |

### D-2 `current-0226` の Java コード例はコードブロックとして載せない（作業指示 STEP 3-1）

| 項目 | 内容 |
|---|---|
| 対象 | `03_Tips.rst:334-338` の `code-block:: java`（`SystemRepository.getObject("systemTimeProvider")` で `SystemTimeProvider` を取得し `getDate()` を呼ぶ2行） |
| 判断 | コードブロックを置かない |
| 理由 | `design.md` §3「記載範囲」は、第2部に置くものをコンポーネント設定ファイルの設定項目・記述例・拡張方法に限り、**テストソースコードの実装例は置かない**と定めている。当該コード例は設定ではなく API の使用例であり、第2部の記載範囲外である |
| 内容の引き継ぎ | 内容は落とさず、地の文として残した（`setup/common.rst:45` 「この設定を行うと、テスト対象のアプリケーションが ``SystemTimeProvider`` を通じて取得するシステム日時は、指定した日時に固定される。」）。「固定した日時は `SystemTimeProvider` を通じて取得される」という事実が保持されている |
| **是正ラウンド1での追記** | 判断内容は不変。行数が増えたため当該の地の文は `:45` → **`:49`** に移動した。R1-4（重複説明の圧縮）は `:40` の段落を対象としており、本項の `:49` の文には手を入れていない |

### D-3 採番の記述例は `testdata_examples-table_data` へ `:ref:` で導線を張った（作業指示 STEP 3-2）

| 項目 | 内容 |
|---|---|
| 参照元 | `setup/common.rst:83`（`シーケンス採番をテーブル採番に置き換える` の末尾）。是正ラウンド1で行数が増えたため現在は **`:87`** |
| 参照先 | `` :ref:`テーブルのデータを記述する <testdata_examples-table_data>` `` |
| 理由 | 出典の「Excelファイル記述例」（`03_Tips.rst:389` 以降）は `current-0229` として `dest_page=テストデータの記載例` に割当済みであり、本ページには割り当てられていない。`design.md` §3「『使い方』に該当するものは第3部に置き、第2部からは `:ref:` で参照する」に従い、第2部から第3部へ導線だけを張った |
| ラベル選定の理由 | 採番の記述例そのもの（`testdata_examples.rst:951` の L3 `採番処理のテストデータを記述する`）にはラベルが無い。承認済みの `#10` の成果物である `testdata_examples.rst` にラベルを足さない（作業指示 禁止事項）ため、その親にあたる L2 セクションの既存ラベル `testdata_examples-table_data`（`testdata_examples.rst:776`）を指した |
| 表示テキストの扱い | 作業指示は表示テキストを「テーブルデータを記述する」と記していたが、参照先の実際の見出しは `testdata_examples.rst:778` 「**テーブルのデータを記述する**」である。参照先の見出し文言と表示テキストを一致させる（`testdata_examples.rst:780` 自身が `` :ref:`テーブルのデータを記述する <testdata_notation-table_data>` `` と実見出しに揃えている前例に合わせる）ため、実見出しの表記を採用した |
| **申し送り** | 本参照は L2 セクションを指しており、読者は着地後にページ内で L3 `採番処理のテストデータを記述する` を探す必要がある。**採番の記述例をピンポイントで参照する必要が生じた場合は、`testdata_examples.rst` の当該 L3 に `testdata_examples-id_generation` 相当のセクションラベルを追加し、本ページの `:ref:` をそちらに差し替える。** `#10` 成果物への追記になるため、実施は `#10` の凍結解除またはユーザー判断を伴うタスクで行うこと |

### D-4 出典の2つの単一行の表を、表にせず地の文にした

| 項目 | 内容 |
|---|---|
| 対象 | `03_Tips.rst:326-332`（`fixedDate` の property名／設定内容、grid table・データ行1件）、`03_Tips.rst:740-745`（`nablarch.test.resource-root` のキー／値、simple table・データ行1件） |
| 判断 | いずれも表を作らず、地の文と `code-block` で記述した |
| 理由 | 両表ともデータ行が1件しかなく、表にしても列見出し（`property名`／`設定内容`、`キー`／`値`）が情報を増やさない。`style.md` S-07 は「表を作る場合にどの記法を使うか」を定める規約であり、表を作ること自体を要求していない。内容（キー名・プロパティ名・値の意味・受け付ける形式）はすべて地の文に保持している（本ファイル「出典と本文の対応」参照）。なお本ページは表を1件も持たないため、S-07 の例外（表が多いページは `list-table` に揃える）の適用対象にもならない |
| 落とした情報 | なし。出典の grid table（`+---+` 形式）は `style.md` S-07 が使用を禁じているため、いずれにせよそのままの形では持ち込めない |
| **是正ラウンド1での追記** | 表にしない判断は不変。ただし `nablarch.test.resource-root` を地の文化した `:16` の設定先は、R1-1 により `コンポーネント設定ファイル` → **`環境設定ファイル`** に改めた |

### D-5 コードブロックの言語指定を出典から変更した

出典は `nablarch.test.resource-root` の設定例に `bash`（`03_Tips.rst:753`）と `text`（`:763`）を使い分けているが、内容は同一形式の `キー=値` である。`style.md` S-05「言語指定は実際の内容に応じて個別に付ける」に従い、両方とも `properties` に統一した（FW解説書のライブラリでも `properties` は24件使われている）。

**是正ラウンド1での追記**: R1-1 で設定先を `環境設定ファイル` に改めたが、言語指定は `properties` のまま据え置いた（是正指示が明示。`ja/` 全体で `properties` は76件の実績があり、内容の `key=value` 形式とも一致する）。

### D-6 「機能概要」「拡張例」の見出しとトランザクションの節を置かなかった

`design.md` §3 は第2部のページを「機能概要（任意）→ 使用方法（必須）→ 拡張例（任意）」とし、「出典が無い場合は見出し自体を置かない」と定めている。`verify_mapping.py` の出力でも `[第2部 導入と設定 > 共通設定 > 機能概要]` と `[同 > 拡張例]` はいずれも 0 row である。トランザクションについては、`design.md` §3 の記載内容欄に語としては挙がっているが、本ページに割り当てられたマッピング行が無く（トランザクション関連の `current-0237`・`current-0195` はいずれも `dest_page=コンポーネント単体テスト`）、節を作らなかった。

## レビュー記録

`#11` のレビュー（`design.md` §11.6 の4観点 A:網羅性 / B:トンマナ / C:用語 / D:整合性）は、
コーディネーターが別のサブエージェントで実施する（作業指示 STEP 5）。本節はその結果を記録する欄である。

### ラウンド1（2026-08-07）

4観点（A:網羅性 / B:トンマナ / C:用語 / D:整合性）をそれぞれ別のサブエージェントで実施した（作業指示 STEP 5）。
コーディネーターが4観点の指摘を統合・判定した結果、**是正する4件**と**対応しない8件**の計12件に整理した。以下はその全件記録である。

**区分について**: `design.md` §11.7 の区分は `must` / `decide` / `note` の3つだが、本ラウンドのレビュー役は `must` / `should` / `note` を用いた。`should` は「規約違反ではないが規約の趣旨に照らして直すべき」もので、対応要否は「要」である（`decide` に該当する＝ユーザー判断に回す指摘は本ラウンドに0件）。

| 指摘ID | ラウンド | 観点 | 区分 | 指摘内容 | 対応要否 | 不要の理由 | 対応内容 |
|---|---|---|---|---|---|---|---|
| R1-1 | 1 | C | `must` | `:16`・`:30` が `nablarch.test.resource-root` の設定先を `コンポーネント設定ファイル` と書いているが、`:18` のコードブロックは `properties`（`key=value` 形式）である。`glossary.md` §5.12 は `コンポーネント設定ファイル` を「システムリポジトリに登録するコンポーネントを定義する**XMLファイル**」と定義しており、`key=value` 形式のファイルを指さない。同一ページ内の他2節（`:38`・`:53`・`:69`）は実際に XML であり、同じ語が2つの異なるファイルを指す状態になっていた | 要 | — | `:16` の設定先を **`環境設定ファイル`** に改めた。あわせて `:12` の L2 導入文「いずれもコンポーネント設定ファイルで設定する。」は3項目のうち1つが環境設定ファイルになり誤りになるため「いずれも設定ファイルへの記述で行う。」に改め、**各L3が自分の設定先を名乗る形**にした（読み込み先＝`:16` 環境設定ファイル、日時固定＝`:40` コンポーネント設定ファイル、採番＝`:53`・`:55` コンポーネント設定ファイル）。根拠は `FW:libraries/repository.rst:478`（環境設定ファイルは key-value 形式）・`:504`（コンポーネント設定ファイルは xml）・`:537`（環境設定ファイルには properties を推奨）、および**削除前の現行NTF解説書自身**が同じ `nablarch.*=値` 形式を「環境設定ファイル」と呼び、直後の節で「コンポーネント設定ファイルに監視対象テーブルを記載」（XML）と書き分けていること（`git show 2e501ad:…/04_MasterDataRestore.rst` の `nablarch.suppress-table-sort=true`）。出典 `03_Tips.rst:738` の「コンポーネント設定ファイル」は出典側の誤りであり、`design.md` §8「出典と実装が食い違う場合は実装を優先する」を適用した。`環境設定ファイル` は `glossary.md` 未収載であり揺れ表記ではないため、FW解説書の語をそのまま用いた。コードブロックの言語指定 `properties` は据え置き（D-5 参照）。**あわせて `:34` の `tip` を書き直した**。旧文の `VM引数` は FW解説書に0件（`grep -rc "VM引数" ja/application_framework/` → 0）・承認済みNTFページにも0件であり、FW解説書は同概念を `システムプロパティ` と呼ぶ（`repository.rst:547-563`）。`glossary.md` §2 の採用優先順位1に従い `システムプロパティ` と `-D` の語に置き換え、上書きの仕組みは自分で説明せず `` :ref:`システムプロパティを使って環境依存値を上書きする <repository-overwrite_environment_configuration>` `` に委ねた（表示テキストは参照先の実見出し `repository.rst:547` に一致させた） |
| R1-2 | 1 | D | `must` | `:56` の `class="nablarch.common.idgenerator.OracleSequenceIdGenerator"` は**Nablarch に存在しないクラス**である | 要 | — | `com.example.common.idgenerator.OracleSequenceIdGenerator` に改めた。根拠は (a) 公開 javadoc の HTTP ステータス実測（`https://nablarch.github.io/docs/6u2/javadoc/nablarch/common/idgenerator/OracleSequenceIdGenerator.html` → **404**。同ディレクトリの `FastTableIdGenerator` / `SequenceIdGeneratorSupport` / `TableIdGenerator` / `IdGenerator` はいずれも **200**）、(b) 同じ文書セットの既存ページ `FW:setting_guide/CustomizingConfigurations/index.rst:141` が `class="com.example.common.idgenerator.OracleSequenceIdGenerator"` と書いており、しかも `idTable` に `<entry key="1101" .../>` を入れる**本ページと同一のパターン**である。したがって当該クラスはプロジェクト側が用意するクラスであり、出典 `03_Tips.rst:364` の `nablarch.` 始まりの記述は誤りである。`design.md` §8 の適用であり、桁数 12/15 → 14/17 の是正（D-1）と同じ性質。**クラス名以外は変更していない**（プロパティの説明・コメント・一覧表は追加していない）。`FastTableIdGenerator`（現 `:76`）は実在するため変更していない |
| R1-3 | 1 | B | `should` | `:22` の第2文「複数指定した場合、同名のテストデータファイルが複数のディレクトリに存在すると、最初に見つかったものが読み込まれる。」は `style.md` S-06 の `important`（無視すると不具合・データ不整合につながる、読者が必ず守るべき注意事項）に該当するが、地の文に平坦化されて強度が下がっている。意図しないテストデータが黙って読まれるとテストが誤って通る。出典（`03_Tips.rst:777-779`）はこれを脚注で注意喚起していた。承認済みの `about/index.rst` は同種の制約に `.. important::` を3件使っている | 要 | — | 当該の1文を `.. important::`（現 `:28-30`）に出した。第1文（セミコロンで複数指定できる旨）は地の文（`:22`）に残した。あわせて2点を修正: (a) **「複数」の反復**（2文中3回）は、important への分離で2ブロックに各1回まで減らした。(b) **`同名のテストデータファイル` → `同名のテストデータ`**。`glossary.md` §5.9 は1テストクラス分のテストデータ全体を Excel形式では「1つのExcelファイル」、YAML形式では「同名の1ディレクトリ」と定めており、`テストデータファイル` では YAML形式で厳密でなくなる。出典 `:778` も「同名のテストデータが存在した場合」と書いている。配置は 地の文 → 設定例 → `important` → `tip` とし、説明と例を隣接させたまま注意を後置した |
| R1-4 | 1 | B | `note` | `:36` が1段落4文・約230字で、「実装クラスを `FixedSystemTimeProvider` に差し替える」と「実装クラスを指定している箇所を次のように書き換え」が**同じ操作を2回説明**していた。本ページ最長文136字を含め上位3文がこの段落に集中していた | 要 | — | 重複する2文を1文に統合し、4文 → 3文に短縮した（現 `:40`）。**情報は落としていない**: `SystemTimeProvider` 実装クラスがシステム日時を提供する／テストでは `FixedSystemTimeProvider` に差し替える／`fixedDate` に固定したい日時を指定する／例は2010年9月14日12時34分56秒、のすべてを保持。統合により `コンポーネント設定ファイル` という設定先の明示（R1-1 の「各L3が自分の設定先を名乗る」）も維持されている。段落内で改行していない（1段落1行） |
| R1-5 | 1 | A | `note` | `FastTableIdGenerator` の4プロパティ（`tableName`・`idColumnName`・`noColumnName`・`dbTransactionManager`）に説明表またはXMLコメントを付けるべきである | **不要** | 作業指示の禁止事項「採番のクラスについて、出典に無い説明を補わない。プロパティ一覧表を作らない」に抵触する。出典 `03_Tips.rst:379-385` は設定例XMLのみを示し、各プロパティの意味を説明していない | 本文を変更していない |
| R1-6 | 1 | A | `note` | `FastTableIdGenerator` の初期化設定（`BasicApplicationInitializer` の `initializeList` への登録）が書かれておらず、設定例が単体では動作しない可能性がある | **不要** | R1-5 と同じく、出典に無い内容の追加にあたる（作業指示の禁止事項） | 本文を変更していない。**申し送り**: 実装の Javadoc が初期化を要求しており、`FW:libraries/database/generator.rst:114-122` に `initializeList` への登録例がある。第2部の他ページ（採番を扱う設定ページ）を作る際、または本ページの凍結解除時に、初期化設定への `:ref:` 導線を張るかどうかを再検討すること |
| R1-7 | 1 | D | `should` | L3セクションに `:ref:` ラベルを足すべきである（他ページから個々の節を参照できない） | **不要** | 作業指示 STEP 1 が「本タスクでは先頭ラベルのみとし、セクションラベルは置かない」と明示的に確定している | 本文を変更していない。**申し送り**: `implementation/testdata_notation.rst:1302`・`testdata_examples.rst:1855` が本ページの `システム日時を固定する` の内容を参照したい文脈を持つ。ただし両ページは `#10` で承認済み・変更禁止であるため、いまラベルを足しても**参照元を作れない**。両ページの凍結が解けた時点で、`testing_framework_common-fixed_system_time` 相当のラベル追加と参照元からのリンク追加をセットで行うこと |
| R1-8 | 1 | D | `should` | `:16` の既定値 `test/java` と、`implementation/testdata_notation.rst:48,87` の `src/test/java/...` という配置説明が噛み合わない（読者が既定値の基準ディレクトリを誤解しうる） | **不要** | `testdata_notation.rst` は `#10` で承認済み・変更禁止である。本ページ側で「Maven 標準構成では〜」と補うのは出典外の追記になる | 本文を変更していない。**申し送り**: 既定値 `test/java` は**テスト実行時のカレントディレクトリからの相対パス**であり（実装 `TestSupport.java:30`、本ページ `:16` に明記済み）、`src/test/java` とは基準が異なる。両ページを同時に触れるタスク（凍結解除後、または第2部・第3部の整合を取るタスク）で、どちらかに基準ディレクトリの説明を1箇所だけ置いて相互参照させること |
| R1-9 | 1 | A | `note` | `fixedDate` に14桁を指定した場合のミリ秒 `000` 補完と、形式外の値での `IllegalArgumentException` が書かれていない（実装 `FixedSystemTimeProvider.java:50`・`:51`） | **不要** | いずれも出典 `03_Tips.rst` に無い情報である。作業指示 STEP 2-1 は桁数の**誤りの是正**を求めたのみで、実装から新たな挙動を書き起こすことは求めていない。第2部の記載範囲（設定項目・記述例・拡張方法）にも収まらない | 本文を変更していない。**これは意図的な不記載である**（見落としではない。実装での確認結果は D-1 および `checks/task-11.md` に記録済み） |
| R1-10 | 1 | B | `note` | `tip` の直後に `:ref:` の地の文段落が続く構成（`:83-87`）は座りが悪い | **不要** | FW解説書ライブラリの0字下げ `.. tip::` 90件中18件が同じ形であり、前例が十分にある | 本文を変更していない |
| R1-11 | 1 | D | `note` | `:java:extdoc:` の生成URLが `6-NEXT-SNAPSHOT` を指すため、現時点では404になる | **不要** | `ja/conf.py:58` に由来する**文書全体共通の挙動**であり、本ページ固有の欠陥ではない。`ja/conf.py` は作業指示の禁止事項で変更禁止 | 本文を変更していない |
| R1-12 | 1 | D | `note` | 作業指示 STEP 3-1 が Java コード例の位置を `03_Tips.rst:339-342` としているが、実際は `:334-338` である | **不要** | 指示書側の行番号の誤りであり、成果物の扱い（コードブロックを載せず地の文に残す）自体は正しい。成果物側は D-2 および `checks/task-11.md` ゲート1で正しい行番号 `:334-338` を記録している | 本文を変更していない。記録のみ |

#### ラウンド1の指摘件数と観点の内訳

`#10b` の申し送り「各ラウンドの指摘件数と観点を記録すること」（レビューサイクル改善の効果測定のため）に従う。

| 観点 | `must` | `should` | `note` | 計 | 判定 |
|---|---|---|---|---|---|
| A（網羅性） | 0 | 0 | 7 | 7 | pass |
| B（トンマナ） | 0 | 3 | 3 | 6 | 条件付き pass |
| C（用語） | 1 | 1 | 4 | 6 | 条件付き pass |
| D（整合性） | 1 | 2 | 4 | 7 | fail |
| **合計** | **2** | **6** | **18** | **26** | — |

上表は4観点のレビュー役が挙げた**生の件数**（26件）である。観点間の重複をコーディネーターが統合・判定した結果が上記の R1-1〜R1-12（12件）であり、うち**対応した4件**が R1-1〜R1-4、**対応不要と判定した8件**が R1-5〜R1-12 である。`must` 2件（R1-1・R1-2）はいずれも本ラウンドで解消しており、`must` を残したまま user review に上げる状態にはない。`decide`（ユーザー判断に回す指摘）は0件。

是正ラウンド1の検証は `checks/task-11.md` の「是正ラウンド1」節に記録した。`#10b` の申し送りに従い、**是正差分に限定した観点**（是正が指示範囲に収まっているか／新しい欠陥を生んでいないか）のみを回し、4観点の全再走査は行っていない。

## 出典と本文の対応

`dest_page=共通設定` の5行の全件対応は `checks/task-11.md` のゲート1に記載する。

## `#16` リード文の移動（2026-08-12）

`#16`（`ntf-doc-16-lead-and-design.md`）で、`使用方法` の直下にあった導入文をページ先頭（目次 `.. contents::` の直後、`使用方法` の見出しより前）へ移した。ページ全体を説明する文の置き場所を FW解説書のライブラリに揃えるためで、規約は `mapping/style.md` S-02、アウトラインへの反映は `design.md` §3 に書いた。

| | 内容 |
|---|---|
| 移動前（`使用方法` 直下、`:12`） | ここでは、テストデータの読み込み先の変更、システム日時の固定、シーケンス採番のテーブル採番への置き換えについて説明する。いずれも設定ファイルへの記述で行う。 |
| 移動後（目次の直後、`:10`） | 共通設定では、テストデータの読み込み先の変更、システム日時の固定、シーケンス採番のテーブル採番への置き換えができる。いずれも設定ファイルへの記述で行う。 |

文頭の `ここでは、` を落とし、`共通設定では、` と主語を立てて文末を `〜ができる。` の言い切りに変えた。リード文はページ全体を指すため「ここ」の指す範囲が曖昧になること、FW解説書のリード文が対象を主語に立てて言い切っていること（`FW:libraries/date.rst:8`・`session_store.rst:10`・`exclusive_control.rst:10`）による。列挙している対象と2文目（`いずれも設定ファイルへの記述で行う。`）は変えていない。

見出しの文言・並び順、L3以下の本文・表・コードブロックは変更していない（`checks/task-16.md` ゲート3）。

---

## `#18` テストデータ読み込み先の是正（2026-08-13、作業指示 `ntf-doc-18-default-value-basis.md`）

`#17` の `decide` 2 の回答により、「デフォルト」として載せる値の基準が**デフォルト設定
（`nablarch-testing-default-configuration`）を読み込んだ状態で有効になる実効値**に確定した
（規定は `design.md` §8「設定項目表の「デフォルト値」の基準」）。本ページの1文を基準に合わせて是正した。
既存の記録は書き換えていない。

### 是正した箇所と根拠

| 箇所 | 是正前 | 是正後 |
|---|---|---|
| `:17` | テストデータは、デフォルトでは `test/java` 配下から読み込まれる | テストデータは、デフォルトでは `src/test/java` 配下から読み込まれる |

**是正前の記載は誤りではなかった。** 出典どおりであり（`03_Tips.rst:735`、`2e501ad`）、クラス定数とも
一致していた（`TestSupport.java:30` の `DEFAULT_RESOURCE_ROOT = "test/java/"`）。**基準が変わったことに
よる是正である。**

実効値の根拠は次のとおり。いずれも自分で開いて確認した。

- `nablarch-testing-default-configuration` **6u3** の jar 内 `nablarch/test/test-data.config` が
  `nablarch.test.resource-root=src/test/java` を与える（同ファイル1行のみ）
- 同 jar 内 `nablarch/test/test-data.xml` が `<config-file file="nablarch/test/test-data.config"/>` で
  これを読み込む
- 両アーキタイプの `archetype-resources/src/test/resources/unit-test.xml` がこれを `<import>` する
  — `nablarch-web-archetype` 6u3 の `:14`、`nablarch-jaxrs-archetype` 6u3 の `:14`（いずれも
  `<import file="nablarch/test/test-data.xml"/>`）。両 jar は Maven Central から取得して展開した

したがって、ブランクプロジェクトの読者が実際に目にする値は `src/test/java` である。

**変更は当該1文に限る。** `common.rst` の他のセクション・見出し・コードブロックは変更していない
（`checks/task-18.md` ゲート5。差分は `-1 / +1` 行で、変更は同一文内の1語のみ）。

### `#12` 以降への申し送り R1-8 の帰結

`#11` の申し送り R1-8（「既定値 `test/java` と `testdata_notation.rst` の配置説明の基準ディレクトリが
一致しない」）は、**本是正で解消の方向に進んだ。** 本ページが `src/test/java` になったことで、
テストデータの配置をプロジェクトルートからの相対パスで説明する記述と基準が揃う。ただし
`testdata_notation.rst` 側の記述は本作業指示の変更対象外（`ja/` の変更は本ページと `web.rst` の2ファイルに
限定）のため、突合そのものは `#19` 以降に持ち越す。

## `#27` 冒頭の申し送り（`ntf-doc-weekend-queue.md` §8）

`#26` で追加した `nablarch-testing-yaml` の依存関係は、他ページ（`setup/request_unit_test/rest.rst:24-37` など）と揃えてバージョンを書かない形にしている。書き方としては正しいが、このモジュールは `1.0.0-SNAPSHOT` であり（`/home/tie303177/work/nablarch/nablarch-testing-yaml/pom.xml:16` を実測。`<version>1.0.0-SNAPSHOT</version>`）、BOM に収録されるまでは読者がそのまま貼っても解決できない。**リリース時に BOM 収録を確認する。** 本文はいま変更しない。
