# 作業指示 #35-是正3

対象リポジトリ: nablarch/nablarch-document、ブランチ ntf-yaml-support（`9aa06d7`）

指摘の must は成立する。私の A-3 の指示が誤っていた。`trimTailCopy` が4分岐すべてに
掛かることは事実だが、データ行では直後の `DataFileFragment.java:105`-`:107`
（`String value = i < line.size() ? line.get(i) : "";`）で埋め戻され、効果が打ち消される。
**案B を採る。**

なお `MessageParser.java:27`・`:115` は `FixedLengthFileParser` へ委譲しており、
メッセージも `DataFileParser.onReadLine` を通る（`nablarch-testing@e21bf67`）。
「ファイル・メッセージ」の行に3行分をまとめて書いてよい。

---

## 1. `implementation/testdata_notation.rst:1545` を書き換える

```
    - フィールド名称行・データ型行・フィールド長行の行末の空セルを取り除く（\ Excel\ 形式のみ）。フィールド名称が宣言されていない値は読み込まない
```

トリムに観測できる効果があるのはこの3行だけである。データ型行・フィールド長行は
`DataFileFragment.java:203`・`:287` の `assertSameSizeAsNames` があるため、トリムされないと
フィールド名称と個数が合わずエラーになる。データ行は上記のとおり埋め戻される。
`:1547`（テーブル・`LIST_MAP`）は変更しない。

用語は `mapping/glossary.md` §5.10「ファイルデータの行の名称」の正表記に従う。

---

## 2. 残りの指摘を処置する

- `reviews/page-testdata_notation.md:667` の「`:883` と食い違っていないことの確認」を、
  §1 の確定文面に合わせて書き直す。
- 新節の `mapping/glossary.md` への参照を、行番号から §5.10「ファイルデータの行の名称」へ直す。
- 「（\ Excel\ 形式のみ。前述）」の括弧についての指摘は、**却下でよい**。判断済みである。

---

## 3. 申し送りを1件起こす

`tools/testdata_converter.rst:71` が「データ行の空セルの扱いは形式によって異なるため、詳細は
参照」と送っている先に、**メッセージのデータ行についての記述がない**（`:1152`-`:1309` に
空セル・補完の記述0件）。本タスクの範囲外なので、`#35` では直さず申し送りに起こす。

---

## 4. レビューは差分限定で回す

`ntf-doc-13-standing-rules.md:20` に従い、§1〜§3 の差分について次の2点だけを見る。

- 是正が §1〜§3 の範囲に収まっているか
- **§1 の逐語指定文そのものに反例がないか**（実装で裏を取ること）

指摘件数と観点を `reviews/page-testdata_notation.md` に記録する。

---

## 完了条件

1. `implementation/testdata_notation.rst:1545` が §1 の1行と逐語一致している
2. `:1547` が変更されていない
3. §2 の3件が処置済み
4. §3 の申し送りが起こしてある
5. §4 のレビューを回し、指摘件数と観点を記録済み。`must` を残していない
6. Docker フルビルドが成功し警告0、`git checkout -- locales/ja/LC_MESSAGES/sphinx.mo` 実施済み、
   `_build` 削除済み
7. `ca.crt`・`Dockerfile.ca` が作業ツリーに残っていない
8. **`9aa06d7` に続く1コミットとしてプッシュ済み。`--amend` と force push は行わない**
   （`#35` が2コミットに分かれることは、これで確定とする。申し送り不要）

---

## 補足（user の申し送り予告）

`:883` の Excel 側括弧書きの誤り（「先頭セルが空の行」は `isBlankLine` で読み飛ばされる。
正しい記法は `:1363`/`:1367`）は、今回も含めていない。`#35` 着地後に §3 の申し送りとまとめて
1タスクとして提案する。
