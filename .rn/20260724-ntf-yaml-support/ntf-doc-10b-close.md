# 作業指示: `#10b` の最終是正と締め

配置先: `.rn/20260724-ntf-yaml-support/ntf-doc-10b-close.md`

対象ブランチ: `lovaizu/nablarch-document` の `work`（`c1dd2fc` の続き。本文の最終コミットは `f87629f`）

**`#10b` の公開本文は承認である。** 独立検証で `must` 0件を確認した。本書は締めにあたっての是正1件と申し送り1件であり、それ以外の変更を行わない。

判断を仰がれた2件の結論は次のとおり。

- **数詞の全件表 — 4ラウンド目は実施しない。** 表は既に全件である（根拠は STEP 2）。方式の弱点は申し送りとして残り28ページへ渡す
- **`:401` の `認証エラーケース`** — CC の指摘が正しい。セルの実値に揃える（STEP 1）

---

## STEP 1 — `testdata_examples.rst:401` の `認証エラーケース` をセルの実値に揃える

### 事実（`f87629f` で実測）

| 対象 | 実測値 |
|---|---|
| `testdata_examples.rst:424` | `    - 正常ケース`（Excel 表の `description` 列） |
| `testdata_examples.rst:430` | `    - 認証エラー`（同上） |
| `testdata_examples.rst:478` | `          description: "正常ケース"`（YAML） |
| `testdata_examples.rst:484` | `          description: "認証エラー"`（YAML） |
| 出典 `input/ntf-testdata-doc-examples-testshots.md:102` | `| 2 | 認証エラー | false | 400 | /error | context002 |` |
| 出典 同 `:122` | `        description: "認証エラー"` |
| マッピング | `input-0103`（`src_body_start` 95 / `src_body_end` 137） |

`認証エラーケース` という文字列は、`ja/` 配下・`input/` 配下・`mapping/` 配下のいずれにも存在しない。地の文だけが持つ語である。

### 実施すること

`:401` の `認証エラーケース` を `認証エラー` に改める。

- 変更前 `正常ケースと認証エラーケースの2つのテストショットを実行する例である。`
- 変更後 `正常ケースと認証エラーの2つのテストショットを実行する例である。`

**`2つのテストショット` は変更しない。** `#10b` STEP 1 の是正で確定した形である。文の後半（ `context`\ カラムには… ）も変更しない。

### 前回の指示の訂正

`ntf-doc-10a-followup.md` の禁止事項に「`正常ケース`・`認証エラーケース` という語を落とさない。表に実在するセルの値である」と書いたが、`認証エラーケース` について事実に反していた。**指示側の誤りである。** 本書がこれを取り消す。

---

## STEP 2 — 数詞の全件表は作り直さない

### 判断の根拠（レビュー役が独立に実測した結果）

助数詞ホワイトリストを使わない全走査を独立に組み、`testdata_examples.rst` の地の文および散文ディレクティブ本体を対象に、`数字・漢数字・複数` を含む行を全件抽出した（インラインリテラル `` `` `` 内は除去）。

**結果: 既存の35行の表が取りこぼしている真の数詞は0件である。**

3巡目に3観点が挙げた9出現は、実物を確認すると次のとおりで、いずれも数を数えていない。

| 指摘箇所 | 実体 | 数詞か |
|---|---|---|
| `:1287` | ゾーン10進数（数値表現形式の名称） | 否 |
| `:1978` | 16進数（基数の名称） | 否 |
| `:1569` | MS932（文字エンコーディング名） | 否 |
| `:1756` | 両方（数詞ではない） | 否 |
| `:953`・`:1811`・`:1859` | 本文中のリテラル値（`1101`・`"200"`・`.0`） | 否 |
| `:22`・`:1209` | 行は既に表にある（`複数` の出現が数詞欄に個別記載されていないだけ） | 該当行は収録済み |

すなわち未達なのは「表が全件でないこと」ではなく「同じ表が全件であることを、その抽出方式では証明できないこと」である。公開本文への影響は無い。

### 実施すること

**何もしない。** 数詞の全件表（`checks/task-10a-followup.md` §1-2）を作り直さない。行の追加も削除も行わない。

`checks/task-10a-followup.md` の §1-2 の冒頭に、次の趣旨を1段落だけ追記する。

- 抽出方式はホワイトリスト方式であり、母集合の網羅性を方式自身では証明できない
- レビュー役がホワイトリストを使わない全走査で独立に検証し、真の数詞の取りこぼしが0件であることを確認した（上表の9出現はいずれも数を数えていない）
- 表の作り直しは行わない。方式の是正は `#11` 以降の申し送りとする

既存の表・既存の記述は書き換えない。追記のみとする。

---

## STEP 3 — 申し送りの追加

`reviews/page-testdata_examples.md` と `reviews/page-testdata_notation.md` の双方に、次を申し送りとして追記する。既存の記録は書き換えない。

> **完了条件が「全件表」を求める項目は、母集合をホワイトリストで切り出さない。** 対象を全走査したうえで、非該当と判定したものも判定理由を添えて表に載せる。`#10b` STEP 1 の数詞表はホワイトリスト方式を採ったため、3巡のレビューすべてで「リストに無い語」の指摘を受け、そのたびに表へ追加する対応を繰り返した（追加された語はいずれも数を数えておらず、本文の是正は一度も発生していない）。方式が網羅性を証明できない限り、同じ指摘は何巡でも再発する。

`#10a` の申し送りと合わせて、`#11` の作業指示に反映する。

---

## STEP 4 — 記録と締め

- `checks/task-10a-followup.md` に STEP 1 の是正と STEP 2 の判断を追記する（新規ファイルを作らない）
- `reviews/page-testdata_examples.md` に STEP 1 の是正を追記する。`:401` の語の変遷（`2件` → `2つのテストショット` → `認証エラーケース` の削除）と、セルの実値が `認証エラー` であるという実測を根拠として残す
- `steering.md` の `#10b` を DONE として締める。**`#10a` の Steps を圧縮したのと同じ形にしない。** チェックリストは残したまま `Closed` を追記する

---

## ゲート

すべて実行結果で確認し、`checks/task-10a-followup.md` に記録すること。

1. `python3 mapping/tools/verify_mapping.py` が exit 0、594行 / 12,986 / 11,983 が不変
2. `git diff f87629f HEAD -- .rn/20260724-ntf-yaml-support/mapping/ ja/conf.py .rn/20260724-ntf-yaml-support/design.md` が空
3. 見出し数・文言・並び順が不変（`testdata_notation.rst` は `-` 3 / `~` 10 / `^` 26、`testdata_examples.rst` は L2 9 / L3 28 / L4 56）
4. `ja/development_tools/testing_framework/` 配下の全 `.rst` に `テストケース` が0件（不変）
5. `ja/` 配下に `認証エラーケース` が0件。`testdata_examples.rst` の `認証エラー` は `:401`・`:430`・`:484` の3箇所
6. 両ページに `（番号・説明・期待するステータスコード）` が0件（不変）
7. 記載例ページの特殊記法セクションに `:ref:` が1件以上あり、`testdata_notation-special_notation` を指していること（不変）
8. `:ref:` の未定義参照が0件、段落内改行が0件
9. Docker でフルビルド（`-a`）し、`build succeeded` かつ警告が既知の `db_double_submit.rst` 1件のみ（新規0件）
10. `f87629f` との差分が、`testdata_examples.rst` の1行（`:401`）と記録ファイル（`checks/`・`reviews/`・`steering.md`）のみであること

---

## 禁止事項

- 数詞の全件表を作り直さない。行を追加も削除もしない
- `:401` 以外の導入文を変更しない。`:198`・`:502` を含む
- `:401` の `2つのテストショット` と後半の文を変更しない
- 見出しの文言・並び順を変更しない
- `mapping.csv` / `_batch/` / `vocabulary.md` / `style.md` / `glossary.md` / `design.md` / `ja/conf.py` を変更しない
- 既存のレビュー記録・チェック記録を書き換えない。追記のみとする
- 承認済みの `#8`・`#9`・`#10`・`#10a` および `#10b` の成果を、本書が指示していない範囲で変更しない
- **4観点のレビューを回さない。** 本書の変更は1語の削除であり、ゲート10 が差分を1行に固定している
- **user review** の承認を受けるまで `#11` に着手しない
