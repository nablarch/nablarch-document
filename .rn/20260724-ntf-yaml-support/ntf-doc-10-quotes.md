# 作業指示: `#10` セル値の描画忠実性と規約のスコープ

配置先: `.rn/20260724-ntf-yaml-support/ntf-doc-10-quotes.md`

対象ブランチ: `lovaizu/nablarch-document` の `work`（`38ad208` の続き）

`#10` は user review で**差し戻し**とする。指摘は `must` 1件と、確認1件・規約追記1件である。

判断を仰がれた3件の結論は次のとおり。

- **`#9` の対象外セル格子3件（現状 L524・L1447・L1478 付近）** — 現状維持で承認。ただし STEP 3 で規約に条件を明記する
- **規約4 の参照整合** — 承認。取り消さない
- **smartquotes によるダブルクォートの描画** — **対象外ではなく `must`。** STEP 1 で是正する

セル格子の是正（`#10` 47表・`#9` 2表）と規約2 の差し替えは承認済みである。取り消さない。

---

## STEP 1 — ダブルクォートの描画を是正する（`must`）

### 事実

`ja/conf.py` に `smartquotes` の設定が無いため、Sphinx の既定値（有効）が働く。**リテラル記法（`` `` ``）の外にある素のダブルクォートは、丸クォート（`”`）に変換されて描画される。**

このページの約束は「表をそのまま写せば動くテストデータになる」ことである。丸クォートを写すと `quoting-delimiter` に別の文字が入り、テストが動かない。

### 対象

`testdata_examples.rst` の4セルである。いずれもセル格子の中にあり、リテラル記法の外にある。

| 現状の行 | セルの値 | 文脈 |
|---|---|---|
| L1432 | `"` | `quoting-delimiter` ディレクティブの設定値 |
| L1445 | `"hello, world"` | `NOTE` カラムのデータ |
| L1939 | `" "` | `NAME` カラムのデータ（半角スペース1文字） |
| L1940 | `"""` | `MEMO` カラムのデータ（ダブルクォート1文字） |

### 実施すること

1. **上記4セルの値をリテラル記法（`` `` ``）で囲む。** smartquotes はリテラル記法の中を変換しない
2. **値そのものは変えない。** 囲みを足すだけである。`"""` を `""` や `"` に書き換えない
3. `ja/conf.py` は**変更しない。** Nablarch 解説書全体に影響するため、本タスクの範囲を超える

### 横並び確認

**両ページ（`testdata_examples.rst`・`testdata_notation.rst`）の全 `list-table` について、リテラル記法の外にある素のダブルクォート・シングルクォートを全数走査する。** 上記4件以外に該当が無いことを確認し、あれば同様に是正すること。

`testdata_notation.rst` の模式表にある `...`（現状 L992・L996・L1000・L1004・L1008 付近）は、**対象外とする。** 省略記号であり、写して動くことを意図した値ではない。三点リーダに変換されても実害がない。

確認結果を `checks/task-10-quotes.md`（新規）に記録すること。

---

## STEP 2 — 行番号を指す表現を全数確認する

識別子行を表に追加したことで行番号がずれた箇所がある。副次修正として2件（「前半・後半」→「複数に」、空エントリの行番号非依存化）を対応済みだが、**全数確認は未了である。**

### 実施すること

両ページの `N行目` `先頭行` `最終行` `N列目` といった位置を指す表現を**全数抽出し、1件ずつ判定する。** 判定の観点は「その表現が指しているのは Excel シートの行か、RST の表の行か」である。

- **Excel シートの行を指す場合** — 識別子行を1行目と数えているかを確認する。シート上では識別子行が1行目であるため、`ヘッダ行（2行目）`・`データ行（3行目以降）` は正しい
- **RST の表の行を指す場合** — 識別子行を追加した表では行番号が1つずれている。是正する

判定結果を**全件、表として** `checks/task-10-quotes.md` に記録すること。件数だけの報告にしない。

**特に確認すべき箇所**（他にもありうるため、これで打ち切らない）:

- `testdata_notation.rst` 現状 L466 `ヘッダ行（2行目）`、L479 `データ行（3行目以降）`、L561 `1行目に LIST_MAP= …2行目を Map のキー、3行目以降を Map の値`
- `testdata_notation.rst` 現状 L1451 `例えば以下のように、2行目でテーブルの論理名を` — 直後の表は識別子行を持たない断片であり、印刷された表の行番号としては整合している。**シートの行と誤読されないかを確認し、紛らわしい場合は行番号を使わない表現に改める**
- `testdata_examples.rst` 現状 L137 `データブロックの用途は、先頭行の識別子で決まる`

---

## STEP 3 — `style.md` S-10 規約2 にスコープ条件を追記する

`#9` には識別子行を持たないセル格子が3件残る（現状 L524・L1447・L1478 付近）。**現状維持で正しい。** いずれも複数のデータタイプに共通する規則を示す断片であり、識別子を1つ選ぶと、その機能が特定のデータタイプ専用であるかのように読めてしまうためである。

- L524 付近 — `getListMap` の戻り値との等価性を示す
- L1447 付近 — `//` によるコメントの記法
- L1478 付近 — マーカーカラムの記法

**ただし、同じページに識別子行を持つ格子と持たない格子が混在する状態になる。** 残り32ページで同じ判断が繰り返されるため、規約に条件を書いておく。

### 実施すること

`style.md` S-10 規約2 に、次の趣旨を1〜2行で追記する。

- 複数のデータタイプに共通する規則を示すための断片は、識別子行を持たなくてよい
- 特定のデータブロックの記述例を示す表は、識別子行を含める

**`style.md` の観点は11個から増やさない。規約2 以外の規約を変更しない。**

---

## STEP 4 — 記録

1. `checks/task-10-quotes.md` を新規作成し、STEP 1〜3 の実施結果とゲートの実行出力を記録する
2. `reviews/page-testdata_examples.md` に追記する。既存の記録は書き換えない
3. `reviews/page-testdata_examples.md` の残り32ページへの申し送りに、次を追加する
   - セル格子に素のダブルクォート・シングルクォートを含む値を書く場合は、リテラル記法で囲む（`ja/conf.py` の smartquotes が既定で有効なため）

---

## ゲート

すべて実行結果で確認し、`checks/task-10-quotes.md` に記録すること。

1. `python3 mapping/tools/verify_mapping.py` が `exit 0`、**594行 / 12,986 / 11,983 が不変**
2. `git diff 38ad208 HEAD -- .rn/20260724-ntf-yaml-support/mapping/mapping.csv .rn/20260724-ntf-yaml-support/mapping/_batch/ .rn/20260724-ntf-yaml-support/mapping/vocabulary.md .rn/20260724-ntf-yaml-support/mapping/glossary.md` が**空**
3. `ja/conf.py` に差分が**無い**こと
4. 両ページの `list-table` のセルのうち、**リテラル記法の外に素のダブルクォート・シングルクォートを含むものが0件**であること（走査結果を貼る）
5. 見出し数が不変（`testdata_examples.rst` は L2 9 / L3 28 / L4 56、`testdata_notation.rst` は `-` 3 / `~` 10 / `^` 26）
6. `:header-rows: 0` の表の件数が不変（`testdata_examples.rst` 47件、`testdata_notation.rst` 2件）
7. `style.md` の観点が**11個**で不変
8. 行番号を指す表現の全件判定表が記録されていること
9. `:ref:` の未定義参照が両ページとも0件、段落内改行が0件
10. Docker でフルビルド（`-a`）し、`build succeeded` かつ警告が**既知の `db_double_submit.rst` 1件のみ**（新規0件）
11. **描画結果を確認する。** `_build/html/development_tools/testing_framework/implementation/testdata_examples.html` の該当4箇所を実際に読み、丸クォート（`”` `“`）ではなく直線のダブルクォート（`"`）で出力されていることを確認し、該当部分を引用して記録する
12. 是正の前後で情報が失われていないことを `git diff` で示す。削除行が STEP 1〜3 に由来するものだけであること

---

## 禁止事項

- **`ja/conf.py` を変更しない。** smartquotes の設定変更は Nablarch 解説書全体に及ぶ
- **`mapping.csv` / `_batch/` / `vocabulary.md` / `glossary.md` を変更しない**
- 4セルの**値そのものを変えない。** リテラル記法で囲むだけである
- `#9` の識別子行を持たない3件のセル格子（現状 L524・L1447・L1478 付近）に識別子行を足さない
- `testdata_notation.rst` の模式表の `...` に手を入れない
- 承認済みの作業を取り消さない（セル格子の是正47件＋2件、規約2 の差し替え、規約4 の参照整合、副次修正2件、`#9` への是正3件）
- `style.md` の観点を11個から増やさない。規約2 以外を変更しない
- 判定を件数だけで報告しない。STEP 1 の走査結果と STEP 2 の判定は全件を表で記録する
- 既存のレビュー記録を書き換えない。追記のみとする
- user review の承認を受けるまで `#11` に着手しない
