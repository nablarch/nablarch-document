# 作業指示: `#6` 未確定事項の確定と `design.md` 更新

対象ブランチ: `lovaizu/nablarch-document` の `work`（`888daf2`）

前提: `checks/task-06.md` に提示された判断材料に対し、ユーザーの判断が下りた。本書はその判断内容と実施手順を示す。

---

# 確定した判断

| 論点 | 判断 |
|---|---|
| ① 稼動環境0件 | **案A**。`current-0180` / `current-0267` を第1部「稼動環境」へ移す。「Java・Jakarta EE の要件」は出典がないため `design.md` §2 の表から削除 |
| ② テストデータの形式0件 | **廃止**。`design.md` §3 のツリーから削除。比較に相当する内容は既に第3部にあるため移動作業は発生しない。「使い分け」は出典がないため書かない |
| ③ ページ分割・取引単体テストの構成 | 提示どおり。リクエスト単体テストの設定は6処理方式。取引単体テストの設定は実データのある3処理方式（RESTfulウェブサービス / HTTPメッセージング / MOMによるメッセージング）のみ |
| 未処理1（第2部 機能概要0件10 / 拡張例0件8） | **b案**。`design.md` §3 のページアウトラインを改訂し、第2部では `機能概要`・`拡張例` を必須としない |
| 未処理2（第3部 取引単体テストの小規模ページ） | **統合しない**。5ページとも独立ページとして残す |
| 分類6（第3部 テストデータ2ページの機能概要） | **a案**。既存の候補出典を `機能概要` に割り当てる |
| 分類7（`current-0128` の分割） | **a案**。新規 `SPLIT` として冒頭2行を `機能概要`、残りを `使用方法` に分ける |
| 分類8（テストデータ変換ツール > 導入） | 出典がないため `導入` を設けない。`design.md` §5 に例外として明文化する |
| ④ ファイル名・ディレクトリ構成 | `checks/task-06.md` のツリーと1対1対応表を採用。ただし下記「STEP 6」の2点を反映する |

**この判断により `PENDING_ZERO` 26件はすべて解消する**（分類1〜8の内訳と一致）。`#6` 完了時に0件になることが完了条件である。

---

# 追加の規約: セクションタイトルの要件

`#8〜` のページ作成に効く規約であり、`#6` で `design.md` と `style.md` に反映する。

## 規約

**ページタイトルとセクションタイトルの組で一意であり、かつその組だけで中身が分かること。**

理由: 利用者は見出しを見て目的の情報に辿り着く。加えて Nabledge がセクションタイトルであたりを付けるため、タイトルが検索面そのものになる。

- セクションタイトル単独での文書全体一意は**不要**。ページタイトルが処理方式名等を持つため、組で一意になる（`vocabulary.md` の38ページはすべて一意であることを確認済み）
- 既存の `design.md` §3・§4「セクションタイトルは『〜する』形式とする」は維持する。本規約はそれに**内容の条件を加える**もの
- 「〜する」形式でも、`概要` / `補足` / `注意事項` / `その他` / `準備する` / `設定する` のようにページタイトルを足しても情報が増えない語は不可
- 同一ページ内でセクションタイトルを重複させない
- 出典の見出し（`目的別API使用方法` のような作成側の都合による名前）をそのまま使わない

## 反映先

1. `mapping/style.md` — セクションタイトルの規約として上記を追記する
2. `design.md` §8 — ページ作成時の遵守事項に「セクションタイトルは `style.md` の規約に従う」を追記する
3. `design.md` §11 観点B（トンマナ）のチェック内容に「ページタイトル＋セクションタイトルの組で中身が分かるか」を追記する

---

# 実施手順

`design.md` → `mapping.csv` → 検査 → 記録 の順で行う。`design.md` が唯一の正であるため、先に `design.md` を確定させてから `mapping.csv` を合わせる。

## STEP 1: `design.md` を更新する

| 節 | 変更内容 |
|---|---|
| §2 | 第1部の表から「Java・Jakarta EE の要件」を削除する。「モジュール一覧の集約」の記述は維持する（①で `mapping.csv` 側が追随するため） |
| §3 | ツリーから「テストデータの形式」を削除する。取引単体テストの設定をウェブアプリケーション / Nablarchバッチアプリケーション / テーブルをキューとして使ったメッセージング を除く3ページにする。ページアウトラインを改訂し、`機能概要`・`拡張例` を第2部では任意とする（`使用方法` は必須のまま） |
| §4 | 第3部 取引単体テストは6ページ（実データ5＋テーブルキューの導線1）を維持することを明記する。「テストデータの2ページ」に、一般の第3部ページアウトライン（`機能概要`＋`使用方法`）を適用することを明記する |
| §5 | `テストデータ変換ツール` が `導入` を持たないことを、`HTMLチェックツール` と並べて明記する。理由（`testdata-converter-design.md` 全362行にインストール手順・依存関係・設定の記述がない）を添える |
| §6 | テーブルをキューとして使ったメッセージングの扱いを、**ページ単位で**書き分ける。`リクエスト単体テストの設定` / `リクエスト単体テスト` / `取引単体テスト` の3ページは「導線のみ」で維持し、`取引単体テストの設定` はページを設けない（③により取引単体テストの設定が3処理方式のみになるため）。現状の記述はこの区別をしていないので、必ず直す |
| §8 | セクションタイトルの規約への参照を追記（上記「追加の規約」参照） |
| §11 | 観点Bのチェック内容を追記（同上） |
| §12 | 未確定事項#1・#2・#3 の3件すべてを「確定済み」に更新し、確定内容と判断日、根拠として `checks/task-06.md` を記す。#3 の確定時期の記述（「マッピング作成時に決定」）も実態（`#6` で確定）に直す |
| ファイル構成 | `checks/task-06.md` のツリーと1対1対応表を `design.md` に取り込む。未確定事項#3 の確定内容として節を設ける |

このSTEPだけで commit する。

## STEP 2: `mapping.csv` を更新する

`_batch/*.csv` を編集し、`mapping.csv` は全30バッチの単純連結で再生成する。**`mapping.csv` を直接編集しない。**

### 2-1. ①の移動（2行）

| mapping_id | 変更 |
|---|---|
| `current-0180` | `dest_part` → `第1部 テスティングフレームワークとは`、`dest_page` → `テスティングフレームワークとは`、`dest_section` → `稼動環境` |
| `current-0267` | 同上 |

`note` に判断根拠（`design.md` §2「モジュール一覧の集約」に従う旨、`#6` で確定）を追記する。

### 2-2. 分類6の割当（第3部 テストデータ2ページの機能概要）

候補出典は `checks/task-06.md` 分類6 の表にある `input-0098` / `input-0099` / `input-0114`（テストデータの書き方）と `input-0036` / `input-0037` / `input-0058` / `input-0082` / `input-0093`（テストデータの記載例）。

**候補を機械的に全件移さない。** 各行の出典を実ファイルで通読し、`design.md` §4 の `機能概要`「このページで何ができるようになるか」に該当するものだけを `使用方法` → `機能概要` に変更する。移した行・移さなかった行の両方を根拠 file:line 付きで `checks/task-06.md` に記録する（`#5b` STEP 2 と同じ粒度）。

**注意**: `input-0114` には `#5d` で `[セクション境界]` note を付与済みである。`dest_section` を変更する場合、note 内の「後続の本体行（input-0116）は dest_section=テストデータに置かれている」という記述と矛盾しないか確認し、必要なら書き換える。

### 2-3. 分類7の SPLIT（`current-0128`）

- `current-0128`（`batch.rst` 4-25、22行）を2分割する
- 冒頭の概要的記述を `機能概要`、残りを `使用方法` にする。**分割位置は実ファイルを読んで決める**（`checks/task-06.md` の「冒頭2行」は目安であり、実際の文の切れ目を確認すること）
- `mapping_id` は `current-0128-a` / `current-0128-b` とする（`#5` の `input-0016-a/b`、`#5c` の `input-0198-a/b/c` と同じ規則）
- 行範囲の和集合が元セクション（4-25）と隙間・重複なく一致すること
- **`split-plan.md` に追記する**（`#5c` 差し戻し対応で追加した「`#5c`差し戻し対応での追加分割」節と同様に、`#6` 分の節を設ける）

### 2-4. 暫定表記の解消

- `note` が「暫定」で始まる**27行**すべてについて、`#6` の確定内容を反映して「暫定。」を外し、確定した割当理由に書き換える
- `HTMLチェックツール` の8行（`current-0367`〜`current-0375`）は第4部新設で既に解消しており、文言更新のみで済む（`#5d` の暫定一覧に記載のとおり）
- `mapping/vocabulary.md` の `dest_page` を「確定9件 / 暫定29件」から**全38件を確定**（②③で廃止する4ページを除く34件）に更新する。廃止したページは語彙から削除する

### 2-5. 廃止ページの語彙からの削除

②③で廃止する4ページを `vocabulary.md` から削除する。

- 第2部 テストデータの形式
- 第2部 取引単体テストの設定（ウェブアプリケーション）
- 第2部 取引単体テストの設定（Nablarchバッチアプリケーション）
- 第2部 取引単体テストの設定（テーブルをキューとして使ったメッセージング）

これら4ページには非DROP行が1件も無いため、`mapping.csv` の行の付け替えは発生しない。発生する場合は判断誤りなので、進めずに報告すること。

## STEP 3: `verify_mapping.py` を更新する

1. `SECTION_TEMPLATE["第2部 導入と設定"]` を `["使用方法"]` に変更する（未処理1 b案）
2. **ただし `機能概要`・`拡張例` の0件検出をやめない。** 必須から外したうえで、`reference-only sections` と同じ **advisory 出力**として一覧を出し続ける。任意化によって将来の割当漏れが見えなくなるのを防ぐため
3. `EXPECTED_ZERO_SECTIONS` に `("第4部 ツール", "テストデータ変換ツール", "導入")` を追加する。理由には `design.md` §5 の該当記述を引用する
4. `EXPECTED_ZERO_PAGES` から、廃止した4ページのうち登録済みのもの（テーブルキューの取引単体テストの設定）を削除する。語彙から消えるため検査対象外になる
5. **`PENDING_ZERO` を空にする。** 26件すべてが①②③・未処理1・分類6〜8で解消されるため、1件でも残る場合は解消漏れである

## STEP 4: 検査を通す

```bash
python3 mapping/tools/verify_mapping.py; echo "EXIT: $?"
```

- `EXIT: 0`
- `PENDING_ZERO` **0件**
- `stale allowlist` 0件
- `check_intro_section_split` の ERROR 0件
- `lines` 合計（全行）**12,986 が不変**
- 行数は 593 + 分類7の分割1行 = **594**（分類6は `dest_section` の変更のみで行数は増えない）
- DROP除く合計は 11,983 のまま（①は部内移動、分類6・7は `dest_section` 変更と分割のみ）

`volume.md` を再生成し、独立再計算と一致することを確認する。廃止した4ページは表から削除する。

## STEP 5: `checks/task-06-proposal.md` を削除する

`checks/task-06.md` が完全に置き換えているため削除する。`checks/task-06.md` 内から `task-06-proposal.md` を参照している箇所は、`#6` の初回提示を指す記述に書き換える。削除したファイルへの参照を残さない。

```bash
git rm .rn/20260724-ntf-yaml-support/checks/task-06-proposal.md
```

## STEP 6: ファイル名の2点を確定する

`checks/task-06.md` のツリーを採用するが、次の2点を反映してから `design.md` に取り込む。

### 6-1. `implementation/testdata_format.rst` の改名

第3部「テストデータの書き方」に `testdata_format.rst` を当てているが、②で廃止する第2部のページ名が「テストデータの**形式**」であり、英語名が同じ `format` になる。廃止後も `git` 履歴や `checks/` に「テストデータの形式」が残るため、混同の余地を消す。

`implementation/testdata_notation.rst` に改める（`design.md` §4 が「記法」という語をこのページの役割説明に使っているため）。対応表も更新すること。

### 6-2. `setup/deal_unit_test/db_queue.rst` の不在の明記

ツリーでは `setup/deal_unit_test/` に `db_queue.rst` を作らない一方、`setup/request_unit_test/db_queue.rst` と `implementation/*/db_queue.rst` は作る。この非対称は③（取引単体テストの設定は3処理方式のみ）によるもので正しいが、`design.md` §6 の現在の記述はこの区別をしていない。STEP 1 の §6 改訂で必ず書き分けること。

## STEP 7: self-check と commit

- `checks/task-06.md` に、STEP 1〜6 の実施結果を追記する（判断の記録ではなく**実施の記録**として。実行コマンドと出力をそのまま貼る）
- `steering.md` の `#6` のチェックボックスと State 欄を更新する
- commit & push（STEP 1 と STEP 2〜7 で最低2コミットに分ける）
- **user review** — 別セッションの Claude が push 済みブランチを独立検証する。CC は **commit SHA** と実行コマンド出力を報告して `/rn:dn` で中断し、承認まで `#7` に進まない

---

# `#6` の Completion criteria

- `design.md` の §2 / §3 / §4 / §5 / §6 / §8 / §11 / §12 が上表のとおり更新されている
- `design.md` に ④ のファイル構成（STEP 6 の2点を反映したもの）が節として取り込まれている
- `mapping/style.md` にセクションタイトルの規約が追記されている
- `vocabulary.md` に暫定表記が残っていない。廃止4ページが削除され、全ページが確定
- `mapping.csv` に `note` が「暫定」で始まる行が **0件**
- `verify_mapping.py` の `PENDING_ZERO` が **0件**、`EXIT: 0`、`stale allowlist` 0件
- 594行 / `lines` 全行 12,986 / DROP除く 11,983
- `mapping.csv` が `_batch/*.csv` の単純連結と一致
- `split-plan.md` に `current-0128` の分割が記録されている
- `checks/task-06-proposal.md` が削除され、参照も残っていない
- 分類6で移した行・移さなかった行の両方が根拠 file:line 付きで記録されている

---

# 禁止事項

- `mapping.csv` を直接編集しない（`_batch/*.csv` を編集して再生成）
- 分類6の候補行を機械的に一括移動しない。実ファイル通読で1件ずつ判定する
- 分類7の分割位置を「冒頭2行」と決め打ちしない。実ファイルを読んで文の切れ目で分ける
- 出典のない内容を書かない。②の「使い分け」、①の「Java・Jakarta EE の要件」は書かない
- 検査を advisory に落として件数を減らす方向で「解消」しない。`PENDING_ZERO` は判断で0にする
- 数値が本書の記載と違う場合、先に原因を突き止めてから進む
- コンテキストが70%に近づいたら報告して `/rn:dn` で中断する
