# 作業指示: `#14` の締め — デフォルト値の制約に実装の根拠を書き足す

対象ブランチ: `lovaizu/nablarch-document` の `work`（`104a6c4` の続き）

`#14` の**公開本文は承認である。** 独立検証で `must` 0件を確認した。本書は締めにあたっての追記2件と申し送り1件であり、それ以外の変更を行わない。

**4観点のレビューは回さない。** 変更は表の2セルへの追記だけである。

---

## 背景

`setup/class_unit_test.rst` の「省略したテーブルのカラムのデフォルト値を変更する」の表は、`charValue` の指定できる値を「1文字のASCII文字」、`numberValue` を「0または正の整数」としている。これは出典 `current-0191`（`02_DbAccessTest.rst:457,459`）どおりであり、**記述自体は正しい。**

ただし**なぜその範囲に限るのかが書かれていない。** 理由は実装の挙動にあり、それを知らない読者はこの制約を守る意味が分からない。レビューのラウンド1（`R1-X4`）では「実装が検査していないから緩める」という指摘と「推奨値として維持する」という判断が争点になったが、**どちらも実装が何をするかを示していない点で不十分である。**

## 実装で確認済みの事実

レビュー役が `nablarch/nablarch-testing` の `main`（コミット `e21bf67`）を clone して確認した。

| 事項 | 実装 | `file:line` |
|---|---|---|
| 固定長文字列型（`CHAR`・`NCHAR`）のデフォルト値 | `charValue` を**カラム長の数だけ繰り返した文字列** | `src/main/java/nablarch/test/core/db/BasicDefaultValues.java:158-159`（`StringUtil.repeat(charValue, length)`） |
| 可変長文字列型（`VARCHAR`・`NVARCHAR`）・`CLOB` | `charValue` をそのまま返す | 同 `:147`・`:182` |
| 数値型のデフォルト値 | `numberValue` を**カラム長で先頭から切り詰めた文字列** | 同 `:171-172` → `src/main/java/nablarch/test/NablarchTestUtils.java:290-300`（`substring(0, threshold)`） |
| `charValue` の検査 | 1文字でなければ `IllegalArgumentException`。**ASCII かどうかは検査しない** | `BasicDefaultValues.java:102-108` |
| `numberValue` の検査 | **無し** | 同 `:125-127` |

これにより、出典の制約には次の実質的な理由があることが確認できる。

- **ASCII を勧める理由** — 固定長文字列型ではカラム長の数だけ繰り返されるため、マルチバイト文字を指定するとバイト長で定義されたカラムを超えるおそれがある
- **0または正の整数に限る理由** — 数値型ではカラム長で先頭から切り詰められるため、符号や小数点を含む値は切り詰めによって数値として成立しなくなる（例: 桁数1のカラムに `-5` を指定すると `-` になる）

---

## STEP 1 — `charValue` の説明に固定長時の挙動を書き足す

現在の説明セルは「文字列型のデフォルト値」である。**固定長文字列型ではカラム長の数だけ繰り返した値が使われる**旨を書き足す。

「指定できる値」列の `1文字のASCII文字` は**変更しない。**

## STEP 2 — `numberValue` の説明に切り詰めの挙動を書き足す

現在の説明セルは「数値型のデフォルト値」である。**カラム長を超える場合は先頭から切り詰められる**旨を書き足す。

「指定できる値」列の `0または正の整数` は**変更しない。**

**`dateValue` は変更しない。** `Timestamp.valueOf` に渡されるだけで（`BasicDefaultValues.java:116-118`）、繰り返しも切り詰めも起きない。

---

## STEP 3 — 申し送りを追加する

`reviews/page-class_unit_test.md` に追記する。既存の記録は書き換えない。

> **出典が「〜を指定する」と制約を述べている箇所は、実装がその制約を検査しているかで採否を決めない。** 検査していなくても、その制約に実質的な理由（値の加工・変換・切り詰めなどの挙動）があることが多い。**実装で挙動を確かめ、制約は残したうえで理由を書き添える。** 制約だけを書くと読者が守る意味を判断できず、実装が検査しないことを理由に緩めると、実際には壊れる値を許容してしまう。`#14` の `charValue`（固定長時にカラム長分繰り返される）・`numberValue`（カラム長で切り詰められる）がこの型である。

## STEP 4 — 記録

`checks/task-14.md` に、STEP 1・2 の追記と実装の `file:line` を記録する。**新規ファイルを作らない。**

---

## ゲート

すべて実行結果で確認し、`checks/task-14.md` に記録すること。

1. `git diff 104a6c4 HEAD -- ja/` の変更が `setup/class_unit_test.rst` の**説明セル2行だけ**であること。削除行は0行
2. `setup/class_unit_test.rst` に `1文字のASCII文字` と `0または正の整数` が**それぞれ1件ずつ残っている**こと
3. `python3 mapping/tools/verify_mapping.py` が `exit 0`、**594行 / 12,986 / 11,983 が不変**
4. `git diff 104a6c4 HEAD -- .rn/20260724-ntf-yaml-support/mapping/ ja/conf.py .rn/20260724-ntf-yaml-support/design.md .rn/20260724-ntf-yaml-support/mapping/style.md` が**空**
5. 見出しの文言・並び順が不変。`:ref:` の未定義参照0件、段落内改行0件
6. Docker でフルビルド（`-a`）し、`build succeeded` かつ警告が**既知の `db_double_submit.rst` 1件のみ**（新規0件）

## 禁止事項

- 表の「指定できる値」列を変更しない。`1文字のASCII文字`・`0または正の整数` を残す
- `dateValue` の行を変更しない
- 表の列構成・見出し・他のセクションを変更しない
- `mapping.csv` / `_batch/` / `vocabulary.md` / `style.md` / `glossary.md` / `design.md` / `ja/conf.py` を変更しない
- 承認済みの他ページを変更しない
- 4観点のレビューを回さない
- 既存のレビュー記録・チェック記録を書き換えない。**追記のみとする**
- user review の承認を受けるまで `#15` に着手しない
