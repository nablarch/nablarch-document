# 個別指示 — テーブルをキューとして使ったメッセージングの3ページ（`#27-16`〜`#27-18`）

宛先: CC ／ 親指示: `ntf-doc-weekend-queue.md`

## 0. なぜ個別指示があるか

**この3ページは出典が0行である。** `mapping.csv` を `dest_page` で絞ると1行も返らない。他のページと同じ手順（出典を読んで書く）が使えないため、書く内容をこの指示で与える。

**0行は欠落ではなく設計である。** `checks/task-06.md:569`・`:590`・`:356` がいずれも `EXPECTED_ZERO（design.md §6「導線のみ」）` と記録している。出典を探しに行かないこと。

## 1. 書く内容は `design.md:384` が決めている

> **「導線のみ」で維持する3ページ**: `リクエスト単体テストの設定` / `リクエスト単体テスト` / `取引単体テスト`（いずれもテーブルをキューとして使ったメッセージング）。**章として立てる。** 読者は章見出しを探して辿り着くため、独立した見出しを設ける。**中身は導線のみとする。** 「Nablarchバッチアプリケーションと同じ方法でテストする」旨と、Nablarchバッチアプリケーションの章への `:ref:` 参照だけを記載する。コード例・テストデータ例は記載しない。

**この規定のとおりに書く。足さない。** 具体的には次を守る。

- **コード例・XML例・テストデータ例を書かない。** 1つも置かない。
- **設定項目表を置かない。**
- **`.. contents::` の目次を置くかどうかは、節が1つも無いなら置かない。** 目次だけが並ぶページになる。
- **`機能概要`・`使用方法` の見出しも置かない。** 導線1文のページに2階層の見出しは合わない。リード文だけで完結させる。

## 2. 3ページの仕様

ページ先頭ラベルは `style.md` S-08 の一覧から引く。**新たに考案しない。** 下表の値は S-08 の実測値である（`style.md:363`・`:379`・`:385`）。

| # | ファイル | ラベル | ページタイトル | `:ref:` の飛び先 |
|---|---|---|---|---|
| 16 | `setup/request_unit_test/db_queue.rst` | `request_unit_test_setting_db_queue` | リクエスト単体テストの設定（テーブルをキューとして使ったメッセージング） | `setup/request_unit_test/batch.rst` のページ先頭ラベル |
| 17 | `implementation/request_unit_test/db_queue.rst` | `request_unit_test_db_queue` | リクエスト単体テスト（テーブルをキューとして使ったメッセージング） | `implementation/request_unit_test/batch.rst`（`#27-14` で作成済み） |
| 18 | `implementation/deal_unit_test/db_queue.rst` | `deal_unit_test_db_queue` | 取引単体テスト（テーブルをキューとして使ったメッセージング） | `implementation/deal_unit_test/batch.rst`（`#27-08` で作成済み） |

**飛び先のラベルは、そのページの実ファイルを開いて先頭ラベルを確かめてから書く。** `style.md` S-08 の表から引き写すのでも構わないが、**実ファイルと一致していることを必ず確認する。** 一致していなければビルドが `undefined label` を出す。

**`#27-17` と `#27-18` は、飛び先のページが先に作られていることが前提である。** キューの順番どおりに進めていれば `#27-08`・`#27-14` は済んでいる。もし飛ばしていたら、この3ページを後回しにして先にそちらを作る。

**3ファイルとも `#27-00` でスタブとして作成済みである。** 新規作成ではなく、スタブへの追記として扱う。`toctree` への登録も `#27-00` で済んでいる。

**`setup/deal_unit_test/db_queue.rst` は作らない。** `design.md:386` が「取引単体テストの設定は実データのある3処理方式のみをページ化するため、テーブルをキューとして使ったメッセージングは導線ページも設けない」と規定している。`setup/deal_unit_test/` に3ページ（`rest`・`http_messaging`・`mom`）しか無いのは意図した非対称であり、`design.md:891` が明記している。**欠落と誤認して作らないこと。**

## 3. 本文の型

3ページとも同じ型でよい。次は `#27-17` の例である。**そのまま貼るのではなく、ページごとに主語を変える。**

```rst
.. _request_unit_test_db_queue:

リクエスト単体テスト（テーブルをキューとして使ったメッセージング）
==================================================================

テーブルをキューとして使ったメッセージングのリクエスト単体テストは、\ :ref:`リクエスト単体テスト（Nablarchバッチアプリケーション） <（実ファイルのラベル）>`\ と同じ方法で行う。
```

**「同じ方法で行う」以上のことを書かない。** 差分がある可能性に気づいても、この週末では書かない。気づいた点は `reviews/page-<ラベル>.md` の「判断待ち」節に `decide` として記録し、先へ進む。

## 4. 忘れずにやること

- **`toctree` は触らない。** 3ページとも `#27-00` で `setup/index.rst`・`implementation/index.rst` に登録済みである。**並びが `design.md:837-886` のファイルツリー順（`db_queue` は各グループの最後、`mom` の次）になっていることだけ確認する。** ずれていたらここで直す。
- **3ページとも `reviews/page-<ラベル>.md` を作る。** 出典0行である事実と、`design.md:384` を根拠に導線のみとしたことを記録する。出典が無いページでもレビュー記録は省かない。
- **`mapping.csv` は触らない。** 出典0行のページなので追加する行が無い。`verify_mapping.py` は `EXPECTED_ZERO` としてこの3ページを既に許容している（`checks/task-06.md:569` ほか）。

## 5. このページ群のゲート

親指示 §5 の G1〜G10 に加えて、次を通す。

| | 内容 |
|---|---|
| DQ1 | 3ページのいずれにも `code-block` が0件であること |
| DQ2 | 3ページのいずれにも `機能概要`・`使用方法` の見出しが無いこと |
| DQ3 | `:ref:` の飛び先ラベルが実ファイルの先頭ラベルと文字列一致すること（引き写しでなく実ファイルで確認） |
| DQ4 | `setup/deal_unit_test/db_queue.rst` を作っていないこと |
| DQ5 | フルビルドで `undefined label` が増えていないこと |
