# 作業指示: `#10a` 承認後の仕上げ3件

対象ブランチ: `lovaizu/nablarch-document` の `work`（`699dfd0` の続き）

`#10a` は user review で**承認**である。本書はその後の仕上げであり、いずれも `note` 相当の小さな是正である。**`#11` に着手する前に適用する。**

判断を仰がれた2件の結論は次のとおり。

- **`testdata_examples.rst:401` の数え方** — QA の判断が正しい。ただし数詞を明確にする（STEP 1）
- **記載例ページの特殊記法セクションの参照文** — **設計レビュアーの反論が正しい。追加する**（STEP 2）

前回の回答文書（`ntf-doc-terminology-answer.md`）の (c) が定めたのは導入文の列挙の扱いであって、参照文の禁止ではない。禁止と読める書き方をしていた指示側の不備である。

---

## STEP 1 — `testdata_examples.rst:401` の数詞を明確にする

### 現状

> 正常ケースと認証エラーケースの2件を実行する例である。

### 判断

**「正常ケース」は表に実在するセルの値である。** 現状 `:424` が `- 正常ケース`、`:478` が `description: "正常ケース"` であり、導入文はその値を引いている。兄弟の導入文（現状 `:198`「明細1件の正常注文と明細2件の大量注文という2つのテストショット」）はセルの値を引いていない要約であるため、**両者が非対称であること自体は問題ではない。**

ただし「2件」が何を数えているかが曖昧である。

### 実施すること

`2件` を、テストショットを数えていることが分かる形に改める。**セルの値である「正常ケース」「認証エラーケース」はそのまま引く。**

例:

> 正常ケースと認証エラーケースの2つのテストショットを実行する例である。

**他の導入文は変更しない。** 数詞が何を数えているか曖昧な箇所が他にないかを全数確認し、確認結果を全件表で記録すること。是正が必要と判断した箇所があれば、その理由とともに報告する（本書では是正を指示しない）。

---

## STEP 2 — 記載例ページの特殊記法セクションに参照文を足す

### 判断

`null・空文字・改行など特殊な値を記述する`（記載例ページ、現状 L1849 付近）は、見出しが空文字と改行を約束しているにもかかわらず、配下に両者の例が無い。

導入文から列挙を落としただけでは、**読者から見た結果は「記載例ページだけ別見出しにする」案とほぼ同じになる。** 見出しの約束と中身が食い違い、どこを見ればよいかの手がかりも無い状態が残る。

### 実施すること

当該セクションの導入文に、次の趣旨の1文を足す。

- このページに例が無い値の種類（空文字・改行など）については、記法を「テストデータの書き方」の該当セクションで説明している

``:ref:`testdata_notation-special_notation``` へリンクすること。

**例が無い値の種類を具体的に列挙するかは、STEP 3 の突合結果に従う。** 突合が未了の場合は「空文字・改行など」にとどめ、断定的な列挙をしない。

**見出しは変更しない。** 両ページで `null・空文字・改行など特殊な値を記述する` のまま一致させる。

---

## STEP 3 — 値の種類の突合（前回の宿題）

`ntf-doc-terminology-answer.md` の「3. あわせて確認すること」が未了であれば、本書で実施する。実施済みであれば結果を再掲するだけでよい。

`testdata_notation.rst` の当該セクションが説明している値の種類（`null`・文字列の `null`・空文字・先頭ゼロ付き数値・`true`／`false`・半角スペース1文字・ダブルクォート1文字・バイナリファイル参照・文字種指定・改行文字）について、記載例ページに例があるかを1件ずつ突合する。

例が無いものについて、次のどちらかを判定する。

- **マッピング65行に該当する出典が無い** → 「出典が無いため例を作らない」という判断として記録する
- **出典があるのに落ちている** → 網羅性の欠落である。**`must` として報告する。本書では例の追加を指示しない**

突合表を全件、`checks/task-10a-followup.md`（新規）に記録すること。

---

## STEP 4 — `testdata_notation.rst` の定義セルから括弧書きを落とす

### 現状（現状 L108 付近）

> テストショット — リクエスト送信・バッチ起動などの1回の実行を表すエントリ（番号・説明・期待するステータスコード）

### 判断

**括弧書きがエンティティバリデーション体系に当てはまらない。**

- `nablarch-testing` の `EntityTestSupport.java:69-74` の `REQUIRED_COLUMNS_FOR_RELATIONAL_VALIDATION` は `title`・`expectedMessageId1`・`propertyName1` であり、`no`・`description`・`expectedStatusCode` を含まない
- ページ自身も `使用方法` 側で「エンティティバリデーションを除く各処理方式に共通する必須カラム」と断っている
- `glossary.md:201` の定義は「バリデーション実行など」を含んでおり、括弧書きと整合しない

### 実施すること

**括弧書きを削除する。** 主文だけを残す。

> テストショット — リクエスト送信・バッチ起動などの1回の実行を表すエントリ

**言い換えて残さない。** このセルはラウンド2〜4で3回連続して是正対象になっている。カラムの内訳は `使用方法` 側で処理方式ごとに正確に説明されており、`テストデータの構造` の概観表で繰り返す必要がない。

記載例ページに同じ括弧書きがあれば、同様に削除すること。

---

## STEP 5 — 記録と申し送り

1. `checks/task-10a-followup.md` を新規作成し、STEP 1〜4 の実施結果とゲートの実行出力を記録する
2. `reviews/page-testdata_notation.md`・`reviews/page-testdata_examples.md` の双方に追記する。既存の記録は書き換えない
3. **残り32ページへの申し送りに次を追加する**
   - 完了条件が「全件表」を求めている項目は、**ゲートの実行順の先頭に置く。** `#10a` では同じ表の作成漏れが3ラウンド連続で発生した
   - 概観表・定義セルに具体的なカラム名を列挙しない。処理方式ごとにカラム体系が異なるため、`使用方法` 側で説明する

---

## ゲート

すべて実行結果で確認し、`checks/task-10a-followup.md` に記録すること。

1. `python3 mapping/tools/verify_mapping.py` が `exit 0`、**594行 / 12,986 / 11,983 が不変**
2. `git diff 699dfd0 HEAD -- .rn/20260724-ntf-yaml-support/mapping/ ja/conf.py .rn/20260724-ntf-yaml-support/design.md` が**空**
3. 見出し数が不変（`testdata_notation.rst` は `-` 3 / `~` 10 / `^` 26、`testdata_examples.rst` は L2 9 / L3 28 / L4 56）。**見出しの文言と並び順も不変**であること
4. `ja/development_tools/testing_framework/` 配下の全 `.rst` に `テストケース` が**0件**（不変）
5. 記載例ページの特殊記法セクションに `:ref:` が1件以上あり、`testdata_notation-special_notation` を指していること
6. 両ページに `（番号・説明・期待するステータスコード）` が**0件**
7. STEP 1 の数詞の全件確認表、STEP 3 の突合表が、いずれも**全件**記録されていること
8. `:ref:` の未定義参照が0件、段落内改行が0件
9. Docker でフルビルド（`-a`）し、`build succeeded` かつ警告が**既知の `db_double_submit.rst` 1件のみ**（新規0件）
10. `699dfd0` との差分で、削除された段落・表の行・コードブロックが**STEP 1・2・4 に由来するものだけ**であること

---

## 禁止事項

- **`mapping.csv` / `_batch/` / `vocabulary.md` / `style.md` / `glossary.md` / `design.md` / `ja/conf.py` を変更しない**
- 見出しの文言・並び順を変更しない。`#10a` で確定している
- STEP 1 で `正常ケース`・`認証エラーケース` という語を落とさない。表に実在するセルの値である
- STEP 1 で他の導入文を変更しない。確認と報告にとどめる
- STEP 3 で例を追加しない。突合と判定のみを行う
- STEP 4 で括弧書きを言い換えて残さない。削除する
- 承認済みの `#8`・`#9`・`#10`・`#10a` の成果を、本書が指示していない範囲で変更しない
- 判定を件数だけで報告しない。STEP 1 と STEP 3 は全件を表で記録する
- 既存のレビュー記録を書き換えない。追記のみとする
- user review の承認を受けるまで `#11` に着手しない
