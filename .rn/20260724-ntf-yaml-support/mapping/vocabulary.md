# dest_part / dest_page / dest_section 語彙一覧

`mapping.csv` の `dest_part` / `dest_page` / `dest_section` に使ってよい値の一覧。
30バッチのプロンプトに埋め込む前に、この一覧が `design.md` と一致しているかを
確認するために先にコミットする。

`status` 列の意味:

- **確定** — `design.md` が確定として書いている値。`mapping/tools/extract_vocabulary.py`
  が `design.md` から機械抽出し、この一覧はその出力をそのまま転記したもの
  （再現方法は本ファイル末尾を参照）。
- **暫定** — `design.md` 自身が「暫定構成」「未確定事項」と明記している範囲
  （§3冒頭・§10 未確定事項#1・#2）に属する値。`#6`（未確定事項の確定）で
  正式な語彙に置き換わる。`#5` では暫定語彙の使用を許可するが、`#6` の完了条件は
  「暫定語彙を使用している行が0件であること」を機械検証する
  （`steering.md` #6 Completion criteria）。

## dest_part（確定・3件）

| dest_part |
|---|
| 第1部 テスティングフレームワークとは |
| 第2部 導入と設定 |
| 第3部 テストの実装方法 |

## dest_page

### 確定（12件）

| dest_part | dest_page |
|---|---|
| 第1部 テスティングフレームワークとは | テスティングフレームワークとは |
| 第3部 テストの実装方法 | テストデータの書き方 |
| 第3部 テストの実装方法 | テストデータの記載例 |
| 第3部 テストの実装方法 | リクエスト単体データ作成ツール |
| 第3部 テストの実装方法 | エンティティ単体テスト |
| 第3部 テストの実装方法 | コンポーネント単体テスト |
| 第3部 テストの実装方法 | ウェブアプリケーション |
| 第3部 テストの実装方法 | RESTfulウェブサービス |
| 第3部 テストの実装方法 | HTTPメッセージング |
| 第3部 テストの実装方法 | Nablarchバッチアプリケーション |
| 第3部 テストの実装方法 | MOMによるメッセージング |
| 第3部 テストの実装方法 | テーブルをキューとして使ったメッセージング |

出典: `design.md` §2（第1部・1ページ）、§4 のツリー図（第3部。`クラス単体テスト`・
`リクエスト単体テスト`はページではなくカテゴリ見出しのため語彙に含めない。子の
6処理方式は§5の処理方式表とNTF対象=○で完全一致）。

### 暫定（#6で確定。design.md §3冒頭・§10 未確定事項#1・#2に対応）

第2部の8ページは `design.md` §3 が「暫定構成」と明記しているものをそのまま使う
（フラットな1階層。処理方式ごとの分割はまだ確定していない）。

| dest_part | dest_page |
|---|---|
| 第2部 導入と設定 | 共通設定 |
| 第2部 導入と設定 | クラス単体テストの設定 |
| 第2部 導入と設定 | テストデータの形式 |
| 第2部 導入と設定 | テストデータ変換ツール |
| 第2部 導入と設定 | JUnit 5用拡張機能 |
| 第2部 導入と設定 | マスタデータ復旧機能 |
| 第2部 導入と設定 | マスタデータ投入ツール |

**処理方式ごとの分割が既に自明な3カテゴリ**は、暫定段階でも由来（どの処理方式の
内容か）を `dest_page` の値自体に保持させる。理由: 暫定語彙を使ったセクションが
40件超（`03_DealUnitTest/` 配下等）見込まれ、由来を `note` の文言だけに頼ると
`#6` での再分離に全行の読み直しが必要になり非現実的なため（2026-07-27 ユーザー判断）。
`#6` ではページ名を正式名称へ一括置換するだけでよい。

| dest_part | dest_page | 備考 |
|---|---|---|
| 第2部 導入と設定 | リクエスト単体テストの設定（ウェブアプリケーション） | `split-plan.md` current-0211等の既存precedentと同形式 |
| 第2部 導入と設定 | リクエスト単体テストの設定（RESTfulウェブサービス） | |
| 第2部 導入と設定 | リクエスト単体テストの設定（HTTPメッセージング） | `split-plan.md` current-0066/0106と同形式 |
| 第2部 導入と設定 | リクエスト単体テストの設定（Nablarchバッチアプリケーション） | `split-plan.md` current-0037と同形式 |
| 第2部 導入と設定 | リクエスト単体テストの設定（MOMによるメッセージング） | |
| 第2部 導入と設定 | リクエスト単体テストの設定（テーブルをキューとして使ったメッセージング） | design.md §5「中身は導線のみ」の章。採用要否は#6で判断 |
| 第2部 導入と設定 | 取引単体テストの設定（ウェブアプリケーション） | |
| 第2部 導入と設定 | 取引単体テストの設定（RESTfulウェブサービス） | |
| 第2部 導入と設定 | 取引単体テストの設定（HTTPメッセージング） | |
| 第2部 導入と設定 | 取引単体テストの設定（Nablarchバッチアプリケーション） | |
| 第2部 導入と設定 | 取引単体テストの設定（MOMによるメッセージング） | current-0158の暫定値（後述） |
| 第2部 導入と設定 | 取引単体テストの設定（テーブルをキューとして使ったメッセージング） | 採用要否は#6で判断 |
| 第3部 テストの実装方法 | 取引単体テスト（ウェブアプリケーション） | |
| 第3部 テストの実装方法 | 取引単体テスト（RESTfulウェブサービス） | |
| 第3部 テストの実装方法 | 取引単体テスト（HTTPメッセージング） | |
| 第3部 テストの実装方法 | 取引単体テスト（Nablarchバッチアプリケーション） | |
| 第3部 テストの実装方法 | 取引単体テスト（MOMによるメッセージング） | `split-plan.md` current-0156と同形式 |
| 第3部 テストの実装方法 | 取引単体テスト（テーブルをキューとして使ったメッセージング） | 採用要否は#6で判断 |

処理方式名の6件は `design.md` §5「処理方式の名称」表の `NTF対象=○` 行と完全一致
させる（`extract_vocabulary.py` の `extract_processing_methods()` と同じ抽出）。
実際にセクションが存在しない組み合わせ（例: 取引単体テストが本当に6処理方式すべてに
存在するかは未検証）は `mapping.csv` に現れないだけでよく、語彙として先回りで
用意しておくこと自体は許容する。

**current-0158（#4a `split-plan.md` で「割当先ページは未確定」とされた行）の暫定値**:
`第2部 導入と設定 > 取引単体テストの設定（MOMによるメッセージング）`。
（旧: `steering.md` に記載していた「リクエスト単体テストの設定」は由来を失うため
2026-07-27 に撤回・修正した）

## dest_section

### 確定（Part1: 6件、Part2共通テンプレート: 3件、Part3共通テンプレート: 2件）

| dest_part | dest_section |
|---|---|
| 第1部 テスティングフレームワークとは | 全体像 |
| 第1部 テスティングフレームワークとは | アーキテクチャ |
| 第1部 テスティングフレームワークとは | テストの種類 |
| 第1部 テスティングフレームワークとは | テストデータ |
| 第1部 テスティングフレームワークとは | 対象範囲 |
| 第1部 テスティングフレームワークとは | 稼動環境 |
| 第2部 導入と設定 | 機能概要 |
| 第2部 導入と設定 | 使用方法 |
| 第2部 導入と設定 | 拡張例 |
| 第3部 テストの実装方法 | 機能概要 |
| 第3部 テストの実装方法 | 使用方法 |

`design.md` の「ページのアウトライン」ツリーにある `<個別設定>する` /
`<拡張手順>する` のようなプレースホルダのサブ項目（`テストを実行できるようにする`
`コンポーネントを設定する` `テストクラスを作成する` 等の具体的な小見出しを含む）は
`dest_section` の語彙に含めない。これらはページごとに内容が異なり
`design.md` がプレースホルダのまま定義していない（機械抽出できる確定値が
存在しない）ため。ページ作成時（`#8〜`）に各ページの `使用方法` セクション内で
個別に判断する。`#5` の `mapping.csv` では `dest_section` はこの11件のみを使う。

## 再現方法

```
python3 mapping/tools/extract_vocabulary.py
```

`design.md` から機械抽出したときの件数を assert しているため、`design.md` が
更新されて構成が変わった場合はこのスクリプトが失敗する。失敗したら本ファイルを
再生成し、暫定語彙（処理方式ごとの仮ページ名を含む）を見直すこと。
