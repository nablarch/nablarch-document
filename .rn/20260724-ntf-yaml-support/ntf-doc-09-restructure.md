# 作業指示: `#9`「テストデータの書き方」の再構成

対象ブランチ: `lovaizu/nablarch-document` の `work`（`d545d95` の続き）

本書は `ntf-doc-09-fix.md` の後続である。`ntf-doc-09-fix.md` は完了済みであり、参照はしてよいが再実施しない。

---

## 背景と目的

このページは、NTF解説書の中で人にも AI にも最も多く参照されるページになる。現状の構成には、第3部の共通アウトライン（機能概要＋使用方法）に引きずられた歪みが2つある。

1. **器の説明（構造）が「使用方法」の中に埋もれている。** 冒頭3セクション（配置・データブロック・グループID）は「どう書くか」ではなく「テストデータとは何でできているか」であり、後続7セクションとは性質が違う
2. **`共通 → 形式別` の順序が守られていない。** `style.md` S-10 は共通の内容を先に置くと定めているが、`テーブルのデータを記述する` では形式別 L4 が先に来て、その後ろに形式非依存の `準備データ` `期待値` `カラムを省略する` が並んでいる

本書はこの2点を是正し、あわせて未解消の `must` 2件・`note` 1件・規約の不整合を解消する。

**内容の追加・削除は行わない。既存の記述の配置替えと、規約の明文化のみである。**

---

## 事前に確認した事実（Nabledge の検索構造）

STEP B と STEP F-3 の根拠であるため先に示す。`nablarch/nabledge` の `plugins/nabledge-6/skills/nabledge-6` を実際に読んで確認した。

**(a) 検索の入口 `knowledge/index.md` に出るのは level 2 と level 3 のみで、level 4 は出ない。**

`semantic-search.md` Phase A はこのファイルだけを読んでページを選定する。インデントは0と2の2種類しかない（実測: インデント0が1,093件、インデント2が907件、3段目は0件）。4階層を持つ `component/adapters/adapters-redisstore-lettuce-adaptor` では、`s2`（level 3）の次が `s5` になっており、level 4 の `s3`・`s4` は番号ごと欠落している。

**(b) ページ JSON にはすべての見出しが `level` 付きで入っており、Phase D はページ JSON 全体を読む。**

同ページの JSON では `s3`（level 4「コンポーネント設定ファイルを修正する」）・`s4`（level 4「環境設定値を修正する」）が存在する。したがって、ページが選定された後は L4 も候補になる。

**(c) セクションはフラットに切られており、L3 のセクションは配下の L4 の本文を含まない。**

同ページの `s2`（level 3「設定内容」）の `content` は57文字の導入文のみで、実体の2,802文字は `s3`（level 4）側にある。

**この (c) が重要である。** NTF解説書でも、`テーブルのデータを記述する` を読んだだけでは共通の導入文しか返らず、記法の実体は `Excel形式の場合`・`YAML形式の場合` を別途選ばないと取れない。**各 L3 の導入文は、単独で読んで何を扱うセクションかが分かる状態でなければならない。**

---

## STEP A — L2「テストデータの構造」を新設する

### A-1. ページ構成

```
テストデータの書き方
├── 機能概要
├── テストデータの構造            ← 新設
│     ├── テストクラスとテストデータの対応
│     ├── データブロックとデータタイプ
│     └── グループIDによる使い分け
└── 使用方法
      ├── テーブルのデータを記述する
      ├── LIST_MAPのデータを記述する
      ├── テストケース一覧（testShots）を記述する
      ├── ファイルのデータを記述する
      ├── メッセージングのデータを記述する
      ├── 値を特殊記法で記述する
      └── コメント・マーカーカラム・空エントリを扱う
```

現状の L3 のうち先頭3つ（`テストデータを配置する`・`データブロック（テストケース、準備データ、期待値）を記述する`・`グループIDでデータブロックを分ける`）を、新設した L2 の配下へ移す。**セクションの中身は移動のみで、記述内容は変更しない**（STEP C・D・E で指示する是正を除く）。

`使用方法` 配下の7セクションは、**現在の並び順のまま維持する。** 変更しない。

### A-2. 新設セクションのタイトルは体言止めにする

`style.md` S-03 は「〜する」形式を「使用方法」「拡張例」配下の小見出しに適用すると定めており、新設 L2 の配下は対象外である。構造セクションが答えるのは「これは何か」という問いであるため、体言止めにする。

- `テストクラスとテストデータの対応`
- `データブロックとデータタイプ`
- `グループIDによる使い分け`

`style.md` S-03 に、この適用範囲を1〜2行で明記すること（STEP F-1 と同じ編集の中で行う）。

### A-3. `vocabulary.md` と `mapping.csv` の更新

1. `vocabulary.md` の `dest_section` に `テストデータの構造` を追加する。**適用対象は `dest_page=テストデータの書き方` のみである旨を明記する。** 他ページの `dest_section` は変更しない
2. `mapping.csv` の `dest_page=テストデータの書き方` の140行のうち、新設 L2 配下の3セクションに対応する行の `dest_section` を `使用方法` から `テストデータの構造` へ変更する
3. **`mapping.csv` は直接編集せず、`mapping/_batch/batch-01〜30.csv` を編集して単純連結で再生成する**（`steering.md` の既定手順）
4. 変更した行を `mapping_id` の一覧として `checks/task-09-restructure.md` に記録する。**件数と、`機能概要` 1 + `テストデータの構造` N + `使用方法` (139-N) = 140 が成り立つことを明示する**

**行の総数（594）・`lines` 合計（12,986 / DROP除く 11,983）は変わらない。** 変わっていたら作業を止めて報告すること。

### A-4. `design.md` の更新

`design.md` §4「ページのアウトライン」に、本ページを例外として明記する。3〜5行で、経緯は書かない。

- 第3部は原則として `機能概要` + `使用方法` の2セクション構成とする
- 「テストデータの書き方」のみ、`機能概要` + `テストデータの構造` + `使用方法` の3セクション構成とする
- 理由: 本ページは手順ではなく記法の仕様であり、器の説明（構造）と記法の説明（使用方法）を分けたほうが読者の探索単位に一致するため

`design.md` §4「テストデータの2ページ」にある `テストデータの記載例` の見出し文言一致の規約は**変更しない。** 記載例側の見出しは `使用方法` 配下と一致していればよく、L2 がどちらに属するかは問わない。現状の記載例スキャフォールドに `テストデータを配置する` が無いことも、既存の規定どおりで変更不要である。

---

## STEP B — 各 L3 を「共通 → 形式別」の順に統一する

### B-1. 全 L3 に適用する形

```
<L3 見出し>
  共通の導入文（このセクションが何を扱うか。単独で読んで分かること）
  ├── 用途別・規則別の L4    … 意味・制約・解釈規則（形式に依らない）
  ├── Excel形式の場合        … 必ず末尾から2番目
  └── YAML形式の場合         … 必ず末尾
```

**形式別の L4 対は、常にその L3 の末尾2つに置く。** 読者が自分の形式の記法を毎回同じ位置で見つけられるようにするためである。`style.md` S-10 規約3 の「1つの L3 に見出し対は1組だけ」は維持する。

### B-2. `テーブルのデータを記述する` の L4 の並び

現状と変更後は次のとおり。**内容の追加・削除は行わず、順序の入れ替えのみとする。**

| | 現状 | 変更後 |
|---|---|---|
| 1 | Excel形式の場合 | **準備データ（SETUP_TABLE）を記述する** |
| 2 | YAML形式の場合 | **共通の準備データをまとめる** |
| 3 | 共通の準備データをまとめる | **期待値（EXPECTED_TABLE等）を記述する** |
| 4 | 準備データ（SETUP_TABLE）を記述する | **カラムを省略する** |
| 5 | 期待値（EXPECTED_TABLE等）を記述する | **Excel形式の場合** |
| 6 | カラムを省略する | **YAML形式の場合** |

`共通の準備データをまとめる`（`setUpDb`）は、`準備データ（SETUP_TABLE）を記述する` の直後に置く。個別の規則を読んだ直後に、それをテストクラス全体でまとめる話が続く形にする。

### B-3. 他の L3 の確認

`使用方法` 配下の残り6セクション（`LIST_MAP`・`testShots`・`ファイル`・`メッセージング`・`特殊記法`・`コメント`）についても、B-1 の形になっているかを確認し、形式別 L4 が末尾2つでない箇所があれば並べ替える。

`テストケース一覧（testShots）を記述する` は形式別 L4 対を持たず、処理方式別の L4 が6つ並ぶ構成である。**この構成は変更しない。** 形式差は各 L4 の中で太字ラベルにより扱われており、`style.md` S-10 規約3 の例外（既に L4 の下にあり、これ以上の見出しレベルを追加できない場合）に該当する。

### B-4. 各 L3 の導入文を確認する

事前確認 (c) のとおり、L3 の導入文は Nabledge において単独で切り出される。**全 L3 について、導入文だけを読んで「何を扱うセクションか」「配下に何があるか」が分かる状態かを確認し、不足していれば1〜2文を補う。**

補う場合も、`mapping.csv` に無い主題内容は追加しない。既存のセクション内の記述を要約する範囲にとどめる。

---

## STEP C — 形式非依存の内容が形式別 L4 に閉じ込められている箇所を解消する（`must`）

現状 L84–94、`テストデータを配置する` の `YAML形式の場合` の末尾に、次の3つが入っている。

- 接続文「ここまでのファイル構成を踏まえたうえで、テストデータを記述する際は次の点に留意する。」
- `important`（テストデータは実行順序によって結果が変わらないように記述する）
- `tip`（マスタデータの共通化、マスタデータ投入ツール・`gsp-dba-maven-plugin`・マスタデータ復旧機能への参照）

いずれも形式に依らない内容であり、**Excel を使う読者は `Excel形式の場合` を読んで次へ進むため、このページで最も重い注意に到達しない。** `style.md` S-10 規約1 に反する。

RST の制約上、L4 を開いた後は L3 の地の文に戻れない。**この3つを、当該 L3 の最初の L4（`Excel形式の場合`）より前へ移すこと。** 記述内容は変更しない。

STEP A で当該セクションが `テストクラスとテストデータの対応` に改題・移設されるため、**STEP A の後に実施する。**

STEP B-3 の確認の中で、他の L3 の形式別 L4 にも形式非依存の内容が入っていないかを併せて確認し、あれば同様に前へ出すこと。

---

## STEP D — 行番号の矛盾を解消する（`must`）

`テーブルのデータを記述する` の中で、カラム名の行の番号が食い違っている。

| 箇所 | 記述 |
|---|---|
| 現状 L339（L3 導入文） | 1行目に `データタイプ=値`、2行目にカラム名、3行目以降にデータ行 |
| 現状 L343（`Excel形式の場合` 冒頭） | 「`SETUP_TABLE=テーブル名`」の行に続けて、**1行目にカラム名、2行目以降にデータ** |
| 現状 L374 | **ヘッダ行（2行目）**は、末尾に空セルが続いても… |
| 現状 L385 | **データ行（3行目以降）**のセル数が… |

同じ小見出しの中で、カラム名の行を1行目とも2行目とも書いている。**L339 の数え方（データタイプ行を1行目とする）に統一すること。**

あわせて、**L339 の「1行目／2行目／3行目」という表現は Excel の行を前提としており、形式非依存の位置に置くべきではない。** YAML に行という概念はない。この数え方の説明は `Excel形式の場合` の中へ移し、L3 の導入文には両形式に共通する内容（3つのデータタイプの役割、`LIST_MAP` との関係）だけを残すこと。

---

## STEP E — `setUpDb` の YAML 側を補う（`note`）

`共通の準備データをまとめる` は「``setUpDb``\ という名前の読み込み単位（\ Excel\ 形式では ``setUpDb`` という名前のシート）」とだけ書いており、YAML 形式での対応（ファイル名）に触れていない。両形式の対比が本ページの前提である以上、YAML の読者が判断できない。

YAML 形式での対応を追記すること。**実装または既存の出典で確認できる範囲にとどめ、確認できない場合は追記せず、確認できなかった旨を `checks/task-09-restructure.md` に記録すること。** 推測で書かない。

---

## STEP F — 規約の不整合を解消する

### F-1. `style.md` S-03 と S-10 の衝突

S-03 は「同一ページ内でセクションタイトルを重複させない」と定めているが、S-10 規約3 は各 L3 に `Excel形式の場合` / `YAML形式の場合` を置くよう求めており、現在8組が重複している。**このままでは観点B・Dのレビューが残り33ページで毎回この重複を指摘し続ける。**

S-03 の当該箇所に、S-10 規約3 の形式別 L4 対を例外とする旨を明記すること。根拠として、Nabledge の検索面（`index.md`）には level 4 が現れないため一意性の問題が生じないこと（事前確認 (a)）を1行で添える。

あわせて、STEP A-2 の「新設 L2 配下は体言止めでよい」という適用範囲も S-03 に明記する。

### F-2. `style.md` S-10 に順序の規約を追加

規約3 に、次の1文を追加する。

- 形式別の L4 対は、その L3 の**末尾2つ**に置く。用途別・規則別の L4 がある場合は、それらを先に置く

### F-3. `style.md` に L3 導入文の規約を追加（新規 S-11）

事前確認 (c) を根拠として、次の規約を追加する。観点は11個になる。

- **規約**: L4 見出しを持つ L3 セクションは、最初の L4 より前に、そのセクションが何を扱うかが単独で分かる導入文を置く
- **根拠**: Nabledge の知識 JSON はセクションをフラットに切り、L3 の `content` に配下の L4 の本文を含まない（`nablarch/nabledge` の `plugins/nabledge-6/skills/nabledge-6/knowledge/component/adapters/adapters-redisstore-lettuce-adaptor.json` の `s2`（level 3）は57文字、`s3`（level 4）は2,802文字）。導入文が薄いと、L3 だけを切り出して読んだ際に情報がほぼ得られない
- **参照した時点**: 2026-08-06、`nablarch/nabledge` の `main`

---

## STEP G — 記録

1. `checks/task-09-restructure.md` を新規作成し、STEP A〜F の実施結果とゲートの実行出力を記録する
2. `reviews/page-testdata_notation.md` に**ラウンド3**の表を追加する。ラウンド1・2の表は書き換えない
   - STEP C・D は `must`、STEP E は `note`、STEP A・B・F は構成変更として区分する
   - STEP A のユーザー判断（2026-08-06、L2 新設と各節の共通→形式別統一）を根拠として記録する
3. `steering.md` の `#9` エントリは、user review 承認を受けるまで圧縮しない

---

## ゲート

すべて実行結果で確認し、`checks/task-09-restructure.md` に記録すること。

1. `python3 mapping/tools/verify_mapping.py` が `exit 0`、**594行 / 12,986 / 11,983 が不変**
2. `mapping.csv` の `dest_page=テストデータの書き方` が **140行**で不変。`dest_section` の内訳が `機能概要` 1 + `テストデータの構造` N + `使用方法` (139-N) = 140 になること。N の値と対象 `mapping_id` の一覧を記録する
3. `mapping.csv` が `_batch/*.csv` の単純連結と**バイト一致**すること
4. `vocabulary.md` に `テストデータの構造` があり、適用対象が `dest_page=テストデータの書き方` に限定されている
5. `testdata_notation.rst` の `-` 見出しが **3件**（`機能概要`・`テストデータの構造`・`使用方法`）
6. `~` 見出しが **10件**。うち `テストデータの構造` 配下が3件、`使用方法` 配下が7件
7. `使用方法` 配下の各 L3 について、`Excel形式の場合` と `YAML形式の場合` が存在する場合、**その L3 の最後の2つの L4** であること（`testShots` を除く）
8. `テストデータを配置する` 由来のセクション内で、`important`（実行順序）と `tip`（マスタデータ）が**最初の L4 より前**にあること
9. `testdata_notation.rst` 内で、カラム名の行を「1行目」と呼ぶ箇所が**0件**
10. `:ref:` の未定義参照が0件。`.. _` ラベルのうち直後が見出しでないものが0件
11. 段落内改行が0件（`steering.md` Rules L39）
12. Docker でフルビルド（`-a`）し、`build succeeded` かつ警告が**既知の `db_double_submit.rst` 1件のみ**。ログの該当箇所を引用して記録する
13. 本作業の前後で、`testdata_notation.rst` の**情報が失われていないこと**を確認する。作業前のコミット（`d545d95`）の当該ファイルと現状を突き合わせ、削除された段落・表の行・コードブロックが**STEP C〜E で指示した変更に由来するものだけ**であることを差分で示す

---

## 禁止事項

- **内容の追加・削除を行わない。** 本書が指示しているのは配置替え・順序変更・規約の明文化であり、新しい主題内容を書き加えない。STEP B-4 で導入文を補う場合も、既存のセクション内の記述の要約にとどめる
- `使用方法` 配下7セクションの**並び順を変更しない**
- `テストケース一覧（testShots）を記述する` の6つの処理方式別 L4 の構成を変更しない
- `mapping.csv` を直接編集しない。`_batch/` を編集して再生成する
- `mapping.csv` の**行数・`lines` 列・`disposition` 列を変更しない。** 変更するのは `dest_section` 列のみ
- `glossary.md` を変更しない
- `style.md` の観点を11個から増やさない
- 他の33ページの `dest_section` を変更しない
- ラウンド1・2のレビュー記録を書き換えない。追記のみとする
- `ntf-doc-09-fix.md` の STEP を再実施しない
- user review の承認を受けるまで `#10` に着手しない
- Nabledge の構造について、本書の事前確認に書いていないことを推測で記録しない。参照は `nablarch/nabledge` の `main`、2026-08-06 時点である
