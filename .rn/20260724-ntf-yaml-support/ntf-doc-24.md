# `#24` 判断待ちへの回答 ／ 作業指示（ラウンド3 まで反映）

- 宛先: CC
- 発行: レビュー役、2026-08-14（ラウンド3 の回答を反映して同日更新）
- 対象: `#24` 取引単体テストの実行方法の是正（`5e87f6e`・`db5a84a`・`82dbe16`）
- 参照点: `nablarch/nablarch-document` `ntf-yaml-support` = `82dbe16`（`git fetch` 後の `git log` で確認）。削除前の現行解説書は `2e501ad`
- **本書は `#24` の唯一の指示書である。ラウンド2 の指示は本書を書き換えて更新した。以下の記述が現時点の指示であり、ラウンド2 に渡した版の文言は破棄する。**

---

## 0. 結論

### ラウンド3（`82dbe16` に対する判断待ち5件）

| # | 判断待ち | 回答 |
|---|---|---|
| M1 | 「JUnitでの自動実行はできず」 | **削る。指摘は正しい**（§2-2） |
| — | `important` の扱い | **地の文に降ろす。** M1 を削ると S-06 の要件を満たさなくなるため。CC がラウンド1 で起案した `S-3` の判断が正しかった（§2-2） |
| M2 | MOM の名指し | **(a) MOM を名指しから外す。** 名指しは RESTfulウェブサービス・Nablarchバッチアプリケーション の2つに絞る（§2-2） |
| M3 | 「自動実行する」→「できる」 | **戻す。目的語も補う。**「JUnit」は出典2段の合成であり、出典を `design.md:86` に明記する（§2-2） |
| 4 | §2-1 の訂正 | **CC の指摘が正しい。レビュー役の誤りである**（§1-2） |
| 5 | `style.md` 範囲外2件 | **2件とも `#24` に含める**（§4-2） |

### ラウンド2（`db5a84a` に対する判断待ち3件）— 回答済み

| # | 判断待ち | 回答 |
|---|---|---|
| — | 公開本文の差し戻し（M-1） | **差し戻しは正しい。原因はレビュー役の指示である**（§1） |
| (1) | 公開本文の書き方 | **処理方式を名指しする**（§2） |
| (2) | `send_sync.rst`・`http_send_sync.rst` の帰属 | **`mapping.csv` は変更しない。別タスクにもしない。** 未決着ではなく `#6` で確定済みであり、かつ (1) の結論を左右しない（§3） |
| (3) | 範囲外2件を `#24` に含めるか | **2件とも含める。加えて `should` 13件のうち11件も同じラウンドで処理する**（§4） |

---

## 1. 差し戻しは正しい。原因はレビュー役の指示である

**M-1 は正しい。** `about/index.rst:73`・`:79` の「それ以外の処理方式はJUnitで自動実行」が、出典を持たない処理方式まで断定で覆っている。旧版が `index.rst` 1ページの記述を全体に広げたのと同型の過度一般化であり、向きが反転しただけ、という指摘もそのとおりである。

**この文言を書けと指示したのはレビュー役である。** `指示/task-22-回答.md:49` に「**手動なのはウェブアプリケーションだけであり、他はすべて自動である。**」、同 `:73` に「ウェブアプリケーションは手動操作、それ以外はリクエスト単体テストを連続実行してJUnitで自動実行する」と書いた。出典5件を開いたのは事実だが、**開いていない2処理方式（HTTPメッセージング、テーブルをキューとして使ったメッセージング）を「それ以外」に含めて断定した。** CC は指示どおりに実装し、CC 自身のレビューがレビュー役の誤りを捕まえた。差し戻しの判断も、範囲を広げずに止めた判断も正しい。

**是正の骨格（対比表と直後の説明を処理方式による違いに改める）そのものは維持する。** 変えるのは、覆う範囲を出典のある処理方式に限る点だけである。

### 1-2 ラウンド3 の訂正 — §2-1 の「実施方法をひとことも書いていない」は誤りである

**CC の指摘4は正しい。レビュー役の誤りである。** 本書の旧 §2-1 は「`send_sync.rst`・`http_send_sync.rst` は、取引単体テストの実施方法をひとことも書いていない」と書いたが、実測（`git show 2e501ad:<path>`）では次のとおりである。

- `send_sync.rst:4` ページ題「同期応答メッセージ送信処理を伴う取引単体テストの実施方法」。`:7` 「…モックアップクラスを使用する。」以下、ページ全体が実施方法の説明である
- `http_send_sync.rst:4` ページ題「HTTP同期応答メッセージ送信処理を伴う取引単体テストの実施方法」。`:9` 「取引単体テスト実施方法は、\ :ref:`dealUnitTest_send_sync`\ を参照すること。」。`:17` L2見出し「モックアップクラスを使用した取引単体テストの実施方法」

**両ファイルは実施方法を書いている。書いていないのは「手動か自動か」の区別だけである。** 走査語を `手動|自動|連続実行` に限ったために0件になったのを、実施方法そのものが無いと読み違えた。§2-1 の表と結論は、この語で訂正した形に差し替えてある。

**結論（HTTPメッセージングについて公開本文で何も述べない）は変わらない。** `http_send_sync.rst:7` が対象とするのは「HTTP同期応答メッセージ送信処理を伴う**ウェブアプリケーション**」であり、参照先の `send_sync.rst` も同じである。手動か自動かを述べた文はどちらにもない。

---

## 2. (1) 公開本文の書き方 — 名指しに改め、`important` は地の文に降ろす

### 2-1 実測した事実（レビュー役が `db5a84a` の作業ツリーで独立に再取得）

`git show 2e501ad:<path>`。パスは `ja/development_tools/testing_framework/guide/development_guide/05_UnitTestGuide/03_DealUnitTest/`。ディレクトリの `.rst` は8ファイルで全件（`git ls-tree -r --name-only 2e501ad` で確認）。

8ファイルを `grep -n "手動\|自動\|連続実行"` に掛けた全件は次のとおり。

| ファイル | 該当行 | 実施方法として読めるか |
|---|---|---|
| `index.rst` | `:8` 「手動でアプリケーションを操作しテストを行う。」 | **手動** |
| `batch.rst` | `:5` 「自動テストフレームワークを使用してテストを行う。」／`:6` 「リクエスト単体テストを連続実行することにより、取引単位でのテストを行う。」 | **自動** |
| `rest.rst` | `:7` 「リクエスト毎のテストを連続実行することにより取引単体テストが実施可能である。」 | **自動** |
| `real.rst` | `:10` 「リクエスト毎のテストを連続実行することにより取引単体テストが実施可能である。」 | **自動** |
| `delayed_send.rst` | 0件 | — |
| `delayed_receive.rst` | 0件 | — |
| `send_sync.rst` | `:151` 「サイズを自動計算する」／`:176` 「Excelファイルを手動で編集して」／`:207` 表の列見出し「自動テストフレームワークの動作」 | **いずれも実施方法ではない** |
| `http_send_sync.rst` | **0件** | — |

**`send_sync.rst`・`http_send_sync.rst` は、実施方法そのものは書いているが、それが手動なのか自動なのかを述べていない。**（§1-2 の訂正を参照。旧版の「実施方法をひとことも書いていない」は誤りだった。）両ファイルの `:7` は「（HTTP）同期応答メッセージ送信処理を伴うウェブアプリケーションで、取引単体テストを行う場合は、Nablarchが提供するモックアップクラスを使用する。」であり、使うクラスを述べた文であって、手動・自動の区別を述べた文ではない。したがって `checks/task-24.md` の全件表の「出典が述べる実施方法」列で、`send_sync.rst` を「手動」、`http_send_sync.rst` を「手動のみ」としているのは**本文の記述ではなく推定**である。正しくは「手動・自動の区別の記述なし」。

**8ファイルに「JUnitでの自動実行はできない」に類する記述は0件である。** 実測: `grep -n "できな\|できず\|不可\|行えな\|対応していな\|サポートしていな"` の全ヒットは `send_sync.rst:163` 「file-type テスティングフレームワークが固定長のみしか対応していないため。」の1件のみで、これは取引単体テストの実行方法とは無関係である。`index.rst:7-8` は実際に行う手順を述べているだけで、自動実行の不可能性は述べていない（M1 の根拠）。

`delayed_send.rst`・`delayed_receive.rst` はいずれも全7行で、`:5-6` のみが本文である。

**`S-13` の指摘は正しい。** `delayed_send.rst:5` 「取引単体テストの実施方法は、同期応答メッセージ送信処理と同じである。」が指す先は `send_sync.rst`（ウェブアプリケーション＋モックアップクラス）だが、`:6` のリンクは `:doc:`./real`` であり、`real.rst:2` のページ題は「取引単体テストの実施方法（同期応答メッセージ受信**処理**)」である。出典内部の食い違いであり、`delayed_send.rst` を自動の根拠にはできない。**ただし MOM を自動とする根拠は `real.rst:8-10` と `delayed_receive.rst:5-6` で足りるため、結論は変わらない。**

以上から、出典が手動・自動の区別を述べている処理方式は次のとおり。**公開本文で名指しするのは「名指しする」列が○の3処理方式だけである。**

| 処理方式（`glossary.md` §5.2 正表記） | 出典が述べる区別 | 名指しする | 出典 |
|---|---|---|---|
| ウェブアプリケーション | 手動 | ○ | `index.rst:7-8`。補強 `02_RequestUnitTest/double_transmission.rst:28,33,37`（「クライアントサイドにてテストを実施する」「デバッグモードでアプリケーションを起動する」「打鍵にてテスト対象リクエストのボタンを選択する」） |
| RESTfulウェブサービス | 自動 | ○ | `rest.rst:7` |
| Nablarchバッチアプリケーション | 自動 | ○ | `batch.rst:5-6` |
| MOMによるメッセージング | **受信は自動、送信は記述なし** | **×**（§2-2 M2） | 受信 `real.rst:8-10`・`delayed_receive.rst:5-6`／送信 `send_sync.rst`（区別の記述なし） |
| HTTPメッセージング | **記述なし** | × | `http_send_sync.rst`（区別の記述0件） |
| テーブルをキューとして使ったメッセージング | **出典0件** | × | — |

**「JUnitで」と書ける根拠は、次の2段の合成である。** どちらも出典があり、推定ではない。

1. 取引単体テストは、リクエスト単体テストを連続実行することで行う — `03_DealUnitTest/rest.rst:7`、`03_DealUnitTest/batch.rst:6`
2. リクエスト単体テストは JUnit で実行する — `02_RequestUnitTest/batch.rst:533` 「クラス単体テストと同様。通常のJUnitテストと同じように実行する。」、`02_RequestUnitTest/rest.rst:64` 「テスティングフレームワークはJUnit4をベースとしているため、テスト対象メソッドに ``@Test`` アノテーションを付与する。」

この2段を `design.md:86` に明記すること。1段目だけを根拠に「JUnitで」と書くと、次のラウンドで同じ指摘を受ける。

### 2-2 公開本文はこう書く

**出典が手動・自動の区別を述べている3処理方式だけを名指しし、残り3処理方式については何も書かない**（ラウンド3 で MOM を名指しから外し、4→3 に改めた）。「それ以外」「すべて」のような閉じた表現は使わない。書かないことは誤りではない。読者はその処理方式の第3部ページで答えを得る。

| 箇所 | 現在 | 改める内容 |
|---|---|---|
| `about/index.rst:73`（対比表 実行方法セル） | `ウェブアプリケーションは手動操作、それ以外の処理方式はJUnitで自動実行` | `処理方式によって異なる（手動操作またはJUnitで自動実行）` |
| `about/index.rst:75`（対比表 備考セル） | `手動操作の場合は、アプリケーションサーバへのデプロイが必要。エビデンスは画面ハードコピー・DBダンプ` | `手動操作の場合は、アプリケーションサーバへのデプロイが必要で、エビデンスは画面ハードコピー・DBダンプとなる` |
| `about/index.rst:77-79`（`.. important::` とその本文） | `.. important::` ディレクティブ＋3文 | **アドミニションを外し、対比表の直後の地の文にする。** 本文は下記の3文に差し替える |

差し替え後（対比表の直後、`.. _testing_framework_about-test_type_names:` の前。字下げなしの地の文。1段落1行）:

```
取引単体テストの実行方法は、対象とする処理方式によって異なる。ウェブアプリケーションでは、テスト対象のアプリケーションをアプリケーションサーバにデプロイし、手動で操作してテストを行う。RESTfulウェブサービスとNablarchバッチアプリケーションでは、リクエスト単体テストを連続実行することにより、取引単体テストをJUnitで自動実行できる。
```

3文それぞれの根拠と、ラウンド3 の判断待ちへの対応は次のとおり。

| 文 | 根拠 | 対応した指摘 |
|---|---|---|
| 第1文「処理方式によって異なる」 | 出典が処理方式ごとに異なる実行方法を述べていること自体（§2-1 の表） | — |
| 第2文「デプロイし、手動で操作してテストを行う」 | `index.rst:7-8` の逐語。**「JUnitでの自動実行はできず」は削る** | **M1**。8ファイルに不可能性の記述0件（§2-1） |
| 第3文「RESTfulウェブサービスとNablarchバッチアプリケーションでは…自動実行できる」 | `rest.rst:7`・`batch.rst:5-6` ＋ `02_RequestUnitTest/` の JUnit 出典（§2-1 の2段） | **M2**（MOM を外す）・**M3**（「する」→「できる」、目的語「取引単体テストを」を補う） |

**M2 は (a)「MOM を名指しから外す」を採る。** 理由は、`about/index.rst:92` が `MOMによるメッセージング` を「MOMによる要求電文の受信、または同期応答メッセージ送信を1リクエスト単位で行うテスト」と定義しており、この処理方式名が受信と送信の両方を覆っているためである。実測でも `mapping.csv` は `03_DealUnitTest/send_sync.rst` の取引単体テスト本文を `current-0154`・`current-0155`・`current-0156-b`・`current-0157` の4行で第3部「取引単体テスト（MOMによるメッセージング）」に割り当てている（`csv.DictReader` で集計。第2部「取引単体テストの設定（MOMによるメッセージング）」にも `current-0158` の1行）。この4行が述べるのはモックアップクラスを使う方法であって、リクエスト単体テストの連続実行ではない。**処理方式名の粒度では真偽が定まらないため、名指ししない。**

(b)「受信側に限定して名指しする」は採らない。本解説書の語彙に受信と送信を分ける処理方式名が無く（`:83-94` の6分類にも無い）、読者が対応先のページを引けないためである。(c)「現状維持」も採らない。`:79` と `:92` が同一ページ内で食い違うという指摘は正しく、放置できない。

**`important` は外し、地の文に降ろす。ラウンド1 で CC が起案した `S-3` が正しかった。** レビュー役はこれを採らず「`important` は残す」と指示したが、その根拠は第2文の「JUnitでの自動実行はできず」が S-06 の言う守るべき注意事項に当たる、という一点だけだった。M1 でその句を削る以上、根拠は消える。S-06 の規約は「無視すると不具合・非推奨機能の誤用・データ不整合につながる、読者が必ず守るべき注意事項に使う」（`style.md:230-231`。実測で行番号を再取得した）であり、残る3文はいずれも事実の説明であって注意事項ではない。

**地の文に降ろしても対比表との重複にはならない。** 対比表のセル（`:73`）は「処理方式によって異なる（手動操作またはJUnitで自動実行）」までしか述べず、どの処理方式がどちらか・自動実行がどう成り立つかは地の文だけが担う。`:73`・`:75` はラウンド2 で改めた形のままとし、ラウンド3 では変更しない。

### 2-3 `design.md` の直し方

| 箇所 | 改める内容 |
|---|---|
| `design.md:84`（対比表 取引単体テスト行） | 実行方法セル・備考セルを `about/index.rst:73`・`:75` と**逐語一致**させる。`APサーバ` は使わず `アプリケーションサーバ` に統一する（`S-12`。新NTFページに `APサーバ` は0件） |
| `design.md:86` | 「全件確認した結果」の内容を §2-1 の実測に差し替える |

`design.md:86` に必ず含める事実は次の7点。**ラウンド3 で 1・3・4 を差し替え、5〜7 を足した。**

1. 公開本文で名指しするのは3処理方式（ウェブアプリケーション＝手動、RESTfulウェブサービス・Nablarchバッチアプリケーション＝自動）であること。`file:line` を添える
2. `MOMによるメッセージング`・`HTTPメッセージング`・`テーブルをキューとして使ったメッセージング` については、**公開本文で何も述べない**という決定と、その理由
3. `send_sync.rst`・`http_send_sync.rst` は取引単体テストの実施方法を書いているが、それが手動か自動かは述べていないこと（`http_send_sync.rst:9` は `send_sync.rst` への参照指示である）
4. `MOMによるメッセージング` を名指ししない理由（§2-2 の M2。`about/index.rst:92` の定義と `mapping.csv` の割り当て4行を根拠に、処理方式名の粒度では真偽が定まらない）
5. 「JUnitで」と書ける根拠が §2-1 の2段の合成であること。2段それぞれの `file:line`
6. `delayed_send.rst:5-6` は本文とリンク先が食い違う出典であるため根拠に使っていないこと
7. 走査の母集団と走査語を明記すること（母集団 = `2e501ad` の `05_UnitTestGuide/03_DealUnitTest/` の `.rst` 8ファイル、走査語 = `手動|自動|連続実行` および不可能性の語）

**走査記録は、書いたコマンドをそのまま実行して再現する形にすること。** ラウンド2 の `design.md:86` は「8ファイルを `手動|自動|連続実行` で走査した全件」としながら、その grep では0ヒットの `delayed_receive.rst:5-6` をヒット行として挙げていた。`delayed_receive.rst:5-6` は「同期応答メッセージ受信処理と同じである」＋`:doc:`./real`` であり、`real.rst` を追って得た事実であって grep のヒットではない。**grep で得た事実と、リンクをたどって得た事実を、記録の上で分けること。** これはレビュー役の指示が招いた誤りであり、CC の指摘は正しい。

**`design.md:86` は段落を分けること。** ラウンド2 の `:86` は1段落に走査結果・決定・理由・申し送りが同居しており、`reviews/page-about_index.md` との逐語重複も生んでいる。`## 走査した事実` / `## 決定と理由` / `## 申し送り` のように小見出しで区切り、`reviews/` 側には公開本文の最終形と実測結果だけを置く（`S-11`）。

---

## 3. (2) 帰属 — `mapping.csv` は変更しない。別タスクにもしない

### 3-1 結論と理由

**変更しない。`#25` も起こさない。**

**理由1: (1) の結論を左右しない。** §2-1・§1-2 のとおり、両ファイルは実施方法を書いてはいるが、それが手動か自動かは述べていない。手動になるのは**テスト対象アプリケーションがウェブアプリケーションだから**であり、この事実は、その行が第3部のどのページに載るかとは独立である。したがって「`mapping.csv` が正なら『手動なのはウェブアプリケーションだけ』は偽」という `checks/task-24.md` の推論は成り立たない。どちらに決まっても、`index.rst:7-8` に基づく「ウェブアプリケーションは手動」は真のままである。

**理由2: 未決着ではなく `#6` で確定済みである。** `design.md:240` は「第3部の取引単体テストは全6処理方式に実データまたは導線としての出典があるため、ページ構成を縮小しない（`#6`確定、`checks/task-06.md`③未処理2参照）」と書き、行数として「HTTPメッセージング33行」を挙げている。この33行は `current-0138`（10行）＋`current-0139`（23行）＝ `http_send_sync.rst` の2行そのものである（`mapping.csv:594-595` を実測）。帰属を動かすことは `#6` の確定を開け直すことであり、新たな事実が出たわけではない現時点でそれを行う理由がない。

**理由3: 帰属としては `mapping.csv` のほうが読者の引き方に合う。** 旧 `toctree` が両ファイルをウェブアプリケーション節に置いたのは、テスト対象アプリケーションの種類で並べたからである。新解説書の処理方式の軸は、読者が「自分のアプリケーションは何をするか」で引くためのものであり、モックアップクラスを必要とするのは同期応答メッセージ送信・HTTPメッセージ送信を行うアプリケーションである。ウェブアプリケーションのページに置くと、メッセージ送信をしない読者にとって不要な節になり、メッセージ送信をする読者は辿り着けない。

### 3-2 ただし、この2ページには申し送りが要る（`checks/task-24.md` の「判断を要する事項」3 は本物）

`取引単体テスト（MOMによるメッセージング）` と `取引単体テスト（HTTPメッセージング）` の2ページは、自動実行の説明とモックアップクラスによる手動テストの説明の両方を抱える。これは帰属の誤りではなく、**出典がそうなっている**というだけである。両ページを書くときに、リード文で「テスト対象がウェブアプリケーションであり、そのアプリケーションが（HTTP）同期応答メッセージ送信処理を伴う場合」という前提を明示する必要がある。

`#24` ではこの2ページを作らない。**`design.md:86` の段落末尾に、この申し送りを1文だけ残すこと。** 別タスクは起こさない。

---

## 4. (3) 範囲外2件を含めるか — 2件とも含める。`should` も11件を同ラウンドで処理する

### 4-1 ラウンド2 の2件と `should` 13件

**「判断を要する事項」1（`glossary.md:168`）と2（`style.md` の `design.md` 参照）は、2件とも `#24` に含める。**

`glossary.md` を変更禁止としたのは `指示/task-22-回答.md` §6 の作業範囲指定であり、`#24` ラウンド2ではこれを解除する。禁止の本体は `glossary.md` §5.15 の証拠一覧を書き換えないことであって、§5.5 の意味欄ではない。`#22` で §5.4 の `:160`（`モックアップクラス`）の意味欄を是正した先例がある。

`should` 13件の裁定は次のとおり。**11件を含め、2件は対応不要とする。**

| # | 裁定 | 補足 |
|---|---|---|
| S-1 | **含める** | §2-2 の備考セル文言。1文にまとめてスコープの割れを断つ |
| S-2 | **含める** | 公開本文・`design.md` とも `実行方法` に統一する。対比表の列見出し（`about/index.rst:61`）とリード文（`:54`）が `実行方法` であるため。出典側の `実施方法` は引用時のみ使う |
| S-3 | **含める（ラウンド3 で `important` を外す形に改めた）** | §2-2 のとおり。CC の起案どおり地の文に降ろす |
| S-4 | **含める** | `:widths:`（`about/index.rst:58`）を新しいセル文言の**実測比**で決め直す。数値の根拠を `checks/task-24.md` に残すこと。レビュー役は数値を指定しない |
| S-5 | **前半のみ含める** | 「それ以外の処理方式」の廃止で `:98` との衝突は解消する。**前方参照（`処理方式` の初出が `:73`、6分類の定義が `:83-94`）は対応不要。** `design.md:90` が「内訳の表はリクエスト単体テストの6処理方式のみを載せる」と確定しており、取引単体テスト側に分類表を置くことはできない。理由を `checks/task-24.md` に記録すること |
| S-6 | **含める** | `glossary.md:168` の意味欄「複数リクエストにまたがる業務の流れを**手動操作で確認する**テスト」を、実施方法を含まない定義に改める。推奨は「複数リクエストにまたがる業務の流れを対象とするテスト」。同表の `クラス単体テスト`（`:166`）・`リクエスト単体テスト`（`:167`）が実施方法を含む定義であることとの非対称は許容する（取引単体テストだけが処理方式によって実施方法が変わるため）。**揺れ表記列・別義列・採用根拠列は変更しない** |
| S-7 | **含める** | `style.md` の `design.md` 行番号参照は**10件**（`:63`・`:64`・`:65`・`:67`・`:70`・`:77`・`:78`・`:79`・`:80`・`:81`。`grep -n 'design\.md:[0-9]' style.md` で実測）。`#24` ラウンド1 で直した `:56-58` と同じく、全件を節見出し参照に置き換える。1件だけ直して9件残す状態にしない |
| S-8 | **含める** | `style.md:45-46`（ラウンド1の是正）と `:77`（「第2部ページのアウトライン。機能概要→使用方法→拡張例の3セクション」）の自己矛盾。`:77` を `:45-46` に合わせる |
| S-9 | **含める** | `style.md:53-54`・`:62-67`・`:80-81` が根拠にする集約方針は `design.md:139` で撤回済み。実測: `design.md:118` の節題は「**モジュール一覧は第1部に置かない**。「稼動環境」は対応バージョンの premise 1文＋`:ref:`のみとする」であり、`style.md:53-54` の「依存関係（モジュール一覧）は第1部…1箇所に集約する」は逆を書いている。`design.md:137-139` の「処理方式固有の依存（RESTfulウェブサービスの `nablarch-testing-rest` 等、YAML形式の `nablarch-testing-yaml`）は当該ページ（第2部・第3部）にのみ記載し、本ページには集約しない」に合わせる。**「モジュール一覧」という見出しを置かない、という規約自体は維持する**（`design.md:118` の節題どおり） |
| S-10 | **含める** | `style.md:69-73` の「見出しは置き、本文に『なし。』とだけ書く」を削る。`:45-46` の一択（見出し自体を置かない）に揃える |
| S-11 | **含める（記録先の整理のみ）** | `reviews/page-about_index.md` の `#24` 節から `style.md` の監査結果を落とし、`checks/task-24.md` に置く。`#24` 節は CC 自身が `5e87f6e` で書いたものであり、自分の節の整理にあたる。**`#24` より前の既存記録は1行も変更しない** |
| S-12 | **含める** | §2-3 のとおり `design.md:84` を `アプリケーションサーバ` に統一し、公開本文と逐語一致させる |
| S-13 | **含める** | §2-1・§2-3 の6点目のとおり、`design.md:86` に判断根拠を書く |

### 4-2 ラウンド3 で追加する `style.md` の2件 — 2件とも含める

**どちらも実物で確認した。指摘は正しく、残り11ページの判断を誤らせる。** `style.md` は `#24` で既に変更対象であり、別タスクに分ける理由がない。

| 件 | 実測した状態（`82dbe16`） | 直し方 |
|---|---|---|
| `style.md:96-102`「根拠（拡張例の省略）」 | 規約は `:70-72` の「拡張例の見出し自体を置かない」の一択（`S-10` の是正後）。しかし根拠の先頭2件 `:98`（`FW:libraries/exclusive_control.rst:403-405`）・`:99`（`FW:libraries/service_availability.rst:112-114`）は、いずれも「見出しを置き『なし。』とだけ書く」実例であり、**採らなかった書き方を支えている** | 根拠は `:100-102` の5ファイル（見出し自体が無い実例）だけを残す。`:98`・`:99` は削り、代わりに「FW解説書には『なし。』とだけ書く実例もあるが、本解説書では採らない」旨を1文で残す（判断の記録を消さないため） |
| `style.md:189`（S-04 の表 L2 行） | 用途欄が「機能概要/モジュール一覧/使用方法/拡張例」「4つの大セクション」。`:53-54`（モジュール一覧の見出しは置かない）・`:47`（第3部は拡張例を置かない）と食い違う。変更前から存在し、`S-9`・`S-10` の是正で顕在化した | 用途欄を列挙にしない。「ページの大セクション。構成は `S-02` による」とし、`S-02` を唯一の出所にする。**`-` という記号の対応は変えない。** 根拠欄（`:198`）が挙げる FW解説書の実例に「モジュール一覧」が含まれるのは、FW解説書側の事実として正しいのでそのまま残す |

---

## 5. `#24` の作業指示（ラウンド3）

### 5-1 変更してよいファイル

**ラウンド3 では `about/index.rst` の `:77-79`（アドミニションと本文）と `style.md` の2箇所を追加で許可する。** それ以外は変更しない。

| パス | 何を |
|---|---|
| `ja/development_tools/testing_framework/about/index.rst` | `:58`（`:widths:`）・`:73`・`:75`・`:77-79`（`.. important::` の削除と本文の差し替え）のみ |
| `.rn/20260724-ntf-yaml-support/design.md` | `:84`・`:86` のみ（`:86` は §2-3 に従って段落を分けるため行数が増えてよい） |
| `.rn/20260724-ntf-yaml-support/mapping/style.md` | `:45-46`・`:53-54`・`:62-67`・`:69-73`・`:77`〜`:81`、および **`:96-102`・`:189`（ラウンド3 で追加。§4-2）** |
| `.rn/20260724-ntf-yaml-support/mapping/glossary.md` | `:168` の**意味欄のみ** |
| `.rn/20260724-ntf-yaml-support/reviews/page-about_index.md` | 自分が `5e87f6e` で追記した `## #24` 節のみ |
| `.rn/20260724-ntf-yaml-support/checks/task-24.md` | 追記（コミット対象外） |

### 5-2 変更してはならないファイル

`mapping/mapping.csv`、`mapping/_batch/`、`mapping/volume.md`、`mapping/vocabulary.md`、`glossary.md` の `:168` 以外の全行（**特に §5.15 の証拠一覧**）、`ja/conf.py`、`about/index.rst` 以外の `ja/` 配下、`steering.md`、`reviews/` の `#24` 節より前の全行。

### 5-3 ゲート

ラウンド1 のゲート1〜12 をそのまま再実行したうえで、次を足す。

| # | ゲート |
|---|---|
| A | `about/index.rst` に「それ以外」「すべての処理方式」に類する閉じた表現が0件であること（`grep -n "それ以外\|すべての処理方式" ja/development_tools/testing_framework/about/index.rst`） |
| B | 差し替えた段落が名指しする処理方式が、`glossary.md` §5.2 の正表記と逐語一致し、かつ §2-1 の表で「名指しする」が○の**3処理方式だけ**であること（`MOMによるメッセージング`・`HTTPメッセージング`・`テーブルをキューとして使ったメッセージング` が段落に現れないこと） |
| C | `design.md:84` と `about/index.rst:73`・`:75` が逐語一致すること（`APサーバ` 0件） |
| D | `about/index.rst` と `design.md` の `実施方法` が0件、`実行方法` に統一されていること（出典の引用箇所を除く） |
| E | `style.md` に `design.md:` の行番号参照が0件であること（`grep -c 'design\.md:[0-9]' style.md` = 0）。置き換え先の節見出しが `design.md` に実在し一意であることを、全件について `grep -n` の結果で示すこと |
| F | `glossary.md` の差分が `:168` の1行のみであり、§5.15 の差分が0件であること |
| G | `git diff 5e87f6e..HEAD -- .rn/.../reviews/page-about_index.md` の変更行が、`## #24` 節の内側だけに収まっていること |
| H | Docker フルビルド（`-a`）で `build succeeded, 1 warning.`（既知の `db_double_submit.rst:108` のみ、新規0件）。実行後に `git checkout -- locales/ja/LC_MESSAGES/sphinx.mo` を実行し、`git status --porcelain` に現れないこと |
| I | `commit & push` 直前にゲート1を再実行すること |
| J | 差し替えた段落が地の文であること（直前に `.. important::` が無く、字下げが0であること）。`about/index.rst` の `.. important::` が2件（`:96`・`:100` 相当の Jakarta Batch・マルチスレッドの2件）だけであること |
| K | 差し替えた段落に `できず`・`できない`・`不可` が0件であること |
| L | `design.md:86` に書いた走査コマンドをそのまま実行し、記載したヒット行と一致すること。リンクをたどって得た事実が grep のヒットとして書かれていないこと |
| M | `style.md:189` 相当行の用途欄に `モジュール一覧` が現れないこと。`style.md` の「根拠（拡張例の省略）」に `なし。` を書く実例が根拠として残っていないこと |
| N | `design.md:86` と `reviews/page-about_index.md` の `#24` 節に、同一の文が逐語で重複していないこと（`S-11`） |

### 5-4 記録

- `checks/task-24.md` にラウンド3の節を追記する（ラウンド1・2の記述は書き換えない）
- `reviews/page-about_index.md` の `#24` 節を §4 の S-11 に従って整理し、公開本文の最終形と出典の実測結果だけを残す
- 4観点レビューをもう一巡回す。**サブエージェントには本書を渡さない**（中立の枠組みを保つため）
- **ラウンド3 の `must` が0件になったら、判断を仰がずに `commit & push` してよい。** 本書で回答済みの5件を蒸し返す指摘は、回答の根拠（§2-1・§2-2）と食い違う新しい一次情報を伴う場合にだけ上げること

---

## 6. 判断が要る場合

§2-1 の実測とCC の手元の実物が食い違った場合、または §2-2 の文言が `design.md` の他の確定事項（特に `:88`・`:90`・`:92`・`:94`）と両立しない場合は、**本文を書かずに報告すること。** 範囲を自分で広げない。

`#25` は起こさない（§3）。`#23`（テーブルデータの0件、`指示/task-23.md`）は独立したタスクであり、`#24` と順序の依存はない。
