# task-terminology Completion Check

対象タスク: `#10a`（作業指示 `ntf-doc-terminology.md` ＋ 追補 `ntf-doc-terminology-addendum.md`）

基準コミット: `6ba0d2a`（`#10` 承認時点の成果物）

初回コミット: `7a74832`（`#10a` の1回目。差し戻し対応はこの上に積んだ）

差し戻し対応時点の `HEAD`: `751cf29`（コーディネーターによる回答文書 `ntf-doc-terminology-answer.md` の受領コミット。`design.md`・両ページには差分を持たない）

差し戻し対応ラウンド2（§11）時点の `HEAD`: `06df423`。ラウンド2の本文適用は `7ea37ac`、ラウンド1の本文適用は `6ce81b5` である

---

## 1. STEP 1 — `glossary.md` の改訂

作業指示 1-2 が挙げた8箇所を、行番号ではなく**内容で照合**して編集した。編集後の `git diff -U0` のハンク位置が、指示の8箇所と1対1で対応する。

```
$ git diff -U0 6ba0d2a -- .rn/20260724-ntf-yaml-support/mapping/glossary.md | grep '^@@'
@@ -201 +201 @@
@@ -207 +207 @@
@@ -215 +215 @@
@@ -359,2 +359,2 @@
@@ -552 +552 @@
@@ -557,3 +557,3 @@
@@ -622 +622 @@
@@ -637 +637 @@
```

| # | 指示の箇所 | 実施内容 |
|---|---|---|
| 1 | `:201` | 正表記を `テストショット`、揺れ表記を `テストケース`（現行155件・input38件）にした。定義文を「1回のリクエスト送信またはバッチ起動に対応する、1組の入力と期待値」に改め、業務上のテストケース1件が複数のテストショットになりうるという層の違いを含めた。採用根拠に、予約ID `testShots` が現行のIDであること・`testCases` は下位互換のフォールバックであること（`AbstractHttpRequestTestTemplate.java:222`）・公開クラス名が `TestShot`（`@Published`）であることを記載した。あわせて `テストメソッド`／普通名詞 `テスト` との使い分けを注記した |
| 2 | `:215` | 正表記を `テストショット一覧`（現行5件）、揺れ表記に `テストケース一覧`（現行24件・input5件）・`テストケース表`（現行9件）・`テストショット表`（現行2件）を集めた。定義文の「テストケースを列挙した」を「テストショットを列挙した」に改めた |
| 3 | `:207` | `テストデータ` の定義文中の「テストケース一覧」を「テストショット一覧」に改めた |
| 4 | `:359`–`:360` | `テストケース一覧` を「揺れ表記」、`テストショット一覧` を「正表記」に入れ替えた（行順は五十音順のまま維持） |
| 5 | `:552` | 置換ルールを逆向きにした（`テストケース` → `テストショット`）。除外条件も `テストケース一覧`・`テストケース表` に反転し、`テストメソッド`／`テスト` へ振り分ける場合の但し書きを加えた |
| 6 | `:557`–`:559` | 置換先を `テストショット一覧` に改め、置換元の `テストショット一覧` を `テストケース一覧` に差し替えた（3行のまま） |
| 7 | `:622` | 「日本語の総称は `テストケース一覧` を使う」を `テストショット一覧` に改めた |
| 8 | `:637` | 分類軸の説明にある「テストケース一覧」を「テストショット一覧」に改めた |

**変更してはならない `:403`〜`:449`（現行解説書の見出し文字列の一覧）は無変更**（ゲート4 参照）。判断に迷って変更しなかった行は無い。

---

## 2. STEP 2 — `テストケース` の全件置換判定

**一括置換は使っていない。** `sed -i` の無条件置換も `replace_all` も使わず、対象行の前後を実際に読んで文脈を確認したうえで、1件ずつ `Edit`（一意な文脈文字列を指定）で置き換えた。

### 2-0. 総括

| 置換先 | testdata_notation.rst | testdata_examples.rst | about/index.rst | 計 |
|---|---|---|---|---|
| `テストショット一覧` | 20 | 23 | 0 | 43 |
| `テストショット` | 16 | 10 | 0 | 26 |
| `テストメソッド` | 0 | 0 | 0 | 0 |
| `テスト` | 6 | 0 | 2 | 8 |
| **計** | **42** | **33** | **2** | **77** |

置換前の総数（基準コミット `6ba0d2a` 実測）:

```
$ git show 6ba0d2a:.../testdata_notation.rst | grep -o 'テストケース' | wc -l
42
$ git show 6ba0d2a:.../testdata_examples.rst | grep -o 'テストケース' | wc -l
33
$ git show 6ba0d2a:.../about/index.rst | grep -o 'テストケース' | wc -l
2
```

### 2-1. `testdata_notation.rst`（42件）

行番号は `6ba0d2a` 時点のもの。1行に複数出現する場合は出現順に枝番を付けた。

| # | 旧行 | 置換前の文（該当部分） | 判定 | 置換後 |
|---|---|---|---|---|
| N-1a | 40 | マスタデータへの変更を伴う**テストケース**であっても | `テスト` | マスタデータへの変更を伴う**テスト**であっても（この列は置換直後の状態。**最終文は §11-1 #2／§12 R3-A-2／§13 R4-3 を参照**） |
| N-1b | 40 | 他の**テストケース**に影響を与えずに実行できる | `テスト` | 他の**テスト**に影響を与えずに実行できる（この列は置換直後の状態。**最終文は §11-1 #2／§12 R3-A-2／§13 R4-3 を参照**） |
| N-2 | 98 | **テストケース**・準備データ・期待値という3つの用途のデータ | `テストショット` | **テストショット**・準備データ・期待値という3つの用途のデータ |
| N-3 | 107 | 表の「用途」列のセル `テストケース` | `テストショット` | `テストショット` |
| N-4 | 109 | **テストケース一覧**（データタイプ `LIST_MAP`、ID は `testShots`） | `テストショット一覧` | **テストショット一覧**（データタイプ `LIST_MAP`、…） |
| N-5 | 126 | グループID・**テストケース一覧**・ファイルデータ・メッセージングの各記法 | `テストショット一覧` | グループID・**テストショット一覧**・… |
| N-6 | 132 | code-block `├── テストケース一覧    … testShots` | `テストショット一覧` | `├── テストショット一覧  … testShots`（表示幅20に合わせて余白を4→2に調整） |
| N-7 | 138 | **テストケース一覧**・ファイルデータ・メッセージングと、 | `テストショット一覧` | **テストショット一覧**・ファイルデータ・メッセージングと、 |
| N-8 | 159 | キーバリュー形式の汎用データ（**テストケース一覧**・期待値など） | `テストショット一覧` | （**テストショット一覧**・期待値など） |
| N-9a | 252 | **テストケース一覧**の各カラムでグループIDを参照する | `テストショット一覧` | **テストショット一覧**の各カラムで… |
| N-9b | 252 | **テストケース**ごとに異なる準備データ・期待値を使い分けられる | `テストショット` | **テストショット**ごとに異なる… |
| N-10 | 254 | **テストケース一覧**のカラムで、デフォルトグループを | `テストショット一覧` | **テストショット一覧**のカラムで、… |
| N-11 | 348 | テーブル・`LIST_MAP`・**テストケース一覧**・ファイル・メッセージング | `テストショット一覧` | **テストショット一覧**・`LIST_MAP`・テーブル・ファイル・メッセージング（STEP 3 の並び替えに伴い列挙順も是正。§3 の Z-1） |
| N-12 | 396 | 一部だけが更新される更新系**テストケース**で特に有効 | `テスト` | 更新系**テスト**で特に有効（同段落の後続文が既に「更新系テスト」と書いており揃う） |
| N-13 | 429 | 「…削除フラグが `1` に更新されること」という**テストケース**で | `テスト` | …という**テスト**で（何を検証するかを述べている）。この列は置換直後の状態であり、**最終文は §10 A-4 を参照**（`テスト` の近接重複を解消して `…を確認するために、` に改めた。現在の行は `notation:725`） |
| N-14 | 519 | **テストケース一覧**・リクエストパラメータ・期待値オブジェクト・期待ログ | `テストショット一覧` | **テストショット一覧**・… |
| N-15a | 521 | 利用例の1つである**テストケース一覧**（`testShots`） | `テストショット一覧` | 利用例の1つである**テストショット一覧**（`testShots`） |
| N-15b | 521 | 後続の :ref:`**テストケース一覧**（testShots）を記述する` | `テストショット一覧` | 前述の :ref:`**テストショット一覧**（testShots）を記述する`（方向語も是正。§3 の Z-2） |
| N-16 | 583 | 見出し `テストケース一覧（testShots）を記述する` | `テストショット一覧` | `テストショット一覧（testShots）を記述する` |
| N-17a | 585 | **テストケース一覧**（`testShots`）は、 | `テストショット一覧` | **テストショット一覧**（`testShots`）は、 |
| N-17b | 585 | 1エントリを1つの**テストケース**として実行するためのデータブロック | `テストショット` | 1エントリを1つの**テストショット**として実行する… |
| N-18 | 587 | 実際の記述例は :ref:`**テストケース一覧**（testShots）を記述する` | `テストショット一覧` | :ref:`**テストショット一覧**（testShots）を記述する` |
| N-19a | 591 | `testShots` は、**テストケース一覧**を表す予約 ID である | `テストショット一覧` | **テストショット一覧**を表す予約 ID |
| N-19b | 591 | 各エントリを1つの**テストケース**として実行する | `テストショット` | 各エントリを1つの**テストショット**として実行する |
| N-20 | 595 | `testShots` の1行が1つの**テストケース**に対応する | `テストショット` | 1つの**テストショット**に対応する |
| N-21 | 621 | **テストケース**番号を1からの連番で記載する（`no` カラム） | `テストショット` | **テストショット**番号を1からの連番で記載する |
| N-22 | 623 | その**テストケース**の説明を記載する（`description` カラム） | `テストショット` | その**テストショット**の説明を記載する |
| N-23a | 678 | **テストケース一覧**と行単位で対応させて記載する | `テストショット一覧` | **テストショット一覧**と行単位で… |
| N-23b | 678 | **テストケース一覧**の先頭行の | `テストショット一覧` | **テストショット一覧**の先頭行の |
| N-23c | 678 | 先頭行の**テストケース**には | `テストショット` | 先頭行の**テストショット**には |
| N-23d | 678 | **テストケース**との対応が分かりやすいよう | `テストショット` | **テストショット**との対応が分かりやすいよう |
| N-23e | 678 | **テストケース**番号をマーカーカラム（後述）として | `テストショット` | **テストショット**番号をマーカーカラム（後述）として |
| N-24a | 682 | **テストケース**番号の列だけを記載した列を用意し | `テストショット` | **テストショット**番号の列だけを記載した列 |
| N-24b | 682 | **テストケース**数分の行を用意する | `テストショット` | **テストショット**数分の行を用意する |
| N-25 | 739 | **テストケース一覧**に上記以外のカラムを追加すると | `テストショット一覧` | **テストショット一覧**に上記以外の… |
| N-26 | 803 | `title` カラムの説明「**テストケース**の説明」 | `テストショット` | 「**テストショット**の説明」 |
| N-27 | 869 | グループIDで**テストケース一覧**と紐付ける | `テストショット一覧` | グループIDで**テストショット一覧**と紐付ける |
| N-28 | 1017 | **テストケース一覧**の `setUpFile` などに記載されたグループID | `テストショット一覧` | **テストショット一覧**の `setUpFile` などに… |
| N-29a | 1135 | 1つの読み込み単位に1**テストケース**のみ記述する | `テストショット` | 1**テストショット**のみ記述する |
| N-29b | 1135 | 事実上1**テストケース**しか記述できない | `テストショット` | 事実上1**テストショット**しか記述できない |
| N-30 | 1218 | 同期応答メッセージ送信の**テストケース**と異なり | `テスト` | 同期応答メッセージ送信の**テスト**と異なり（`testShots` の行ではなく、比較対象のテストそのものを指す） |
| N-31 | 1220 | （**テストケース**上、長さを揃えられない場合は手動でテストを行う） | `テスト` | （**テストの内容**上、…）（`テスト上` では直後の「テストを行う」と重複して読みにくいため「テストの内容上」とした）。**この1件は1:1の置換ではない。** `テストケース` → `テスト` の語の置換に加えて「の内容」を挿入している。判定表の他の76件はすべて語の1:1置換であり、語の挿入・削除を伴うのは本件のみである。この列は置換直後の状態であり、**最終文は §12 R3-A-7／§13 R4-2 を参照** |

**付随して変更した1箇所（`テストケース` の略記）**

| 旧行 | 置換前 | 置換後 | 理由 |
|---|---|---|---|
| 108 | 1エントリ1**ケース**の実行条件（期待するステータスコードなど） | 1エントリが1回の実行（リクエスト送信・バッチ起動・バリデーション実行など）に対応する、1組の入力と期待値（期待するステータスコードなど） | 同じ表の「用途」列（N-3）を `テストショット` にしたため、その行の説明文だけ `ケース`（= `テストケース` の略記）が残ると同一セル群で語が割れる。`テストデータの構造` 配下の表であり `使用方法` 配下ではない。**当初は `1エントリ1テストショットの実行条件` としたが、これは `テストショット` を `テストショット` で説明する循環定義であり退行だった。差し戻し対応 A-1 で、同じ表の `準備データ` 行と同じ水準の定義文に改めた**（§10 A-1） |

### 2-2. `testdata_examples.rst`（33件）

| # | 旧行 | 置換前の文（該当部分） | 判定 | 置換後 |
|---|---|---|---|---|
| E-1 | 14 | **テストケース**・準備データ・期待値を共存させる記述例 | `テストショット` | **テストショット**・準備データ・期待値を… |
| E-2 | 16 | 見出し `1つの読み込み単位にテストケース・準備データ・期待値をまとめる` | `テストショット` | `1つの読み込み単位にテストショット・準備データ・期待値をまとめる`（アンダーライン 61→63） |
| E-3 | 18 | **テストケース一覧**・準備データ・期待値・期待ログという4つのデータブロック | `テストショット一覧` | **テストショット一覧**・… |
| E-4 | 22 | **テストケース一覧**の例を示す | `テストショット一覧` | **テストショット一覧**の例を示す |
| E-5 | 137 | 「LIST_MAP=testShots」が**テストケース一覧** | `テストショット一覧` | 「LIST_MAP=testShots」が**テストショット一覧** |
| E-6 | 188 | `id: testShots` が**テストケース一覧** | `テストショット一覧` | `id: testShots` が**テストショット一覧** |
| E-7 | 194 | **テストケース**ごとに異なる準備データ・期待値を使い分ける記述例 | `テストショット` | **テストショット**ごとに… |
| E-8 | 196 | 見出し `テストケースごとに準備データと期待値を使い分ける` | `テストショット` | `テストショットごとに準備データと期待値を使い分ける` |
| E-9a | 198 | 2つの**テストケース**を実行する例である | `テストショット` | 2つの**テストショット**を実行する例である |
| E-9b | 198 | **テストケース一覧**の `setUpTable`・`expectedTable` カラム | `テストショット一覧` | **テストショット一覧**の… |
| E-10 | 204 | 表のヘッダセル `テストケース` | `テストショット` | `テストショット` |
| E-11 | 218 | **テストケース一覧**の例を示す | `テストショット一覧` | **テストショット一覧**の例を示す |
| E-12a | 651 | **テストケース一覧**とは行単位で対応するため | `テストショット一覧` | **テストショット一覧**とは行単位で… |
| E-12b | 651 | **テストケース**番号と説明をマーカーカラムとして | `テストショット` | **テストショット**番号と説明を… |
| E-13 | 710 | **テストケース一覧**の `expectedLog` カラムに | `テストショット一覧` | **テストショット一覧**の… |
| E-14 | 749 | L2見出し `テストケース一覧（testShots）を記述する` | `テストショット一覧` | `テストショット一覧（testShots）を記述する` |
| E-15a | 751 | **テストケース一覧**（`testShots`）の記述例を | `テストショット一覧` | **テストショット一覧**（`testShots`）の記述例を |
| E-15b | 751 | :ref:`**テストケース一覧**（testShots）を記述する` | `テストショット一覧` | :ref:`**テストショット一覧**（testShots）を記述する` |
| E-16 | 753 | 見出し `ウェブアプリケーションのテストケース一覧を記述する` | `テストショット一覧` | `ウェブアプリケーションのテストショット一覧を記述する`（アンダーライン 50→52） |
| E-17 | 759 | **テストケース一覧**の例を示す | `テストショット一覧` | **テストショット一覧**の例を示す |
| E-18a | 790 | **テストケース**数分の行を持つ `requestParams` | `テストショット` | **テストショット**数分の行を持つ… |
| E-18b | 790 | **テストケース**ごとに1つずつ記述する | `テストショット` | **テストショット**ごとに1つずつ記述する |
| E-19 | 854 | 見出し `Nablarchバッチアプリケーションのテストケース一覧を記述する` | `テストショット一覧` | `…テストショット一覧を記述する`（アンダーライン 58→60） |
| E-20 | 860 | **テストケース一覧**の例を示す | `テストショット一覧` | **テストショット一覧**の例を示す |
| E-21 | 928 | 見出し `メッセージングのテストケース一覧を記述する` | `テストショット一覧` | `メッセージングのテストショット一覧を記述する`（表示幅44≤50のためアンダーライン50のまま） |
| E-22 | 934 | **テストケース一覧**の例を示す | `テストショット一覧` | **テストショット一覧**の例を示す |
| E-23 | 991 | 見出し `エンティティバリデーションのテストケース一覧を記述する` | `テストショット一覧` | `…テストショット一覧を記述する`（アンダーライン 54→56） |
| E-24 | 997 | **テストケース一覧**の例を示す | `テストショット一覧` | **テストショット一覧**の例を示す |
| E-25 | 1471 | **テストケース**ごとに異なる入力ファイルを使い分ける例 | `テストショット` | **テストショット**ごとに… |
| E-26 | 1705 | **テストケース一覧**の `expectedMessage` カラムに記述したグループID | `テストショット一覧` | **テストショット一覧**の… |
| E-27a | 1713 | **テストケース一覧**の記述例は | `テストショット一覧` | **テストショット一覧**の記述例は |
| E-27b | 1713 | :ref:`**テストケース一覧**（testShots）を記述する` | `テストショット一覧` | :ref:`**テストショット一覧**（testShots）を記述する` |
| E-28 | 1734 | `group_id:` の値が**テストケース一覧**の `expectedMessage` カラムに対応 | `テストショット一覧` | `group_id:` の値が**テストショット一覧**の… |

### 2-3. `about/index.rst`（2件・別枠。追補 `ntf-doc-terminology-addendum.md` による）

**置き換え前に確認したこと**（追補の「判定にあたって確認すること」）:

```
$ grep -rn 'テストショット\|testShots\|LIST_MAP' ja/development_tools/testing_framework/about/
（0件）
$ grep -n ':ref:' ja/development_tools/testing_framework/about/index.rst
28:...両形式の記法は :ref:`テストデータの書き方 <testdata_notation>` で、...
117:...:ref:`JUnit 5用拡張機能 <junit5_extension>` を参照。
```

- 第1部に `テストショット`・`testShots`・`LIST_MAP` の記述は**1件も無い**
- 対象の L24 が属する節は `テストコードは定型かつ少量で済む`（L22–L24）であり、この節の中に第3部への `:ref:` は無い（L28 の `:ref:` は**次の**節 `利用目的に応じたテストデータ形式を選べる` に属する）
- したがって `テストショット` を使うと**定義なしの初出**になる

| # | 旧行 | 置換前の文（該当部分） | 判定 | 置換後 | なぜその語を選んだか |
|---|---|---|---|---|---|
| A-1 | 24 | **テストケース**を追加しても実装量が増えにくい | `テスト` | **テスト**を追加しても実装量が増えにくい | 第1部は特徴を述べる場所であり、データブロックの粒度に踏み込む必要がない。上記のとおり `テストショット` は定義なしの初出になる。文意は「テストを増やしても Java の実装量は増えない」であり `テスト` で成立し、同じ段落の他の語（「テストロジック」「テストデータ」「テストコード」「テスト対象」「テスト結果」）とも整合する |
| A-2 | 24 | 大量の**テストケース**を効率的に用意・保守する | `テスト` | 大量の**テスト**を効率的に用意・保守する | 直前が「テストデータのパターンも多数に上りやすいため」であり、ここで数えているのは個々の testShots 行ではなくテスト全体の規模である。加えて、同一段落内で A-1 と語を混在させると読者が2つの粒度を区別できないため、A-1 と同じ `テスト` に揃えた |

**`テストショット` を選ばなかったことの明示**: STEP 2-1 の基準を機械的に当てると「数えられるもの」に該当し `テストショット` になるが、上記の実測（第1部に定義・導線が無い）を根拠に `テスト` を選んだ。定義なしの初出を避ける判断である。

---

## 3. STEP 3 — 「使用方法」の並び替えと前後関係の是正

### 3-1. 並び替え

`testdata_notation.rst` の `使用方法` 配下（`~` 見出し）と、`testdata_examples.rst` の L2（`-` 見出し）を、指示 STEP 3-1 の順にした。並び替えは**ブロック単位の移動**（ラベル `.. _xxx:` から次のラベル直前までを丸ごと入れ替え）で行い、移動前後で行の多重集合が完全に一致することをスクリプトで表明（assert）した。`テストデータの構造` 配下3節の並びは変更していない。

```
$ awk '/^使用方法$/{u=1} u&&/^~~~/{print prev} {prev=$0}' .../testdata_notation.rst
テストショット一覧（testShots）を記述する
LIST_MAPのデータを記述する
テーブルのデータを記述する
ファイルのデータを記述する
メッセージングのデータを記述する
null・空文字・改行など特殊な値を記述する
コメント・マーカーカラム・空エントリを扱う
```

### 3-2. 前方参照の手当て

`テストショット一覧（testShots）を記述する` の節の**冒頭**（第1文の直後）に、次の1文を追加した。

> これは\ ``LIST_MAP``\ というデータタイプの利用例の1つであり、``LIST_MAP``\ 自体の記法は後述の\ :ref:`LIST_MAPのデータを記述する <testdata_notation-list_map>`\ で説明する。

「後述の :ref:`…`」という書き方は、既存の解説書 `ja/application_framework/application_framework/libraries/database/universal_dao.rst:645`（「後述の :ref:`@Table <universal_dao_jpa_table>` を用いて」）に先例がある。追加はこの1文のみで、節の中身は変更していない。

**差し戻し対応 A-3 での追加（記載例ページ側）**: 上の1文は `testdata_notation.rst`（354）にしか置いておらず、対応する記載例ページの節（`testdata_examples.rst` の `テストショット一覧（testShots）を記述する`、397）には同趣旨の文が無かった。記載例ページの調子（「…の記述例を、…示す」）に合わせて次の1文を追加した。

> これは\ ``LIST_MAP``\ というデータタイプの利用例の1つであり、``LIST_MAP``\ 自体の記述例は後述の\ :ref:`LIST_MAPのデータを記述する <testdata_examples-list_map>`\ に示す。

`:ref:` のリンク先は同ページの `testdata_examples-list_map`（672）であり、参照元（397）より後にあるため「後述の」が成立する。

**訂正（差し戻し対応ラウンド3 R3-D-2 b）**: 上に引用した2文はいずれも**追加した当時の文言**であり、現物とは一致しない。`notation:354` はラウンド3 R3-A-3 で `利用例`→`用途` に、`examples:397` はラウンド2 R2-A-3（事実誤りと先行詞の是正）とラウンド3 R3-A-3 で書き換わっている。**現物の文言は §11-1 の #3・§12 の R3-A-3 を参照すること。** 方向語（`後述の`）と `:ref:` の参照先は当時から変わっておらず、§3-3 の Z-7・Z-23 の判定に影響しない。

### 3-3. 前後関係を表す語の全数確認（24件）

**件数について（差し戻し対応ラウンド3 R3-D-1 で確定した値。以前の見出しは「17件」だった）**: 本表の行数は **24件**であり、内訳は次のとおりである。**本ファイル・`reviews/` を通じて、この 24／22 という値に統一する。**

- `grep` が拾った方向語の行 **22件**（`testdata_notation.rst` **18行**・`testdata_examples.rst` **4行**）＝ Z-2〜Z-21・Z-23・Z-24
- 方向語を持たないが読解で見つけた列挙順 **1件** ＝ Z-1（`notation:348`）
- 方向語を持たないが移動で向きが反転した相互参照のうち、本表に移した **1件** ＝ Z-22（`examples:436`。差し戻し対応 D-4）

`前述`／`後述`／`後続`／`次節`／`前節`／`上記`／`以下の節`／`先述`／`既述`／`前掲`／`後掲`／`下記` を両ページから全数抽出し、1件ずつ整合を確認した。

```
$ grep -c '前述\|後述\|後続\|次節\|前節\|上記\|以下の節\|先述\|既述\|前掲\|後掲\|下記' \
    testdata_notation.rst testdata_examples.rst
testdata_notation.rst:18
testdata_examples.rst:4
$ grep -n '前述\|後述\|後続\|次節\|前節\|上記\|以下の節\|先述\|既述\|前掲\|後掲\|下記' \
    testdata_examples.rst
67:（後続の）  397:（後述の）  676:（前述の）  765:（前掲の）
```

**行番号は差し戻し対応ラウンド3 の本文編集（§12）の後の最終値である**（ラウンド3の本文編集はすべて同一行内の置換であり、両ページとも行数・行位置は変わっていない）。

| # | ファイル | 新行 | 語 | 指している先 | 判定 |
|---|---|---|---|---|---|
| Z-1 | notation | 348 | 「示したうえで」（列挙順） | `使用方法` 配下の対象別セクションの並び | **是正**。「テーブル・`LIST_MAP`・テストケース一覧・ファイル・メッセージング」→「テストショット一覧・`LIST_MAP`・テーブル・ファイル・メッセージング」 |
| Z-2 | notation | 586 | 後続の → **前述の** | `テストショット一覧（testShots）を記述する`（352）| **是正**。移動により参照先が前になったため。文末も「で行う。」→「を参照。」に改め、同ファイルの支配的な言い回し（「…は :ref:`…` を参照。」）に合わせた。**ラウンド3 R3-A-3 で、この文はさらに「用途の1つであるテストショット一覧（``testShots``\ ）のカラムについては、処理方式ごとに前述の :ref:`…` で説明している。」に改めた**（`利用例`→`用途` と「の」の連結の解消。文末は「を参照。」から「で説明している。」に変わったが、**方向語「前述の」と参照先は不変**であり本行の判定は変わらない） |
| Z-3 | notation | 28 | 後述する | 同一節内の `Excel形式の場合`（42）・`YAML形式の場合`（81） | 変更不要（節内。移動対象外） |
| Z-4 | notation | 136 | 上記の | 直前の code-block 内の列挙 | 変更不要（ブロック内） |
| Z-5 | notation | 200 | 前掲の | 同一節内のデータタイプ一覧の表（142） | 変更不要（節内） |
| Z-6 | notation | 282 | 後続の `TABLE3` | code-block 内の例の後続行 | 変更不要（データの前後であって節の前後ではない） |
| Z-7 | notation | 354 | **後述の**（新規追加） | `LIST_MAPのデータを記述する`（582） | 新規。352 < 582 で成立 |
| Z-8 | notation | 447 | （後述） | `コメント・マーカーカラム・空エントリを扱う`（1447） | 変更不要。テストショット一覧（352）は並び替え後も最初であり、コメント節は最後のまま |
| Z-9 | notation | 508 | 上記以外のカラム | 同一節内の直前の表 | 変更不要（節内） |
| Z-10 | notation | 656 | 後続の行 | YAML の `rows:` の後続行 | 変更不要（データの前後） |
| Z-11 | notation | 698 | 前述のとおり（2箇所） | 同一節内の `準備データ（SETUP_TABLE）を記述する`・`期待値（EXPECTED_TABLE等）を記述する` | 変更不要（節内。節ごと移動したため相対順序は不変） |
| Z-12 | notation | 792 | 後続の行（2箇所） | YAML の `rows:` の後続行 | 変更不要（データの前後） |
| Z-13 | notation | 815 | 後述する | 同一節内の `Excel形式の場合`（971）・`YAML形式の場合`（1086） | 変更不要（節内） |
| Z-14 | notation | 1121 | 後述する4つのデータタイプ | 同一節内の表（1141） | 変更不要（節内） |
| Z-15 | notation | 1123 | 前述のファイルデータ | `ファイルのデータを記述する`（813） | 変更不要。ファイル（813）＜メッセージング（1117）は並び替え後も維持 |
| Z-16 | notation | 1224 | 前述のファイルデータ | 同上 | 変更不要 |
| Z-17 | notation | 1500 | （前述）可変長ファイルの空行 `""` | `ファイルのデータを記述する`（813） | 変更不要。813 ＜ 1447 |
| Z-18 | notation | 1515 | （前述）マーカーカラム | 同一節内の直前の段落 | 変更不要（節内） |
| Z-19 | notation | 1517 | （前述）デフォルト値の補完 | `テーブルのデータを記述する`（648） | 変更不要。648 ＜ 1447（テーブルはコメント節より前のまま） |
| Z-20 | examples | 67 | 後続の「LIST_MAP=expectedLog」 | 同一シート例の後続データブロック | 変更不要（データの前後） |
| Z-21 | examples | 765 | 前掲のリクエストパラメータ | 同一L2内の `リクエストパラメータを記述する`（678） | 変更不要（L2内。L2ごと移動したため相対順序は不変） |
| Z-22 | examples | 436 | 方向語なし（「（\ :ref:`LIST_MAPのデータを記述する <testdata_examples-list_map>`\ 参照）」） | 記載例ページの `LIST_MAPのデータを記述する`（新672 / 旧643） | **方向が反転**。旧構成は後方参照（LIST_MAP 643 ＜ 参照元 790）、新構成は前方参照（参照元 436 ＜ LIST_MAP 672）。**文面の是正は不要**と判断した。理由は (a) この文は「（…参照）」であり `前述`／`後述` の方向語を持たないため、方向が反転しても文が誤りにならない、(b) 差し戻し対応 A-3 により、この参照元が属する節 `テストショット一覧（testShots）を記述する` の冒頭（397）に「``LIST_MAP``\ 自体の記述例は**後述の**\ :ref:`LIST_MAPのデータを記述する <testdata_examples-list_map>`\ に示す」という前方参照の1文を置いたため、節の入口で方向が読者に示される。**当初この行は §3-3 の補助表（方向語を持たない相互参照）にのみ記載し、記載例ページ側の確認が本表に現れていなかった。差し戻し対応 D-4 で本表に移した**（§10 D-4） |
| Z-23 | examples | 397 | **後述の**（本タスクが追加。ラウンド1 A-3） | 記載例ページの `LIST_MAPのデータを記述する`（ラベル `.. _testdata_examples-list_map:` が672、見出しが674） | 新規。参照元 397 ＜ 参照先 672 で「後述の」が成立する。**差し戻し対応ラウンド3 R3-D-1 で本表に追加**（notation 側の同種の追加は Z-7 として当初から本表にあったのに、記載例ページ側だけが漏れていた）。ラウンド2 R2-A-3 とラウンド3 R3-A-3 で文言が変わったが、方向語と参照先は不変（現行文言は §12 の R3-A-3） |
| Z-24 | examples | 676 | **前述の**（本タスクが追加。ラウンド2 R2-A-4） | 記載例ページの `テストショット一覧（testShots）を記述する`（ラベル `.. _testdata_examples-test_shots:` が393、見出しが395） | 新規。参照先 393 ＜ 参照元 676 で「前述の」が成立する。**差し戻し対応ラウンド3 R3-D-1 で本表に追加**。ラウンド3 R3-A-4 で文言が変わったが、方向語と参照先は不変（現行文言は §12 の R3-A-4） |

**方向語を持たない相互参照のうち、移動により向きが変わったもの**（いずれも方向語が無いため文面の是正は不要。全数確認済み）:

| ファイル | 新行 | 参照元の節 → 参照先 | 移動前 | 移動後 |
|---|---|---|---|---|
| notation | 650 | `テーブルのデータを記述する` → :ref:`LIST_MAPのデータを記述する <testdata_notation-list_map>` | 前方参照（テーブル352 ＜ LIST_MAP515） | 後方参照（LIST_MAP582 ＜ テーブル648） |
| notation | 684 | `テーブルのデータを記述する` の `important` → 同上 | 前方参照 | 後方参照 |
| notation | 696 | `カラムを省略する` の `important` → 同上 | 前方参照 | 後方参照 |
| examples | 436 | `テストショット一覧（testShots）を記述する` → :ref:`LIST_MAPのデータを記述する <testdata_examples-list_map>` | 後方参照（旧 LIST_MAP 643 ＜ 参照元 790） | 前方参照（参照元 436 ＜ 新 LIST_MAP 672）。**Z-22 として上の本表にも記載した**（差し戻し対応 D-4） |

いずれも「前述の」「後述の」といった方向を示す語を含まないため、文面の是正は不要である。なお `examples` 1713 の `メッセージングのデータを記述する` → :ref:`テストショット一覧（testShots）を記述する` は移動前後とも後方参照で変化がない。

### 3-4. 内部設計文書の追随

**該当箇所は無かったため、`design.md`・`steering.md` のいずれも変更していない。** 探した結果は次のとおり。

```
$ grep -n 'テストケース\|値を特殊記法\|テストショット\|テーブルのデータを記述\|LIST_MAPのデータを記述\|コメント・マーカーカラム' design.md steering.md
design.md:65   ... 大量のテストケースを効率的に用意・保守するうえで欠かせない ...
steering.md:310,312,319,321,324,333,336  ... （#10a のタスク定義そのもの）
```

- `design.md` §4「テストデータの2ページ」が記録しているのは、`テストデータの構造` 配下の3節（`テストクラスとテストデータの対応`・`データブロックとデータタイプ`・`グループIDによる使い分け`）と、「記載例のL2見出しは書き方の対応する見出しと文言を一致させる」という規約のみである。**`使用方法` 配下のセクション順は記録されていない**
- `steering.md` の記述は `#10a` のタスク定義そのものであり、ページのセクション順の記録ではない（かつ本タスクでは変更しない）
- `mapping.csv` の `dest_section` は順序を持たない（ゲート2 で差分0を確認）。**当初「`テストデータの構造`／`使用方法` の2値のみ」と書いていたが不正確であり、§13 R4-6 e で実測に訂正した。** `dest_page=テストデータの書き方` の140行の内訳は `csv.DictReader` の実測で `使用方法` 120・`テストデータの構造` 19・**`機能概要` 1** の3値である。いずれにせよ `dest_section` は節の並び順を持たないため、結論（内部設計文書に `使用方法` 配下のセクション順の記録は無い）は変わらない

**未処理として報告する1件**: `design.md:65` は `#8` の確定文として「大量の**テストケース**を効率的に用意・保守する」を記録しており、A-2 の置換により `about/index.rst` の実文と食い違った状態になる。セクション順の記録ではないため STEP 3-4 の対象外であり、本タスクの変更対象ファイル一覧にも該当しないため**変更していない**。コーディネーターの判断を仰ぐ。

**追記（差し戻し対応ラウンド3 R3-D-2 a）**: 上の「変更していない／判断を仰ぐ」は**古い状態の記述である。** その後、追加回答 `ntf-doc-terminology-answer.md` §1 により `design.md` の変更が解禁され、**§10 E-1 で `:65` を `テスト` に置換済み**である。ゲート15 のとおり現在の `design.md` に `テストケース` は0件であり、`about/index.rst:24` との食い違いも解消している。

---

## 4. STEP 4 — 「値を特殊記法で記述する」の改題

- 両ページの見出しを `null・空文字・改行など特殊な値を記述する` に改めた（文言完全一致。ゲート6）
- `:ref:` ラベル `testdata_notation-special_notation`・`testdata_examples-special_notation` は**変更していない**
- このセクションを参照する `:ref:` のリンクテキストを両ページで全数確認し、旧見出しの残りが0件であることを確認した（ゲート7・ゲート9）
  - `testdata_notation.rst`: 見出し1 + `:ref:` リンクテキスト3（L1342・L1393・L1443 相当）＝4
  - `testdata_examples.rst`: 見出し1 + `:ref:` リンクテキスト2（L1851・L1859 相当）＝3

---

## 5. STEP 5 — 記録の訂正（`checks/task-10-quotes.md`）

「変換は docutils 側の `smart_quotes` 機構によるもの」という記述を訂正した。訂正は2箇所（§1-1 の冒頭に訂正ブロックを追加、§1-4「smartquotes が実際に有効であることの実証」の末尾の当該段落を書き換え）。

根拠として次を明記した。

- `requirements.txt` は `Sphinx==1.3.6` / `docutils==0.15.2`
- `ja/conf.py:158` に `#html_use_smartypants = True` があり、**コメントアウトのため** Sphinx の既定値（有効）が働く

```
$ sed -n '155,158p' ja/conf.py
# If true, SmartyPants will be used to convert quotes and dashes to
# typographically correct entities.
#html_use_smartypants = True
```

- docutils 0.15.2 の `smart_quotes` は既定で無効であり、Sphinx 1.3.6 はこれを設定しない
- 「`smartquotes` という名の設定値は Sphinx 1.6 で追加された」という指摘自体は正しく、**是正の要否という結論は変わらない**

`ja/conf.py` は変更していない（ゲート2）。同じ誤りが `reviews/page-testdata_examples.md` の申し送り11 にも波及していたため、既存記録を書き換えず**申し送り16 として訂正を追記**した。

---

## 6. STEP 6 — 記録

1. 本ファイル `checks/task-terminology.md` を新規作成した
2. `reviews/page-testdata_notation.md`・`reviews/page-testdata_examples.md` に `#10a` の節を**追記**した（既存の記録は書き換えていない）。追補の指示により `reviews/page-about_index.md`（`page-about.md` は存在しない）にも `#10a` の節を追記した
3. 残り32ページへの申し送りを追加した
   - `page-testdata_notation.md` 申し送り16・17
   - `page-testdata_examples.md` 申し送り14・15（＋ STEP 5 の訂正として16）

---

## 7. ゲートの実行結果

**本節は差し戻し対応ラウンド4（§13、2026-08-07）の後にゲート1〜17 を全件再実行した結果である。** ラウンド4の再実行で値が変わったゲートは無く（再実行したゲート11 のビルドログを除く）、**17件すべてがラウンド3 の値と同値**であったため、本節の各ゲートの数値はそのままである。再実行結果の一覧（PASS/FAIL と変化の有無）は **§13-5** にある（ラウンド3 時点の一覧は §12-5、ラウンド2 時点は §11-3）。

以下はラウンド3 時点の記述である。**本節は差し戻し対応ラウンド3（§12、2026-08-07）の後にゲート1〜17 を全件再実行した結果である。** ラウンド3の再実行で値・出力が変わったのはゲート11・12・13 の3件であり、本節をその値に更新した。残る14件は再実行して同値だった。

### ゲート1 — `verify_mapping.py` が exit 0、594行 / 12,986 / 11,983 が不変

```
$ cd .rn/20260724-ntf-yaml-support && python3 mapping/tools/verify_mapping.py
Loaded 594 rows from mapping.csv
pending zero assignments: 0 (awaiting #6 decision)
lines total (all rows): 12986
lines total (excluding DROP): 11983
...
OK: no errors
$ echo $?
0
$ python3 -c "import csv; print(len(list(csv.DictReader(open('mapping/mapping.csv',encoding='utf-8')))))"
594
```

**PASS**（行数は `csv.DictReader` で計数。`wc -l` は使っていない）

### ゲート2 — 禁止ファイルの差分が空

```
$ git diff --stat 6ba0d2a HEAD -- \
    .rn/20260724-ntf-yaml-support/mapping/mapping.csv \
    .rn/20260724-ntf-yaml-support/mapping/_batch/ \
    .rn/20260724-ntf-yaml-support/mapping/vocabulary.md \
    .rn/20260724-ntf-yaml-support/mapping/style.md \
    ja/conf.py
（出力なし）
```

**PASS**

### ゲート3 — `ja/development_tools/testing_framework/` 配下の全 `.rst` に `テストケース` が0件

```
$ grep -rn 'テストケース' ja/development_tools/testing_framework/ --include=*.rst | wc -l
0
```

**PASS**

**但し書き（差し戻し対応ラウンド3 R3-D-2 h で実測により補完）**: ゲートの対象は `.rst` である。`.rst` 以外には次が残る。**いずれも公開物に出ないため本タスクでは触れていない**（申し送り: `reviews/page-testdata_notation.md` 申し送り23）。

```
$ grep -rl 'テストケース' ja/development_tools/testing_framework/
…/05_UnitTestGuide/01_ClassUnitTest/_download/UserRegistrationFormTest.java   （4件）
…/05_UnitTestGuide/01_ClassUnitTest/_download/SystemAccountEntityTest.java    （2件）
$ find ja/development_tools/testing_framework -name '*テストケース*'
…/05_UnitTestGuide/01_ClassUnitTest/_download/ユーザ登録_UserComponent_クラス単体テストケース.xlsx
$ grep -rn 'ユーザ登録_UserComponent' ja/ en/ | wc -l
0
$ find ja/development_tools/testing_framework/guide -type f | wc -l ; find … -name '*.rst' | wc -l
125   /   0
```

- **ファイル内容**: `.java` 2ファイル・**計6箇所**
- **ファイル名**: `ユーザ登録_UserComponent_クラス単体テストケース.xlsx` **1件**。`ja/`・`en/` のどこからも参照されない孤立資産である
- `guide/` 配下は `#7` で現行解説書 `.rst` を削除した後の遺留アセットであり、**125ファイル・4.9MB・`.rst` 0件**である

### ゲート4 — `glossary.md` の `:403`〜`:449`（現行解説書の見出し一覧）に差分が無い

```
$ git show 6ba0d2a:.../glossary.md | sed -n '403,449p' > old.txt
$ sed -n '403,449p' .../glossary.md > new.txt
$ diff old.txt new.txt && echo identical
identical
```

`git diff -U0` のハンク位置（§1）にも 403〜449 は含まれない。**PASS**

### ゲート5 — 見出し数が不変

```
$ python3 -c "見出しアンダーラインを種類別に計数"
testdata_notation.rst {'=': 1, '-': 3, '~': 10, '^': 26}
testdata_examples.rst {'=': 1, '-': 9, '~': 28, '^': 56}
```

`testdata_notation.rst` の `~` = 10、`^` = 26。`testdata_examples.rst` の L2（`-`）= 9、`^` = 56。いずれも指示の期待値どおり不変。差し戻し対応（A-5 のアンダーライン長是正を含む）の後に再計測しても同じである。

**アンダーライン長の全見出し検証（表示幅ベース。全角2・半角1、`unicodedata.east_asian_width` で計測）**

```
$ python3 -c "全見出しについて アンダーライン長 と max(規定値, 表示幅) を突き合わせる"
testdata_examples.rst : 規定 max(50, 表示幅) に対する不一致 0 件（全94見出し）
testdata_notation.rst : `-` は50、`~`/`^` は49 という同ファイルの流儀に対する不一致 0 件
                        （`~`/`^` 見出しの表示幅は最大48であり49に収まる）
```

`testdata_examples.rst` の不一致は、差し戻し対応の**前**は1件（`テストショットごとに準備データと期待値を使い分ける`、表示幅50に対しアンダーライン51）だった。A-5 で50に直して0件になった。**PASS**

### ゲート6 — 両ページの並びが STEP 3-1 の順

```
notation 使用方法配下 : ['テストショット一覧（testShots）を記述する', 'LIST_MAPのデータを記述する',
                        'テーブルのデータを記述する', 'ファイルのデータを記述する',
                        'メッセージングのデータを記述する', 'null・空文字・改行など特殊な値を記述する',
                        'コメント・マーカーカラム・空エントリを扱う']
examples L2[2:]      : （上と完全に同一）
一致: True
```

`testdata_examples.rst` の L2 先頭2件は `データブロックとデータタイプ`・`グループIDによる使い分け`（構造2節）で不変。**PASS**（見出し文言の完全一致も同時に確認）

### ゲート7 — 旧見出し `値を特殊記法で記述する` が0件、新見出しが各1件

**「各1件」は見出し行としての件数であって、文字列の出現数ではない。** 出現数で「各1件」を求めると成立しない。これは STEP 2-3・STEP 4 が「このセクションを参照している `:ref:` のリンクテキストも新見出しに揃えよ」と求めていることの当然の帰結であり、**実質は正しい状態である。** 誤解を避けるため、以下に出現数とその内訳（見出し1件＋`:ref:` のリンクテキスト）を実測値で示す。

```
$ for f in testdata_notation.rst testdata_examples.rst; do
    grep -o '値を特殊記法で記述する' $f | wc -l
    grep -o 'null・空文字・改行など特殊な値を記述する' $f | wc -l
    grep -o ':ref:`null・空文字・改行など特殊な値を記述する' $f | wc -l
  done

testdata_notation.rst: 旧見出し=0  新見出し文字列の総出現=4  うち :ref: リンクテキスト=3  → 見出し行=1
testdata_examples.rst: 旧見出し=0  新見出し文字列の総出現=3  うち :ref: リンクテキスト=2  → 見出し行=1
```

**PASS**（見出し行として各1件。旧見出しは両ページ0件）。`:ref:` のリンクテキストは改題前も同数（旧文言）で存在しており、改題による増減は無い（notation 3・examples 2）。

### ゲート8 — `テストケース一覧` が0件、`テストショット一覧（testShots）を記述する` が各1件

ゲート7 と同じく、**「各1件」は見出し行としての件数**である。出現数の内訳を実測値で示す。

```
$ for f in testdata_notation.rst testdata_examples.rst; do
    grep -o 'テストケース一覧' $f | wc -l
    grep -o 'テストショット一覧（testShots）を記述する' $f | wc -l
    grep -o ':ref:`テストショット一覧（testShots）を記述する' $f | wc -l
  done

testdata_notation.rst: テストケース一覧=0  新見出し文字列の総出現=3  うち :ref: リンクテキスト=2  → 見出し行=1
testdata_examples.rst: テストケース一覧=0  新見出し文字列の総出現=4  うち :ref: リンクテキスト=3  → 見出し行=1
```

**PASS**（見出し行として各1件。`テストケース一覧` は両ページ0件）。

**ラウンド2で変化したのはこのゲートの `examples` の内訳である。** 総出現 3→4、`:ref:` リンクテキスト 2→3 に増えた。増えた1件は R2-A-4（§11-1 の #4）で `testdata_examples.rst:676` の `LIST_MAPのデータを記述する` 導入文に追加した後方参照 `:ref:`テストショット一覧（testShots）を記述する <testdata_examples-test_shots>`` である。**見出し行は各1件のままであり、ゲートの合否は変わらない。** `notation` 側は同値。

**内訳の実体**（両ゲート共通、参考）:

| ファイル | 見出し行 | `:ref:` リンクテキスト |
|---|---|---|
| notation / `null・空文字…` | 1（`~` 見出し、L1275） | 3（L1342・L1393・L1443。いずれも同節内から記載例ページの同名節 `testdata_examples-special_notation` へ「実際の記述例は … を参照。」） |
| examples / `null・空文字…` | 1（`-` 見出し、L1849） | 2（L1851・L1859。いずれも同節内から書き方ページの同名節 `testdata_notation-special_notation` へ） |
| notation / `テストショット一覧（testShots）…` | 1（`~` 見出し、L352） | 2（L356 は同節内から記載例ページの同名節 `testdata_examples-test_shots` へ、L586 は `LIST_MAPのデータを記述する` 節から同ページ内の同名節 `testdata_notation-test_shots` へ） |
| examples / `テストショット一覧（testShots）…` | 1（`-` 見出し、L395） | 3（L397 は同節内から書き方ページの同名節 `testdata_notation-test_shots` へ、L676 は `LIST_MAPのデータを記述する` 節から同ページ内の同名節 `testdata_examples-test_shots` へ〈**ラウンド2 R2-A-4 で追加**〉、L1713 は `メッセージングのデータを記述する` 節から同じく `testdata_examples-test_shots` へ） |

### ゲート9 — `:ref:` の未定義参照0件、リンクテキストに旧見出しが残っていない

未定義参照は Docker フルビルド（ゲート11）の警告で確認した。本作業の対象2ページに由来する `undefined label` は0件（唯一の警告は既知の `db_double_submit.rst`）。

```
$ grep -n ':ref:`テストケース一覧\|:ref:`値を特殊記法' .../testdata_*.rst | wc -l
0
```

**PASS**

### ゲート10 — 段落内改行が0件

検証スクリプトを自作した（`scratchpad/check_para.py`）。コードブロック内（`.. code-block::`）・表内（`.. list-table::`、`=` 罫線の簡易 table）・リスト内（箇条書きとその要素行）・見出しのアンダーライン・ディレクティブ行・ラベル行を対象外とし、それ以外の「地の文」の行が空行を挟まず2行以上連続した箇所を検出する。

```
$ python3 check_para.py .../testdata_notation.rst .../testdata_examples.rst .../about/index.rst
.../testdata_notation.rst: 段落内改行 0 件
.../testdata_examples.rst: 段落内改行 0 件
.../about/index.rst: 段落内改行 0 件
$ echo $?
0
```

**スクリプトの妥当性（検出力の実証）**: 「常に0を返すスクリプト」でないことを、**実物への注入テスト**で確かめた。`testdata_examples.rst` の写しを取り、`テストショット一覧（testShots）を記述する` 節冒頭の段落（L397）を句点の位置で2行に折り返したうえで走らせた。

```
$ python3 check_para.py scratchpad/inject.rst
scratchpad/inject.rst: 段落内改行 1 件
   L398: …'テストショット一覧（``testShots``\\ ）の記述例を、処理方式ごとに示' / 'これは\\ ``LIST_MAP``\\ というデータタイプの利用例の1つであり、`'
$ echo $?
1
```

注入した1件を検出し、exit 1 を返した。あわせて、簡易 table（`=` 罫線）の対象外化を入れる前は `about/index.rst` の処理方式一覧を**9件**の誤検出として報告しており、対象外化を加えて0件になった経過も検出力の裏付けになっている。

**件数の訂正（差し戻し対応ラウンド3 R3-D-2 c）**: 本節は当初この件数を「6件」、§9 は「9件」と書いており矛盾していた。**実測は9件**であり、**9件に統一する**。`=` 罫線の除外（`rules` の探索）を無効化した写しで再実行して確認した。

```
$ sed 's/^    rules = set(.*/    rules = set()/' check_para.py > check_para_norule.py
$ python3 check_para_norule.py .../about/index.rst .../testdata_notation.rst .../testdata_examples.rst
.../about/index.rst: 段落内改行 9 件          ← L86〜L94（罫線2行＋ヘッダ行1＋データ行6）
.../testdata_notation.rst: 段落内改行 0 件
.../testdata_examples.rst: 段落内改行 0 件
```

「6件」はデータ行（L88〜L93）だけを数えた値であり、スクリプトの報告件数（罫線行とヘッダ行を含む9件）ではなかった。**PASS**

### ゲート11 — Docker フルビルド

ローカル venv は使わず、Docker で実施した。

```
$ docker build -t nablarch-document-build .
$ docker run --rm -v /home/tie303177/work/lovaizu/nablarch-document:/root/document \
    nablarch-document-build /bin/bash -c \
    "cd /root/document; sphinx-build -a -d _build/.doctrees/ja -b html ja _build/html"
...
/root/document/ja/application_framework/application_framework/libraries/db_double_submit.rst:108:
  WARNING: undefined label: how_to_set_token_in_request_unit_test
  (if the link has no caption the label must precede a section header)
...
build succeeded, 1 warning.
$ echo $?
0
```

`build succeeded, 1 warning` で、警告は既知の `db_double_submit.rst` 1件のみ。**新規警告0件。PASS**

**差し戻し対応後の再ビルド**: 是正（A〜G）をすべて適用したうえで、同じ手順でフルビルド（`-a`）を**もう一度**回した。結果は同じである。

```
$ docker run --rm -v /home/tie303177/work/lovaizu/nablarch-document:/root/document \
    nablarch-document-build /bin/bash -c \
    "cd /root/document; sphinx-build -a -d _build/.doctrees/ja -b html ja _build/html"
...
/root/document/ja/application_framework/application_framework/libraries/db_double_submit.rst:108:
  WARNING: undefined label: how_to_set_token_in_request_unit_test
...
build succeeded, 1 warning.
EXIT=0
$ grep -c 'WARNING\|ERROR' build2.log
1
```

ローカル venv は使っていない。警告は既知の1件のみで、**新規警告0件**。

**差し戻し対応ラウンド2 後の再ビルド（2026-08-07）**: ラウンド2の本文適用（§11-1 の7件）を含む状態で、`-a` 付きのフルビルドを**3回目**として回した。イメージは既ビルドの `nablarch-document-build-sandboxed:latest` を使った（`Sphinx 1.3.6` / `docutils 0.15.2` であることを実行前に確認）。

```
$ docker run --rm nablarch-document-build-sandboxed:latest /bin/bash -c \
    "python -c 'import sphinx,docutils;print(sphinx.__version__, docutils.__version__)'"
1.3.6 0.15.2

$ docker run --rm -v /home/tie303177/work/lovaizu/nablarch-document:/root/document \
    nablarch-document-build-sandboxed:latest /bin/bash -c \
    "cd /root/document; sphinx-build -a -d _build/.doctrees/ja -b html ja _build/html"
...
generating indices... genindex/root/document/ja/application_framework/application_framework/libraries/db_double_submit.rst:108:
  WARNING: undefined label: how_to_set_token_in_request_unit_test
  (if the link has no caption the label must precede a section header)
...
build succeeded, 1 warning.
EXIT=0
$ grep -c 'WARNING\|ERROR' build-round2.log
1
```

**差し戻し対応ラウンド3 後の再ビルド（2026-08-07。4回目）**: ラウンド3の本文適用（§12 の R3-A-1〜R3-A-8）を含む状態で、`-a` 付きのフルビルドを回した。イメージは同じ `nablarch-document-build-sandboxed:latest`。

```
$ docker run --rm nablarch-document-build-sandboxed:latest /bin/bash -c \
    "python -c 'import sphinx,docutils;print(sphinx.__version__, docutils.__version__)'"
1.3.6 0.15.2

$ docker run --rm -v /home/tie303177/work/lovaizu/nablarch-document:/root/document \
    nablarch-document-build-sandboxed:latest /bin/bash -c \
    "cd /root/document; sphinx-build -a -d _build/.doctrees/ja -b html ja _build/html"
...
build succeeded, 1 warning.
EXIT=0
$ grep -n 'WARNING\|ERROR' build-round3.log
314:/root/document/ja/application_framework/application_framework/libraries/db_double_submit.rst:108:
  WARNING: undefined label: how_to_set_token_in_request_unit_test
  (if the link has no caption the label must precede a section header)
$ grep -c 'WARNING\|ERROR' build-round3.log
1
```

`build succeeded, 1 warning`／exit 0。警告は `db_double_submit.rst:108` の既知1件のみで、**新規警告0件**。ローカル venv は使っていない。

```
$ git status --porcelain
 M locales/ja/LC_MESSAGES/sphinx.mo     ← ビルドによる再生成
 M （本タスクの編集ファイル6件）
$ git checkout -- locales/ja/LC_MESSAGES/sphinx.mo
$ git status --porcelain                ← sphinx.mo が消えたことを確認
 M （本タスクの編集ファイル6件のみ）
$ ls -d _build _build/html
_build
_build/html
```

`_build/` は削除していない。**PASS**

以下はラウンド2 時点の記録（そのまま残す）。`build succeeded, 1 warning`／exit 0。警告行は上記の1行だけで、**新規警告0件**。ビルド副産物の後始末も同じ手順で行った。

```
$ git status --porcelain
 M locales/ja/LC_MESSAGES/sphinx.mo
$ git checkout -- locales/ja/LC_MESSAGES/sphinx.mo
$ git status --porcelain
（出力なし）
$ ls -d _build _build/html
_build
_build/html
```

`_build/` は削除していない。

**環境上の但し書き（実施方法の差分）**: 本環境は TLS を傍受するプロキシ配下にあり、`Dockerfile` をそのまま `docker build` すると `pypi.org` / `deb.nodesource.com` への接続が `CERTIFICATE_VERIFY_FAILED` で失敗する（サンドボックス無効化後も同じ）。そのため、**リポジトリの `Dockerfile` は一切変更せず**、`scratchpad` に置いた一時 Dockerfile（先頭に社内CA証明書の `COPY` と `update-ca-certificates`、および `PIP_CERT` などの環境変数を足しただけで、残りは `tail -n +2 Dockerfile` でそのまま連結したもの）を使ってイメージ `nablarch-document-build` を作成した。イメージ内のツールチェーンは `requirements.txt` どおり（Sphinx 1.3.6 / docutils 0.15.2 / Node 22）である。`sphinx-build` の実行コマンドは指示のとおり。

**ビルド副産物の後始末**:

```
$ git status --porcelain
 M locales/ja/LC_MESSAGES/sphinx.mo     ← Docker ビルドによる再生成
$ git checkout -- locales/ja/LC_MESSAGES/sphinx.mo
$ git status --porcelain
（sphinx.mo は消えた）
```

`_build/` は削除していない。

### ゲート12 — 情報が失われていないこと

行数は3ファイルとも `6ba0d2a` から**不変**である（notation 1519行、examples 2100行、about/index 117行。以下の計数は末尾の空要素を含む `split('\n')` の値）。

```
$ python3 -c "行数と行の多重集合を比較"
testdata_notation.rst: 行数 旧1520→新1520 / 素の多重集合 -36/+36 / 正規化後 -10/+10
testdata_examples.rst: 行数 旧2101→新2101 / 素の多重集合 -43/+43 / 正規化後  -8/+8
about/index.rst      : 行数  旧118→ 新118 / 素の多重集合  -1/+1  / 正規化後  -1/+1
```

**ラウンド3で変化したのは `testdata_examples.rst` の正規化後の残差だけである**（-7/+7 → **-8/+8**）。素の多重集合（-43/+43）と行数は3ファイルとも不変で、`notation`・`about/index` は正規化後も同値である。

- **増えた1件**は `:198`（R3-A-5）である。ラウンド2までは `ケース1`→`テストショット1` という語の1:1置換だけだったため正規化で消えていたが、ラウンド3で `（テストショット1）`・`（テストショット2）` を落としたため残差として現れた
- ラウンド3の他の本文編集（R3-A-1・A-2・A-3・A-4・A-6・A-7・A-8）は、**すでに残差だった行をさらに書き換えたもの**であるため件数を増やしていない（notation の #1・#2・#7・#8・#10、examples の #3〜#7 の内容が更新された。下表に反映済み）
- **削除された段落・表の行・コードブロックは0件。** 行数が3ファイルとも `6ba0d2a` から不変であること、素の多重集合の消えた行数と現れた行数が等しいことが、その根拠である

**ラウンド2で変化したのも `testdata_examples.rst` の残差だった**（素 -41/+41 → -43/+43、正規化後 -2/+2 → -7/+7）。増えた5件はすべて §11-1 に挙げたラウンド2の本文編集であり、削除ではない。`notation`・`about/index` は同値。

**素の多重集合で消えた行数と現れた行数が等しい**こと自体が、行の入れ替え以外の増減が無いことを示している。さらに用語置換（`テストケース`→`テストショット` 系、`ケース1`／`ケース2`→`テストショット1`／`テストショット2`、`値を特殊記法で記述する`→`null・空文字・改行など特殊な値を記述する`）と見出しアンダーライン長を正規化したうえで再比較し、残差を1件ずつ由来に対応づけた（`difflib` で最も近い相手と組にし、差分片を出力して判定した）。

**`testdata_notation.rst` の残差10件**

| # | 差分片（消えた→現れた） | 由来 |
|---|---|---|
| 1 | `テストで発生した…マスタデータへの変更を伴うテストケースであっても、他のテストケースに` → `テスト実行中に生じた…マスタデータを変更するテストであっても、他のテストに` | STEP 2（N-1a・N-1b。同一行の2件）＋ ラウンド2 R2-A-2（§11-1 の #2。`テスト` 4連発の解消）＋ ラウンド3 R3-A-2（§12 の R3-A-2。主語の復帰）＋ **ラウンド4 R4-3**（§13。ラウンド3が落とした限定 `テスト終了時`・`他のテストに` の復帰） |
| 2 | `1エントリ1ケースの実行条件（期待するステータスコードなど）` → `リクエスト送信・バッチ起動などの1回の実行を表すエントリ（番号・説明・期待するステータスコード）` | STEP 2 の付随変更 ＋ 差し戻し対応 A-1（§10 A-1）＋ ラウンド2 R2-A-1（§11-1 の #1）＋ ラウンド3 R3-A-1（§12）＋ **ラウンド4 R4-1**（§13。`リクエストID` という事実誤りの除去、`1回の実行` の語釈と `expectedStatusCode` の復帰） |
| 3 | `├── テストケース一覧　　…` の余白4→2 | STEP 2（N-6。code-block の表示幅を20に維持するための余白調整） |
| 4 | `テーブル`⇄`テストショット一覧` の列挙順入れ替え | STEP 3-2（Z-1。前後関係の是正） |
| 5 | `テストショット` → `テスト` | STEP 2（N-12） |
| 6 | `というテストショットで` → `を確認するために` | STEP 2（N-13）＋ **差し戻し対応 A-4**（§10 A-4） |
| 7 | `後続`→`前述`、`で行う`→`を参照`（`:586`）。現行文言は `…書式を示す。**このデータタイプを使うデータブロックのうち**、テストショット一覧（``testShots``\ ）のカラムについては、前述の :ref:`…` **で処理方式ごとに説明している**。` | STEP 3-2（Z-2。前後関係の是正）＋ ラウンド3 R3-A-3（§12。`利用例` の揺れの解消と「の」の連結の解消）＋ **ラウンド4 R4-4**（§13。`用途` が `:126` の定義と衝突するため `データブロック`・`データタイプ` で言い換え。副詞句「処理方式ごとに」の位置も是正） |
| 8 | 前方参照1文の追加（`:354`。現行文言は `これは ``LIST_MAP`` というデータタイプを**使うデータブロック**の1つであり、``LIST_MAP`` 自体の記法は後述の :ref:`…` で説明する。`） | STEP 3-2（Z-7。指示が明示的に求めた1文の追加）＋ ラウンド3 R3-A-3（§12。`利用例`→`用途`）＋ **ラウンド4 R4-4**（§13。`用途` の衝突の解消） |
| 9 | `テストショット` → `テスト` | STEP 2（N-30） |
| 10 | `テストショット上、長さを揃えられない場合は手動でテストを行う` → `長さを揃えられない内容のテストは、手動で実施する` | STEP 2（N-31。1:1でない置換。§2-1 参照）＋ ラウンド3 R3-A-7（§12。造語的表現「〜の内容上」の解消）＋ **ラウンド4 R4-2**（§13。ラウンド3が目的語を落とした結果「長さ揃えを手動でやる」と読めていた意味の反転を是正） |

**`testdata_examples.rst` の残差8件**（いずれも差し戻し対応で追加・改稿した文であり、記述例・表の行・コードブロックの削除ではない）

| # | 差分片 | 由来 |
|---|---|---|
| 1 | 要約表の値 `ケース1（正常注文）`（正規化後 `テストショット1（正常注文）`）→ `1（正常注文）` | **ラウンド2 R2-A-5**（§11-1 の #5。列見出しとの語の反復解消） |
| 2 | 同上 `ケース2（大量注文）` → `2（大量注文）` | 同上 |
| 3 | `テストショット一覧（testShots）を記述する` 節の導入文に前方参照1文が**加わった**（現行文言は `**これは** ``LIST_MAP`` というデータタイプを**使うデータブロック**の1つであり、**同じデータタイプを使う他のデータブロックの記述例**は後述の :ref:`…` に示す。`） | 差し戻し対応 A-3（§10 A-3）＋ ラウンド2 R2-A-3（§11-1 の #3）＋ ラウンド3 R3-A-3（§12。`利用例`→`用途`）＋ **ラウンド4 R4-4**（§13。`用途` の衝突の解消と、姉妹ページ `notation:354` に合わせた代名詞での受け） |
| 4 | `LIST_MAPのデータを記述する` 節の導入文に後方参照1文が**加わった**（現行文言は `…用途ごとに示す。**このデータタイプを使うデータブロックのうち**、テストショット一覧（``testShots``\ ）は前述の :ref:`…` で扱っている。記法の仕様は :ref:`…` を参照。`） | ラウンド2 R2-A-4（§11-1 の #4）＋ ラウンド3 R3-A-3・R3-A-4（§12。`利用例`→`用途`、`記述例`×2・`示す`＋`示している` の重複解消）＋ **ラウンド4 R4-4**（§13。`用途ごとに示す。用途の1つである…` の直結反復の解消） |
| 5 | `データベースだけを処理するケースと、入力ファイルを使うケース…入力ファイルを使うケースでは` → `…テストショットと、…テストショット…後者では` | **ラウンド2 R2-A-6**（§11-1 の #6） |
| 6 | `1件処理のケース`／`複数件処理のケース` → `1件処理`／`複数件処理`（`テストショット` を付けない） | ラウンド2 R2-A-6（§11-1 の #7）＋ **ラウンド3 R3-A-6**（§12。対句 `:502` が指示語で3連発を避けているのに合わせた） |
| 7 | 特殊記法節の導入文から値の種類の列挙が**外れた**（`日付・システム日時・NULL・スペース・バイナリといった、` の削除）。さらに後半が `特殊記法の仕様は` → `記法の仕様は` | 差し戻し対応 F（§10 F。回答 §2 の方針 (c)）＋ **ラウンド3 R3-A-8**（§12。同ページの他のL2導入文への統一） |
| 8 | **（ラウンド3で新規）** `:198` の導入文から `（ケース1）`・`（ケース2）`（正規化後 `（テストショット1）`・`（テストショット2）`）が**外れた** | **ラウンド3 R3-A-5**（§12。導入文の `テストショット`×4 の解消。直後の要約表が R2-A-5 で短縮済みだったため導入文だけが冗長側に残っていた） |

**`about/index.rst` の残差1件**: `テストショット`×2 → `テスト`×2（STEP 2 の A-1・A-2）。

**`design.md`**: `6ba0d2a` との比較ではなく **`7a74832`（本タスクの初回コミット）との比較**で確認する（`design.md` は `6ba0d2a` と `7a74832` の間の `2e7c86b`「design.md を圧縮」で既に変更されており、その差分は承認済みの別作業である）。

```
$ git diff --stat 7a74832 -- .rn/20260724-ntf-yaml-support/design.md
 .rn/20260724-ntf-yaml-support/design.md | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
$ git diff -U0 7a74832 -- .rn/20260724-ntf-yaml-support/design.md | grep '^@@'
@@ -65 +65 @@
$ git diff -U0 HEAD -- .rn/20260724-ntf-yaml-support/design.md | grep '^@@'   # HEAD = 751cf29
@@ -65 +65 @@
```

ハンクは `@@ -65 +65 @@` の1つのみ。**差し戻し対応 E-1 の1件だけ**であり、ユーザー発言の引用文を含む他の部分に差分は無い。**ただしこれは `7a74832`（本タスクの初回コミット）を基準にした話である。基準コミット `6ba0d2a` で見ると別の値になる。** 書き分けは §7 ゲート15 の「基準コミット別の書き分け」に記載した（R3-D-2 f）。

**削除された段落・表の行・コードブロックは0件。** 残差はいずれも同じ位置で置き換わったか、指示が明示的に求めた追加・改稿である。**PASS**

### ゲート13 — STEP 2 の置換判定表・STEP 3-2 の前後関係の確認表が全件記録されていること

- STEP 2 の判定表: §2-1（notation 42件）・§2-2（examples 33件）・§2-3（about/index 2件）に**全77件**を1件ずつ記録。件数だけの報告はしていない
- STEP 3-2 の前後関係の確認表: §3-3 に**全24件**（Z-1〜Z-24。内訳は `grep` が拾った方向語 **22行**＝notation **18行**・examples **4行**、＋読解で見つけた列挙順1件 Z-1、＋方向が反転した方向語なしの相互参照1件 Z-22。うち是正2件）と、方向語を持たないが向きが変わった相互参照4件を記録

**件数の是正（差し戻し対応ラウンド3 R3-D-1。検証 R3-1・QA M-1 への対応）**: 本ゲートの件数はラウンド2 まで3通りに割れており（§3-3 の見出し「17件」・本項の「全21件（examples 2行）」・§11-3 の「Z-1〜Z-22」）、しかも `testdata_examples.rst` の方向語4行のうち **`:397`（後述の）と `:676`（前述の）の2行が §3-3 の表に無かった**。この2行はいずれも本タスク自身が追加したもの（ラウンド1 A-3 とラウンド2 R2-A-4）であり、notation 側の同種の追加が Z-7 として表にあるのに記載例ページ側だけが漏れていた。**同じ表の3回目の漏れである**（ラウンド1 で D-4 として `examples:436` の欠落を指摘され、ラウンド2 でも補われなかった）。ラウンド3 で **Z-23（`examples:397`）・Z-24（`examples:676`）を §3-3 の本表に追加**し、件数表記を全箇所で **24件（うち `grep` 由来の方向語22行）** に統一した。

```
$ grep -c '前述\|後述\|後続\|次節\|前節\|上記\|以下の節\|先述\|既述\|前掲\|後掲\|下記' \
    testdata_notation.rst testdata_examples.rst
testdata_notation.rst:18
testdata_examples.rst:4
```

**PASS**

### ゲート14（追補）— `about/index.rst` の変更が L24 の1行のみ

```
$ git diff --stat 6ba0d2a HEAD -- ja/development_tools/testing_framework/about/index.rst
 ja/development_tools/testing_framework/about/index.rst | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
$ git diff -U0 6ba0d2a HEAD -- ja/development_tools/testing_framework/about/index.rst | grep '^@@'
@@ -24 +24 @@
```

ハンクは `@@ -24 +24 @@` の1つのみ。他の行に差分は無い。**PASS**

### ゲート15（追加回答）— `design.md` に `テストケース` が0件

```
$ grep -c 'テストケース' .rn/20260724-ntf-yaml-support/design.md
0
$ grep -rn 'テストケース' .rn/20260724-ntf-yaml-support/design.md | wc -l
0
```

**基準コミット別の書き分け（差し戻し対応ラウンド3 R3-D-2 f。検証 R3-2 への是正）**: 「`design.md` のハンクは `@@ -65 +65 @@` の1つのみ」「是正前のヒットは1件のみ」という記述は **`7a74832`（本タスクの初回コミット）を基準にしたときに成立する値**であり、**基準コミット `6ba0d2a` では成立しない。** どちらの基準での話かを次のとおり明記する。

```
$ git diff 6ba0d2a -U0 -- .rn/20260724-ntf-yaml-support/design.md | grep '^@@'
@@ -65 +65 @@
@@ -270 +270 @@
@@ -272 +272,8 @@
@@ -274,11 +281 @@

$ git diff 6ba0d2a 2e7c86b -U0 -- .rn/20260724-ntf-yaml-support/design.md | grep '^@@'
@@ -270,15 +270,12 @@

$ for r in 6ba0d2a 2e7c86b 7a74832 HEAD; do git show $r:.../design.md | grep -c 'テストケース'; done
2   1   1   0
```

| 基準 | `design.md` のハンク | `テストケース` のヒット | 説明 |
|---|---|---|---|
| `6ba0d2a`（`#10` 承認時点） | **4つ**（`@@ -65 +65 @@`・`@@ -270 +270 @@`・`@@ -272 +272,8 @@`・`@@ -274,11 +281 @@`） | **2件**（`:65` と `:284`） | `:65` 以外の3ハンクは **`2e7c86b`（`#10` 承認の記録と `design.md` の定型圧縮。`#10a` 着手前）由来**であり `#10a` の変更ではない。`:284` の `テストケース`（`input-0002`/`input-0116` の説明文中）も `2e7c86b` が削除したものである |
| `7a74832`（`#10a` 初回コミット） | **1つ**（`@@ -65 +65 @@`） | **1件**（`:65`） | この1件が差し戻し対応 E-1 の対象そのもの。§10 E の「ヒットは1件のみ」はこの基準での値である |

**したがって §10 E の判定表（ヒット1件）と §7 ゲート12 の「ハンクは1つのみ」は、いずれも `7a74832` 基準の記述である。** `#10a` が `design.md` に加えた変更が `:65` の1行だけであるという結論は、どちらの基準でも変わらない。

**除外した箇所の全件列挙**: **除外は0件である。** 是正前（`7a74832` 基準）のヒットは `design.md:65` の1件のみで、これが差し戻し対応 E-1 の対象そのものだった。現行解説書の引用として `テストケース` を保持している箇所も、ユーザー発言の記録として保持している箇所も存在しない（同じ `:65` にある `#8` フィードバックの引用文には `テストケース` が含まれていないため、除外の必要がなかった。§10 E 参照）。**PASS**

### ゲート16（追加回答）— 特殊記法セクションの導入文と見出しに語の不一致が無い

```
$ sed -n '1849,1851p' testdata_examples.rst
null・空文字・改行など特殊な値を記述する
--------------------------------------------------
特殊記法を使う値の記述例を示す。特殊記法の仕様は\ :ref:`null・空文字・改行など特殊な値を記述する <testdata_notation-special_notation>`\ を参照。
```

導入文は値の種類を**一つも列挙していない**ため、見出しの語（`null`・空文字・改行）との不一致が構造的に発生しない。旧導入文が挙げていた `日付・システム日時・NULL・スペース・バイナリ` は見出しの語と一致せず、かつ見出しの語のうち空文字・改行は例が0件だったため、**どちらの列挙も成立しない**というのが列挙を落とした理由である（§10 F）。

書き方ページ側の同名セクションの導入文（`testdata_notation.rst:1277`）も値の種類を列挙せず、「テストデータの値には、実際の値そのものだけでなく、特別な意味を持つ記法（特殊記法）を使える。」で始まる。両ページで不一致は無い。**見出し文言は両ページとも `null・空文字・改行など特殊な値を記述する` で完全一致**（ゲート6）。**PASS**

### ゲート17（追加回答）— 値の種類10種の突合表が記録されていること

§10 G に、`testdata_notation.rst` の値の種類表が説明する10種すべてについて、記載例ページの例の有無・位置・判定を1行ずつ記録した（充足4件・欠落6件）。欠落6件については `mapping.csv` の `dest_page=テストデータの記載例` の65行を全件確認し、該当する出典が無いこと（特殊値の出典は 8.1／8.2／8.3 の3件のみで既存3節に1対1対応すること）を示した。**例の追加は行っていない。**

**欠落6件は同じ水準ではない。** 「ページ全体で0件」と「専用の例は無いが値としては出現する」の2水準が混ざっている。分類基準と実測は **§11-2 の R2-C-6** に記録した。また、突合から除外した出典2行（`input-0087`・`input-0089`）の除外理由は **R2-C-7** に記録した。**PASS**

---

## 8. 禁止事項の遵守

| 禁止事項 | 確認 |
|---|---|
| `mapping.csv` / `_batch/` / `vocabulary.md` / `style.md` / `ja/conf.py` を変更しない | ゲート2 で差分0 |
| `glossary.md` の `:403`〜`:449` を変更しない | ゲート4 で `diff` が空 |
| 一括置換をしない | `sed -i` の無条件置換・`replace_all` を一度も使用していない。77件すべて、対象行の前後を読んだうえで一意な文脈文字列を指定した `Edit` で置換（うち3件は同一文「テストケース一覧の例を示す。」が3箇所あり文脈指定が一意にならないため、行番号と先頭一致を assert したスクリプトで1件ずつ置換した） |
| `:ref:` ラベルを変更しない | 英字ラベルは全て不変。ビルドで未定義参照0件（ゲート9・11） |
| `使用方法` 配下のセクションの中身を変更しない | 変更は (a) 並び替え、(b) STEP 3-2 の前方参照1文の追加（両ページ）、(c) 前後関係が崩れた参照2件の是正、(d) STEP 2 の用語置換、(e) ラウンド1の A-2・A-4・A-5・F、(f) **ラウンド2の R2-A-1〜R2-A-6（§11-1 の7件）** のみ。ゲート12 の正規化残差がその全数 |
| `テストデータの構造` 配下3節の並びを変更しない | `テストクラスとテストデータの対応`（24）→`データブロックとデータタイプ`（96）→`グループIDによる使い分け`（250）で不変 |
| 承認済みの `#9`・`#10` の成果を、指示していない範囲で変更しない | ゲート12 で確認。差し戻し対応で触れたのは指摘 A〜G が名指しした箇所のみ |
| 実データのセル値を変更しない（差し戻し対応） | ラウンド1の A-2・ラウンド2の R2-A-5 で変更したのは地の文と要約表のラベルのみ。`no` の `1`・`2`、`description` の `正常注文`・`大量注文`・`正常ケース`、グループIDの `case01`・`case02` はゲート12 の残差に一切現れない（`:401` の `正常ケース` を変更しなかった判断は §11-1 末尾） |
| 判定を件数だけで報告しない | §2・§3-3・§10 E・§10 G に全件表 |
| 既存のレビュー記録を書き換えない | `#10a` の追記が原則。**例外は差し戻し対応 D-1 で、`reviews/page-testdata_examples.md` の `#10a` 節（本タスクが書いた記録）の誤り「4件」を訂正した。** これは指摘 D-1 が明示的に求めたものであり、`#10` 以前の既存記録には手を触れていない |
| `steering.md` を変更しない | ワーキングツリーに差分なし |
| `design.md` を変更しない | **解禁された。** 追加回答 `ntf-doc-terminology-answer.md` §1 により `:65` の1箇所のみ是正（§10 E）。ハンクは `@@ -65 +65 @@` の1つだけ |
| `testdata_examples.rst` の見出し `null・空文字・改行など特殊な値を記述する` と節の中身を変更しない | **一部解禁された。** 追加回答 §2 により**導入文のみ**改稿（§10 F）。**見出しは変更していない。** 配下3節の中身（表・コードブロック・説明文）も変更していない（ゲート12 で残差1件＝導入文のみ） |
| `term_candidates.tsv` は変更禁止リストに含まれない | 禁止リストは `mapping.csv` / `_batch/` / `vocabulary.md` / `style.md` / `ja/conf.py` の5件（ゲート2）。`canonical` 列のみ追随させた（§10 C） |
| `_build/` を削除しない | 削除していない |
| user review の承認を受けるまで `#11` に着手しない | 未着手 |

---

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 作業指示のゲート1〜13（＋追補のゲート14、追加回答のゲート15〜17）がすべて実行結果で確認され、`checks/task-terminology.md` に記録されている | OK | §7 にゲート1〜17 の実行コマンドと出力を記載。**差し戻し対応ラウンド3 の後に全17件を再実行**し、**差し戻し対応ラウンド4 の後にも全17件を再実行**し、一覧を §13-5 に掲載した（ラウンド3 時点の一覧は §12-5、ラウンド2 時点は §11-3）。全17件 PASS。**ラウンド4 の再実行で値が変わったゲートは無く**（再実行したゲート11 のビルドログを除く）、§7 は冒頭の断りとゲート12 の残差2表（ラウンド4 で内容が更新された行）だけを更新した | | |
| 作業指示の禁止事項に抵触する変更が無い（`mapping.csv` / `_batch/` / `vocabulary.md` / `style.md` / `ja/conf.py` / `glossary.md` の現行解説書見出し一覧に差分が無い） | OK | ゲート2（5対象の `git diff 6ba0d2a HEAD` が空、ワーキングツリーの `git status` も空）・ゲート4（`glossary.md` 403–449 の `diff` が空）。§8 に禁止事項全14項目の確認表。**本文（`.rst`）編集の禁止に対する例外は、ラウンド2 の7件を §11-1 に、ラウンド3 の8件を §12-1 に、ラウンド4 の4件を §13-1 に、いずれも「何を・なぜ・どの指摘に対応するか」つきで全件列挙**した（ラウンド4 のハンクは7個・すべて `@@ -n +n @@` の等長置換で行の増減0） | | |
| STEP 2 の置換判定と STEP 3-2 の前後関係確認が、いずれも件数ではなく全件の表で記録されている | OK | §2-1（42件）・§2-2（33件）・§2-3（2件）＝全77件。§3-3 に **Z-1〜Z-24（全24件）**＋向きが変わった相互参照4件。**ラウンド3 R3-D-1 で `examples:397`（Z-23）・`examples:676`（Z-24）の欠落を補い、3通りに割れていた件数表記を全箇所で24件（うち `grep` 由来の方向語22行＝notation 18・examples 4）に統一した**（§12-3）。ゲート13。件数の訂正（§10 A-2 の「7箇所」＝7行・8出現）は §11-2 の R2-C-1 に実測つきで記録。**ラウンド4 では §2-1 の4行（N-1a・N-1b・N-13・N-31）に最終文の参照先を、Z-12 に「（2箇所）」を補った（R4-6 a・f）。行数で数える件数〈77件・24件〉は不変** | | |
| `ja/development_tools/testing_framework/` 配下の全 `.rst` に `テストケース` が0件 | OK | ゲート3: `grep -rn 'テストケース' … --include=*.rst \| wc -l` → `0`（ラウンド4後に再実行、同値）。**`.rst` 以外の残存（`.java` 2ファイル6箇所＋`.xlsx` ファイル名1件）を R3-D-2 h で、`en/` と `ja/biz_samples/` の `.rst` 3件（いずれもゲート3 の射程外）を R4-6 h で、それぞれ実測により但し書きに補完** | | |
| Docker フルビルドが `build succeeded` で、警告が既知の1件のみ（新規0件） | OK | ゲート11: **ラウンド4後の5回目のフルビルド（`-a`）も `build succeeded, 1 warning`／`EXIT=0`**。警告は `db_double_submit.rst:108` の既知1件のみで新規0件。`locales/ja/LC_MESSAGES/sphinx.mo` は `git checkout` で復元し `git status --porcelain` が無出力であることを確認。`_build/` は削除していない | | |
| （差し戻し対応）レビュー指摘12件（A-1〜A-5・B-1〜B-4・C-1・D-1〜D-4）に、既存成果物を作り直さず是正を積んで対応した | OK | §10 A〜D に、指摘ごとの実施内容を1件ずつ記録。C-1 は `verify_glossary.py` の是正前後の出力（18件→16件）を掲載。**ラウンド2（R2-A-1〜R2-A-6・R2-B-1〜R2-B-2）も、ラウンド3（R3-A-1〜R3-A-8・R3-B-1〜R3-B-3・R3-C-1・R3-D-1〜R3-D-2）も、同じく先行ラウンドを作り直さず上に積んだ**（§11-1・§12）。**ラウンド3 で対応しなかった指摘7件は §12-4 に、ラウンド4 で対応しなかった指摘7件は §13-4 に、いずれも却下・申し送りの別と理由つきで全件記録した**（ラウンド4 はうち2件を両論併記のうえ user review に委ねている） | | |
| （追加回答）`design.md` の是正・特殊記法節の導入文・値の種類10種の突合を実施した | OK | §10 E（`design.md` 全数判定表、ヒット1件）・§10 F（導入文の是正と列挙を落とした理由4点）・§10 G（10種の突合表と `mapping.csv` 65行の確認）。ゲート15〜17。**突合表の欠落6件の分類基準は §11-2 の R2-C-6、除外2行の理由は R2-C-7**。R2-C-6 の空文字の行数は R3-D-2 e で 12→**14** に訂正済み。**`examples:1851` の導入文はラウンド3 R3-A-8 で後半の言い回しのみ「記法の仕様は」に揃えたが、値の種類の列挙を落とすという方針 (c) は変えていない** | | |
| （手法）すべての主張を出典に照合した | OK | §9「照合したもの」参照（差し戻し対応で追加した7行を含む）。**ラウンド2で追加照合したもの: `input/` の4ファイル（R2-C-4 の出典5件を実物で確認）、`mapping.csv` の `input-0087`・`input-0089`（R2-C-7）、2ページのアンダーラインの機械計測（R2-C-8）、作業指示 `ntf-doc-terminology.md:43`（R2-C-3 の字句）。指示字句からの逸脱（R2-C-3）と、記録が誤った出典を根拠に挙げていたこと（R2-C-4）は、隠さず §11-2 に逸脱・誤りとして記録した。ラウンド3で追加照合したもの: `ntf-doc-terminology.md:19` の原文と `mapping.csv` 全594行からの `テストケース` 実測（R3-B-3。196件は本リポジトリで再現不能＝未照合と明記）、`design.md` の基準コミット別ハンク／ヒット数（R3-D-2 f）、`guide/` 配下の遺留アセット（R3-D-2 h）、`ja/` 全体の `利用例`／`使用例` の先例数（R3-A-3。0件／27件）。ラウンド4で追加照合したもの: `testShots` の共通必須カラム `:389`〜`:394` と `context`／`requestPath` の実物（R4-1。`リクエストID` が `testShots` のカラムでないことの確認）、`mapping.csv` の `dest_section` 3値（R4-6 e）、`notation:792` の「後続の行」2箇所（R4-6 f）、`ja/`＋`en/` の `.rst` に残る `テストケース` 3件（R4-6 h）、作業指示 `ntf-doc-terminology.md:102`・`:162` の字句（R4-7）。指示字句からの逸脱（R4-7）は R2-C-3 と同じ体裁で §13-3 に記録した** | | |
---

## 9. 照合したもの（writing のための出典確認）

「作りながら1件ずつ出典に照合する」を実施した。照合した出典と対象は次のとおり。

| 主張・判断 | 照合した出典 | 結果 |
|---|---|---|
| STEP 2 の全77件の判定 | 各対象行の**前後の段落・表・見出し**を実際に読んだ（`Read` で `testdata_notation.rst` 全1519行、`testdata_examples.rst` の該当領域、`about/index.rst` 全117行） | 判定表 §2 のとおり。3通りの置換先に振り分けた |
| `about/index.rst` を `テスト` とした判断 | `grep -rn 'テストショット\|testShots\|LIST_MAP' about/` → 0件、`grep -n ':ref:' about/index.rst` → L28・L117（いずれも対象節の外） | 定義なしの初出になることを実測で確認。§2-3 |
| 「後述の :ref:`…`」という言い回し | 既存解説書 `ja/application_framework/application_framework/libraries/database/universal_dao.rst:645` | 先例あり。`design.md` の言い回しは転記していない |
| 「前節で説明した」など他の候補 | `ja/application_framework/.../repository.rst:579` | 先例はあるが、同ページの支配的な言い回し（`前述の`／`後述の`）に合わせた |
| 見出しアンダーラインの流儀 | 両ページの既存アンダーラインを機械計測（notation は `~`/`^` 49・`-` 50、examples は 50 を基本に、表示幅が超える場合は表示幅に合わせる） | 改題4件のアンダーラインを `max(50, 表示幅)` に合わせた。`unicodedata.east_asian_width` で計測 |
| code-block の桁揃え（N-6） | 同 code-block 内の他3行の表示幅を計測（いずれもラベル＋余白が表示幅20） | `テストショット一覧`（表示幅18）＋余白2 = 20 に合わせた |
| `=` 罫線の簡易 table の有無 | 両ページを機械走査（`=+( +=+)+` にマッチする行） | 両ページとも0件。セル幅調整を要する編集は発生しなかった |
| 段落内改行（1段落1行） | 自作スクリプトで全行走査（ゲート10） | 0件。スクリプトの検出力は**誤検出9件**（`about/index.rst` L86〜L94）→対象外化→0件という経過で裏付け。**§7 ゲート10 が「6件」と書いていたのは誤りで、9件に統一した**（R3-D-2 c。再実行で確認） |
| STEP 5 の `html_use_smartypants` | `ja/conf.py:155-158` を実際に読んだ（`# If true, SmartyPants will be used…` / `#html_use_smartypants = True`）。`requirements.txt` の `Sphinx==1.3.6` / `docutils==0.15.2` | コメントアウトされていることを実測。訂正内容の根拠として §5 に引用 |
| STEP 3-4 の「該当箇所なし」 | `design.md`・`steering.md` を `grep` し、ヒットした `design.md:65` と `steering.md` 7箇所を**実際に読んで**内容を判断 | セクション順の記録は存在しない。`design.md:65` は未処理として §3-4 に報告 |
| ゲート1 の行数 | `csv.DictReader` で計数（`wc -l` を使わない） | 594行 |
| ゲート12 の情報欠落 | 行の多重集合比較＋用語正規化後の再比較 | **最新値（ラウンド3 後）は notation 正規化後 -10/+10・examples -8/+8・about -1/+1。** 全件を STEP 1〜5 とラウンド1〜3 の是正に対応づけた（ゲート12 の2つの残差表）。**「examples 1件」はラウンド1以前の古い値であり、R3-D-2 d で更新した** |
| A-1 の定義文の水準 | 同じ表の `準備データ` 行の `内容` セル（「テスト実行前にデータベース・ファイルへ投入するデータ」）と、`glossary.md:201` の定義文 | 語の実体を述べる同じ水準に揃えた。`期待するステータスコードなど` の例示は残した |
| B-2 の未照合出典 | `AbstractHttpRequestTestTemplate.java` を本リポジトリで全数検索（`git ls-files` にも作業ツリーにも無い）。`ntf-doc-terminology.md:18` を実際に読んだ | 主張の出所は作業指示。`nablarch-testing` は別リポジトリであり本リポジトリでは照合不能。`glossary.md:201` にその旨を明記した |
| B-3 の定義の包含範囲 | `testdata_examples.rst:637` 相当（`エンティティバリデーションのテストショット一覧を記述する`）・`testdata_notation.rst:561` 相当（`エンティティバリデーションのカラムを記述する`）を実際に読んだ | いずれもリクエスト送信・バッチ起動を伴わない。定義文を「1回の実行（リクエスト送信・バッチ起動・バリデーション実行など）」に調整した |
| C-1 の不一致件数 | `verify_glossary.py` を是正前後で実行 | 18件 → 16件。残る16件は本タスク以前からの既存の不一致 |
| E の `design.md` の `テストケース` | `grep -n 'テストケース' design.md` を全数実行 | 1件のみ（`:65`）。ユーザー発言の引用文には含まれない（ゲート15） |
| G の網羅性突合 | `testdata_notation.rst` の値の種類表（Excel形式・YAML形式の各12行）と `testdata_examples.rst` 全2101行を機械走査。`mapping.csv` の `dest_page=テストデータの記載例` の65行を全件確認 | §10 G の突合表。特殊値の出典は 8.1／8.2／8.3 の3件のみで、既存3節と1対1に対応する |

---

## 10. 差し戻し対応（レビュー指摘への是正、2026-08-07）

`#10a` の初回成果（`7a74832`）に対するレビュー指摘12件と、追加回答 `ntf-doc-terminology-answer.md` による2件（E・F）＋調査1件（G）への対応である。`7a74832` を作り直さず、その上に是正を積んだ。

### A. 本文の是正

| # | 指摘 | 実施内容 |
|---|---|---|
| A-1 | `testdata_notation.rst` の `データブロックとデータタイプ` 表で、`用途`＝`テストショット` の行の `内容` セルが `1エントリ1テストショットの実行条件（…）` となっており、**テストショットをテストショットで説明する循環定義**。置換前の `1エントリ1ケースの実行条件` は循環していなかったため、本タスクが持ち込んだ退行 | 同じ表の `準備データ` 行（`テスト実行前にデータベース・ファイルへ投入するデータ`）と同じ水準の定義に改めた。`1エントリが1回の実行（リクエスト送信・バッチ起動・バリデーション実行など）に対応する、1組の入力と期待値（期待するステータスコードなど）`。`glossary.md` の定義（B-3 で調整後）に沿い、`期待するステータスコードなど` の例示は残した。**セル内で完結させ、段落は追加していない**（`list-table` のため表示幅の調整も不要） |
| A-2 | `テストショットごとに準備データと期待値を使い分ける` 節で、見出し・導入文・要約表ヘッダが `テストショット` である一方、同じ表の値と本文が `ケース1`／`ケース2` と呼んでおり呼応が切れている | `ケース1`／`ケース2` は執筆者が本文で付けたラベルであり実データの値ではないことを確認したうえで、`テストショット1`／`テストショット2` に統一した。対象7箇所（旧 `:198`・`:207`・`:210`・`:245`・`:264`・`:283`・`:306`）。**実データのセル値（`no` の `1`・`2`、`description` の `正常注文`・`大量注文`、グループIDの `case01`・`case02`）は変更していない**（ゲート12 の残差にも現れない） |
| A-3 | `testdata_notation.rst:354` にはテストショット一覧が `LIST_MAP` の利用例である旨の前方参照1文があるが、記載例ページの対応箇所に無い | `testdata_examples.rst:397`（節冒頭）に、記載例ページの調子に合わせた同趣旨の1文を追加した。詳細は §3-2 |
| A-4 | `…というテストで、テスト対象の機能に無関係なカラム…` で `テスト` が近接重複（旧文は `というテストケースで、テスト対象の`） | `…というテストで、` を `…を確認するために、` に改めた。`テスト対象` は用語であり動かせないため、重複する側を落とした。「…する際に」「…する場合」の重複を避けるため「ために」を選んだ（後続が「…テーブルを扱う場合、」） |
| A-5 | 見出し `テストショットごとに準備データと期待値を使い分ける`（表示幅50）のアンダーラインが51文字。同ファイルの他の見出しはすべて `max(50, 表示幅)` | 50文字に直した。見出し文言は変更していない。是正後、同ファイルの全見出しが `max(50, 表示幅)` に一致することを機械計測で確認（不一致0件。ゲート5） |

### B. `glossary.md` の是正

| # | 指摘 | 実施内容 |
|---|---|---|
| B-1 | `テストショット` の行（`:201`）の「別義・旧名称」欄が `なし` だが、同じ行の定義文自身が「プロジェクトのテスト仕様書上の**テストケース**1件が…」と `テストケース` を業務上の別義で使っており自己矛盾 | 「別義・旧名称（文脈により使う）」欄に、業務上の（プロジェクトのテスト仕様書上の）テストケースを指す用法は別義である旨を記載した。あわせて置換ルール表（`:552`）の条件欄に「**業務上のテストケースを指す場合は置き換えない**」という除外条件を加えた |
| B-2 | `AbstractHttpRequestTestTemplate.java:222` と `@Published` は本リポジトリ内に存在せず（`nablarch-testing` は別リポジトリ）、他の全行が `NTF:`／`FW:`／`S:` の照合可能な出典を挙げているのにこの2件だけ照合手段が無い | 主張は削除せず、採用根拠の中に「**この2点は作業指示 `S:ntf-doc-terminology.md:18`（2026-08-07 ユーザー提示）に由来する主張であり、`nablarch-testing` は本リポジトリ外のため本リポジトリでは未照合である**」と明記した。`S:` 参照は `verify_glossary.py` で解決することを確認した（refs 258→259、不一致は増えていない） |
| B-3 | 定義「1回のリクエスト送信またはバッチ起動に対応する」が、両ページの `エンティティバリデーションのテストショット一覧を記述する`／`エンティティバリデーションのカラムを記述する`（クラス単体テストのバリデーション検証。リクエスト送信もバッチ起動も伴わない）を包含していない | 定義文を「**1回の実行（リクエスト送信・バッチ起動・バリデーション実行など）に対応する、1組の入力と期待値**」に調整した。作業指示 1-2 は「次の趣旨に改める」であり、趣旨（1回の実行に対応する1組の入力と期待値）は保っている |
| B-4 | §5.8 の `エンティティバリデーション` の行に `testShots表の入力カラム体系`・`testShots表でウェブ等と並ぶ` が残っており、`テストショット表` を揺れ表記に指定した本改訂と不整合 | 同行の `testShots表` 3箇所（意味欄・別義欄・採用根拠欄）をすべて `テストショット一覧` に改めた |
| B-5 | 現行解説書の見出し文字列の一覧（`:403`〜`:449`）を変更しないこと | 無変更（ゲート4 で `diff` が空） |

### C. `term_candidates.tsv` の追随

| # | 指摘 | 実施内容 |
|---|---|---|
| C-1 | `canonical` 列に `テストケース`・`テストケース一覧` が残り、`verify_glossary.py` の不一致が 16件 → **18件**に増えていた（本タスクが持ち込んだ退行） | `canonical` 列を追随させた（6行）。`surface` 列は**変更していない**（コーパス走査の対象表記は変わらないため） |

```
# 是正前
107: テストの骨格 <TAB> テストケース     <TAB> テストケース
108: テストの骨格 <TAB> テストケース     <TAB> テストショット
125: テストデータ <TAB> テストケース一覧 <TAB> テストケース一覧
126: テストデータ <TAB> テストケース一覧 <TAB> テストショット一覧
127: テストデータ <TAB> テストケース一覧 <TAB> テストケース表
128: テストデータ <TAB> テストケース一覧 <TAB> テストショット表

# 是正後
107: テストの骨格 <TAB> テストショット     <TAB> テストケース
108: テストの骨格 <TAB> テストショット     <TAB> テストショット
125: テストデータ <TAB> テストショット一覧 <TAB> テストケース一覧
126: テストデータ <TAB> テストショット一覧 <TAB> テストショット一覧
127: テストデータ <TAB> テストショット一覧 <TAB> テストケース表
128: テストデータ <TAB> テストショット一覧 <TAB> テストショット表
```

**`verify_glossary.py` の是正前後の出力**

```
# 是正前（7a74832 時点）
terms      検証  192 件 / 不一致   4 件
  [term] §5: tsv の正表記 'テストケース' が §5 の正表記列に無い
  [term] §5: tsv の正表記 'テストケース一覧' が §5 の正表記列に無い
  [term] §5: §5 の表記 'テストコード' が term_candidates.tsv に無い（件数を検証できない）
  [term] §5: §5 の表記 'テストソースコード' が term_candidates.tsv に無い（件数を検証できない）
不一致 18 件

# 是正後
terms      検証  192 件 / 不一致   2 件
  [term] §5: §5 の表記 'テストコード' が term_candidates.tsv に無い（件数を検証できない）
  [term] §5: §5 の表記 'テストソースコード' が term_candidates.tsv に無い（件数を検証できない）
不一致 16 件
```

**残る16件は本タスク以前からの既存の不一致であり、解消は本タスクの範囲外である。触っていない**（内訳は `[ref]` 13件＝`design.md` の行番号ずれ、`[section]` 1件＝`テストソースコード`、`[term]` 2件＝上記）。`mapping.csv` / `_batch/` / `vocabulary.md` / `style.md` / `ja/conf.py` の変更禁止リストに `term_candidates.tsv` は含まれない（ゲート2 で差分0を再確認）。

### D. 記録の是正

| # | 指摘 | 実施内容 |
|---|---|---|
| D-1 | `reviews/page-testdata_examples.md` の「アンダーラインの長さ」が **4件**とあるが実測は **5件**（61→63 / 50→51 / 50→52 / 58→60 / 54→56） | 件数を5件に正し、A-5 の是正（51→50）も反映した。うち4件は表示幅超過による延長、1件は延長不要だったため基準コミットと同じ50に戻した旨を記載 |
| D-2 | `（テストケース上、…）` → `（テストの内容上、…）` は語の置換に加えて「の内容」を挿入した**1:1ではない書き換え** | STEP 2 判定表の N-31 に、**1:1の置換ではない**旨と、他の76件はすべて語の1:1置換である旨を明記した（§2-1） |
| D-3 | ゲート7・8 の「各1件」は文字列の出現数では成立しない（実測は notation 4／examples 3、および各3。内訳は見出し1＋`:ref:` リンクテキスト） | ゲート7・8 の記録を書き直した。「各1件」が**見出し行としての件数**であること、出現数の内訳（見出し1＋`:ref:` リンクテキスト）を実測値と行番号つきで明記した。これは STEP 2-3・STEP 4 が「リンクテキストも新見出しに揃えよ」と求めていることの帰結であり実質は正しい状態である旨も記載（§7 ゲート7・8） |
| D-4 | STEP 3-2 の前後関係確認表に `testdata_examples.rst:436` の `:ref:` が無い（旧構成では後方参照、新構成では前方参照に**方向が反転**）。記載例ページ側の確認が表に現れていない | 主表に **Z-22** として追加し、是正の要否（**不要**）とその理由2点を記載した（§3-3） |

### E. `design.md` の是正（追加回答 §1）

**根拠**: `design.md` §8「出典と確定設計が食い違う場合は確定設計を優先する」により、`about/index.rst:24` だけを直すと、**次ページの作者が規約を正しく適用した結果として旧用語に戻す経路が開く**。（`glossary.md:196` はこの根拠にならない。`:196` は用語表のヘッダ行であり `design.md:65` を引用していない。`design.md:65` を引用しているのは `glossary.md:199`（`テストコード`）と `:210`（`YAML形式`）だが、いずれも `テストケース` の規範としての引用ではない。）

**`design.md` 全体の `テストケース` 全数判定**（`grep -n 'テストケース' design.md` の全ヒット）

| # | 行 | 置換前の文（該当部分） | 判定 | 置換後 | 理由 |
|---|---|---|---|---|---|
| D-1 | 65 | テストデータをテストコードから切り離せることは、大量の**テストケース**を効率的に用意・保守するうえで欠かせない | `テスト` | 大量の**テスト**を効率的に用意・保守するうえで欠かせない | `about/index.rst:24` の置換結果（A-2、`テスト`）と同じ語に揃えた。数えているのは個々の `testShots` 行ではなくテスト全体の規模であり、`about/index.rst` 側の判定理由と同一 |

**ヒットは1件のみである。** 同じ `:65` にある `#8` フィードバックの引用文（「テストコードは定型かつ少量で済むには、暗黙的にテストデータの外部化が含まれてますよね。基幹システムだとテーブル数やカラム数など大規模なデータが想定されるので、外部化必須ですよね」）には `テストケース` が含まれず、**改変していない**（ユーザー発言の記録である）。

### F. `testdata_examples.rst` の特殊記法セクション — 導入文（追加回答 §2、方針 (c)）

**見出し `null・空文字・改行など特殊な値を記述する` は変更していない**（F-1。両ページで一致したまま）。

| | 内容 |
|---|---|
| 是正前（L1851） | 日付・システム日時・\ NULL\ ・スペース・バイナリといった、特殊記法を使う値の記述例を示す。特殊記法の仕様は\ :ref:`…`\ を参照。 |
| 是正後（L1851） | 特殊記法を使う値の記述例を示す。特殊記法の仕様は\ :ref:`…`\ を参照。 |

**列挙を落とす形を選んだ。理由:**

1. 回答 §2 の3案のうち「列挙を落として『特殊記法を使う値の記述例を示す。特殊記法の仕様は〜を参照』とする形」に該当する。**「特殊記法を使う値の記述例を示す」が主**になる（F-2）
2. 列挙を残す場合、見出しの語（`null`・空文字・改行）と揃えると、**空文字と改行の例が0件**であるため導入文が存在しない例を約束することになる。逆に実際に例のある語だけを挙げると見出しとの不一致が導入文に移るだけである（F-3 が禁じた形）。**どちらの列挙も現状では成り立たない**
3. 同ページの他のL2導入文（`テストショット一覧（testShots）を記述する`、`コメント・マーカーカラム・空エントリを扱う` など）も「〜の記述例を示す。〜の仕様は :ref:`…` を参照。」という形であり、列挙を持たない形のほうが**ページ内で一貫する**
4. 網羅性の欠落（G）は導入文で隠さず、G の突合表として記録に残した

### G. 網羅性の突合（値の種類10種。**調査と報告のみ。例は追加していない**）

`testdata_notation.rst` の `null・空文字・改行など特殊な値を記述する` 配下の値の種類表（`Excel形式の場合`・`YAML形式の場合` の各表）が説明している10種について、`testdata_examples.rst` に例があるかを機械走査で突合した。

| # | 値の種類 | 記載例ページの例 | 位置 | 判定 |
|---|---|---|---|---|
| 1 | `null`（データベースに `null` を格納） | **あり** | 特殊記法節 `日付・システム日時・NULLを記述する`（Excel の `NULL` セル、YAML のアンクォート `null`）。ほかに `テーブルのデータを記述する` にも `Null` の例 | 充足 |
| 2 | 文字列の `null`（`"null"`） | **説明のみ・記述例なし** | `テーブルのデータを記述する` L862 に「``"null"``\ とクォートした場合も同じ結果になる」という説明文があるだけで、セル値・エントリ値としての例は無い | 欠落 |
| 3 | 空文字 | **専用の例なし（付随的な出現のみ）** | `LIST_MAPのデータを記述する` L732・L734 や `複数レコードレイアウトを記述する` L1276（`["H001", "01", ""]`）に値として現れるが、「空文字をこう書く」と示した例は無い | 欠落 |
| 4 | 先頭ゼロ付き数値 | **専用の例なし（付随的な出現のみ）** | `バイナリデータを記述する` の `FILE_ID` が `001`／`002`（YAML では `"001"`）、`コメントとマーカーカラムを記述する` の `PK_COL1` が `0000000001` | 欠落 |
| 5 | `true`・`false`（文字列） | **専用の例なし（付随的な出現のみ）** | `ウェブアプリケーションのテストショット一覧を記述する` の `isValidToken` が Excel `true`／`false`、YAML `"true"`／`"false"` | 欠落 |
| 6 | 半角スペース1文字 | **あり** | 特殊記法節 `スペース・ダブルクォートを記述する`（Excel `" "`、YAML `NAME: " "`） | 充足 |
| 7 | ダブルクォート1文字 | **あり** | 同上（Excel `"""`、YAML `"\""`／`'"'`） | 充足 |
| 8 | バイナリファイル参照（`${binaryFile:パス}`） | **あり** | 特殊記法節 `バイナリデータを記述する`。ほかに `準備データ（SETUP_TABLE）を記述する` にも | 充足 |
| 9 | 文字種指定（`${文字種,文字数}`） | **なし（0件）** | ページ全体で `${文字種` ・`半角英字` ともに0ヒット | 欠落 |
| 10 | 改行文字（`\r`・`\n`） | **なし（0件）** | ページ全体で0ヒット | 欠落 |

**欠落6件（#2・#3・#4・#5・#9・#10）について、`mapping.csv` の `dest_page=テストデータの記載例` の65行に該当する出典があるかを全件確認した。**

```
$ python3 -c "…dest_page=='テストデータの記載例' の65行を列挙…"
# 特殊値に関する出典は次の3件のみ
input-0083 | input/ntf-testdata-doc-examples-special.md:10-58  | 8.1 日付型・Timestamp・特殊値
input-0084 | input/ntf-testdata-doc-examples-special.md:60-87  | 8.2 QuotationTrimmer によるスペース値明示記法
input-0085 | input/ntf-testdata-doc-examples-special.md:89-120  | 8.3 バイナリデータ
```

この3件は、記載例ページの既存3節（`日付・システム日時・NULLを記述する`／`スペース・ダブルクォートを記述する`／`バイナリデータを記述する`）と**1対1に対応する**。`input-0083` の本文を実際に読み、`EXPECTED_TABLE=SCHEDULE` の4行が記載例ページの表と一致することを確認した。

文字種生成・改行文字の記述がある出典は、いずれも `dest_page` が **`テストデータの書き方`** である。

```
$ python3 -c "…'8.5'/'文字種'/'改行' を heading_path に含む行…"
input-0148 | ntf-testdata-doc.md:531-553 | dest_page=テストデータの書き方 | 8.1 値の種類と Excel / YAML 対比（文字種生成・改行文字の行を含む）
input-0150 | ntf-testdata-doc.md:559-576 | dest_page=テストデータの書き方 | 8.3 インタープリタ一覧
input-0152 | ntf-testdata-doc.md               | dest_page=テストデータの書き方 | 8.5 文字種生成の有効文字種
current-0243 | 03_Tips.rst                     | dest_page=テストデータの書き方 | 目的別API使用方法 > テストデータに空白、空文字、改行やnullを記述したい
```

**判定: 欠落6件はいずれも「記載例ページに割り当てられた出典が無い」。** したがって回答 §3 の分岐のうち「**出典が無いため例を作らない**」に該当する。`reviews/page-testdata_examples.md` に判断として記録した。**本ラウンドでは例を追加していない**（`#10` は承認済みであり、追加は改めての指示を待つ）。

なお `current-0243`（現行解説書 `03_Tips.rst` の「テストデータに空白、空文字、改行やnullを記述したい」）は、空文字・改行・null を扱う唯一の現行解説書の節だが `dest_page` が `テストデータの書き方` であり、記載例ページの出典ではない。**記載例ページ側に例を足すかどうかは `mapping.csv` の割当の問題**であり、`mapping.csv` は本タスクの変更禁止対象である。

---

## 11. 差し戻し対応ラウンド2（2026-08-07）

§10 のラウンド1の是正が「局所的には正しいが、その場所の日本語が悪くなった」という指摘を、再レビュー（4観点）で複数のレビュアーから独立に受けた。その2巡目の対応である。

**本節の項番はラウンド2のものであり、§10（ラウンド1）の `A-1`〜`A-5`／`B-1`〜`B-5`／`C-1`／`D-1`〜`D-4` とは別系列である。** 混同を避けるため、本節では接頭辞 `R2-` を付ける（本文編集は `R2-A-*`／`R2-B-*`、記録の訂正・追加は `R2-C-*`）。§10 の記述は書き換えず、本節で訂正・追加する。

### 11-1. 本文編集の全件（R2-C-2）

作業指示は原則として**本文（`.rst`）の新規編集を禁じている**。ラウンド2では、レビュー指摘への是正としてこの禁止の例外を行使した。**その全件が次の7件である**（コミット `7ea37ac`）。行番号は現在の内容と照合して確定した。

```
$ git show 7ea37ac --format= -- .../testdata_notation.rst .../testdata_examples.rst | grep '^@@\|^diff'
diff --git a/.../testdata_examples.rst b/.../testdata_examples.rst
@@ -204,10 +204,10 @@ YAML形式の場合
@@ -394,7 +394,7 @@ YAML形式の場合
@@ -499,7 +499,7 @@ YAML形式の場合
@@ -673,7 +673,7 @@ YAML形式の場合
@@ -1468,7 +1468,7 @@ YAML形式の場合
diff --git a/.../testdata_notation.rst b/.../testdata_notation.rst
@@ -37,7 +37,7 @@
@@ -105,7 +105,7 @@ YAMLファイルは\ YAML 1.2\ に準拠する。\ YAML 1.1\ との主な違い
```

ハンクは examples 5個・notation 2個で計7個。うち `@@ -204,10 +204,10 @@` の1ハンクだけが2行（`:207`・`:210`）を含み、残る6ハンクは各1行である。**行の増減は無い**（すべて `n,7 → n,7` などの等長置換）。

| # | ファイル:行（現在） | 何を | なぜ | 指摘 |
|---|---|---|---|---|
| 1 | `testdata_notation.rst:108` | `データブロックとデータタイプ` 表の `用途`＝`テストショット` 行の `内容` セルを、`1エントリが1回の実行（…）に対応する、1組の入力と期待値（…）` → `1回の実行（リクエスト送信・バッチ起動・バリデーション実行など）に対応する、1エントリ分の実行条件（期待するステータスコードなど）` | ラウンド1の文は (a) 「1組の**入力**と**期待値**」が兄弟行 `準備データ`（入力）・`期待値`（実行後の状態）を包含し3つの用途が排他にならない、(b) `1エントリが…に対応する、1組の…` の係り受けが二通りに読める、(c) 他2行が25〜33字の名詞句なのに約60字＋埋め込み主語で水準が揃わない、の3点があった。頭の名詞を `実行条件` にして兄弟行と排他にし、`1エントリ分` に掛けて1エントリ＝1回の実行を読み取れるようにした | R2-A-1 |
| 2 | `testdata_notation.rst:40` | `テストで発生したマスタデータの変更をテスト終了時に…マスタデータへの変更を伴うテストであっても、他のテストに` → `実行中に生じたマスタデータの変更をテスト終了時に…マスタデータを変更する場合でも、他のテストに` | STEP 2 の置換（N-1a・N-1b）の結果、1文の中に `テスト` が4回出ていた（置換前は `テスト`×2＋`テストケース`×2 で語が分かれていた）。**意味を変えずに** `テスト` を2回に減らした | R2-A-2 |
| 3 | `testdata_examples.rst:397` | 節冒頭の前方参照文を、`これは ``LIST_MAP`` というデータタイプの利用例の1つであり、``LIST_MAP`` 自体の記述例は後述の :ref:`…` に示す。` → `テストショット一覧は ``LIST_MAP`` というデータタイプの利用例の1つであり、``LIST_MAP`` の他の利用例は後述の :ref:`…` に示す。` | ラウンド1で足した文が**事実に反していた**。参照先 `LIST_MAPのデータを記述する`（`:674`）の配下は `リクエストパラメータを記述する`・`期待ログを記述する` の2節だけで、「`LIST_MAP` 自体の記述例」は存在しない（書き方ページが「`LIST_MAP` 自体の**記法**は後述の…で説明する」と書けるのは、あちらに記法の節が実在するため）。あわせて `これ` の先行詞が主文末の「記述例」とも読めたので明示した | R2-A-3 |
| 4 | `testdata_examples.rst:676` | `LIST_MAPのデータを記述する` の導入文に `利用例の1つであるテストショット一覧（``testShots``\ ）の記述例は、前述の :ref:`テストショット一覧（testShots）を記述する <testdata_examples-test_shots>`\ に示している。` を追加 | 書き方ページは同じ位置（`testdata_notation.rst:586`）に「前述の :ref:`…` を参照」を持つのに、記載例ページの同名節には無く**両ページで非対称**だった。`:ref:` 先（`:395`）は参照元より前にあるため「前述の」が成立する | R2-A-4 |
| 5 | `testdata_examples.rst:207`・`:210` | 要約表の値 `テストショット1（正常注文）`／`テストショット2（大量注文）` → `1（正常注文）`／`2（大量注文）` | 列見出しが `テストショット` であるため、ラウンド1の A-2 の置換によって列見出しと値で語が反復した（置換前の `ケース1（正常注文）` には反復が無かった）。**列見出しを保ち値だけ短縮**した。列見出しが語を与えるため意味は変わらない。本文側（`:245`・`:264`・`:283`・`:306`）と導入文（`:198`）は自然であるため変更していない | R2-A-5 |
| 6 | `testdata_examples.rst:502` | `データベースだけを処理する**ケース**と、入力ファイルを使う**ケース**の2件を実行する例である。入力ファイルを使う**ケース**では、` → `…テストショットと、…テストショットの2件を実行する例である。**後者では**、` | ラウンド1の A-2 は `:196` の節だけを直したため、同じ用法（`ケース` ＝ `テストショット` の略記）が他の節に残っていた。3文目は同語3連発を避けて指示語にした | R2-A-6 |
| 7 | `testdata_examples.rst:1471` | `1件処理の**ケース**`／`複数件処理の**ケース**` → いずれも `…テストショット` | 同上。対句であるため両方を置換した | R2-A-6 |

**変更しなかった `ケース`（判断）**: `testdata_examples.rst:401` の `正常ケースと認証エラーケース` は変更していない。これは `description` セルの実値（`正常ケース`、`:424`・`:478`）の引用であり、地の文が付けたラベルではないため、置き換えると表・コードブロックの実値と食い違う。ページに残る `ケース` はこの **3行・4出現**（`:401` の `正常ケース`・`認証エラーケース` の2出現、`:424` の Excel セル値、`:478` の YAML `description` 値）だけである（`grep -n 'ケース' … | grep -v 'テストケース'` の全ヒット。`テストケース` はゲート3 のとおり0件）。

**補完（差し戻し対応ラウンド3 R3-D-2 g。検証 R3-8 への対応）**: 上の判断は `testdata_examples.rst` 限定であり、`testdata_notation.rst` に残る `ケース` 2件が記録のどこにも現れていなかった。**両ページ分を次の表に揃える。**

```
$ grep -n 'ケース' testdata_notation.rst testdata_examples.rst | grep -v 'テストケース'
notation:666  notation:850  examples:401  examples:424  examples:478
```

| ファイル:行 | 該当箇所 | 判定 | 理由 |
|---|---|---|---|
| `notation:666` | `…親テーブルの DELETE が FK 違反により失敗する**ケース**がある。` | **置換不要** | 「場合」の意の一般語である。`testShots` のエントリを指しておらず、`テストショット` に置き換えると意味が変わる（失敗するのは実行単位ではなく状況） |
| `notation:850` | `ファイルデータの記述時にエラーとなるのは、以下のような**ケース**である。` | **置換不要** | 同じく「場合」の意の一般語。直後の箇条書きが列挙しているのはエラー条件であってテストショットではない |
| `examples:401` | `正常**ケース**と認証エラー**ケース**の2件を実行する例である。`（2出現） | **置換不要** | `description` セルの実値（`正常ケース`）の引用であるため（上記のとおり） |
| `examples:424` | Excel セル値 `正常ケース` | **置換不要** | 実データのセル値 |
| `examples:478` | YAML `description: "正常ケース"` | **置換不要** | 実データのエントリ値 |

**両ページを合わせて 5行・6出現。すべて置換不要と判定した。** `テストケース` はゲート3 のとおり両ページ0件である。

**本文以外のラウンド2の編集**: `mapping/glossary.md` の2行（`:201` の採用根拠に `testCases` が旧称として動作する旨の照合済み出典 `S:input/ntf-testdata-doc.md:167` を足し、未照合な主張を2点に限定＝R2-B-1／`:552` の置換ルールの除外条件が「解説書本文にも `テストケース` を残してよい」と読めたため、例外の適用範囲を用語集・設計文書に限る旨を明記＝R2-B-2）と、`reviews/page-testdata_notation.md`・`page-testdata_examples.md` への追記。`glossary.md` は STEP 1 の編集対象であって禁止対象ではない（禁止は `:403`〜`:449` のみ。ゲート4）。

### 11-2. 記録の訂正・追加（R2-C-1・R2-C-3〜R2-C-8）

#### R2-C-1 — §10 A-2 の件数の訂正

§10 A-2 は `ケース1`／`ケース2` → `テストショット1`／`テストショット2` の置換を「対象7箇所」と記録している。これは**行数**としては正しいが、**出現数では8**である（`:198` の1行に `ケース1` と `ケース2` が2出現する）。

```
# 削除側（置換前）を re.findall('ケース[12]') で計数
$ git show 6ce81b5 --unified=0 --format= -- .../testdata_examples.rst | grep '^-' | grep -v '^---' | python3 …
削除行総数 10
ケース1/2 を含む行数 7
ケース1/2 の出現数 8
  2 -注文明細テーブル（\ ``ORDER_DETAIL``\ ）に対して、明細1件の正常注文（ケース1）と明細2件の大量注…
  1 -  * - ケース1（正常注文）
  1 -  * - ケース2（大量注文）
  1 -ケース1の準備データの例を示す。
  1 -ケース1の期待値の例を示す。
  1 -ケース2の準備データの例を示す。
  1 -ケース2の期待値の例を示す。

# 追加側（置換後）
$ … | python3 …
テストショット1/2 を含む行数 7 出現数 8

$ git show 6ce81b5 --unified=0 --format= -- .../testdata_examples.rst | grep '^@@'
@@ -197,2 +197,2 @@ / @@ -207 +207 @@ / @@ -210 +210 @@ / @@ -245 +245 @@ /
@@ -264 +264 @@ / @@ -283 +283 @@ / @@ -306 +306 @@
（ほかに @@ -397 +397 @@ と @@ -1851 +1851 @@ があるが、これは A-3・F の別件）
```

**訂正: 正しくは「7行・8出現」である。** §10 A-2 が挙げた旧行番号7件（`:198`・`:207`・`:210`・`:245`・`:264`・`:283`・`:306`）はそのまま正しい。§10 の既存記述は書き換えていない。同じ訂正は `reviews/page-testdata_examples.md` の `#10a` 節にも反映済みである。

なお、この8出現のうち `:207`・`:210` の2出現は、ラウンド2の R2-A-5（§11-1 の #5）で `1（正常注文）`／`2（大量注文）` に短縮され、現在は `テストショット1`／`テストショット2` として残っていない。

#### R2-C-3 — 指示字句からの逸脱（`glossary.md` の `テストショット` の定義）

作業指示 `ntf-doc-terminology.md:43`（1-2 の1）は、定義文を次の趣旨に改めよと指示している。

```
$ sed -n '42,43p' ntf-doc-terminology.md
1. **`:201`** — 正表記を `テストショット` に、揺れ表記を `テストケース` にする。定義文を次の趣旨に改める
   - **1回のリクエスト送信またはバッチ起動に対応する、1組の入力と期待値**
```

§10 B-3 で採った文言は `1回の実行（リクエスト送信・バッチ起動・**バリデーション実行**など）に対応する、1組の入力と期待値` であり、**指示の字句そのままではない**。

| | 内容 |
|---|---|
| 逸脱の内容 | 「リクエスト送信またはバッチ起動」という2択の限定列挙を、「1回の実行（…など）」という上位概念＋例示の開いた列挙に広げ、`バリデーション実行` を例に加えた |
| 逸脱した理由 | 指示の字句のままでは、両ページの `エンティティバリデーションのテストショット一覧を記述する`（`testdata_examples.rst`）・`エンティティバリデーションのカラムを記述する`（`testdata_notation.rst`）が定義から漏れる。これらはクラス単体テストのバリデーション検証であり、リクエスト送信もバッチ起動も伴わない（両節を実際に読んで確認。§9 の B-3 の行） |
| 趣旨は保っているか | **保っている。** 指示は「次の**趣旨に**改める」であり、趣旨は「1回の実行に対応する、1組の入力と期待値」である。上位概念に置き換えたのは限定列挙の側だけで、「1回の実行に対応する1組の入力と期待値」という核は変えていない |
| 影響 | この定義は `testdata_notation.rst:108` の `内容` セル（§11-1 の #1）と対応しており、そちらも同じ列挙になっている |

**判断を仰ぐ点**: 指示の字句どおりに戻すべきであれば、`glossary.md:201` と `testdata_notation.rst:108` の2箇所を同時に戻す必要がある。

#### R2-C-4 — 出典の取り違え（`"null"` のクォートあり表記）

記録・レビュー記録の中で「`"null"`（クォートあり）は文字列として格納される」という主張の**根拠**として挙げていた出典2件は誤りである。該当する `input/` の4ファイルを実際に開いて確認した。

| | 参照 | 実物の記述（確認済み） | 判定 |
|---|---|---|---|
| 誤 | `input/ntf-testdata-doc-examples-table.md:50` | `- NULL 値はアンクォートの ``null``。``"null"`` とクォートすると文字列として格納される` | **誤り**。実装・確定設計と食い違う |
| 誤 | `input/ntf-testdata-doc-examples-special.md:49` | `- ``NULL`` 文字列は ``NullInterpreter`` が Java null に変換する。…YAML ではアンクォートの ``null`` で記述し、``"null"`` とクォートすると文字列として格納される` | **誤り**。同上 |
| 正 | `input/ntf-testdata-doc.md:252` | 値の指定の対比表の行。Excel 列＝「セルに `null`（大文字小文字不問）」、YAML 列＝「アンクォートの `null`（**`"null"` でも同結果**）」 | 正しい |
| 正 | `input/ntf-testdata-doc.md:535` | 値の種類の対比表の行（`null`＝DB に null を格納）。備考列＝「**YAML の `"null"`（クォートあり）も同結果**」 | 正しい |
| 正 | `input/ntf-testdata-doc.md:549` | `- ``null`` のみクォートなしで記述（ただし ``"null"`` でも同じく Java null になる）` | 正しい |

**根拠**（`input/testdata-converter-design.md:110-112`、確定設計。実物で確認）:

```
- **形式の構文に属する記法**（クォートによる文字列明示）：形式ごとに担い手が違う。
  - Excel：クォートは NTF 独自の記法で、本体の `QuotationTrimmer` が外す。
  - YAML：クォートは **YAML 標準仕様**であり、YAML ライブラリ（SnakeYAML Engine）が読み込み時に解決する。
    よって YAML には `QuotationTrimmer` を適用しない（適用すると二重処理になる）。
```

SnakeYAML が**読み込み時＝インタープリタチェーンより前**にクォートを解決するため、チェーンが受け取るのは裸の `null` であり、`NullInterpreter` が一致して Java の null になる。`"null"` と裸の `null` が同結果になるのはこの適用順の帰結である。

**本文は正しく、修正していない**（`design.md` §8「出典と確定設計が食い違う場合は確定設計を優先する」）。`input/` 配下は出典資料であるため、誤りがあっても書き換えない。

#### R2-C-5 — 残り32ページへの申し送り（追加不要だった）

R2-C-4 の内容を申し送りに追加する必要があるかを確認するため、`reviews/page-testdata_notation.md`・`page-testdata_examples.md` の現状を先に読んだ。**両ファイルとも `7ea37ac` で追記済みであり、同趣旨の記述が既にある。したがって本ラウンドでは追記していない。**

| ファイル | 既にある記述 |
|---|---|
| `reviews/page-testdata_examples.md` | 見出し `出典と本文の食い違い（C-4、2026-08-07）— ``"null"``（クォートあり）の扱い` の節（誤っている出典2件・正しい出典3件・機構の根拠を file:line つきで記載）と、申し送り **17** |
| `reviews/page-testdata_notation.md` | 申し送り **18**（本ページの `null（データベースに null を格納）` 行と `文字列の null`＝`該当なし` が正しいこと、詳細は `page-testdata_examples.md` の当該節を見ること） |

#### R2-C-6 — ゲート17 の欠落6件の分類基準

§10 G は10種のうち6件を「欠落」としているが、**この6件は同じ水準ではない**。次の2水準で分類する。

- **水準1 — ページ全体で0件**: その値の種類に該当する記述が、**値としても0件**である（説明文だけがある場合を含む）
- **水準2 — 専用の例は無いが値としては出現する**: 「この値はこう書く」と示した例は無いが、他の節の例の中に値として現れる

実測（`testdata_examples.rst` 全2101行を機械走査。行番号は現在の内容）:

| # | 値の種類 | 実測 | 水準 |
|---|---|---|---|
| 2 | 文字列の `null`（`"null"`） | 値としての出現 **0件**。`:862` の説明文1件のみ（`` ``"null"``\ とクォートした場合も同じ結果になる`` ） | **水準1** |
| 3 | 空文字 | 値として **14行**に出現（**R3-D-2 e で訂正。以前は「12行」と書いていたが、続く列挙は YAML 12行＋Excel 2行＝14行であり数字と合っていなかった**）。内訳は YAML の `""` **12行**（`:153`・`:154`・`:565`・`:732`・`:734`・`:1276`・`:1690`・`:1691`・`:1700`・`:1701`・`:1806`・`:1807`）と、対応する Excel 例の空セル **2行**（`:710`・`:712`） | **水準2** |
| 4 | 先頭ゼロ付き数値 | 値として出現。Excel はアンクォート（`:703` `0000000101`、`:1178`・`:1182` `001`／`002`、`:2028` `0000000001`）、YAML はクォート（`:725` `"0000000101"`、`:1989`・`:1991` `"001"`／`"002"`、`:2054` `"0000000001"`） | **水準2** |
| 5 | `true`／`false` | 値として **4行**。Excel はアンクォート（`:425`・`:431`）、YAML はクォート（`:479` `"true"`、`:485` `"false"`） | **水準2** |
| 9 | `${文字種,文字数}` | `${文字種`・`半角英字`・`半角数字`・`半角記号` ともに **0ヒット** | **水準1** |
| 10 | 改行文字（`\r`・`\n`） | リテラルの `\r`・`\n` が **0ヒット**（`:1354` の「CRLF 改行のファイル」は可変長ファイルのディレクティブの説明であり値ではない） | **水準1** |

**内訳: 水準1 が3種（#2・#9・#10）、水準2 が3種（#3・#4・#5）。**

**是正指示との差（明記しておく）**: 是正指示は水準1を「文字列の `null`・空文字・`${文字種,文字数}`・改行文字 の4種」としていたが、上の実測のとおり**空文字は水準2 に属する**（YAML の `""` として12行、Excel の空セルとして `:710`・`:712` に現れる）。したがって内訳は 4/2 ではなく **3/3** である。**欠落6件という §10 G の判定そのものは変わらない**（水準2 の3種も「その値の種類を示すことを目的とした例」は0件であるため）。

#### R2-C-7 — 突合から除外した出典2行（`input-0087`・`input-0089`）

§10 G の `mapping.csv` 突合では、`dest_page=テストデータの記載例` の65行のうち、改行・空文字という語を含む次の2行を検討のうえ除外した。両行を `mapping.csv` から実際に引いて内容を確認した。

| 行 | `heading_path` | `note` の要旨（実物） | 除外が妥当な理由 |
|---|---|---|---|
| `input-0087` | `… > 9. ディレクティブ > 9.2 可変長ファイルのディレクティブ` | 「タブ区切り・CRLF 改行のファイルを扱うケースの Excel・YAML 記述例。…`record-separator` の有効値（NONE/CR/LF/CRLF/任意リテラル）…」 | 扱っているのは**ディレクティブとしての改行コード指定**（`record-separator`）であって、**値の中に改行文字を書く記法**（`\r`・`\n`）ではない。値の種類 #10 の出典にならない。かつ本行は既に `testdata_examples.rst:1352` の `可変長ファイルのディレクティブを指定する` として実現済みで、未消化の出典でもない |
| `input-0089` | `… > 10. ヘッダ・コメント・空エントリ > 10.2 空エントリのスキップ` | 「全要素が null または空文字のエントリが読み飛ばされるケースの Excel・YAML 記述例。…Excel は全セルが空の行が自動的にスキップされる…」 | 扱っているのは**エントリ全体が空のときの読み飛ばし挙動**であって、**1つの値として空文字を書く記法**ではない。値の種類 #3 の出典にならない。かつ本行は既に `testdata_examples.rst:2064` の `空エントリを記述する` として実現済みである |

**したがって §10 G の「欠落6件に該当する出典が無い」という判定は、この2行を除外しても変わらない。**

#### R2-C-8 — 見出しアンダーラインの流儀の差は退行ではない

2ページはアンダーライン長の流儀が異なる。**この差は基準コミット `6ba0d2a` 以前から存在し、本タスクが持ち込んだ退行ではない。** `unicodedata.east_asian_width` による表示幅計測で、`6ba0d2a` と現在（`HEAD`＋ワーキングツリー）の両方を突き合わせた。

| ファイル | 階層 | `6ba0d2a` | 現在 | 流儀 |
|---|---|---|---|---|
| `testdata_notation.rst` | `=`（L1）/`-`（L2） | 50 × 1 / 50 × 3 | 同じ | `max(50, 表示幅)`（不一致0） |
| `testdata_notation.rst` | `~`（L3）/`^`（L4） | **49 × 10 / 49 × 26**（表示幅の最大は 42 / 48） | **同じ** | **49固定**（10/10・26/26 が一致。`max(50, 表示幅)` には 0/36 しか一致しない） |
| `testdata_examples.rst` | `=`/`-`/`~`/`^` | 1 / 9 / 28 / 56 件すべてが `max(50, 表示幅)` に一致（不一致0） | 同じ（不一致0） | **`max(50, 表示幅)`** |

`testdata_examples.rst` の `~` の分布だけが変わっている（`6ba0d2a`: `{50:24, 61:1, 59:1, 58:1, 54:1}` → 現在: `{50:23, 63:1, 60:1, 59:1, 56:1, 52:1}`）が、これは改題により見出しの表示幅が伸びた分に追随した結果であり、**どちらも `max(50, 表示幅)` に100%一致している**（流儀は不変）。`testdata_notation.rst` は `6ba0d2a` と現在で分布そのものが完全に一致する。

**結論: 流儀の差は本タスク以前からの既存の状態であり、退行ではない。** 各ページは自ページの流儀に一貫しているため、本タスクでは統一していない（統一は改題と無関係な全見出しの書き換えになり、指示の範囲外である）。

### 11-3. ゲート1〜17 の再実行

ラウンド2の本文適用後に**全17件を再実行**した。「変化」列が `—` のものは再実行して同値だったことを示す。

| ゲート | 内容 | 結果 | 変化 |
|---|---|---|---|
| 1 | `verify_mapping.py` が exit 0、594行 / 12,986 / 11,983 | **PASS** | — （`OK: no errors`／exit 0。行数は `csv.DictReader` で 594。`wc -l` は使っていない） |
| 2 | 禁止ファイル5対象の差分が空 | **PASS** | — （`git diff --stat 6ba0d2a HEAD -- …` が無出力。ワーキングツリー側の `git status --porcelain` も無出力） |
| 3 | `testing_framework/` 配下の全 `.rst` に `テストケース` 0件 | **PASS** | — （`grep -rn … --include=*.rst \| wc -l` → `0`） |
| 4 | `glossary.md` の `:403`〜`:449` に差分なし | **PASS** | — （`diff` が空。`identical`） |
| 5 | 見出し数が不変・アンダーライン長の不一致0 | **PASS** | — （notation `{'=':1,'-':3,'~':10,'^':26}`／examples `{'=':1,'-':9,'~':28,'^':56}`。不一致0。R2-C-8 の計測を兼ねる） |
| 6 | 両ページの並びが STEP 3-1 の順 | **PASS** | — （7見出しが両ページで同一順・同一文言） |
| 7 | 旧見出し0件・新見出しが見出し行として各1件 | **PASS** | — （notation 旧0／総出現4／`:ref:`3、examples 旧0／総出現3／`:ref:`2） |
| 8 | `テストケース一覧` 0件・`テストショット一覧（testShots）を記述する` が見出し行として各1件 | **PASS** | **変化あり**。examples の総出現 3→**4**、`:ref:` リンクテキスト 2→**3**（R2-A-4 が `:676` に1件追加したため）。見出し行は各1件のままで合否は不変。notation は同値 |
| 9 | `:ref:` の未定義参照0件・リンクテキストに旧見出しなし | **PASS** | — （`grep … \| wc -l` → `0`。未定義参照はゲート11 のビルド警告で確認） |
| 10 | 段落内改行が0件 | **PASS** | — （3ファイルとも `0 件`、exit 0） |
| 11 | Docker フルビルド（`-a`） | **PASS** | **再実行**。`build succeeded, 1 warning`／`EXIT=0`。警告は `db_double_submit.rst:108` の既知1件のみで**新規0件**。`locales/ja/LC_MESSAGES/sphinx.mo` は `git checkout` で復元し `git status --porcelain` が無出力であることを確認。`_build/` は削除していない |
| 12 | 情報が失われていないこと | **PASS** | **変化あり**。examples の残差が 素 -41/+41 → **-43/+43**、正規化後 -2/+2 → **-7/+7**。増えた5件はすべて §11-1 の本文編集であり、削除された段落・表の行・コードブロックは0件。notation・about は同値 |
| 13 | STEP 2 の置換判定表・STEP 3-2 の前後関係の確認表が全件記録 | **PASS** | — （§2 に全77件、§3-3 に Z-1〜Z-22 ＋ 方向語なし4件）。**※ この行はラウンド2 時点の値である。ラウンド3 の R3-D-1 で §3-3 に Z-23・Z-24 を追加し、正しくは `Z-1〜Z-24`（全24件）となった。最新の一覧は §12-5 を参照** |
| 14 | `about/index.rst` の変更が L24 の1行のみ | **PASS** | — （ハンクは `@@ -24 +24 @@` の1つのみ） |
| 15 | `design.md` に `テストケース` が0件 | **PASS** | — （`grep -c` → `0`。`design.md` のハンクは `@@ -65 +65 @@` の1つのみで、ワーキングツリーに追加差分なし） |
| 16 | 特殊記法セクションの導入文と見出しに語の不一致なし | **PASS** | — （examples `:1849`-`:1851`、notation `:1275`-`:1277` を再確認。導入文はどちらも値の種類を列挙していない） |
| 17 | 値の種類10種の突合表が記録されていること | **PASS** | — （§10 G の突合表。ラウンド2で**分類基準（R2-C-6）と除外理由（R2-C-7）を追加**した。例は追加していない） |

---

## 12. 差し戻し対応ラウンド3（2026-08-07）

4観点の再レビュー（QA／設計／クラフト／検証）で、**設計と検証が FAIL** を出した。そのトリアージに基づく3巡目の対応である。**本ラウンドはレビューの上限ラウンドであり、指示された是正をすべて完了させた。** §10（ラウンド1）・§11（ラウンド2）の記述は書き換えず、その上に是正を積んでいる。

**本節の項番はラウンド3のものであり、§10 の `A-1`〜`D-4`／§11 の `R2-A-*`〜`R2-C-*` とは別系列である。** 混同を避けるため、本節では接頭辞 `R3-` を付ける（本文編集は `R3-A-*`、`glossary.md` は `R3-B-*`、`reviews/` は `R3-C-*`、記録の是正は `R3-D-*`）。

**誤読の罠として明示的に除外したもの**: `testdata_notation.rst` の `文字列の null` に関する2箇所（`Excel形式の場合` の配下で「`"null"` で表現可」、`YAML形式の場合` の配下で「該当なし」）は**矛盾ではない**。形式差を意図して書き分けている（`checks/task-09-recordtype.md:31`）。**本文の修正は行っていない。**

### 12-1. 本文編集の全件（R3-A-1〜R3-A-8）

作業指示は原則として**本文（`.rst`）の新規編集を禁じている**。ラウンド3ではレビュー指摘への是正としてこの禁止の例外を行使した。**その全件が次の8件である。**

```
$ git show 6cc01b7 -U0 --format= -- .../testdata_notation.rst .../testdata_examples.rst | grep '^@@\|^diff'
diff --git a/.../testdata_examples.rst b/.../testdata_examples.rst
@@ -198 +198 @@ / @@ -397 +397 @@ / @@ -676 +676 @@ / @@ -1471 +1471 @@ / @@ -1851 +1851 @@
diff --git a/.../testdata_notation.rst b/.../testdata_notation.rst
@@ -40 +40 @@ / @@ -108 +108 @@ / @@ -354 +354 @@ / @@ -586 +586 @@ / @@ -1220 +1220 @@
```

**証拠コマンドの訂正（§13 R4-6 d）**: 本節は当初 `git diff -U0 HEAD -- …`（ワーキングツリー基準）を証拠として挙げていたが、ラウンド3の内容は `6cc01b7` としてコミット済みであり、そのコマンドを今叩いても無出力になって再現しない。上のとおり `git show 6cc01b7` を基準にしたコマンドに差し替えた。

ハンクは examples 5個・notation 5個で計10個。**すべて `@@ -n +n @@` の1行等長置換であり、行の増減は0である**。指摘8件に対しハンクが10個になるのは、R3-A-3 が4行にまたがり、そのうち `examples:676` だけが R3-A-4 と同一行であるためである（8件が触れた**異なる行**は 40・108・354・586・198・397・676・1471・1220・1851 の10行）。

| # | ファイル:行（現在） | 何を | なぜ | 指摘 |
|---|---|---|---|---|
| R3-A-1 | `testdata_notation.rst:108` | `データブロックとデータタイプ` 表の `用途`＝`テストショット` 行の `内容` セルを、`1回の実行（リクエスト送信・バッチ起動・バリデーション実行など）に対応する、1エントリ分の実行条件（期待するステータスコードなど）` → **`1回の実行に対応する、1エントリ分の実行条件（番号・説明・リクエストIDなど）`** | 頭の名詞が `実行条件` なのに唯一の例示が `期待するステータスコード`＝**実行後に確認する値**であり、兄弟行 `期待値`（`テスト実行後に確認する状態`）と読者の区別が曖昧だった。また1セル内に `実行` が3回出て、兄弟行（`準備データ` **26字**・`期待値` 38字。**当初「25字・38字」と書いていたのを §13 R4-6 c で実測に訂正**）に対し65字と分量が突出していた。`:381`〜`:394` の共通必須カラム（`no`＝テストショット番号、`description`＝説明、`expectedStatusCode`）と `:396` 以降の処理方式別カラム（`context` の `REQUEST_ID` など）を実際に読み、**実行の識別・パラメータ側**の例に替えた。**例示に `テストショット番号` を使うと循環定義に戻るため `番号` とした。** 結果は39字・`実行`×2 で兄弟行と同水準。`list-table` のため表示幅の調整は不要。**この文言は §13 R4-1 で差し替えた**（`リクエストID` は `testShots` のカラムではなく事実誤りであったため。現行は48字） | 設計#2・クラフト⑥ |
| R3-A-2 | `testdata_notation.rst:40` | `また :ref:`マスタデータ復旧機能`\ を使うと、**実行中に**生じたマスタデータの変更を**テスト終了時に**…マスタデータを変更する**場合**でも、**他のテストに**影響を与えずに実行できる。` → `また :ref:`マスタデータ復旧機能`\ を使うと、**テスト実行中に**生じたマスタデータの変更を**終了時に**…マスタデータを変更する**テストであっても**、**他に**影響を与えずに実行できる。` | (a) 「実行中に」の直前の名詞が「マスタデータ復旧機能」であり「復旧機能の実行中」と読めた、(b) 「マスタデータを変更する**場合**でも、…**実行できる**」で「実行できる」の主語が消えていた（旧文の主語は「テストケース」）。`テスト` 系の語を2回に保ったまま主語を戻した | クラフト② |
| R3-A-3 | **4行**（`notation:354`・`notation:586`・`examples:397`〈2出現〉・`examples:676`。うち `examples:676` は R3-A-4 と同一行であり、削除側の `grep -c 利用例` は4行・5出現） | `利用例` → **`用途`**。あわせて `notation:586` の「〜が持つカラムの処理方式ごとの説明は、前述の :ref:`…` を参照。」→「〜の**カラムについては、処理方式ごとに**前述の :ref:`…` **で説明している**。」（「の」の連結の解消） | `利用例` は `ja/` 配下の既存解説書に**先例が0件**だった（`grep -rho '利用例' --include='*.rst' ja/ \| wc -l` は今回編集した2ページの5件のみ。`使用例` は27件）。しかも `examples:676` は同じ関係を「用途ごとに示す」と書き、`用途` は `notation:104` の表の列見出しでもある。**用語統一を目的とする本タスクが新しい揺れを持ち込んでいた** | クラフト① |
| R3-A-4 | `testdata_examples.rst:676` | `…の記述例を、用途ごとに示す。**利用例**の1つであるテストショット一覧（``testShots``\ ）の**記述例**は、前述の :ref:`…`\ に**示している**。記法の仕様は :ref:`…`\ を参照。` → `…の記述例を、用途ごとに示す。**用途**の1つであるテストショット一覧（``testShots``\ ）は、前述の :ref:`…`\ で**扱っている**。記法の仕様は :ref:`…`\ を参照。` | `記述例`×2・`示す`＋`示している`・`用途` と `利用例` の同義競合が1段落に同居していた。**「前述の」が成立することを参照先ラベルの実位置で再確認した**（`.. _testdata_examples-test_shots:` が393、見出しが395、参照元が676。393 ＜ 676 で成立。§3-3 の Z-24） | クラフト⑤ |
| R3-A-5 | `testdata_examples.rst:198` | `明細1件の正常注文**（テストショット1）**と明細2件の大量注文**（テストショット2）**という2つのテストショットを実行する例である。` → `明細1件の正常注文と明細2件の大量注文という2つのテストショットを実行する例である。` | 導入文だけ `テストショット`×4 で冗長側に残っていた。直後の表（`:204`〜`:212`）はラウンド2 R2-A-5 で列見出し `テストショット` ＋ 値 `1（正常注文）` の形に短縮済みであり、導入文がその形と揃っていなかった。`テストショット`×2 になった | クラフト④ |
| R3-A-6 | `testdata_examples.rst:1471` | `グループIDなしのデータブロックが1件処理**のテストショット**に、``case2``\ のデータブロックが複数件処理**のテストショット**に対応する。` → `…が1件処理に、…が複数件処理に対応する。` | 対句である `:502` はラウンド2 R2-A-6 で指示語（`後者では`）を使って `テストショット` 3連発を回避したのに、`:1471` は素の置換のままで不揃いだった。何のテストショットかは同じ文の冒頭（`テストショットごとに異なる入力ファイルを使い分ける例である。`）が与える | クラフト③ |
| R3-A-7 | `testdata_notation.rst:1220` | `（テストの内容**上**、長さを揃えられない場合は手動で**テストを**行う）` → `（テストの内容**によって**長さを揃えられない場合は手動で行う）` | 「〜の内容上」は硬い造語的表現であり、本タスクの STEP 2（N-31）が作ったものだった | クラフト⑧ |
| R3-A-8 | `testdata_examples.rst:1851` | `特殊記法を使う値の記述例を示す。**特殊記法の仕様は** :ref:`…`\ を参照。` → `特殊記法を使う値の記述例を示す。**記法の仕様は** :ref:`…`\ を参照。` | 同ページの他のL2導入文が「記法の仕様は :ref:`…` を参照。」で揃っていることを実測で確認して合わせた（`:676`・`:780`・`:1030`・`:1588`・`:1998`）。**回答 `ntf-doc-terminology-answer.md` §2 が決めた「値の種類の列挙を落とす」という方針 (c) は変えていない。変えたのは後半の言い回しのみ** | クラフト⑧ |

**R3-A-9（全体確認）**: 上記8件について、編集した各文を**前後の段落ごと通しで読み直した。** 個別修正の積み上げによる新たな不整合は次のとおり無かった。

- 語の重複: `notation:108` の `実行`×2（兄弟行と同水準）、`notation:40` の `テスト` 系×2、`examples:198` の `テストショット`×2、`examples:1471` の `テストショット`×1。いずれも解消側に振れている
- 係り受け: `notation:40` の「実行できる」の主語が「マスタデータを変更するテスト」に戻っていること、`notation:586` の「〜のカラムについては、処理方式ごとに…で説明している」が二重の「の」を持たないことを確認
- 参照の向き: `examples:397`（後述の → 672）・`examples:676`（前述の → 393）・`notation:354`（後述の → 580）・`notation:586`（前述の → 350）の4件を、ラベルの実位置で再確認（§3-3 の Z-2・Z-7・Z-23・Z-24）
- `利用例` の全廃: `grep -rn '利用例' ja/ --include=*.rst` → **0件**

### 12-2. `glossary.md`・`reviews/` の編集（R3-B-1〜B-3・R3-C-1）

| # | 指摘 | 実施内容 |
|---|---|---|
| R3-B-1【重大】 | **用語集の定義と本文の定義セルの関係が明示されていない。** `glossary.md:201` は `1組の入力と期待値`、`testdata_notation.rst:108` は `1エントリ分の実行条件`。ラウンド2が本文を書き換えた理由は「『1組の入力と期待値』は兄弟行 `準備データ`・`期待値` を包含し3つの用途が排他にならない」だったのに、**規範側である用語集には旧文言がそのまま残っており、次ページの作者が旧文言を見て書く経路が開いていた** | **定義文そのもの（`1組の入力と期待値`）は書き換えていない。** 作業指示 `ntf-doc-terminology.md` 1-2 がユーザーの指定した趣旨だからである。代わりに **`テストショット` の項の採用根拠欄の末尾に、レイヤの違いを述べる注記を加えた**。(a) 用語集の定義は**用語そのものの意味**を述べたものであること、(b) `データブロックとデータタイプ` の表は**1つの読み込み単位に共存する3つの用途を排他に分ける**という別の目的の表であり、そこでは兄弟行と重ならないよう同じ概念を `実行条件` の側から言い表していること、(c) **両者は同じものを指すこと**、(d) 残り32ページでもこの使い分けに従うこと。既存の欄の調子に合わせて簡潔に書いた（設計#1・QA M-3・検証 R3-6） |
| R3-B-2 | **`テストケース` が `揺れ表記（使わない）` 欄と `別義・旧名称（文脈により使う）` 欄の両方にあり、しかも別義欄自身が「解説書では常に置き換える」と打ち消していて、表の列の意味として自己矛盾していた** | 2欄の役割が**欄の記述だけで**区別できるように書き分けた。**揺れ表記欄**: 「NTF解説書の本文に現れるテストケースはすべてこの揺れ表記であり、右欄の別義には当たらない。常に正表記側（§8 の対応表が定める置換先）に置き換える」。**別義欄**: 「**解説書の外で使われる別義**。…NTF解説書の本文にはこの用法が現れないため、左隣の揺れ表記欄が定める置換の対象にならない。用語集・設計文書が業務上のテストケースと `テストショット` の層の違いを対比するときにだけこの語を使う」。**「解説書では常に置き換える」という結論は変えていない**（設計#3） |
| R3-B-3 | `glossary.md:552` の置換ルールにある「マッピング対象範囲内の `テストケース` **196件**を実物で全数確認した」が本リポジトリでは再現できない | **主張は削除せず**、`R2-B-1` と同じ扱いで「本リポジトリでは未照合である（出典は作業指示）」と明示した。`ntf-doc-terminology.md:19` を実際に読み、何を数えた196なのかを確認したうえで、自分でも計数して**いずれも196にならない**ことを示した（下記）（検証 R3-7） |
| R3-C-1 | `reviews/page-testdata_notation.md` の申し送り17 の原則文「読者が最初に書き始めるもの（全体）を先に置き、その**構成要素（個別）**を後に置く」が、実際の並び（`LIST_MAP` の**用途の1つ**である `テストショット一覧` を `LIST_MAP` より前に置く）と語義が逆だった（テストショット一覧は LIST_MAP の構成要素ではない） | 原則名を「**作業の入口から詳細へ**」に改め、包含関係が「一種である」「用途の1つである」のどちらでも誤適用しない言い方にした。`#10a` の実例（テストショット一覧は `LIST_MAP` の用途の1つ）を明記し、「全体→構成要素」という言い方は使わないと書いた。**並び順の判断そのものは変えていない。** `page-testdata_examples.md` の申し送り15 にも同じ言い方があったため同時に是正した（設計#7） |

**R3-B-3 の計数（実測）**: 作業指示 `ntf-doc-terminology.md:19` の原文は「マッピング対象範囲内の `テストケース` 196件を実物で確認した結果、すべて `testShots` の行を指すか、何を検証するかを述べるかのいずれかであった」である。本リポジトリで数え方を変えて計数したが、196になるものは無かった。

```
# mapping.csv の全594行について src_body_start〜src_body_end を実物から切り出して計数
# 現行解説書 .rst は #7 で削除済みのため 6bf8cfb^ の内容で読む
マッピング対象範囲内 : current 174 + input 41 = 215
出典ファイル全体     : current 191 + input 43 = 234
対象3ページ（6ba0d2a）: notation 42 + examples 33 + about 2 = 77
glossary.md §5.7 の自記録: 現行155 + input38 = 193
```

**判定: 出典（`S:ntf-doc-terminology.md:19`）が正しいことは確認できるが、196 という数は本リポジトリでは再現できない。** 主張は削除せず、`glossary.md:552` に「本リポジトリでは未照合である」と明記した。

### 12-3. 記録の是正（R3-D-1・R3-D-2）

| # | 箇所 | 実施内容 |
|---|---|---|
| R3-D-1【重大】 | §3-3・§7 ゲート13・§11-3 | **§3-3 の本表に Z-23（`examples:397`）・Z-24（`examples:676`）を追加**した（参照先ラベルの実位置と方向の成立を実測で確認して記載）。**§12 に追記して済ませず §3-3 本体を直した**（完了条件が「表で記録されている」ことを求めているため）。行番号は R3-A の本文編集後の最終値である。**件数表記は3通りに割れていた**（§3-3 見出し「17件」／ゲート13 本文「全21件（examples 2行）」／§11-3「Z-1〜Z-22」）ため、実測（notation 18行・examples 4行＝**方向語22行**）に合わせて **全24件（Z-1〜Z-24）** に統一した（検証 R3-1・QA M-1） |
| R3-D-2 a | §3-4 | 「`design.md:65` は変更していない。コーディネーターの判断を仰ぐ」が古い状態のままだったため、「その後 §10 E-1 で置換済み・ゲート15 で0件」の追記を入れた（QA M-2） |
| R3-D-2 b | §3-2 | 追加文として引用している文言がラウンド2・3 で書き換わる前のものだったため、「現物は §11-1 #3・§12 の R3-A-3 を参照」の訂正を入れた。方向語と `:ref:` の参照先は当時から不変であり Z-7・Z-23 の判定に影響しないことも明記（検証 R3-5） |
| R3-D-2 c | §7 ゲート10・§9 | ゲート10 の誤検出件数が §7「6件」／§9「9件」で矛盾していた。`=` 罫線の除外を無効化した写しで再実行し、**実測9件**（`about/index.rst` L86〜L94）を確認して**9件に統一**した。「6件」はデータ行だけを数えた値だった（検証 R3-3） |
| R3-D-2 d | §9 | ゲート12 の残差が §9 だけ「examples 1件」（ラウンド1以前の値）だったため、**最新値（notation -10/+10・examples -8/+8・about -1/+1）** に更新した（検証 R3-3） |
| R3-D-2 e | §11-2 R2-C-6 | 「空文字は値として**12行**に出現」だが続く列挙は YAML 12行＋Excel 空セル2行＝**14行**だった。実測に合わせて **14行**に訂正し、内訳を明示した（検証 R3-4） |
| R3-D-2 f | §7 ゲート15・ゲート12 | 「`design.md` のハンクは `@@ -65 +65 @@` の1つのみ」は基準 `6ba0d2a` では**誤り**だった。`git diff 6ba0d2a -U0 -- design.md \| grep '^@@'` の実出力（4ハンク）と `git show <rev>:design.md \| grep -c` の実出力（`6ba0d2a` 2件／`2e7c86b` 1件／`7a74832` 1件／`HEAD` 0件）で裏付け、**どの基準コミットでの話かを表で書き分けた**。`:65` 以外の3ハンクと `:284` の `テストケース` の消滅は `2e7c86b`（`#10` 承認時の定型圧縮、`#10a` 着手前）由来である（検証 R3-2） |
| R3-D-2 g | §11-1 末尾 | 「変更しなかった `ケース`」の判断が `testdata_examples.rst` 限定で、`testdata_notation.rst:666`・`:850` が記録のどこにも現れていなかった。**両ページ分を1つの表に揃え**、notation の2件（いずれも「場合」の意の一般語）を置換不要と判定した理由を書いた。**両ページ合わせて 5行・6出現、すべて置換不要**（検証 R3-8） |
| R3-D-2 h | §7 ゲート3 | 但し書き「`.rst` 以外では `_download/*.java` 2ファイルに残る」が不完全だった。**ファイル内容6箇所（2ファイル）に加えて、ファイル名にも1件残る**（`…/_download/ユーザ登録_UserComponent_クラス単体テストケース.xlsx`。`grep -rn 'ユーザ登録_UserComponent' ja/ en/` が0件＝孤立資産）ことと、`guide/` 配下が **125ファイル・4.9MB・`.rst` 0件**であることを実測で補った（検証 R3-9） |

### 12-4. 対応しなかった指摘と、その理由（全件）

**却下したものも黙って落とさず、全件を理由つきで記録する。**

| 指摘 | 判断 | 理由 | 申し送り先 |
|---|---|---|---|
| **設計#4**: STEP 4 の改題が導線文（`notation:126`・`:138`・`:348`）に伝播していない | **本タスクでは対応しない。申し送りに回す** | 作業指示 STEP 4 は見出しの改題のみを求めており、3箇所の導線文の書き換えは本タスクの範囲外（本文編集は禁止事項の例外として最小限に限る運用）。改善提案としては妥当 | `page-testdata_notation.md` 申し送り**19** |
| **設計#6**: `examples:1851` の導入文に「空文字・改行の記述例は書き方ページを参照」の1文を足す | **却下** | 回答 `ntf-doc-terminology-answer.md` §2 でユーザーが (c) を採用・(b) を不採用と決めている。導入文に範囲の説明を戻すのはその決定に反する。例の欠落は §10 G の突合表で開示済み | — |
| **設計#8**: `notation:126`・`:138` の列挙が `テーブル`・`LIST_MAP` を落としている | **本タスクでは対応しない。申し送りに回す** | **基準コミット `6ba0d2a` 以前から存在する既存の穴であり `#10a` 由来ではない。** `git show 6ba0d2a:….rst` の `:126`・`:138` も「グループID・**テストケース**一覧・ファイルデータ・メッセージング」の4つだけで、語が `テストケース一覧` だった点しか違わない | `page-testdata_notation.md` 申し送り**20** |
| **クラフト⑦**: `about/index.rst:24` の `テスト` 接頭語3連 | **却下** | `#8` 承認済みの確定文であり、`design.md:65` と語を揃えた結果（§10 E-1）。再度書き換えると `design.md` と再び食い違う。**ゲート14（`about/index.rst` の差分は L24 の1行のみ）を維持する** | — |
| **QA S-1**: ゲート7・8 の「各1件」という文言を指示書側で直す | **本タスクでは対応しない。申し送りに回す** | 指示書はユーザーの作業指示であり本タスクの変更対象ではない。記録側では既に「見出し行としての件数」と明示済み（§7 ゲート7・8） | `page-testdata_notation.md` 申し送り**21** |
| **QA S-2**: ゲート1 の数値不変チェックはゲート2 から自動的に従う | **本タスクでは対応しない。申し送りに回す** | ゲートの設計に関する観察であり、成果物の欠陥ではない | `page-testdata_notation.md` 申し送り**22** |
| **QA S-3・検証 R3-9**: `guide/` 配下の遺留アセット（125ファイル・4.9MB、`.java` 6箇所＋`.xlsx` ファイル名1件に `テストケース`） | **申し送りに回す**（但し書きの補足のみ R3-D-2 h で対応） | どの `.rst` からも参照されず公開物に出ない。ゲート3 の対象外 | `page-testdata_notation.md` 申し送り**23**（`#last` の棚卸し対象） |

**申し送りの追加にあたって、`page-testdata_notation.md`・`page-testdata_examples.md` の現状を先に全件読み、同趣旨の重複が無いことを確認した。** 既存の申し送り番号（16・17・18／14〜17）は振り直していない（`checks/task-terminology.md` §11-2 の R2-C-5 が番号で参照しているため）。

### 12-5. ゲート1〜17 の再実行結果

ラウンド3の本文適用後に**全17件を再実行**した。「変化」列が `—` のものは再実行して同値だったことを示す。**§7 は本表の値に更新済みである。**

| ゲート | 内容 | 結果 | 変化 |
|---|---|---|---|
| 1 | `verify_mapping.py` が exit 0、594行 / 12,986 / 11,983 | **PASS** | — （`Loaded 594 rows`／`lines total (all rows): 12986`／`(excluding DROP): 11983`／`OK: no errors`／exit 0。行数は `csv.DictReader` で 594。`wc -l` は使っていない） |
| 2 | 禁止ファイル5対象の差分が空 | **PASS** | — （`git diff --stat 6ba0d2a -- mapping.csv _batch/ vocabulary.md style.md ja/conf.py` が無出力。ワーキングツリー込みで確認） |
| 3 | `testing_framework/` 配下の全 `.rst` に `テストケース` 0件 | **PASS** | — （`grep -rn … --include=*.rst \| wc -l` → `0`）。**但し書きを R3-D-2 h で実測により補完した**（`.rst` 以外の残存6箇所＋ファイル名1件） |
| 4 | `glossary.md` の `:403`〜`:449` に差分なし | **PASS** | — （`diff` が空。`identical`）。ラウンド3 の `glossary.md` 編集は `:201` と `:552` の2行のみ |
| 5 | 見出し数が不変・アンダーライン長の不一致0 | **PASS** | — （notation `{'=':1,'-':3,'~':10,'^':26}`／examples `{'=':1,'-':9,'~':28,'^':56}`。`unicodedata.east_asian_width` による表示幅計測で、notation は `-`/`=` が `max(50,表示幅)`・`~`/`^` が49固定、examples は全94見出しが `max(50,表示幅)` に一致。**不一致0件**。ラウンド3 の本文編集は見出しを1つも触っていない） |
| 6 | 両ページの並びが STEP 3-1 の順 | **PASS** | — （notation `~` の4〜10番目と examples `-` の3〜9番目が、7見出しとも同一順・同一文言） |
| 7 | 旧見出し0件・新見出しが見出し行として各1件 | **PASS** | — （notation 旧0／総出現4／`:ref:`3→見出し行1、examples 旧0／総出現3／`:ref:`2→見出し行1） |
| 8 | `テストケース一覧` 0件・`テストショット一覧（testShots）を記述する` が見出し行として各1件 | **PASS** | — （notation `テストケース一覧`0／総出現3／`:ref:`2、examples 0／総出現4／`:ref:`3。**ラウンド2 の値と同値**） |
| 9 | `:ref:` の未定義参照0件・リンクテキストに旧見出しなし | **PASS** | — （`grep -n ':ref:`テストケース一覧\|:ref:`値を特殊記法' … \| wc -l` → `0`。未定義参照はゲート11 のビルド警告で確認） |
| 10 | 段落内改行が0件 | **PASS** | — （3ファイルとも `0 件`、exit 0）。**誤検出件数の矛盾を R3-D-2 c で9件に統一した** |
| 11 | Docker フルビルド（`-a`） | **PASS** | **再実行（4回目）**。`build succeeded, 1 warning`／`EXIT=0`。警告は `db_double_submit.rst:108` の既知1件のみで**新規0件**（`grep -c 'WARNING\|ERROR'` → `1`）。イメージは `nablarch-document-build-sandboxed:latest`（Sphinx 1.3.6 / docutils 0.15.2 を実行前に確認）。ローカル venv は不使用。`locales/ja/LC_MESSAGES/sphinx.mo` は `git checkout` で復元済み。`_build/` は削除していない |
| 12 | 情報が失われていないこと | **PASS** | **変化あり**。examples の正規化後の残差が **-7/+7 → -8/+8**（増分1件＝`:198`／R3-A-5）。素の多重集合（notation -36/+36・examples -43/+43・about -1/+1）と行数（1520／2101／118）は3ファイルとも不変。**削除された段落・表の行・コードブロックは0件。** ラウンド3の他の本文編集はすでに残差だった行の書き換えであり件数を増やしていない |
| 13 | STEP 2 の置換判定表・STEP 3-2 の前後関係の確認表が全件記録 | **PASS** | **変化あり**。§3-3 に **Z-23・Z-24 を追加**し、件数表記を全箇所で **全24件（うち `grep` 由来の方向語22行＝notation 18・examples 4）** に統一した（R3-D-1）。§2 の全77件は不変 |
| 14 | `about/index.rst` の変更が L24 の1行のみ | **PASS** | — （`git diff -U0 6ba0d2a -- about/index.rst \| grep '^@@'` → `@@ -24 +24 @@` の1つのみ。**クラフト⑦ を却下したためこのゲートを維持できている**） |
| 15 | `design.md` に `テストケース` が0件 | **PASS** | — （`grep -c` → `0`）。**基準コミット別の書き分けを R3-D-2 f で追加した**（`6ba0d2a` 基準ではハンク4つ・ヒット2件、`7a74832` 基準ではハンク1つ・ヒット1件） |
| 16 | 特殊記法セクションの導入文と見出しに語の不一致なし | **PASS** | — （examples `:1849`-`:1851`、notation `:1275`-`:1277` を再確認。**R3-A-8 で `:1851` の後半を「記法の仕様は」に揃えたが、値の種類を一つも列挙しないという方針 (c) は不変**であり、見出しの語との不一致は構造的に発生しない） |
| 17 | 値の種類10種の突合表が記録されていること | **PASS** | — （§10 G の突合表。ラウンド2 の分類基準〈R2-C-6〉と除外理由〈R2-C-7〉も維持。**R3-D-2 e で R2-C-6 の空文字の行数を 12→14 に訂正**した。例は追加していない） |

**全17件 PASS。値が変わったのはゲート12・13 の2件と、再実行したゲート11 である。**

**本表はラウンド3 時点の値である。ラウンド4 の再実行結果は §13-5 にある**（17件すべてがラウンド3 と同値であった）。

---

## 13. 差し戻し対応ラウンド4（2026-08-07）

### 13-0. 本ラウンドの位置づけ

ラウンド3の是正（§12）に対する4観点の再レビューで、**クラフトと設計が FAIL** を出した。**原因はラウンド3の是正そのものが本文に持ち込んだ退行であり、本ラウンドはその退行の是正だけを行っている。新しい改善・推敲は足していない。**

**本節の項番はラウンド4のものであり、§10 の `A-1`〜`D-4`／§11 の `R2-*`／§12 の `R3-*` とは別系列である。** 混同を避けるため、本節では接頭辞 `R4-` を付ける（本文編集は `R4-1`〜`R4-4`、記録の是正は `R4-6`・`R4-7`）。§10〜§12 の記述は原則として書き換えず、本節で訂正・追加する（例外は §13-3 に挙げた8件で、いずれも「現物と一致しない記録」であるため該当箇所を直接直した）。

**誤読の罠として明示的に除外したもの**: `testdata_notation.rst` の `文字列の null` に関する2箇所（`Excel形式の場合` 配下で「`"null"` で表現可」、`YAML形式の場合` 配下で「該当なし」）は**矛盾ではない**。形式差を意図して書き分けている。**本文の修正は行っていない**（§12 冒頭と同じ扱い）。

### 13-1. 本文編集の全件（R4-1〜R4-4）

```
$ git diff -U0 -- .../testdata_notation.rst .../testdata_examples.rst | grep '^@@\|^diff'
diff --git a/.../testdata_examples.rst b/.../testdata_examples.rst
@@ -397 +397 @@ / @@ -676 +676 @@
diff --git a/.../testdata_notation.rst b/.../testdata_notation.rst
@@ -40 +40 @@ / @@ -108 +108 @@ / @@ -354 +354 @@ / @@ -586 +586 @@ / @@ -1220 +1220 @@
```

ハンクは examples 2個・notation 5個で計7個。**すべて `@@ -n +n @@` の1行等長置換であり、行の増減は0である**（是正4件のうち R4-4 が4行にまたがるため、ハンクは7個になる）。

| # | ファイル:行（現在） | 何を | なぜ | 対応するレビュー指摘 |
|---|---|---|---|---|
| R4-1 | `testdata_notation.rst:108` | `データブロックとデータタイプ` 表の `用途`＝`テストショット` 行の `内容` セルを、`1回の実行に対応する、1エントリ分の実行条件（番号・説明・リクエストIDなど）` → **`リクエスト送信・バッチ起動などの1回の実行を表すエントリ（番号・説明・期待するステータスコード）`** | ラウンド3の是正が**3つの退行**を持ち込んでいた。(1) **`リクエストID` は `testShots` のカラムではない（事実誤り）。** `:408` のとおり `context` は「リクエストID・ユーザ情報・HTTPメソッドを記載した ``LIST_MAP`` **名**」であり、リクエストIDは `context` が指す別の `LIST_MAP` の `REQUEST_ID` カラムにある。バッチ・メッセージングの `testShots` カラムは `:476`・`:529` の `requestPath`（リクエスト**パス**）である。(2) **`1回の実行` の語釈が本文から消えた。** ラウンド3が落とした `（リクエスト送信・バッチ起動・バリデーション実行など）` が両ページで唯一の語釈だった（`grep -n 'バッチ起動\|リクエスト送信' testdata_notation.rst testdata_examples.rst` のヒットは `:502` のコマンドライン引数の説明のみ）。`テストショット` は本タスクが新たに導入した語であり、読者は用語集を読まない。(3) **`expectedStatusCode` が落ちた。** これは `:389`〜`:394` のとおり `no`・`description` と並ぶ全処理方式共通の必須カラム3つの1つであり、消えたことで3用途の表が「期待値は `EXPECTED_*`／`RESPONSE_*` にしかない」と読めるようになっていた。**頭の名詞を `実行条件` に固定するのをやめた**（`no`・`description` は識別・注記であって実行条件ではないという噛み合わせの問題も同時に解く）。兄弟行が `データ`（準備データ）・`状態`（期待値）という**内容の種類**で分かれるのに対し、テストショットの弁別的な特徴は「**1回の実行を表すエントリそのもの**」であるため、この軸で書いた。挙げた3カラムはすべて `:389`〜`:394` で実在を確認済み。**48字**（兄弟行は `準備データ` 26字・`期待値` 38字）・`実行`×1・循環定義なし。`list-table` のため表示幅の調整は不要 | クラフト・設計・検証（4名中3名が FAIL／軽微として指摘、残る1名も同旨） |
| R4-2 | `testdata_notation.rst:1220` | `（テストの内容によって長さを揃えられない場合は手動で行う）` → **`（長さを揃えられない内容のテストは、手動で実施する）`** | ラウンド3が「手動で**テストを**行う」から目的語を落とした結果、直前が「長さを揃える」であることと相まって**「長さ揃えを手動でやる」**と読める状態になっていた。原文の趣旨は「そのテスト自体を手動で実施する」であり、**意味が反転していた** | クラフト |
| R4-3 | `testdata_notation.rst:40` | `テスト実行中に生じたマスタデータの変更を**終了時に**…マスタデータを変更するテストであっても、**他に**影響を与えずに実行できる。` → `…を**テスト終了時に**…**他のテストに**影響を与えずに実行できる。` | ラウンド3は主語を1つ戻す代わりに、`テスト終了時`→`終了時`、`他のテストに`→`他に` と限定を2つ落としていた。「他に」は単独で名詞として立たない | クラフト |
| R4-4 | `notation:354`・`notation:586`・`examples:397`・`examples:676`（4行） | `用途` を使わない言い換え。`notation:354`「これは ``LIST_MAP`` というデータタイプ**を使うデータブロック**の1つであり」／`notation:586`「**このデータタイプを使うデータブロックのうち**、テストショット一覧（``testShots``\ ）のカラムについては、前述の :ref:`…` **で処理方式ごとに**説明している」／`examples:397`「**これは** ``LIST_MAP`` というデータタイプを使うデータブロックの1つであり、**同じデータタイプを使う他のデータブロックの記述例**は後述の :ref:`…` に示す」／`examples:676`「**このデータタイプを使うデータブロックのうち**、テストショット一覧（``testShots``\ ）は前述の :ref:`…` で扱っている」 | ラウンド3が `利用例` を `用途` に寄せた結果、**同一ページの `:126` と正面から衝突した**。`:126` は「データタイプは、データブロックの**用途**を示す予約語」と定義しているのに、`:354` は「1つのデータタイプが複数の用途を持つ」と書くことになる。さらに `:104` の列見出し `用途`（3値）・`:147` の列見出し `用途`（14データタイプ）と合わせて同一ページで3粒度になっていた。**`利用例` に戻すのも不可**（`ja/` 配下に先例0件）。そのページが既に確立している語彙（`データブロック`・`データタイプ`）で言い換えた。「テストショット一覧はデータタイプ ``LIST_MAP`` を使うデータブロックの1つである」は `:126` の定義と矛盾しない事実である。**`:104`・`:126`・`:147` の列見出しと定義文には触れていない。** あわせて、`examples:397` の重さ（見出し直下2行で `テストショット一覧`3回・`LIST_MAP`3回。対になる `notation:354` は代名詞で書いており鏡合わせのはずの2ページで主語の立て方が揃っていなかった）、`examples:676` の `用途ごとに示す。用途の1つである…` という句点をまたいだ直結反復、`notation:586` の「処理方式ごとに前述の」が一続きに読める副詞句の位置を、**この4文の書き換えの範囲内で**解消した（新しい文は足していない） | 設計・クラフト |

**編集後の確認**: `grep -rn '利用例' ja/ --include=*.rst` → **0件**（ラウンド3から維持）。`grep -n '用途' testdata_notation.rst testdata_examples.rst` のヒットに `:354`・`:586`・`examples:397` は含まれず、`examples:676` は1文目の「用途ごとに示す」（兄弟L2導入文 `:780`・`:1030`・`:1588` と揃った形。触れていない）のみが残る。`:126` の定義文との矛盾は無い。

### 13-2. 通し読み（R4-5）

編集した各文を、**編集箇所だけでなくその節全体**を通しで読み直した。ラウンド3が個別修正の積み上げで新たな不整合を持ち込んだのと同じ失敗を繰り返さないためである。確認したのは次の点で、新たな不整合は見つからなかった。

- **事実関係**: `:108` に挙げた3カラムが `:389`〜`:394` の共通必須カラム表と一致すること、`リクエストID` が本文から消えたこと（`:408` の `context` の説明とは矛盾しない）
- **兄弟行との排他**: `:108` の頭の名詞が `エントリ` になったことで、`準備データ`（データ）・`期待値`（状態）と種類が重ならないこと。`expectedStatusCode` を挙げても `期待値` 行（`テスト実行後に確認する状態`）と衝突しないこと（`testShots` のカラムであり、`EXPECTED_*` のデータブロックではないため）
- **係り受け**: `:1220` の括弧内で「手動で実施する」の目的語が「テスト」であること、`:40` の「実行できる」の主語が「マスタデータを変更するテスト」であること
- **参照の向き**: `notation:354`（後述の）・`notation:586`（前述の）・`examples:397`（後述の）・`examples:676`（前述の）の4件が、R4-4 の書き換え後も方向語と参照先の位置関係を保っていること（§3-3 の Z-2・Z-7・Z-23・Z-24 の判定は不変。ラベル位置も行番号も変わっていない）
- **語の重複**: `:108` の `実行`×1、`examples:397` の `テストショット一覧`1回＋`:ref:` リンクテキスト1回・`LIST_MAP`1回＋リンクテキスト1回、`examples:676` の `用途`1回。いずれも解消側に振れている

**通し読みで見つけたもの**: `examples:401` の「正常ケースと認証エラーケースの2件を実行する例である」が、兄弟の導入文（`:198`・`:1030`）と数え方の語が非対称のまま残っている。これはレビュアー間で判断が割れており、**§13-4 に両論を記録して user review に委ねた**（本ラウンドでは変更していない）。それ以外に、R4-1〜R4-4 の編集が節内の他の文と食い違う箇所は無かった。

### 13-3. 記録の是正（R4-6 a〜h・R4-7）

| # | 箇所 | 実施内容 |
|---|---|---|
| R4-6 a | §2-1 判定表 | `置換後` 列が現物と一致しない4行（N-1a・N-1b＝`notation:40`、N-13＝`notation:725`、N-31＝`notation:1220`）に、**この列が置換直後の状態であることと最終文の参照先**を書き足した（N-1a・N-1b → §11-1 #2／§12 R3-A-2／§13 R4-3、N-13 → §10 A-4、N-31 → §12 R3-A-7／§13 R4-2）。他の該当行（N-11・N-15b・付随行）が既に持っているポインタと扱いを揃えた |
| R4-6 b | §12-1 R3-A-3 | 行リストが1行不足していた。実測では `examples:676` も `利用例`→`用途` の対象である（削除側の `grep -c 利用例` は**4行・5出現**）。**「4行（うち `examples:676` は R3-A-4 と同一行）」**に書き直し、あわせて「指摘8件に対しハンクは10個」の説明を自己整合する形（8件が触れた**異なる行**が10行であること）に直した |
| R4-6 c | §12-1 R3-A-1 | 「兄弟行（25字・38字）」が実測と1字ずれていた。`準備データ` の `内容` セルは **26字**（`期待値` は38字で正しい）。実測に訂正し、その定義セルが本ラウンド R4-1 で差し替えられたこと（現行48字）も併記した |
| R4-6 d | §12-1 | 証拠コマンド `git diff -U0 HEAD -- …` は、ラウンド3が `6cc01b7` としてコミット済みであるため今叩くと無出力になり再現しない。**`git show 6cc01b7 -U0 --format= -- …` に差し替えた**（差し替えた旨も明記） |
| R4-6 e | §3-4 | 「`mapping.csv` の `dest_section` は `テストデータの構造`／`使用方法` の2値のみ」が不正確だった。`csv.DictReader` の実測では `dest_page=テストデータの書き方` の140行の内訳は `使用方法` 120・`テストデータの構造` 19・**`機能概要` 1** の**3値**である。実測に訂正した（「順序を持たない」という結論は不変） |
| R4-6 f | §3-3 Z-12 | `notation:792` の「後続の行」は実測では**2箇所**（「後続の行がこのキーの一部を持たない場合」「後続の行に最初の行のキーにないものを追加しても」）。Z-11（`:698`）が「（2箇所）」と明記しているのに揃えて **`後続の行（2箇所）`** とした。**本表の件数は行数で数えているため、`24件`／`方向語22行` という値は変わらない**（Z-11 と同じ扱い） |
| R4-6 g | `reviews/page-testdata_notation.md:294` | サマリ行が「`使用方法` 配下を**「全体から個別へ」**に改めた」のままで、**同じファイルの申し送り17 が名指しで禁じている言い方**を使っていた（R3-C-1 の是正漏れ）。申し送り17 と同じ「**作業の入口から詳細へ**」に直し、指示字句からの逸脱については §13 R4-7 を見るよう参照を付けた。`grep -rn '全体から個別へ\|全体→構成要素' .rn/…` の残るヒットは、作業指示 `ntf-doc-terminology.md:102`・`:162` と `steering.md:313`（いずれも本タスクの変更対象外）と、「この言い方は使わない」と禁じている申し送り17・15 の本文だけである |
| R4-6 h | 申し送り23（`reviews/page-testdata_notation.md`） | `#last` の棚卸し対象が `ja/` の `.java` 6箇所と `.xlsx` ファイル名1件しか挙がっていなかった。実測では `.rst` にも残る。`grep -rn 'テストケース' --include=*.rst ja/ en/` の全ヒットは **3件・2ファイル**で、`en/…/06_TestFWGuide/01_Abstract.rst:66`（行頭 `..` のコメント行。`en/` はゲート3 の射程外）と `ja/biz_samples/11/index.rst:64`・`:68`（業務サンプル集でありNTF解説書32ページに含まれない）である。**いずれもゲート3 の射程外**であることを明記したうえで申し送り23 に併記した |

#### R4-7 — 指示字句からの逸脱（申し送りの原則名）

作業指示 `ntf-doc-terminology.md` は、申し送りに残す原則名を逐語で「**全体から個別へ**」と指定している。

```
$ grep -n '全体から個別へ' ntf-doc-terminology.md
102:**理由は「全体から個別へ」である。** 読者はテストショット一覧から書き始める。…
162:   - セクションの並びは「全体から個別へ」を原則とする
```

ラウンド3の R3-C-1 はこれを「**作業の入口から詳細へ**」に改称した。**指示の字句そのものを変えた旨の断りが無かったため、`R2-C-3`（`glossary.md` の定義文の逸脱）と同じ体裁で1項として記録する。**

| | 内容 |
|---|---|
| 逸脱の内容 | 申し送りに残す原則名を、指示の字句「全体から個別へ」から「**作業の入口から詳細へ**」に改称した（`reviews/page-testdata_notation.md` 申し送り17・`page-testdata_examples.md` 申し送り15、および同ファイルのサマリ行＝本ラウンド R4-6 g） |
| 逸脱した理由 | 実際の包含関係と逆になるため。並びは `テストショット一覧` を `LIST_MAP` より前に置いたが、**テストショット一覧は `LIST_MAP` の構成要素ではなく、`LIST_MAP` を使うデータブロックの1つ**である。「全体→構成要素」という言い方をそのまま原則名にすると、残り32ページで「包含する側を先に置く」と誤適用され、`LIST_MAP` を先に置く判断を導きうる |
| 趣旨は保っているか | **保っている。** 指示 `:102` の理由文自身が「読者はテストショット一覧から書き始める」＝作業順を根拠にしており、「作業の入口から詳細へ」はその根拠をそのまま名前にしたものである。**並び順の判断そのものは指示のとおりで、1つも変えていない**（ゲート6 で7見出しの順が両ページとも指示どおりであることを確認） |
| 影響 | 影響するのは申し送りの**原則名**だけであり、本文・見出し・並び順には及ばない |

**判断を仰ぐ点**: 指示の字句どおり「全体から個別へ」に戻すべきであれば、`reviews/page-testdata_notation.md` の申し送り17・サマリ行と `page-testdata_examples.md` の申し送り15 の3箇所を同時に戻す必要がある。ただしその場合、上記の誤適用のリスクが残る。

### 13-4. 対応しなかった指摘と、その理由（全件）

**却下したものも黙って落とさず、全件を理由つきで記録する。**

| 指摘 | 判断 | 理由 | 申し送り先 |
|---|---|---|---|
| **クラフト⑦**: `examples:401`「正常ケースと認証エラーケースの2件を実行する例である」を `テストショット` で数える形に直す | **対応しない。両論を記録し user review に委ねる** | **レビュアー間で判断が割れた。** QA は現状の判定を「妥当」と支持し、クラフトは兄弟の導入文（`:198`「2つのテストショットを実行する例である」・`:1030`）との非対称を指摘している。`正常ケース` は `:424`（Excel セル値）・`:478`（YAML の `description` 値）の**実データの引用**であり、地の文だけを変えると表・コードブロックの実値と食い違う（§11-1 末尾の判断表のとおり）。一方で「2件」の数え方だけを `2つのテストショット` に揃えることは実値を変えずに可能であり、クラフトの指摘は成り立つ。**どちらを採るかは書き手の判断に属するため本ラウンドでは変更していない** | — |
| **設計 提案5・クラフト⑧・設計 提案6**: 改題が導線文（`notation:126`・`:138`・`:348`）に伝播していない | **対応しない。申し送り19 で追跡済み** | 作業指示 STEP 4 は見出しの改題のみを求めており、導線文の書き換えは範囲外（本文編集は禁止事項の例外として最小限に限る運用）。ラウンド3で既に申し送りに記録済みであり、重複させない | `page-testdata_notation.md` 申し送り**19**（既存） |
| **設計 提案5**: `examples` の特殊記法セクションに「空文字・改行の記述例は書き方ページを参照」の1文を足す | **対応しない。反論も併記し user review に委ねる** | 回答 `ntf-doc-terminology-answer.md` §2 でユーザーが (c) を採用・(b) を不採用と決めている。欠落は §10 G の突合表で開示済みである。**ただし設計レビュアーは「(c) が定めたのは導入文の語列挙の扱いであって参照文の禁止ではない」と反論しており、この反論も成り立つ。** 回答の解釈に関わるため書き手の一存で決めず、判断を user review に委ねる | `page-testdata_examples.md` 申し送り**18**（欠落の追跡） |
| **設計 提案7**: `notation:98`「3つの用途」と `examples:18` ほか「4つのデータブロック」の食い違い（`LIST_MAP=expectedLog` が3行表に分類されない） | **対応しない。申し送りに追加する** | **基準コミット `6ba0d2a` 以前から同文であり `#10a` 由来ではない。** `git show 6ba0d2a:….rst` で確認した実出力は `notation:98`「テストケース・準備データ・期待値という3つの用途のデータ」・`examples:18`「テストケース一覧・準備データ・期待値・期待ログという用途の異なる4つのデータブロック」であり、語が `テストケース` 系だった点しか違わない | `page-testdata_notation.md` 申し送り**25**（新規） |
| **QA 提案1**: 「新語の初出に定義があること」をゲートに加える | **対応しない。申し送りに追加する** | 残り32ページのゲート設計に関する提案であり、本タスクのゲート1〜17 はユーザーの作業指示が定めたものである。**ただし指摘は妥当**（ラウンド3で定義セルから語釈が落ちたとき、17件のゲートのどれもそれを検出できなかった） | `page-testdata_notation.md` 申し送り**26**（新規） |
| **QA 提案2**: ゲート3 の対象を `.rst` から公開物（生成HTML・`searchindex.js`・`_download/`）へ広げる | **対応しない。申し送りに追加する** | 同上。`#last` の棚卸しで扱う（申し送り23 と同じ棚卸し） | `page-testdata_notation.md` 申し送り**27**（新規） |
| **QA 提案3**: 記載例ページの特殊記法セクションに例が無いことを `#11` 以降の申し送りにも残す | **申し送りに追加する** | 追跡が切れないようにするため。`checks` 側（§10 G・§11-2 R2-C-6）と `reviews` 側の節には記録済みだが、**申し送り欄には無かった** | `page-testdata_examples.md` 申し送り**18**（新規） |
| **検証 提案6**: 定義セルの `リクエストID` は水準が揃っていない | **R4-1 で対応済み**（重複指摘） | — | — |

**申し送りの追加にあたって、`page-testdata_notation.md`・`page-testdata_examples.md` の現状を先に読み、同趣旨の重複が無いことを確認した。** 既存の申し送り番号（notation 16〜23／examples 14〜17）は振り直していない（他の記録が番号で参照しているため）。追加は notation 24〜27・examples 18 である。

### 13-5. ゲート1〜17 の再実行結果

ラウンド4の本文適用後に**全17件を再実行**した。「変化」列が `—` のものは再実行して同値だったことを示す。

| ゲート | 内容 | 結果 | 変化 |
|---|---|---|---|
| 1 | `verify_mapping.py` が exit 0、594行 / 12,986 / 11,983 | **PASS** | — （`Loaded 594 rows from mapping.csv`／`lines total (all rows): 12986`／`(excluding DROP): 11983`／`OK: no errors`／exit 0。行数は `csv.DictReader` で 594。`wc -l` は使っていない） |
| 2 | 禁止ファイル5対象の差分が空 | **PASS** | — （`git diff --stat 6ba0d2a -- mapping.csv _batch/ vocabulary.md style.md ja/conf.py` が無出力。`git status --porcelain` を同じパスに限っても無出力） |
| 3 | `testing_framework/` 配下の全 `.rst` に `テストケース` 0件 | **PASS** | — （`grep -rn … --include=*.rst \| wc -l` → `0`）。**但し書きを R4-6 h で `en/`・`ja/biz_samples/` の3件まで広げた**（いずれも射程外） |
| 4 | `glossary.md` の `:403`〜`:449` に差分なし | **PASS** | — （`diff` が空。`identical`）。**ラウンド4 は `glossary.md` を1行も編集していない** |
| 5 | 見出し数が不変・アンダーライン長の不一致0 | **PASS** | — （`unicodedata.east_asian_width` による表示幅計測。notation `{'=':1,'-':3,'~':10,'^':26}`／examples `{'=':1,'-':9,'~':28,'^':56}`。notation は `=`/`-` が `max(50,表示幅)`・`~`/`^` が49固定、examples は全94見出しが `max(50,表示幅)`。**不一致0件**。ラウンド4 の本文編集は見出しを1つも触っていない） |
| 6 | 両ページの並びが STEP 3-1 の順 | **PASS** | — （notation `~` の4〜10番目と examples `-` の3〜9番目が、7見出しとも同一順・同一文言） |
| 7 | 旧見出し0件・新見出しが見出し行として各1件 | **PASS** | — （notation 旧0／総出現4／`:ref:`3→見出し行1、examples 旧0／総出現3／`:ref:`2→見出し行1。ラウンド3 と同値） |
| 8 | `テストケース一覧` 0件・`テストショット一覧（testShots）を記述する` が見出し行として各1件 | **PASS** | — （notation `テストケース一覧`0／総出現3〈`:352`見出し・`:356`・`:586`〉、examples 0／総出現4〈`:395`見出し・`:397`・`:676`・`:1713`〉。**R4-4 は `examples:397` の前半を書き換えたが、末尾の `:ref:` リンクテキストは残したため件数は不変**） |
| 9 | `:ref:` の未定義参照0件・リンクテキストに旧見出しなし | **PASS** | — （`grep -n ':ref:`テストケース一覧\|:ref:`値を特殊記法' … \| wc -l` → `0`。未定義参照はゲート11 のビルド警告で確認） |
| 10 | 段落内改行が0件 | **PASS** | — （`check_para.py` を3ファイルに再実行し `段落内改行 0 件`／exit 0。ラウンド4 の編集はすべて1行等長置換であり行を折り返していない） |
| 11 | Docker フルビルド（`-a`） | **PASS** | **再実行（5回目）**。`build succeeded, 1 warning`／`EXIT=0`。警告は `db_double_submit.rst:108` の既知1件のみで**新規0件**（`grep -c 'WARNING\|ERROR'` → `1`）。イメージは `nablarch-document-build-sandboxed:latest`。ローカル venv は不使用。`locales/ja/LC_MESSAGES/sphinx.mo` は `git checkout` で復元し `git status --porcelain` に現れないことを確認。`_build/` は削除していない |
| 12 | 情報が失われていないこと | **PASS** | — （行数 notation 1520／examples 2101／about 118 が `6ba0d2a` から不変。素の多重集合 -36/+36・-43/+43・-1/+1、正規化後 -10/+10・-8/+8・-1/+1 で**ラウンド3 と同値**）。**残差は1件も増えていない。** ラウンド4 が編集した7行は**すべて既に残差だった行**であり（notation の #1・#2・#7・#8・#10、examples の #3・#4）、その内容が更新されただけである（§7 ゲート12 の2表に反映済み）。**削除された段落・表の行・コードブロックは0件** |
| 13 | STEP 2 の置換判定表・STEP 3-2 の前後関係の確認表が全件記録 | **PASS** | — （§2 の全77件・§3-3 の Z-1〜Z-24 とも件数不変。R4-6 a で §2-1 の4行にポインタを、R4-6 f で Z-12 に「（2箇所）」を足したが、いずれも**行数で数える件数は変えない**） |
| 14 | `about/index.rst` の変更が L24 の1行のみ | **PASS** | — （`git diff -U0 6ba0d2a -- about/index.rst \| grep '^@@'` → `@@ -24 +24 @@` の1つのみ。ラウンド4 は `about/index.rst` を編集していない） |
| 15 | `design.md` に `テストケース` が0件 | **PASS** | — （`grep -c` → `0`。ラウンド4 は `design.md` を編集していない。基準コミット別の書き分けは §7 ゲート15 のとおり） |
| 16 | 特殊記法セクションの導入文と見出しに語の不一致なし | **PASS** | — （examples `:1849`-`:1851`、notation `:1275`-`:1277` を再確認。ラウンド4 は特殊記法セクションを編集していない。方針 (c) は不変） |
| 17 | 値の種類10種の突合表が記録されていること | **PASS** | — （§10 G の突合表、§11-2 R2-C-6 の分類基準、R2-C-7 の除外理由をいずれも維持。例は追加していない。**申し送り欄への転記を R4-6／§13-4 の QA 提案3 対応として `page-testdata_examples.md` 申し送り18 に追加した**） |

**全17件 PASS。ラウンド4 の再実行で値が変わったゲートは無い**（再実行したゲート11 のビルドログを除く）。**§7 の数値はラウンド3 の値のままで正しい**ため、§7 は冒頭の断りとゲート12 の2表（ラウンド4 で内容が更新された残差行）だけを更新した。

### 13-6. ラウンド3の反省

ラウンド3の R3-A-1 は「頭の名詞と例示の向きを揃える」という**それ自体は正しい是正**だった（§12 の含意にもそう書いた）。しかし、その適用が

1. **`testShots` のカラムではない `リクエストID` を例に挙げるという事実誤りを、公開される本文に持ち込んだ**
2. **本タスクが新たに導入した語 `テストショット` の唯一の語釈を本文から消した**

という2つの退行を生んだ。**推敲のための書き換えであっても、定義文を書き換えるときは、挙げるカラム・語がページ内に実在するかを `grep` で必ず確認し、新語の語釈が本文のどこかに残ることを確認する。** この教訓は残り32ページへの申し送りとして `reviews/page-testdata_notation.md` の**申し送り24** に1項として追加した。

あわせて R3-A-3（`利用例`→`用途`）も、寄せ先の語の**先例数**だけを確認して**同一ページ内で定義語として使われていないか**を確認しなかったため、`:126` の定義と衝突した。この教訓は `reviews/page-testdata_examples.md` のラウンド4 の節の「`#11` 以降への含意」に記した。

---

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | ラウンド3後の再レビューで **PASS**。ゲート1〜17 を独立に組み直して全件再現（`csv.DictReader` で594行、`glossary.md` の `:403`〜`:449` は `IDENTICAL`、Docker フルビルドを自身で実行して `build succeeded, 1 warning`）。「全件の表で記録されている」2項目は母集合から数え直し、STEP 2 は**行番号と1行内の出現多重度まで**基準コミットと一致、STEP 3-2 は notation 18行・examples 4行と表が一致。ゲート3 の 0件 は分かち書き・全角半角・`:ref:` 内・コードブロック内・表セル内・生成HTML のいずれでも再現。**重大な指摘は0件。** 軽微5件のうち、記録の不正確さ（§2 判定表の `置換後` 列4行）は R4-6 a で、`en/` 配下の残存は R4-6 h で是正。`steering.md` のチェックリストが古い件はコーディネーターが check-off 時に是正 |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | セクション順は STEP 3-1 の表と完全一致し、両ページ9対が同一順・同一文言。方向語（notation 18行・examples 4行）と `:ref:` の向きを全数抽出してラベル実位置と突合し、**崩れているものは0件**。2ページ間の対称性（見出し文言・導入文・双方向の相互参照）も全9対で成立 |
| System-wide integrity (interfaces, cross-doc consistency) | NG → 是正済み | ラウンド2後の再レビューで **FAIL**。`glossary.md:201` の定義（`1組の入力と期待値`）が、ラウンド2が「兄弟行と排他にならない」として本文から外した文言のまま規範側に残っていた。→ R3-B-1 で、用語集の定義は用語そのものの意味、本文の表は3用途を排他に分ける別目的で `実行条件` 側から言い表したものであり両者は同じものを指す旨と、残り32ページでの使い分けを注記。ラウンド3後の再レビューでは本項の指摘は解消され、**残った FAIL は下記 Craft 側と同一の `:108` の退行**（R4-1 で是正） |

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG → 是正済み | ラウンド3後の再レビューで **FAIL**（重大2件）。いずれも**ラウンド3の是正自体が持ち込んだ退行**。(1) `:1220` が「手動で**テストを**行う」から目的語を落とし、直前の「長さを揃える」に係って**意味が反転**した。(2) `:108` が `1回の実行` の語釈（両ページで唯一）を落とし、`testShots` に存在しないカラム `リクエストID` を例示に挙げた。→ R4-2・R4-1 で是正。コーディネーターも `:389`〜`:394` と `:408` を実読して両件を独立に確認 |
| Consistency with existing style | NG → 是正済み | ラウンド2後の再レビューで、本タスクが `利用例`（`ja/` 配下に先例0件、`使用例` は27件）という新しい揺れを持ち込んでいることを検出。→ R3-A-3 で `用途` に寄せたが、これが `:126`（「データタイプは、データブロックの**用途**を示す予約語」）と正面衝突する新たな揺れを生んだ（設計レビュアーが検出）。→ R4-4 で `用途`・`利用例` のどちらも使わず、ページが確立している語彙（`データタイプ`・`データブロック`）で言い換えて解消。`利用例` は `ja/` 配下0件、`:104`・`:126`・`:147` の列見出しと定義文は不変。段落内改行0件・アンダーライン長の不足0件は各ラウンドで再現 |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (claims verified) | OK | ラウンド3後の再レビューで **PASS**。記録の数値を全件検算し一致（置換77件は行番号と行内出現数まで、方向語22行、594行/12,986/11,983、ゲート12 の残差18件を全出力して §7 の残差表と1対1、空文字14行、`196` が再現不能であることの実測 215/234）。`file:line` 引用はラウンド3のずれを含めて全件実在を確認。未照合の主張はいずれも `glossary.md` に「本リポジトリでは未照合」と明記済み。Docker フルビルドを自身で実行して `build succeeded, 1 warning`・新規0件 |
| Coverage (claims) | NG → 是正済み | ラウンド2後の再レビューで **FAIL**。Completion criteria が名指しする「STEP 3-2 の前後関係確認の全件表」に、**本タスク自身が追加した2参照**（`examples:397`・`:676`）が欠落し、件数が記録内で3通り（17件／21件／Z-22）に割れていた（同じ表の3回目の漏れ）。→ R3-D-1 で Z-23・Z-24 を §3-3 本体に追加し、件数を全箇所で統一。ラウンド3後の再レビューでは母集合から数え直して一致を確認し、本項の指摘は解消。残った軽微4件（記録の記述精度）は R4-6 b〜f で是正 |

## Overall Verdict

- Self-check: OK（差し戻し対応ラウンド4 まで反映。ラウンド4は**ラウンド3の是正が本文に持ち込んだ退行の是正だけ**を行った。本文編集4件〈§13-1。`:108` の定義セルの事実誤り〈存在しないカラム `リクエストID`〉と語釈の消失、`:1220` の意味の反転、`:40` の落ちた限定、`用途` と `:126` の定義の衝突〉、通し読み〈§13-2〉、記録の是正8件と指示字句からの逸脱の記録〈§13-3 の R4-6 a〜h・R4-7〉を実施し、対応しなかった指摘7件を却下／申し送り／user review 委譲の別と理由つきで §13-4 に全件記録した。申し送りは `page-testdata_notation.md` に24〜27、`page-testdata_examples.md` に18 を追加。ゲート1〜17 を全件再実行して全件 PASS〈§13-5〉、値が変わったゲートは無い。Docker フルビルド〈5回目〉は `build succeeded, 1 warning`／exit 0 で新規警告0件、`sphinx.mo` は `git checkout` で復元済み。以下はラウンド3 時点の記載。ラウンド3では本文編集8件〈§12-1〉・`glossary.md` 3件と `reviews/` 1件〈§12-2〉・記録の是正10件〈§12-3〉を実施し、対応しなかった指摘7件を却下／申し送りの別と理由つきで §12-4 に全件記録した。ゲート1〜17 を全件再実行して全件 PASS〈§12-5〉。Docker フルビルド〈4回目〉は `build succeeded, 1 warning`／exit 0 で新規警告0件。以下はラウンド2 時点の記載。ラウンド1の指摘12件＋追加回答2件＋調査1件に加え、ラウンド2の本文編集7件〈§11-1〉と記録の訂正・追加8件〈§11-2 の R2-C-1・R2-C-3〜R2-C-8。R2-C-5 は追記済みのため追加なし〉を実施し、ゲート1〜17 を全件再実行して全件 PASS〈§11-3〉。Docker フルビルドは `build succeeded, 1 warning`／exit 0 で新規警告0件）
- QA: OK（ラウンド3後の再レビューで PASS。重大0件）
- Design expert: OK（ラウンド2後 FAIL → R3-B-1 で是正、ラウンド3後 FAIL → R4-1 で是正。いずれもコーディネーターが実測で確認済み）
- Craft expert: OK（ラウンド3後 FAIL → R4-1・R4-2・R4-4 で是正。コーディネーターが `:389`〜`:394`・`:408`・`:126` を実読して確認済み）
- Verification expert: OK（ラウンド2後 FAIL → R3-D-1 で是正、ラウンド3後 PASS）
- Ready to check off: **user review 待ち**。4観点の指摘はすべて是正または理由つきで記録済みで、コーディネーターの独立検証（ゲート2/3/14、Docker フルビルド `build succeeded, 1 warning`／exit 0、`:108` の挙示カラムの実在、`利用例` 0件）も通っている。ただし次の3点を user review で判断されたい。(1) **レビューのラウンド上限3を超えて4ラウンド実施した**（ラウンド3の是正が公開本文に事実誤り〈存在しないカラム `リクエストID`〉を持ち込んだため、上限で打ち切らず是正を優先した）。(2) `examples:401` の数え方（`正常ケースと認証エラーケース`）について QA とクラフトで判断が割れ、両論を §13-4 に併記して委譲した。(3) 記載例ページの特殊記法セクションに参照文を足すかどうかについて、設計レビュアーが回答 §2 (c) の読み方に反論しており、これも §13-4 に併記して委譲した
