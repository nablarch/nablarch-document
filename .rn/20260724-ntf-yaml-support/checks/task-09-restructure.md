# task-09-restructure Completion Check

本ファイルはメタデータ側（STEP A-3 / A-4 / F-1 / F-2 / F-3、およびゲート1〜4）の記録である。
RST本文側（STEP B〜E、ゲート5〜13）は別担当の記録による。

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| ゲート1: `python3 mapping/tools/verify_mapping.py` が `exit 0`、594行 / `lines`合計 12,986 / DROP除く 11,983 が不変 | OK | `cd .rn/20260724-ntf-yaml-support && python3 mapping/tools/verify_mapping.py` → `exit=0`、`Loaded 594 rows from mapping.csv` / `lines total (all rows): 12986` / `lines total (excluding DROP): 11983` / `OK: no errors`。`csv.DictReader`による独立カウントでも `DictReader rows: 594` / `lines total: 12986` / `lines excl DROP: 11983`（`wc -l`は不使用）。advisory件数も作業前後で不変（duplicate destinations 44件・reference-only 2件・intro split 5件・part2 optional 18件） | | |
| ゲート2: `mapping.csv` の `dest_page=テストデータの書き方` が140行で不変。内訳 `機能概要` 1 + `テストデータの構造` N + `使用方法` (139-N) = 140 | OK | `csv.DictReader`集計 → `page rows: 140 {'使用方法': 120, 'テストデータの構造': 19, '機能概要': 1}`、`1 + 19 + 120 = 140`。N=19。`テストデータの構造`を使う行が他ページに0件であることも確認（`other pages using テストデータの構造: []`） | | |
| ゲート3: `mapping.csv` が `_batch/*.csv` の単純連結とバイト一致 | OK | `{ head -1 _batch/batch-01.csv; for f in _batch/batch-*.csv; do tail -n +2 "$f"; done; } > /tmp/g3.csv; md5sum mapping/mapping.csv /tmp/g3.csv` → 両者とも `f0335d837f353c96ebb85fc61f0a016b` | | |
| ゲート4: `vocabulary.md` に `テストデータの構造` があり、適用対象が `dest_page=テストデータの書き方` に限定されている | OK | `mapping/vocabulary.md` `## dest_section` 配下に `### ページ限定の追加値（1件）` を新設。表 `\| 第3部 テストの実装方法 \| テストデータの構造 \| テストデータの書き方（このページのみ） \|` と、直後に「**`テストデータの構造`は`dest_page=テストデータの書き方`の行にのみ使う。**」を明記。`verify_mapping.py` の `check_vocabulary`（19行すべて語彙照合を通過）・`check_unused_vocabulary`（エラー0件）ともに通過 | | |
| 変更したのは `dest_section` 列のみ（行数・`lines`・`disposition`・`heading_path` 等は不変） | OK | `HEAD:mapping.csv` と現行を `csv.DictReader` で行ごと突合 → `rows differing: 19 — all dest_section 使用方法→テストデータの構造 only`（差分キーが `['dest_section']` 以外の行は0件） | | |
| 他の33ページの `dest_section` を変更していない | OK | 上記突合で差分19行はすべて `dest_page=テストデータの書き方`。`git diff -U0 mapping/mapping.csv` の追加行19本の `mapping_id` も同一集合 | | |
| `style.md` の観点が11個（増やしていない） | OK | `grep -c "^### S-" mapping/style.md` → `11`（S-01〜S-11）。作業前は10個 | | |
| ゲート5: `testdata_notation.rst` の `-` 見出しが3件（`機能概要`・`テストデータの構造`・`使用方法`） | OK | 見出し判定スクリプト（下線記号と直前行から `=`→L1/`-`→L2/`~`→L3/`^`→L4 を復元）→ `3 ['機能概要', 'テストデータの構造', '使用方法']`。下線長も `awk` で全件測定し `-` の3本はいずれも50文字（L1の `=` と同じ既存幅） | | |
| ゲート6: `~` 見出しが10件。`テストデータの構造` 配下3件 / `使用方法` 配下7件 | OK | 同スクリプト → L3総数 `10`。`テストデータの構造` 3件 = `テストクラスとテストデータの対応`・`データブロックとデータタイプ`・`グループIDによる使い分け`。`使用方法` 7件 = `テーブルのデータを記述する`・`LIST_MAPのデータを記述する`・`テストケース一覧（testShots）を記述する`・`ファイルのデータを記述する`・`メッセージングのデータを記述する`・`値を特殊記法で記述する`・`コメント・マーカーカラム・空エントリを扱う`（並び順は作業前と同一） | | |
| ゲート7: 形式別L4対を持つL3では、それがL3の最後の2つのL4であること（`testShots` を除く） | OK | L3ごとにL4の並びを列挙して末尾2つを判定 → 8つのL3すべて `OK`。`テーブルのデータを記述する` は `['準備データ（SETUP_TABLE）を記述する', '共通の準備データをまとめる', '期待値（EXPECTED_TABLE等）を記述する', 'カラムを省略する', 'Excel形式の場合', 'YAML形式の場合']`。`testShots` は形式別対なし（処理方式別L4が6つ、構成変更なし）、`コメント・マーカーカラム・空エントリを扱う` はL4なし | | |
| ゲート8: `テストクラスとテストデータの対応` 内で `important`（実行順序）と `tip`（マスタデータ）が最初のL4より前にある | OK | 見出し `テストクラスとテストデータの対応` から最初の `Excel形式の場合` までの範囲を切り出して判定 → `important: True / tip: True`。実位置は L29〜L39（`Excel形式の場合` は L41） | | |
| ゲート9: カラム名の行を「1行目」と呼ぶ箇所が0件 | OK | `grep -n "1行目"` の全3件を実読。L423「1行目に「SETUP_TABLE=テーブル名」、2行目にカラム名、3行目以降にデータを記載する。」（データタイプ行=1行目に統一済み）、L544「1行目に ``LIST_MAP=``…2行目を Map のキー、3行目以降を Map の値」（同じ数え方）、L1097「フレームワーク制御ヘッダ以降のメッセージボディは、1行目がフィールド名称…」（カラム名ではなくフィールド名称行であり、かつ「フレームワーク制御ヘッダ以降」を起点とする相対の数え方。テーブルデータの数え方とは別体系のため対象外と判断）。カラム名の行を1行目と呼ぶ箇所は0件。**【ラウンド6で更新】この記録はラウンド4時点（3件）の値である。HEAD での実測は下記「ゲート5〜13 の HEAD 再実行結果」を参照（該当2件、いずれも `Excel形式の場合` の中）** | | |
| ゲート10: `:ref:` の未定義参照0件 / `.. _` ラベル直後が見出しでないもの0件 | OK | ページ内ラベル参照は `testdata_notation-group_id`・`-list_map`・`-test_shots` の3種で、いずれも `.. _` 定義あり（未定義0件）。ラベル13本すべて「ラベル → 空行 → 見出し行 → 下線」の並びであることを機械判定（違反0件）。ページ外参照13種（`testdata_examples*` 10種・`master_data_tool`・`master_data_restore`・`gsp-maven-plugin`）はゲート12のフルビルドで `undefined label` 警告が出ていないことをもって解決を確認 | | |
| ゲート11: 段落内改行が0件 | OK | 「空行を挟まず日本語を含む行が連続し、かつ後続行が見出し下線でもディレクティブ・リスト・表でもない」箇所を機械判定 → `段落内改行: 0` | | |
| ゲート12: Docker でフルビルド（`-a`）し `build succeeded` かつ警告が既知の `db_double_submit.rst` 1件のみ | OK | 末尾 `build succeeded, 1 warning.`。警告の全文は `/root/document/ja/application_framework/application_framework/libraries/db_double_submit.rst:108: WARNING: undefined label: how_to_set_token_in_request_unit_test (if the link has no caption the label must precede a section header)` の1件のみ（`grep -i "warning\|error"` で全出力を走査し、他に `WARNING`/`ERROR` 行なし）。**`docker build` は本セッションのサンドボックスで `pip install` がネットワーク不通により失敗したため、既存イメージ `nablarch-document-build-sandboxed:latest`（過去セッションで同じ `Dockerfile` から構築済み）を使用した。** 実行コマンド: `docker run --rm -v /home/tie303177/work/lovaizu/nablarch-document:/root/document nablarch-document-build-sandboxed /bin/bash -c "cd /root/document; sphinx-build -a -d _build/.doctrees/ja -b html ja _build/html"`。`_build/` は削除していない | | |
| ゲート13: `d545d95` からの情報欠落なし（消えた行はSTEP C〜Eに由来するもののみ） | OK | `git show d545d95:…/testdata_notation.rst` と現状の非空行を `collections.Counter` で多重集合比較（移動を差分として数えない方式）→ 旧のみ17行 / 新のみ22行。17行の内訳と由来は後述の「ゲート13: 消えた17行の由来」のとおりで、すべてSTEP A（改題・L2新設）・STEP B（導入文の充実）・STEP C（形式非依存内容の移動に伴う指示語修正）・STEP D（行番号の統一）・STEP E（`setUpDb` のYAML側追記）に由来する。段落・表の行・コードブロックの実体が失われた行は0件。**【ラウンド6で更新】この記録はラウンド4時点（`51f8020`）の値である。`ea4b5d3` / `d8089aa` を経た HEAD での実測は 旧のみ43行 / 新のみ60行。内訳と由来は下記「ゲート5〜13 の HEAD 再実行結果」を参照** | | |

## メタデータ側の実施記録

### 前提: `_batch/*.csv` が `mapping.csv` と乖離していた（本作業で解消）

作業開始時点で `_batch/*.csv` の単純連結は `mapping.csv` とバイト一致していなかった。原因は2つ。

1. **内容の乖離（8行）**: `#8` フィードバック対応の3コミット（`82ca8ce` / `ef9d245` / `a6baf64`）が
   `mapping.csv` を直接編集し `_batch/` を更新していなかった。乖離行は
   `current-0163` / `current-0175` / `current-0176` / `current-0180` / `input-0002` / `input-0114` /
   `input-0116` / `current-0267`。**`mapping.csv` 側を正として `_batch/` に反映**した
   （`mapping.csv` の内容は変えていない）。
2. **改行コードの不統一**: `mapping.csv` は全行CRLFだが、`_batch/` の9ファイル
   （batch-02・03・05・06・07・08・09・13・14）がLFだった。`mapping.csv` に合わせて
   `_batch/` 側をCRLFへ統一した。

この2点の解消により、以後は「`_batch/` を編集 → 単純連結で `mapping.csv` を再生成」が
バイト一致で成立する（ゲート3）。`git diff` の突合では、`_batch/` の差分行は24行
（上記の同期8行 ＋ 本作業の再割当19行 − 重複3行）であり、それ以外の意味的差分は0件。

### STEP A-3: 再割当した `mapping_id`（N = 19）

判断手順は、(1) `testdata_notation.rst` の現状 L23〜336（新設L2配下となる3セクション）を通読し、
(2) 各行の `src_file` の `src_body_start`〜`src_body_end` を実際に読み、
(3) その内容がどのセクションに反映されているかで判定した（`heading_path` の文字列では判定していない）。

新見出しの対応: `テストデータを配置する` → **S1 `テストクラスとテストデータの対応`**（RST L23-91）、
`データブロック（テストケース、準備データ、期待値）を記述する` → **S2 `データブロックとデータタイプ`**（RST L93-241）、
`グループIDでデータブロックを分ける` → **S3 `グループIDによる使い分け`**（RST L243-333）。

| # | mapping_id | 出典の実体（実読） | 反映先 | 判断根拠 |
|---|---|---|---|---|
| 1 | `current-0167` | `01_Abstract.rst:198-202` 「Javaソースコードよりスプレッドシートのほうが可読性・編集のしやすさで有利」 | S1 | RST L25 の導入文そのもの |
| 2 | `current-0168` | `01_Abstract.rst:205-255` Excelファイル名・配置パスの命名規約、ファイル種類/ディレクトリ/ファイル名の表、シート名＝テストメソッド名、命名規約はtipの推奨 | S1 | RST L31-60（S1 `Excel形式の場合`）に表・tipごと反映 |
| 3 | `current-0169` | `01_Abstract.rst:258-325` データタイプの概念、13種のデータタイプ一覧表、`データタイプ=値` の共通書式 | S2 | RST L137-188 の14種一覧表と L189 の識別規則。冒頭の「1行目/2行目」の行構造は `テーブルのデータを記述する` 側にも一部かかるが、**主たる反映先はデータタイプ一覧そのものであるS2** |
| 4 | `current-0172` | `01_Abstract.rst:393-402` セルの書式は文字列のみ、罫線・色付けは任意、important（文字列以外の書式では読み取れない） | S1 | RST L62-66（S1 `Excel形式の場合` 末尾の地の文＋important） |
| 5 | `current-0175` | `01_Abstract.rst:587-612` 実行順序に依存しないテスト、事前条件は自クラスで準備、マスタデータの共通化とtip（マスタデータ投入ツール・復旧機能） | S1 | RST L83-91（S1 `YAML形式の場合` 末尾のimportant＋tip）。※STEP C でこの2ブロックはS1の最初のL4より前へ移動する予定だが、いずれにせよS1内である |
| 6 | `current-0177` | `01_Abstract.rst:627-664` データタイプを混在させるとデータの読み込みが途中で終了する、NG/OKのコードブロック | S3 | RST L273-295（S3 `Excel形式の場合` のimportant前半、NG/OK例そのもの） |
| 7 | `current-0221` | `03_Tips.rst:205-228` グループIDを付与して複数テストケースのデータを1シートに混在させる、書式 `データタイプ[グループID]=テーブル名` | S3 | RST L247（S3導入文）と L269（`SETUP_TABLE[case_001]=EMPLOYEE_TABLE`） |
| 8 | `current-0223` | `03_Tips.rst:245-292` `case_001` / `case_002` の `SETUP_TABLE` / `EXPECTED_TABLE` 記述例 | S3 | RST L269 の例および L297-316 のグループID交互記述NG/OK例（`EMPLOYEE` / `case_001` / `case_002`）の素材。`使用方法` 配下7セクションには反映箇所がない |
| 9 | `current-0224` | `03_Tips.rst:295-300` 複数グループIDのデータはグループIDごとにまとめて記述すること | S3 | RST L297-316（S3 `Excel形式の場合` のimportant後半） |
| 10 | `input-0002` | `ntf-doc-terms.md:13-35` データタイプを起点とする用語ドメイン関係図（グループID・testShots・ファイル・メッセージング・解釈規則） | S2 | RST L127-135 の関係図と L125 の地の文 |
| 11 | `input-0011` | `ntf-doc-terms.md:119-128` グループIDの定義、書式 `データタイプ[グループID]=値`、`default` | S3 | RST L247-249（デフォルトグループ・`default`）と L269（書式） |
| 12 | `input-0116` | `ntf-testdata-doc.md:23-48` テストケース/セットアップ/検証の3用途表、格納階層図、データブロック＝種別＋識別子 | S2 | RST L97-124（3用途表・格納階層図）と L189 |
| 13 | `input-0117` | `ntf-testdata-doc.md:50-81` テストデータはテストクラスと1対1、Excel/YAMLの読み込み単位対応表、ディレクトリ構成、YAML 1.2準拠・JSON Schema | S1 | RST L27-79（S1本文・両`^`見出し）に丸ごと反映 |
| 14 | `input-0118` | `ntf-testdata-doc.md:83-91` ファイルなし時・空ファイル時の扱い、値の書き方（Excelは文字列書式／YAMLはダブルクォート） | S1 | 「セルは必ず文字列書式」がRST L62（S1）に反映。加えて `input-0117`（`(L2直下)` 導入文）と同じ親 `2. テストデータの基本構造` の唯一の兄弟であり、`verify_mapping.py` の `check_intro_section_split` が `(L2直下)` の`dest_section`不一致をERRORとするため、両者は同じ`dest_section`でなければならない（分離するとゲート1が落ちる） |
| 15 | `input-0119` | `ntf-testdata-doc.md:95-129` 識別の構成要素（種別＋識別子の値）、Excel前方一致/YAML完全一致、YAMLトップレベルキー対応表、重複ルール | S2 | RST L189-241（S2の`Excel形式の場合`・`YAML形式の場合` 全体） |
| 16 | `input-0120` | `ntf-testdata-doc.md:131-150` データブロック種別14種の一覧表 | S2 | RST L137-188（14種の一覧表） |
| 17 | `input-0121` | `ntf-testdata-doc.md:152-162` 全件収集タイプ、YAMLはキーごと独立で交互記述の制約なし、`LIST_MAP`/`MESSAGE` の重複ID先着一致、Excelとの違い | S3 | RST L251-265（収集方法の表）・L273-316（Excelの交互記述制約）・L331（YAMLは順序不問） |
| 18 | `input-0124` | `ntf-testdata-doc.md:187-210` groupIdによるグループ化、Excel `[groupId]`／YAML `group_id:`、デフォルトグループ、バッチの `"default"` | S3 | RST L247-249・L269・L321-331 |
| 19 | `input-0179` | `ntf-testdata-loading.md:237-249` Single/Group の2方式と該当データタイプの表 | S3 | RST L251-265（「収集方法は、データタイプによってあらかじめ決まっている」＋単一/グループの表） |

### STEP A-3: 変更しないと判定した主な行（`使用方法` 側に反映されているもの）

実読のうえで `使用方法` 配下7セクション側と判定し、変更しなかった代表例。

- `current-0170`（コメント）・`current-0171`（マーカーカラム）・`input-0176` → `コメント・マーカーカラム・空エントリを扱う`
- `current-0173`（日付の記述方法）・`current-0174`（セルへの特殊な記述方法）・`input-0171` → `値を特殊記法で記述する`
- `current-0080`・`current-0122`・`current-0192`（`setUpDb` シート）・`input-0012` → `テーブルのデータを記述する`
- `current-0186`〜`current-0190`（カラム省略・デフォルト値）・`current-0197`（データ型制約） → `テーブルのデータを記述する`
- `input-0178`（`getSetupFile` / `getExpectedTableData` のまとめ単位） → `カラムを省略する`（RST L489）と `ファイルのデータを記述する`（RST L977）。**データブロック識別の話ではなく入口APIの返却単位の話であるため構造側にしない**
- `input-0122`（`testShots` の予約ID） → `テストケース一覧（testShots）を記述する`
- `input-0114` は従来どおり `機能概要`（変更なし）

### 内容が構造側と使用方法側にまたがった行の扱い（主たる反映先で判断）

- `current-0169`: 「データ1行目は `データタイプ=値`、2行目以降はデータタイプにより異なる」という
  行構造の記述は `テーブルのデータを記述する`（RST L339/L343）にもかかるが、本行の実体の大半は
  データタイプ一覧表（13種）とデータタイプという概念の定義であり、**主たる反映先はS2**と判断した。
- `input-0118`: 「値の書き方」行のExcel側（文字列書式）はS1（RST L62）、YAML側（ダブルクォート）は
  `値を特殊記法で記述する`（RST L1284）に分かれる。ただし「ファイルなし時」「空ファイル時」を含む
  本行全体は読み込み単位そのものの扱いであり、かつ `input-0117` との `check_intro_section_split`
  制約があるため、**主たる反映先はS1**と判断した。
- `current-0175`: important（実行順序）とtip（マスタデータ）はいずれも形式非依存だが、現状のRSTでは
  S1（`テストデータを配置する`）の末尾に置かれている。STEP C で同一セクション内の前方へ移動する
  指示であり、セクションをまたぐ移動ではないため **S1** で確定。

### STEP A-4: `design.md` §4 の更新

- §4「ページのアウトライン」の末尾（`**セクションタイトルは「〜する」形式とする。**` の直後）に、
  例外規定を1段落（5行相当）追加した。内容は (a) 第3部は原則 `機能概要`＋`使用方法` の2セクション構成、
  (b) 「テストデータの書き方」のみ `機能概要`＋`テストデータの構造`＋`使用方法` の3セクション構成、
  (c) 配下3セクションの名称、(d) 理由（手順ではなく記法の仕様であり、器の説明と記法の説明を分けた
  ほうが読者の探索単位に一致する）、(e) 体言止めとする旨（`style.md` S-03 参照）。経緯は書いていない。
- 整合のための最小修正2箇所:
  - §4「テストデータの2ページ」冒頭の「**「テストデータの書き方」には一般の第3部ページアウトライン
    （機能概要＋使用方法）を適用するが**」→ 上記例外の形で適用する旨に修正（そのままでは新設した
    例外規定と直接矛盾するため）。
  - 同節末の `input-0002` / `input-0116` の割当経緯の記述に、`#9` で `dest_section=テストデータの構造`
    へ再割当した旨を追記。
- `テストデータの記載例` の見出し文言一致の規約（`使用方法` 配下と一致していればよい／`テストデータを
  配置する` に対応する見出しを設けない）は**変更していない**（指示どおり）。

**【ラウンド5・6で訂正 — 作業指示の明示的禁止を上書きした】**

上記の最後の項目は、その後のラウンド5（`65fc852`）で覆した。作業指示 `ntf-doc-09-restructure.md` の STEP A-4（L94）は
「`design.md` §4「テストデータの2ページ」にある `テストデータの記載例` の見出し文言一致の規約は**変更しない**」と
明示的に禁じているが、本作業ではこれを**変更した**。指示の明示的禁止を上書きした判断として記録する。

- **上書きした理由**: 同じ作業指示の STEP A-2 が3つのL3見出しの改題を指示しており、その結果、指示 L94 が前提としていた
  「記載例側の見出しは `使用方法` 配下と一致していればよく、L2 がどちらに属するかは問わない」という状態が崩れた。
  改題により `テストデータの記載例` のL2見出し9件のうち2件（`データブロック（テストケース、準備データ、期待値）を記述する`・
  `グループIDでデータブロックを分ける`）は `使用方法` 配下に対応先を持たなくなり、規約が名指しする
  `テストデータを配置する` という見出し名も消滅した。禁止を守ると `design.md` が実態と矛盾したまま残るため、
  指示内の他STEP（A-2）との整合を優先して上書きした。
- **上書きの範囲**: `design.md` §4「テストデータの2ページ」の当該規約のみ。`testdata_examples.rst` 本体・`mapping.csv`・
  `vocabulary.md` は変更していない（記載例ページ側の変更は `#10` の担当範囲）。
- 対応する指摘は `reviews/page-testdata_notation.md` の R5-5（ラウンド6で理由を追記）。

### STEP F-1: `style.md` S-03 に例外を明記

S-03 の内容条件の箇条書きの直後に、例外2件を追加した。

- **例外1（形式別L4対は重複禁止の対象外）**: S-10 規約3 が定める `Excel形式の場合` /
  `YAML形式の場合` のL4見出し対を「同一ページ内でセクションタイトルを重複させない」の対象外とする。
  根拠として、Nabledge の検索の入口 `knowledge/index.md` に現れるのは level 2 と level 3 だけで
  level 4 は現れないため一意性の問題が生じないことを1文で添えた（指示書「事前に確認した事実(a)」。
  参照時点は `nablarch/nabledge` の `main`、2026-08-06）。
- **例外2（構造セクション配下は体言止め）**: 「〜する」形式の適用対象は「使用方法」「拡張例」配下で
  あり、`テストデータの構造` 配下（`design.md` §4 の例外）は対象外とし体言止めにする旨を明記。
  3つの新見出し名も併記した。

### STEP F-2: `style.md` S-10 規約3 に順序規約を追加

規約3 の「1つのL3セクションにこの見出し対は1組だけ置く（…）」の直後に、
「**形式別のL4対は、そのL3の末尾2つに置く。用途別・規則別のL4がある場合は、それらを先に置く。**」
と、その理由（読者がどのL3でも同じ位置で自分の形式の記法を見つけられるようにするため）を追加した。

### STEP F-3: `style.md` に S-11 を新設

`### S-11 L4見出しを持つL3セクションの導入文` を S-10 の直後・`## 3. 検証していない事項` の前に新設。

- 規約: L4見出しを持つL3セクションは、最初のL4より前に、そのセクションが何を扱うかが単独で読んで
  分かる導入文を置く。配下にどのようなL4があるかも導入文から分かる状態にする。
- 根拠: Nabledge の知識JSONはセクションをフラットに切り、L3の `content` に配下のL4の本文を含まない
  （`nablarch/nabledge` の
  `plugins/nabledge-6/skills/nabledge-6/knowledge/component/adapters/adapters-redisstore-lettuce-adaptor.json`
  の `s2`（level 3）は57文字、`s3`（level 4）は2,802文字）。導入文が薄いとL3だけを切り出して読んだ際に
  情報がほぼ得られない。
- 補足として、ページ選定後のフェーズではページJSON全体が読まれL4も候補になる旨（事前確認(b)）を併記。
- 参照した時点: 2026-08-06、`nablarch/nabledge` の `main`。

Nabledge の構造については、作業指示書「事前に確認した事実」(a)(b)(c) に記載された内容のみを根拠と
した（`nablarch/nabledge` リポジトリ自体は参照していない）。

### 観点数の推移

作業前 10個（S-01〜S-10）→ 作業後 11個（S-11 新設）。冒頭の「観点は次の10個に限定する」と
`## 3. 検証していない事項` の「本書は10観点」も11個へ更新した。11個から増やしていない。

## RST側の実施記録

RST本文側（STEP A-1 / A-2・B-1〜B-4・C・D・E、およびゲート5〜13）の記録である。
STEP A-3 / A-4 / F は別担当が `db95c8b` で完了済みであり、再実施していない。
`mapping/style.md` の S-03 例外2（構造セクション配下は体言止め）・S-10 規約3 の順序規約（形式別L4対はL3の末尾2つ）・
新設 S-11（L4を持つL3には単独で分かる導入文を置く）を、本作業が従う規約として先に通読したうえで着手した。

### STEP A-1 / A-2: L2「テストデータの構造」の新設と3セクションの移設・改題

- `.. _testdata_notation-structure:` を新規に付与し、L2 `テストデータの構造` を `機能概要` と `使用方法` の間に新設した。
  下線は `-` 50文字（既存の `機能概要`・`使用方法` と同幅）。
- 既存ラベル `-usage` / `-file_structure` / `-data_block_types` / `-group_id` はすべて維持した（改題してもラベル名は変えていない）。
  `.. _testdata_notation-usage:` は `使用方法` の見出しごと、`グループIDによる使い分け` の後ろへ移動した。
- 改題（S-03 例外2 に従い体言止め）:
  - `テストデータを配置する` → `テストクラスとテストデータの対応`
  - `データブロック（テストケース、準備データ、期待値）を記述する` → `データブロックとデータタイプ`
  - `グループIDでデータブロックを分ける` → `グループIDによる使い分け`
- 下線長は `unicodedata.east_asian_width`（全角=2 / 半角=1）で本文幅を実測し、既存のL3下線と同じ49文字に揃えた
  （旧 `データブロック（…）を記述する` の下線60文字は、改題に伴い49文字になった）。全下線について
  「下線幅 ≥ 見出し幅」を機械判定し違反0件。
- 改題に伴う参照の是正: ページ内リンク1箇所
  `:ref:`グループIDでデータブロックを分ける <testdata_notation-group_id>`` → `:ref:`グループIDによる使い分け <…>``。
  `testdata_examples.rst` 側を指す4箇所（`<testdata_examples-data_block_types>` 2件・`<testdata_examples-group_id>` 2件）の
  リンク文言は**変更していない**。これらは記載例ページ側の見出し文言（同ページのL2見出しは旧文言のまま）であり、
  `#10` の担当範囲であるため触っていない。
- `使用方法` にはもともと導入文が無く、`機能概要`（L14）もページ構成に言及していないため、
  移設に伴う整合のための修正は不要だった（事実として「配下に何があるか」を述べている箇所が存在しない）。
  新設した `テストデータの構造` にも、既存の `使用方法` と揃えて導入文は置いていない（内容の追加を避けるため）。

### STEP B-1 / B-2: 各L3を「共通 → 形式別」に統一

- `テーブルのデータを記述する` のL4を指示どおり並べ替えた（順序の入れ替えのみ）。
  変更後: `準備データ（SETUP_TABLE）を記述する` → `共通の準備データをまとめる` → `期待値（EXPECTED_TABLE等）を記述する`
  → `カラムを省略する` → `Excel形式の場合` → `YAML形式の場合`。
- 並べ替え後に節をまたぐ指示語を読み直した結果は次のとおり。
  - `カラムを省略する` の「主キーカラムを省略できない点は**前述のとおり**」「省略したカラムは**前述のとおり**比較対象外」
    （ラウンド3の R3-1 で前2節への参照に整理した箇所）は、参照先の `準備データ` と `期待値` が引き続き前方にあるため
    **修正不要**であることを実読で確認した（旧順でも4・5番目、新順でも1・3番目で、いずれも `カラムを省略する` より前）。
  - `期待値` 節の「このため主キーカラムだけは期待側でも省略できない」も、前方の `準備データ` 節を受ける形のまま成立する。
  - `共通の準備データをまとめる` は `準備データ` の直後に移した（個別の規則の直後に、それをテストクラス全体でまとめる話が続く形）。
- 他の7つのL3では、形式別L4対はすでに見出しとしては末尾2つだったため見出しの並べ替えは行っていない
  （STEP C で本文を移動した結果、はじめて「末尾2つ」が実質的にも成立した。次項参照）。

### STEP C: 形式非依存の内容を形式別L4の外へ出す（`must`）

指示書が名指しした1件（`テストデータを配置する` の `YAML形式の場合` 末尾）に加え、
**STEP B-3 後半の指示に従い全10のL3を実読したところ、同型の問題が他に5件あった。**
いずれも「`YAML形式の場合` のL4見出しの後ろに、形式に依らない本文がそのまま続いている」ケースであり、
RSTの節構造上その本文は `YAML形式の場合` の一部である（Excel利用者は到達しない）。S-10 規約1 違反として同じ扱いで前へ出した。

| # | L3 | 移した内容（作業前の行範囲） | 移動先 |
|---|---|---|---|
| C-1 | `テストクラスとテストデータの対応` | 接続文＋`important`（実行順序）＋`tip`（マスタデータ）L81-91 | 最初のL4（`Excel形式の場合`）より前 |
| C-2 | `テーブルのデータを記述する` | データ型の制約（`SqlPStatement` 対応型のみ）L371 | L3導入文の直後 |
| C-3 | `LIST_MAPのデータを記述する` | `getListMap` の取得方法・表とListの等価例・ID解決規則 L523-555 | 最初のL4より前 |
| C-4 | `ファイルのデータを記述する` | 空ファイル・固定長/可変長の記法制約・エラーケース・パディング/バイナリ・収集単位・ディレクティブ（固定長11個/可変長9個）・データ型一覧 L946-1079 | 最初のL4より前 |
| C-5 | `メッセージングのデータを記述する` | 同期応答メッセージ送信の4データタイプ・`errorMode:` による障害系テスト・応答不要メッセージ受信・取引単体テストのモックアップ L1136-1223 | 最初のL4より前 |
| C-6 | `値を特殊記法で記述する` | インタープリタ一覧・`${systemTime}` 等の解説・文字種14種・日付形式・空行の `""` 記法・`${attach:}` L1332-1395 | 最初のL4より前 |

C-2 の判断根拠: 出典（`current-0197`、削除前の `06_TestFWGuide/02_DbAccessTest.rst:551-554`）は
「Excelファイルには…」というExcel前提の文面だが、**実装を確認したところ両形式で共通の制約である**。
`nablarch-testing-yaml` の `YamlTestDataParser#getSetupTableData` は Excel 側と同じ
`nablarch.test.core.db.TableData` を返し（`YamlTableDataBuilder.java:5,74,95,119`）、INSERT を行う
`TableData#insertData`（`TableData.java:137,141`）が `SqlPStatement` を使う。したがって
「`SqlPStatement` が対応している型のみ」という制約はYAMLにも同じくかかる。この確認に基づき、
文頭を「Excel\ ファイルに記述できる」→「テストデータに記述できる」に改め、L3の共通部へ移した。

移動に伴う指示語の是正（実読して直したもの）:

- C-1: 「**ここまでのファイル構成を踏まえたうえで**、テストデータを記述する際は次の点に留意する。」
  → 位置が `Excel形式の場合` より前になり「ここまで」が成立しないため
  「テストデータを記述する際は、**形式によらず**次の点に留意する。」に変更。`important`・`tip` の本文は無変更。
- C-6: 「**これらの記法**を実現するインタープリタは、以下のとおりである。」
  → 「これらの記法」が指していた記法表（`Excel形式の場合`／`YAML形式の場合`）が後ろに回るため
  「**特殊記法**を実現するインタープリタは、以下のとおりである。」に変更。
- C-6: 記法表の直前にあった「主な特殊記法は、以下のとおりである。」は、共通部の末尾ではなく
  形式別L4の直前に残す必要があるため位置を保ち、前に共通部が挟まったことに合わせて
  「主な特殊記法**の書き方は、形式ごとに**以下のとおりである。」とした。
- C-6の副次的是正: 移動対象の中にあった「セル内の改行（Alt+Enter）は LF として扱われる…**Excel 自体の仕様**であり…」は
  Excel専用の記述であるため、共通部には置かず `Excel形式の場合` のL4の中（記法表の直後）へ入れた。
- C-5の副次的是正: `YAML形式の場合` の末尾にあった `important`（旧版の `record_type: FW_HEADER` と現行の `fw_header:` の関係）は
  YAML固有の内容のため `YAML形式の場合` に残し、他L4と同じく節末が記載例リンクで終わるよう
  「記載例リンク → important」の順を「important → 記載例リンク」に入れ替えた。

**形式非依存の内容を形式別L4に残した箇所（判断と理由）**:

- `メッセージングのデータを記述する` の「取引単体テストのモックアップクラスでは…シート名 `message` 固定の1つのExcelファイル…」は
  Excel前提の文面だが、共通部（同期応答メッセージ送信の説明の流れ）に置いたままとした。YAML側の対応は出典にも実装確認にも
  根拠がなく、書き換えると推測になるため、文面を変えずに位置だけを共通部へ移した（C-5に含まれる）。

  **【ラウンド6で訂正 — 上記の記録は現状と一致しない】** その後の `d8089aa` で、この段落は**分割された**。
  実物（`testdata_notation.rst` L1202 / L1220）を確認した結果は次のとおりである。

  - **共通部（L3 `メッセージングのデータを記述する` の共通部、L1202）**: 「取引単体テストのモックアップクラスでは、
    応答電文・要求電文のフォーマットとデータを、``message``\ という固定名の**読み込み単位**にまとめて定義する。
    識別子の書式がグループIDを持たない点が同期応答メッセージ送信のテストケースと異なり、データは応答電文の場合のみ記載する
    （要求電文はログ出力用のフォーマットのみを定義する）。障害系のテストも同様に ``errorMode:``\ の記法で行える。」
    — 形式非依存の3点（識別子の書式がグループIDを持たない／データは応答電文のみ／``errorMode:``\ で障害系も可）を共通部に残し、
    「シート名」を形式非依存語「読み込み単位」に置き換えた。
  - **`Excel形式の場合`（L1220）**: 「取引単体テストのモックアップクラスの電文は、シート名 ``message``\ 固定の1つの
    Excelファイルにまとめて定義する。」— Excel固有の部分のみを形式別L4へ移した。

  したがって「文面を変えずに位置だけを共通部へ移した」という上記の記録は**現状と正反対**であり、
  実際には段落を分割し、Excel固有部分を `Excel形式の場合` へ、形式非依存の3点を共通部へ振り分けている
  （レビュー記録の R6-3 に対応）。

### STEP B-3 / B-4: 導入文の確認と補充

S-11（L4を持つL3には単独で読んで分かる導入文を置く）に照らして全10のL3の導入文を確認した。
補充は「既存のセクション内の記述の要約」に限り、新しい主題内容は書き加えていない。

| L3 | 対応 |
|---|---|
| `テストクラスとテストデータの対応` | 既存文「テストデータは、テストクラスと1対1で対応する。」に、配置と読み込み単位の対応を形式別に後述する旨を1文追加 |
| `データブロックとデータタイプ` | 既存の識別規則の文に「識別子の書き方は、形式によって異なる。」を追加（配下2つのL4の内容の要約） |
| `グループIDによる使い分け` | 収集方法の表の直後、最初のL4の前に「グループIDの記述方法は、形式によって異なる。」を1行追加 |
| `テーブルのデータを記述する` | 導入文を書き換え（STEP D と同時）。配下に準備データ・期待値・カラム省略・形式別書式があることを明示 |
| `LIST_MAPのデータを記述する` | 既存導入文の末尾に、取得方法とID解決規則→形式別書式という並びを1文追加 |
| `テストケース一覧（testShots）を記述する` | **導入文が1文も無く、見出しの直後がいきなりL4だった。** `testShots` の定義（1エントリ＝1テストケース）と、本体＋4処理方式＋コマンドライン引数というL4構成を要約した導入文を新規に1段落追加 |
| `ファイルのデータを記述する` | 既存導入文に、共通の制約・ディレクティブ・データ型→形式別書式という並びを1文追加 |
| `メッセージングのデータを記述する` | 既存導入文の末尾に、電文データブロックの構成→同期応答メッセージ送信・障害系→形式別書式という並びを1文追加 |
| `値を特殊記法で記述する` | 既存導入文（`Interpreter` チェーンの説明）で足りると判断し追加なし。C-6 の並べ替えにより、導入文の直後にインタープリタ一覧が来る形になった |
| `コメント・マーカーカラム・空エントリを扱う` | L4を持たないためS-11の対象外。追加なし |

### STEP D: 行番号の矛盾の解消（`must`）

作業前は同じ小見出しの中でカラム名の行を1行目とも2行目とも書いていた。指示どおり
**データタイプ行を1行目とする数え方に統一**し、**行の数え方の説明そのものを `Excel形式の場合` の中へ移した。**

- L3導入文（旧L339）: 「いずれも、1行目に ``データタイプ=値``\ 、2行目にカラム名、3行目以降にデータ行を置く共通の書式を持つ。」
  → 「いずれも、データタイプと識別子の値・カラム名・データ行という共通の構成を持つ。」
  （YAMLに「行」という概念がないため、形式非依存の位置から行番号を除いた。続く「``LIST_MAP`` もこの**3行の書式**を共有するが」も
  「この**構成**を共有するが」に合わせた）
- `Excel形式の場合`（旧L343）: 「「SETUP_TABLE=テーブル名」の行に続けて、1行目にカラム名、2行目以降にデータを記載する。」
  → 「1行目に「SETUP_TABLE=テーブル名」、2行目にカラム名、3行目以降にデータを記載する。」
- 同節内の「ヘッダ行（2行目）」「データ行（3行目以降）」は、この数え方と一致するため無変更。
- `LIST_MAPのデータを記述する` の `Excel形式の場合`（「1行目に ``LIST_MAP=``…2行目を Map のキー、3行目以降を Map の値」）も
  同じ数え方であることを確認した（無変更）。
- S-10 規約4（識別子行はコード書式ではなく普通の文字で、必要なら「」で囲む）に従い、`「SETUP_TABLE=テーブル名」` の
  書き方は維持した。

### STEP E: `setUpDb` のYAML側（`note`）— 追記した

**実装で確認できたため追記した。推測ではない。**

- `DbAccessTestSupport#setUpDb(String sheetName)`（`nablarch-testing`、`DbAccessTestSupport.java:165,175,182`）は
  `sheetName` をそのまま `getSetupTableData` に渡す。
- YAML側の `YamlTestDataParser#getSetupTableData(String path, String resourceName, …)`（`YamlTestDataParser.java:108-112`）は
  `YamlLoader.load(path, resourceName)` を呼ぶ。
- `YamlLoader#buildFilePath`（`YamlLoader.java:72-83`、`YAML_EXTENSION = ".yaml"`、`YamlLoader.java:42`）は
  `basePath + "/" + resourceName + ".yaml"` を組み立てる。
- したがって、Excelの `setUpDb` シートに対応するYAMLの読み込み単位は、テストクラスに対応するディレクトリ配下の
  `setUpDb.yaml` というファイルである。

`共通の準備データをまとめる` の該当文を
「（\ Excel\ 形式では ``setUpDb`` という名前のシート、\ YAML\ 形式ではテストクラスに対応するディレクトリ配下の
``setUpDb.yaml`` というファイル）」に改めた。他の記述は変更していない。

参照した実装は `/home/tie303177/work/nablarch/` 配下の `nablarch-testing` / `nablarch-testing-yaml`
（作業指示で参照先として明示されたもの。過去のラウンドでも参照実績あり）。

### ゲート13: 消えた17行の由来

`d545d95` の当該ファイルと現状を、非空行の多重集合として比較した（移動を削除＋追加として数えない方式）。
旧のみに存在する17行はすべて下表のとおり説明でき、段落・表の行・コードブロックの実体が失われた行は0件である。

| # | 消えた行 | 由来 |
|---|---|---|
| 1 | `テストデータを配置する`（見出し） | STEP A-2 改題（→ `テストクラスとテストデータの対応`） |
| 2 | `データブロック（テストケース、準備データ、期待値）を記述する`（見出し） | STEP A-2 改題（→ `データブロックとデータタイプ`） |
| 3 | `グループIDでデータブロックを分ける`（見出し） | STEP A-2 改題（→ `グループIDによる使い分け`） |
| 4 | `~` 60文字の下線1本 | 上記#2の改題に伴い49文字に短縮（`~` 49文字の行が1本増えている） |
| 5 | `テストデータは、テストクラスと1対1で対応する。` | STEP B-4。同文を残したまま末尾に配置の対応を形式別に後述する旨を追加した新しい1行に置き換え |
| 6 | `ここまでのファイル構成を踏まえたうえで、テストデータを記述する際は次の点に留意する。` | STEP C-1。`Excel形式の場合` より前へ移した結果「ここまで」が成立しないため「形式によらず」に変更 |
| 7 | `データブロックは、データタイプと識別子の値（…）の組み合わせで識別する。` | STEP B-4。同文の末尾に「識別子の書き方は、形式によって異なる。」を足した1行に置き換え |
| 8 | `同一シート内に…（詳細は :ref:`グループIDでデータブロックを分ける <testdata_notation-group_id>` を参照）。` | STEP A-2 改題に伴うページ内リンク文言の追随（ラベルは不変） |
| 9 | `データベースのテーブルに対応するテストデータ（テーブルデータ）は…1行目に ``データタイプ=値``…` | STEP D＋B-4。行番号の説明を除き、配下のL4構成を示す1文を足した1行に置き換え |
| 10 | `「SETUP_TABLE=テーブル名」の行に続けて、1行目にカラム名、2行目以降にデータを記載する。` | STEP D。数え方をデータタイプ行=1行目に統一した1行に置き換え |
| 11 | `Excel\ ファイルに記述できるカラムのデータ型には制約がある。…` | STEP C-2。文頭を「テストデータに記述できる」に改めてL3共通部へ移動 |
| 12 | `テストクラス全体で共通する準備データは、``setUpDb``…（\ Excel\ 形式では ``setUpDb`` という名前のシート）…` | STEP E。YAML側（`setUpDb.yaml`）を括弧内に補った1行に置き換え |
| 13 | `キーバリュー形式の汎用データをまとめて記述したい場合は…さまざまな用途で使われる。` | STEP B-4。末尾に配下構成の1文を足した1行に置き換え |
| 14 | `固定長ファイル・可変長ファイルに対応するテストデータ（ファイルデータ）は…以下の順序で記述する。` | STEP B-4。中ほどに配下構成の1文を挿入した1行に置き換え |
| 15 | `メッセージング（MOMによるメッセージング・HTTPメッセージングなど）のテストデータでは…予約語を使う。` | STEP B-4。末尾に配下構成の1文を足した1行に置き換え |
| 16 | `これらの記法を実現するインタープリタは、以下のとおりである。` | STEP C-6。参照方向の反転に伴い「特殊記法を実現する…」に変更 |
| 17 | `主な特殊記法は、以下のとおりである。` | STEP C-6。共通部が前に挟まったため「主な特殊記法の書き方は、形式ごとに以下のとおりである。」に変更 |

新のみに存在する22行の内訳は 13 + 4 + 3 + 2 = 22 であり、次のとおりである。

- 上表 #5〜#17 の置き換え後の行 13本
- 上表 #1〜#4 に対応する改題後の見出し3本と `~` 49文字の下線1本（計4本）
- 新設したL2の3行（`.. _testdata_notation-structure:` / `テストデータの構造` / `-` 50文字の下線）
- STEP B-4 で純粋に新規追加した2行（`グループIDの記述方法は、形式によって異なる。` と
  `テストケース一覧（testShots）を記述する` の導入文1段落）

### 通し読みで見つけた不整合と対応

すべての編集後、ページ全体を上から通しで読み直して確認した。

1. **（対応済み）形式別L4の中に形式非依存の本文が閉じ込められている箇所が、指示書が名指しした1件以外に5件あった。**
   → STEP C-2〜C-6 として是正（上表）。
2. **（対応済み）`テストケース一覧（testShots）を記述する` に導入文が1文も無かった。** → STEP B-4 で追加。
3. **（対応済み）`値を特殊記法で記述する` のExcel専用記述（セル内の改行）が共通部に残る形になった。**
   → `Excel形式の場合` へ入れた。
4. **（対応済み）`メッセージングのデータを記述する` の `YAML形式の場合` が「記載例リンク → important」で終わっていた。**
   → 他L4と同じく記載例リンクで終わる順に入れ替えた。
5. **（記録のみ・未対応）`メッセージングのデータを記述する` のL3導入文にある「フレームワーク制御ヘッダ以降の
   メッセージボディは、1行目がフィールド名称…4行目以降がデータ」は、Excelの行を前提とした数え方である。**
   テーブルデータについてはSTEP Dで同種の是正を行ったが、本作業の指示範囲は
   `テーブルのデータを記述する` に限定されており、かつメッセージボディの数え方は「フレームワーク制御ヘッダ以降」を
   起点とする別体系であるため、本ラウンドでは変更していない。次ラウンド以降の検討対象として記録する。
6. **（記録のみ・未対応）`ファイルのデータを記述する` の共通部にある「0バイトの空ファイル」のコードブロックは、
   `SETUP_VARIABLE=work/members.csv` というExcel/テキスト形式の記法で書かれている。** 作業前は
   `YAML形式の場合` の中にありさらに不整合だったため、共通部へ出したことで悪化はしていない。
   YAML形式での空ファイル表現は出典・実装いずれでも未確認のため、推測を避けて文面は変更していない。

## レビュー指摘への対応（ラウンド5）

ラウンド4の再構成に対する4観点レビュー（A網羅性 / Bトンマナ / C用語 / D整合性）の指摘への対応記録である。
RST本文側の`must`はコミット`ea4b5d3`で、規約・設計文書側（`design.md`・`style.md`）の`must`は後続コミットで対応した。
指摘の全文と却下理由は `reviews/page-testdata_notation.md` の「ラウンド5」を参照。

### 対応した指摘

| 区分 | 指摘 | 対応 | コミット |
|---|---|---|---|
| must（A） | `メッセージングのデータを記述する`のL3導入文の`no`の説明が実装と矛盾（先頭列は読み込み対象外のラベル列であり、電文の突合は記述順で行われる） | 実装確認のうえ是正。`expectedMessages`の適用範囲も同期応答メッセージ受信に限定 | `ea4b5d3` F-1 |
| must（B） | 共通部（形式別L4より前）に形式依存の記述が10箇所残存。STEP Cの引き上げ時に移した文の語彙を形式非依存に直していなかったことが原因 | 10箇所すべてを移動または形式非依存語への言い換えで解消 | `ea4b5d3` F-2 |
| must（B/D） | `Excel形式の場合`の主語欠落1件、`testShots`の記載例リンクが最後のL4に閉じ込められていた件、L3導入文の不備2件（配下L4の列挙漏れ・列挙順が実配置と不一致） | 主語を補ってYAML側と対称化、記載例リンクをL3共通部へ移動、導入文を実配置に一致させた | `ea4b5d3` F-3〜F-5 |
| must（C） | `glossary.md` §8の正表記違反2件（`メッセージ同期送信`・`メッセージング処理`） | `同期応答メッセージ送信`ほか正表記へ置換 | `ea4b5d3` F-6 |
| must（D、G-1） | `design.md` §4の記載例見出し一致規約が再構成に追随していない。`testdata_examples.rst`のL2見出し9件のうち2件は`テストデータの構造`へ移り体言止めへ改題されたため「`使用方法`配下と同名」を満たさず、規約が言及する`テストデータを配置する`という見出し名も存在しない | §4の当該規約を、(1)`使用方法`配下の各L3と同名の見出しを並べる、(2)`テストデータの構造`配下のうち記述例を持つ2節は改題後の名前で見出しを置く、(3)`テストクラスとテストデータの対応`は実例を持たないため見出しを置かない、の3点が読み取れる形に改訂 | 本コミット |
| must（D、G-2） | `style.md` S-02が第3部を無条件に「機能概要 → 使用方法」としており、S-03例外2が前提にしている3セクション構成の例外をS-02本体が知らない片肺状態 | S-02の第3部の項に`design.md` §4と同内容の例外を1項追記 | 本コミット |
| must（B、G-3） | `style.md` S-03例外1のスコープ不足。重複禁止のみを対象外とし、「〜する」形式（動詞終止形）の規約に`Excel形式の場合`・`YAML形式の場合`が違反したままだった（`使用方法`配下に10件） | 例外1に「〜する」形式についても対象外である旨を明記 | 本コミット |
| must（D、G-4） | `style.md` S-03例外1がS-10規約3を「各L3に置くよう定める」と誤要約。実際は「規約1に該当しない記述方法の説明がある場合に立てる」条件付き規定で、実態も10個のL3のうち8個 | 条件付きの規定であることと実態（10個中8個、うち5個が`使用方法`配下）が分かる文言へ修正 | 本コミット |
| note（D、G-5） | S-03例外1の根拠（`index.md`にlevel 4が現れない）が、S-11の述べる「ページ選定後はページJSON全体が読まれL4も候補になる」範囲をカバーしていない | 例外1の根拠に、ページ内の`.. contents::`目次では各L4が親L3の下にネストされるため位置で判別できる旨を追記。`_build/html/development_tools/testing_framework/implementation/testdata_notation.html`の目次で実測（`テストデータの構造`3組・`使用方法`5組の形式別L4がすべて親L3配下にネスト表示されることを確認） | 本コミット |
| note（B/D） | 新設L2`テストデータの構造`に導入文が無い、タクソノミ図に扱いセクションの案内が無い、「以下では」の定型が5箇所連続、収集方法の重複記述 | 導入文の追加・タクソノミ図直後の境界注記の追加・定型の解消・重複の参照化 | `ea4b5d3` F-7〜F-9 |

### 却下した指摘

| 指摘 | 却下理由 |
|---|---|
| `checks/task-09-restructure.md`が未コミットである | 本ファイルはコーディネーターがタスクのチェックオフ時にコミットする運用であり、実装担当の作業ツリーに未追跡で残っているのは正常な状態 |
| 空シート・0バイトファイルの扱いがRSTに無い（`input-0118`の網羅漏れ） | 前ラウンドで「低価値な自明情報」として削除する判断が済んでおり記録済み（`steering.md` `#9`の2026-08-06の記録(2)(3)） |
| `テストデータの構造`セクションに運用寄りの`important`/`tip`がある | 作業指示 STEP C が「最初のL4より前へ移すこと」と明示的に指示した配置であり、形式別L4に閉じ込められてExcel利用者が到達できない状態を解消するための意図的な配置 |
| `glossary.md`に`テストデータの構造`を追加すべき | 本作業指示の禁止事項が`glossary.md`の変更を禁じている。`#10`への申し送りとする |

### RST修正後のゲート再実行結果

`ea4b5d3` によるRST修正後に、機械判定可能なゲートを現行ファイルに対して再実行した。

| ゲート | 結果 | 実測 |
|---|---|---|
| ゲート5: `-` 見出しが3件 | OK | `['機能概要', 'テストデータの構造', '使用方法']` |
| ゲート6: `~` 見出しが10件（構造3 / 使用方法7） | OK | L3総数10、`テストデータの構造`配下3・`使用方法`配下7。並び順は再構成時と同一 |
| ゲート7: 形式別L4対がL3の末尾2つ | OK | 形式別対を持つ8つのL3すべてで末尾2つが `['Excel形式の場合', 'YAML形式の場合']`。`testShots`（処理方式別L4が6つ）・`コメント・マーカーカラム・空エントリを扱う`（L4なし）は対象外 |
| ゲート8: `important`（実行順序）と`tip`（マスタデータ）が最初のL4より前 | OK | `テストクラスとテストデータの対応`のL24〜L42（最初のL4 `Excel形式の場合` はL42）に両方が存在 |
| ゲート9: カラム名の行を「1行目」と呼ぶ箇所が0件 | OK | `grep "1行目"` の該当は2件のみ。L426（`Excel形式の場合`内、「1行目に「SETUP_TABLE=テーブル名」、2行目にカラム名、3行目以降にデータ」）・L547（`LIST_MAP`の`Excel形式の場合`内、同じ数え方）。いずれもデータタイプ行を1行目とする統一後の記述で、カラム名の行を1行目と呼ぶ箇所は0件。ラウンド4で記録のみとしていたメッセージボディの数え方（旧L1097）は`ea4b5d3` F-1 の書き換えにより消滅した |
| ゲート10: `:ref:` の未定義参照0件 | OK | `testdata_notation.rst`から`testdata_examples.rst`を指す参照は10種で、すべて`testdata_examples.rst`側に`.. _`定義がある（未解決0件）。`#10`で見出しを改題してもラベルは変わらないため参照は壊れない |

### 今後のページ作成タスクへの申し送り（ゲート設計の弱点）

本ラウンドのレビューは、`must`のうち複数を「ゲートを通過したまま素通りしていた欠陥」として検出した。
ゲートそのものに次の弱点があるため、以降のページ作成タスクではゲート文言を見直す。

1. **ゲート7（形式別L4対がL3の末尾2つ）は見出しの並びしか見ていない。** 形式別L4の配下に形式非依存の本文が
   ぶら下がる欠陥（ラウンド1〜3で通り続け、ラウンド4のR4-2で5件が新規検出された）を検出できない。
   見出しの並びに加えて「形式別L4配下の本文が当該形式固有の内容だけであること」を判定する検査が要る。
2. **ゲート9（カラム名の行を「1行目」と呼ぶ箇所が0件）はSTEP Dの症状しか見ていない。** STEP Dが述べた原則は
   「行・セルというExcel前提の語を形式非依存の位置に置かない」であり、この原則で判定すれば別の違反を捕捉できる。
   「L3共通部（L3見出しから最初のL4まで）に`行目`／`セル`／`シート`が出現しないこと」を判定すべきである。
   なお本ページで同判定を試行すると、L4を持つL3の共通部で6箇所が該当する（`データブロックとデータタイプ`3・
   `LIST_MAPのデータを記述する`1・`ファイルのデータを記述する`1・`値を特殊記法で記述する`1）。
   実読するといずれも「Excel\ の1シート、\ YAML\ の1ファイル」「データ行のセル数（Excel形式）または
   ``rows:`` の各要素の長さ（YAML形式）」のように両形式を対で併記した記述であり、S-10規約1が共通部へ置くと
   定めたものである。したがって検査には「片方の形式だけを名指しする場合に限る」という条件を組み込む必要がある。
3. **ゲート3（`mapping.csv`が`_batch/*.csv`の単純連結とバイト一致）は文言が実態と合っていない。**
   各バッチファイルがヘッダ行を持つため`cat _batch/*.csv`では一致しない。実際に成立するのは
   「batch-01のヘッダ＋各ファイルの2行目以降」であり、ゲート文言は実行するコマンドそのもの
   （`{ head -1 _batch/batch-01.csv; for f in _batch/batch-*.csv; do tail -n +2 "$f"; done; }`）で書くべきである。

## レビュー指摘への対応（ラウンド6）

ラウンド5の対応（`ea4b5d3` / `65fc852`）に対する再レビューの指摘への対応記録である。
RST本文側は `d8089aa`、規約文書側（`design.md` / `style.md`）とレビュー記録の訂正は本コミットで対応した。
指摘の全文は `reviews/page-testdata_notation.md` の「ラウンド6」を参照。

### ラウンド5の記録の訂正（実物を確認したうえで）

| 記録箇所 | 誤っていた記録 | 実際 |
|---|---|---|
| 本ファイル「形式非依存の内容を形式別L4に残した箇所」 | 取引単体テストのモックアップ段落は「文面を変えずに位置だけを共通部へ移した」 | `d8089aa` で段落は**分割**された。Excel固有部分は `Excel形式の場合`（L1220）、形式非依存の3点は共通部（L1202）。詳細は当該箇所に訂正を追記済み |
| 本ファイル Completion Criteria ゲート9 | 「`grep -n "1行目"` の全3件」 | HEAD では2件（L431・L554）。いずれも `Excel形式の場合` の中。下表参照 |
| 本ファイル Completion Criteria ゲート13 | 「旧のみ17行 / 新のみ22行」 | HEAD では旧のみ43行 / 新のみ60行。下表参照 |
| `reviews/...` R5-2 | 「共通部の形式依存記述10箇所すべて解消」 | レビューが名指しした10箇所は解消したが、同型の未解消箇所が複数残っていた（R6-1〜R6-4 として再検出）。`d8089aa` で機械走査により是正 |
| `reviews/...` R5-4 | 「`glossary.md` §8 の正表記違反2件を修正」 | §8違反として解消したのは**1件**（`メッセージ同期送信` → `同期応答メッセージ送信`）のみ。`メッセージング処理` は §8 の適用条件（MOMを指すことが文脈から確定できる場合に限る）が成立せず、正表記へは置換していない。実際には第3の表現 `メッセージングのテスト` を新造した。結果は妥当（禁止語なし・MOMと誤断定なし）だが、記録の表現が誤りだった |
| 本ファイル STEP A-4 | 「`テストデータの記載例` の見出し文言一致の規約は変更していない（指示どおり）」 | ラウンド5で**変更した**。作業指示 STEP A-4 の明示的禁止を上書きした判断であり、理由を当該箇所に追記済み |

### `d8089aa` の検査方式（K-4 の訂正の根拠）

ラウンド4のSTEP Cで形式別L4から共通部へ引き上げた記述が形式非依存に直りきっていない欠陥は、
ラウンド4・5と2回にわたり「解消した」と記録しながら再発した。個別対応では取りこぼすため、
`d8089aa` では機械的な全件走査に切り替えた。方式は次のとおりである。

1. 下線記号（`=`/`-`/`~`/`^`）から L1〜L4 の所属を復元し、各行がどの L2 / L3 / L4 に属するかを再構築する
2. `Excel形式の場合` / `YAML形式の場合` の L4 に属する行を除いた残りを**形式非依存領域**として切り出す
   （L2本文・L3共通部・形式別でないL4のすべてを含む）
3. その領域に対し、Excel固有語（`シート` `セル` `Excel` `ブック` `N行目` `ヘッダ行` `データ行` `空セル`）と
   YAML固有キー（`YAML` `rows:` `fields:` `setup_tables:` `expected_tables:` `list_maps:` `setup_files:`
   `expected_files:` `fw_header:` `directives:` `group_id:` `record_type:`）の出現を全件列挙する
   （`:header-rows:` のようなディレクティブオプション行は除外する）
4. 各ヒットについて、Excel固有語とYAML固有キーが**同一行に対で現れているか**で一次分類し、
   片形式だけを名指ししているものを1件ずつ実読して判定する

**本ラウンドで同じ方式を再実行した実測値**（`ea4b5d3` = `d8089aa` 直前、および HEAD）:

| 対象 | 形式非依存領域のヒット総数 | 両形式を対で併記 | 片形式のみ名指し |
|---|---|---|---|
| `ea4b5d3`（`d8089aa` 直前） | 30 | 22 | 8 |
| HEAD（`d8089aa` 後） | 29 | 24 | 5 |

`d8089aa` の実変更を `git show d8089aa` で1件ずつ確認した結果、判定の内訳は次のとおりである。

- **(B) 形式別L4へ移した — 4件**（いずれも `Excel形式の場合` へ）
  1. メッセージボディの行構造（先頭ラベル列・`no`・データ行の連番）
  2. 取引単体テストのモックアップの「シート名 `message` 固定の1つのExcelファイル」
  3. 0バイトの空ファイルのコードブロック（`SETUP_VARIABLE=work/members.csv` 形式）
  4. グループID付き識別子の書式例（`EXPECTED_REQUEST_BODY_MESSAGES[グループID]=リクエストID`）
- **(C) 両形式対比へ書き換えた — 6件**
  1. グループ収集の表（Excelは前方一致 / YAMLは完全一致）
  2. 「行末の空セルを取り除く」→「（Excel形式のみ。YAML形式では `rows:` の各要素をそのまま読み込む）」
  3. 1リクエストIDでN回送信する場合の連番 →「（Excel形式では、連番の値もその順序に合わせる）」
  4. 0バイトの空ファイル（共通部を形式非依存文にし、Excel/YAML双方へ記法を配置）
  5. コメント記法の `//` → 太字ラベル `**Excel\ 形式**\ では` を付与
  6. `errorMode:` の記載位置から Excel の `no` ラベル列前提を外す
- **(A) 共通部に残した** — 下記「(A) 共通部に残す判断をした箇所と理由」のとおり

**数値についての注記**: 本ラウンドの再実行では、コーディネーターから示された「実ヒット26件 = (A)20 / (B)4 / (C)6」という
内訳をそのままは再現できなかった（20+4+6=30 であり26と一致しない）。(B) 4件・(C) 6件は `git show d8089aa` の実変更と
一致することを確認したが、(A) の件数は走査に使う語彙リストの粒度に依存する。上表は本ラウンドで実際に用いた
語彙リスト（上記3.）による実測値であり、記録としてはこちらを採る。

### (A) 共通部に残す判断をした箇所と理由（K-6）

HEAD の形式非依存領域に残る29件について、実読して判定した理由を種別ごとに記す。
作業指示の完了条件「未対応の指摘が残っていない、または残す判断とその理由が記録されている」を満たすための記録である。

| 種別 | 件数 | 該当行 | 残す理由 |
|---|---|---|---|
| 両形式を対で併記している記述 | 24 | L14・L26・L28・L98・L121・L122・L269・L354・L372・L514・L516・L586・L811・L844・L867・L1107・L1109・L1159・L1278・L1297・L1460・L1462・L1482・L1493 | `style.md` S-10 規約1 が「両形式の対応関係そのものを理解しないと概念が掴めないもの」「片方の形式の挙動・制約がもう片方の実装上の理由に由来する関係性の説明」を共通部に置くと定めている。いずれもExcelとYAMLの双方を同一文中で対比しており、片方の形式の読者だけを対象にした記述ではない（例: L98「1つの読み込み単位（Excelの1シート、YAMLの1ファイル）」、L372 `setUpDb` のシート/ファイル対応、L1493「行末の空セルを取り除く（Excel形式のみ。YAML形式では `rows:` の各要素をそのまま読み込む）」） |
| 「セル・エントリ値」のように両形式の単位を並記した記述 | 2 | L126・L1259 | 単一の固有語（`セル`）としてヒットするが、実文では「セル値やエントリ値」「各セル・エントリ値」と両形式の単位を並べており、片形式を名指ししていない。値の解釈規則が全データタイプ・両形式に横断的にかかることを述べる箇所であり、形式別L4へ移すと解釈規則の適用範囲が読み取れなくなる |
| 「データ行」を一般語として使う記述 | 2 | L837・L1157 | `データ行` はExcelの行に限らず、テストデータの1レコードを指す一般語として使っている。L837 は固定長/可変長ファイルの構成要素を並べた表のラベル、L1157 はヘッダとボディの件数一致という制約の説明であり、いずれも形式固有の書式（セル位置・キー名）には踏み込んでいない。YAML形式でも `rows:` の各要素が同じ概念に対応する |
| 太字ラベルで形式を明示している記述 | 1 | L1433 | `コメント・マーカーカラム・空エントリを扱う` は形式別L4対を持たないL3である。`style.md` S-10 規約3 の例外条件2-(ii)（形式別L4対を持たないL3で短い記述方法の違いに触れる場合）に該当し、太字ラベル `**Excel\ 形式**\ では` で形式を明示している。直後のL1460・L1462・L1482 も同じ節で両形式を対比しており、節全体として片形式に閉じていない |

### ゲート5〜13 の HEAD 再実行結果（K-2）

`d545d95` からの累積（`51f8020` → `ea4b5d3` → `65fc852` → `d8089aa`）に対し、ゲート5〜13 を再実行した。

| ゲート | 結果 | HEAD での実測 |
|---|---|---|
| ゲート5: `-` 見出しが3件 | OK | `3 ['機能概要', 'テストデータの構造', '使用方法']` |
| ゲート6: `~` 見出しが10件（構造3 / 使用方法7） | OK | L3総数10。`テストデータの構造` 3件（`テストクラスとテストデータの対応`・`データブロックとデータタイプ`・`グループIDによる使い分け`）、`使用方法` 7件（`テーブルのデータを記述する`・`LIST_MAPのデータを記述する`・`テストケース一覧（testShots）を記述する`・`ファイルのデータを記述する`・`メッセージングのデータを記述する`・`値を特殊記法で記述する`・`コメント・マーカーカラム・空エントリを扱う`）。並び順は再構成時と同一 |
| ゲート7: 形式別L4対がL3の末尾2つ | OK | 形式別対を持つ8つのL3すべてで末尾2つが `['Excel形式の場合','YAML形式の場合']`。`テーブルのデータを記述する` は6つのL4が `['準備データ（SETUP_TABLE）を記述する','共通の準備データをまとめる','期待値（EXPECTED_TABLE等）を記述する','カラムを省略する','Excel形式の場合','YAML形式の場合']`。対象外は `テストケース一覧（testShots）を記述する`（処理方式別L4が6つ）・`コメント・マーカーカラム・空エントリを扱う`（L4なし） |
| ゲート8: `important`（実行順序）と `tip`（マスタデータ）が最初のL4より前 | OK | `テストクラスとテストデータの対応`（L24）から最初のL4 `Excel形式の場合`（L42）までの範囲に `.. important::` `.. tip::` の両方が存在 |
| ゲート9: カラム名の行を「1行目」と呼ぶ箇所が0件 | OK | `grep -n "1行目"` の該当は**2件**（ラウンド4の記録は3件）。L431（`テーブルのデータを記述する` の `Excel形式の場合`「1行目に「SETUP_TABLE=テーブル名」、2行目にカラム名、3行目以降にデータを記載する。」）・L554（`LIST_MAP` の `Excel形式の場合`、同じ数え方）。いずれもデータタイプ行を1行目とする統一後の記述であり、カラム名の行を1行目と呼ぶ箇所は0件。`2行目`/`3行目` の該当（L459・L472・L1433）も同じ数え方と整合。ラウンド4で「記録のみ・未対応」としていたメッセージボディの数え方（旧L1097）は `ea4b5d3` F-1 で消滅済み |
| ゲート10: `:ref:` 未定義参照0件 / `.. _` ラベル直後が見出しでないもの0件 | OK | ページ内ラベル16本。ページ内参照はすべて定義済み（未定義0件）。ラベル16本すべてが「ラベル → 空行 → 見出し行 → 下線」の並び（違反0件）。ページ外参照はゲート12のフルビルドで `undefined label` 警告が出ていないことをもって確認 |
| ゲート11: 段落内改行が0件 | OK | `段落内改行: 0` |
| ゲート12: Docker フルビルド（`-a`）で `build succeeded` かつ警告が既知1件のみ | OK | 末尾 `build succeeded, 1 warning.`。警告の全文は `/root/document/ja/application_framework/application_framework/libraries/db_double_submit.rst:108: WARNING: undefined label: how_to_set_token_in_request_unit_test (if the link has no caption the label must precede a section header)` の1件のみ。`grep -i "warning\|error"` で全出力を走査し、他に `WARNING`/`ERROR` 行なし。実行コマンドは作業指示のとおり（`nablarch-document-build-sandboxed:latest`）。`_build/` は削除していない。ビルドで更新された `locales/ja/LC_MESSAGES/sphinx.mo` は `git checkout -- locales/ja/LC_MESSAGES/sphinx.mo` で復元した |
| ゲート13: `d545d95` からの情報欠落なし | OK | 非空行の `collections.Counter` による多重集合比較 → **旧のみ43行 / 新のみ60行**（ラウンド4時点は 旧17 / 新22）。増分の由来は下表 |

**ゲート13: 旧のみ43行の由来（HEAD 時点）**

ラウンド4時点の17行（本ファイル「ゲート13: 消えた17行の由来」に既記録）に加え、`ea4b5d3` / `d8089aa` による
書き換えで26行が入れ替わった。由来は次のとおりで、すべて既記録の是正に対応し、段落・表の行・
コードブロックの**実体が失われた行は0件**である。

| 由来 | 該当する消えた行 |
|---|---|
| R5-1（`no` の説明が実装と矛盾）と R6-1（レコード種別の事実誤り）による書き換え | メッセージボディ構成の文、`MESSAGE=setUpMessages…先頭行の名前は固定` の文、整形・補完の表の2行（`行末の空セルを取り除く` / `レコード種別が空欄の行に、既定のレコード種別を補う`） |
| R5-2 / R6-4（共通部の片形式語の言い換え） | `1シートに1テストケース`→`1つの読み込み単位に1テストケース`、`1セルに記述すると`→`1つのフィールドに記述すると`、`エントリ数（rows合計）`→`データ行の合計数`、`行内の全セル`→`行内の全フィールド`、`ヘッダ行を持つ`→`カラム名を持つ`、`Excelの見た目のためだけに`→`テストデータの見た目のためだけに`、`同期応答メッセージ受信…のメッセージング処理では`→`…のメッセージングのテストでは`、`MESSAGE=expectedMessages の記述は不要`→`expectedMessages のデータブロックを記述する必要はない` |
| R5-3（記載例リンクの位置統一・導入文の是正） | `LIST_MAP` 節・`testShots` 節・`テーブル` 節の導入文と記載例リンク行の再配置 |
| R6-2（構造L2導入文の中間層除去） | `テストクラスに対応するテストデータファイル・読み込み単位・データブロックという階層で構成する` |
| R6-3（モックアップ段落の分割） | `取引単体テストのモックアップクラスでは…シート名 message 固定の1つのExcelファイルにまとめて定義する。識別子の書式（…）でどの電文かを示し…` の1行 |
| R6-4(a)（0バイト空ファイルの記法の分離） | 共通部にあった `SETUP_VARIABLE=work/members.csv` のコードブロック4行（`Excel形式の場合` へ移動、行の実体は保持） |
| R6-4(b)〜(c) | `グループID付きの書式例は…である。ディレクティブには…` の1行（分割して `Excel形式の場合` へ）、`errorMode:` の `no を除く最初のフィールド` の1行 |
| 機能概要のメタ説明除去（`d8089aa`） | `…「Excel形式の場合」「YAML形式の場合」という小見出しに分けて示す。` の1行 |
| 参照の具体化（`d8089aa`） | `収集方法の表を参照` 系の遠回りな参照2本 |

新のみ60行は、上記の置き換え後の行に加え、`d8089aa` で新規追加した次の行を含む。

- `使用方法` L2 の導入文1行、`データブロックとデータタイプ` / `グループIDによる使い分け` / `テーブルのデータを記述する` /
  `LIST_MAPのデータを記述する` の各L3共通部へ移した記載例リンク4行
- `Excel形式の場合` に新設した識別子行の説明・メッセージボディのラベル列の説明
- **内容を追加した2文**（作業指示「内容の追加・削除を行わない」に対する例外。下記）

### 内容を追加した2文（作業指示の禁止事項に対する例外。K-7）

`d8089aa` で、次の2文を新たに書き加えた。**これは作業指示 `ntf-doc-09-restructure.md` の禁止事項
「内容の追加・削除を行わない」に対する例外である。**

- `ファイルのデータを記述する` の `YAML形式の場合`:
  「0バイトの空ファイルを表現するには、``records:``\ に空配列 ``[]``\ を記載する（``records:``\ キー自体は省略できない）。」
- `メッセージングのデータを記述する` の `YAML形式の場合`:
  「同期応答メッセージ送信で使う電文の連番は、``rows:``\ の記述順（1始まり）から自動的に付与されるため、
  テストデータに記載しない。」

- **根拠**: JSON Schema（`nablarch-testing-yaml` の jar に `nablarch/test/ntf-testdata-yaml-schema.json` として同梱。
  所在は `input/ntf-testdata-doc.md:70` に記載）の `file_data.records`、および実装。
- **例外とした理由**: これらの記法はもともとExcel固有の書き方（`SETUP_VARIABLE=…` のコードブロック、
  先頭ラベル列に書く連番）でしか書かれておらず、それを `Excel形式の場合` へ移すだけでは、
  **YAML形式の読者に該当記法が一切残らなくなる**。移動だけでは情報の欠落が生じるため、
  対応するYAML側の記法を1文ずつ補った。

### 未検証の可能性がある箇所（K-7）

現行RSTの `メッセージングのデータを記述する` には、`Excel形式の場合`（L1218）に
「フィールド名称行の先頭要素に記載した値がレコード種別として使われることはない。」、
`YAML形式の場合`（L1245）に「``record_type``\ の値は、フレームワーク内部で常に ``"default"``\ に置き換えられる。
Excel・YAMLとも任意の値を装飾的に記述できるが、実行時の挙動には影響しない。」とある。

この記述は `MESSAGE` 系（`MessageParser`）については真だが、**Excelの同期応答送信経路（`SendSyncMessageParser`）は
`onReadingNames` を上書きしないため、`DataFileParser` によりフィールド名称行の先頭要素がレコード種別として
使われる可能性がある。** この経路については実装で確認していない。厳密には「Excel・YAMLとも装飾的」が
成り立たない可能性があるため、**未検証の可能性がある箇所として記録する。**
次にメッセージングの節に触れる際に `SendSyncMessageParser` / `DataFileParser` / `MessageParser` の3者を読んで確定させること。

### 規約文書側の是正（本コミット）

| 対象 | 是正内容 |
|---|---|
| `style.md` S-02（G-7） | 第3部の例外を「『テストデータの書き方』の1ページのみ」から**2ページ**へ改めた。`テストデータの記載例` が `機能概要` も `使用方法` も持たず記述例の見出しをL2として並べる構成である旨を追加（`testdata_examples.rst` で実測: L2見出し9件、`機能概要`・`使用方法` いずれも0件） |
| `design.md` §4「ページのアウトライン」（G-7） | 同上。例外を箇条書き2項に分けた。あわせて「テストデータの2ページ」節の冒頭文を「上記2つの例外のうち…」と接続し、**同一節の内部で矛盾していた状態を解消**した |
| `style.md` S-10 規約3（G-8） | 太字ラベルの例外条件2 を、末尾固定という新しい前提のもとで実態に合う2条件へ書き直した。(i) 形式別L4対を**持つ**L3の共通部で短い記述方法の違いに触れる場合、(ii) 形式別L4対を**持たない**L3で短い記述方法の違いに触れる場合。あわせて死文となっていた「同じセクション内でExcel/YAMLの話題が複数回出てくる場合も、**最初の**見出し対の下に集約する」を「2組目の見出し対を作らず、その1組の下にまとめる」に改めた。判断は `testdata_notation.rst` の太字ラベル使用箇所4件（L586・L867・L1433・L1462）の全件走査に基づく（内訳は `reviews/page-testdata_notation.md` R6-6 の表） |
| `style.md` 観点数 | 11個のまま（S-01〜S-11）。`grep -c "^### S-" mapping/style.md` → `11` |

### `#10` への申し送り（本ラウンドで追加したもの）

`reviews/page-testdata_notation.md` の「`#10`（テストデータの記載例）への申し送り」に、次を追加・拡張した。

- 申し送り3の拡張: `style.md` S-03 例外1 の**根拠自体**が見出しレベルに依存しており、形式別分割がL3に落ちる
  記載例ページには適用できない。`#10` 着手前に (a) 見出し名を変える / (b) 形式別に分割しない /
  (c) 根拠を張り替える のいずれを採るか決める必要がある
- 申し送り4（新規）: `mapping.csv` の `dest_page=テストデータの記載例` の**65行すべてが `dest_section=使用方法`**
  であり、`design.md` §4・`vocabulary.md` 例外3・`testdata_examples.rst` と食い違う。
  **本ラウンドでは修正しない。** 作業指示の禁止事項が「他の33ページの `dest_section` を変更しない」と
  定めており、`テストデータの記載例` は本作業の対象ページではないためである
- 申し送り5（新規）: 生成HTMLで形式別L4のアンカーが `#excel` / `#yaml` / `#id7` / `#id8` … と自動採番になり、
  見出しの増減で深いリンクが壊れる（ビルド済みHTMLで16個を実測）
- 申し送り6（新規）: 上記「内容を追加した2文」
- 申し送り7（新規）: 上記「未検証の可能性がある箇所」

## レビュー指摘への対応（ラウンド7 = 最終ラウンド）

ラウンド6の対応（`d8089aa` / `c94a870`）に対する最終の修正ラウンド。指摘は移動を伴わない局所的な語句修正が中心である。
**事実に関わる修正はすべて実装ソース（`/home/tie303177/work/nablarch/`）または JSON Schema を自分で読んで裏付けた。**
指摘の詳細と実装の file:line は `reviews/page-testdata_notation.md` の「ラウンド7」を参照。

### 対応した指摘

| ID | 区分 | 対応内容 | 裏付け |
|---|---|---|---|
| F-1 | must（事実誤り） | レコード種別の記述3箇所（現状 L1218-1220 / L1247 / L1497）の適用範囲を限定した。Excelは `MESSAGE` のみ `"default"` 固定、同期応答メッセージ送信系4データタイプとモックアップ経路は書いた値がそのままレコード種別になる。YAMLはメッセージ系の全キーが `"default"` 固定 | `MessageParser.java:58-67`（`onReadingNames` 上書き）／`SendSyncMessageParser.java:110-143`（`onReadingNames` を上書きしない）／`DataFileParser.java:259-263`（`setRecordType(fieldNamesLine.get(0))`）／`BasicTestDataParser.java:99-103,113-117`／`GroupMessageParser.java:43`／`YamlFileBuilder.java:177,182-184`／`YamlMessageBuilder.java:189,207`／`ntf-testdata-yaml-schema.json` `$defs/record_fragment` |
| F-2 | must（自己矛盾） | `errorMode:` の `important`（現状 L1196）の自己矛盾を解消。RESPONSE系こそが `RequestTestingSendSyncSupport` 経路であり、そこで `errorMode:` は反映される | `RequestTestingMessagingProvider.java:190-206`／`RequestTestingSendSyncSupport.java:157`／`RequestTestingMessagePool.java:78-84`（判定あり）／`SendSyncSupport.java:288-294`（モックアップ側も判定あり）／`RequestTestingMessagePool.java:109`（要求電文側には判定なし）。**出典 `input/ntf-testdata-doc.md:492` は実装と食い違っており誤りである** |
| F-3 | must（回帰） | `ファイルのデータを記述する` のL3共通部の用語表を形式非依存にした（`〜行` → `〜`、定義からも「行」を除去）。導入文の「各行の役割」「各行の名称と役割」も「各要素の」に改めた。これによりメッセージング側の「前述のファイルデータと同じ構成を持つ」という参照が成立する | YAMLの構造は `records:` 配下の `record_type` / `fields:`（`name`・`type`・`length`）/ `rows:`（`YamlFileBuilder.java` の `buildFragmentsInternal`）。Excel固有の行としての書き方は `Excel形式の場合` の名称表に既存のため移動不要 |
| F-4 | must（削除の回帰） | 落ちていた具体例 `EXPECTED_REQUEST_HEADER_MESSAGES=リクエストID` を復活させた。**旧位置（L3共通部）ではなく `Excel形式の場合` に置いた**（`データタイプ=識別子の値` はExcel固有の書式であり、共通部へ戻すと `d8089aa` R6-4(b) で解消した欠陥が再発するため） | `grep -c "EXPECTED_REQUEST_HEADER_MESSAGES=" `: `d545d95`=1 → `c94a870`=0 → 本ラウンド=1 |
| F-5a | note | YAMLのファイルデータの必須キーを `path` のみから `path`・`type`・`records` の3キーに是正 | `ntf-testdata-yaml-schema.json` の `$defs/file_data.required` = `["path","type","records"]`（`python3 -c` で実読） |
| F-5b | note | 収集方法の表の「単一」行を「データタイプが合致する（Excel形式ではデータタイプ名の前方一致）データブロックのうち、IDが完全一致する最初の1件を取得」に是正 | `TestDataParsingTemplate.java:321-333`（`startsWith`）／`SingleDataParsingTemplate.java:33-40`（`typeValue.equals(id)`） |
| F-5c | note | `データブロックとデータタイプ` の `Excel形式の場合` の参照の主語を、参照先の表の列見出し（「同一IDのデータブロックが複数ある場合」）に揃えた | — |
| S-1 | must（規約） | `style.md` S-10 規約3 の太字例外条件(iii) の根拠を実測に合う表現へ差し替えた。**本ラウンド終了時点の実測は6箇所**（ラウンド6時点の5箇所＋F-1 で追加した表セル1件）。分割の是非は `#10` 以降へ申し送り | 全件走査の内訳は `reviews/page-testdata_notation.md` ラウンド7の表 |
| S-2 | must（規約） | `style.md` S-07 に例外を明文化し、本ページの `list-table` 34件・simple table 0件という形を追認した。34表の変換は行わない | 実測: `testdata_notation.rst` `list-table` 34 / simple table 0、`about/index.rst` `list-table` 1 / simple table 3、`list-table` 34件中 `:ref:` を含むセル0件。根拠は `steering.md` Rules L43（表示幅で揃えないと `Malformed table`、2026-08-03 に `about/index.rst` で実発生） |

### 記録の訂正

- `reviews/page-testdata_notation.md` に、**ラウンド1の「S-01〜S-03, S-05〜S-08は違反なし」のうち S-07 の記録が基準の取り違えであった**旨を、ラウンド7の節で訂正として追記した（ラウンド1の表そのものは書き換えていない）
- **申し送り7（`SendSyncMessageParser` 経路が未検証）は F-1 の実測により確定したためクローズ**し、確定内容を申し送り7に追記した

### ゲート再実行結果（ラウンド7）

| ゲート | 結果 | 実測 |
|---|---|---|
| 1: `-` 3件 / `~` 10件（構造3・使用方法7、並び順不変）/ 形式別L4対が各L3の末尾2つ | OK | `-` = 3（`機能概要`・`テストデータの構造`・`使用方法`）。`~` = 10（構造3: `テストクラスとテストデータの対応`・`データブロックとデータタイプ`・`グループIDによる使い分け` / 使用方法7: `テーブルのデータを記述する`・`LIST_MAPのデータを記述する`・`テストケース一覧（testShots）を記述する`・`ファイルのデータを記述する`・`メッセージングのデータを記述する`・`値を特殊記法で記述する`・`コメント・マーカーカラム・空エントリを扱う`）。並び順は `c94a870` と同一。形式別対を持つ8つのL3すべてで末尾2つが `['Excel形式の場合','YAML形式の場合']`。対象外は `testShots`（処理方式別L4が6つ）と `コメント…`（L4なし） |
| 2: `.. _` ラベルの数と名前が修正前後で不変 | OK | `c94a870` 16本 / 本ラウンド後 16本、**リストが完全一致**（名前・順序とも） |
| 3: 段落内改行0件 | OK | 0件 |
| 4: 見出しのアンダーラインが東アジア文字幅で本文以上 | OK | `unicodedata.east_asian_width` で全39見出し（L2:3・L3:10・L4:26）を判定し、下線幅が見出し幅未満のものは0件 |
| 5: Docker フルビルド（`-a`）で `build succeeded` かつ警告が既知1件のみ | OK | 末尾 `build succeeded, 1 warning.`。警告は `/root/document/ja/application_framework/application_framework/libraries/db_double_submit.rst:108: WARNING: undefined label: how_to_set_token_in_request_unit_test (if the link has no caption the label must precede a section header)` の1件のみ（`grep -i "warning\|error"` で全出力を走査）。`_build/` は削除していない。`locales/ja/LC_MESSAGES/sphinx.mo` は更新されたため `git checkout --` で復元（`git status` がクリーンであることを確認済み） |
| 6: `d545d95` と HEAD の非空行の多重集合比較 | OK | 旧のみ**57行** / 新のみ**75行**（`c94a870` 時点は 旧43 / 新60）。本ラウンドで新たに消えたのは**14行**で、内訳は下表。実体が失われた行は0件 |
| 7: `style.md` の観点が11個 | OK | `grep -c "^### S-" mapping/style.md` → `11` |
| 8: `reviews/page-testdata_notation.md` のラウンド1〜4が不変 | OK | `git diff -U0` のハンク開始は `@@ -173,0 +174,48 @@`（ラウンド6の直後への挿入）と `@@ -198 +246,18 @@`（申し送り7）の2箇所のみ。ラウンド1〜4（L9-99）・ラウンド5（L101-131）・ラウンド6（L133-172）はいずれも差分なし |
| 9: `verify_mapping.py` が exit 0、594行 / 12,986 / 11,983 が不変 | OK | `Loaded 594 rows from mapping.csv` / `lines total (all rows): 12986` / `lines total (excluding DROP): 11983` / `OK: no errors` / `exit=0`。本ラウンドで `mapping/` 配下のデータは変更していない（変更したのは `mapping/style.md` のみ） |

**ゲート6: 本ラウンドで新たに消えた14行の由来（すべて同一箇所の書き換えであり、置き換え後の行が存在する）**

| 由来 | 消えた行 | 件数 |
|---|---|---|
| F-3（用語表の脱「行」化） | `各行の名称と役割は、以下のとおりである。`／`  * - レコード種別行`／`    - ファイルデータのレコード種別を示す行`／`  * - フィールド名称行`／`    - 各フィールド名称を並べた行`／`  * - データ型行`／`    - フィールドのデータ型を示す行（日本語表記の例: ``半角英字``\ ）`／`  * - フィールド長行`／`    - 各フィールドのバイト長を示す行（固定長ファイルのみ存在）`／`  * - データ行`／`    - 実データを並べた行` | 11 |
| F-5b（収集方法の表の是正） | `    - データタイプとIDが完全一致する最初の1ブロックを取得` | 1 |
| F-2（`errorMode:` の `important` の是正） | `  ``errorMode:``\ による障害系テストが機能するのは、応答電文（\ RESPONSE\ 系）およびモックアップクラス経由の送受信に限られる。…` | 1 |
| F-1（YAMLの `record_type` の適用範囲の限定） | `` `directives:`（`text-encoding` など）と `fw_header:`（`requestId` など）は別キーである。…`` で始まる段落（末尾2文を書き換えたため行全体が差し替わった） | 1 |

（`c94a870` との比較では20行が消えているが、うち6行は `d545d95` 以降のラウンドで追加された行の書き換えであり、`d545d95` 起点の欠落には数えない。復活した行は0行。）

### 本ラウンドで内容を追加・削除した箇所

- **追加**: `Excel形式の場合` に「（慣行に従って ``no``\ と記載した場合は、レコード種別も ``no``\ になる）」の1節。F-1 の要件「読者が自分の書いたレコード種別がどう扱われるかを判断できること」を満たすため
- **復活**: `EXPECTED_REQUEST_HEADER_MESSAGES=リクエストID` の具体例（F-4。`d8089aa` で失われたものを戻した）
- **削除**: なし。`records:` キー自体を省略できない旨（F-5a）は、必須キー3つの記述に包含されたため重複表現を落としただけである

### 通し読みで追加に見つけた不整合（ラウンド7）

| 箇所 | 内容 | 扱い |
|---|---|---|
| 現状 L1253 の `important` | 「``record_type`` に特別な予約値はない」は JSON Schema の記述と一致するが、実装は `YamlFileBuilder.java:177` でメッセージ系に限り `record_type: FW_HEADER` のレコードを読み飛ばす | 申し送り13として記録。修正は仕様意図（後方互換の読み飛ばしか、予約値か）の確認を要するため本ラウンドでは行わない |
| 現状 L254 のグループID `default` | 「テストケース一覧のカラムで ``default`` と記載する」と「Nablarchバッチアプリケーションでは…（ウェブアプリケーション・メッセージングには適用されない）」が互いを打ち消し合って読める | 申し送り12として記録（作業指示に沿う） |
| 記載例リンクの重複 | 同一アンカーへの「実際の記述例は…を参照。」が21本中14本 | 申し送り8として記録（作業指示に沿う） |
| `値を特殊記法で記述する` の `Excel形式の場合` | 16個の形式別L4のうち唯一、導入文なしで表から始まる | 申し送り11として記録（作業指示に沿う） |
| `フィールド名称に重複した名称は許容されない` の重複 | メッセージングのL3共通部だけで2回、ページ全体では3回 | 申し送り10として記録。内容の削除を伴うため本ラウンドでは対応しない |

## QA Expert Review

（以下はコーディネーターが記入。4観点のレビューは独立したサブエージェントで**3ラウンド**実施した。
ラウンドA = `51f8020` に対して4観点、ラウンドB = `65fc852` に対して4観点、ラウンドC = `c94a870` に対して3観点
（A網羅性・C用語/事実検証・D整合性。Bトンマナは前ラウンドの指摘が規約文書側で解消済みのため、
コーディネーターが機械チェックで代替）。各ラウンドのプロンプトには `steering.md` Rules の3点
（実測コマンドで裏付ける／付属の検証スクリプトを正解として使わず独立に組む／敵対的にレビューする）を必ず含めた。）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 検証手法が目的に対して意味があるか（「通った」ではなく正しいものを見ているか） | NG → 是正 | ラウンドAで「ゲート7は見出しの並びしか見ておらず、形式別L4配下に形式非依存の本文がぶら下がる欠陥を検出できない」「ゲート9はSTEP Dの症状しか見ていない」と判定。実際にこの穴を通って欠陥が3ラウンド再発した。`d8089aa` で形式非依存領域を機械走査する検査を導入し、ゲート設計の弱点は `#10` 以降への申し送りとして記録した |
| ゲート記録と実物の一致 | NG → 是正 | ラウンドBで「モックアップ段落の記録が実物と正反対」「ゲート13の記録値が陳腐化（17/22 vs 実測43/60）」を検出。`c94a870` で全ゲートをHEADに対して再実行し記録を更新。ラウンドCでは全ゲート値が独立実測と一致することを確認 |

## Expert Reviews

### Design Expert（D: 整合性）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 構造が目的に適合しているか | OK | 最終ラウンドで「構造側3節＝器、使用方法側7節＝対象別記法＋解釈規則。逆流なし」。形式別L4対は8つのL3すべてで末尾2つ（コーディネーターも独立再現） |
| 文書システム全体の整合（design.md / style.md / vocabulary.md / mapping.csv / RST の5者） | NG → 是正 | `design.md` §4 の記載例見出し規約が改題に追随せず `testdata_examples.rst` と不整合（`65fc852` で是正）、`style.md` S-02 の例外が1ページのみで第2の例外を落とす（`c94a870` で是正）、S-10 規約3 の太字例外条件が「末尾2つ固定」と自己矛盾（`c94a870` で是正）、同条件(iii)の根拠が実測と不一致（`fed3705` で是正）。最終ラウンドの5者集計は整合 |

### Craft Expert（B: トンマナ）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 媒体固有のベストプラクティス（RST・日本語解説書） | NG → 是正 | 移動による主語欠落1件（`ea4b5d3` で混入 → `d8089aa` で是正）、`ja/application_framework/` に前例のない語（「小見出し」「以下では」）の使用（`d8089aa` で是正）。段落内改行0件・東アジア文字幅の下線違反0件は全ラウンドで維持 |
| 既存の文体との一貫性 | NG → 一部申し送り | S-07（表の記法）で `#8`（simple table 3件）と本ページ（list-table 34件・simple 0件）が正反対であり、ラウンド1の「S-07 違反なし」は基準の取り違えだった。`fed3705` で S-07 に例外を明文化して本ページの形を追認し、ラウンド1の記録を訂正した |

### Verification Expert（C: 事実検証 / A: 網羅性）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 主張が実際に裏付けられているか | NG → 是正 | 事実誤りを**3ラウンド連続で検出**した。(1) `no` を「連番・送信順序を示す電文内フィールド」とした記述（`51f8020`、実装では先頭列は読み込み対象外で突合は記述順）、(2) その是正で書いた「先頭要素はレコード種別として扱われる」（`ea4b5d3`、`MessageParser` は `"default"` を固定挿入）、(3) さらにその是正で書いた「レコード種別として使われることはない／常に `default` に置換」（`d8089aa`、SendSync 経路では記述値がそのまま採用される）。最終ラウンドでレビュアが自作 Java プローブを実行し `MESSAGE → default` / `SENDSYNC → 記述値` を確定させ、`fed3705` で適用範囲を限定した |
| カバレッジ（主張・出典・エッジケース） | NG → 是正 | 情報欠落: `d8089aa` がモックアップ段落を分割した際に `EXPECTED_REQUEST_HEADER_MESSAGES=リクエストID` の具体例を落としていた（`fed3705` で復活。現在1件）。出典側の誤り: `errorMode:` の適用範囲について `input/ntf-testdata-doc.md:492` が実装と食い違っており、実装を優先して訂正した |

## Overall Verdict

- Self-check: OK
- QA: OK（ラウンドA・Bで検出した検証手法の穴と記録の不一致は是正済み。ゲート設計の弱点は `#10` 以降へ申し送り）
- Design expert: OK（規約4件を是正。残る不整合は `#10` 申し送り1・4・9として記録）
- Craft expert: OK（S-07 は例外の明文化で追認。ラウンド1の記録を訂正）
- Verification expert: OK（事実誤り3件を実測で確定・是正。出典誤り1件を実装優先で訂正）
- Ready to check off: **No** — 成果物としては完成しており全ゲートを満たすが、`steering.md` の Rules
  「user review の承認を受けるまで次タスクに着手しない」および本作業指示の禁止事項
  「user review の承認を受けるまで `#10` に着手しない」により、**user review の判定（`/rn:ty` または `/rn:gm`）待ち**。

### 未再レビュー事項（正直な申告）

**最終の修正コミット `fed3705` は、レビューを受けていない。** 作業指示のレビュー反復上限（3ラウンド）に
到達したため、`fed3705` の内容についてはコーディネーターによる独立検証（見出し構成・形式別L4対の位置・
段落内改行・東アジア文字幅・`verify_mapping.py`・Docker フルビルド・`d545d95` との行集合比較）のみを
実施した。実装に基づく事実記述（F-1 のレコード種別の限定、F-2 の `errorMode:`）については、
実装の file:line が本ファイルに記録されているため追試は可能である。

### user review で判断を仰ぐ事項

1. **作業指示の明示的禁止を1件上書きした。** STEP A-4 は「`design.md` §4 の `テストデータの記載例` の
   見出し文言一致の規約は変更しない」と定めていたが、STEP A-2 の改題により当該規約の前提（3見出しが
   `使用方法` 配下にあり文言が一致している）が崩れたため、変更しないと `design.md` が実態と矛盾したまま
   残る。変更した（`65fc852`・`c94a870`）。**元に戻すことは可能。**
2. **「内容の追加・削除を行わない」に対する例外を2件設けた。** YAML の空ファイル記法（`records: []`）と
   連番の自動付与について各1文を追記した（`d8089aa`）。形式別L4へ移すだけでは YAML 読者に該当記法が
   一切残らなくなるための措置で、JSON Schema（`ntf-testdata-yaml-schema.json` の `$defs/file_data`）を
   根拠としている。
3. **S-07（表の記法）の裁定。** `#8` と本ページが正反対の形になっていたため、`fed3705` で S-07 に例外を
   明文化して本ページの形（list-table 34件）を追認した。逆（本ページを simple table 化する）を選ぶ場合は
   34表の変換が必要になる。残り33ページに波及する判断である。
