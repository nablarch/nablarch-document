# self-check: `#9` レコード種別の形式差の明記（`ntf-doc-09-recordtype.md`）

対象: `ja/development_tools/testing_framework/implementation/testdata_notation.rst`
基点コミット: `b7e97b3`（`a2fb950` は `steering.md` のみの `/rn:dn` コミット）
作業指示: `.rn/20260724-ntf-yaml-support/ntf-doc-09-recordtype.md`

## 実施した変更

本文への追記は `important` **2件のみ**。既存記述の削除・書き換えは0件（`git diff --numstat` が `8 0`、削除行0）。

| # | 箇所 | 追記内容 | 由来 |
|---|---|---|---|
| 1 | `メッセージングのデータを記述する` 共通部（新 L1121〜1124） | 電文のレコード種別の扱いが両形式で異なること（Excel: 同期応答送信の4データタイプ・取引単体テストのモックアップ電文では記載値がそのままレコード種別／YAML: `messages` と同期応答送信の4キーのいずれでも `record_type` は常に `"default"` 固定／したがって Excel→YAML 変換で扱いが変わる） | 作業指示 手順1（`must`） |
| 2 | `テーブルのデータを記述する` 共通部（新 L358〜361） | 行にカラムの値を書かなかった場合の扱いが両形式で異なること（Excel: 空文字補完／YAML: `null`） | 作業指示 手順3 の横並び確認で自発検出 |

どちらも「どちらが正しい」「どちらが仕様である」とは書いていない。現在の挙動を両形式について並べるにとどめた。実装の内部構造（クラス名・メソッド名）は本文に書いていない。

## 手順3: 他の形式差の有無（形式別L4対を持つ8つのL3すべて）

確認方法: 各L3の `Excel形式の場合` / `YAML形式の場合` の本文を突合し、**同じ事象について異なる結果を述べていて、かつその差が共通部にも他のどこにも書かれていない**箇所を探した。ページ内の既存記述どうしの突合のみで、新規の事実調査は行っていない。

| # | L3 | 突合結果 | 判定 |
|---|---|---|---|
| 1 | テストクラスとテストデータの対応 | Excel は「1ファイル＝テストクラス／1シート＝読み込み単位」、YAML は「1ディレクトリ＝テストクラス／1ファイル＝読み込み単位」。挙動差ではなく構造の対応関係の違いであり、共通部（L28）が「ファイルの配置と読み込み単位の対応は形式によって異なるため、それぞれについて後述する」と明示している | 該当なし |
| 2 | データブロックとデータタイプ | Excel は識別子が前方一致、YAML はトップレベルキーの完全一致。**差そのものはYAML側L4に明記されている**（「完全一致のため前方一致は発生しない」）うえ、共通部 L192 が「識別子の書き方は、形式によって異なる」と予告している。YAML の「トップレベルキーの重複はエラー」は YAML 構文レベルの制約であり、Excel の「同一データタイプの複数ブロック」と同じ事象ではない（YAML では同一キー下のリスト要素として並ぶ） | 該当なし（検討したが、差は既に本文に書かれている） |
| 3 | グループIDによる使い分け | Excel は記述順序が結果を変える（データタイプ・グループIDを交互に記述すると収集が途中で終了）、YAML は「記述順序や交互記述を気にする必要がない」。**差は YAML 側L4の当該文が Excel 側の制約を直接打ち消す形で明記されている**。また Excel→YAML 変換で意味が変わる方向の差ではなく（制約が緩む方向のみ）、正しく記述された Excel データの解釈が変わることはない | 該当なし（検討したが追加不要と判断） |
| 4 | テーブルのデータを記述する | Excel「データ行のセル数がヘッダ行より少ない場合、記述しなかった分のカラムには**空文字**が設定されたものとして扱われる」（現 L476）と、YAML「後続の行が先頭行のキーの一部を持たない場合、そのカラムは **`null`** を明示的に指定したのと同じ扱いになる」（現 L493）。同じ事象（行にカラムの値を書かなかった場合）について異なる結果を示しており、その差はどこにも書かれていない。`ファイルのデータを記述する` では両形式とも空文字補完で揃っている（共通部 L844 に明記）ため、テーブルデータだけが揃っていない | **該当あり → 共通部に `important` を追加**（上表 #2） |
| 5 | LIST_MAPのデータを記述する | Excel は「2行目＝Mapのキー、3行目以降＝Mapの値」、YAML は `list_maps:` の `id` エントリに `rows:` で記述。記法の違いのみで、取得結果・ID解決規則（完全一致・先着一致・不存在は空データ）は共通部 L548 に形式非依存で書かれている | 該当なし |
| 6 | ファイルのデータを記述する | Excel 固有の「データの先頭要素を必ず空にする」制約は、YAML 側L4が「先頭を空にするという Excel 形式の制約はない」と明記している。値が足りない行の扱い（不足フィールドは `""` 補完）と空ファイルの表現は、共通部 L844 が**両形式のトリガー方法まで含めて**書き分け済み | 該当なし |
| 7 | メッセージングのデータを記述する | レコード種別の扱いが両形式で異なる（本作業の `must`） | **該当あり → 共通部に `important` を追加**（上表 #1） |
| 8 | 値を特殊記法で記述する | Excel・YAML の記法対照表が同じ行ラベル（通常の文字列／null／文字列の null／空文字／先頭ゼロ付き数値／…／改行文字）で並んでおり、差は行単位で直接読み取れる。唯一の挙動差である「文字列の `null`」（Excel は `"null"` で表現可、YAML は該当なし）も、YAML 表の該当行に「該当なし」と明記され、直前の地の文 L1385 が「`"null"` でも同じく Java の `null` になる」と述べている | 該当なし（差は本文に書かれている） |

**結論**: 8セクション中、該当2件（#4・#7）。うち #7 は作業指示の `must`、#4 は横並び確認で新たに検出したもの。残る6セクションは「確認した、該当なし」。#2・#3・#8 は挙動差そのものは存在するが、いずれも差がページ本文に書かれているため追加不要と判断した（判断理由は上表に記載）。

## ゲート

| ゲート | 結果 | 実行結果 |
|---|---|---|
| 1. `verify_mapping.py` が `exit 0`、594行 / 12,986 / 11,983 が不変 | OK | `Loaded 594 rows from mapping.csv` / `lines total (all rows): 12986` / `lines total (excluding DROP): 11983` / `OK: no errors` / `EXIT=0` |
| 2. `git diff b7e97b3 HEAD -- .rn/20260724-ntf-yaml-support/mapping/` が空 | OK | 作業ツリー込みの `git diff b7e97b3 -- .../mapping/` の行数が **0**（`mapping/` 配下は一切変更していない） |
| 3. `-` 3件 / `~` 10件 / `^` 26件が不変 | OK | `dash: 3` / `tilde: 10` / `caret: 26`（変更前と同値） |
| 4. 追加した `important` が `メッセージングのデータを記述する` の最初の `^` 見出しより前 | OK | 機械判定 → L3見出し行 = 1109、最初の `^` 下線 = 1221（見出し行 1220 = `Excel形式の場合`）、追加した `important` = **1121**。1109 < 1121 < 1220 |
| 5. 旧 L1220・旧 L1247 の2文が変更されていない | OK | `git diff --numstat` = `8 0`（挿入8・削除0）、`git diff -U0` の削除行数 **0**、ハンク2つはいずれも `@@ -357,0 +358,4 @@` / `@@ -1116,0 +1121,4 @@` の純粋な挿入。旧 L1220・旧 L1247 は現 L1224・現 L1251 に位置がずれただけで内容は同一 |
| 6. 段落内改行0件 / `:ref:` 未定義0件 | OK | 段落内改行の機械判定（リテラルブロック除外後）→ `段落内改行: 0`。ページ内ラベル参照は全て `.. _` 定義あり（未定義0件）。ページ外参照13種（`testdata_examples*` 10種・`master_data_tool`・`master_data_restore`・`gsp-maven-plugin`）はゲート7のフルビルドで `undefined label` 警告が出ていないことをもって解決を確認 |
| 7. Docker フルビルド（`-a`）が `build succeeded`、警告は既知の1件のみ | OK | `docker run --rm -v $PWD:/root/document nablarch-document-build-sandboxed /bin/bash -c "cd /root/document; sphinx-build -a -d _build/.doctrees/ja -b html ja _build/html"` → `build succeeded, 1 warning.`。警告は `ja/application_framework/application_framework/libraries/db_double_submit.rst:108: WARNING: undefined label: how_to_set_token_in_request_unit_test` のみ（既知・`#7` で記録済み）。新規警告0件 |
| 8. 手順3の確認結果（対象8セクション、該当の有無）を記録 | OK | 上記「手順3」の表に8セクション分を記録 |

生成HTMLでも2件の `important` のレンダリングを確認した（`_build/html/.../testdata_notation.html` に「重要」ボックスとして出力。ページ全体の `important` は 13件 → **15件**）。

## 禁止事項の遵守

| 禁止事項 | 遵守状況 |
|---|---|
| `mapping.csv` / `_batch/` / `vocabulary.md` / `glossary.md` / `design.md` / `style.md` を変更しない | 遵守（`git diff b7e97b3 -- .../mapping/` が空、他4ファイルも `git status` に現れない） |
| 旧 L1220・L1247 を書き換えない | 遵守（削除行0・純粋な挿入のみ） |
| 「どちらが正しい／どちらが仕様か」を書かない | 遵守（両形式の現在の挙動を並記するにとどめた） |
| 実装の内部構造（クラス名・メソッド名）を本文に書かない | 遵守（クラス名・メソッド名は本ファイルと `reviews/page-testdata_notation.md` にのみ記載） |
| 承認済み3件を再検討・取り消ししない | 遵守（`design.md` §4 の上書き・内容追加2件の例外・`style.md` S-07 の裁定はいずれも触れていない） |
| 手順3でページ外の挙動差を新規調査しない | 遵守（ページ内の既存記述どうしの突合のみ。実装ソースは読んでいない） |
| ラウンド1〜6のレビュー記録を書き換えない | 遵守（`reviews/page-testdata_notation.md` は末尾への追記のみ） |

## 申し送り

`reviews/page-testdata_notation.md` の `#10` 以降への申し送りに2件追加した。

- 申し送り14: レコード種別の形式差が仕様意図か YAML 経路の挙動乖離かは未確定（`nablarch/nablarch-testing` PR #75 と同型の可能性）。根拠の `file:line` を併記
- 申し送り15: テーブルデータの行内カラム値未記載時の形式差（空文字 / `null`）も同型の未確定事項

## 判断を仰ぐ事項

- **本文への追記が `important` 1件ではなく2件になった。** 作業指示の禁止事項は「本件は本文への追記1件のみである」と述べているが、手順3が「該当があれば同じ形で共通部に `important` を追加し」と定めているため、横並び確認で検出した1件（テーブルデータの空文字 / `null`）も追加した。追加が不要であれば、`テーブルのデータを記述する` 共通部の `important`（新 L358〜361）を落とせば作業指示どおりの1件のみになる
