# check: `#10` 差し戻し対応 — Excelのセル格子を表す表に識別子行を含める

作業指示: `.rn/20260724-ntf-yaml-support/ntf-doc-10-cellgrid.md`
対象コミット: `7da0b07` の続き
実施日: 2026-08-07

## 1. 対応の要旨

Excel のセル格子を表す表から識別子行が抜けており、表をそのまま写しても動くテストデータに
ならないという `must` 指摘に対応した。識別子を1列目に置き2列目以降を空セルにする方法で、
セル結合を使わずに識別子行を表の中に入れた。

- `style.md` S-10 規約2 を差し替え（「表の外に出す」→「表の中に含める」）。観点は11個で不変
- `testdata_examples.rst`（`#10`）のセル格子 **47件** を是正
- `testdata_notation.rst`（`#9`）のセル格子 **2件** を是正
- 識別子行のほか、ディレクティブ行・フレームワーク制御ヘッダ行・コメント行も表に含めた

## 2. ゲート結果

| # | ゲート | 結果 | 根拠 |
|---|---|---|---|
| 1 | `verify_mapping.py` が `exit 0`、594行 / 12,986 / 11,983 が不変 | **PASS** | `exit=0`。`Loaded 594 rows from mapping.csv` / `lines total (all rows): 12986` / `lines total (excluding DROP): 11983` |
| 2 | `mapping.csv` / `_batch/` / `vocabulary.md` / `glossary.md` の diff が空 | **PASS** | §3 に実行結果 |
| 3 | 見出し数不変（`#10` L2 9 / L3 28 / L4 56、`#9` `-` 3 / `~` 10 / `^` 26） | **PASS** | `#10` `{'=': 1, '-': 9, '~': 28, '^': 56}`、`#9` `{'=': 1, '-': 3, '~': 10, '^': 26}` |
| 4 | `:header-rows: 0` の表の1行目1列目が識別子 | **PASS** | 独立スクリプトで全表を走査し `PROBLEMS: 0`。ただし識別子の集合は指示の11種では足りない（§6-1） |
| 5 | 全49件（`#10`）＋34件（`#9`）の判定表 | **PASS** | §5 に全83件を1行ずつ記録 |
| 6 | `style.md` の観点が11個で不変、規約2 に「表の外に地の文で書く」が残っていない | **PASS** | `grep -c '^### S-'` = 11。規約2 本文に該当記述なし（`表の外` の残存は由来欄の履歴記述1件のみ） |
| 7 | `:ref:` の未定義参照が両ページとも0件 | **PASS** | ビルドログの `undefined label` は `db_double_submit.rst:108` の既知1件のみ |
| 8 | 段落内改行が0件 | **PASS** | 両ページとも `段落内改行: 0` |
| 9 | Docker フルビルド（`-a`）で `build succeeded`、警告は既知1件のみ、`Malformed table` 0件 | **PASS** | `build succeeded, 1 warning.`。警告は `db_double_submit.rst:108: WARNING: undefined label` のみ。`Malformed table` 0件 |
| 10 | 情報が失われていないことを `git diff` で示す | **PASS** | §4 |

## 3. ゲート2（禁止ファイルの不変）

```
$ git diff 7da0b07 HEAD -- .rn/20260724-ntf-yaml-support/mapping/mapping.csv \
    .rn/20260724-ntf-yaml-support/mapping/_batch/ \
    .rn/20260724-ntf-yaml-support/mapping/vocabulary.md \
    .rn/20260724-ntf-yaml-support/mapping/glossary.md
（出力なし）
```

## 4. ゲート10（情報の非欠落）

`git diff --stat`: 322 insertions / 93 deletions。削除93行の内訳は次のとおりで、
すべて STEP 2-2 の手順3（`:header-rows:` の変更）と手順4（地の文の重複削除）に由来する。

| 内訳 | 件数 |
|---|---|
| `testdata_examples.rst` の `:header-rows: 1` → `0` | 47 |
| `testdata_examples.rst` の地の文の書き換え（手順4） | 42 |
| `testdata_notation.rst` の `:header-rows: 1` → `0` | 2 |
| `testdata_notation.rst` の地の文の書き換え（手順4） | 2 |
| 合計 | 93 |

**表の値は1つも変更していない。** 表への変更は行の追加のみである。

### 識別子の追跡（機械検証）

是正前の地の文に現れた識別子と、是正後に表の1行目1列目に現れる識別子を突き合わせた。

- 旧・地の文の識別子（ユニーク）: 35
- 新・表の識別子（ユニーク）: 34
- **表に移らなかったもの: `SETUP_FIXED=input/empty.dat` の1件のみ。** これは「空ファイルを
  記述する」節の例で、レコード定義を持たないため表そのものが存在しない。地の文に残すのが正しく、
  規約4 にこの場合の扱いを明記した
- **表にあって旧・地の文になかったもの: 0件**（識別子を新たに創作していない）

是正後も地の文に識別子が残る箇所は3つで、いずれも「識別子の意味」を説明する文であり、
STEP 2-2 手順4 が残すべきとした種類のものである。

| 行 | 内容 | 残した理由 |
|---|---|---|
| L67 | `expectedLog` カラムが後続の「LIST_MAP=expectedLog」を指す | カラムとデータブロックの参照関係の説明（意味） |
| L137 | データブロックの用途は先頭行の識別子で決まる、の例示 | 識別子の値が何を指すかの説明（意味） |
| L1569 | 「SETUP_FIXED=input/empty.dat」…ディレクティブ行だけを記述する | 表を持たない記述例（上記） |

## 5. 全 `list-table` の判定（83件）

判定基準は `style.md` S-10 規約2 の定義による。**セル格子** = シートに実際に並ぶセルの配置を
そのまま写した表。**説明表** = 列見出しと説明を対にした表。

集計:

| ページ | セル格子 | 説明表 | 計 |
|---|---|---|---|
| `testdata_examples.rst`（`#10`） | 47 | 2 | 49 |
| `testdata_notation.rst`（`#9`） | 2（＋対象外3、§6-2） | 29 | 34 |


#### ja/development_tools/testing_framework/implementation/testdata_examples.rst
| # | 行 | 所属セクション | 判定 | 1行目1列目 | 理由 |
|---|---|---|---|---|---|
| T01 | L24 | データブロックとデータタイプ / 1つの読み込み単位にテストケース・準備データ・期待値をまとめる | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T02 | L44 | データブロックとデータタイプ / 1つの読み込み単位にテストケース・準備データ・期待値をまとめる | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T03 | L69 | データブロックとデータタイプ / 1つの読み込み単位にテストケース・準備データ・期待値をまとめる | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T04 | L82 | データブロックとデータタイプ / 1つの読み込み単位にテストケース・準備データ・期待値をまとめる | **セル格子** | `SETUP_TABLE=ORDER_HEADER` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T05 | L101 | データブロックとデータタイプ / 1つの読み込み単位にテストケース・準備データ・期待値をまとめる | **セル格子** | `EXPECTED_TABLE=ORDER_HEADER` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T06 | L124 | データブロックとデータタイプ / 1つの読み込み単位にテストケース・準備データ・期待値をまとめる | **セル格子** | `LIST_MAP=expectedLog` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T07 | L200 | グループIDによる使い分け / テストケースごとに準備データと期待値を使い分ける | **説明表** | `テストケース` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T08 | L220 | グループIDによる使い分け / テストケースごとに準備データと期待値を使い分ける | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T09 | L247 | グループIDによる使い分け / テストケースごとに準備データと期待値を使い分ける | **セル格子** | `SETUP_TABLE[case01]=ORDER_DETAIL` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T10 | L266 | グループIDによる使い分け / テストケースごとに準備データと期待値を使い分ける | **セル格子** | `EXPECTED_TABLE[case01]=ORDER_DETAIL` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T11 | L285 | グループIDによる使い分け / テストケースごとに準備データと期待値を使い分ける | **セル格子** | `SETUP_TABLE[case02]=ORDER_DETAIL` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T12 | L308 | グループIDによる使い分け / テストケースごとに準備データと期待値を使い分ける | **セル格子** | `EXPECTED_TABLE[case02]=ORDER_DETAIL` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T13 | L411 | テーブルのデータを記述する / 準備データ（SETUP_TABLE）を記述する | **セル格子** | `SETUP_TABLE=MEMBER` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T14 | L432 | テーブルのデータを記述する / 準備データ（SETUP_TABLE）を記述する | **セル格子** | `SETUP_TABLE=MEMBER` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T15 | L493 | テーブルのデータを記述する / 期待値（EXPECTED_TABLE・EXPECTED_COMPLETE_TABLE）を記述する | **セル格子** | `EXPECTED_TABLE=MEMBER` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T16 | L520 | テーブルのデータを記述する / 期待値（EXPECTED_TABLE・EXPECTED_COMPLETE_TABLE）を記述する | **セル格子** | `EXPECTED_COMPLETE_TABLE=ORDER_HEADER` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T17 | L580 | テーブルのデータを記述する / 採番処理のテストデータを記述する | **セル格子** | `SETUP_TABLE=TEST_SBN_TBL` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T18 | L593 | テーブルのデータを記述する / 採番処理のテストデータを記述する | **セル格子** | `EXPECTED_TABLE=TEST_SBN_TBL` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T19 | L606 | テーブルのデータを記述する / 採番処理のテストデータを記述する | **セル格子** | `EXPECTED_TABLE=USER_INFO` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T20 | L657 | LIST_MAPのデータを記述する / リクエストパラメータを記述する | **セル格子** | `LIST_MAP=requestParams` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T21 | L716 | LIST_MAPのデータを記述する / 期待ログを記述する | **セル格子** | `LIST_MAP=expectedLog` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T22 | L761 | テストケース一覧（testShots）を記述する / ウェブアプリケーションのテストケース一覧を記述する | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T23 | L792 | テストケース一覧（testShots）を記述する / ウェブアプリケーションのテストケース一覧を記述する | **セル格子** | `LIST_MAP=context001` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T24 | L808 | テストケース一覧（testShots）を記述する / ウェブアプリケーションのテストケース一覧を記述する | **セル格子** | `LIST_MAP=context002` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T25 | L862 | テストケース一覧（testShots）を記述する / Nablarchバッチアプリケーションのテストケース一覧を記述する | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T26 | L883 | テストケース一覧（testShots）を記述する / Nablarchバッチアプリケーションのテストケース一覧を記述する | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T27 | L936 | テストケース一覧（testShots）を記述する / メッセージングのテストケース一覧を記述する | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T28 | L953 | テストケース一覧（testShots）を記述する / メッセージングのテストケース一覧を記述する | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T29 | L999 | テストケース一覧（testShots）を記述する / エンティティバリデーションのテストケース一覧を記述する | **セル格子** | `LIST_MAP=testShots` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T30 | L1034 | ファイルのデータを記述する（L2導入部、L3配下ではない） | **説明表** | `Excel\ 形式の行` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T31 | L1057 | ファイルのデータを記述する / 固定長ファイルを記述する | **セル格子** | `SETUP_FIXED=work/input.txt` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T32 | L1088 | ファイルのデータを記述する / 固定長ファイルを記述する | **セル格子** | `EXPECTED_FIXED=work/output.txt` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T33 | L1157 | ファイルのデータを記述する / 可変長ファイルを記述する | **セル格子** | `SETUP_VARIABLE=input/data.csv` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T34 | L1219 | ファイルのデータを記述する / 複数レコードレイアウトを記述する | **セル格子** | `SETUP_FIXED=input/multi.dat` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T35 | L1293 | ファイルのデータを記述する / 固定長ファイルのディレクティブを指定する | **セル格子** | `SETUP_FIXED=input/data.dat` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T36 | L1360 | ファイルのデータを記述する / 可変長ファイルのディレクティブを指定する | **セル格子** | `SETUP_VARIABLE=input/data.tsv` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T37 | L1419 | ファイルのデータを記述する / カンマを含む値を記述する | **セル格子** | `EXPECTED_VARIABLE=output/data.csv` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T38 | L1477 | ファイルのデータを記述する / グループIDでファイルを使い分ける | **セル格子** | `SETUP_FIXED=work/input.txt` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T39 | L1504 | ファイルのデータを記述する / グループIDでファイルを使い分ける | **セル格子** | `SETUP_FIXED[case2]=work/input.txt` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T40 | L1598 | メッセージングのデータを記述する / 要求電文・応答電文を記述する | **セル格子** | `MESSAGE=setUpMessages` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T41 | L1641 | メッセージングのデータを記述する / 要求電文・応答電文を記述する | **セル格子** | `MESSAGE=expectedMessages` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T42 | L1715 | メッセージングのデータを記述する / 同期応答メッセージ送信の要求電文の期待値を記述する | **セル格子** | `EXPECTED_REQUEST_HEADER_MESSAGES[case1]=RM21AA0104_01` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T43 | L1762 | メッセージングのデータを記述する / 同期応答メッセージ送信の応答電文を配置する | **セル格子** | `RESPONSE_BODY_MESSAGES=REQ001` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T44 | L1817 | メッセージングのデータを記述する / ステータスコードを省略する | **セル格子** | `RESPONSE_BODY_MESSAGES=REQ001` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T45 | L1865 | 値を特殊記法で記述する / 日付・システム日時・NULLを記述する | **セル格子** | `EXPECTED_TABLE=SCHEDULE` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T46 | L1928 | 値を特殊記法で記述する / スペース・ダブルクォートを記述する | **セル格子** | `EXPECTED_TABLE=ITEM` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T47 | L1965 | 値を特殊記法で記述する / バイナリデータを記述する | **セル格子** | `SETUP_TABLE=FILE_TABLE` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T48 | L2008 | コメント・マーカーカラム・空エントリを扱う / コメントとマーカーカラムを記述する | **セル格子** | `SETUP_TABLE=TEST_TABLE` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T49 | L2072 | コメント・マーカーカラム・空エントリを扱う / 空エントリを記述する | **セル格子** | `SETUP_TABLE=USER` | シートに実在するセル配置の写し。1行目に識別子行を持つ |

#### ja/development_tools/testing_framework/implementation/testdata_notation.rst
| # | 行 | 所属セクション | 判定 | 1行目1列目 | 理由 |
|---|---|---|---|---|---|
| T01 | L55 | テストデータの構造 / テストクラスとテストデータの対応 | **説明表** | `ファイルの種類` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T02 | L100 | テストデータの構造 / データブロックとデータタイプ | **説明表** | `用途` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T03 | L142 | テストデータの構造 / データブロックとデータタイプ | **説明表** | `データタイプ` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T04 | L208 | テストデータの構造 / データブロックとデータタイプ | **説明表** | `データタイプ` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T05 | L258 | テストデータの構造 / グループIDによる使い分け | **説明表** | `方式` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T06 | L406 | 使用方法 / テーブルのデータを記述する | **説明表** | `カラム型` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T07 | L437 | 使用方法 / テーブルのデータを記述する | **セル格子** | `SETUP_TABLE=テーブル名` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T08 | L453 | 使用方法 / テーブルのデータを記述する | **説明表** | `値の指定` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T09 | L468 | 使用方法 / テーブルのデータを記述する | **説明表** | `ID` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T10 | L500 | 使用方法 / テーブルのデータを記述する | **説明表** | `値の指定` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T11 | L530 | 使用方法 / LIST_MAPのデータを記述する | **セル格子（対象外）** | `empNo` | シートのセル（カラム名行＋データ行）の写しだが識別子行を持たない。`List<Map>` との等価性を示すための断片であり、作業指示が `#9` の対象を L435・L972 の2件に限定しているため変更しない（要判断） |
| T12 | L597 | 使用方法 / テストケース一覧（testShots）を記述する | **説明表** | `処理方式` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T13 | L614 | 使用方法 / テストケース一覧（testShots）を記述する | **説明表** | `カラム名` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T14 | L631 | 使用方法 / テストケース一覧（testShots）を記述する | **説明表** | `カラム名` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T15 | L696 | 使用方法 / テストケース一覧（testShots）を記述する | **説明表** | `カラム名` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T16 | L749 | 使用方法 / テストケース一覧（testShots）を記述する | **説明表** | `カラム名` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T17 | L796 | 使用方法 / テストケース一覧（testShots）を記述する | **説明表** | `カラム名` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T18 | L827 | 使用方法 / ファイルのデータを記述する | **説明表** | `用語` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T19 | L875 | 使用方法 / ファイルのデータを記述する | **説明表** | `ディレクティブキー` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T20 | L902 | 使用方法 / ファイルのデータを記述する | **説明表** | `ディレクティブキー` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T21 | L929 | 使用方法 / ファイルのデータを記述する | **説明表** | `型名称` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T22 | L977 | 使用方法 / ファイルのデータを記述する | **セル格子** | `SETUP_FIXED[グループID]=ファイルパス` | シートに実在するセル配置の写し。1行目に識別子行を持つ |
| T23 | L1010 | 使用方法 / ファイルのデータを記述する | **説明表** | `名称` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T24 | L1049 | 使用方法 / ファイルのデータを記述する | **説明表** | `レコード種別` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T25 | L1141 | 使用方法 / メッセージングのデータを記述する | **説明表** | `データタイプ` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T26 | L1179 | 使用方法 / メッセージングのデータを記述する | **説明表** | ```file-type``\ の値` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T27 | L1196 | 使用方法 / メッセージングのデータを記述する | **説明表** | `設定値` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T28 | L1281 | 使用方法 / 値を特殊記法で記述する | **説明表** | `インタープリタ` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T29 | L1306 | 使用方法 / 値を特殊記法で記述する | **説明表** | `使用できる文字種` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T30 | L1347 | 使用方法 / 値を特殊記法で記述する | **説明表** | `値の種類` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T31 | L1399 | 使用方法 / 値を特殊記法で記述する | **説明表** | `値の種類` | 列見出しと説明を対にした説明表。シートのセル配置ではない |
| T32 | L1453 | 使用方法 / コメント・マーカーカラム・空エントリを扱う | **セル格子（対象外）** | `NO` | シートのセルの写しだが識別子行を持たない。`//` によるコメント切り捨てを示すための断片。同上の理由で変更しない（要判断） |
| T33 | L1484 | 使用方法 / コメント・マーカーカラム・空エントリを扱う | **セル格子（対象外）** | `[no]` | シートのセルの写しだが識別子行を持たない。マーカーカラムの除外を示すための断片。同上の理由で変更しない（要判断） |
| T34 | L1504 | 使用方法 / コメント・マーカーカラム・空エントリを扱う | **説明表** | `対象` | 列見出しと説明を対にした説明表。シートのセル配置ではない |

## 6. 判断を仰ぐ事項・作業指示との差分

### 6-1. ゲート4 の識別子リストが実態より狭い（報告のみ、対応済み）

作業指示のゲート4 は識別子を11種（`SETUP_TABLE` / `EXPECTED_TABLE` /
`EXPECTED_COMPLETE_TABLE` / `LIST_MAP` / `SETUP_FIXED` / `SETUP_VARIABLE` /
`EXPECTED_FIXED` / `EXPECTED_VARIABLE` / `MESSAGE` / `SETUP_DB` / `EXPECTED_LOG`）
と定めているが、実際のページはこれと一致しない。

- **リストに無いが実在する識別子が2種ある**: `EXPECTED_REQUEST_HEADER_MESSAGES`（T42）・
  `RESPONSE_BODY_MESSAGES`（T43・T44）。いずれも同期応答メッセージ送信のデータタイプであり、
  `testdata_notation.rst` に仕様の記載がある。**これらも識別子として表に入れた**
- **リストにあるが `#10` に出現しない識別子が2種ある**: `SETUP_DB`・`EXPECTED_LOG`。
  期待ログは `LIST_MAP=expectedLog` として記述されるため `EXPECTED_LOG` は使わない

ゲート4 の趣旨（1行目1列目が識別子であること）は全49件で満たしている。

### 6-2. `#9` に、対象外とされた3件のセル格子がある（**要判断**）

作業指示は `#9` の対象を L435 付近・L972 付近の2件に限定し、禁止事項で
「`#9` の他の記述を変更しない」と定めている。指示どおり2件のみ是正した。

ただし全34件を独立に判定した結果、**新しい規約2 の定義に照らすとセル格子に当たる表が
ほかに3件ある**（いずれも識別子行を持たない）。

| 表 | 行 | 内容 | 用途 |
|---|---|---|---|
| T11 | L530 | `empNo` / `expected` ＋データ2行 | `List<Map>` との等価性を示す |
| T32 | L1453 | `NO` / `FIRST_NAME` … ＋ `//` コメント行 | `//` によるコメント切り捨てを示す |
| T33 | L1484 | `[no]` / `id` / `name` ＋データ2行 | マーカーカラムの除外を示す |

3件とも「ある規則を示すための断片」であり、データブロック全体のレイアウトを示す表ではない
（読者がそのまま写して使うことを意図していない）。この点で L435・L972 の2件とは性格が異なる。
一方、`#9` の L435 の表も「1行目に識別子、2行目にカラム名」という規則の説明図であり、
線引きは自明ではない。

**指示の禁止事項に従い変更していない。** 是正するか現状のままとするか、判断を仰ぐ。

### 6-3. `style.md` 規約4 の参照を整合させた（**要判断**）

旧・規約4 は「**規約2で表から出した**識別子行は…」と書き出しており、規約2 の差し替えで
前提が失われる。禁止事項は「規約2 以外の規約を変更しない（S-03 例外1 の整合修正を除く）」と
定めているが、参照が宙に浮いたまま残すと規約として読めなくなるため、**規約の内容
（コード書式を使わず普通の文字で書く）は変えずに、参照だけを実態に合わせた。**
あわせて、表に入る場合は囲みを付けない／表を持たない記述例では従来どおり「」で囲んでよい、
という使い分けを明記した。

### 6-4. S-03 例外1 は変更していない（**報告のみ**）

作業指示は「S-03 例外1 に `#10` で追記した記載例ページの但し書きを、規約2 がページを問わない
形になるため整合するよう見直すこと（記載例ページ限定の例外である旨が残っていれば削る）」と
している。実際に S-03 例外1（L122-130）を確認したところ、`#10` の追記は
**L4見出し対（`Excel形式の場合` / `YAML形式の場合`）を全L3に置くかどうか**についての記述であり、
識別子行や規約2 とは無関係である。また文面は「『テストデータの記載例』のように、全L3が同一の型を
持つページでは」という一般的な条件で書かれており、記載例ページ限定の例外にはなっていない。

`style.md` 全体を `識別子` / `記載例` で grep しても、識別子行に触れる規約は規約2・規約4 の
2箇所だけであった。**したがって削るべき記述が無いため、変更していない。**

### 6-5. 「前半・後半」の記述を1件だけ改めた（**報告のみ**）

`#10` L22 の地の文は「カラム数が多いため、ここでは前半・後半に分けて示す」と書かれていたが、
当該箇所の `LIST_MAP=testShots` は実際には**3つの表**（T01・T02・T03）に分割されている。
識別子行を3つとも付けたことで分割数が表からも見えるようになるため、
「ここでは**複数に**分けて示す」に改めた。他の3箇所（L409・L860・L934）は実際に2分割であり、
「前半・後半」のままとした。

### 6-6. 空エントリの説明から行番号を外した（**報告のみ**）

`#10` の「空エントリを記述する」の地の文は「**3行目**は全セルが空であるため読み飛ばされ」と
書かれていた。識別子行を1行目に追加したことで空行は4行目に移るため、行番号のままでは誤りになる。
「**全セルが空の行は**読み飛ばされるため」に改め、行番号への依存を外した。

### 6-7. 既存の smartquotes によるダブルクォートの表示（**要判断・本作業の対象外**）

`#10` はダブルクォートを含むセルを持つ（T37 の `quoting-delimiter` の値、T46 の `" "` ・ `"""`）。
`conf.py` に `smartquotes` の設定が無く既定で有効なため、これらは HTML 上で
**曲がったクォート（`&#8220;` / `&#8221;`）として描画される。** T46 の2件は `#10` 作成時から
存在する既存の挙動であり、本作業で追加した T37 のディレクティブ値も同じ描画になる。

「そのまま写せる」ことを目的とするページで、直線のダブルクォート（U+0022）が必要な箇所が
曲がって表示されるのは実害がありうる。ただし禁止事項が表の値の変更を禁じており、
また既存セルにも同じ問題が及ぶためページ全体の方針判断が要る。**本作業では手を入れていない。**

## 7. STEP 4 の結果

### 7-1. 他ページの全数確認

`ja/development_tools/testing_framework/` 配下の `.rst` 全11本を確認した。

| ファイル | `list-table` | 判定 |
|---|---|---|
| `about/index.rst` | 1 | **対象外**。「テストの種類／実行方法／テスト範囲／備考」の説明表。simple table 3件も確認したが、いずれも「処理方式／内容」等の説明表 |
| `implementation/testdata_examples.rst` | 49 | 是正済み（47件） |
| `implementation/testdata_notation.rst` | 34 | 是正済み（2件）。§6-2 の3件は要判断 |
| `index.rst`・`implementation/index.rst`・`setup/index.rst`・`tools/index.rst` | 0 | toctree のみ |
| `setup/junit5_extension.rst`・`setup/master_data_restore.rst`・`tools/master_data_tool.rst`・`tools/testdata_converter.rst` | 0 | 見出しのみのスタブ |

**Excel のセル格子を表す表は、上記2ページ以外に存在しない。**

### 7-2. 残りページへの申し送り

`reviews/page-testdata_examples.md` の申し送りに2件を追加した。

## 8. 写して動くことの確認（STEP 2-3）

是正後の47件すべてについて、識別子行・構造行・データ行が揃っており、地の文を読まないと
補えない行が残っていないことを確認した。構造行を持つ表の内訳は次のとおり。

| 追加した構造行 | 対象 | 件数 |
|---|---|---|
| 識別子行のみ | 上記以外のセル格子 | 40 |
| 識別子行＋ディレクティブ行1行 | T33（`field-separator`）・T42（`text-encoding`） | 2 |
| 識別子行＋ディレクティブ行2行 | T35（`text-encoding`・`positive-zone-sign-nibble`）・T36（`field-separator`・`record-separator`）・T37（`field-separator`・`quoting-delimiter`） | 3 |
| 識別子行＋ディレクティブ行1行＋フレームワーク制御ヘッダ行2行 | T40（`text-encoding` ＋ `requestId`・`userId`） | 1 |
| 識別子行＋コメント行 | T48（`// この行はコメントです`） | 1 |

「名前・値」形式の行（ディレクティブ行・フレームワーク制御ヘッダ行）は、1列目に名前、
2列目に値、3列目以降を空セルとした。これは `testdata_notation.rst` の
「ディレクティブ名のセルの右のセルに設定値を記載する」（L1010 付近）および
「フレームワーク制御ヘッダは…`| フィールド名 | 値 |`（ディレクティブと同じ「名前・値」形式）で
記載する」（L1221 付近）に一致する。

T48 は、地の文が「カラム名の行の前に『// この行はコメントです』だけを記述した行を置き」と
説明しつつ「（表にはカラム名の行以降を示す）」と断っていたため、**コメント行を表に入れ、
その断り書きを削除した。** これにより表だけで完結する。

### 分割表の扱い

カラム数が多く前半・後半（T01 は3分割）に分けている表では、**分割したすべての表に識別子行を
付けた。** いずれの分割も1列目（`no` / `MEMBER_ID` などのキー列）を繰り返す形になっており、
シート上では識別子行の1列目がその位置に当たるため、各表の1行目1列目に識別子を置くのが実態に
一致する。対象は T01・T02・T03、T13・T14、T25・T26、T27・T28。

## 9. HTML 出力の確認

`_build/html/.../testdata_examples.html` を確認し、次を実測した。

- 識別子行が `<thead>` ではなく `<tbody>` の最初の `<tr>` として出力されている
- 空セルが `<td>&nbsp;</td>` として出力され、列がずれていない
- ディレクティブ行が「名前 / 値 / 空 / 空」として出力されている（T36・T40 で確認）
- コメント行が表内に出力されている（T48 で確認）
- `\\t` が `\t` として描画されている（T36）

`_build/` はユーザーがブラウザで直接レビューするため削除していない。
Docker ビルドが再生成した `locales/ja/LC_MESSAGES/sphinx.mo` は `git checkout` で戻した。
