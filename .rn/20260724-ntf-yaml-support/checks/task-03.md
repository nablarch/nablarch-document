# task-03 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 用語集に、現行解説書とinput資料に登場する主要な用語が網羅されている | OK | `mapping/glossary.md`（285→516行）に11カテゴリ・正表記99語・表記186件を掲載（`load_terms("tools/term_candidates.tsv")` で確認）。**掲載基準を「discover が揺れを検出したか」から切り離し**、掲載基準1（骨格語）を筆頭に置き直した（glossary.md §3）。今回追加した主な語群: バリデーション系4語（`バリデーション`/`相関バリデーション`/`単項目バリデーション`/`ドメインバリデーション`）、骨格語4語（`テストクラス`/`テストメソッド`/`テストケース`/`ハンドラキュー`）、YAML側の単位語8語（`読み込み単位`/`YAMLファイル`/`トップレベルキー`/`エントリ`/`エントリ値`/`セル`/`Excel形式`/`YAML形式`）、`リクエストID`、`稼動環境`、`必須カラム`/`オプションカラム`/`記述例`/`制約`/`補足`、`データベースを使用するクラスのテスト`、`テーブルデータ`/`ファイルデータ`。網羅性の裏付けとして `discover` を4ルール実行（punct 3・paren 2・longvowel 6・**spacing 60** グループ）。`spacing` は本タスクで追加した規則で、散文・表セルにしか現れない `グループ ID`・`リクエスト ID`・`HTML ダンプ`・`FW 制御ヘッダ` 等を初めて機械検出した。検出器の限界（何を人手で見つけたか）は §4「検出できるもの／検出できないもの」に明記 |  |  |
| 各用語の「揺れ表記」に file:line の根拠がある | OK | `python3 mapping/tools/verify_glossary.py` → `refs 検証 264 件 / 不一致 0 件`。ファイル存在・行番号範囲に加え、**参照先の行に「直前で名指しした表記または引用」が実在すること**まで検査する（`NTF:`/`NTF-root:` は基準コミット `c2419060` の内容、`FW:`/`S:` は作業ツリー）。出現数と全 file:line は `mapping/scan-terms.tsv`（748行）にコミットし、§5 の表は代表1件＋件数に絞った（最長セル 375→326字） |  |  |
| 処理方式の名称が design.md の正式名称と一致している | OK | glossary.md §5.2 の7名称は `design.md:166`〜`:172` と文字列一致。`verify_glossary.py` の `terms` 検査（183件・不一致0）が `term_candidates.tsv` の正表記と §5「正表記」列の一致を機械照合する。§5.2 前書きの「編」の記述は実測に合わせて訂正（B-1）。処理方式ではない `常駐バッチ`/`都度起動バッチ` は §5.3「バッチの起動方式」に分離（C-3） |  |  |
| FW解説書と異なる表記を採用した場合、理由が採用根拠に記載されている | OK | 該当3件、いずれも採用根拠に**太字で**明記。(1) `スーパクラス` — FW用例1件のみで同文書群の他のカタカナ語は長音を省く（`インタフェース` FW75/`ユーザ` FW175/`サーバ` FW122）。(2) `前提事項` — FW解説書は `前提事項`・`前提条件` のいずれもセクションタイトルとしては0件で先例がない。(3) `単項目バリデーション` — FWに確立表記がなく（`単項目バリデーション` はコード内コメント1件、散文「項目単体のバリデーション」2件のみ）、`相関バリデーション`（FW34件）と対になる複合語を採用優先順位3で確定。§2 にも該当3件を明記 |  |  |
| （ラウンド2再確認）F-1〜F-10（対象コミット `277e23a` の4観点レビュー、コーディネーターが実測で裏付け確認済み・全件Valid）に全件対応 | OK | 詳細は下記「ラウンド2（F-1〜F-10）対応一覧」を参照。`モックアップクラス`（現行21件・4ファイル）・`複数レコードレイアウト`（input5件・2ファイル、出典不明の造語「マルチレイアウト」を置換）・`エンティティバリデーション`（input5件・2ファイル）を新規掲載。`NTF:06_TestFWGuide/03_Tips.rst` の誤引用2件（`:19`→`:786`、`セル`の代表引用を `02_entityUnitTestWithNablarchValidation.rst:60`＝「エクセルシート」の部分文字列から `batch.rst:224`＝実際の単独用例に差替え）を修正。`design.md` のYAML形式の採用根拠を実際の出典（`:52`本文使用）に基づく記述に修正。§5.5 の `単体テスト` 脚注の過度な一般化（8件すべてを「3種を束ねた総称」とする記述）を撤回し8件それぞれの性格を書き分け。`メッセージ同期送信処理`/`メッセージ同期送信`/`HTTPメッセージ同期送信処理`（語順違いの表記揺れ）を `term_candidates.tsv` に登録し §5.4・§8 に反映。250字超のセル4件（`HTTPメッセージ送信`採用根拠 332→232字、`テスティングフレームワーク`採用根拠 240→232字、`グループID`別義 280→248字、`使用方法`採用根拠 278→215字）を短縮。§4に部分文字列衝突（4つ目の限界事項）を追記。§8の「現行NTF解説書」表記を「現行解説書」に統一 |  |  |
| （#3差し戻し）`term-candidates.csv` の全候補が `glossary.md` に採用/不採用（理由付き）のいずれかで対応しており、未判定が0件である | OK | `mapping/tools/extract_terms.py`（新規）が現行解説書見出し（`sections-current.csv` の `heading_path` 分解）・`input/ntf-doc-terms.md` 見出し・`design.md` 見出しと処理方式名の4出典から母集団を機械抽出し `mapping/term-candidates.csv`（339行・331種類、`csv.DictReader` で確認）を出力。`glossary.md` に新設した §5.15（5.15.1〜5.15.5）が全331種類を採用（36件、§5への相互参照付き）または不採用（295件、候補ごとの具体的理由付き）に判定。`verify_glossary.py` の新設 `population` 検査が `term-candidates.csv` の全行がこの節に過不足なく対応すること（未判定0件）を機械検証（`検証 331 件 / 不一致 0 件`） |  |  |
| （#3差し戻し）`design.md` の章・セクション名がすべて `glossary.md` に存在する | OK | `design.md` の `##`/`###` 見出し（H1文書題を除く、H2=10・H3=12の21種類）を §5.15.2 に全行掲載（`design.md` 所在の file:line 付き）。処理方式名7件は §5.2 で既に一致済み（§5.15.1 に集約）。`verify_glossary.py` の新設 `design_sections` 検査が全21件の存在を機械検証（`検証 21 件 / 不一致 0 件`） |  |  |
| （#3差し戻し）処理方式の名称が `design.md` の正式名称と一致していることが機械検証されている | OK | `verify_glossary.py` の新設 `scheme_names` 検査が `design.md`「5. 処理方式の名称」表の名称列7件（`extract_design_schemes` で抽出）すべてが §5.2 の「正表記」列に文字列一致することを検証（`検証 7 件 / 不一致 0 件`）。従来の `terms` 検査（tsvとの一致）に加え、`design.md` を直接の正とする検証を独立に追加した |  |  |
| （#3差し戻し）母集合の全件判定が `verify_glossary.py` によって機械検証されている | OK | 既存5検査（refs/counts/sections/terms/applies）に加えて `population`/`design_sections`/`scheme_names`/`reasons` の4検査を追加し計9検査に拡張。`reasons` は §5.15 の不採用テーブル全行（143件）に空でない理由が書かれていることを検証。9検査すべて `python3 mapping/tools/verify_glossary.py` で `不一致 0 件`・`RESULT: OK`（終了コード0） |  |  |
| （#3差し戻し）G-4〜G-8個別誤りの修正 | OK | 詳細は下記「#3差し戻し 対応一覧」を参照。G-7（150字超セル41個）は全件実測・特定し150字以内に短縮（現在0件）。G-6（件数の誤り）・G-4（採用根拠の引用不一致）は `detect_term_variants.py scan` 再実行と全採用根拠（124件）の再読み合わせにより再検証し、既存記述に不一致は無かった（G-7の短縮作業中に自己混入させた不一致2件は同作業内で復元済み）。G-8（分類の誤り）は §9 の判定語（採用/表記変更/範囲限定/一部不採用/不採用の5分類）を全15行で定義と照合し、L398行の「一部不採用」（不採用要素が実際には無い）を「表記変更」に訂正 |  |  |

### Method（writing）の適用記録

#### レビュー指摘への対応一覧

| # | 対応 | 内容 |
|---|---|---|
| A-1 | 対応 | 実測（`精査` 現行47・`単項目精査` 38・`項目間精査` 7 / FW `バリデーション` 316・`相関バリデーション` 34・`単項目バリデーション` 1・`ドメインバリデーション` 28）のうえ §5.6 を新設。`精査`→`バリデーション`、`項目間精査`→`相関バリデーション`、`単項目精査`→`単項目バリデーション`。§8 に3行追加 |
| A-2 | 対応 | §5.7「テストの骨格」を新設し `テストクラス`(現行138)・`テストメソッド`(70)・`テストケース`(155) を、§5.1 に `ハンドラキュー`(現行1/FW84) を掲載。§3 の掲載基準の順序を「骨格語→セクションタイトル→検出された揺れ」に入れ替え、揺れの有無を掲載条件から外した |
| A-3 | 対応 | §5.9 を新設。`S:input/ntf-testdata-doc.md:32`・`:53` に基づく**粒度をそろえた4段の対応表**（テストクラス分全体／読み込み単位／データブロック／値1個）を置き、「セルの対応物はデータブロックではない」と明示。`読み込み単位`・`YAMLファイル`・`トップレベルキー`・`エントリ`・`エントリ値`・`セル`・`Excel形式`・`YAML形式` を立項 |
| A-4 | 対応 | `テストショット`(現行10)→`テストケース`、`テストショット表`(2)→`テストケース一覧`、`リクエスト ID`(input13)→`リクエストID`(現行47/FW120)、`Form単体テスト`(5)/`Entity単体テスト`(5)/`Component単体テスト`(2)/`Action単体テスト`(1) を §5.5 と §8 に追加。`稼動環境` はFWのページ題（`FW:nablarch/platform.rst:3`）を採り、FWの「稼働」5件がすべて動詞・別複合語で競合しないことを根拠に明記。input反復見出しは `必須カラム`/`オプションカラム`/`記述例` を採用、`制約・補足` はFWが見出しとして分けて使う（`制約` 51見出し・`補足` 8見出し）ため `制約`+`補足` に分割 |
| B-1 | 対応 | 「編」を持つ見出しは6件（`FW:batch/index.rst:3`・`messaging/index.rst:3`・`web/index.rst:3`・`web_service/index.rst:3`・`web_service/rest/index.rst:3`・`web_service/http_messaging/index.rst:3`）と実測し、§5.2 前書きを「カテゴリ章とウェブサービスの下位章に付く」に訂正 |
| B-2 | 対応 | FW `グループID` 22件の内訳を「19件Maven／3件は認可機能（`FW:libraries/authorization/permission_check.rst:92`・`:109`・`:131`）」に訂正 |
| B-3 | 対応 | 「いずれのコマンドも辞書順に整列する」を撤回。glossary.md §1 と `detect_term_variants.py` の docstring を「discover は正規化キー・表記の辞書順、scan は用語定義ファイルの記載順」に修正（テスト `test_row_order_follows_terms_file_order` と一致） |
| B-4 | 対応 | 現行解説書の `NTF` 2件（`http_real.rst:153`・`http_send_sync.rst:49`）を件数に算入。「解説書本文では使わない」という限定は §1 に格上げし、**適用対象は解説書のページ本文のみ・作業文書は対象外**と明言。§8 の `NTF` 行にも適用条件として明記 |
| B-5 | 対応 | 「FW解説書は付けない」を「見出しはすべて『処理』なし。ただし本文に `応答不要メッセージ送信処理` がFW1件（`FW:libraries/system_messaging/mom_system_messaging.rst:179`）ある」に修正 |
| B-6 | 対応 | `使用方法` を「FW63件だが独立見出しとしては36件（うち35件が libraries 配下）、残り27件は本文中の平文とライブラリ外の見出し」に訂正。反例も明示（`FW:libraries/data_io/data_format.rst:483`・`FW:web/getting_started/index.rst:12`・`FW:handlers/web/SessionStoreHandler.rst:187`） |
| B-7 | 対応 | `JUnit5`（`NTF:06_TestFWGuide/index.rst:20`）を揺れ表記から撤回し、「別義・旧名称」列に移して toctree のファイル名の一部であると明記。§8 の行にも「散文に限る」を付けた |
| B-8 | 対応 | `単体テスト` を `クラス単体テスト` の揺れ表記から**撤回**。`単体テスト` を含むより長い13表記を tsv に登録して数え直し、単独用法は現行8件（生201件）と実測。8件も3種を束ねた総称であることを注記し、「別義・旧名称」列に移した。§8 には「原則として置き換えない」を適用条件として明記 |
| B-9 | 対応 | 生の `データシート` 14件を「単独7・`テストデータシート` の一部6・`準備データシート` の一部1」に分解（両語を tsv に登録して機械的に分離）。単独7件のうち2件（`01_Abstract.rst:70`・`:77`）がRSTコメント行であることを注記 |
| B-10 | 対応 | `事前準備データ` の input 2件（`:48`・`:50`）を明記／`DBアクセステスト` は全コーパス0件で `DB アクセステスト` が input1件と訂正し、正表記 `データベースを使用するクラスのテスト` を与えた／行の名称の分布を実測（`フィールド名称行` 5ファイル・`データ型行` 4・`フィールド長行` 4・`レコード種別行` 1）し、未定義語 `ファイルデータ` を §5.8 で定義／`レコード種別行` の根拠が図中ラベルで弱いことを明示し §10 未解決事項3 に登録／`パーサ` は掲載理由（固有名詞ではなくカタカナ語の長音揺れであり §5.14 の他項と同じ規則の対象）を明記して掲載継続／`概要` の対抗数字（FW42件、独立見出し7件・libraries 配下0件）を併記／`バッチアプリケーション` を §5.2「別義」列と §8 に追加 |
| C-1 | 対応 | FW解説書に `HTTPメッセージ送信`(FW5)・`HTTPメッセージ受信`(FW4) が実在することを確認し、正表記を `HTTP同期応答メッセージ送信／受信` から**変更**。見出し（`http_system_messaging.rst:132`・`:94`）と直後の本文（`:134`）を引用。旧未解決事項#4 は削除 |
| C-2 | 対応 | §6（括弧）・§7（空白）の規範的決定を削除し、§6 を「記述様式は #4 `style.md` の管轄」に改めて実測データのみを申し送り。FWライブラリ見出しの実測（半角26／全角5、具体例つき）、現行の実測（半角12／全角21＋混在1）、`spacing` 60グループ、送り仮名の実測表（`出来る`4/`できる`207 ほか5組）を保存。§11.2 にも申し送りとして再掲。`style.md` は作成していない |
| C-3 | 対応 | §5.2「処理方式」（design.md と一致する7行）と §5.3「バッチの起動方式」に分割 |
| C-4 | 対応 | §8 に**適用条件列**を新設し全87行に条件を記載。指摘された危険行はすべて条件付き（`概要`→セクションタイトルのみ／`データシート`→単独のみ＋`テストデータシート`・`準備データシート` を別行／`groupId`→Maven・参照ラベル・YAMLキーを除外／`本フレームワーク`→FW引用箇所は除外／`実施方法`→ページ内セクションのみ／`メッセージング処理`→MOM確定時のみ）。§5 の揺れ表記が §8 に全件あることを `verify_glossary.py` の `sections` 検査（77件・不一致0）で機械検査 |
| C-5 | 対応 | `データブロック` を「読み込み単位の中に置く1用途分のデータのまとまり」と他語に依存せず定義し、`データタイプ`（用途を表す予約語）・`グループID`（同種を区別する標識）をその属性として再定義。循環していた理由を §5.8 末尾の注記で明示。旧未解決事項#1 は §11.1-3 の申し送りへ移動 |
| C-6 | 対応 | §5.13 を「アウトラインの**枠**の名称」と定義し直し、「〜する」形式は枠の**中に置く下位見出し**に適用すると主語で書き分けた |
| C-7 | 対応 | 「揺れなし」（同義の別表記がない）と「未出現」（全コーパスに0件）の2語に統一。§5 前書きで定義 |
| C-8 | 対応 | `内蔵サーバ`・`HTMLダンプ` はウェブアプリケーション向けに限定（根拠 `02_RequestUnitTest.rst:11`）／`テストデータ` を「準備データと期待値とテストケース一覧の総称」に拡張し、係り受けを「テストクラスとは別のファイルに記述する」に修正／`テスティングフレームワーク` を「機能を提供するフレームワーク」と定義し直し、「本」の指示対象の衝突を「本解説書が説明する対象」に整理／`エンティティ単体テスト`・`コンポーネント単体テスト` の意味欄に対象クラスを明示し §8 に略称8行を追加 |
| C-9 | 対応 | コンポーネント（Javaクラス／システムリポジトリの登録単位）、データ型行とデータタイプ、セクション（テストデータの単位／文書構造）の3組に「別義」注記を追加。セクションは `section_id`・`heading_path` に言及して #5 との衝突を明示 |
| C-10 | 対応 | §5 の表を5列にし、「揺れ表記（使わない）」と「別義・旧名称（文脈により使う）」を分離。`Webアプリケーション`・`MOMメッセージング`・`バッチアプリケーション`・`本フレームワーク`・`groupId`・`単体テスト`・`JUnit5` を後者へ、`JSR352に準拠したバッチアプリケーション` は旧名称として後者へ |
| C-11 | 対応 | §11.1-1 に受け皿のないページ5件（`HTMLチェックツール`・`mail.rst`・`fileupload.rst`・`double_transmission.rst`・`02_DbAccessTest.rst`）を #6 への申し送りとして明記。`design.md` にこれらが一度も現れないことを確認済み |
| C-12 | 対応 | 旧未解決事項#3 の design.md 誤読を訂正。「未確定事項1は第2部・2は取引単体テスト。第3部のMOMページは `design.md` §4 が確定として記述している」と書き直し、§11.1-2 の #6 論点に移した |
| D-1 | 対応 | `mapping/tools/verify_glossary.py` を新規作成（268行）。refs / counts / sections / terms / applies の5検査。不一致で終了コード1。作成後、glossary.md の修正中に反復実行し、検出された不一致（初回 refs 31・counts 7・sections 8・terms 12・applies 1）をすべて解消。検証器自体のテスト `test_verify_glossary.py`（25件）も追加し、壊した用語集を素通ししないことを確認 |
| D-2 | 対応 | `DEFAULT_BASE_COMMIT = "c24190607fef5d76c607aa08b36d2ab2f813efe5"` を定数として固定（環境変数 `NTF_BASE_COMMIT` で上書き可）。出力の先頭に `#` コメントで生成コマンド・コーパス・基準コミットを刻む。glossary.md §1 に固定の理由を明記 |
| D-3 | 対応 | `match_line` を真の最長一致（長さ降順→開始位置昇順、区間の重なりで排除）に修正。docstring・glossary.md §1/§4・`term_candidates.tsv` の説明も一致させた。変化した件数は下表 |
| D-4 | 対応 | §4 に「検出できるもの／検出できないもの」を新設。3ルールの機械検出は11グループのみで残りは人手由来であること、`paren` が現行の半角括弧見出し13件中6件を検出できないこと（未報告6件の file:line を列挙）、送り仮名は原理的に検出できないこと、`discover` の既定コーパスに `fw` が無く `scan` は全4コーパスであることを明記。**抽出単位を本文全行に広げた `spacing` ルールを追加**し、検出された60グループのうち用語として裁定したものを §5 に反映 |
| D-5 | 対応 | コーパス取得層のテストを追加（`TestBaseCommit` 3件・`TestCorpusLoading` 9件・`TestMain` 3件）。対象ファイル数（RST 47・md 10・FW全 .rst）、基準コミットの固定と環境変数上書き、基準コミットの内容と一致すること（作業ツリーではない）、行番号が1始まりで原文と一致すること、`.rst` 以外（png/xlsx/java）を読まないこと、パスがリポジトリ相対であること、整列が決定的であることを検証。GWT コメント形式（`test_extract_sections.py` に準拠） |
| E | 対応 | 「tsv相対」の数え方を §1 に格上げし再現しない実例を明示（`電文` 46対生191／`データ` 483対生828／`単体テスト` 8対生201）／`(応答不要メッセージ送信)` などの引用に「原文は半角括弧」を付記し、§5.4 の6行すべてで見出し文言を引用して扱いを揃えた／§5 の全表を5列（正表記・意味・揺れ表記・別義-旧名称・採用根拠）に統一し §5.14 にも意味列を追加／意味列の文末を体言止めに統一／§6 を表から箇条書きへ／§1 に用語集・design.md 等の作業文書が適用対象外であることを明記／§2 の `ntf-doc-terms.md` の扱いを「意味も本書を基準とする」に統一／§8 全体をコードスパンに／§9 に判定語の凡例（採用・表記変更・範囲限定・一部不採用・不採用）を追加／§5 の揺れ表記列を代表1件＋件数に絞り全 file:line を `scan-terms.tsv` へ委譲（最長セル 375→326字）／Pythonのエラーメッセージを日本語に統一し `unknown_corpus_message()` で CLI と `load_corpora` の文言を一本化、`main` の進捗出力も日本語化／`punct`・`paren` の docstring を実装（「」内の語も抽出する・括弧の中身もマスクする）に一致させた |

**対応しなかった項目: なし。** A〜E の全項目に対応した。

#### ラウンド2（F-1〜F-10、対象コミット `277e23a`）対応一覧

| # | 対応 | 内容・実測根拠 |
|---|---|---|
| F-1 | 対応 | `モックアップクラス` を §5.4 に追加。現行解説書21件・4ファイルを実測（`git show c2419060:<path> \| grep -o モックアップクラス \| wc -l` を4ファイル分合算し21と一致を確認）。うち見出し「モックアップクラスの設定」3件（`http_send_sync.rst:147`・`03_DealUnitTest/http_send_sync.rst:54`・`send_sync.rst:286`）。`term_candidates.tsv` の メッセージング方式 セクションに登録し `scan-terms.tsv` を再生成（`current 21 4`、代表 `send_sync.rst:7`と一致） |
| F-2 | 対応 | `複数レコードレイアウト` を §5.8 に追加。input資料2ファイル・5件（`ntf-testdata-doc-examples-file.md:35,284,301`・`ntf-testdata-doc.md:403`）を `grep -n` で実測し `scan` 結果（`input 5 2`）と一致を確認。旧glossary.mdが使っていた出典不明の「マルチレイアウト」（旧:248,497の2箇所）はglossary.md自身以外のどのコーパスにも出現しないことを `grep -rn マルチレイアウト .rn/20260724-ntf-yaml-support/input/ design.md` で確認（0件）。2箇所とも `複数レコードレイアウト` に置換 |
| F-3 | 対応 | `エンティティバリデーション` を §5.8 に追加。input資料2ファイル・5件（`ntf-testdata-doc-examples-testshots.md:10,38,46,251`・`ntf-testdata-doc.md:182`）を実測し `scan` 結果（`input 5 2`）と一致。testShots表でウェブアプリケーション／バッチ処理／メッセージングと並ぶカテゴリ（`EntityTestSupport`）であることを出典付きで明記し、既存の `エンティティ単体テスト`（Formクラス・Entityクラスの**テストの種類**を指す語）とは別の軸の語であることを別義列に明記 |
| F-4 | 対応 | `メッセージング処理`→`MOMによるメッセージング` の行の引用元行番号を `03_Tips.rst:19`（実際は無関係な `:ref:` 行）から `:786`（引用文と一致する行）に修正。`git show c2419060:...03_Tips.rst \| sed -n '786p'` で「メッセージング処理でテストデータに対し定型的な変換処理を追加したい」と完全一致することを確認 |
| F-5 | 対応 | `セル` の代表引用を `02_entityUnitTestWithNablarchValidation.rst:60`（実際は「エクセルシート参照のこと」＝`エクセル`の部分文字列で`セル`本来の用例ではない）から `batch.rst:224`「ディレクティブ名のセルの右のセルに設定値を記載する」（実際に単独で使われている箇所）に差替え。§4「検出できるもの／検出できないもの」に4つ目の限界事項として部分文字列衝突（`セル`⊂`エクセル`、「データ」対「データベース」等でも起こりうる）を追記 |
| F-6 | 対応 | `YAML形式` の採用根拠から「`design.md` が第2部の章名（`:65`）に使うと決めている」という不正確な記述を削除。`design.md:65` の実際の内容は「テストデータの形式」であり `YAML形式` という文字列は現れないことを `grep -n YAML形式 design.md`（1件のみ、`:52`）で確認。採用根拠を実際の出典（`:52` の本文「YAML形式（`nablarch-testing-yaml`）」使用）に基づく記述に修正 |
| F-7 | 対応 | §5.5 の `単体テスト` 脚注（現行8件）を全件読み直し、一般化「3種を束ねた総称」を撤回。総称と言えるのは `01_entityUnitTest/*.rst:7` の2件のみと確定。`03_Tips.rst:558`「データベースアクセスクラスの単体テストを行う場合」は§8ルールに該当する明確なクラス単体テストの用例と明記。残り5件（`mail.rst:28`・`RequestUnitTest_rest.rst:139`＝リクエスト単体テストの省略表現、`send_sync.rst:364`＝一般名詞見出し、`index.rst:4`＝3ガイドを束ねるページ題、`02_RequestUnitTest.rst:50`＝RSTの行折り返しで「リクエスト単体テスト」の「リ」が前行に分かれた結果）をそれぞれ個別に記述 |
| F-8 | 対応 | `メッセージ同期送信処理`（現行8件）・`メッセージ同期送信`（現行2件・input2件）・`HTTPメッセージ同期送信処理`（現行4件）・`HTTP メッセージ同期送信`（input2件）を語順違いの表記揺れとして `term_candidates.tsv` に登録。`git grep` で現行解説書の生カウント（`メッセージ同期送信処理`を含む行12件のうちHTTP付き4件・無し8件、`メッセージ同期送信`のみ2件）を実測し `scan` 結果と完全一致。input資料 `ntf-doc-terms.md:266-269` の4件も確認。§5.4（`同期応答メッセージ送信`・`HTTPメッセージ送信`の行）と §8（4行追加）に反映 |
| F-9 | 対応 | 250字超のセルを短縮。`HTTPメッセージ送信`採用根拠 332→232字、`テスティングフレームワーク`採用根拠 240→232字、`グループID`別義 280→248字、`使用方法`採用根拠 278→215字、`応答不要メッセージ送信`採用根拠 253→175字。内容は落とさず、冗長な言い回し・重複引用を削除。短縮後に `verify_glossary.py` を再実行し、file:line参照・件数主張の整合を維持していることを確認済み |
| F-10 | 対応 | §8（旧370行付近）の「現行NTF解説書に由来する記述に限る」を「現行解説書に由来する記述に限る」に統一。`grep -c 現行NTF解説書 glossary.md` が0、`grep -c 現行解説書` が98であることを確認 |

**対応しなかった項目: なし。** F-1〜F-10 の全項目に対応した。

#### `match_line` 修正（D-3）で変わった件数

同一の `term_candidates.tsv` に対し、旧アルゴリズム（開始位置昇順）と新アルゴリズム（最長一致）で `scan` を実行して比較した。変化は5件のみ。

| 表記 | コーパス | 旧 | 新 |
|---|---|---|---|
| `バッチアプリケーション` | current | 0 | 1 |
| `バッチアプリケーション` | fw | 72 | 81 |
| `常駐バッチ` | current | 3 | 2 |
| `常駐バッチ` | fw | 23 | 22 |
| `都度起動バッチ` | fw | 36 | 28 |

原因は `都度起動バッチアプリケーション`・`常駐バッチアプリケーション` で、先に始まる短い表記が後から始まる長い表記を抑止していた。glossary.md の該当件数はすべて新しい値に更新済み。

#### 検証結果

| 項目 | 値 |
|---|---|
| `verify_glossary.py` | `refs 264 / counts 114 / sections 77 / terms 183 / applies 87`、**不一致 0 件**、`RESULT: OK`（終了コード0） |
| 検証した file:line 参照 | 264件（実在・行番号範囲・**参照先の行に名指しした表記／引用が実在すること**まで） |
| 検証した件数主張 | 114件（`scan-terms.tsv` と突き合わせ、全一致） |
| §5 と §8 の整合 | 揺れ表記77件すべてが §8 対応表にある |
| tsv と §5 の整合 | 表記183件が一致（tsv 正表記99・エントリ186・カテゴリ11） |
| §8 の適用条件 | 全87行に記載あり |
| テスト | `python3 -m pytest test_detect_term_variants.py test_verify_glossary.py test_extract_sections.py -q` → **140 passed, 96 subtests passed**（前回88 passed から新規52） |
| カバレッジ（`detect_term_variants.py`） | 77% → **99%**（未到達は防御的な1行と `__main__` ガードのみ） |
| カバレッジ（コーパス取得層＋`main`） | 0%（51 stmts 全未到達）→ **98%**（65 stmts 中未到達1＝`__main__` ガード） |
| カバレッジ（`verify_glossary.py`） | 96% |
| `build_mapping.sh` | `RESULT: OK` / `[build_mapping] Done.` |
| 生成済みCSV | 無変更。実行前後の md5 が一致（`sections-current.csv` = `d77abd49f2140e615d26362eec8fff0e`、`sections-input.csv` = `762714ba70a1a46841af6cae91145b2c`）。`git status --short` にも現れない |
| `discover` の検出グループ数 | punct 3 / paren 2 / longvowel 6 / **spacing 60**（spacing は本タスクで追加） |
| glossary.md の分量 | 285行 → 516行 |

#### ラウンド2（F-1〜F-10）修正後の検証結果

| 項目 | 値 |
|---|---|
| `verify_glossary.py` | `refs 277 / counts 120 / sections 81 / terms 190 / applies 91`、**不一致 0 件**、`RESULT: OK`（終了コード0） |
| `term_candidates.tsv` の追加 | 7行（224→231行）。`モックアップクラス`・`複数レコードレイアウト`・`エンティティバリデーション`・`メッセージ同期送信処理`・`メッセージ同期送信`・`HTTPメッセージ同期送信処理`・`HTTP メッセージ同期送信` |
| `scan-terms.tsv` の再生成 | `python3 mapping/tools/detect_term_variants.py scan --max-locations 0 -o mapping/scan-terms.tsv` を実行し748→776行に更新。新規語の件数（`モックアップクラス` current21・`複数レコードレイアウト` input5・`エンティティバリデーション` input5・`メッセージ同期送信処理` current8・`メッセージ同期送信` current2/input2・`HTTPメッセージ同期送信処理` current4・`HTTP メッセージ同期送信` input2）はすべて独立した `git grep`/`grep -n` の手実測と一致（本文中に実測コマンドと結果を記載済み） |
| テスト | `python3 -m pytest .rn/20260724-ntf-yaml-support/mapping/tools/ -q` → **140 passed, 96 subtests passed**（既存テストへの回帰なし。tsv・glossary.md はデータ変更のみでコード変更なしのため件数は前回と同じ） |
| 250字超セルの残存確認 | 採用根拠／別義列を機械走査し、250字以上のセルが0件であることを確認（F-9対象4件はすべて232字以下に短縮） |
| glossary.md の分量 | 516行 → 530行 |

#### Method（書きながら検証する）の適用

- `verify_glossary.py` を**用語集の書き直しより先に**作成し、修正のたびに実行した。初回実行で旧用語集の不一致（`同期応答メッセージ送信処理` の input 件数 5→4、`単体テスト` の現行件数 21→8 など）を検出し、書き直し中も refs 31件→0、counts 7件→0、sections 8件→0、terms 12件→0 と反復して潰した
- レビュアが提示した実測値も鵜呑みにせず全件を再測定した。数え方の違いで値が変わったもの: `テストショット` 17→10（`テストショット一覧`・`テストショット表` を分離）、`シート` 108→101（`テストデータシート`・`準備データシート` を分離）、`精査` 92→47（`単項目精査`・`項目間精査` を分離）、FW `バリデーション` 379→316（`相関`・`ドメイン`・`単項目` を分離）。FW `使用方法` の見出し36件はレビュア指摘と一致
- 現行解説書の file:line はすべて `git show c24190607fef5d76c607aa08b36d2ab2f813efe5:<path>` の内容に対する行番号であることを、`verify_glossary.py` の refs 検査で機械的に保証した（作業ツリーは参照していない）
- `scan` の全出力を `mapping/scan-terms.tsv` としてコミットし、glossary.md の代表1件と全件の対応を再現可能にした

#### #3差し戻し（母集合の再構成）対応一覧

ラウンド1〜3のレビュー指摘（G-1, G-2, G-5, G-9, G-10）が「用語集がカバーすべき用語の母集合を定義していない」ことに起因して収束しなかったため（`steering.md` #3差し戻しの記載）、母集合を機械抽出し「母集合に対する未判定ゼロ」の機械検証に置き換えた。テストは先に書き、実装してから通した（TDD）。

| # | 対応 | 内容 |
|---|---|---|
| 母集合抽出 | 対応 | `mapping/tools/extract_terms.py` を新規作成。4出典から `term-candidates.csv`（`term,source,occurrences,file_line`）を出力。current-heading 266件（`sections-current.csv` の `heading_path` を `>` 分解し `(冒頭)`/`(L{n}直下)` マーカーを除く）、ntf-doc-terms-heading 45件、design-heading 21件、design-scheme 7件、計339行・331種類。テストは `test_extract_terms.py`（20件）で先行作成し、実装後に全件緑化 |
| ntf-doc-terms.md の見出し規則 | 対応 | 発注元指示の「35件」に対し、実測は H1=1・H2=10・H3=27・H4=8（H1を除く45件）。どのレベル組み合わせが35かの明記が無いため、35に合わせる恣意的なレベル選定はしない方針を採り、「H1（文書題）を除く全レベル」という単純な規則を明示的に選び、結果の45件をそのまま報告する（`extract_terms.py` 冒頭docstringに規則と根拠を明記） |
| file:line の正確性 | 対応 | 当初 `current-heading` の `file_line` を `sections-current.csv` の該当行の `src_line` からそのまま作っていたところ、子見出しを持つ見出し（ページ題など）は自分自身のセクション行を持たないため、無関係な本文行を指す誤引用になることが判明（用語集で禁じている「採用根拠が引用先の内容を支持しない」G-4型の欠陥と同型）。基準コミット時点の `.rst` を実際に再パースして見出し自身の行番号を求める方式（`_heading_locations`）に修正し、全266件で正しい行番号になることをテストと `verify_glossary.py` の `refs` 検査で確認 |
| glossary.md への反映 | 対応 | §5.15「`term-candidates.csv` との対応（母集団の全件判定）」を新設（5.15.1〜5.15.5）。5.15.1 採用36件（§5の該当箇所への相互参照。§2で求める「既存語→候補」対応を兼ねる）。5.15.2 不採用20件（design.mdの章・セクション見出し。本節への掲載自体で網羅性要件を満たす）。5.15.3 不採用39件（input見出し。中核語が採用済みのもの・識別子・一般語・§9で判定済みのものに分類）。5.15.4 不採用39件（現行解説書の複数ファイル再出現見出し。手順見出し／現行独自の節枠／識別子／ページ固有題に分類し個別理由）。5.15.5 不採用197件（1ファイルのみの見出し。識別子14件は個別、残り183件は表を圧縮して複数候補を1行にまとめた） |
| verify_glossary.py 拡張 | 対応 | 既存5検査（refs/counts/sections/terms/applies）を維持し、`population`（term-candidates.csv 全331種類が§5採用または§5.15不採用理由付きで判定されていること）・`design_sections`（design.md章・セクション21件がglossary.mdに存在すること）・`scheme_names`（処理方式7件がdesign.mdの正式名称と一致すること）・`reasons`（§5.15不採用テーブル全143行に空でない理由）の4検査を追加し計9検査に。`test_verify_glossary.py` に15件追加（40件）してから実装 |

#### G-4/G-6/G-7/G-8 個別誤りの監査結果

いずれも母集合とは別の、根拠を実測せずに書いたことによる記述ミスとして個別に発注されたもの。詳細な指摘一覧は保存されていなかったため（`steering.md` に一行要約のみ）、4パターンごとに `glossary.md` 全体を監査して自分でインスタンスを再発見した。

| # | 監査方法 | 結果 |
|---|---|---|
| G-7（150字超セル41個） | `read_cells` で全セルの文字数を機械走査 | **41個を実測で特定**（発注元の「41個」と一致）。長い揺れ表記リスト（`／`区切りの複数表記×file:line）は先頭表記のみ引用を残し残りは件数のみに圧縮、採用根拠は言い回しを詰めて全41件を150字以内に短縮。圧縮に伴い件数主張の係り先（`counts`検査）とref内容一致（`refs`検査）が2箇所壊れたが同一作業内で検出・復元。最終的に150字超セル0件、`verify_glossary.py` 9検査すべて不一致0件 |
| G-6（件数の誤り） | `detect_term_variants.py scan` を再実行（`scan-terms.tsv` は再生成後も差分なし＝既に最新）。表中の「現行N件/inputN件/FWN件/designN件」（119件、`counts`検査で機械検証）に加え、`現行解説書N件`/`input資料N件` 形式の散文主張（約130件）も行の正表記に対して再検証。§1の生grep実測（`電文`46/191、`データ`483/828、`単体テスト`8/201）、§4のdiscover実行結果（punct 3/paren 2/longvowel 6/spacing 60）も再実行して確認 | **既存記述に不一致は見つからなかった**（再検証は全件パス）。G-7の圧縮作業で「input資料」→「input」のように短縮した結果、`資料`の有無でCOUNT_RE命中が変わり新たな不一致を2箇所自己混入させたが、同作業内で「input資料」表記に復元して解消（最終 `counts` 検査 119件・不一致0） |
| G-4（採用根拠の引用不一致） | 採用根拠列の全コードスパン参照124件について、参照先の実際の行内容を取得し周辺の主張文と突き合わせ（機械的な`refs`検査に加えて手作業で文脈も確認） | **既存記述に不一致は見つからなかった**。G-7の圧縮作業中に2箇所（`テスティングフレームワーク`行・`データベースを使用するクラスのテスト`行）で、引用直前の「」引用文を削ったため無関係な行内容とのミスマッチを自己混入させたが、`refs`検査で検出し実際の引用文に復元して解消（最終 `refs` 検査 326件・不一致0） |
| G-8（分類の誤り） | §9の判定語（採用/表記変更/範囲限定/一部不採用/不採用の5分類、`glossary.md`が凡例で定義）を全15行で定義文と理由を突き合わせ。§2のFW解説書と異なる表記を採用した3件（`スーパクラス`/`前提事項`/`単項目バリデーション`）が網羅的か、他のカタカナ語（`オーバーライド`/`インタフェース`/`ユーザ`/`サーバ`/`データ`/`バイナリデータ`/`パーサ`）のFW多数派表記との一致も実測で確認 | `HTTP 同期応答メッセージ送信の用語読み替え（L398）` 行が「一部不採用」と判定されていたが、理由を読むと節内の2要素（クラス名対応表は識別子として採用／節題は`HTTPメッセージ送信`に表記変更）のどちらも不採用ではなく、「一部不採用」の定義（節内に採用語と不採用語が混在）に該当する不採用要素が実際には無い。「表記変更」に訂正。§2の3件は他7語すべてFW多数派と一致することを実測で確認し網羅的と判断（追加の訂正なし） |

#### #3差し戻し後の検証結果

| 項目 | 値 |
|---|---|
| `verify_glossary.py` | `refs 326 / counts 119 / sections 81 / terms 190 / applies 91 / population 331 / design_sections 21 / scheme_names 7 / reasons 143`、**不一致 0 件**、`RESULT: OK`（終了コード0） |
| `term-candidates.csv` | 339行・331種類（`csv.DictReader` で確認。`wc -l` は使用していない）。内訳: current-heading 266・ntf-doc-terms-heading 45・design-heading 21・design-scheme 7 |
| §5.15 の判定内訳 | 採用36件・不採用295件（design.mdの章・セクション見出し20件／input見出し39件／現行解説書の見出し236件〈複数ファイル再出現39件＋1ファイルのみ197件〉） |
| テスト | `python3 -m pytest mapping/tools/ -q` → **175 passed, 96 subtests passed**（新規: `test_extract_terms.py` 20件、`test_verify_glossary.py` +15件） |
| 150字超セル | 監査で41件を実測特定 → 修正後0件 |
| glossary.md の分量 | 530行 → 749行（§5.15の新設、および41+2件のセル短縮による純増減） |

## レビューの経過

| ラウンド | 対象コミット | QA | 設計 | クラフト | 検証 | 備考 |
|---|---|---|---|---|---|---|
| 1 | `771e9b0` | NG | NG | NG | NG | 4観点とも fail。指摘を A-1〜A-4 / B-1〜B-10 / C-1〜C-12 / D-1〜D-5 / E に整理して発注 |
| 2 | `277e23a` | 未実施 | 未実施 | 未実施 | 未実施 | 再レビューを起動したが利用枠の都合で中断。**再開時はここから** |

ラウンド1でコーディネーターが Invalid と判定した指摘は1件のみ。「`checks/task-03.md` が未追跡でコミットに含まれていない」— 本ファイルは実装エキスパートがコミットせず、コーディネーターが check-off コミットに含める運用のため成立しない。他はすべて Valid として発注（エスカレーション該当なし）。

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | NG（ラウンド1）／ラウンド2 未実施 | 「網羅されている」の論拠が循環していた。`discover` が機械検出した揺れは11グループのみで、§5 の揺れ約40件の大半は人手作成の `term_candidates.tsv` 由来。検出器が見ていない領域は最初から出てこないため、「検出グループが全部反映されている」は網羅性の証明にならない。最大の欠落として `精査`（現行92件）↔`バリデーション`（FW379件）が未裁定。テストは純関数に厚くコーパス取得層0%で、壊れると致命的な部分が無テスト。→ A-1・D-1〜D-5 で発注 |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | NG（ラウンド1）／ラウンド2 未実施 | 掲載基準が実質「discover が揺れを検出したか」に支配され、揺れのない最頻出の骨格語（`テストクラス`140/`テストケース`191/`テストメソッド`70）が欠落。本セッションの主題であるYAML側の語彙が実質空。→ A-2・A-3 で発注 |
| System-wide integrity (interfaces, cross-doc consistency) | NG（ラウンド1）／ラウンド2 未実施 | design.md との矛盾2件・事実誤認1件。(1) HTTPメッセージング送受信を「FW解説書に該当語なし」として造語採用のうえ未解決事項に先送りしたが、FW解説書に実在（`http_system_messaging.rst:94`・`:132`、FW9件）し優先順位1に違反。(2) §6 の全角括弧規則が design.md §7（FWライブラリに揃える）と逆行（FWライブラリ見出しは半角26/全角5）。原因は `DISCOVER_DEFAULT_CORPORA` に `fw` が無くFW側を見ずに決めていたこと。(3) 未解決事項#3 が design.md の未確定事項1・2を誤読。→ C-1・C-2・C-12 で発注 |

### Craft Expert (writing)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG（ラウンド1）／ラウンド2 未実施 | §8 対応表を無条件適用すると壊れる（`概要`→`機能概要` で「機能機能概要」、`データシート`→`シート` で「テストデータシート」が「テストシート」、`groupId`→`グループID` がMavenの `<groupId>` を破壊）。`データブロック`／`データタイプ`／`グループID` の定義が三つ巴で循環。§5.8 の表と直後の `:192` が自己矛盾。表の1行が最長551字で読めない。→ C-4・C-5・C-6・E で発注 |
| Consistency with existing style | NG（ラウンド1）／ラウンド2 未実施 | だ・である調と曖昧語は問題なし（機械検査で違反0）。一方「なし」が二義（揺れなし／未出現）、表ヘッダの列数不揃い、意味列の文末不統一、§9 の判定語「修正」が4通りの意味、Pythonのエラーメッセージが日英混在、docstring が実装より狭い。→ C-7・E で発注 |

### Verification Expert (fact-check)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (claims verified) | NG（ラウンド1）／ラウンド2 未実施 | 機械的な裏付けは良好（参照219/219が正しく解決、件数181/181が再現、否定の主張もすべて実測で真）。落ちたのは**採用根拠の散文**で、検証済みの数値に出典が支持しない一般化を接続した事実誤り7件（「編はウェブ系3章にだけ」実際は6件／「FWのグループID 22件はすべてMaven」実際は3件が認可機能／「scan は辞書順に整列」実際は tsv 順でリポジトリ自身のテストが反証／「NTFは解説書本文では使わない」現行に2件／「FW解説書は『処理』を付けない」FW1件あり／「使用方法 FW63件は各ライブラリの見出し」見出しは36件／`JUnit5` は toctree のファイル名で揺れ表記ではない）。→ B-1〜B-10 で発注 |
| Coverage (claims) | NG（ラウンド1）／ラウンド2 未実施 | 数え方の定義（`term_candidates.tsv` 相対の最長非重複マッチ）が1箇所にしか書かれておらず、生の grep と一致しない差が読者に再現不能だった（`電文` 46対生191、`データ` 485対生828）。→ E で発注 |

## Overall Verdict

- Self-check: OK（#3差し戻し・母集合再構成分を含む。上記「#3差し戻し（母集合の再構成）対応一覧」「#3差し戻し後の検証結果」を参照。`verify_glossary.py` 9検査すべて不一致0件）
- QA: NG（ラウンド1）／ラウンド2 未実施
- Design expert: NG（ラウンド1）／ラウンド2 未実施
- Craft expert: NG（ラウンド1）／ラウンド2 未実施
- Verification expert: NG（ラウンド1）／ラウンド2 未実施
- Ready to check off: No — ラウンド2の再レビューが未実施のため
