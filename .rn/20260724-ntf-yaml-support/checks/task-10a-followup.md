# #10b Completion Check

作業指示: `.rn/20260724-ntf-yaml-support/ntf-doc-10a-followup.md`（`#10a` 承認後の仕上げ3件）
基準コミット: `699dfd0`（作業指示のゲート2・10 が指定する比較元）。`699dfd0..HEAD` は `steering.md` と作業指示ファイルのみの変更であり、`ja/` 配下に差分は無い。
実行日: 2026-08-07

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 作業指示のゲート1〜10 がすべて実行結果で確認され、`checks/task-10a-followup.md` に記録されている | OK | 本書 §6（ゲート7 を先頭に置き、以降 1〜6・8〜10）。**是正ラウンド2 で全10件を再実行し出力を差し替えた。** 全10件 PASS、NG 0件 | | |
| STEP 1 の数詞の全件確認表、STEP 3 の値の種類の突合表が、いずれも件数ではなく**全件**の表で記録されている | OK | §1-2 に数詞の全件表（**35行**。`testdata_examples.rst` の地の文＋散文ディレクティブ本体から機械抽出した数詞出現行の全件。是正ラウンド1〈F-2〉で25行→29行、**是正ラウンド2〈G-4〉で助数詞 `加算`・`重` と語順が逆の序数を加えて29行→35行**。抽出条件を表の冒頭に明記）、§3-2 に値の種類の突合表（**12行**。`testdata_notation.rst` の Excel形式・YAML形式の両表から機械抽出した「値の種類」の全件。是正ラウンド1〈F-4〉で判定基準を明記）。**是正ラウンド2 で §3-2a〈空セルの全件分類、210箇所を5分類〉と §3-3 の出典判定表〈6種それぞれの出典の有無〉を追加した** | | |
| 両ページに `（番号・説明・期待するステータスコード）` が0件である | OK | ゲート6。`grep -c` → `testdata_examples.rst:0` / `testdata_notation.rst:0` | | |
| 記載例ページの特殊記法セクションの導入文が `testdata_notation-special_notation` を参照している | OK | ゲート5。**節の範囲を機械判定して L1847〜L1993 に確定**（次節のラベルは L1994。是正ラウンド1 までの L1995 は誤り）。この範囲の `:ref:` は**2件**で、**2件とも `testdata_notation-special_notation` を指す**（L1851 の導入文・L1859 の `important`）。**是正ラウンド1 で増えていたページ先頭ラベル `testdata_notation` への1件は、是正ラウンド2〈G-1〉で括弧書きごと削除した** | | |
| 作業指示の禁止事項に抵触する変更が無い（`mapping.csv` / `_batch/` / `vocabulary.md` / `style.md` / `glossary.md` / `design.md` / `ja/conf.py` に差分が無く、見出しの文言・並び順が不変） | OK | ゲート2（`git diff 699dfd0 HEAD` および `git diff 699dfd0`〈作業ツリー込み〉がいずれも空）、ゲート3（見出し数・文言・並び順が両ページとも `699dfd0` と完全一致） | | |
| Docker フルビルドが `build succeeded` で、警告が既知の `db_double_submit.rst` 1件のみ（新規0件） | OK | ゲート9。**是正ラウンド2 で再実行**。`build succeeded, 1 warning.` / `EXIT=0`。警告行は `db_double_submit.rst:108` の1件のみ。`sphinx.mo` を復元し `git status --porcelain` の実出力で確認、`_build/` は残している | | |
| 残り32ページへの申し送り2件が `reviews/page-*.md` に追記されている | OK | §5。`page-testdata_notation.md` 申し送り**28**（全件表を求める完了条件はゲート実行順の先頭に置く）・**29**（概観表・定義セルに具体的なカラム名を列挙しない）。`page-testdata_examples.md` にも同2件を追記（申し送り19・20）。**是正ラウンド1 で `page-testdata_notation.md` に申し送り30〈F-6: `glossary.md` §5.7 の `実行条件` 標準と定義セルの食い違い〉、`page-testdata_examples.md` に申し送り21〈F-5-2: インラインリテラル前の区切りの割れ〉を追記した。是正ラウンド2 で `page-testdata_examples.md` に申し送り22〈G-6: 見出しの約束と導入文の齟齬、書き方ページ側の無条件の前方参照3箇所〉を追記し、`#10b` の表の直後に撤回の上書き注記1行を追記した〈G-7〉**（いずれも追記のみ。既存の記録は1行も書き換えていない） | | |

---

## 1. STEP 1 — `testdata_examples.rst` の数詞を明確にする

### 1-1. 実施した是正（1箇所）

作業指示 STEP 1 が指定した1箇所のみを是正した。

| 項目 | 内容 |
|---|---|
| 位置 | `ja/development_tools/testing_framework/implementation/testdata_examples.rst:401`（`grep -n` で現在位置を特定。指示作成時点と同じ `:401`） |
| 是正前 | `正常ケースと認証エラーケースの2件を実行する例である。` |
| 是正後 | `正常ケースと認証エラーケースの2つのテストショットを実行する例である。` |
| 根拠 | `2件` が何を数えているかが曖昧であったため、テストショットを数えていることが分かる形にした。**セルの値である `正常ケース` は落としていない**（`:424` の Excel セル `- 正常ケース`、`:478` の YAML `description: "正常ケース"` を `grep -n` で実在確認済み）。`認証エラーケース` も落としていない |
| 兄弟の導入文との整合 | `:198` が既に `…という2つのテストショットを実行する例である。` の形であり、同じ形に揃った |

**セル値の実在確認**:

```
$ grep -n "正常ケース" ja/.../testdata_examples.rst
424:    - 正常ケース
478:          description: "正常ケース"
```

### 1-2. 数詞の全件確認表（**全35件**。是正はこの表の #6 の1件のみ）

**是正ラウンド1（F-2）で母集合を再定義し、25行から29行に増えた。** 初回の抽出はディレクティブ本体を一律に除外し、助数詞リストに「バイト」を持たず、助数詞を伴わない「複数」単独を拾わなかったため、`:957`・`:1213`・`:1262`・`:1565` の4行が母集団から漏れていた（**いずれも曖昧ではなく本文の是正は不要**。詳細は表の #15・#17・#18・#23）。

**是正ラウンド2（G-4）でさらに6行を追加し、29行から35行になった。** 助数詞リストに `加算`・`重` が無かったこと、および「数詞＋助数詞」という語順を前提にしていたことで、`:99`・`:1413` と `:245`・`:264`・`:283`・`:306` が母集団から漏れていた（**いずれも曖昧ではなく本文の是正は不要**。詳細は表の #30〜#35）。

**表への追加という形をとった理由**（作業指示 G-4 は「表に追加するか、抽出条件の限界の明記にとどめるか」を選ばせている）: 完了条件が求めているのは「数詞の**全件**確認表」であり、母集合は「数詞が出現する行」である。助数詞リストは母集合を機械的に切り出すための道具にすぎず、道具の穴を但し書きで開示するだけでは、表が全件でない状態が残る。同じ判断は是正ラウンド1（F-2）で既に下されている（散文ディレクティブ本体を「抽出条件の限界」として但し書きにせず、母集団に加えて再抽出した）。ここで方針を変えると、同じ表の中に「拾った漏れ」と「開示だけした漏れ」が混在する。

#### 抽出条件（是正ラウンド1で明記・再抽出、是正ラウンド2で助数詞と語順を拡張）

抽出スクリプトは是正ラウンド2 で**記録を参照せず独立に組み直した** `scratchpad/num3.py`（是正ラウンド1 の `num2.py`、初回の `num.py` を差し替え）。下記の抽出条件のうち是正ラウンド1 までの部分だけで走らせると**29行**が得られ、是正ラウンド1 の記録と行番号まで完全に一致する（`[14, 18, 22, 198, 397, 401, 436, 452, 502, 506, 580, 784, 792, 840, 957, 1209, 1213, 1262, 1358, 1417, 1450, 1471, 1565, 1588, 1922, 1926, 1942, 1946, 2070]`）。行番号は是正後の現在の内容。

| 項目 | 内容 |
|---|---|
| **母集団に含めるもの** | (a) カラム0から始まる本文行（地の文）、(b) **散文ディレクティブの本体** — `.. note::`／`.. important::`／`.. tip::`／`.. warning::`／`.. caution::`／`.. attention::`／`.. admonition::` の本体行。**(b) は初回の抽出が一律に除外していた分であり、是正ラウンド1 で母集団に加えた** |
| **母集団から除外するもの** | 見出し行・下線行、空行、箇条書き（`* `／`- `／`1. ` で始まる行）、`.. code-block::`／`.. list-table::`／`.. image::`／`.. contents::`／`.. toctree::` など**散文でないディレクティブの本体**（＝コードブロックの中身・`list-table` の表セル）、ディレクティブのオプション行（`:header-rows:`・`:widths:` など）、ディレクティブの開始行そのもの、散文ディレクティブに属さないインデント行 |
| **助数詞リスト** | `つ`・`件`・`個`・`種類`・`種`・`箇所`・`行`・`列`・`回`・`通り`・`パターン`・`組`・`点`・`段`・`階層`・`ページ`・`ファイル`・`シート`・`レコード`・`フィールド`・`カラム`・`セル`・`エントリ`・`文字`・`例`・`番目`・`対`・`方向`・`方`・`層`・**`バイト`**・`ビット`・`桁`・`倍`・`割`・`度`・`人`・`つ目`・`文`・`語`・`語目`・`ブロック`・`テーブル`・`クラス`・`メソッド`・**`加算`**・**`重`**（**太字のうち `バイト` は是正ラウンド1 で、`加算`・`重` は是正ラウンド2〈G-4〉で追加**。`分`・`秒` は「テストショット数分」のような非数詞にしか当たらないため入れていない） |
| **数詞部** | `[0-9０-９一二三四五六七八九十百複数]+` のうち、**少なくとも1文字が数字・漢数字・`複` であるもの**（`数分` のように `数` 単独で始まるものは数詞として数えない）。加えて、**助数詞を伴わない `複数` 単独**も拾う（**是正ラウンド1 で追加**。#18 `:1262` がこれに当たる） |
| **語順が逆の序数**（**是正ラウンド2〈G-4〉で追加**） | 上記の「数詞部＋助数詞」という語順の前提から外れる、`数えられるもの＋数詞` の形（`テストショット1`・`テストショット2`）。`grep -nE 'テストショット[0-9０-９一二三四五六七八九十]'` で全走査した結果、該当は `:245`・`:264`・`:283`・`:306` の**4行のみ**であり、他の名詞（`データブロック`・`レコード`・`カラム` など）では0件である |
| 位置の区分 | `INTRO` = 見出し直下の最初の段落、`body` = 導入文以外の地の文、`DIRBODY(名)` = 散文ディレクティブの本体 |

`.. tip::`・`.. important::` などの散文ディレクティブは本ページに**10件**ある（`:329`・`:786`・`:868`・`:955`・`:1211`・`:1407`・`:1411`・`:1707`・`:1754`・`:1857`）。ディレクティブ本体を母集団に含めて再抽出した結果、数詞を含むのは `:957`（`:955` の `tip`）と `:1213`（`:1211` の `important`）の**2行だけ**であり、他に漏れは無い。作業指示が例に挙げた `:957`（`採番処理が1度だけ行われる`）はこの2行のうちの1行である。

**ヒットは35行**（是正ラウンド1 までの条件で29行〈`INTRO` 24行 / `body` 3行 / `DIRBODY` 2行〉、是正ラウンド2 の拡張で `body` 5行・`DIRBODY` 1行が加わる）。

**#1〜#29 は行番号の昇順である。#30〜#35（是正ラウンド2〈G-4〉の追加分）は、本書の他の節が `#6`・`#9`・`#12`・`#22` などの番号で個別の行を参照しているため、既存の番号を保つ目的で末尾に追加している**（#30〜#35 の内部は行番号の昇順）。

| # | 行 | 位置 | 見出し | 数詞 | 何を数えているか | 判定 |
|---|---|---|---|---|---|---|
| 1 | `:14` | 導入文 | [L2] データブロックとデータタイプ | `1つ` / `1シート` / `1ファイル` | 読み込み単位・Excel のシート・YAML のファイル。いずれも数える対象が数詞の直後にある | 明確。是正不要 |
| 2 | `:18` | 導入文 | [L3] 1つの読み込み単位にテストショット・準備データ・期待値をまとめる | `1つ` / `4つ` | `1つの読み込み単位` / `用途の異なる4つのデータブロック`。対象が直後にある | 明確。是正不要 |
| 3 | `:22` | 導入文 | [L4] Excel形式の場合 | `1行` | 「実際のシートでは1行に続けて記述する」。シート上の行 | 明確。是正不要 |
| 4 | `:198` | 導入文 | [L3] テストショットごとに準備データと期待値を使い分ける | `1件` / `2件` / `2つ` | `明細1件` / `明細2件`（注文明細の件数）/ `2つのテストショット` | 明確。是正不要。**STEP 1 の是正後の `:401` はこの形に揃った** |
| 5 | `:397` | 導入文 | [L2] テストショット一覧（testShots）を記述する | `1つ` | 「``LIST_MAP`` というデータタイプを使うデータブロックの1つ」 | 明確。是正不要 |
| 6 | `:401` | 導入文 | [L3] ウェブアプリケーションのテストショット一覧を記述する | `2つ` | **テストショット**（是正前は `2件` で対象が無かった） | **是正済み（STEP 1）** |
| 7 | `:436` | 導入文以外 | [L4] Excel形式の場合 | `1つ` / `1件` / `1エントリ` | `テストショットごとに1つずつ`（``LIST_MAP`` を数える）/ `1件目の例`（直前文が数える対象＝``context`` から参照される ``LIST_MAP`` を与えている）/ `1エントリのみが有効` | 明確。是正不要 |
| 8 | `:452` | 導入文以外 | [L4] Excel形式の場合 | `2件` | `2件目の例`。#7 と対になり、同じく ``LIST_MAP`` を数える | 明確。是正不要 |
| 9 | `:502` | 導入文 | [L3] Nablarchバッチアプリケーションのテストショット一覧を記述する | `2件` | 「データベースだけを処理するテストショットと、入力ファイルを使うテストショットの2件」。**数える対象（テストショット）が同じ文の中に2回明示されている** | 明確。是正不要 |
| 10 | `:506` | 導入文 | [L4] Excel形式の場合 | `1行` | シート上の行 | 明確。是正不要 |
| 11 | `:580` | 導入文 | [L4] Excel形式の場合 | `1行` | シート上の行 | 明確。是正不要 |
| 12 | `:784` | 導入文 | [L3] 準備データ（SETUP_TABLE）を記述する | `一通り` | 数詞ではなく網羅の程度を表す副詞（「値の種類を一通り含んでいる」） | 該当外。是正不要 |
| 13 | `:792` | 導入文 | [L4] Excel形式の場合 | `1行` | シート上の行 | 明確。是正不要 |
| 14 | `:840` | 導入文 | [L4] YAML形式の場合 | `1行` / `1つ` | 「1行を1つのオブジェクトとして記述する」。対象が直後にある | 明確。是正不要 |
| 15 | `:957` | **`.. tip::` 本体**（`:955`） | [L3] 採番処理のテストデータを記述する | `1度` | 「テスト内で採番処理が1度だけ行われることを想定している」。数える対象（採番処理の実行）が同じ節の主語として直前にある | 明確。是正不要。**是正ラウンド1（F-2）で追加** |
| 16 | `:1209` | 導入文 | [L3] 複数レコードレイアウトを記述する | `1つ` | `1つのデータブロック` | 明確。是正不要 |
| 17 | `:1213` | **`.. important::` 本体**（`:1211`） | [L3] 複数レコードレイアウトを記述する | `1つ` / `40バイト` | `1つの固定長ファイルのデータブロック内では`（対象が直後）/ `フィールド長の合計が40バイトになるよう`（対象＝フィールド長の合計が直前） | 明確。是正不要。**是正ラウンド1（F-2）で追加** |
| 18 | `:1262` | 導入文 | [L4] YAML形式の場合 | `複数`（助数詞なし） | 「``records:`` 配列に、レコードレイアウトを複数並べて記述する」。数える対象（レコードレイアウト）が直前にある | 明確。是正不要。**是正ラウンド1（F-2）で追加** |
| 19 | `:1358` | 導入文 | [L4] Excel形式の場合 | `2文字` | 「バックスラッシュと ``t`` の2文字」。対象が直前に列挙されている | 明確。是正不要 |
| 20 | `:1417` | 導入文 | [L4] Excel形式の場合 | `1文字` | 「``quoting-delimiter`` に指定できるのは1文字だけ」 | 明確。是正不要 |
| 21 | `:1450` | 導入文 | [L4] YAML形式の場合 | `1層` | 「外側のダブルクォート1層」。対象が直前にある | 明確。是正不要 |
| 22 | `:1471` | 導入文 | [L3] グループIDでファイルを使い分ける | `1件` / `複数件` | `1件処理` / `複数件処理`。数えているのは**入力ファイル中のレコード件数**（**是正ラウンド1〈F-3〉で行番号を実測しなおした**。グループIDなしのデータブロックはデータ行1件〈値 `10001` は `:1498`。データ行の先頭マーカーは `:1497`〉、`case2` は2件〈値 `20001` は `:1525`・`20002` は `:1529`。先頭マーカーは `:1524`・`:1528`〉） | **やや曖昧。是正候補として報告（本タスクでは変更しない）**。理由は §1-3 |
| 23 | `:1565` | 導入文 | [L3] 空ファイルを記述する | `0バイト` | 「0バイトの空ファイルを表現する例である」。数える対象（ファイルのサイズ）が数詞の直後にある | 明確。是正不要。**是正ラウンド1（F-2）で追加** |
| 24 | `:1588` | 導入文 | [L2] メッセージングのデータを記述する | `4つ` | `4つのデータタイプ` | 明確。是正不要 |
| 25 | `:1922` | 導入文 | [L3] スペース・ダブルクォートを記述する | `1文字` ×2 | `半角スペース1文字` / `ダブルクォート1文字`。対象が直前にある | 明確。是正不要 |
| 26 | `:1926` | 導入文 | [L4] Excel形式の場合 | `1層` | 「外側の1層だけが除去される」。直前が「前後をダブルクォートで囲むと」 | 明確。是正不要 |
| 27 | `:1942` | 導入文以外 | [L4] Excel形式の場合 | `1文字` ×2 | #25 と同じ対 | 明確。是正不要 |
| 28 | `:1946` | 導入文 | [L4] YAML形式の場合 | `1文字` ×2 | #25 と同じ対 | 明確。是正不要 |
| 29 | `:2070` | 導入文 | [L4] Excel形式の場合 | `2件` | 「``001`` と ``002`` の2件だけが登録される」。数える対象の実値が同じ文に列挙されている | 明確。是正不要 |
| 30 | `:99` | 導入文以外 | [L4] Excel形式の場合 | `1加算` | 「バッチ処理によって ``ITEM_COUNT``\ が1加算され」。数える対象（加算する量）が助数詞 `加算` に含まれ、加算される先（``ITEM_COUNT``）も直前にある | 明確。是正不要。**是正ラウンド2（G-4）で追加** |
| 31 | `:245` | 導入文以外 | [L4] Excel形式の場合 | `テストショット1`（語順が逆の序数） | 何番目のテストショットか。数える対象（テストショット）が数詞と同じ語に含まれる。`:198` の導入文が「2つのテストショット」と数えたうえで、その1つ目・2つ目をこの形で指しており対応が取れている | 明確。是正不要。**是正ラウンド2（G-4）で追加** |
| 32 | `:264` | 導入文以外 | [L4] Excel形式の場合 | `テストショット1`（語順が逆の序数） | #31 と同じ。準備データに対する期待値の側 | 明確。是正不要。**是正ラウンド2（G-4）で追加** |
| 33 | `:283` | 導入文以外 | [L4] Excel形式の場合 | `テストショット2`（語順が逆の序数） | #31 と対になる2つ目のテストショット | 明確。是正不要。**是正ラウンド2（G-4）で追加** |
| 34 | `:306` | 導入文以外 | [L4] Excel形式の場合 | `テストショット2`（語順が逆の序数） | #33 の期待値の側 | 明確。是正不要。**是正ラウンド2（G-4）で追加** |
| 35 | `:1413` | **`.. tip::` 本体**（`:1411`） | [L3] カンマを含む値を記述する | `二重` | 「``""``\ と二重に記述してエスケープする」。数える対象（ダブルクォート文字の重ね方）が実体 ``""`` として同じ文に示されている。`重` は数える対象を伴わない程度の表現であり、`一通り`（#12）と同じく本来の助数詞ではない | 明確。是正不要。**是正ラウンド2（G-4）で追加** |

**内訳（全35件）: 明確32件 / 該当外1件（#12、`一通り`）/ 是正済み1件（#6）/ やや曖昧・是正候補として報告するもの1件（#22、変更していない）。**

**是正ラウンド1（F-3）の行番号の訂正**: 初回の記録は #22 の根拠として「データ行1件〈`:1489` の `10001`〉、`case2` は2件〈`:1521`・`:1527` の `20001`・`20002`〉」と書いていたが、**3件とも実ファイルと合わない**。実測では `:1489` は `  * -`（データ行の先頭マーカー）、`:1521` は `    - 5`（フィールド長の行）、`:1527` は `    - morning`（`20001` の行の `MESSAGE` セル）である。正しくは `10001` が `:1498`、`20001` が `:1525`、`20002` が `:1529`。**判定（1件 vs 2件）は変わらない**（`grep -n` で再確認済み。§6 ゲート7 に出力を掲載）。

### 1-3. 是正が必要と判断した箇所（報告のみ。本タスクでは変更していない）

作業指示 STEP 1 は「**他の導入文は変更しない。**…是正が必要と判断した箇所があれば、その理由とともに報告する（本書では是正を指示しない）」としているため、次の1件（#22）は**変更していない**。

| 箇所 | 現状 | 理由 |
|---|---|---|
| `testdata_examples.rst:1471` | `テストショットごとに異なる入力ファイルを使い分ける例である。グループIDなしのデータブロックが1件処理に、``case2`` のデータブロックが複数件処理に対応する。` | `1件処理`・`複数件処理` の「件」が**何を数えているかが文中に無い**。実際に数えているのは入力ファイル中のレコード件数であり（実測: グループIDなし＝データ行1件、`case2`＝2件）、直前の文が数えている「テストショット」でも「データブロック」でもない。同じ文に3つの数えられるもの（テストショット・データブロック・レコード）が同居しているため、`:401` と同種の曖昧さが残る。**`1件のレコードを処理するテストショット`／`複数件のレコードを処理するテストショット` のように対象を明示する形が候補になる。** ただし `1件処理`／`複数件処理` は `#10a` ラウンド3（R3-A-5・R3-A-6）で「テストショット」の3連発を避けるために意図的に短縮した経緯があり、単純に語を戻すと当時の指摘が再燃する。**是正するかどうかは指示を待つ** |

#### 是正ラウンド1（F-5）で追加した報告のみの2件

いずれも**作業指示の禁止事項により本タスクでは是正していない**。

| # | 箇所 | 実測した現状 | 是正しない理由 |
|---|---|---|---|
| F-5-1 | `testdata_examples.rst:502` | 同一 L2（`テストショット一覧（testShots）を記述する`）配下の L3 導入文で、数詞の形が3通りに割れている。`:198` = `…明細1件の正常注文と明細2件の大量注文という2つのテストショットを実行する例である。`、`:401`（是正後） = `正常ケースと認証エラーケースの2つのテストショットを実行する例である。`、`:502` = `データベースだけを処理するテストショットと、入力ファイルを使うテストショットの2件を実行する例である。`。**`:502` だけが `…の2件` で、`2つのテストショット` の形になっていない**（`:198` は `という2つの`、`:401` は `の2つの` で、これも助詞が揃っていない） | **曖昧さは無い**（`:502` は数える対象の `テストショット` を同じ文の中に2回明示している。§1-2 #9 の判定どおり）。作業指示 STEP 1 の禁止事項「**STEP 1 で他の導入文を変更しない。確認と報告にとどめる**」に従い、変更していない。是正するなら `:198`・`:401`・`:502` の3文をまとめて同じ形に揃えるべきであり、`:502` だけを直すと別の不揃いが生まれる |
| F-5-2 | `testdata_examples.rst:401`（およびページ全体） | `記述した ``LIST_MAP``` が**半角スペース区切り**のため HTML 描画で可視の空白が出る一方、同じ行の `` 。``context`` `` は空白なしで不揃いである。実測すると、全角文字の直後に半角スペースを置いて `` `` `` を始めている箇所が**51行・73箇所**、全角文字に直接続けている箇所が**46箇所**、`\ `（エスケープした空白）でつないでいる箇所が**24箇所**ある | **`#10b` 由来ではなく、承認済みの `#10` の成果に含まれる既存パターンである**（`:401` の是正は `2件` → `2つのテストショット` の置換だけで、バッククォート周りは触れていない）。本タスクの作業指示の範囲外であり、`#10b` の禁止事項「承認済みの `#8`・`#9`・`#10`・`#10a` の成果を、本書が指示していない範囲で変更しない」に該当するため変更していない。**申し送り候補として `reviews/page-testdata_examples.md` の申し送り21 に記録した**（残り32ページに波及するため、`\ ` に統一するかどうかの方針を `#11` 以降で決めるべきものである） |

---

## 2. STEP 2 — 記載例ページの特殊記法セクションに参照文を足す

### 2-1. 実施内容

| 項目 | 内容 |
|---|---|
| 位置 | `testdata_examples.rst:1851`（節ラベル `.. _testdata_examples-special_notation:` は `:1847`、見出し `null・空文字・改行など特殊な値を記述する` は `:1849`。いずれも `grep -n` で現在位置を特定） |
| 是正前 | `特殊記法を使う値の記述例を示す。記法の仕様は\ :ref:`null・空文字・改行など特殊な値を記述する <testdata_notation-special_notation>`\ を参照。` |
| 是正後 | 上記に続けて**1文**を追加: `ただし、見出しに挙げた値のうち、空文字・改行の記述例はここに置いていない。`（**是正ラウンド2〈G-1〉で書き直した後の文面。§2-4 が最新である**。初回の文面は `空文字・改行など、このページに例が無い値の種類の記法も、同じ参照先で説明している。` で事実誤りであった〈§2-3〉。是正ラウンド1 の文面は `` ただし、空文字・改行を単独で示した記述例は、この節には置いていない（記法は\ :ref:`テストデータの書き方 <testdata_notation>`\ で説明している）。 `` で、括弧内の参照先が粗すぎた〈§2-4〉） |
| 追加した文の数 | **1文**（作業指示「STEP 2 の追加は1文である」に一致）。既存の2文は1文字も変更していない |
| 見出し | **変更していない。** 両ページとも `null・空文字・改行など特殊な値を記述する` のまま（ゲート3で並び順・文言の不変を確認） |

### 2-2. 判断の記録（**初回の記録。是正ラウンド1〈F-1〉が一部を上書きしている。§2-3 が最新である**）

> **是正ラウンド1 による上書き。** 下表1行目は「(2) 6種のうち3種（空文字・先頭ゼロ付き数値・`true`・`false`）は他の節の例の中に値としては現れるため、**『このページに例が無い』と断定して列挙すると不正確になる**」と正しく述べておきながら、**その直後に追加文で `空文字` を断定文で列挙してしまった**。この自己矛盾が F-1 の `must` である。列挙しないという結論（1行目の前半）と、記法そのものをこのページに書かないという役割分担（3行目）は維持しているが、**「空文字・改行など、このページに例が無い」という文面と、`:ref:` を繰り返さないという判断（2行目）は §2-3 で覆した**（現在の追加文は書き方ページへの `:ref:` を1件持ち、主張の範囲を「この節」に限定している）。

| 論点 | 判断 |
|---|---|
| 例が無い値の種類を具体的に列挙するか | **列挙しない。「空文字・改行など」にとどめた。** 作業指示 STEP 2 は「例が無い値の種類を具体的に列挙するかは、STEP 3 の突合結果に従う」としており、突合（§3）は完了している。それでも列挙しない理由は次の2点である。(1) 突合の結果、例が無いのは**6種**（文字列の `null`／空文字／先頭ゼロ付き数値／`true`・`false`／文字種指定／改行文字）であり、6語を導入文に並べると `#10a` 回答 §2 が (c) で退けた「導入文が語の列挙で膨らむ」状態に戻る。(2) 6種のうち3種（空文字・先頭ゼロ付き数値・`true`・`false`）は**他の節の例の中に値としては現れる**（水準2。`checks/task-terminology.md` §11-2 R2-C-6）ため、「このページに例が無い」と断定して列挙すると不正確になる。**見出しが約束している語（空文字・改行）だけを挙げ、残りを `など` で受ける形が、見出し・本文・突合結果のいずれとも矛盾しない** |
| `:ref:` の書き方 | 追加した1文では `:ref:` を繰り返さず「同じ参照先で」と受けた。**直前の文が同じセクションへ `:ref:` でリンクしており、同一段落内で同じリンクテキスト（`null・空文字・改行など特殊な値を記述する`）を2回並べると読みにくくなるためである。** ゲート5（節に `:ref:` が1件以上あり `testdata_notation-special_notation` を指す）は満たしている（節内に2件。うち導入文が `:1851`） |
| 役割分担との整合 | 追加文は「記法の説明は書き方ページにある」と述べるだけで、記法そのものをこのページに書いていない。記載例＝実例、書き方＝記法の仕様という分担を崩していない |

**参照先ラベルの実在確認**:

```
$ grep -rn "_testdata_notation-special_notation:" ja/
ja/development_tools/testing_framework/implementation/testdata_notation.rst:1273:.. _testdata_notation-special_notation:
```

### 2-3. 是正ラウンド1（F-1）— 追加文の書き直し（**是正ラウンド2〈G-1〉が一部を上書きしている。§2-4 が最新である**）

> **是正ラウンド2 による上書き。** 下表の「是正後の文面」「満たした条件(3) 参照先ページの明示」「満たした条件(4) 情報構造」「ゲート5」の4行は、**是正ラウンド2〈G-1〉で覆した**。括弧書き（`（記法は\ :ref:`テストデータの書き方 <testdata_notation>`\ で説明している）`）は削除され、参照は2文目の `testdata_notation-special_notation` に一本化された。節内の `:ref:` は3件から**2件**に戻り、**うち2件とも `testdata_notation-special_notation` を指す**。また下表が「節の範囲は `:1847`〜`:1995`」としていた点も、是正ラウンド2 の実測で **`:1847`〜`:1993`** に訂正した（`:1994` は次節のラベル行）。条件(1)（断定しない）・(2)（この節に例が無いことの実測）・(5)（否定はひらがな）・(6)（1文・1行）と、見出し・2文目を変更しないという扱いは維持している。

4観点のレビュー（QA・設計・クラフト・検証）が独立に実施され、**4名全員が同一の `must` を検出した**。

| 項目 | 内容 |
|---|---|
| 何が誤りだったか | 初回に追加した3文目「空文字・改行など、**このページに例が無い**値の種類の記法も、同じ参照先で説明している。」が、**「空文字」に例が無いと断定していた点で公開本文の事実誤り**であった。空文字の記法（Excel＝空セル／YAML＝`""`。`testdata_notation.rst:1363`-`:1364`〈Excel〉・`:1415`-`:1416`〈YAML〉の記法表が定義）どおりの例が、同じページに実在する |
| 反例（実測） | `:710`・`:712`（Excel の空セル）と `:732`・`:734`（`orderStatus: ""`・`toDate: ""`）は、`LIST_MAPのデータを記述する` > `リクエストパラメータを記述する` の**同一データの Excel／YAML 対**であり、空文字を両形式で並べて示している。`:2064` の `空エントリを記述する` 節は、`:2066` の地の文が「全要素が null または**空文字**のエントリは読み飛ばされる」と述べ、`:2082`-`:2083` でその空セルを実演している。**ページ全体では Excel のデータ行の空セルが16箇所、YAML の `""` が12行**（全件は §3-2 の `空文字` 行） |
| **本書自身の自己矛盾** | §2-2 が「6種のうち3種（空文字・先頭ゼロ付き数値・`true`・`false`）は他の節の例の中に値としては現れるため、**『このページに例が無い』と断定して列挙すると不正確になる**」と書いた直後に、その `空文字` を断定文で列挙していた。**§3-2 の `空文字` の行が `:2082`・`:2083` を落としていたこと（F-4）が、この誤りに直結した** |
| 是正後の文面 | `` ただし、空文字・改行を単独で示した記述例は、この節には置いていない（記法は\ :ref:`テストデータの書き方 <testdata_notation>`\ で説明している）。 `` |
| 満たした条件(1) 断定しない | 「このページに例が無い」という断定を削除した。主張の範囲を**この節**に限定している |
| 満たした条件(2) この節に例が無いことの実測 | 節の範囲は `:1847`（ラベル）〜`:1995`（次の L2 見出し `:1996` の直前）で、配下は `日付・システム日時・NULLを記述する`／`スペース・ダブルクォートを記述する`／`バイナリデータを記述する` の3節。この範囲で (a) YAML の空文字リテラル `""` は**0件**（`""` を含む3行 `:1940`・`:1946`・`:1955` はいずれも**ダブルクォート1文字**の記法であって空文字ではない）、(b) `\r`・`\n` は**0件**、(c) list-table の空セルは `:1870`-`:1872`・`:1933`-`:1934`・`:1970` の6行のみで、**いずれもデータブロック識別子行（`* - EXPECTED_TABLE=SCHEDULE` / `* - EXPECTED_TABLE=ITEM` / `* - SETUP_TABLE=FILE_TABLE`）の埋めセルであり、データ行の値ではない**。ゲート5 に出力を掲載 |
| 満たした条件(3) 参照先ページの明示 | `:ref:`テストデータの書き方 <testdata_notation>`` でページを明示した（`testdata_notation.rst:1` の `.. _testdata_notation:` が実在ラベル）。初回の「同じ参照先で」は、直前の文が**節見出し**へのリンクで終わるため、どのページを指すか読み取れなかった |
| 満たした条件(4) 情報構造 | 主節を「**この節には置いていない**」（2文目に含意されていない新しい事実）とし、既出情報である「記法は書き方ページで説明している」を括弧内の補足に移した。**〈是正ラウンド2〈G-5-1〉で訂正〉初回の記録はこの括弧書きを「本ページの既存パターンである（`:436`・`:331`）」と書いていたが、これは言い過ぎであった。**`:436`・`:331` は実測すると `（\ :ref:`…`\ **参照**）` の形であり（`grep -n` で確認: `:331` = `` （\ :ref:`グループIDによる使い分け <testdata_notation-group_id>`\ 参照）``、`:436` = `` （\ :ref:`LIST_MAPのデータを記述する <testdata_examples-list_map>`\ 参照）``）、追加した括弧書きの `（記法は\ :ref:`…`\ **で説明している**）` とは**形が異なる**。括弧の中に主語と述語を持つ完全な文を入れる形は、本ページに前例が無かった。**この括弧書き自体を是正ラウンド2〈G-1〉で削除したため、条件(4) は「主節に新しい事実を置く」だけを指す**（是正ラウンド2 の文面は括弧を持たず、主節が「見出しに挙げた値のうち、空文字・改行の記述例はここに置いていない」である） |
| 満たした条件(5) 否定はひらがな | `置いていない`。**`無い` は使っていない**（`grep -n "無い\|無く\|無し"` で `testdata_examples.rst` は0件） |
| 満たした条件(6) 1文・1行 | 段落は1行のまま（ゲート8 で段落内改行0件） |
| 見出し・2文目 | **変更していない** |
| ゲート5 | 維持している。節内の `:ref:` は**3件**（`:1851` に2件・`:1859` に1件）で、うち `testdata_notation-special_notation` を指すものが**2件**（`:1851`・`:1859`）。追加した1件は書き方ページの**ページラベル** `testdata_notation` を指す |

### 2-4. 是正ラウンド2（G-1）— 括弧書きの削除（**最新**）

4観点のレビュー（QA・設計・クラフト・検証）を再実施した結果、QA・検証は pass、設計・クラフトが fail となり、**`must` は1件（G-1）に収束した**。

| 項目 | 内容 |
|---|---|
| 位置 | `testdata_examples.rst:1851`（3文目。1文目・2文目は変更していない） |
| 是正前（是正ラウンド1 の文面） | `` ただし、空文字・改行を単独で示した記述例は、この節には置いていない（記法は\ :ref:`テストデータの書き方 <testdata_notation>`\ で説明している）。 `` |
| **是正後（最新）** | `ただし、見出しに挙げた値のうち、空文字・改行の記述例はここに置いていない。` |
| 是正の理由(1) 参照先が粗い | 括弧内の `:ref:` の宛先が `testdata_notation`（**ページ先頭のラベル**。`testdata_notation.rst:1` の `.. _testdata_notation:`）であり、作業指示 STEP 2 が名指しした `testdata_notation-special_notation`（`testdata_notation.rst:1273`）ではなかった |
| 是正の理由(2) 二重案内 | 直前の2文目が既に `testdata_notation-special_notation` を精密に指しているのに、同じ段落でページ先頭という粗い宛先を重ねて案内していた。読者が実際に必要とする記法は `special_notation` 節の中にある（実測: 空文字は `testdata_notation.rst:1363`-`:1364`〈Excel〉／`:1415`-`:1416`〈YAML〉、改行文字は `:1387`-`:1389`〈Excel〉／`:1439`-`:1441`〈YAML〉）。`testdata_notation.rst` は全**1,520行**（末尾の空行を含む）で、`special_notation` 節はその末尾近くにあるため、ページ先頭へ送るのは案内として粗い |
| 是正の理由(3) 情報の部分集合 | 括弧の中身（「記法は書き方ページで説明している」）は2文目の情報の部分集合であり、削除しても情報が失われない |
| 語の是正 | `単独で示した` を落とした（この限定語は本文のどこにも定義が無く、読者が線引きを判定できない）。代わりに `見出しに挙げた値のうち` で範囲を示した。`この節` は `ここ` に改めた（`この節`・`本節`・`この項` は NTF解説書・FW解説書ライブラリのいずれにも用例が無く、この追加文が初出であった） |
| 1文・1行 | 維持している。段落内で改行していない（ゲート8） |
| 見出し・2文目 | **変更していない** |

#### 節の範囲の確定（**レビュアー間で `:1993` と `:1995` に割れていたため機械判定した**）

ラベル行・見出し行・下線行の位置を実測して確定させた。**この節の範囲は `:1847` 〜 `:1993` である。**

```
$ python3 scratchpad/g1.py
LABEL start line = 1847        （.. _testdata_examples-special_notation:）
next label line  = 1994        ['.. _testdata_examples-comment_and_marker:']
next L2 heading  = (1850, '-', 'null・空文字・改行など特殊な値を記述する')   ← 見出し文言は :1849、下線は :1850
=> SECTION RANGE = 1847 .. 1993 (next label at 1994)
```

```
$ awk 'NR>=1845 && NR<=1852' testdata_examples.rst
1845|            - ["RESULT_OK"]
1846|
1847|.. _testdata_examples-special_notation:
1848|
1849|null・空文字・改行など特殊な値を記述する
1850|--------------------------------------------------
1851|特殊記法を使う値の記述例を示す。記法の仕様は\ :ref:`…<testdata_notation-special_notation>`\ を参照。ただし、見出しに挙げた値のうち、空文字・改行の記述例はここに置いていない。
1852|
$ awk 'NR>=1992 && NR<=1997' testdata_examples.rst
1992|          FILE_DATA: "${binaryFile:testdata.bin}"
1993|                                  ← 空行。この節の最終行
1994|.. _testdata_examples-comment_and_marker:   ← 次節のラベル
1995|
1996|コメント・マーカーカラム・空エントリを扱う
1997|--------------------------------------------------
```

**是正ラウンド1 までの記録は終端を `:1995` としていたが、`:1994`・`:1995` は次節のラベル行とその直後の空行であり、この節には属さない。** 本書の §2-3・§5b・ゲート5 の該当箇所を `:1847`〜`:1993` に統一した。配下の L3 は `日付・システム日時・NULLを記述する`（`:1854`）／`スペース・ダブルクォートを記述する`（`:1921`）／`バイナリデータを記述する`（`:1958`）の3節で変わらない。

#### 新しい文の主張に対する反証（**実測。ゲート5 に出力を掲載**）

確定した範囲 `:1847`〜`:1993` で反証を試みた。**反例は1件も見つからなかった。**

| 反証の観点 | 結果 |
|---|---|
| 空文字リテラル `""` | **0件。** `""` を含む3行（`:1940` `` ``"""`` ``／`:1946` の地の文／`:1955` `MEMO: "\""`）は、いずれも**ダブルクォート1文字**の記法であって空文字ではない |
| 改行文字 `\r`・`\n` | **0件** |
| list-table の空セル | 6件（`:1870`・`:1871`・`:1872`・`:1933`・`:1934`・`:1970`）。**いずれもデータブロック識別子行の埋めセル**であり、データ行の値ではない（`:1869` `* - EXPECTED_TABLE=SCHEDULE`／`:1932` `* - EXPECTED_TABLE=ITEM`／`:1969` `* - SETUP_TABLE=FILE_TABLE` の続き。§3-2 の分類 A に当たる） |
| YAML の空値（`key:` の後が空） | **0件**（`:1900`・`:1902`・`:1950`・`:1952`・`:1986`・`:1988` は `expected_tables:`・`rows:`・`setup_tables:` というマッピングのキーであり、値の空ではない） |

---

## 3. STEP 3 — 値の種類の突合

`#10a` の §10 G（`checks/task-terminology.md`）で実施済みであるが、作業指示 §3 Method に従い**独立に再実施**した。抽出も突合も、記録を参照せずに実ファイルから行っている。

### 3-1. 母集団の抽出（`testdata_notation.rst` の両表）

`null・空文字・改行など特殊な値を記述する`（`:1275`）配下の `Excel形式の場合`（表: `:1347`〜`:1389`）と `YAML形式の場合`（表: `:1399`〜`:1441`）の「値の種類」列を機械抽出した（スクリプト `scratchpad/kinds.py`）。**両表ともヘッダ行1＋データ行12 で、値の種類の集合は完全に一致する。**

作業指示が挙げる**10種**は、この12種から `通常の文字列` と `${systemTime}`・`${updateTime}`・`${setUpTime}` を除いたものである。**下表は12種すべてを載せる**（10種には「指示10種」列に ○ を付す）。

### 3-2. 突合の全件表（**全12件**）

#### 判定基準（**是正ラウンド1〈F-4〉で明記**）

| 項目 | 内容 |
|---|---|
| **「例あり」の基準** | **その値の種類を主題として示した専用の例が記載例ページにあるか。** すなわち、その値の記法を読者に見せることを目的として置かれた表・コードブロックがあるかどうかで判定している。**値としての出現回数では判定していない** |
| **「専用の例なし（値としては出現）」の意味** | その値の書き方どおりの文字列がページ上に**実在する**が、それは別の主題の例（テストショット一覧・リクエストパラメータ・空エントリなど）に付随して現れているだけで、その値の記法を示すために置かれた例ではない、という状態である。**「ページ上に存在しない」という意味ではない** |
| **誤読への注意** | この基準のため、`先頭ゼロ付き数値`（Excel `001`＝`:1973`・`:1975`、YAML `"001"`＝`:1989`）や `true`・`false`（Excel＝`:425`・`:431`、YAML＝`:479`・`:485`）のように、**書き方ページの「記述方法」セルと完全一致する形でページ上に実在するもの**も「専用の例なし」の側に並ぶ。`空文字` も同様である（下表の該当行に全出現箇所を列挙した）。**表の「例なし」を「ページのどこにも書かれていない」と読まないこと** |
| ページ全体で0件のもの | `文字列の null`・`${文字種,文字数}`・`改行文字` の3種だけが、値としての出現も0件である（#3・#11・#12） |

#### 全件表

| # | 値の種類（`notation` の行） | 指示10種 | `testdata_examples.rst` の例 | 実測（`grep`） | 判定 |
|---|---|---|---|---|---|
| 1 | 通常の文字列 | — | **あり** | ページ全体の全表・全コードブロック（例: `:424` `正常ケース`、`:1204` `["001", "山田太郎", "5000"]`） | 例あり |
| 2 | null（データベースに `null` を格納） | ○ | **あり** | `:813` `- Null`（Excel）／`:857` `SCORE: null`（YAML）／`:1883`-`:1884` `- NULL`（Excel）／`:1909`-`:1910` `START_DATE: null`・`CREATED_AT: null`（YAML）。特殊記法節の `日付・システム日時・NULLを記述する` が専用の例 | 例あり |
| 3 | 文字列の `null`（`"null"`） | ○ | **なし** | `grep -n '"null"'` のヒットは `:862` の説明文1件のみ（「``"null"`` とクォートした場合も同じ結果になる」）。値としての出現0件 | **出典なしのため例を作らない**（§3-3） |
| 4 | 空文字 | ○ | **専用の例なし**（値としては**多数**出現） | **是正ラウンド1（F-4）で実測しなおした全件。** **Excel（データ行の空セル、16箇所）**: `:61`・`:62`（`testShots` の `setUpFile`・`expectedFile`）／`:544`（`testShots` の `setUpFile`）／`:710`・`:712`（`requestParams` の `orderStatus`・`toDate`）／`:1242`（複数レコードレイアウトの `FILLER`）／`:1633`・`:1635`・`:1637`（`MESSAGE=setUpMessages`）／`:1664`・`:1667`・`:1668`（`MESSAGE=expectedMessages`）／`:1783`・`:1787`（`RESPONSE_BODY_MESSAGES` の `errorMode`）／**`:2082`・`:2083`（`空エントリを記述する` 節が意図的に置いた空データ行）**。**YAML（`""`、12行）**: `:153`・`:154`／`:565`／`:732`・`:734`／`:1276`／`:1690`・`:1691`／`:1700`・`:1701`／`:1806`・`:1807`。**Excel と YAML は同一データの対になっている**（`:61`/`:62`↔`:153`/`:154`、`:544`↔`:565`、`:710`/`:712`↔`:732`/`:734`、`:1242`↔`:1276`、`:1633`/`:1635`/`:1637`↔`:1690`/`:1691`、`:1664`/`:1667`/`:1668`↔`:1700`/`:1701`、`:1783`/`:1787`↔`:1806`/`:1807`。`:2082`・`:2083` は YAML 側に対応物が無い＝`:2089` が「YAML 形式では ``rows:`` に空のエントリを書く機会はほとんどない」と述べているため）。**除外した空セルの全種類は §3-2a に列挙した**（是正ラウンド1 の記録は除外条件を「データブロック識別子行の埋めセル」の1種類しか宣言していなかったが、実際には4種類を除外していた。是正ラウンド2〈G-3〉で全種類を明記。**16箇所という結論は変わらない**）。上記はいずれも他の主題の例に付随するものであり、「空文字をこう書く」と主題にした専用の例は0件 | **本ページに割り当てられた出典に専用の例が無いため作らない**（§3-3。**ただし出典そのものは存在し、その値は本文に反映済みである**）。**ページ上に空文字の例は多数実在する**（G-1 の本文はこの事実と矛盾しない） |
| 5 | 先頭ゼロ付き数値 | ○ | **専用の例なし**（値としては出現） | Excel: `:703`・`:709` `0000000101`／`0000000102`、`:1178`・`:1182`・`:1256`・`:1322`・`:1326`・`:1444`・`:1973`・`:1975`・`:2080` `001`／`002`、`:2028` `0000000001`。YAML: `:725`・`:731` `"0000000101"`、`:1989`・`:1991` `"001"`／`"002"`、`:2054` `"0000000001"` | **出典なしのため専用の例を作らない**（§3-3）。**ただし書き方ページの「記述方法」セル（Excel `001` / YAML `"001"`）と完全一致する形が `:1973`・`:1975`・`:1989` に実在する**（`バイナリデータを記述する` 節の `FILE_ID`） |
| 6 | `true`・`false`（文字列） | ○ | **専用の例なし**（値としては出現） | Excel: `:425` `true`／`:431` `false`。YAML: `:479` `isValidToken: "true"`／`:485` `isValidToken: "false"`。いずれもウェブアプリケーションのテストショット一覧の `isValidToken` カラムに付随するもの | **出典なしのため専用の例を作らない**（§3-3）。**ただし書き方ページの「記述方法」セル（Excel `true` / YAML `"true"`）と完全一致する形が `:425`・`:431`・`:479`・`:485` に実在する** |
| 7 | 半角スペース1文字 | ○ | **あり** | `:1939` `` ``" "`` ``（Excel セル）／`:1954` `NAME: " "`（YAML）。特殊記法節の `スペース・ダブルクォートを記述する` が専用の例 | 例あり |
| 8 | ダブルクォート1文字 | ○ | **あり** | `:1940` `` ``"""`` ``（Excel セル）／`:1946` の地の文と `:1955` `MEMO: "\""`（YAML）。同上 | 例あり |
| 9 | `${systemTime}`・`${updateTime}`・`${setUpTime}` | — | **あり** | Excel: `:1887`・`:1888` `${systemTime}`、`:1891` `${updateTime}`、`:1892` `${setUpTime}`。YAML: `:1913`・`:1914`・`:1917`・`:1918` | 例あり |
| 10 | `${binaryFile:パス}` | ○ | **あり** | `:830`・`:834`（Excel、`SETUP_TABLE`）／`:853`・`:860`（YAML）／`:1976`（Excel、バイナリ節）／`:1992`（YAML） | 例あり |
| 11 | `${文字種,文字数}` | ○ | **なし（ページ全体で0件）** | `grep -cE '\$\{文字種\|半角英字\|半角数字\|半角記号\|全角英字\|全角数字\|全角ひらがな\|全角カタカナ\|全角漢字\|全角記号\|中国語\|サロゲートペア\|外字'` → `0` | **出典なしのため例を作らない**（§3-3） |
| 12 | 改行文字（`\r`・`\n`） | ○ | **なし（ページ全体で0件）** | `grep -nF '\r'` → 0件、`grep -nF '\n'` → 0件。`:1354` の「CRLF 改行のファイル」は可変長ファイルのディレクティブ（`record-separator`）の説明であり、値としての改行文字ではない | **出典なしのため例を作らない**（§3-3） |

**判定の内訳: 例あり6件（#1・#2・#7・#8・#9・#10）／専用の例を作らない6件（#3・#4・#5・#6・#11・#12）／出典があるのに落ちている（`must`）**: **0件**。

**〈是正ラウンド2〈G-2〉で訂正〉「専用の例を作らない6件」の理由は一様ではない。** 6種のうち**4種（#3 文字列の `null`／#4 空文字／#5 先頭ゼロ付き数値／#6 `true`・`false`）には該当する出典が65行の中に実在し、その内容は本文に反映済みである**（専用の例として立てられていないだけ）。**出典そのものが皆無なのは #11 `${文字種,文字数}` と #12 改行文字の2種だけである。** 種別ごとの内訳は §3-3 の表を参照。

### 3-2a. 空文字の列挙から除外した空セルの全種類（**是正ラウンド2〈G-3〉で追加**）

`testdata_examples.rst` の全 `list-table`（**49件**。うち識別子行を持つテストデータの表が47件、説明用の表が2件）をパースし、`* - `／`- ` で始まるセル行を行単位に組み直して空セルを全件分類した（スクリプト `scratchpad/g3b.py`）。**空セルは全部で210箇所あり、そのうち194箇所を4種類の理由で除外し、残る16箇所を「データ行の空セル（値としての空文字）」としている。**

| 分類 | 内容 | 該当行数 | 空セル数 | 扱い |
|---|---|---|---|---|
| A | **データブロック識別子行の埋めセル** — `* - LIST_MAP=testShots`・`* - SETUP_TABLE=MEMBER` のように識別子だけを持つ行の2列目以降（`style.md` S-10 規約2 が「2列目以降は空セルにする」と定めている） | 47 | **126** | 除外 |
| B | **データ行の先頭ラベル空セル** — ラベル列を持つデータタイプ（`SETUP_FIXED`・`EXPECTED_FIXED`・`SETUP_VARIABLE`・`EXPECTED_VARIABLE`・`MESSAGE`・`EXPECTED_REQUEST_*_MESSAGES`・`RESPONSE_*_MESSAGES`）で、データ型行・フィールド長行・データ行の先頭要素を空にしたもの。`:1055` が「データ行の先頭要素は空にする」、`:1596` が「メッセージボディの先頭要素はラベル列であり」と明示している | 42 | **42** | 除外 |
| C | **ディレクティブ行・フレームワーク制御ヘッダ行（「名前・値」形式の行）の余白セル** — `text-encoding`・`field-separator`・`record-separator`・`quoting-delimiter`・`positive-zone-sign-nibble`（ディレクティブ）と `requestId`・`userId`（フレームワーク制御ヘッダ）。`testdata_notation.rst:1263` が「Excel 形式ではどちらも「名前・値」の行」と述べており、3列目以降は表の幅を揃えるための余白である | 10 | **18** | 除外 |
| D | **コメント行の余白セル** — `// この行はコメントです`（`:2017`）・`// この行もスキップされます`（`:2032`）の2列目以降 | 2 | **8** | 除外 |
| E | **データ行の空セル（値としての空文字）** | 11 | **16** | 列挙対象 |
| X | 説明用 `list-table`（識別子行を持たない2件）の空セル | 0 | **0** | — |
| | **合計** | | **210** | |

**E の全16箇所**: `:61`・`:62`・`:544`・`:710`・`:712`・`:1242`・`:1633`・`:1635`・`:1637`・`:1664`・`:1667`・`:1668`・`:1783`・`:1787`・`:2082`・`:2083`。**是正ラウンド1 の記録と完全に一致する（16箇所という結論は変わらない）。**

**分類の境界で注意した2点**（いずれも「先頭セルが空かどうか」だけでは判定できない）:

- **`:1239`-`:1242`**（`SETUP_FIXED=input/multi.dat` の `HEADER` レコードのデータ行）は、先頭セル `:1239` が**ラベル列の空セル（B）**である一方、同じ行の `:1242` は `FILLER` カラムの**値としての空文字（E）**である。1行が B と E の両方を含む唯一の例である
- **`:2082`-`:2083`**（`空エントリを記述する` 節の `SETUP_TABLE=USER`）は先頭セルも空だが、この表はラベル列を持たないデータタイプ（`SETUP_TABLE`）であり、1列目は `USER_ID` という実データのカラムである（`:2078` がカラム名行、`:2080` `001`／`:2084` `002` がデータ行）。したがって**2セルとも E** である

### 3-3. 「出典が無い」の確認（`mapping.csv` の65行）

`csv.DictReader` で `mapping/mapping.csv` を読み、`dest_page=='テストデータの記載例'` の行を数えた（**`wc -l` は使っていない**。`mapping.csv` は読むだけで書き換えていない）。

```
$ python3 -c "import csv; rows=list(csv.DictReader(open('mapping/mapping.csv',encoding='utf-8'))); \
              print(len(rows), len([r for r in rows if r['dest_page']=='テストデータの記載例']))"
594 65
```

65行のうち、特殊記法・特殊値を扱う出典は次の**3件のみ**である。

| `mapping_id` | `src_file`:行 | `heading_path` 末尾 |
|---|---|---|
| `input-0083` | `.rn/20260724-ntf-yaml-support/input/ntf-testdata-doc-examples-special.md`:10-58 | `8.1 日付型・Timestamp・特殊値` |
| `input-0084` | `.rn/20260724-ntf-yaml-support/input/ntf-testdata-doc-examples-special.md`:60-87 | `8.2 QuotationTrimmer によるスペース値明示記法` |
| `input-0085` | `.rn/20260724-ntf-yaml-support/input/ntf-testdata-doc-examples-special.md`:89-120 | `8.3 バイナリデータ` |

（**`src_file` 列は、是正ラウンド2〈G-5-2〉で `mapping.csv` 上の実値に直した。** 是正ラウンド1 までは `input/ntf-testdata-doc-examples-special.md` と短縮して書いていたが、CSV の値はリポジトリルートからの相対パスである。`src_body_start`-`src_body_end` 列も実値を掲げている。）

この3件は記載例ページの既存3節（`日付・システム日時・NULLを記述する`／`スペース・ダブルクォートを記述する`／`バイナリデータを記述する`）と**1対1に対応する**。突合表の「例あり」6件は、この3件の出典と、他の節に付随して現れる `通常の文字列`・`${systemTime}` 系で説明が付く。

#### 例が無い6種の出典の有無（**是正ラウンド2〈G-2〉で判定しなおした**）

**是正ラウンド1 までの記録は「例が無い6種に該当する出典は、65行の中に1件も無い」と書いていたが、これは誤りである。** 65行それぞれの `src_file` を `src_body_start`-`src_body_end` の範囲で開いて機械走査したところ（スクリプトは `csv.DictReader` で65行を取り出し、各行の本文を6種のパターンで検索するもの）、**6種のうち4種には該当する出典が実在した。**

| # | 値の種類 | 65行の中の出典 | 出典の実体（実測） | 本文への反映 | 判定 |
|---|---|---|---|---|---|
| 3 | 文字列の `null`（`"null"`） | **あり（2件）** | `input-0083`（`…special.md:49`）「YAML ではアンクォートの `null` で記述し、`"null"` とクォートすると文字列として格納される」／`input-0095`（`…table.md:50`）「NULL 値はアンクォートの `null`。`"null"` とクォートすると文字列として格納される」。**いずれも制約の説明文であって、記述例（表・コードブロック）ではない** | **反映済み。** `testdata_examples.rst:862` が地の文としてこれを受けている（申し送り17 の是正を織り込んだうえで） | **出典はあるが、記述例としての出典ではない。専用の例を作らない** |
| 4 | 空文字 | **あり（複数件）** | `input-0097`（`…table.md`）の `LIST_MAP=searchParams` の例 — Excel `:138` のデータ行が `orderStatus`・`toDate` の2セルを空にしている（Markdown 表記の該当行は `2` / `0000000102` / 空 / `2024-01-01` / 空 / `全件検索`）、YAML `:162` `orderStatus: ""`・`:164` `toDate: ""`。ほかに `input-0065`（`…messaging.md:100`-`:102` `setUpTable: ""`・`expectedTable: ""`・`expectedLog: ""`）、`input-0078`（`…overview.md:47`-`:48` `setUpFile: ""`・`expectedFile: ""`）、`input-0106`（`…testshots.md:185` `setUpFile: ""`）、`input-0089`（`…special.md:249`「全要素が null または空文字のエントリは読み飛ばされる」＋ `:257` の全セル空の行）。**いずれも他の主題（リクエストパラメータ・テストショット一覧・空エントリ）の例に値として現れるもので、「空文字をこう書く」を主題にした出典ではない** | **反映済み。** `input-0097` は `:708`-`:712`（Excel）・`:732`・`:734`（YAML）に、`input-0078` は `:61`・`:62`・`:153`・`:154` に、`input-0089` は `:2082`・`:2083` にそのまま現れる | **出典はあるが、専用の例の出典ではない。専用の例を作らない** |
| 5 | 先頭ゼロ付き数値 | **あり（複数件）** | `input-0095`（`…table.md:20`・`:21` `0000000101`／`0000000102`、`:32`・`:39` `MEMBER_ID: "0000000101"`、`:49`「値は文字列で記述（`"0000000101"` のようにクォート）」）、`input-0097`（`…table.md:137`・`:155` ほか）、`input-0085`（`…special.md:97`・`:106` `001`／`"001"`）、`input-0089`・`input-0091`・`input-0042`・`input-0043` ほか多数 | **反映済み。** `:703`・`:709`・`:725`・`:731`（`0000000101`／`0000000102`）、`:1973`・`:1975`・`:1989`（`001`／`"001"`）ほか | **出典はあるが、専用の例の出典ではない。専用の例を作らない** |
| 6 | `true`・`false`（文字列） | **あり（1件）** | `input-0103`（`…testshots.md:101`-`:102` の Excel `true`／`false`、`:117`・`:123` の YAML `isValidToken: "true"`／`"false"`） | **反映済み。** `:425`・`:431`（Excel）・`:479`・`:485`（YAML） | **出典はあるが、専用の例の出典ではない。専用の例を作らない** |
| 11 | `${文字種,文字数}` | **無し（0件）** | 65行の本文に `${文字種` も14種の文字種名も1件も現れない。文字種生成を扱う出典は `input-0148`（`8.1 値の種類と Excel / YAML 対比`）・`input-0150`（`8.3 インタープリタ一覧`）・`input-0152`（`8.5 文字種生成の有効文字種`）・`input-0171`・`current-0174`（`セルへの特殊な記述方法`）で、**すべて `dest_page=テストデータの書き方`** である（`input-0006` のみ `dest_page` が空で `disposition=DROP`） | — | **出典が皆無のため例を作らない** |
| 12 | 改行文字（`\r`・`\n`） | **無し（0件）** | 65行の本文に値としての `\r`・`\n` は1件も現れない。唯一「改行」の語を含むのは `input-0087`（`…special.md:166`「タブ区切り・CRLF 改行のファイルを扱うケース」）だが、これは `record-separator` によるファイルの改行コード指定であって値の記法ではない。値としての改行文字を扱う出典は `input-0148`（`\r`・`"\r"`）・`input-0150`（`LineSeparatorInterpreter`）で、いずれも `dest_page=テストデータの書き方` である | — | **出典が皆無のため例を作らない** |

**したがって「出典が皆無」と言えるのは #11 と #12 の2種だけである。** #3・#4・#5・#6 は「**出典に該当する記述はあるが、その値の記法を主題にした専用の例としては採られていない**」が正しい書き方であり、しかも**出典の内容自体は本文に反映済み**である（値として、あるいは地の文の制約として）。

**改行・空文字という語を含む記載例ページ側の2行**（`input-0087` `9.2 可変長ファイルのディレクティブ`、`input-0089` `10.2 空エントリのスキップ`）は `note` と本文の実物を読んで内容を確認した。前者は `record-separator` によるファイルの改行コード指定、後者はエントリ全体が空のときの読み飛ばし挙動であり、**いずれも「値として改行文字をどう書くか」ではない**。かつ両者は既に `testdata_examples.rst` の `可変長ファイルのディレクティブを指定する`・`空エントリを記述する` として実現済みである。

**結論: `must`（出典があるのに落ちている）は0件である。この結論は是正ラウンド2 の再判定でも変わらない。** #3〜#6 は出典の内容が本文に反映済みであり落丁ではない。#11・#12 は出典が `dest_page=テストデータの書き方` に割り当てられており、これは `mapping.csv` の割当の問題である。`mapping.csv` は本タスクの変更禁止対象であり、**本タスクでは例を追加していない。**

---

## 4. STEP 4 — 定義セルから括弧書きを落とす

| 項目 | 内容 |
|---|---|
| 位置 | `testdata_notation.rst:108`（`データブロックとデータタイプ` の `用途`＝`テストショット` 行の `内容` セル。`grep -n` で現在位置を特定。指示作成時点と同じ `:108`） |
| 是正前 | `リクエスト送信・バッチ起動などの1回の実行を表すエントリ（番号・説明・期待するステータスコード）` |
| 是正後 | `リクエスト送信・バッチ起動などの1回の実行を表すエントリ` |
| 言い換え | **していない。括弧書きを削除しただけで、主文は1文字も変えていない**（作業指示「言い換えて残さない」） |
| 記載例ページ側 | 同じ括弧書きは `testdata_examples.rst` に**存在しなかった**ため、変更していない（ゲート6 で両ページ0件を確認） |
| 語釈の維持 | 主文に `リクエスト送信・バッチ起動などの1回の実行` が残るため、`page-testdata_notation.md` 申し送り24 が求める「新語 `テストショット` の語釈が本文に残ること」は満たしている |

---

## 5. STEP 5 — 記録と申し送り

1. 本書 `checks/task-10a-followup.md` を新規作成した（**コミットしない。作業ツリーに残す**）
2. `reviews/page-testdata_notation.md`・`reviews/page-testdata_examples.md` の双方に `## #10b（…）` 節を**追記**した。既存の記録は1行も書き換えていない
3. 残り32ページへの申し送り2件を追加した
   - `page-testdata_notation.md` 申し送り**28**（`page-testdata_examples.md` 申し送り**19**）— 完了条件が「全件表」を求めている項目は、ゲートの実行順の先頭に置く
   - `page-testdata_notation.md` 申し送り**29**（`page-testdata_examples.md` 申し送り**20**）— 概観表・定義セルに具体的なカラム名を列挙しない

### 5-1. 是正ラウンド1 で追加した記録・申し送り

いずれも**末尾追記のみ**であり、既存の行は1行も書き換えていない。

| 追記先 | 内容 |
|---|---|
| `reviews/page-testdata_examples.md` | `## #10b` 節の末尾に `### #10b 是正ラウンド1（2026-08-07）` を追加。F-1 の本文の是正内容（是正前・是正後の文面、事実誤りであった理由と反例、是正後の文が満たす6条件）と、F-2・F-3・F-4 の記録の是正を表で記載した |
| `reviews/page-testdata_examples.md` 申し送り**21** | **F-5-2**。インラインリテラル `` `` `` の前の区切りが半角スペース（51行・73箇所）／直結（46箇所）／`\ `（24箇所）に割れている。`#10` の成果に含まれる既存パターンであり本タスクの範囲外のため変更していない。`#11` 以降で統一方針を決めること |
| `reviews/page-testdata_notation.md` 申し送り**30** | **F-6**。`mapping/glossary.md` §5.7（`:201`）が「3つの用途を対比する表では `実行条件` 側の言い方を使う」と定めているのに対し、`testdata_notation.rst:108` の現行セルは `実行条件` 側の言い方になっていない。**`glossary.md` は変更禁止のため申し送りに記録した。** 用語集の記述を実物で読んで引用を照合済み（読むだけで変更していない）。`#11` 以降で (a) 用語集を現状に合わせる か (b) セルを `実行条件` 側に戻す かを判断すべきものである |

**F-6 を `page-testdata_examples.md` に置くかどうか**: 置いていない。内容が `testdata_notation.rst` の定義セル固有であり、記載例ページ側に同じ表・同じセルが存在しないためである（ゲート6 で `testdata_examples.rst` に括弧書きが0件であることを確認済み。STEP 4 の対象も `testdata_notation.rst:108` のみであった）。

---

## 5b. 是正ラウンド1（2026-08-07）の是正結果 — F-1〜F-6

4観点のレビュー（QA・設計・クラフト・検証）が独立に実施され、**4名全員が同一の `must`（F-1）を検出した**。

| # | 区分 | 内容 | 是正結果 | 記録先 |
|---|---|---|---|---|
| **F-1** | `must` | `testdata_examples.rst:1851` の3文目「空文字・改行など、**このページに例が無い**値の種類の記法も、同じ参照先で説明している。」が公開本文の事実誤り（空文字の例はページに多数実在する） | **是正した。** `` ただし、空文字・改行を単独で示した記述例は、この節には置いていない（記法は\ :ref:`テストデータの書き方 <testdata_notation>`\ で説明している）。 `` に書き直した。1文・1行、見出しと1〜2文目は不変。`無い` → `置いていない`（ひらがな）。参照先ページを `:ref:` で明示。主節に新しい事実を置いた。**節の範囲 L1847〜L1995 に空文字・改行の例が無いことを `grep` で実測し、反例が0件であることを確認済み** | §2-3、ゲート5 |
| **F-2** | `should` | STEP 1 の全件表の母集合が申告どおりでない（地の文の数詞3件＋αが抽出条件から漏れていた） | **是正した。** 抽出条件（母集団に含める／除外する対象、助数詞リスト、数詞部の定義）を表の冒頭に明記し、**散文ディレクティブ本体を母集団に加えて再抽出**した。**25行→29行**。追加は `:957`（`.. tip::` 本体の `1度`）・`:1213`（`.. important::` 本体の `1つ`・`40バイト`）・`:1262`（助数詞なしの `複数`）・`:1565`（`0バイト`）の4行で、**いずれも曖昧ではなく本文の是正は不要**。散文ディレクティブ10件を全走査し、他に漏れが無いことを確認した | §1-2、ゲート7 |
| **F-3** | `should` | 全件表 #19（現 #22、`:1471`）の行番号引用が3件とも実ファイルと合わない | **是正した。** `10001`→`:1498`、`20001`→`:1525`、`20002`→`:1529`（旧記録の `:1489`・`:1521`・`:1527` はそれぞれ `* -`・`- 5`・`- morning`）。**判定〈1件 vs 2件〉は変わらない** | §1-2 #22 と直後の注記、ゲート7 |
| **F-4** | `should` | STEP 3 突合表の判定基準が表に書かれていない／`空文字` の空セルの列挙が不完全 | **是正した。** 判定基準（「その値の種類を**主題として示した専用の例**があるか」であり、値としての出現回数では判定していないこと）を表の冒頭に明記した。`空文字` の行を実測しなおし、**Excel のデータ行の空セル16箇所・YAML の `""` 12行を全件列挙**（初回が落としていた `:2082`・`:2083` を含む）。`先頭ゼロ付き数値`・`true`／`false` の行にも、書き方ページの「記述方法」セルと完全一致する形が実在することを明記した。**F-1 の是正後の本文とこの表は矛盾しない**（本文は「この節に置いていない」、表は「ページ上に多数実在するが専用の例ではない」） | §3-2 |
| **F-5** | `note` | (1) `:502` の数詞の形が兄弟と不揃い、(2) `:401` のインラインリテラル前の半角スペース | **是正していない**（作業指示の禁止事項による）。理由とともに §1-3 に記録した。(2) は申し送り21 にも記録した | §1-3（F-5-1・F-5-2） |
| **F-6** | `should` | `glossary.md` §5.7 が定めた `実行条件` 側の言い方と、`testdata_notation.rst:108` の現行セルの食い違い | **申し送りに記録した**（`glossary.md` は変更禁止）。`glossary.md` §5.7（`:201`）を実物で読んで引用を照合済み。**読むだけで変更していない** | `reviews/page-testdata_notation.md` 申し送り30、§5-1 |

**本文（`ja/` 配下）の変更は F-1 の1行だけである。** `testdata_notation.rst` は触っていない（`git diff --numstat 699dfd0 -- ja/` が `b2f616a` 時点と同じ 2/2・1/1）。`testdata_examples.rst:401` にも触っていない。

---

## 5c. 是正ラウンド2（2026-08-07）の是正結果 — G-1〜G-7

4観点のレビュー（QA・設計・クラフト・検証）を再実施した結果、**QA・検証は pass、設計・クラフトが fail**。`must` は**1件（G-1）**に収束した。

| # | 区分 | 内容 | 是正結果 | 記録先 |
|---|---|---|---|---|
| **G-1** | `must` | `testdata_examples.rst:1851` の3文目の括弧書きが、(1) 作業指示 STEP 2 が名指しした `testdata_notation-special_notation` ではなくページ先頭ラベル `testdata_notation` を指している、(2) 直前の2文目が既に精密に指している宛先を粗い宛先で重ねて案内している、(3) 括弧の中身が2文目の情報の部分集合である、の3点。加えて `単独で示した` という限定語が本文のどこにも定義されていない | **是正した。** 括弧書きごと削除し、参照を2文目に一本化した。`ただし、見出しに挙げた値のうち、空文字・改行の記述例はここに置いていない。`。`単独で示した` を落として `見出しに挙げた値のうち` に、`この節` を `ここ` に改めた。1文・1行、見出しと1〜2文目は不変。**節の範囲を機械判定して `:1847`〜`:1993` に確定させ、この範囲に空文字の値・`\r`・`\n` が0件であることを実測（反例0件）** | §2-4、ゲート5 |
| **G-2** | `should` | §3-3 の「例が無い6種に該当する出典は65行の中に1件も無い」が反証されている（QA と検証で指摘内容が食い違っていた） | **是正した。** 65行それぞれの `src_file` を `src_body_start`-`src_body_end` の範囲で開いて機械走査し、6種を個別に判定しなおした。**出典が皆無なのは `${文字種,文字数}` と改行文字の2種のみ**で、文字列の `null`・空文字・先頭ゼロ付き数値・`true`／`false` の4種には出典が実在し、内容も本文に反映済みである。QA の指摘（`input-0097`・`input-0095`）も検証の指摘（`input-0083` の制約と `:862`）も**どちらも成立していた**（両者は互いに排他ではなく、別々の出典を指していた）。**「出典があるのに落ちている（`must`）は0件」という結論は変わらない** | §3-2 の内訳注記、§3-3 |
| **G-3** | `should` | §3-2 の `空文字` の除外条件が「データブロック識別子行の埋めセル」1種類しか宣言していないが、実測では他にも除外されている | **是正した。** 全49件の `list-table` をパースして空セル**210箇所**を全件分類し、除外の全4種類（A 識別子行の埋めセル126／B データ行の先頭ラベル空セル42／C ディレクティブ行・フレームワーク制御ヘッダ行の余白セル18／D コメント行の余白セル8）を §3-2a に明記した。**16箇所という結論は変わらない**（E の16箇所は是正ラウンド1 の列挙と完全一致）。QA と検証が挙げた行番号の食い違いは、`:1239`-`:1242`（B と E が同居する唯一の行）と `:2082`-`:2083`（ラベル列を持たない表のため先頭セルも E）の2箇所の扱いに起因していた | §3-2a |
| **G-4** | `note` | STEP 1 の全件表に、助数詞リスト外の漏れがある（`加算`・`重`・語順が逆の序数） | **是正した。表に追加した（29行→35行）。** 追加は `:99`（`1加算`）・`:245`・`:264`・`:283`・`:306`（`テストショット1`・`テストショット2`）・`:1413`（`二重`）の6行で、**いずれも曖昧ではなく本文の是正は不要**。抽出条件に助数詞 `加算`・`重` と「語順が逆の序数」を追加した。**表に追加した理由**（抽出条件の但し書きにとどめなかった理由）は §1-2 に明記した | §1-2 #30〜#35、ゲート7 |
| **G-5** | `note` | (1) §2-3 の条件(4) が `:436`・`:331` を「既存パターン」としているのは言い過ぎ、(2) §3-3 の `src_file` が `mapping.csv` の実値と違う | **是正した。** (1) `:331`・`:436` を実測して `（\ :ref:`…`\ 参照）` 形式であることを確認し、削除した括弧書きとは形が異なる旨に書き直した（括弧書き自体は G-1 で削除済み）。(2) `src_file` を `mapping.csv` 上の実値（`.rn/20260724-ntf-yaml-support/input/…`）に直した | §2-3 条件(4)、§3-3 |
| **G-6** | `should` | 見出しの約束と導入文の齟齬、および書き方ページ側の前方参照が無条件のまま残っている件を申し送る | **申し送りに追加した。** `reviews/page-testdata_examples.md` 申し送り**22**。`design.md:47`・`:56` と `testdata_notation.rst:1342`・`:1393`・`:1443` を実物で読んで引用を照合済み（**いずれも読むだけ。`design.md` は変更禁止、`testdata_notation.rst` は本項では変更しない**）。既存の申し送り18（6種の欠落）とは重複させず、そこから参照する形にした | `reviews/page-testdata_examples.md` 申し送り22 |
| **G-7** | `note` | `reviews/page-testdata_examples.md` の `#10b` の表に、撤回済みの初回文面と「節内の `:ref:` は2件」という記述が残っている | **是正した。** 既存の行は書き換えず、**表の直後に上書き注記を1行追記**した（本書 §2-2 と同様の形）。撤回済みであること、最終的な文面と `:ref:` の件数は後述の小節を参照すべきことを明示した | `reviews/page-testdata_examples.md`（`#10b` 表の直後） |

**本文（`ja/` 配下）の変更は G-1 の1行だけである。** `testdata_notation.rst` は触っていない（4観点全員が STEP 4 の是正を正しいと判定したため）。`testdata_examples.rst:401` にも触っていない（`認証エラーケース` の件はユーザー判断待ち）。

### 5c-1. 是正ラウンド2 で追記した記録・申し送り

いずれも `reviews/page-testdata_examples.md` への追記のみであり、**既存の行は1行も書き換えていない**。

| 追記先 | 内容 |
|---|---|
| `reviews/page-testdata_examples.md`（`#10b` 表の直後） | **G-7**。撤回済みの初回文面と `:ref:` 件数についての上書き注記1行 |
| `reviews/page-testdata_examples.md` 末尾 | **G-1〜G-5**。`### #10b 是正ラウンド2（2026-08-07）` を追加し、本文の是正内容（是正前・是正後の文面、削除した理由3点、語の是正、確定させた節の範囲、反証の試み、ゲート5）と記録の是正（G-2〜G-5）を表で記載した |
| `reviews/page-testdata_examples.md` 申し送り**22** | **G-6**。見出しの約束と導入文の齟齬、書き方ページ側の無条件の前方参照3箇所、行き止まりになる導線、`#11` 以降での3つの選択肢。申し送り18 から参照する形 |

**`page-testdata_notation.md` には追記していない。** 是正ラウンド2 の是正はすべて `testdata_examples.rst` とその記録に閉じており、`testdata_notation.rst` には触れていないためである（G-6 が言及する `:1342`・`:1393`・`:1443` は記載例ページ側の導入文との齟齬として立ち上がった問題であり、記載例ページの申し送りに置くのが自然である）。

---

## 6. ゲートの実行結果（**ゲート7 を先頭に置く**）

作業指示 STEP 5-3 の申し送り（`#10a` で全件表の作成漏れが3ラウンド連続で発生した）に従い、全件表を求めるゲート7 を実行順の先頭に置いた。

### ゲート7（先頭）— STEP 1 の数詞の全件確認表、STEP 3 の突合表が、いずれも**全件**記録されていること

**PASS**

**是正ラウンド2（G-2・G-3・G-4）で再実行し、出力を差し替えた。**

- STEP 1 の数詞の全件確認表 → **§1-2**。`testdata_examples.rst` の地の文**＋散文ディレクティブ本体**から数詞を機械抽出した**35行を全件**掲載（是正ラウンド1 の29行に、G-4 の6行を追加）。件数だけの報告はしていない。**抽出条件（母集団に含めるもの／除外するもの／助数詞リスト／数詞部の定義／語順が逆の序数）を表の冒頭に明記した**
- STEP 3 の突合表 → **§3-2**。`testdata_notation.rst` の Excel形式・YAML形式の両表から抽出した「値の種類」**12行を全件**掲載（作業指示の10種＋`通常の文字列`＋`${systemTime}` 系）。各行に実測 `grep` の結果と判定を付した。**判定基準を表の冒頭に明記**したうえで、**G-2 で「専用の例なし」6件の出典の有無を1件ずつ判定しなおした表（§3-3）**、**G-3 で空セルの除外条件を全種類分類した表（§3-2a）**を追加した

**是正ラウンド2 の抽出スクリプト `scratchpad/num3.py` は、記録を参照せず抽出条件から独立に組み直したものである。** 是正ラウンド1 までの条件だけで走らせると29行が得られ、**是正ラウンド1 の記録と行番号まで完全に一致する**（回帰の確認）。

```
$ python3 scratchpad/num3.py
--- 現行の抽出条件 : 29 行
--- 助数詞に 加算・重 を追加 : 31 行
現行のみ: []
追加分  : [99, 1413]
   L99   body          ['1加算']
   L1413 DIRBODY(tip)  ['二重']
--- 語順が逆の序数（テストショット＋数字）
   L245 / L264 / L283 / L306   （他の名詞では0件）
現行条件のヒット行: [14, 18, 22, 198, 397, 401, 436, 452, 502, 506, 580, 784, 792, 840, 957,
                     1209, 1213, 1262, 1358, 1417, 1450, 1471, 1565, 1588, 1922, 1926, 1942,
                     1946, 2070]     ← 是正ラウンド1 の記録と完全一致（29行）

=> 是正ラウンド2 の母集団 = 29 + 2 + 4 = 35 行
```

```
$ python3 scratchpad/g3b.py          # G-3: 空セルの全件分類
list-table 総数 = 49  識別子行を持つ表 = 47  説明用の表 = 2
### A データブロック識別子行の埋めセル                       該当行=47  空セル=126
### B データ行の先頭ラベル空セル                             該当行=42  空セル=42
### C ディレクティブ行・フレームワーク制御ヘッダ行の余白セル  該当行=10  空セル=18
### D コメント行の余白セル                                   該当行=2   空セル=8
### E データ行の空セル（値としての空文字）                    該当行=11  空セル=16
### X 説明用 list-table の空セル                              該当行=0   空セル=0
空セル総数 = 210
E の全件 = [61, 62, 544, 710, 712, 1242, 1633, 1635, 1637, 1664, 1667, 1668, 1783, 1787, 2082, 2083]
                                             ← 是正ラウンド1 の16箇所と完全一致
$ python3 scratchpad/kinds.py
--- Excel形式の場合 値の種類表: 13 rows   （ヘッダ1＋データ12）
--- YAML形式の場合 値の種類表: 13 rows    （ヘッダ1＋データ12）
```

**F-3（#22 の行番号の訂正）の実測**:

```
$ grep -n "10001\|20001\|20002" testdata_examples.rst | sed -n '13,15p'
1498:    - 10001      ← グループIDなしのデータブロックのデータ行（1件）
1525:    - 20001      ← case2 のデータ行1件目
1529:    - 20002      ← case2 のデータ行2件目（計2件）

$ sed -n '1489p;1521p;1527p' testdata_examples.rst    # 旧記録が引いていた行
  * -            （データ行の先頭マーカー。値ではない）
    - 5          （フィールド長の行）
    - morning    （20001 の行の MESSAGE セル）
```

**F-4（`空文字` の出現箇所）の実測**: §3-2 #4 に全件（Excel のデータ行の空セル16箇所・YAML の `""` 12行）を列挙した。初回の表が落としていた `:2082`・`:2083`（`空エントリを記述する` 節）を含む。**是正ラウンド2（G-3）で、この16箇所を得るために除外している空セルの全種類（A〜D、194箇所）を §3-2a に明記した。16箇所という結論は変わらない。**

### ゲート1 — `verify_mapping.py` が `exit 0`、594行 / 12,986 / 11,983 が不変

**PASS**

```
$ python3 .rn/20260724-ntf-yaml-support/mapping/tools/verify_mapping.py
Loaded 594 rows from mapping.csv
pending zero assignments: 0 (awaiting #6 decision)
lines total (all rows): 12986
lines total (excluding DROP): 11983
...
OK: no errors
EXIT=0
```

行数594は `csv.DictReader` によるカウントである（`wc -l` は使っていない。`steering.md` Rules）。

### ゲート2 — 禁止対象の差分が空

**PASS**

```
$ git diff 699dfd0 HEAD -- .rn/20260724-ntf-yaml-support/mapping/ ja/conf.py .rn/20260724-ntf-yaml-support/design.md
（出力なし）
$ git diff 699dfd0 -- .rn/20260724-ntf-yaml-support/mapping/ ja/conf.py .rn/20260724-ntf-yaml-support/design.md
（出力なし。作業ツリーの未コミット変更を含めても空）
```

`mapping/` 配下には `mapping.csv`・`_batch/`・`vocabulary.md`・`style.md`・`glossary.md` がすべて含まれる。作業ツリー側も `git status --porcelain` が `testdata_examples.rst`・`testdata_notation.rst` の2件のみを報告する。

### ゲート3 — 見出し数が不変。見出しの文言と並び順も不変

**PASS**

**是正ラウンド1 で再実行（出力は不変）。**

```
$ python3 scratchpad/heads.py
testdata_notation.rst
  now: {'=': 1, '-': 3, '~': 10, '^': 26}  base699dfd0: {'=': 1, '-': 3, '~': 10, '^': 26}
  順序・文言 一致: True  件数: 40 40
testdata_examples.rst
  now: {'=': 1, '-': 9, '~': 28, '^': 56}  base699dfd0: {'=': 1, '-': 9, '~': 28, '^': 56}
  順序・文言 一致: True  件数: 94 94
```

見出しリスト（記号と文言の並び）を `699dfd0` 版と要素単位で比較し、**完全一致**（`==` が `True`）であることを確認した。指定値（notation `-`3 / `~`10 / `^`26、examples L2 9 / L3 28 / L4 56）とも一致する。

### ゲート4 — `testing_framework/` 配下の全 `.rst` に `テストケース` が0件

**PASS**

```
$ grep -rn "テストケース" ja/development_tools/testing_framework/ --include=*.rst | wc -l
0
```

（`#10a` と同じく、`en/` 配下1件・`ja/biz_samples/11/index.rst` 2件は射程外。`page-testdata_notation.md` 申し送り23 の追記で `#last` の棚卸し対象として追跡済み。）

### ゲート5 — 記載例ページの特殊記法セクションに `:ref:` が1件以上あり、`testdata_notation-special_notation` を指していること

**PASS**

**是正ラウンド2（G-1）で再実行し、出力を差し替えた。** 節の範囲は**ラベル行を境界として機械判定**した（是正ラウンド1 は次の L2 見出しの直前を終端としていたため `:1995` としていたが、`:1994`・`:1995` は次節のラベル行とその直後の空行であり、この節には属さない。**正しくは `:1847`〜`:1993`**）。

```
$ python3 scratchpad/g1.py
GATE5: 節の範囲 = L1847 .. L1993 (見出し行 L1849, 下線 L1850, 次節ラベル L1994)
  L1851: target=testdata_notation-special_notation       text=null・空文字・改行など特殊な値を記述する
  L1859: target=testdata_notation-special_notation       text=null・空文字・改行など特殊な値を記述する
  :ref: 件数 = 2
  testdata_notation-special_notation 参照 = 2 → 1件以上:  True
```

**是正ラウンド1 で `:1851` に増えていた `testdata_notation`（ページ先頭ラベル）への `:ref:` は、G-1 で括弧書きごと削除した。** 節内の `:ref:` は2件に戻り、**2件とも `testdata_notation-special_notation` を指す**。

`:1851` の現在の全文:

```
特殊記法を使う値の記述例を示す。記法の仕様は\ :ref:`null・空文字・改行など特殊な値を記述する <testdata_notation-special_notation>`\ を参照。ただし、見出しに挙げた値のうち、空文字・改行の記述例はここに置いていない。
```

参照先ラベルの実在も確認済み。

```
$ grep -rn "^\.\. _testdata_notation:\|^\.\. _testdata_notation-special_notation:" ja/
ja/development_tools/testing_framework/implementation/testdata_notation.rst:1:.. _testdata_notation:
ja/development_tools/testing_framework/implementation/testdata_notation.rst:1273:.. _testdata_notation-special_notation:
```

**G-1 の主張の反証を試みた結果**（節の範囲 L1847〜L1993 に空文字・改行の記述例が無いこと）:

```
$ python3 scratchpad/g1.py
=== 1) 空文字リテラル "" ===        → 3行ヒットするが、いずれも「ダブルクォート1文字」の記法であって空文字ではない
   1940 '    - ``"""``'
   1946 '半角スペース1文字は ``" "``\\ 、ダブルクォート1文字は ``"\\""``\\ または ``\'"\'``\\ と記述する。'
   1955 '          MEMO: "\\""'
   count = 3
=== 2) 改行文字 \r \n ===            → 0件
   count = 0
=== 3) list-table の空セル（- のみの行）=== → 6行。いずれもデータブロック識別子行の埋めセルであり、データ行の値ではない
   1870 / 1871 / 1872   （:1869 * - EXPECTED_TABLE=SCHEDULE の続き）
   1933 / 1934          （:1932 * - EXPECTED_TABLE=ITEM の続き）
   1970                 （:1969 * - SETUP_TABLE=FILE_TABLE の続き）
   count = 6
=== 4) YAML の空値（key: の後が空）=== → 6行すべて expected_tables: / rows: / setup_tables: というマッピングのキーであり値の空ではない
   1900 / 1902 / 1950 / 1952 / 1986 / 1988
   count = 6
=== 5) 「改行」という語 ===          → 3行（:1849 見出し／:1851 導入文／:1859 important のリンクテキスト）。記述例は0件
=== 6) 「空文字」という語 ===        → 3行（同上）。記述例は0件
```

**反例は1件も見つからなかった。**（分類の根拠は §3-2a の A〜E。この節の空セル6件はすべて分類 A に当たり、分類 E〈値としての空文字〉は `:1847`〜`:1993` の範囲に1件も無い。）

### ゲート6 — 両ページに `（番号・説明・期待するステータスコード）` が0件

**PASS**

```
$ grep -c "（番号・説明・期待するステータスコード）" \
    ja/development_tools/testing_framework/implementation/testdata_examples.rst \
    ja/development_tools/testing_framework/implementation/testdata_notation.rst
ja/.../testdata_examples.rst:0
ja/.../testdata_notation.rst:0
```

### ゲート8 — `:ref:` の未定義参照が0件、段落内改行が0件

**PASS**

**是正ラウンド2（G-1）で再実行し、出力を差し替えた。**

`:ref:` の解決: 両ページの全 `:ref:` からターゲットラベルを取り出し、`ja/**/*.rst` から収集した `.. _ラベル:` の集合と突合した。

```
両ページの :ref: 総数 = 52          ← G-1 で :1851 の1件を削除したため、是正ラウンド1 の53件から1件減
未定義 :ref: = 0 []
```

段落内改行: 両ページの本文行を分類（blank / indented〈表セル・コードブロック〉/ directive / 見出し・下線 / 箇条書き / 本文）し、**本文行が空行を挟まず2行以上連続する箇所**を数えた。

```
testdata_examples.rst 段落内改行: 0 []
testdata_notation.rst  段落内改行: 0 []
TOTAL = 0
```

（初回の判定では notation で6件を報告したが、内訳はすべて `- ` で始まる箇条書きであった。箇条書きを本文行から除外して再判定し0件になることを確認している。）

未定義参照はゲート9 のビルド警告でも裏付けている（NTF解説書の2ページに起因する `undefined label` は0件）。

### ゲート9 — Docker でフルビルド（`-a`）し、`build succeeded` かつ警告が既知の `db_double_submit.rst` 1件のみ（新規0件）

**PASS**

```
$ docker run --rm -v /home/tie303177/work/lovaizu/nablarch-document:/root/document \
    nablarch-document-build /bin/bash -c \
    "cd /root/document; sphinx-build -a -d _build/.doctrees/ja -b html ja _build/html"
...
build succeeded, 1 warning.
EXIT=0
```

警告の全件（`grep -n "WARNING\|ERROR"`、**1件**）:

```
313:/root/document/ja/application_framework/application_framework/libraries/db_double_submit.rst:108: WARNING:
    undefined label: how_to_set_token_in_request_unit_test (if the link has no caption the label must precede a section header)
$ grep -c "WARNING\|ERROR" build-r2.log
1
```

**是正ラウンド2（G-1 の本文変更後）に再実行した出力である。** 既知の1件のみで**新規0件**。ローカル venv（`/home/tie303177/venv`）は使っていない。

ビルドで書き換わる `locales/ja/LC_MESSAGES/sphinx.mo` は `git checkout -- locales/ja/LC_MESSAGES/sphinx.mo` で復元し、**`git status --porcelain` の実出力で消えたことを確認した**（初回は復元したと記録しながら実測では残っていた）。

```
$ git status --porcelain            # 復元前
 M .rn/20260724-ntf-yaml-support/reviews/page-testdata_examples.md
 M ja/development_tools/testing_framework/implementation/testdata_examples.rst
 M locales/ja/LC_MESSAGES/sphinx.mo
?? .rn/20260724-ntf-yaml-support/checks/task-10a-followup.md

$ git checkout -- locales/ja/LC_MESSAGES/sphinx.mo
$ git status --porcelain            # 復元後（sphinx.mo が消えている）
 M .rn/20260724-ntf-yaml-support/reviews/page-testdata_examples.md
 M ja/development_tools/testing_framework/implementation/testdata_examples.rst
?? .rn/20260724-ntf-yaml-support/checks/task-10a-followup.md
```

（是正ラウンド2 では `page-testdata_notation.md` に追記していないため、是正ラウンド1 の出力にあった同ファイルの ` M` 行が無い。§5c-1 に理由を記した。）

**`_build/` は削除していない**（ユーザーが HTML を直接ブラウザでレビューするため。`_build/` はコミットしない）。

### ゲート10 — `699dfd0` との差分で、削除された段落・表の行・コードブロックが STEP 1・2・4 に由来するものだけであること

**PASS**

**是正ラウンド2 で再実行。G-1 も F-1 と同じく STEP 2 の `:1851` の1行を書き直したものであるため、ハンクの数・位置は変わっていない。**

```
$ git diff --numstat 699dfd0 -- ja/
2	2	ja/development_tools/testing_framework/implementation/testdata_examples.rst
1	1	ja/development_tools/testing_framework/implementation/testdata_notation.rst
$ git diff 699dfd0 -- ja/ | grep -E '^@@'
@@ -398,7 +398,7 @@
@@ -1848,7 +1848,7 @@
@@ -105,7 +105,7 @@
```

差分は**3行の1対1置換のみ**であり、追加行数と削除行数が各ファイルで一致する。ハンクは次の3つで、いずれも STEP 1・2・4 に対応する。

| ハンク | 対応 |
|---|---|
| `examples @@ -398,7 +398,7 @@` — `2件` → `2つのテストショット` | STEP 1 |
| `examples @@ -1848,7 +1848,7 @@` — 導入文に1文追加（**是正ラウンド1 F-1・是正ラウンド2 G-1 で、その1文を書き直した**） | STEP 2 |
| `notation @@ -105,7 +105,7 @@` — 定義セルの括弧書き削除 | STEP 4 |

削除行の全件（`git diff 699dfd0 -- ja/ | grep -E '^-[^-]'`、**3行**）:

```
-正常ケースと認証エラーケースの2件を実行する例である。``context``\ カラムには、リクエスト\ ID\ ・実行ユーザ・\ HTTP\ メソッドを記述した ``LIST_MAP``\ の\ ID\ を指定する。\ Excel\ 形式・\ YAML\ 形式のそれぞれについて示す。
-特殊記法を使う値の記述例を示す。記法の仕様は\ :ref:`null・空文字・改行など特殊な値を記述する <testdata_notation-special_notation>`\ を参照。
-    - リクエスト送信・バッチ起動などの1回の実行を表すエントリ（番号・説明・期待するステータスコード）
```

**独立して削除された段落・表の行（`* - ` 行）・コードブロックは0件である**（削除行3行はすべて対応する追加行を持つ置換であり、削除された行はいずれも `* - ` 行・`.. code-block::` 行・空行ではない。3行目は `list-table` のセル行だが `- ` で始まる継続セルであって行そのものは残っており、括弧書きだけが消えている）。

### ゲート一覧（サマリ）

**是正ラウンド2（2026-08-07）で、ゲート1〜10 を全件再実行した。**（G-1 が本文を変更したため、ゲート5・8・9・10 と、全件表を扱うゲート7 は必須。残りも同時に流している。）**結果はすべて PASS で変わらない。**

| 実行順 | ゲート | 内容 | 結果 |
|---|---|---|---|
| 1 | **7** | 全件表2つが全件記録されていること | **PASS**（是正ラウンド2 で再実行。数詞の全件表 **29行→35行**〈G-4〉・突合表 12行＋出典判定表6行〈G-2〉＋空セル分類表6行〈G-3〉。**全表に条件／基準を明記**） |
| 2 | 1 | `verify_mapping.py` exit 0 / 594 / 12,986 / 11,983 | **PASS**（是正ラウンド2 で再実行。`Loaded 594 rows` / `12986` / `11983` / `OK: no errors` / `EXIT=0`） |
| 3 | 2 | 禁止対象の差分が空 | **PASS**（是正ラウンド2 で再実行。`HEAD` 比較・作業ツリー込みとも出力なし） |
| 4 | 3 | 見出し数・文言・並び順が不変 | **PASS**（是正ラウンド2 で再実行。notation 40件・examples 94件とも `699dfd0` と要素単位で完全一致） |
| 5 | 4 | `テストケース` 0件 | **PASS**（是正ラウンド2 で再実行。0） |
| 6 | 5 | 特殊記法セクションの `:ref:` | **PASS**（是正ラウンド2 で再実行。節の範囲を `:1847`〜`:1993` に確定。`:ref:` **2件／2件とも `special_notation` を指す**〈G-1 でページ先頭ラベルへの1件を削除〉） |
| 7 | 6 | 括弧書き0件 | **PASS**（是正ラウンド2 で再実行。両ページとも0） |
| 8 | 8 | 未定義 `:ref:` 0件・段落内改行0件 | **PASS**（是正ラウンド2 で再実行。両ページの `:ref:` 総数は53→**52**、未定義0件、段落内改行0件） |
| 9 | 9 | Docker フルビルド `build succeeded, 1 warning` | **PASS**（是正ラウンド2 で再実行。`EXIT=0`／警告は `db_double_submit.rst:108` の1件のみで新規0件／`sphinx.mo` 復元を `git status --porcelain` で実測確認／`_build/` は残している） |
| 10 | 10 | 削除が STEP 1・2・4 由来のみ | **PASS**（是正ラウンド2 で再実行。`--numstat` 2/2・1/1、ハンク3つのまま。削除行3行を全件掲載） |

**NG は0件。**

---

## 7. 禁止事項の遵守

| 禁止事項 | 遵守 | 根拠 |
|---|---|---|
| `mapping.csv` / `_batch/` / `vocabulary.md` / `style.md` / `glossary.md` / `design.md` / `ja/conf.py` を変更しない | OK | ゲート2（`git diff` が空。作業ツリー込みでも空） |
| 見出しの文言・並び順を変更しない | OK | ゲート3（両ページとも `699dfd0` 版と要素単位で完全一致） |
| STEP 1 で `正常ケース`・`認証エラーケース` を落とさない | OK | §1-1。是正後の文にも両語が残る。セル値 `:424`・`:478` は未変更 |
| STEP 1 で他の導入文を変更しない | OK | ゲート10（`examples` の変更は `:401` と `:1851` の2行のみ）。§1-3 の是正候補1件は報告のみ |
| STEP 3 で例を追加しない | OK | ゲート10（追加行は `:1851` の置換1行のみで、表・コードブロックの追加は0件） |
| STEP 4 で括弧書きを言い換えて残さない | OK | §4。削除のみ。主文は未変更 |
| 承認済みの `#8`・`#9`・`#10`・`#10a` の成果を、指示外の範囲で変更しない | OK | ゲート10（差分3行のみ）。`about/index.rst` は未変更。**是正ラウンド1・2 でも、`testdata_notation.rst`（STEP 4 の是正は4観点全員が正しいと判定。G-6 が言及する `:1342`・`:1393`・`:1443` も読むだけで変更していない）と `testdata_examples.rst:401`（別途ユーザー判断待ち）には触れていない。F-5-2 のインラインリテラルの区切りも `#10` 由来のため変更していない。`design.md` は G-6 の照合のため読んだだけで変更していない（ゲート2）** |
| 判定を件数だけで報告しない | OK | §1-2（**35行全件**・抽出条件を明記）・§3-2（12行全件・**判定基準を明記**）・§3-2a（空セル**210箇所を5分類で全件**）・§3-3（6種それぞれの出典の有無を**全件**） |
| 既存のレビュー記録を書き換えない | OK | `reviews/` の2ファイルはいずれも追記のみ。**是正ラウンド2 の追記は `page-testdata_examples.md` の2箇所**（末尾に `### #10b 是正ラウンド2` と申し送り22、および `#10b` の表の直後に G-7 の上書き注記1行）で、**既存の行は1行も書き換えていない**（`git diff` の削除行が0行であることで確認） |
| `#11` に着手しない | OK | 新規ページの作成は行っていない |

---

## QA Expert Review

4観点はすべて**別のサブエージェント**で、Rules の3点（実測で裏付ける／付属の検証スクリプトを正解として使わず独立に組む／敵対的に見る）をプロンプトに含めて実施した。**巡回は2巡**（初回 `b2f616a` に対する1巡目、是正ラウンド2 `4c16caa` に対する2巡目。2巡目は1巡目で fail を出した設計・クラフトの2観点）。

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 検証のやり方が目的に対して意味を持っているか（「実行した」「通った」で済ませていないか） | OK | 1巡目 QA は `verify_mapping.py` を使わず `csv.DictReader` で 594 / 12,986 / 11,983 を独立に再計算し、見出し不変を件数ではなく `(記号, 文言)` の配列比較で確認、Docker フルビルドも自分で再実行した。ゲート1〜6・8〜10 を独立に再現 |
| 全件表が本当に全件か | OK（是正後） | 1巡目 QA が母集合の漏れ3件（`:1565`・`:1262`・`:1213`）を検出 → 是正ラウンド1 で29行に。2巡目に向けた再検証で助数詞リスト外の6行を追加し35行に。設計の独立走査（助数詞ホワイトリストを使わない全文走査）で「表から漏れた真の数詞は0件」を確認 |
| STEP 3 の突合の網羅性・判定の正しさ | OK | 4名全員が母集団（Excel／YAML 両表の12種）を独立抽出して集合一致を確認。`must`（出典があるのに落ちている）0件という結論は4名とも支持 |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | 1巡目 NG → 2巡目 OK | 1巡目 `must` F-a: 是正ラウンド1 が追加文の `:ref:` をページ先頭ラベル `testdata_notation` に格下げし、直前の文が指す節ラベルを粗い宛先で上書きしていた。是正ラウンド2〈G-1〉で括弧書きごと削除して解消。2巡目で「節内の `:ref:` は2件とも `testdata_notation-special_notation`、ページ先頭ラベルの残存0件」を実測確認 |
| System-wide integrity（見出し・相互参照・cross-doc 整合） | OK | 見出しは両ページとも `699dfd0` と `IDENTICAL(text+order)`。未解決参照0件。STEP 4 で削除した3カラムの実体が `使用方法` 側 `:389`・`:391`・`:393` に実在し、`:381` の限定と `:563` の別体系の記述で正しく囲われていることを確認。情報の欠落なし |
| `glossary.md` の標準との整合 | 食い違いあり・記録済み | `glossary.md:201` は「3つの用途を対比する表では `実行条件` 側の言い方を使う」と定めるが現行セルはそうなっていない。用語集は変更禁止・STEP 4 は「言い換えて残さない」と命じているため本タスクで直せる余地はなく、申し送り30 に記録 |
| 未解決（2巡目 `should`） | — | (1) 是正ラウンド2 が限定句「単独で示した」と明示スコープ「この節には」を同時に外し、スコープ標識が「ここ」1語になった。(2) 申し送り22（往復の行き止まり）が `page-testdata_examples.md` にしかなく、直す対象の `testdata_notation.rst:1342`・`:1393`・`:1443` 側の申し送りに無い |

### Craft Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice（日本語・RST） | 1巡目 NG → 2巡目 **NG（未解決）** | 1巡目 `must` M-1（設計 F-a と同一。是正ラウンド2 で解消）。**2巡目 `must`: `:1851` の `ここに置いていない` の指示範囲が節ともページとも読め、ページ読みでは同ページの `:710`-`:712`・`:732`-`:734`・`:2066`・`:2082`-`:2083` に反して偽になる。是正ラウンド2 が `この節には` → `ここに` と縮めた際の退行である。** 最小修正案は `` なお、空文字・改行については、この節に記述例を置いていない。 `` |
| Consistency with existing style | 2巡目 `should` 3件（未解決） | (1) `ただし、` の係り先が直前文でなく1文目。同一スロットの兄弟 `:1588` は `なお、`（NTF解説書で `ただし、` はこの1件のみ）。(2) `見出しに挙げた値のうち`・`置いていない` は `ja/` 全体で前例0件のメタ参照・言い切り。既存の言い方は `:397`「後述の…に示す」・`:676`「前述の…で扱っている」。(3) 段落内で `空文字・改行` が3回（見出し・リンクキャプション・追加文） |
| STEP 1 の書き換え / STEP 4 後の定義セル | OK | `:401` は `:198` と形が揃った。定義セル3行は名詞句止め・26/26/44字で粒度が揃い、循環定義なし。言い換えて残していない |

### Verification Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked（主張が出典に照合されているか） | 1巡目 NG → 是正後 OK | 1巡目で §1-2 #19 の引用行番号3件の誤りと §3-2 の空セル列挙の不完全を検出（F-3・F-4 で是正）。是正ラウンド1 後の再検証で、§1-2 の29行・§3-2 の約60箇所を全件引き直して不一致0件 |
| Coverage（ゲート・主張の網羅性） | OK | ゲート1〜10 を2度とも自分で再実行して全件再現。Docker フルビルドは `build succeeded, 1 warning.`／`EXIT=0`、警告は `db_double_submit.rst:108` の既知1件のみ・新規0件 |
| 未検証の断定が公開本文に無いか | 1巡目 `must` → 是正後 OK（ただしクラフト2巡目と割れる） | 1巡目の `must`（「このページに例が無い」）は解消。是正ラウンド1 の文面について節スコープでの反証を試みて反例0件。**是正ラウンド2 の文面はこの観点では未再評価であり、クラフト2巡目が同じ箇所に `must` を出している** |
| 記録の精度（`should`。是正ラウンド2 で処理済み） | OK | §3-3 の「出典が1件も無い」が反証され G-2 で書き直し、§3-2 の除外条件の未明記が G-3 で全種類明記、節範囲 `:1995` の誤りが G-1 で `:1993` に確定 |

## Overall Verdict

- Self-check: OK（**是正ラウンド2〈2026-08-07〉まで完了。** 4観点の再レビューで QA・検証は pass、設計・クラフトが fail となり `must` は1件に収束した。**G-1**〈`must`〉= `testdata_examples.rst:1851` の3文目から**括弧書きを削除**し、参照を2文目の `testdata_notation-special_notation` に一本化した。最終文面は `ただし、見出しに挙げた値のうち、空文字・改行の記述例はここに置いていない。`。`単独で示した` を `見出しに挙げた値のうち` に、`この節` を `ここ` に改めた。**節の範囲をラベル行基準で機械判定して `:1847`〜`:1993` に確定**させ〈是正ラウンド1 の `:1995` は次節のラベル行を含む誤り〉、この範囲に空文字の値・`\r`・`\n` が0件であることを実測して反例0件を確認した。**G-2** = §3-3 の「例が無い6種に該当する出典は65行の中に1件も無い」は誤りで、実測すると出典が皆無なのは `${文字種,文字数}` と改行文字の**2種のみ**、文字列の `null`・空文字・先頭ゼロ付き数値・`true`／`false` の4種には出典が実在し内容も本文に反映済みである〈`must` 0件という結論は不変〉。**G-3** = 全49件の `list-table` の空セル**210箇所**を全件分類し、除外の全4種類を §3-2a に明記〈16箇所という結論は不変〉。**G-4** = STEP 1 の全件表を**29行→35行**に拡張〈追加6行はいずれも曖昧でなく本文の是正不要〉。**G-5** = §2-3 条件(4) の「既存パターン」という記述と §3-3 の `src_file` 表記を実測に合わせた。**G-6** = `design.md:47`・`:56` と `testdata_notation.rst:1342`・`:1393`・`:1443` を実物で読んで照合のうえ `reviews/page-testdata_examples.md` 申し送り22 を追加〈両ファイルとも読むだけで変更していない〉。**G-7** = `#10b` の表の直後に撤回の上書き注記を1行追記。**本文の変更は G-1 の1行のみ**で、`testdata_notation.rst`・`:401` には触れていない。ゲート1〜10 を再実行して全件 PASS、Docker フルビルドは `build succeeded, 1 warning`／`EXIT=0` で新規警告0件、`sphinx.mo` の復元を `git status --porcelain` の実出力で確認、`_build/` は残している。以下は是正ラウンド1 の記録: 初回で STEP 1〜5 を実施しゲート1〜10 を PASS させたのち、4観点のレビューが全員一致で検出した `must` を含む F-1〜F-6 を是正した。**F-1**〈`must`〉= `testdata_examples.rst:1851` の3文目が「空文字にこのページの例が無い」と断定していた事実誤りを、`` ただし、空文字・改行を単独で示した記述例は、この節には置いていない（記法は\ :ref:`テストデータの書き方 <testdata_notation>`\ で説明している）。 `` に書き直し、節の範囲 L1847〜L1995 に反例が0件であることを `grep` で実測確認。**F-2** = 数詞の全件表の抽出条件を明記して散文ディレクティブ本体を母集団に加え、25行→**29行**に再抽出（追加4行はいずれも曖昧でなく本文の是正不要）。**F-3** = #22 の行番号引用3件を実測値〈`:1498`・`:1525`・`:1529`〉に訂正（判定は不変）。**F-4** = 突合表に判定基準を明記し、`空文字` の出現箇所を Excel 16箇所・YAML 12行で全件列挙〈初回が落としていた `:2082`・`:2083` を含む〉。**F-5** = 禁止事項により是正せず §1-3 に2件を記録。**F-6** = `glossary.md` を読んで照合のうえ `reviews/page-testdata_notation.md` 申し送り30 に記録〈用語集は変更していない〉。本文の変更は F-1 の1行のみで、`testdata_notation.rst`・`:401` には触れていない。ゲート1〜10 を再実行して全件 PASS、Docker フルビルドは `build succeeded, 1 warning`／`EXIT=0` で新規警告0件、`sphinx.mo` の復元を `git status --porcelain` の実出力で確認、`_build/` は残している）
- QA: OK（1巡目 pass。`must` 0件、`should` 5件はすべて是正ラウンド1・2 で処理済み）
- Design expert: OK（1巡目 fail → 2巡目 pass。`must` 0件。`should` 2件が未解決 — スコープ標識の後退、申し送り22 の片側掲載）
- Craft expert: **NG**（1巡目 fail → 2巡目 fail。**`must` 1件が未解決** — `:1851` の `ここに` の指示範囲が一意に定まらない）
- Verification expert: OK（1巡目 pass。`must` 0件。是正ラウンド2 の文面はこの観点では未再評価）
- Ready to check off: **No** — クラフト2巡目の `must` 1件が未処理。是正ラウンド3（規定の上限内、3回目）で `:1851` の3文目のスコープ標識を戻す必要がある。あわせて設計2巡目の `should` 2件を処理する。**加えて、`:401` の `認証エラーケース` について作業指示の禁止事項と実測の食い違いをユーザーにエスカレーション済みで、回答待ちである**（禁止事項は「表に実在するセルの値だから落とすな」と定めるが、実在するセル値は `:430`・`:484` の `認証エラー` であり `認証エラーケース` は地の文にしか無い）
