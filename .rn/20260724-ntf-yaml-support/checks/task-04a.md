# task-04a Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| 対象23セクションすべてに split の判定と rationale がある | OK | `mapping/sections-current.csv` から `lines >= 100` を抽出すると23件（current-0006, 0007, 0008, 0009, 0016, 0018, 0019, 0028, 0037, 0066, 0089, 0106, 0126, 0156, 0158, 0160, 0164, 0174, 0184, 0185, 0189, 0206, 0211）。`mapping/split-plan.md` の表は `grep -c '^| current-' split-plan.md` で23行、全行に `split`（yes/no）と `rationale` 列が埋まっていることを目視確認済み。全23件について、マージベースコミット `c24190607fef5d76c607aa08b36d2ab2f813efe5` の該当ファイルを `git show <commit>:<path> \| sed -n '<start>,<end>p'` で実際に読み、本文の内容に基づいて判定した（見出しパスのみからの推測はしていない）。 |
| split=yes の全件について、parts の行範囲の和集合が元セクションの body_start_line〜body_end_line と一致する（隙間・重複ゼロ） | OK | split=yes は6件（current-0037, current-0066, current-0106, current-0156, current-0184, current-0185）。マージベースコミット `c24190607fef5d76c607aa08b36d2ab2f813efe5` の実ファイルを行番号付きで読み、見出し・空行段落境界・RSTディレクティブの完全な塊のみを境界に採用（文中・表中・ディレクティブ内部を切らない）。Pythonで6件とも検証: current-0037は body(170-446, 277行)に対しparts(170-262, 263-316, 317-446)の和集合277行で一致（旧案の275/276境界は`.. tip::`ディレクティブ1つの内部を分断していたため263/316の完全なディレクティブ境界に修正）。current-0066は body(44-159, 116行)にparts(44-119, 120-129, 130-159)で116行一致（変更なし）。current-0106は body(99-208, 110行)にparts(99-166, 167-177, 178-208)で110行一致（変更なし）。current-0156は body(67-220, 154行)にparts(67-172, 173-198, 199-220)の和集合154行で一致（記法部分が挙動説明を挟むサンドイッチ型の3分割）。current-0184は body(55-165, 111行)にparts(55-100, 101-165)の和集合111行で一致。current-0185は body(168-268, 101行)にparts(168-220, 221-268)の和集合101行で一致。6件とも `expected_total == covered` かつ各partsの開始/終了行が連続（隙間なし）で、先頭partの開始行がbody_start_line、末尾partの終了行がbody_end_lineと一致することをスクリプトで確認済み（`sum==expected and bounds_ok and contig` を6件ともTrueで出力）。 |
| parts の割当先が design.md の章構成に存在するページ・セクションである | OK | 6件のsplit=yesのpartsの割当先は以下の通りで、いずれも design.md に存在する章立てに対応する。(1) 第3部 テストの実装方法 > テストデータの書き方（design.md §4「テストデータの書き方 1ページ」。current-0037/0066/0106/0156/0184/0185のいずれも記法部分をこのページへ割り当てており、既にsplit=noで同ページへ丸ごと割り当て済みのcurrent-0126・current-0189と同種の内容であることをrationale欄で相互参照した）。(2) 第2部 導入と設定 > リクエスト単体テストの設定（Nablarchバッチアプリケーション／HTTPメッセージング／MOMによるメッセージング）（design.md §3「リクエスト単体テストの設定　処理方式ごと」および §5 の処理方式名称表に準拠、current-0037/0066/0106のコンポーネント設定・properties設定部分）。(3) 第3部 テストの実装方法 > 取引単体テスト（MOMによるメッセージング）／クラス単体テスト＞コンポーネント単体テストの各「使用方法」ページ（current-0156の再読み込み挙動、current-0184/0185の手順・シーケンス図・テストソースコード実装例。いずれも元の宛先を維持）。split=noの17件についても、宛先は第1部（全体像）、第2部（各処理方式の設定ページ、コンポーネント設定項目一覧）、第3部（クラス単体テスト＞エンティティ/コンポーネント単体テスト、リクエスト単体テスト各処理方式、取引単体テスト、テストデータの書き方）のいずれかに対応させており、design.md の§2〜§5に列挙された部・ページ・処理方式名の外に出ていない。current-0158のみ、第2部の暫定ページ一覧（design.md §3）に「取引単体テストの設定」という名称のページが明示されていないため、rationale欄で「割当先ページは未確定（§10未確定事項#1に依存）」と明記し、Part（第2部）レベルでは確定・ページレベルは今後のマッピング作成時に決定する旨を記載した。 |

## Expert Reviews（本タスク専用の2観点。QA/設計/クラフト/検証の4観点は実施しない）

### 割当先の妥当性（design.md の章構成に照らした宛先判定）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 23行全件の宛先が design.md の章構成・記載範囲ルールに合致する | OK（1ラウンド指摘→修正後 OK） | ラウンド1: current-0156/0184/0185がテストデータ記法（current-0126/0189と同種）をfeature固有ページに丸ごと残しており、design.md §4のテストデータの書き方への集約ルールに反すると指摘（NG）。current-0158の第2部レベル割当は健全と確認。修正（`ac533b5`）で3件をsplit=yesに変更し記法部分をテストデータの書き方へ移動。再レビューで4行（current-0037/0156/0184/0185）を実測突合し、宛先はすべてdesign.mdの章立てに存在し内容とも整合すると確認（OK）。 |

### 分割位置の妥当性（分割する場合の境界、しない場合の一体性）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| split=yesの境界が実内容の切れ目（見出し・空行段落・ディレクティブの完全な塊）にあり、隙間・重複ゼロ | OK（1ラウンド指摘→修正後 OK） | ラウンド1: current-0037の旧境界(275/276)が`.. tip::`ディレクティブ1つ（263-316行）の内部（例示と設定指示の間）を分断していると指摘（NG）。他22行は妥当と確認。修正（`ac533b5`）で境界を262/263に移動しディレクティブを1パートに統合。再レビューで4行（current-0037/0156/0184/0185）を実測突合し、全境界が見出し・空行・ディレクティブ終端等の実際の切れ目にあり、表・コードブロック・ディレクティブの途中を切っていないこと、6件全ての和集合が隙間・重複ゼロであることを確認（OK）。 |

## Overall Verdict

- Self-check: OK
- 割当先の妥当性: OK（修正後）
- 分割位置の妥当性: OK（修正後）
- Ready to check off: Yes
